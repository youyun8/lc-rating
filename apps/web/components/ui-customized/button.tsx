// Unified with the canonical button component. Kept as a re-export so existing
// "ui-customized/button" import paths continue to resolve to the same design.
export { Button } from "@/components/ui/button";
