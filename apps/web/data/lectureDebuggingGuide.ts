// 測試與除錯方法論，顯示於講義總覽頁。
// 針對 LeetCode 常見的 WA / TLE / RE 來源，提供自我檢查清單與手動模擬建議。

export const lectureDebuggingGuide = [
  "下列檢查清單能攔下 LeetCode 上大多數的 WA（答案錯）、TLE（逾時）與 RE（執行期錯誤）。",
  "### 一、動手前先想複雜度與邊界",
  [
    "- 由資料範圍反推可接受的複雜度（見「通用解題心法」）。",
    "- 列出邊界輸入：空陣列、單一元素、全部相同、最大/最小值、負數、重複值。",
  ].join("\n"),
  "### 二、常見的 WA 來源",
  [
    "- 整數溢位：加總、乘法、距離可能超過 `int` 時用 `long long`；無限大設成不會在加法後溢位的值（如 `4e18`）。",
    "- 二分中點：用 `mid = left + (right - left) / 2`，避免 `left + right` 溢位。",
    "- 排序比較器：必須是嚴格弱序，`<` 不可寫成 `<=`，否則行為未定義甚至崩潰。",
    "- 邊界與開閉區間：半開區間 `[l, r)` 較不易出錯；相等元素的歸屬（取嚴格或非嚴格）要前後一致。",
    "- 初始化：最大化用很小的值、最小化用很大的值、計數型 `dp[0] = 1`。",
  ].join("\n"),
  "### 三、常見的 TLE / RE 來源",
  [
    "- 遞迴過深：DFS 在 `n` 很大時可能爆 stack，改用顯式堆疊或迭代。",
    "- 重複計算：同一子問題重算，補記憶化或改遞推。",
    "- 容器誤用：在迴圈內反覆建立大型容器，或對 `vector` 做頻繁的頭部刪除。",
  ].join("\n"),
  "### 四、手動模擬（dry run）",
  [
    "- 取一個 3 到 5 元素的小輸入，用紙筆走一遍狀態變化，對照程式輸出。",
    "- 對拍：寫一個暴力解與隨機資料產生器，比對兩者輸出，快速定位反例。",
  ].join("\n"),
].join("\n\n");
