// 跨章節的通用解題心法，顯示於講義總覽頁底部。
// 內容為與單一演算法無關的共通判斷準則，適合讀完各主題後回頭對照。

export const lectureMetaSummary = [
  "貫穿各章節的共通判斷準則，與單一演算法無關，適合在讀完各主題後回頭對照。",
  "### 一、先估複雜度，再選方法",
  "從輸入規模反推可接受的複雜度，再回頭限定可用的 pattern。常見對應如下：",
  [
    "- `n <= 12` 容許 `O(n!)` 全排列。",
    "- `n <= 20` 容許 `O(2^n)` 狀態壓縮。",
    "- `n <= 40` 可用 meet in the middle 的 `O(2^{n/2})`。",
    "- `n <= 500` 容許 `O(n^3)`。",
    "- `n <= 5000` 容許 `O(n^2)`。",
    "- `n <= 2e5` 通常需 `O(n log n)` 或線性。",
    "- `n >= 1e9` 指向 `O(log n)`、`O(sqrt(n))` 或封閉式公式。",
  ].join("\n"),
  "### 二、三把通用鑰匙",
  [
    "- 二分答案：把最佳化轉成判定，前提是可行性單調。",
    "- 貢獻法：換一個枚舉對象（每個元素貢獻多少），把 `O(n^2)` 配對降為線性。",
    "- 正難則反：正面計數困難時改算補集（總方案減去不合法方案）。",
  ].join("\n"),
  "### 三、降維是主旋律",
  [
    "- 固定一個維度（右端點、答案值、中心節點或樹根），把另一維化為查詢、判定或預處理表。",
    "- 「枚舉右、維護左」與「枚舉中間」是把雙重枚舉降階的兩種骨架。",
  ].join("\n"),
  "### 四、離線與分治",
  [
    "- 離線：把查詢按門檻排序使限制單調，常把 polylog 再降一檔（掃描線、莫隊、線段樹分治、CDQ）。",
    "- 根號分治：「大的暴力、小的預處理」是 sqrt 類做法的共通骨架。",
  ].join("\n"),
  "### 五、正確性與工程細節",
  [
    "- 先寫可證明正確的樸素式，再判斷能否用資料結構或單調性消去枚舉。",
    "- 易錯點：`long long` 防溢位、半開區間防越界、初始化（無限大、`dp[0]`）、相等元素的去重歸屬、對拍驗證邊界。",
  ].join("\n"),
].join("\n\n");
