import type { TutorialData } from "@/types";

export const googleInterview =
{
  "id": 1,
  "title": "Google 面試準備",
  "src": "",
  "last_update": "2026-05-20T04:21:26.227Z",
  "summary": "這份 Google 面試準備題單不只列題，也把每一章的「觀念說明」整理成可直接複習的筆記。展開章節後，你會看到：何時使用這類技巧、面試中要先問自己的判斷問題、常見複雜度，以及可改寫到題目中的 C++ 模板。\n\n後續章節覆蓋資料平行、資源限制、平台系統 API 與高階綜合題；建議先讀觀念說明，再限時做題，最後用模板反推自己的程式碼。",
  "children": [
    {
      "id": 2,
      "title": "1. 圖搜尋與最短路",
      "summary": "**觀念起點**\n\n圖搜尋的第一步是把「題目中的一步」定義清楚。節點不一定是城市或網格格子，也可能是一個字串、一個已拿鑰匙集合、一個剩餘資源量，或一個包含時間的局面。只要目前局面與下一步選擇能被明確列出，就可以把問題看成狀態圖。\n\n選 BFS、0-1 BFS 或 Dijkstra 前，先看每條邊的成本。每一步成本完全相同時，BFS 的層序保證最短步數；成本只有 0 和 1 時，用 deque 把 0 成本放前面；成本是非負任意值時，才需要 priority queue。\n\nvisited 的維度要等於完整狀態。若同一格剩餘障礙消除次數不同，或同一位置拿到的鑰匙集合不同，未來可走路徑就不同，不能只用座標去重。\n\n延伸主題：邊權只有 0 與 1 用 deque 的 0-1 BFS，含負權用 Bellman-Ford 或 SPFA，全點對最短路用 Floyd；多段條件（折扣、通行證）用分層圖，多起點用超級源點或多源 BFS。\n\n這章處理「從一個狀態走到其他狀態」的問題。從零開始看，圖搜尋不是只在明顯的 graph 上使用；只要題目可以描述成「目前在哪裡、下一步能做什麼、做一步的代價是多少」，就可以建成狀態圖。網格中的格子、字串轉換後的字串、已收集鑰匙的集合、甚至剩餘資源量，都可能是狀態的一部分。\n\n**核心觀念**：\n- BFS 用在每一步代價相同的最短步數問題。第一次抵達某狀態時，距離一定最短。\n- 0-1 BFS 用在邊權只有 0 和 1 的圖，用 deque 維護候選狀態。\n- Dijkstra 用在非負權重圖。每次取出目前已知距離最小的狀態並鬆弛鄰居。\n- visited 不能只憑直覺寫。若未來選擇會受「剩餘資源、鑰匙集合、方向、時間」影響，這些都要進入狀態。\n\n**系統例題：網格最短路，最多消除 k 個障礙**\n\n**完整問題**：給一個 0/1 網格，從左上走到右下，每步上下左右移動，遇到 1 可以消耗一次消除機會，最多消除 k 次，求最少步數。\n\n解題步驟：\n1. 狀態不是 `(r, c)`，而是 `(r, c, left)`，其中 `left` 是剩餘消除次數。\n2. 每次移動代價都是 1，所以使用 BFS。\n3. 同一格可能以不同 `left` 抵達。若我們曾用更多剩餘次數到過同一格，現在用更少次數到達就沒有價值。\n4. 用 `best[r][c]` 記錄到達此格時看過的最大剩餘消除次數，只在新狀態更好時入隊。\n\n**C++：BFS + 資源維度剪枝**\n\n```cpp\n// shortest path in grid with at most k obstacle eliminations\nint shortestPath(vector<vector<int>>& grid, int k) {\n  int rows = grid.size(), cols = grid[0].size();\n  vector<vector<int>> best(rows, vector<int>(cols, -1));\n  queue<array<int, 4>> q;  // row, col, remaining eliminations, distance\n\n  q.push({0, 0, k, 0});\n  best[0][0] = k;\n  const int kDirs[5] = {1, 0, -1, 0, 1};\n\n  while (!q.empty()) {\n    auto [row, col, left, dist] = q.front();\n    q.pop();\n    if (row == rows - 1 && col == cols - 1) {\n      return dist;\n    }\n\n    for (int dir = 0; dir < 4; ++dir) {\n      int next_row = row + kDirs[dir];\n      int next_col = col + kDirs[dir + 1];\n      if (next_row < 0 || next_row >= rows || next_col < 0 || next_col >= cols) {\n        continue;\n      }\n\n      int next_left = left - grid[next_row][next_col];\n      if (next_left < 0) {\n        continue;\n      }\n      if (next_left <= best[next_row][next_col]) {\n        continue;\n      }\n\n      best[next_row][next_col] = next_left;\n      q.push({next_row, next_col, next_left, dist + 1});\n    }\n  }\n  return -1;\n}\n```\n\n複雜度是 `O(m*n*k)` 的上界，但 `best` 剪枝常把實際狀態數壓小。面試時要主動說明：如果障礙消除次數不是資源，而是有不同代價，模型就要改成 Dijkstra。\n\n**競程課程講義：把搜尋題拆成演算法選型**\n\n**BFS：等權最短路**\n\n訊號：每一步成本相同，題目問最少步數、最少操作次數、最短層數。BFS 的不變式是「queue 中狀態依距離非遞減」，因此第一次到達目標就是最短。\n\n例題：二進位矩陣最短路。8 方向移動，只能走 0，求左上到右下最短路。狀態是格子 `(r, c)`，邊權都是 1。\n\n```cpp\nstruct GridBfs {\n  static constexpr int dr[4] = {1, 0, -1, 0};\n  static constexpr int dc[4] = {0, 1, 0, -1};\n\n  vector<vector<int>> distance_from(vector<vector<int>>& grid, int sr, int sc) {\n    int rows = grid.size(), cols = grid[0].size();\n    vector<vector<int>> dist(rows, vector<int>(cols, -1));\n    queue<pair<int, int>> q;\n\n    if (grid[sr][sc] != 0) {\n      return dist;\n    }\n    dist[sr][sc] = 0;\n    q.push({sr, sc});\n\n    while (!q.empty()) {\n      auto [r, c] = q.front();\n      q.pop();\n      for (int dir = 0; dir < 4; ++dir) {\n        int nr = r + dr[dir], nc = c + dc[dir];\n        if (nr < 0 || nr >= rows || nc < 0 || nc >= cols) {\n          continue;\n        }\n        if (grid[nr][nc] != 0 || dist[nr][nc] != -1) {\n          continue;\n        }\n        dist[nr][nc] = dist[r][c] + 1;\n        q.push({nr, nc});\n      }\n    }\n    return dist;\n  }\n};\n```\n\n**0-1 BFS：邊權只有 0 或 1**\n\n訊號：轉移有免費操作和付費一次操作，例如改方向代價 1、順方向代價 0。用 deque；0 權邊放前面，1 權邊放後面。不變式近似 Dijkstra，但避免 priority queue。\n\n例題：網格每格有建議方向，順方向走成本 0，改方向成本 1，求最小修改次數。\n\n```cpp\nstruct ZeroOneBfs {\n  using Edge = pair<int, int>;  // next node, weight 0 or 1\n  static constexpr int kInf = 1e9;\n\n  vector<int> shortest_paths(const vector<vector<Edge>>& graph, int source) {\n    vector<int> dist(graph.size(), kInf);\n    deque<int> dq;\n    dist[source] = 0;\n    dq.push_front(source);\n\n    while (!dq.empty()) {\n      int u = dq.front();\n      dq.pop_front();\n      for (auto [v, w] : graph[u]) {\n        if (dist[v] <= dist[u] + w) {\n          continue;\n        }\n        dist[v] = dist[u] + w;\n        if (w == 0) {\n          dq.push_front(v);\n        } else {\n          dq.push_back(v);\n        }\n      }\n    }\n    return dist;\n  }\n};\n```\n\n**Dijkstra：非負權最短路**\n\n訊號：邊權是非負數，題目問最短時間、最低成本、最小風險。priority queue 每次取出目前距離最小的點；若彈出距離不是最新值，跳過。\n\n例題：網路延遲時間。從起點 k 傳訊息到所有節點，求最晚收到時間。\n\n```cpp\nstruct Dijkstra {\n  using Edge = pair<int, int>;  // next node, non-negative weight\n  static constexpr long long kInf = 4e18;\n\n  vector<long long> shortest_paths(const vector<vector<Edge>>& graph, int source) {\n    vector<long long> dist(graph.size(), kInf);\n    priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\n    dist[source] = 0;\n    pq.push({0, source});\n\n    while (!pq.empty()) {\n      auto [du, u] = pq.top();\n      pq.pop();\n      if (du != dist[u]) {\n        continue;\n      }\n      for (auto [v, w] : graph[u]) {\n        if (dist[v] <= du + w) {\n          continue;\n        }\n        dist[v] = du + w;\n        pq.push({dist[v], v});\n      }\n    }\n    return dist;\n  }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1765 | [地圖中的最高點](https://leetcode.cn/problems/map-of-highest-peak) | 1783 |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort) | 1948 |\n| 1293 | [網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-grid-with-obstacles-elimination) | 1967 |\n| 1976 | [到達目的地的方案數](https://leetcode.cn/problems/number-of-ways-to-arrive-at-destination) | 2095 |\n| 2812 | [找出最安全路徑](https://leetcode.cn/problems/find-the-safest-path-in-a-grid) | 2154 |\n| 2065 | [最大化一張圖中的路徑價值](https://leetcode.cn/problems/maximum-path-quality-of-a-graph) | 2178 |\n| 2503 | [矩陣查詢可獲得的最大分數](https://leetcode.cn/problems/maximum-number-of-points-from-grid-queries) | 2196 |\n| 3244 | [新增道路查詢後的最短距離 II](https://leetcode.cn/problems/shortest-distance-after-road-addition-queries-ii) | 2270 |\n| 2699 | [修改圖中的邊權](https://leetcode.cn/problems/modify-graph-edge-weights) | 2874 |"
    },
    {
      "id": 3,
      "title": "2. 樹與圖建模",
      "summary": "**觀念起點**\n\n樹與圖建模的重點是先判斷關係類型。無向邊表示雙向可達，適合連通塊、DFS/BFS 或 DSU；有向邊表示依賴或單向轉移，常要檢查 cycle、拓撲排序或 SCC。\n\n樹是沒有環的連通圖，因此每條邊都能把節點切成父側與子樹側。只要題目保證每個節點除根外只有一個父節點，就可以用後序 DFS 把孩子資訊合併成父節點答案。\n\n遇到『刪掉一條邊會不會斷』不要用每條邊重跑 DFS。這是橋的問題；遇到有向圖中『互相依賴的一群點』，則把 SCC 壓縮成 DAG 後再處理。\n\n進階工具：割點與橋、邊雙與點雙連通分量、SCC（縮點成 DAG）、2-SAT（以 SCC 求解）、差分約束（以最短或最長路解不等式組）；樹上則有 LCA、樹鏈剖分與點分治。\n\n這章訓練把題目文字轉成圖、樹或 DAG。初學時最常卡住的不是 DFS 寫法，而是沒有先判斷關係的性質：邊是有向還是無向、是否保證無環、是否一定連通、是否有唯一父節點。Google 題常把「課程、任務、城市、員工、父子關係」包成資料表；只要模型選對，後面通常就是拓撲排序、DFS/BFS、並查集、樹形 DP、Tarjan 找橋或強連通分量。\n\n**從零建模總表**：\n- 有先後依賴：建有向圖。若依賴可能成環，用拓撲排序檢查是否能處理全部節點；若需要最早完成時間，拓撲順序上做 DP。\n- 無向連通關係：建無向圖。若要走訪或統計每個連通塊，用 DFS/BFS；若邊是逐步加入、只問兩點是否連通，用並查集。\n- 每個點除根以外只有一個父節點：通常是樹。用 DFS 回傳子樹資訊，例如 size、sum、高度、是否滿足條件。\n- 問刪掉一條邊是否斷開：考慮橋。橋是無向圖中移除後會增加連通塊數的邊。\n- 問強依賴群組或互相可達：考慮強連通分量。SCC 是有向圖中任兩點都能互相到達的一組點。\n\n**Pattern 1：有向依賴圖 + 拓撲排序**\n\n使用時機：題目有「必須先完成 A 才能做 B」、「課程先修」、「任務依賴」、「build pipeline」等描述。建邊時要固定語意：若 `u -> v` 表示 u 是 v 的 prerequisite，拓撲順序中 u 必須出現在 v 前面。\n\n系統例題：有 n 門課，每門課需要 `time[i]`，關係 `u -> v` 表示必須先完成 u 才能開始 v。可以平行上課，求完成全部課程的最早時間。\n\n解題步驟：\n1. 這是有向依賴圖。若有環，課程無法全部完成。\n2. `finish[i]` 表示完成第 i 門課的最早時間。\n3. 入度為 0 的課可立刻開始，完成時間是 `time[i]`。\n4. 拓撲排序彈出 u 後，用 `finish[u] + time[v]` 更新後繼 v。\n5. 若最後處理節點數小於 n，代表存在 cycle；否則答案是 `max(finish)`。\n\n```cpp\n// Topological sort + longest path on a DAG.\nint minimumTime(int n, vector<vector<int>>& relations, vector<int>& time) {\n  vector<vector<int>> g(n);\n  vector<int> indeg(n), finish(n);\n\n  for (auto& e : relations) {\n    int u = e[0] - 1, v = e[1] - 1;\n    g[u].push_back(v);\n    indeg[v]++;\n  }\n\n  queue<int> q;\n  for (int i = 0; i < n; ++i) {\n    finish[i] = time[i];\n    if (indeg[i] == 0) {\n      q.push(i);\n    }\n  }\n\n  int seen = 0;\n  while (!q.empty()) {\n    int u = q.front();\n    q.pop();\n    seen++;\n    for (int v : g[u]) {\n      finish[v] = max(finish[v], finish[u] + time[v]);\n      if (--indeg[v] == 0) {\n        q.push(v);\n      }\n    }\n  }\n\n  if (seen < n) {\n    return -1;\n  }\n  return *max_element(finish.begin(), finish.end());\n}\n```\n\n**Pattern 2：無向圖連通塊 + DFS/BFS**\n\n使用時機：題目問「有幾個群組」、「每個群組大小」、「從某點能走到哪些點」、「網路是否全部連通」。DFS/BFS 適合一次性走訪整張靜態圖，因為你能在走訪時同步統計連通塊資訊。\n\n系統例題：給 n 個城市和無向邊，求連通塊數量。\n\n解題步驟：\n1. 建無向 adjacency list，因為關係是雙向可達。\n2. 用 `visited` 避免重複走訪。\n3. 每遇到一個未走訪節點，就代表發現新連通塊，從它開始 DFS/BFS 標記整塊。\n4. DFS 和 BFS 在此題複雜度相同，都是 `O(V+E)`。\n\n```cpp\n// Count connected components with DFS.\nint countComponents(int n, vector<vector<int>>& edges) {\n  vector<vector<int>> g(n);\n  for (auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    g[e[1]].push_back(e[0]);\n  }\n\n  vector<char> seen(n, false);\n  function<void(int)> dfs = [&g, &seen, &dfs](int u) {\n    seen[u] = true;\n    for (int v : g[u]) {\n      if (!seen[v]) {\n        dfs(v);\n      }\n    }\n  };\n\n  int components = 0;\n  for (int i = 0; i < n; ++i) {\n    if (seen[i]) {\n      continue;\n    }\n    components++;\n    dfs(i);\n  }\n  return components;\n}\n```\n\n**Pattern 3：逐步合併關係 + 並查集**\n\n使用時機：邊或關係是一條一條加入，並且問題主要是「兩個點是否在同一群」、「加入這條邊是否形成環」、「目前還有幾個集合」。並查集不適合刪邊，也不適合回答路徑長度；它只維護集合代表元。\n\n系統例題：給一批無向邊，判斷是否能形成一棵合法樹。\n\n解題步驟：\n1. 樹需要剛好 `n-1` 條邊，且整張圖連通、無環。\n2. 用 DSU 逐條加入邊；若加入前兩端已在同一集合，這條邊會形成環。\n3. 最後若沒有環且邊數是 `n-1`，就是樹。\n\n```cpp\n// Union Find for validating whether an undirected graph is a tree.\nclass DSU {\n  vector<int> parent, size;\n\n public:\n  DSU(int n) : parent(n), size(n, 1) { iota(parent.begin(), parent.end(), 0); }\n\n  int find(int x) {\n    if (parent[x] == x) {\n      return x;\n    }\n    return parent[x] = find(parent[x]);\n  }\n\n  bool unite(int a, int b) {\n    int ra = find(a), rb = find(b);\n    if (ra == rb) {\n      return false;\n    }\n    if (size[ra] < size[rb]) {\n      swap(ra, rb);\n    }\n    parent[rb] = ra;\n    size[ra] += size[rb];\n    return true;\n  }\n};\n\nbool validTree(int n, vector<vector<int>>& edges) {\n  if ((int)edges.size() != n - 1) {\n    return false;\n  }\n  DSU dsu(n);\n  for (auto& e : edges) {\n    if (!dsu.unite(e[0], e[1])) {\n      return false;\n    }\n  }\n  return true;\n}\n```\n\n**Pattern 4：樹 + 子樹資訊合併**\n\n使用時機：題目保證是樹，或每個點除了根以外只有一個父節點。樹沒有環，所以 DFS 時只需要避開 parent；每個節點的答案通常能由孩子答案合併。\n\n系統例題：給一棵樹與每個節點的值，統計有多少個子樹總和能被 k 整除。若某個子樹總和能被 k 整除，就可以把它切出來，向父節點回傳 0。\n\n解題步驟：\n1. 從任意節點當根做 DFS。\n2. `dfs(u)` 回傳 u 子樹尚未被切走的總和模 k。\n3. 合併所有孩子回傳值與 `value[u]`。\n4. 若當前總和模 k 為 0，答案加一，並向父節點回傳 0。\n\n```cpp\n// Tree DFS that merges child subtree information.\nint countDivisibleSubtrees(int n, vector<vector<int>>& edges, vector<int>& values, int k) {\n  vector<vector<int>> tree(n);\n  for (auto& edge : edges) {\n    tree[edge[0]].push_back(edge[1]);\n    tree[edge[1]].push_back(edge[0]);\n  }\n\n  int answer = 0;\n  function<int(int, int)> dfs = [&tree, &values, k, &answer, &dfs](int node, int parent) {\n    long long sum = values[node] % k;\n    for (int next_node : tree[node]) {\n      if (next_node == parent) {\n        continue;\n      }\n      sum += dfs(next_node, node);\n    }\n    sum %= k;\n    if (sum == 0) {\n      answer++;\n      return 0;\n    }\n    return (int)sum;\n  };\n\n  dfs(0, -1);\n  return answer;\n}\n```\n\n**Pattern 5：無向圖關鍵邊 + 橋（Tarjan low-link）**\n\n使用時機：題目問「移除哪條邊會讓網路斷開」、「critical connections」、「某條邊是否不可替代」。DFS 樹中，若子節點 v 及其子樹無法透過 back edge 回到 u 或 u 的祖先，則 `(u, v)` 是橋。\n\n系統例題：找出無向圖中所有 critical connections。\n\n解題步驟：\n1. `dfn[u]` 是第一次拜訪 u 的時間戳。\n2. `low[u]` 是 u 透過 DFS tree edge 加最多一條 back edge 能到達的最小 dfn。\n3. DFS 完 child v 後，若 `low[v] > dfn[u]`，代表 v 子樹沒有任何路能繞回 u 或 u 祖先，所以 `(u, v)` 是橋。\n4. 無向圖要用 edge id 避免把父邊誤當 back edge。\n\n```cpp\n// Tarjan bridge-finding in an undirected graph.\nvector<vector<int>> criticalConnections(int n, vector<vector<int>>& connections) {\n  vector<vector<pair<int, int>>> g(n);\n  for (int i = 0; i < (int)connections.size(); ++i) {\n    int u = connections[i][0], v = connections[i][1];\n    g[u].push_back({v, i});\n    g[v].push_back({u, i});\n  }\n\n  vector<int> dfn(n, 0), low(n, 0);\n  vector<vector<int>> bridges;\n  int timer = 0;\n\n  function<void(int, int)> dfs = [&g, &dfn, &low, &bridges, &timer, &dfs](int u, int parent_edge) {\n    dfn[u] = low[u] = ++timer;\n    for (auto [v, edge_id] : g[u]) {\n      if (edge_id == parent_edge) {\n        continue;\n      }\n      if (!dfn[v]) {\n        dfs(v, edge_id);\n        low[u] = min(low[u], low[v]);\n        if (low[v] > dfn[u]) {\n          bridges.push_back({u, v});\n        }\n      } else {\n        low[u] = min(low[u], dfn[v]);\n      }\n    }\n  };\n\n  for (int i = 0; i < n; ++i) {\n    if (!dfn[i]) {\n      dfs(i, -1);\n    }\n  }\n  return bridges;\n}\n```\n\n**Pattern 6：有向圖互相依賴群組 + 強連通分量（SCC）**\n\n使用時機：題目描述有向依賴，且要找「互相可達」、「互相依賴」、「一組節點可以彼此到達」。把每個 SCC 壓縮成一個點後，整張圖會變成 DAG，很多問題就能接拓撲排序或 DP。\n\n系統例題：給有向圖，找出所有強連通分量。這裡用 Kosaraju：先在原圖 DFS 得到完成順序，再在反圖依反向完成順序 DFS，每次走到的一整塊就是一個 SCC。\n\n解題步驟：\n1. 原圖 DFS，節點完成時放入 `order`。\n2. 建反圖。\n3. 反向掃 `order`，在反圖上 DFS；每次 DFS 收集到的 nodes 互相可達，是一個 SCC。\n4. 若要解依賴問題，可以把每個 SCC 編號後建 component DAG。\n\n```cpp\n// Kosaraju algorithm for strongly connected components.\nvector<vector<int>> stronglyConnectedComponents(int n, vector<vector<int>>& edges) {\n  vector<vector<int>> g(n), rg(n);\n  for (auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    rg[e[1]].push_back(e[0]);\n  }\n\n  vector<char> seen(n, false);\n  vector<int> order;\n\n  function<void(int)> postorderDfs = [&g, &seen, &order, &postorderDfs](int u) {\n    seen[u] = true;\n    for (int v : g[u]) {\n      if (!seen[v]) {\n        postorderDfs(v);\n      }\n    }\n    order.push_back(u);\n  };\n\n  for (int i = 0; i < n; ++i) {\n    if (!seen[i]) {\n      postorderDfs(i);\n    }\n  }\n\n  vector<vector<int>> components;\n  fill(seen.begin(), seen.end(), false);\n\n  function<void(int, vector<int>&)> rerootDfs = [&rg, &seen, &rerootDfs](int u, vector<int>& comp) {\n    seen[u] = true;\n    comp.push_back(u);\n    for (int v : rg[u]) {\n      if (!seen[v]) {\n        rerootDfs(v, comp);\n      }\n    }\n  };\n\n  reverse(order.begin(), order.end());\n  for (int u : order) {\n    if (seen[u]) {\n      continue;\n    }\n    vector<int> comp;\n    rerootDfs(u, comp);\n    components.push_back(comp);\n  }\n  return components;\n}\n```\n\n**面試時的總結方式**：先說資料如何建圖，再說圖的性質，最後說演算法。比如：「這是有向依賴圖，所以我先檢查 cycle，若無 cycle 就在拓撲順序上做 DP」；或「這是無向靜態連通問題，所以 DFS 一次走訪每個 component 就夠，不需要 DSU」。這種回答比直接丟模板更容易讓面試官確認你真的理解模型。\n\n**競程課程講義：建模後要把每種圖型配到固定工具**\n\n這章的核心不是背 Tarjan 或 DSU，而是能把題目敘述翻譯成圖的性質。競程課中建議每題先寫四行：節點是什麼、邊代表什麼、邊是否有方向、答案需要連通性還是路徑順序。完成這四行後，演算法通常就很明確。\n\n**對照表**：\n- DFS/BFS：靜態圖走訪、連通塊統計、可達性。\n- DSU：動態加邊合併集合、判斷加邊是否成環。\n- Topological sort：DAG 依賴順序、先修限制、任務排程。\n- Tree DFS：子樹 size/sum/depth/height 合併。\n- Bridge：無向圖中不可替代的邊。\n- SCC：有向圖中的互相可達群組，壓縮後變 DAG。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2096 | [從二叉樹一個節點到另一個節點每一步的方向](https://leetcode.cn/problems/step-by-step-directions-from-a-binary-tree-node-to-another) | 1805 |\n| 1377 | [T 秒後青蛙的位置](https://leetcode.cn/problems/frog-position-after-t-seconds) | 1824 |\n| 2872 | [可以被 K 整除連通塊的最大數目](https://leetcode.cn/problems/maximum-number-of-k-divisible-components) | 1968 |\n| 2092 | [找出知曉秘密的所有專家](https://leetcode.cn/problems/find-all-people-with-secret) | 2004 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital) | 2012 |\n| 2467 | [樹上最大得分和路徑](https://leetcode.cn/problems/most-profitable-path-in-a-tree) | 2053 |\n| 2050 | [並行課程 III](https://leetcode.cn/problems/parallel-courses-iii) | 2084 |\n| 1192 | [查詢叢集內的關鍵連線](https://leetcode.cn/problems/critical-connections-in-a-network) | 2085 |\n| 2581 | [統計可能的樹根數目](https://leetcode.cn/problems/count-number-of-possible-root-nodes) | 2228 |\n| 2538 | [最大價值和與最小价值和的差值](https://leetcode.cn/problems/difference-between-maximum-and-minimum-price-sum) | 2398 |\n| 2603 | [收集樹中金幣](https://leetcode.cn/problems/collect-coins-in-a-tree) | 2712 |"
    },
    {
      "id": 4,
      "title": "3. 動態規劃與搜尋優化",
      "summary": "**觀念起點**\n\nDP 可以從暴力搜尋理解：若兩條不同路徑到達同一個局面，而且之後能做的選擇完全相同，就可以把這個局面存起來。狀態不是陣列名稱，而是一句可驗證的語意。\n\n設計 DP 時先寫最後一步。最後一個工作選不選、最後一段從哪裡切、最後一個元素接在哪個前驅後面，這些問題會自然導出轉移式。\n\n搜尋優化通常是在正確 DP 之後才出現。若轉移需要枚舉很多前驅，再觀察前驅能否用排序、二分、前綴最佳值、heap、set 或單調隊列快速取得。\n\nDP 優化的常見手段：單調隊列（窗口轉移）、斜率優化或李超線段樹（`min(m*x+b)` 形式）、決策單調性分治與四邊形不等式；狀態壓縮、數位、區間、樹形 DP 也常與搜尋結合。\n\n這章的核心是把暴力搜尋改成「只記必要資訊」。動態規劃從零開始可以理解成：很多搜尋路徑會重複到達同一種局面，只要這些局面的未來完全一樣，就可以把局面變成狀態並重用答案。設計 DP 時，不要先想陣列大小，先用一句話定義 `dp` 的意義。\n\n**DP 設計流程**：\n1. 狀態：處理到哪個位置？用了多少資源？目前限制還剩多少？\n2. 選擇：這一步選或不選、接在哪個前一狀態後面、或切在哪裡。\n3. 轉移：把選擇寫成公式，確認每個依賴狀態已經算好。\n4. 初始值：最大化用很小值，最小化用 kInf，計數用 0/1 起點。\n5. 優化：如果轉移太慢，再找排序、二分、前綴最佳值或單調資料結構。\n\n**系統例題：帶權工作排程**\n\n**完整問題**：每份工作有開始時間、結束時間、收益，不能選時間重疊的工作，求最大收益。\n\n解題步驟：\n1. 先按結束時間排序，讓「前面工作」代表已經較早結束。\n2. 定義 `dp[i]`：只考慮前 i 個排序後工作時的最大收益。\n3. 對第 i 個工作，有兩種選擇：不選它，答案是 `dp[i-1]`；選它，就要接在最後一個結束時間 `<= start` 的工作後面。\n4. 用二分找到相容工作的數量 `j`，轉移為 `dp[i] = max(dp[i-1], dp[j] + profit)`。\n\n**C++：排序 + 二分 + 一維 DP**\n\n```cpp\n// weighted interval scheduling\nint jobScheduling(vector<int>& start, vector<int>& end, vector<int>& profit) {\n  int n = start.size();\n  vector<array<int, 3>> jobs;\n  for (int i = 0; i < n; ++i) {\n    jobs.push_back({end[i], start[i], profit[i]});\n  }\n  sort(jobs.begin(), jobs.end());\n\n  vector<int> ends(n);\n  for (int i = 0; i < n; ++i) {\n    ends[i] = jobs[i][0];\n  }\n\n  vector<int> dp(n + 1, 0);\n  for (int i = 1; i <= n; ++i) {\n    auto [e, s, p] = jobs[i - 1];\n    int j = upper_bound(ends.begin(), ends.end(), s) - ends.begin();\n    dp[i] = max(dp[i - 1], dp[j] + p);\n  }\n  return dp[n];\n}\n```\n\n複雜度是 `O(n log n)`。面試時要能說出為什麼排序後可以只看前綴，為什麼 `dp[j]` 已經包含所有不重疊工作，而不是只選第 j 份工作。\n\n**競程課程講義：DP 從暴力搜尋開始教**\n\n**Pattern 1：記憶化 DFS**\n\n訊號：遞迴搜尋會重複遇到同一個 `(index, remaining, state)`。先寫暴力遞迴，再加 memo。\n\n例題：爬樓梯，每次走 1 或 2 階，求方法數。狀態 `f(i)` 是到第 i 階的方法數。\n\n```cpp\n// Memoized DFS: number of ways to climb n stairs.\nint climbStairs(int n) {\n  vector<int> memo(n + 1, -1);\n  function<int(int)> dfs = [&memo, &dfs](int i) {\n    if (i == 0) {\n      return 1;\n    }\n    if (i < 0) {\n      return 0;\n    }\n    if (memo[i] != -1) {\n      return memo[i];\n    }\n    return memo[i] = dfs(i - 1) + dfs(i - 2);\n  };\n  return dfs(n);\n}\n```\n\n**Pattern 2：線性 DP**\n\n訊號：答案只依賴前面幾個位置。例題：House Robber。`dp[i]` 表示前 i 間房子的最大收益；第 i 間選或不選。\n\n```cpp\n// Linear DP with take / skip transition.\nint rob(vector<int>& nums) {\n  int take = 0, skip = 0;\n  for (int x : nums) {\n    int ntake = skip + x;\n    int nskip = max(skip, take);\n    take = ntake;\n    skip = nskip;\n  }\n  return max(take, skip);\n}\n```\n\n**Pattern 3：狀態壓縮 DP**\n\n訊號：限制數量小，例如 `n <= 20` 或技能數小。用 bitmask 表示集合。\n\n例題：最小團隊覆蓋所有技能。`dp[mask]` 保存達成技能集合 mask 的最少人數。\n\n```cpp\n// Bitmask DP for set cover style problems.\nint minPeopleForSkills(vector<int>& person_mask, int skill_count) {\n  int full_mask = 1 << skill_count;\n  const int kInf = 1e9;\n  vector<int> dp(full_mask, kInf);\n  dp[0] = 0;\n  for (int person_mask_value : person_mask) {\n    vector<int> old = dp;\n    for (int mask = 0; mask < full_mask; ++mask) {\n      int next_mask = mask | person_mask_value;\n      dp[next_mask] = min(dp[next_mask], old[mask] + 1);\n    }\n  }\n  return dp[full_mask - 1];\n}\n```\n\n**Pattern 4：排序後 DP + 二分前驅**\n\n訊號：每個選擇有開始/結束時間，轉移需要找前一個相容狀態。先排序，再二分，不要在轉移中線性掃描。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1671 | [得到山形陣列的最少刪除次數](https://leetcode.cn/problems/minimum-number-of-removals-to-make-mountain-array) | 1913 |\n| 1235 | [規劃兼職工作](https://leetcode.cn/problems/maximum-profit-in-job-scheduling) | 2023 |\n| 1335 | [工作計劃的最低難度](https://leetcode.cn/problems/minimum-difficulty-of-a-job-schedule) | 2035 |\n| 1770 | [執行乘法運算的最大分數](https://leetcode.cn/problems/maximum-score-from-performing-multiplication-operations) | 2068 |\n| 1799 | [N 次操作後的最大分數和](https://leetcode.cn/problems/maximize-score-after-n-operations) | 2073 |\n| 1494 | [並行課程 II](https://leetcode.cn/problems/parallel-courses-ii) | 2082 |\n| 1937 | [扣分後的最大得分](https://leetcode.cn/problems/maximum-number-of-points-with-cost) | 2106 |\n| 1547 | [切棍子的最小成本](https://leetcode.cn/problems/minimum-cost-to-cut-a-stick) | 2116 |\n| 2218 | [從堆疊中取出 K 個硬幣的最大面值和](https://leetcode.cn/problems/maximum-value-of-k-coins-from-piles) | 2158 |\n| 1240 | [鋪瓷磚](https://leetcode.cn/problems/tiling-a-rectangle-with-the-fewest-squares) | 2242 |\n| 1125 | [最小的必要團隊](https://leetcode.cn/problems/smallest-sufficient-team) | 2251 |\n| 1723 | [完成所有工作的最短時間](https://leetcode.cn/problems/find-minimum-time-to-finish-all-jobs) | 2284 |\n| 1595 | [連通兩組點的最小成本](https://leetcode.cn/problems/minimum-cost-to-connect-two-groups-of-points) | 2538 |"
    },
    {
      "id": 5,
      "title": "4. 字串、Trie 與雜湊",
      "summary": "**觀念起點**\n\n字串題的核心通常是大量比較。暴力比較每個起點與 pattern 會重複看同一批字元；KMP、Z function、rolling hash、Trie 都是在回答不同種類的比較問題。\n\n固定 pattern 在 text 中找位置時，用 KMP 或 Z；任意兩段子串要反覆判斷相等時，用 hash；大量字典詞共用前綴時，用 Trie；若字串轉換帶成本，常要把字串或字元建成圖再接最短路。\n\n先找昂貴操作，再選工具。工具本身不是完整解法，外層仍可能是 DP、二分、BFS 或資料結構維護。\n\n更重的字串工具：AC 自動機（多模式匹配）、後綴數組（SA）、後綴自動機（SAM）、回文自動機（PAM）；雜湊以雙模或 64 位降低被卡碰撞的風險。\n\n字串題的第一步是判斷「比較單位」。初學時常用雙層迴圈逐字比較，但面試輸入一大就會超時。常見技巧都是為了快速回答某種比較：KMP 和 Z 函式處理前綴/模式匹配，rolling hash 處理任意子串相等，Trie 處理大量字典詞的共同前綴。\n\n**常見選型**：\n- 找 pattern 在 text 中所有出現位置：KMP 或 Z 函式。\n- 判斷大量子串是否相等：rolling hash，實務上可用雙模或 64-bit 降低碰撞風險。\n- 對字典詞反覆查前綴：Trie。\n- 字串轉換帶代價：把字元或字串建成圖，再接最短路或 DP。\n\n**系統例題：找出 pattern 在 text 中的所有位置**\n\n暴力做法是每個位置都比較一次 pattern，最壞 `O(n*m)`。KMP 的關鍵是：當匹配失敗時，不必把 pattern 退回 0，而是利用 pattern 自己的最長相等前後綴，跳到下一個仍可能匹配的位置。\n\n解題步驟：\n1. 對 `pattern` 建 prefix function，`pi[i]` 表示 `pattern[0..i]` 的最長 proper prefix，同時也是 suffix 的長度。\n2. 掃描 text 時用指標 `j` 表示 pattern 已匹配長度。\n3. 若字元不等，沿 `pi` 回退；若相等，`j++`。\n4. `j == m` 時找到一次匹配，記錄起點並繼續回退到 `pi[m-1]`。\n\n**C++：KMP 模式匹配**\n\n```cpp\n// KMP prefix function and pattern search\nvector<int> prefix_function(const string& s) {\n  int n = s.size();\n  vector<int> pi(n, 0);\n  for (int i = 1; i < n; ++i) {\n    int j = pi[i - 1];\n    while (j > 0 && s[i] != s[j]) {\n      j = pi[j - 1];\n    }\n    if (s[i] == s[j]) {\n      j++;\n    }\n    pi[i] = j;\n  }\n  return pi;\n}\n\nvector<int> find_matches(const string& text, const string& pattern) {\n  vector<int> pi = prefix_function(pattern);\n  vector<int> answer;\n  int j = 0, pattern_size = pattern.size();\n\n  for (int i = 0; i < (int)text.size(); ++i) {\n    while (j > 0 && text[i] != pattern[j]) {\n      j = pi[j - 1];\n    }\n    if (text[i] == pattern[j]) {\n      j++;\n    }\n    if (j == pattern_size) {\n      answer.push_back(i - pattern_size + 1);\n      j = pi[j - 1];\n    }\n  }\n  return answer;\n}\n```\n\nKMP 是 `O(n+m)`。如果題目不是固定 pattern，而是任意兩段子串比較，KMP 就不合適，應改用 hash 或後綴相關工具。\n\n**LeetCode 例題：1044. Longest Duplicate Substring**\n\n題目要找最長的重複子字串。檢查某個長度 `len` 是否可行時，必須比較所有長度為 `len` 的子字串：`s[0..len)`、`s[1..len+1)`、`s[2..len+2)` 等等。這些比較不是拿固定 pattern 去掃 text，而是任意兩段同長度子字串互相比，所以 KMP 無法直接回答。rolling hash 可以把每段子字串轉成 `O(1)` 可查的 hash，再用二分答案找最大長度。\n\n**Rolling hash 小模塊公式**：\n給定模數 `M`、底數 `B`，以及字元值 `value(s[i])`。實作時通常直接記成這三行：\n\n```text\npow[0] = 1\npow[i + 1] = pow[i] * B % M\n\npref[0] = 0\npref[i + 1] = (pref[i] * B + value(s[i])) % M\n\nhash(l, r) = (pref[r] - pref[l] * pow[r - l] % M + M) % M\n```\n\n其中 `hash(l, r)` 對應半開區間 `s[l..r)`。這個公式讓同一個字串內任意子串 hash 查詢變成 `O(1)`。但 hash 相等只代表「可能相等」，嚴格解法仍要在 hash 相同時比對原字串。\n\n解題步驟：\n1. 預處理 `prefix` 與 `power`，讓 `hash(l, r)` 可在 `O(1)` 取得 `s[l..r)` 的取模 hash。\n2. 二分重複子字串長度 `len`。\n3. 對每個起點 `i` 取長度 `len` 的 hash；hash 只用來分桶，代表哪些子字串需要進一步比較。\n4. 因為 hash 有碰撞風險，hash 相同時再用 `s.compare(...)` 做嚴格字串比對；只有真的相等才回傳答案。\n\n**C++：Rolling hash + 二分答案**\n\n```cpp\n// LeetCode 1044. Longest Duplicate Substring\nclass Solution {\n  static constexpr long long kMod = 1000000007;\n  static constexpr long long kBase = 911382323;\n\n  string s;\n  vector<long long> pref, power;\n\n  long long get_hash(int left, int len) {\n    int right = left + len;\n    return (pref[right] - pref[left] * power[len] % kMod + kMod) % kMod;\n  }\n\n  bool same_substring(int a, int b, int len) { return s.compare(a, len, s, b, len) == 0; }\n\n  int duplicate_start(int len) {\n    if (len == 0) {\n      return 0;\n    }\n    unordered_map<long long, vector<int>> seen;\n    for (int i = 0; i + len <= (int)s.size(); ++i) {\n      long long hash = get_hash(i, len);\n      for (int previous : seen[hash]) {\n        if (same_substring(previous, i, len)) {\n          return i;\n        }\n      }\n      seen[hash].push_back(i);\n    }\n    return -1;\n  }\n\n public:\n  string longestDupSubstring(string input) {\n    s = input;\n    int n = s.size();\n    pref.assign(n + 1, 0);\n    power.assign(n + 1, 1);\n\n    for (int i = 0; i < n; ++i) {\n      int value = s[i] - 'a' + 1;\n      pref[i + 1] = (pref[i] * kBase + value) % kMod;\n      power[i + 1] = power[i] * kBase % kMod;\n    }\n\n    int low = 0, high = n, bestStart = 0, bestLen = 0;\n    while (low <= high) {\n      int mid = low + (high - low) / 2;\n      int start = duplicate_start(mid);\n      if (start != -1) {\n        bestStart = start;\n        bestLen = mid;\n        low = mid + 1;\n      } else {\n        high = mid - 1;\n      }\n    }\n    return s.substr(bestStart, bestLen);\n  }\n};\n```\n\n這題的核心不是「找某個 pattern 出現在哪裡」，而是「大量同長度子字串彼此是否相等」。這就是 KMP 與 rolling hash 的分界：固定 pattern 用 KMP；任意子串比較用 hash、suffix array、suffix automaton 或 suffix tree。\n\n**競程課程講義：字串演算法是比較工具，不是答案本身**\n\n**Pattern 1：KMP / prefix function**\n\n訊號：固定 pattern 匹配、多次問 border、週期性。`pi[i]` 是 `s[0..i]` 的最長相等真前後綴長度。模板提供 prefix function 與所有匹配起點。\n\n```cpp\nstruct KMP {\n  static vector<int> prefix_function(const string& s) {\n    vector<int> pi(s.size());\n    for (int i = 1; i < (int)s.size(); ++i) {\n      int j = pi[i - 1];\n      while (j > 0 && s[i] != s[j]) {\n        j = pi[j - 1];\n      }\n      if (s[i] == s[j]) {\n        j++;\n      }\n      pi[i] = j;\n    }\n    return pi;\n  }\n\n  static vector<int> find_all(const string& text, const string& pattern) {\n    if (pattern.empty()) {\n      return {};\n    }\n\n    vector<int> pi = prefix_function(pattern);\n    vector<int> positions;\n    int matched = 0;\n\n    for (int i = 0; i < (int)text.size(); ++i) {\n      while (matched > 0 && text[i] != pattern[matched]) {\n        matched = pi[matched - 1];\n      }\n      if (text[i] == pattern[matched]) {\n        matched++;\n      }\n      if (matched == (int)pattern.size()) {\n        positions.push_back(i - (int)pattern.size() + 1);\n        matched = pi[matched - 1];\n      }\n    }\n    return positions;\n  }\n};\n```\n\n**Pattern 2：Z function**\n\n訊號：大量比較 `s[i...]` 與整個字串前綴。Z-box `[left, right]` 維護目前最右匹配區間。模板提供 Z 陣列與 pattern 搜尋。\n\n```cpp\nstruct ZAlgorithm {\n  static vector<int> z_function(const string& s) {\n    int n = s.size();\n    vector<int> z(n);\n    for (int i = 1, left = 0, right = 0; i < n; ++i) {\n      if (i <= right) {\n        z[i] = min(right - i + 1, z[i - left]);\n      }\n      while (i + z[i] < n && s[z[i]] == s[i + z[i]]) {\n        z[i]++;\n      }\n      if (i + z[i] - 1 > right) {\n        left = i;\n        right = i + z[i] - 1;\n      }\n    }\n    return z;\n  }\n\n  static vector<int> find_all(const string& text, const string& pattern) {\n    if (pattern.empty()) {\n      return {};\n    }\n\n    string combined = pattern + '#' + text;\n    vector<int> z = z_function(combined);\n    vector<int> positions;\n    int offset = pattern.size() + 1;\n\n    for (int i = offset; i < (int)combined.size(); ++i) {\n      if (z[i] >= (int)pattern.size()) {\n        positions.push_back(i - offset);\n      }\n    }\n    return positions;\n  }\n};\n```\n\n**Pattern 3：Rolling hash**\n\n訊號：任意兩段子串要反覆比較。`get(l, r)` 使用半開區間 `s[l..r)`；若題目需要嚴格正確，hash 只用來快速排除不相等，hash 相同時再比對原字串。\n\n```cpp\nstruct RollingHash {\n  static constexpr long long kMod = 1000000007;\n  static constexpr long long kBase = 911382323;\n\n  string s;\n  vector<long long> prefix, power;\n\n  RollingHash(const string& input) : s(input), prefix(s.size() + 1), power(s.size() + 1, 1) {\n    for (int i = 0; i < (int)s.size(); ++i) {\n      int value = s[i];\n      prefix[i + 1] = (prefix[i] * kBase + value) % kMod;\n      power[i + 1] = power[i] * kBase % kMod;\n    }\n  }\n\n  long long get(int l, int r) const { return (prefix[r] - prefix[l] * power[r - l] % kMod + kMod) % kMod; }\n\n  bool same(int l1, int l2, int len) const {\n    if (get(l1, l1 + len) != get(l2, l2 + len)) {\n      return false;\n    }\n    return s.compare(l1, len, s, l2, len) == 0;\n  }\n};\n```\n\n**Pattern 4：Trie**\n\n訊號：多個字典詞共用前綴，或從 target 某位置往後匹配多個候選詞。模板以小寫英文字母為例；若字元集不同，把 `next[26]` 換成 `unordered_map<char, int>`。\n\n```cpp\nstruct Trie {\n  struct Node {\n    int next[26];\n    bool isWord = false;\n\n    Node() { fill(std::begin(next), std::end(next), -1); }\n  };\n\n  vector<Node> nodes;\n\n  Trie() : nodes(1) {}\n\n  void insert(const string& word) {\n    int cur = 0;\n    for (char ch : word) {\n      int c = ch - 'a';\n      if (nodes[cur].next[c] == -1) {\n        nodes[cur].next[c] = nodes.size();\n        nodes.emplace_back();\n      }\n      cur = nodes[cur].next[c];\n    }\n    nodes[cur].isWord = true;\n  }\n\n  bool contains(const string& word) const {\n    int node = walk(word);\n    return node != -1 && nodes[node].isWord;\n  }\n\n  bool starts_with(const string& prefix) const { return walk(prefix) != -1; }\n\n  int walk(const string& s) const {\n    int cur = 0;\n    for (char ch : s) {\n      int c = ch - 'a';\n      if (nodes[cur].next[c] == -1) {\n        return -1;\n      }\n      cur = nodes[cur].next[c];\n    }\n    return cur;\n  }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3170 | [刪除星號以後字典序最小的字串](https://leetcode.cn/problems/lexicographically-minimum-string-after-removing-stars) | 1772 |\n| 1153 | [字串轉化](https://leetcode.cn/problems/string-transforms-into-another-string) | 1949 |\n| 2156 | [查詢給定雜湊值的子串](https://leetcode.cn/problems/find-substring-with-given-hash-value) | 2063 |\n| 3291 | [形成目標字串需要的最少字串數 I](https://leetcode.cn/problems/minimum-number-of-valid-strings-to-form-target-i) | 2082 |\n| 1096 | [花括號展開 II](https://leetcode.cn/problems/brace-expansion-ii) | 2349 |\n| 2468 | [根據限制分割訊息](https://leetcode.cn/problems/split-message-based-on-limit) | 2382 |\n| 1044 | [最長重複子串](https://leetcode.cn/problems/longest-duplicate-substring) | 2429 |\n| 2272 | [最大波動的子字串](https://leetcode.cn/problems/substring-with-largest-variance) | 2516 |\n| 3292 | [形成目標字串需要的最少字串數 II](https://leetcode.cn/problems/minimum-number-of-valid-strings-to-form-target-ii) | 2662 |\n| 2977 | [轉換字串的最小成本 II](https://leetcode.cn/problems/minimum-cost-to-convert-string-ii) | 2696 |"
    },
    {
      "id": 6,
      "title": "5. 資料結構與離線處理",
      "summary": "**觀念起點**\n\n資料結構題要先寫出查詢與更新。題目如果要求即時最大值、前驅後繼、區間和、歷史版本或刪除過期候選，就代表單純陣列或 hash map 不足，需要一個能維護不變式的索引。\n\nheap 適合動態取最大或最小，但不擅長任意刪除；set/map 適合有序查詢；Fenwick 適合單點更新與前綴統計；segment tree 適合區間合併資訊；離線處理則常用排序把查詢門檻變成單調。\n\n若同一份資料有多個查詢角度，要分清楚 canonical state 與 derived index。更新時先刪舊索引、更新真實資料、再插入新索引，否則會留下過期資料。\n\n進階索引與離線：權值線段樹、動態開點、線段樹合併、可持久化線段樹（主席樹）求區間第 k 小；莫隊與線段樹分治處理離線區間查詢或時間軸上的加入與刪除。\n\n資料結構題的關鍵不是背容器名稱，而是維護不變式。從零開始可以這樣想：題目有一串操作，每次操作後，你要快速回答某種查詢。資料結構就是讓更新和查詢都維持在可接受複雜度的索引。不變式一旦破壞，後面的查詢就會錯。\n\n**選型提示**：\n- 動態最大/最小：heap；若元素會失效，常用懶刪除。\n- 有序前驅/後繼、排名、區間：`set` / `map` / Fenwick / segment tree。\n- 歷史版本查詢：每個 key 存 `(time, value)` 序列，查詢時二分。\n- 查詢可離線排序：把資料與 query 依門檻排序，只加入當前可用元素。\n\n**系統例題：Snapshot Array**\n\n**完整問題**：支援 `set(index, val)`、`snap()` 回傳快照 id、`get(index, snap_id)` 回傳該快照時 index 的值。\n\n解題步驟：\n1. 不要每次 `snap` 複製整個陣列，會是 `O(n*snap)`。\n2. 對每個 index 只記錄值改變的時間序列，例如 `history[index] = [(snap_id, value)]`。\n3. `set` 時，如果同一個 snap_id 已經寫過此 index，直接覆蓋最後一筆；否則追加新紀錄。\n4. `get` 時，在此 index 的歷史中找 `snap_id` 以下最後一筆。\n\n**C++：每個位置一條歷史序列**\n\n```cpp\n// snapshot array with binary search over per-index history\nclass SnapshotArray {\n  int current_snap = 0;\n  vector<vector<pair<int, int>>> history;\n\n public:\n  SnapshotArray(int length) : history(length, {{0, 0}}) {}\n\n  void set(int index, int val) {\n    auto& h = history[index];\n    if (h.back().first == current_snap) {\n      h.back().second = val;\n    } else {\n      h.push_back({current_snap, val});\n    }\n  }\n\n  int snap() { return current_snap++; }\n\n  int get(int index, int snap_id) {\n    auto& h = history[index];\n    int pos = upper_bound(h.begin(), h.end(), make_pair(snap_id, INT_MAX)) - h.begin() - 1;\n    return h[pos].second;\n  }\n};\n```\n\n`set` 均攤 `O(1)`，`snap` 是 `O(1)`，`get` 是 `O(log changesAtIndex)`。面試中要明確說出 canonical state 是每個 index 的歷史，而不是某個完整陣列。\n\n**競程課程講義：資料結構題先寫不變式**\n\n**Pattern 1：Heap + lazy deletion**\n\n訊號：要動態取最大/最小，但舊元素可能被更新或刪除。heap 不支援任意刪除，所以保留最新版本表，彈出時檢查是否過期。\n\n```cpp\nclass LazyMaxHeap {\n  priority_queue<pair<int, int>> heap;  // value, id\n  unordered_map<int, int> latest;\n\n  void discard_stale() {\n    while (!heap.empty()) {\n      auto [value, id] = heap.top();\n      auto it = latest.find(id);\n      if (it != latest.end() && it->second == value) {\n        break;\n      }\n      heap.pop();\n    }\n  }\n\n public:\n  void update(int id, int value) {\n    latest[id] = value;\n    heap.push({value, id});\n  }\n\n  void erase(int id) { latest.erase(id); }\n\n  pair<int, int> top() {\n    discard_stale();\n    return heap.empty() ? pair<int, int>{-1, -1} : heap.top();\n  }\n};\n```\n\n**Pattern 2：Ordered set / map**\n\n訊號：需要 predecessor、successor、動態排序、tie-breaker。例題：維護座位區間或即時排名。\n\n```cpp\nstruct OrderedSetHelper {\n  static int lower_bound_value(const set<int>& values, int x) {\n    auto it = values.lower_bound(x);\n    return it == values.end() ? -1 : *it;\n  }\n\n  static int predecessor_value(const set<int>& values, int x) {\n    auto it = values.lower_bound(x);\n    if (it == values.begin()) {\n      return -1;\n    }\n    return *prev(it);\n  }\n};\n```\n\n**Pattern 3：Fenwick tree 離線查詢**\n\n訊號：查詢可以排序後處理，且需要 prefix sum / count。例題：統計每個 query 中小於等於 x 的元素數。\n\n```cpp\nclass Fenwick {\n  vector<long long> bit;\n\n public:\n  Fenwick(int n) : bit(n + 1, 0) {}\n\n  void add(int index, long long delta) {\n    for (++index; index < (int)bit.size(); index += index & -index) {\n      bit[index] += delta;\n    }\n  }\n\n  long long prefix_sum(int right) const {  // sum of [0, right)\n    long long sum = 0;\n    for (; right > 0; right -= right & -right) {\n      sum += bit[right];\n    }\n    return sum;\n  }\n\n  long long range_sum(int left, int right) const {  // sum of [left, right)\n    return prefix_sum(right) - prefix_sum(left);\n  }\n};\n```\n\n**Pattern 4：歷史版本陣列**\n\n訊號：set 後 snap，查過去版本。每個 index 存時間序列，再二分最後一個 `time <= queryTime`。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1146 | [快照陣列](https://leetcode.cn/problems/snapshot-array) | 1771 |\n| 3092 | [最高頻率的 ID](https://leetcode.cn/problems/most-frequent-ids) | 1793 |\n| 1001 | [網格照明](https://leetcode.cn/problems/grid-illumination) | 1873 |\n| 895 | [最大頻率堆疊](https://leetcode.cn/problems/maximum-frequency-stack) | 2028 |\n| 2402 | [會議室 III](https://leetcode.cn/problems/meeting-rooms-iii) | 2093 |\n| 1851 | [包含每個查詢的最小區間](https://leetcode.cn/problems/minimum-interval-to-include-each-query) | 2286 |"
    },
    {
      "id": 7,
      "title": "6. 二分、貪心與滑動視窗",
      "summary": "**觀念起點**\n\n二分、貪心與滑動視窗常被混在同一章，是因為它們都依賴某種單調性。二分需要答案可行性單調；滑動視窗需要左右端移動後能維護合法性；貪心需要證明局部選擇不會破壞最優解。\n\n二分答案時，`check(x)` 只回答可不可行，不負責求最佳。滑動視窗時，窗口狀態必須能在加入右端與移除左端後快速更新。貪心時，必須能用交換論證、反悔或必要條件說明選擇安全。\n\n如果找不到單調性，就不要硬套這三種方法。這通常代表要改用 DP、搜尋或資料結構維護候選集合。\n\n三者皆依賴單調性：二分答案的 `check` 為階梯函數、浮點值域用固定迭代次數；貪心以交換論證、反悔或擬陣證明；滑動視窗的「恰好 K」可拆成「至多 K」減「至多 K-1」。\n\n這章處理「答案不是直接算，而是找邊界、做局部選擇或維護一段連續區間」的題型。二分、貪心、滑動視窗都很常見，但也最容易被誤用。選方法前先問：答案是否有單調性？局部選擇是否能用交換論證證明？窗口合法性是否能隨左右指標增減而維護？\n\n**三種模型**：\n- 二分答案：把「求最小可行值」改成「給定 x，是否可行」。可行性必須單調。\n- 貪心：每一步做局部最優，並能證明任何最優解都可交換成包含這個選擇。\n- 滑動視窗：處理連續子陣列/子字串，右端擴張、左端收縮，維護窗口合法。\n\n**系統例題：最長子字串，最多替換 k 個字元後全相同**\n\n**完整問題**：給大寫字串 s，最多替換 k 個字元，求可變成同一字元的最長子字串。\n\n解題步驟：\n1. 對一個窗口，如果長度是 `len`，最多字元出現次數是 `max_freq`，那需要替換 `len - max_freq` 個字元。\n2. 窗口合法條件：`len - max_freq <= k`。\n3. 右指標逐步擴張並更新字元計數。\n4. 若窗口不合法，移動左指標直到合法。\n5. 每次合法時更新答案。\n\n**C++：滑動視窗維護合法性**\n\n```cpp\n// longest repeating character replacement\nint characterReplacement(string s, int k) {\n  vector<int> cnt(26, 0);\n  int left = 0, max_freq = 0, answer = 0;\n\n  for (int right = 0; right < (int)s.size(); ++right) {\n    int x = s[right] - 'A';\n    cnt[x]++;\n    max_freq = max(max_freq, cnt[x]);\n\n    while ((right - left + 1) - max_freq > k) {\n      cnt[s[left] - 'A']--;\n      left++;\n    }\n    answer = max(answer, right - left + 1);\n  }\n  return answer;\n}\n```\n\n這段程式中的 `max_freq` 不必在左移時降低，因為它只會讓窗口偶爾看起來偏寬，但答案長度仍然只在曾經存在足夠高頻字元的尺度上更新。若面試官追問，可改成每次重算 26 個字母的最大值，仍是 `O(26n)`。\n\n**競程課程講義：二分、貪心、視窗分開證明**\n\n**Pattern 1：二分答案**\n\n訊號：問最小可行值或最大可行值，且可行性單調。課程中要先明確定義 predicate：`can_finish(x)` 的 true 區間在哪一側。\n\n例題：以最小速度吃完香蕉。速度越大越容易完成，所以 `can_finish(speed)` 單調。\n\n```cpp\nint minEatingSpeed(vector<int>& piles, int h) {\n  auto can_finish = [&piles, h](int speed) {\n    long long hours = 0;\n    for (int x : piles) {\n      hours += (x + speed - 1) / speed;\n    }\n    return hours <= h;\n  };\n  int left = 1, right = *max_element(piles.begin(), piles.end());\n  while (left < right) {\n    int mid = left + (right - left) / 2;\n    if (can_finish(mid)) {\n      right = mid;\n    } else {\n      left = mid + 1;\n    }\n  }\n  return left;\n}\n```\n\n**Pattern 2：貪心 + 交換論證**\n\n訊號：排序後每次選最早結束、最小成本、最大收益等局部選擇。必須能說明任意最優解可替換成你的局部選擇而不變差。\n\n例題：最多不重疊區間，選結束時間最早的區間。\n\n```cpp\nint eraseOverlapIntervals(vector<vector<int>>& intervals) {\n  sort(intervals.begin(), intervals.end(), [](const auto& lhs, const auto& rhs) { return lhs[1] < rhs[1]; });\n  int keep = 0, last_end = INT_MIN;\n  for (const auto& interval : intervals) {\n    if (interval[0] >= last_end) {\n      keep++;\n      last_end = interval[1];\n    }\n  }\n  return intervals.size() - keep;\n}\n```\n\n**Pattern 3：滑動視窗**\n\n訊號：子陣列或子字串連續，右端加入元素、左端移除元素後能維護合法性。若條件不能局部維護，視窗通常不適合。\n\n```cpp\nint longestOnes(vector<int>& nums, int k) {\n  int left = 0, zeros = 0, answer = 0;\n  for (int right = 0; right < (int)nums.size(); ++right) {\n    zeros += nums[right] == 0;\n    while (zeros > k) {\n      zeros -= nums[left++] == 0;\n    }\n    answer = max(answer, right - left + 1);\n  }\n  return answer;\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1658 | [將 x 減到 0 的最小運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-x-to-zero) | 1817 |\n| 995 | [K 連續位的最小翻轉次數](https://leetcode.cn/problems/minimum-number-of-k-consecutive-bit-flips) | 1835 |\n| 1871 | [跳躍遊戲 VII](https://leetcode.cn/problems/jump-game-vii) | 1896 |\n| 2106 | [摘水果](https://leetcode.cn/problems/maximum-fruits-harvested-after-at-most-k-steps) | 2062 |\n| 843 | [猜猜這個單詞](https://leetcode.cn/problems/guess-the-word) | 2078 |\n| 2444 | [統計定界子陣列的數目](https://leetcode.cn/problems/count-subarrays-with-fixed-bounds) | 2093 |\n| 2141 | [同時執行 N 臺電腦的最長時間](https://leetcode.cn/problems/maximum-running-time-of-n-computers) | 2265 |"
    },
    {
      "id": 8,
      "title": "7. 資料平行與資料流",
      "summary": "**觀念起點**\n\n資料平行與資料流題先把處理流程拆成 map、filter、scan、reduce。map 代表每筆資料獨立轉換，reduce 代表多筆資料合併成摘要，scan 代表每個位置需要前面的累積資訊。\n\n能否平行，取決於操作是否有相依性。每個元素獨立可平行；需要前綴狀態時要做 scan；需要全域 top-k 時可以先分 shard，各 shard 留候選，再做一次合併。\n\n資料流限制下不能保存全部輸入，只能保存足以回答問題的摘要，例如大小為 k 的 heap、頻率表、滑動窗口統計或 sketch。\n\n能否平行取決於相依性：map 可平行、scan 需前綴、reduce 需結合律；全域 top-k 可先分片（shard），各片留候選再合併。\n\n這一章補強資料平行、資料流與吞吐量思維。即使一般 SWE 面試不要求寫 GPU code，Google 類面試可能會追問：哪些步驟能平行、哪些步驟需要同步、資料是否連續讀寫、如何在記憶體有限或流式資料下得到答案。從零開始理解，可以把演算法拆成 map、filter、scan、reduce 四種常見資料流操作。\n\n**基本概念**：\n- map：每個元素獨立轉換，天然可平行。\n- reduce：多個元素合併成一個值，例如 sum、max、xor，需要合併順序或結合律。\n- prefix scan：每個位置需要前面累積結果，例如前綴和、排名、壓縮後位置。\n- streaming：資料只能看一遍或不能全部放進記憶體，需要維護足夠小的摘要。\n\n**系統例題：資料流中的前 k 大元素**\n\n**完整問題**：數字源源不斷到來，不能保存全部資料，隨時要知道目前最大的 k 個數。\n\n解題步驟：\n1. 若保存全部再排序，空間是 `O(n)`，不符合資料流需求。\n2. 只保存目前前 k 大即可。用大小為 k 的最小堆，堆頂是目前第 k 大。\n3. 新元素若堆未滿就加入；若大於堆頂，彈出堆頂再加入；否則丟棄。\n4. 查詢時堆中就是前 k 大，若要排序輸出再複製排序。\n\n**C++：Streaming Top-K**\n\n```cpp\n// maintain top k largest values in a stream\nclass TopKStream {\n  int k;\n  priority_queue<int, vector<int>, greater<int>> minHeap;\n\n public:\n  TopKStream(int k) : k(k) {}\n\n  void add(int x) {\n    if ((int)minHeap.size() < k) {\n      minHeap.push(x);\n    } else if (x > minHeap.top()) {\n      minHeap.pop();\n      minHeap.push(x);\n    }\n  }\n\n  vector<int> topKDescending() const {\n    auto copy = minHeap;\n    vector<int> values;\n    while (!copy.empty()) {\n      values.push_back(copy.top());\n      copy.pop();\n    }\n    sort(values.rbegin(), values.rend());\n    return values;\n  }\n};\n```\n\n每筆資料處理 `O(log k)`，空間 `O(k)`。如果要平行化，可讓每個 shard 維護自己的 top-k，最後把所有 shard 的候選再做一次 top-k reduction。這就是 map-reduce 思路。\n\n**競程課程講義：資料平行題用 map / reduce / scan 分解**\n\n**Pattern 1：Map**\n\n每個元素獨立轉換，不讀寫其他位置。例題：把每個元素平方或計算每筆資料的局部貢獻。平行化時沒有資料競爭。\n\n```cpp\nvector<long long> squareAll(const vector<int>& a) {\n  vector<long long> out(a.size());\n  for (int i = 0; i < (int)a.size(); ++i) {\n    out[i] = 1LL * a[i] * a[i];\n  }\n  return out;\n}\n```\n\n**Pattern 2：Reduce**\n\n多個值合併成一個值。操作最好具備結合律，例如 sum、max、min、xor，才能分塊後再合併。\n\n```cpp\nlong long blockReduceSum(const vector<int>& a, int block_size) {\n  vector<long long> partial;\n  for (int l = 0; l < (int)a.size(); l += block_size) {\n    long long s = 0;\n    for (int i = l; i < min((int)a.size(), l + block_size); ++i) {\n      s += a[i];\n    }\n    partial.push_back(s);\n  }\n  return accumulate(partial.begin(), partial.end(), 0LL);\n}\n```\n\n**Pattern 3：Prefix scan**\n\n每個位置需要前面累積資訊。競程常用在前綴和、差分還原、壓縮後位置、平行 compaction。\n\n```cpp\nvector<long long> exclusiveScan(const vector<int>& a) {\n  vector<long long> scan(a.size() + 1);\n  for (int i = 0; i < (int)a.size(); ++i) {\n    scan[i + 1] = scan[i] + a[i];\n  }\n  return scan;\n}\n```\n\n**Pattern 4：Streaming sketch**\n\n資料不能全存時，只維護摘要。Top-K 用 min-heap，頻率估計可用 hash map 或 sketch；要說清楚空間上限。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2962 | [統計最大元素出現至少 K 次的子陣列](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times) | 1701 |\n| 1368 | [使網格圖至少有一條有效路徑的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid) | 2069 |\n| 3821 | [二進位制中恰好K個1的第N小整數](https://leetcode.cn/problems/find-nth-smallest-integer-with-k-one-bits) | 2069 |\n| 3123 | [最短路徑中的邊](https://leetcode.cn/problems/find-edges-in-shortest-paths) | 2093 |\n| 2223 | [構造字串的總得分和](https://leetcode.cn/problems/sum-of-scores-of-built-strings) | 2220 |\n| 882 | [細分圖中的可到達節點](https://leetcode.cn/problems/reachable-nodes-in-subdivided-graph) | 2328 |\n| 3797 | [統計在矩形格子裡移動的路徑數目](https://leetcode.cn/problems/count-routes-to-climb-a-rectangular-grid) | 2376 |\n| 2577 | [在網格圖中訪問一個格子的最少時間](https://leetcode.cn/problems/minimum-time-to-visit-a-cell-in-a-grid) | 2382 |\n| 3404 | [統計特殊子序列的數目](https://leetcode.cn/problems/count-special-subsequences) | 2445 |\n| 3410 | [刪除所有值為某個元素後的最大子陣列和](https://leetcode.cn/problems/maximize-subarray-sum-after-removing-all-occurrences-of-one-element) | 2844 |"
    },
    {
      "id": 9,
      "title": "8. 資源限制與位元狀態",
      "summary": "**觀念起點**\n\n資源限制題要先把空間上限寫進狀態設計。若只能保存固定大小資料，就不能依賴完整歷史；若狀態是布林集合且元素數小，bitmask 可以把集合壓成整數。\n\n位元狀態的每一位都要有明確語意：第 i 個物品是否已選、某個字母出現奇偶、某把鑰匙是否已拿到。位運算只是壓縮表示，真正的轉移仍要對應回題意。\n\n這類題也常考溢位與邊界。加總、乘法、距離與容量最好先估最大值，必要時使用 `long long` 與安全的無限大常數。\n\n位元技巧：`for (int s = m; s; s = (s - 1) & m)` 枚舉子集（合計 `O(3^n)`）、lowbit、popcount DP；`bitset` 把布林轉移壓到 `O(n^2/64)`；加總與乘法前轉 `long long`。\n\n這一章更在意資源邊界：記憶體是否固定、是否能避免碎片、整數是否溢位、狀態是否能用 bit mask 表示、操作是否有最壞情況延遲。從零開始看，這類題通常不是問你會不會用 `vector`，而是問你能不能在限制下設計可靠行為。\n\n**複習重點**：\n- 固定容量：明確處理 full/empty，不讓資料結構無限制成長。\n- ring buffer：用陣列和環狀 index 支援佇列，適合固定記憶體。\n- bit mask：用一個整數表示多個布林狀態，集合操作通常是 `O(1)`。\n- 邊界條件：整數溢位、負數、重複釋放、wrap-around index、capacity 為 0。\n\n**系統例題：固定容量循環佇列**\n\n**完整問題**：實作固定容量 queue，支援 `push`、`pop`，不能動態擴容。\n\n解題步驟：\n1. 用長度為 capacity 的陣列保存資料。\n2. `head` 指向下一個要 pop 的位置，`tail` 指向下一個要 push 的位置。\n3. `count` 區分空和滿；否則 `head == tail` 會同時可能代表空或滿。\n4. 每次指標移動都用 `(idx + 1) % capacity` wrap around。\n\n**C++：Ring Buffer**\n\n```cpp\n// fixed-capacity circular queue\nclass RingBuffer {\n  vector<int> buf;\n  int head = 0;\n  int tail = 0;\n  int count = 0;\n\n public:\n  RingBuffer(int capacity) : buf(capacity) {}\n\n  bool push(int x) {\n    if (buf.empty() || count == (int)buf.size()) {\n      return false;\n    }\n    buf[tail] = x;\n    tail = (tail + 1) % buf.size();\n    count++;\n    return true;\n  }\n\n  bool pop(int& out) {\n    if (count == 0) {\n      return false;\n    }\n    out = buf[head];\n    head = (head + 1) % buf.size();\n    count--;\n    return true;\n  }\n\n  int size() const { return count; }\n  bool empty() const { return count == 0; }\n};\n```\n\n若題目改成多執行緒，就要討論鎖、原子操作或 single-producer/single-consumer 的限制；若改成狀態集合，則可把 `vector<bool>` 換成 bit mask 或 bitset 來降低空間。\n\n**競程課程講義：資源限制題要把空間上限寫進設計**\n\n**Pattern 1：固定容量容器**\n\n訊號：不能動態配置、需要 bounded memory、queue 或 log buffer。ring buffer 用 `head/tail/count` 區分空和滿。\n\n**Pattern 2：Bit mask 狀態集合**\n\n訊號：布林狀態數小於 20 到 25。集合加入、刪除、查詢都可用位運算。\n\n```cpp\nstruct BitMaskSet {\n  static bool contains(int mask, int index) { return mask & (1 << index); }\n\n  static int add(int mask, int index) { return mask | (1 << index); }\n\n  static int remove(int mask, int index) { return mask & ~(1 << index); }\n\n  static int toggle(int mask, int index) { return mask ^ (1 << index); }\n};\n```\n\n**Pattern 3：溢位保護**\n\n競程中 `int` 很容易在加總、乘法、距離中溢位。模板：乘法前轉 `long long`，kInf 設成不會在加法後溢位的值。\n\n```cpp\nstruct SafeMath {\n  static constexpr long long kInf = 4e18;\n\n  static long long add_capped(long long a, long long b) {\n    if (a >= kInf || b >= kInf) {\n      return kInf;\n    }\n    if (a > kInf - b) {\n      return kInf;\n    }\n    return a + b;\n  }\n\n  static long long mul_capped(long long a, long long b) {\n    if (a == 0 || b == 0) {\n      return 0;\n    }\n    if (a > kInf / b) {\n      return kInf;\n    }\n    return a * b;\n  }\n};\n```\n\n**Pattern 4：Free list**\n\n訊號：需要反覆 allocate/free 固定數量節點。用陣列存節點，用 stack 保存可重用 index，避免碎片。\n\n```cpp\ntemplate <class T>\nclass ObjectPool {\n  vector<T> value;\n  vector<int> free_ids;\n\n public:\n  ObjectPool(int capacity) : value(capacity) {\n    for (int i = capacity - 1; i >= 0; --i) {\n      free_ids.push_back(i);\n    }\n  }\n\n  int allocate(const T& x) {\n    if (free_ids.empty()) {\n      return -1;\n    }\n    int id = free_ids.back();\n    free_ids.pop_back();\n    value[id] = x;\n    return id;\n  }\n\n  void release(int id) { free_ids.push_back(id); }\n\n  T& operator[](int id) { return value[id]; }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1011 | [在 D 天內送達包裹的能力](https://leetcode.cn/problems/capacity-to-ship-packages-within-d-days) | 1725 |\n| 2502 | [設計記憶體分配器](https://leetcode.cn/problems/design-memory-allocator) | 1746 |\n| 875 | [愛吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) | 1766 |\n| 3281 | [範圍內整數的最大得分](https://leetcode.cn/problems/maximize-score-of-numbers-in-ranges) | 1768 |\n| 3800 | [使二進位制字串相等的最小成本](https://leetcode.cn/problems/minimum-cost-to-make-two-binary-strings-equal) | 1841 |\n| 2439 | [最小化陣列中的最大值](https://leetcode.cn/problems/minimize-maximum-of-array) | 1965 |\n| 3811 | [交替按位異或分割的數目](https://leetcode.cn/problems/number-of-alternating-xor-partitions) | 2006 |\n| 855 | [考場就座](https://leetcode.cn/problems/exam-room) | 2067 |\n| 2616 | [最小化數對的最大差值](https://leetcode.cn/problems/minimize-the-maximum-difference-of-pairs) | 2155 |\n| 2968 | [執行操作使頻率分數最大](https://leetcode.cn/problems/apply-operations-to-maximize-frequency-score) | 2444 |"
    },
    {
      "id": 10,
      "title": "9. 平台 API 與系統資料結構",
      "summary": "**觀念起點**\n\nAPI 題不是只算一次答案，而是設計一個物件長期支援多種操作。第一步是列出每個方法的輸入、輸出與目標複雜度，再決定哪份資料是事實來源。\n\n若需要不同查詢角度，就建立 derived indexes。例如 `id -> value` 是真實資料，`group -> ordered set` 是為了查最大值，`time -> history` 是為了查版本。每次 update 必須同步所有索引。\n\ntie-breaker 也要先定義。若同分要字典序最小、時間最早或 id 最小，排序 comparator 必須完整描述，避免 set 或 heap 中出現不穩定結果。\n\n常見元件：LRU（雙向鏈表 + hash 達 O(1) get/put）、Time-based key-value（每 key 一條時間序列、查詢用二分）；多索引更新時先刪舊索引、改真實資料、再插新索引。\n\n這一章常以 API 設計形式出現：題目不只要求算一次答案，而是要求一個物件支援多個方法。從零開始做 API 題，先列出每個方法的輸入、輸出、目標複雜度，再決定 canonical state 和衍生索引。只要有多個索引，更新時就必須同步刪舊插新，否則會產生 stale entry。\n\n**設計流程**：\n- 列 API：`add`、`remove`、`update`、`query` 各自要什麼複雜度。\n- 找事實來源：哪份 map 保存最可信的實體資料。\n- 建衍生索引：為排名、最值、查 id、查 group 等查詢建立 set、heap 或 map。\n- 定義 tie-breaker：同分時用 name、id 或時間，避免排序不穩。\n\n**系統例題：食物評分系統**\n\n**完整問題**：每個 food 有 cuisine 和 rating，支援修改某 food 的 rating，查某 cuisine 中 rating 最高、同分字典序最小的 food。\n\n解題步驟：\n1. canonical state：`food -> (cuisine, rating)`。\n2. 每個 cuisine 維護一個有序 set，排序規則是 rating 高優先，name 小優先。\n3. 更新 rating 時，先從原 cuisine 的 set 刪掉舊 `(rating, food)`，再插入新值。\n4. 查詢最高評分時取 set begin。\n\n**C++：多索引 API 設計**\n\n```cpp\n// food ratings with per-cuisine ordered index\nclass FoodRatings {\n  struct Entry {\n    int rating;\n    string food;\n  };\n\n  struct Cmp {\n    bool operator()(const Entry& a, const Entry& b) const {\n      if (a.rating != b.rating) {\n        return a.rating > b.rating;\n      }\n      return a.food < b.food;\n    }\n  };\n\n  unordered_map<string, string> cuisineOf;\n  unordered_map<string, int> ratingOf;\n  unordered_map<string, set<Entry, Cmp>> byCuisine;\n\n public:\n  FoodRatings(vector<string>& foods, vector<string>& cuisines, vector<int>& ratings) {\n    for (int i = 0; i < (int)foods.size(); ++i) {\n      cuisineOf[foods[i]] = cuisines[i];\n      ratingOf[foods[i]] = ratings[i];\n      byCuisine[cuisines[i]].insert({ratings[i], foods[i]});\n    }\n  }\n\n  void changeRating(string food, int newRating) {\n    string cuisine = cuisineOf[food];\n    int oldRating = ratingOf[food];\n    byCuisine[cuisine].erase({oldRating, food});\n    ratingOf[food] = newRating;\n    byCuisine[cuisine].insert({newRating, food});\n  }\n\n  string highestRated(string cuisine) { return byCuisine[cuisine].begin()->food; }\n};\n```\n\n每次更新 `O(log nCuisine)`，查詢 `O(1)` 取 begin。面試中要說清楚：set 裡保存的是衍生索引，`ratingOf` 才是目前真實分數。\n\n**競程課程講義：API 題等於多資料結構同步**\n\n**Pattern 1：Canonical state + derived indexes**\n\n先指定哪個 map 是事實來源，再用 set/heap 建查詢索引。更新時先刪舊索引，再更新 state，再插新索引。\n\n**Pattern 2：LRU Cache**\n\n訊號：`get` 和 `put` 都要 O(1)，並且要刪最久未使用。用 list 維護時間順序，用 unordered_map 指到 list iterator。\n\n```cpp\nclass LRUCache {\n  int capacity;\n  list<pair<int, int>> order;  // most recent at front: key, value\n  unordered_map<int, list<pair<int, int>>::iterator> position;\n\n  void touch(list<pair<int, int>>::iterator it) { order.splice(order.begin(), order, it); }\n\n public:\n  LRUCache(int capacity) : capacity(capacity) {}\n\n  int get(int key) {\n    auto it = position.find(key);\n    if (it == position.end()) {\n      return -1;\n    }\n    touch(it->second);\n    return it->second->second;\n  }\n\n  void put(int key, int value) {\n    if (capacity == 0) {\n      return;\n    }\n\n    auto it = position.find(key);\n    if (it != position.end()) {\n      it->second->second = value;\n      touch(it->second);\n      return;\n    }\n\n    if ((int)order.size() == capacity) {\n      position.erase(order.back().first);\n      order.pop_back();\n    }\n    order.push_front({key, value});\n    position[key] = order.begin();\n  }\n};\n```\n\n**Pattern 3：Time based key-value store**\n\n訊號：查某 key 在某時間點的值。每個 key 的歷史按 timestamp 遞增，查詢用二分。\n\n```cpp\nclass TimeMap {\n  unordered_map<int, vector<pair<int, int>>> history;  // key -> sorted (time, value)\n\n public:\n  void set(int key, int time, int value) { history[key].push_back({time, value}); }\n\n  int get(int key, int time) const {\n    auto it = history.find(key);\n    if (it == history.end()) {\n      return -1;\n    }\n\n    const auto& values = it->second;\n    auto pos = upper_bound(values.begin(), values.end(), make_pair(time, INT_MAX));\n    if (pos == values.begin()) {\n      return -1;\n    }\n    return prev(pos)->second;\n  }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2353 | [設計食物評分系統](https://leetcode.cn/problems/design-a-food-rating-system) | 1782 |\n| 3408 | [設計工作管理員](https://leetcode.cn/problems/design-task-manager) | 1807 |\n| 2034 | [股票價格波動](https://leetcode.cn/problems/stock-price-fluctuation) | 1832 |\n| 2013 | [檢測正方形](https://leetcode.cn/problems/detect-squares) | 1841 |\n| 3508 | [設計路由器](https://leetcode.cn/problems/implement-router) | 1851 |\n| 2296 | [設計一個文字編輯器](https://leetcode.cn/problems/design-a-text-editor) | 1912 |\n| 2102 | [序列順序查詢](https://leetcode.cn/problems/sequentially-ordinal-rank-tracker) | 2159 |\n| 1912 | [設計電影租借系統](https://leetcode.cn/problems/design-movie-rental-system) | 2182 |\n| 1825 | [求出 MK 平均值](https://leetcode.cn/problems/finding-mk-average) | 2396 |\n| 3534 | [針對圖的路徑存在性查詢 II](https://leetcode.cn/problems/path-existence-queries-in-a-graph-ii) | 2507 |"
    },
    {
      "id": 1101,
      "title": "10. 狀態空間搜尋與壓縮 BFS",
      "summary": "**觀念起點**\n\n狀態空間搜尋處理的是『位置不夠描述未來』的問題。訪問過哪些點、拿到哪些鑰匙、箱子在哪裡、剩餘幾次操作，都可能是狀態的一部分。\n\n多源 BFS 的想法是把所有合法起點同時放入 queue，距離都設為 0。這比枚舉每個起點各跑一次更乾淨，也能保留 BFS 第一次到達最短的性質。\n\n狀態壓縮前要估上界。`n * 2^n` 對 n 小時可行；若 n 太大，應該尋找貪心、DP 優化或不同建模。\n\n完整狀態常含 mask、剩餘資源或時間；邊權只有 0 與 1 用 0-1 BFS，操作代價不等用狀態圖上的 Dijkstra。\n\n這類題不是在原圖上搜尋，而是在「狀態圖」上搜尋。從零開始理解：如果只知道目前位置還不夠決定未來能走哪裡，就必須把其他資訊放進狀態。例如鑰匙集合、已訪問節點集合、箱子位置、玩家位置、剩餘操作次數。只要兩條路徑到達同一個完整狀態，後續選擇完全相同，就可以去重。\n\n**使用時機**：\n- 題目有「收集所有」、「訪問所有」、「拿到鑰匙後開門」、「推箱子」等描述。\n- 同一位置在不同 mask 下的可行操作不同。\n- 初始狀態可能有多個，例如可從任意節點開始走完所有節點。\n\n**系統例題：訪問所有節點的最短路**\n\n**完整問題**：給無權無向圖，可從任意節點出發，求訪問所有節點的最少邊數。\n\n解題步驟：\n1. 狀態是 `(node, mask)`，`mask` 表示已訪問節點集合。\n2. 邊權全是 1，所以用 BFS。\n3. 因為可以從任意節點出發，將所有 `(i, 1<<i)` 作為多源起點。\n4. 第一次取到 `mask == full` 的狀態，就是最短答案。\n\n**C++：多源 bitmask BFS**\n\n```cpp\n// shortest path visiting all nodes in an unweighted graph\nint shortestPathLength(vector<vector<int>>& graph) {\n  int n = graph.size();\n  int full = (1 << n) - 1;\n  vector<vector<int>> dist(n, vector<int>(1 << n, -1));\n  queue<pair<int, int>> q;\n\n  for (int i = 0; i < n; ++i) {\n    int mask = 1 << i;\n    dist[i][mask] = 0;\n    q.push({i, mask});\n  }\n\n  while (!q.empty()) {\n    auto [u, mask] = q.front();\n    q.pop();\n    if (mask == full) {\n      return dist[u][mask];\n    }\n\n    for (int v : graph[u]) {\n      int nextMask = mask | (1 << v);\n      if (dist[v][nextMask] != -1) {\n        continue;\n      }\n      dist[v][nextMask] = dist[u][mask] + 1;\n      q.push({v, nextMask});\n    }\n  }\n  return -1;\n}\n```\n\n狀態數是 `O(n*2^n)`，邊鬆弛約 `O(E*2^n)`。面試中要先估 n；如果 n 很大，bitmask BFS 就不是正確方向。\n\n**競程課程講義：狀態空間搜尋先估狀態數**\n\n**Pattern 1：位置 + mask**\n\n訊號：訪問所有點、收集所有 key、每個元素只有拿/沒拿。狀態數常是 `O(n 2^n)` 或 `O(mn 2^k)`。\n\n**Pattern 2：多源 BFS**\n\n若起點不固定，把所有合法起點距離設為 0 同時入隊。這比枚舉起點各跑一次 BFS 更乾淨。\n\n**Pattern 3：鑰匙與門**\n\n同一格在不同 key mask 下不是同一狀態；visited 必須包含 `(r, c, mask)`。\n\n```cpp\n// State transition skeleton for grid with keys.\nstruct State {\n  int r, c, mask;\n};\nint encode(int r, int c, int mask, int n, int keyCount) { return ((r * n + c) << keyCount) | mask; }\n```\n\n**Pattern 4：狀態圖 Dijkstra**\n\n若操作代價不同，不要硬套 BFS。把完整狀態當節點，轉移代價當邊權，跑 Dijkstra。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 847 | [訪問所有節點的最短路徑](https://leetcode.cn/problems/shortest-path-visiting-all-nodes) | 2201 |\n| 864 | [獲取所有鑰匙的最短路徑](https://leetcode.cn/problems/shortest-path-to-get-all-keys) | 2259 |\n| 1263 | [推箱子](https://leetcode.cn/problems/minimum-moves-to-move-a-box-to-their-target-location) | 2297 |\n| 2612 | [最少翻轉運算元](https://leetcode.cn/problems/minimum-reverse-operations) | 2824 |"
    },
    {
      "id": 1102,
      "title": "11. 多階段最短路與路徑約束",
      "summary": "**觀念起點**\n\n多階段最短路不是每個條件都塞進一次 Dijkstra。先判斷哪些距離表可以重用，例如從兩個起點到所有點、從終點在反圖到所有點，然後枚舉匯合點合併答案。\n\n如果路徑有折扣、通行證或已使用次數，狀態要分層；如果題目問次短路，每個節點就不能只保存一個距離。狀態設計要剛好保存未來決策需要的資訊。\n\n等待時間、交通燈與週期規則不改變最短路框架，但會改變 relax 前的出發時間。dist 的語意應該是實際抵達時間，而不是走過幾條邊。\n\n常見模型：匯合點枚舉（對終點跑反圖一次取得所有點到終點的距離）、次短路（每點保留最短與嚴格次短）、分層圖（折扣或通行證）。\n\n多階段最短路題通常不是一次 Dijkstra 就結束，而是要把路徑條件拆成幾份可重用的距離表。從零開始看，Dijkstra 只回答「一個起點到所有點的最短距離」。如果題目有兩個起點、匯合點、反向到終點、第二短路或等待規則，就要擴充狀態或跑多次距離表。\n\n**常見模型**：\n- 兩個起點匯合再到終點：從 `src1`、`src2` 跑正圖，從 `dest` 跑反圖，枚舉匯合點。\n- 第二短路：每個點保留最短與次短兩個不同距離。\n- 交通燈或週期等待：鬆弛邊前先根據目前時間計算等待。\n- 有折扣或通行證：狀態加入已使用次數，變成分層圖最短路。\n\n**系統例題：兩個來源到同一終點的最小權重子圖**\n\n**完整問題**：有向加權圖，找一個子圖讓 `src1` 和 `src2` 都能到 `dest`，最小化使用邊權總和。等價於選一個匯合點 x，兩個來源各自走到 x，再從 x 走到 dest。\n\n解題步驟：\n1. 從 `src1` 在原圖跑 Dijkstra，得到 `d1[x]`。\n2. 從 `src2` 在原圖跑 Dijkstra，得到 `d2[x]`。\n3. 從 `dest` 在反圖跑 Dijkstra，得到 `dr[x]`，代表原圖中 `x -> dest` 的距離。\n4. 枚舉 x，最小化 `d1[x] + d2[x] + dr[x]`。\n\n**C++：三次 Dijkstra + 枚舉匯合點**\n\n```cpp\n// minimum total path cost for src1 and src2 to meet and reach dest\nvector<long long> dijkstra(int n, vector<vector<pair<int, int>>>& g, int src) {\n  const long long kInf = 4e18;\n  vector<long long> dist(n, kInf);\n  priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\n  dist[src] = 0;\n  pq.push({0, src});\n\n  while (!pq.empty()) {\n    auto [du, u] = pq.top();\n    pq.pop();\n    if (du != dist[u]) {\n      continue;\n    }\n    for (auto [v, w] : g[u]) {\n      if (dist[v] > du + w) {\n        dist[v] = du + w;\n        pq.push({dist[v], v});\n      }\n    }\n  }\n  return dist;\n}\n\nlong long minimumWeight(int n, vector<vector<int>>& edges, int src1, int src2, int dest) {\n  vector<vector<pair<int, int>>> g(n), rg(n);\n  for (auto& e : edges) {\n    int u = e[0], v = e[1], w = e[2];\n    g[u].push_back({v, w});\n    rg[v].push_back({u, w});\n  }\n\n  auto d1 = dijkstra(n, g, src1);\n  auto d2 = dijkstra(n, g, src2);\n  auto dr = dijkstra(n, rg, dest);\n\n  const long long kInf = 4e18;\n  long long ans = kInf;\n  for (int x = 0; x < n; ++x) {\n    if (d1[x] == kInf || d2[x] == kInf || dr[x] == kInf) {\n      continue;\n    }\n    ans = min(ans, d1[x] + d2[x] + dr[x]);\n  }\n  return ans == kInf ? -1 : ans;\n}\n```\n\n重點是反圖那次 Dijkstra：它把所有點到 dest 的距離一次算完，避免對每個匯合點重跑最短路。\n\n**競程課程講義：多階段最短路是距離表組合**\n\n**Pattern 1：匯合點枚舉**\n\n跑多張距離表後枚舉中繼點。關鍵是從終點跑反圖，取得所有點到終點的距離。\n\n**Pattern 2：次短路**\n\n每個節點保留兩個嚴格不同距離。鬆弛時先更新最短，再更新次短；相同距離不要重複算。\n\n```cpp\nstruct TwoShortestPaths {\n  using Edge = pair<int, int>;  // next node, non-negative weight\n  static constexpr long long kInf = 4e18;\n\n  vector<array<long long, 2>> run(const vector<vector<Edge>>& graph, int source) {\n    vector<array<long long, 2>> dist(graph.size(), {kInf, kInf});\n    priority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\n\n    dist[source][0] = 0;\n    pq.push({0, source});\n\n    while (!pq.empty()) {\n      auto [du, u] = pq.top();\n      pq.pop();\n      if (du > dist[u][1]) {\n        continue;\n      }\n\n      for (auto [v, w] : graph[u]) {\n        long long nd = du + w;\n        if (nd < dist[v][0]) {\n          dist[v][1] = dist[v][0];\n          dist[v][0] = nd;\n          pq.push({nd, v});\n        } else if (dist[v][0] < nd && nd < dist[v][1]) {\n          dist[v][1] = nd;\n          pq.push({nd, v});\n        }\n      }\n    }\n    return dist;\n  }\n};\n```\n\n**Pattern 3：分層圖**\n\n訊號：可用 k 次折扣、通行證、免費邊。狀態加一維 `used`，邊在同層或下一層轉移。\n\n**Pattern 4：等待規則**\n\n若交通燈或週期限制出發時間，dist 仍表示實際抵達時間；relax 前先把 `depart` 調到下一個可出發時間。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2290 | [到達角落需要移除障礙物的最小數目](https://leetcode.cn/problems/minimum-obstacle-removal-to-reach-corner) | 2138 |\n| 2662 | [前往目標的最小代價](https://leetcode.cn/problems/minimum-cost-of-a-path-with-special-roads) | 2154 |\n| 2045 | [到達目的地的第二短時間](https://leetcode.cn/problems/second-minimum-time-to-reach-destination) | 2202 |\n| 2203 | [得到要求路徑的最小帶權子圖](https://leetcode.cn/problems/minimum-weighted-subgraph-with-the-required-paths) | 2364 |\n| 1928 | [規定時間內到達終點的最小花費](https://leetcode.cn/problems/minimum-cost-to-reach-destination-in-time) | 2413 |"
    },
    {
      "id": 1103,
      "title": "12. 樹分解、換根與子樹查詢",
      "summary": "**觀念起點**\n\n高階樹題的基礎是：樹上任一條邊都把節點分成兩塊。後序 DFS 能取得子樹資訊，Euler tour 能把子樹變成連續區間，換根 DP 能把父側資訊推給孩子。\n\n換根時不要背公式，先看根跨過一條邊後哪些節點距離變短、哪些變長，或哪些貢獻從子樹側移到父側。公式來自這個變化。\n\n若有多次祖先或路徑查詢，才需要 binary lifting、Euler + Fenwick 或 heavy-light decomposition；單次查詢通常普通 DFS 就足夠。\n\n工具定位：Euler tour 把子樹映成連續區間、後序子樹 DP、換根 DP、binary lifting 求 LCA 與 k 級祖先；多次路徑查詢用樹鏈剖分，深度相關用長鏈剖分。\n\n高階樹題的核心是把「子樹」變成可計算的範圍，或把「換根後答案」用父節點答案推導出來。從零開始看，樹沒有環，所以每條邊把樹切成兩部分；很多問題就是快速知道某個子樹的 size、sum、xor、高度，或知道把根從父節點移到孩子後答案如何改變。\n\n**常用工具**：\n- 後序 DFS：先算孩子，再合併出子樹資訊。\n- Euler tour：記錄 `tin/tout`，子樹對應一段連續時間區間。\n- 換根 DP：第一次 DFS 算某個根的答案，第二次 DFS 把答案從父節點推給孩子。\n- 祖先判斷：`tin[u] <= tin[v] <= tout[u]` 表示 u 是 v 的祖先。\n\n**系統例題：每個節點到所有節點的距離和**\n\n**完整問題**：給 n 個節點的樹，對每個節點 i，求 i 到所有節點的距離和。\n\n解題步驟：\n1. 先固定 0 為根。第一次 DFS 算 `size[u]` 和 `answer[0]`，其中 `answer[0]` 是 0 到所有點距離和。\n2. 對一條父子邊 `u -> v`，若根從 u 移到 v：v 子樹內的 `size[v]` 個點距離都減 1，其他 `n - size[v]` 個點距離都加 1。\n3. 所以 `answer[v] = answer[u] - size[v] + (n - size[v])`。\n4. 第二次 DFS 套公式下傳答案。\n\n**C++：換根 DP 距離和**\n\n```cpp\n// sum of distances in tree for every root\nvector<int> sumOfDistancesInTree(int n, vector<vector<int>>& edges) {\n  vector<vector<int>> g(n);\n  for (auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    g[e[1]].push_back(e[0]);\n  }\n\n  vector<int> size(n, 1);\n  vector<int> answer(n, 0);\n\n  function<void(int, int, int)> postorderDfs = [&g, &size, &answer, &postorderDfs](int u, int parent, int depth) {\n    answer[0] += depth;\n    for (int v : g[u]) {\n      if (v == parent) {\n        continue;\n      }\n      postorderDfs(v, u, depth + 1);\n      size[u] += size[v];\n    }\n  };\n\n  function<void(int, int)> rerootDfs = [&g, &size, &answer, n, &rerootDfs](int u, int parent) {\n    for (int v : g[u]) {\n      if (v == parent) {\n        continue;\n      }\n      answer[v] = answer[u] - size[v] + (n - size[v]);\n      rerootDfs(v, u);\n    }\n  };\n\n  postorderDfs(0, -1, 0);\n  rerootDfs(0, -1);\n  return answer;\n}\n```\n\n換根題的面試重點是推導公式，而不是背模板。你要能清楚說出哪一部分距離變短、哪一部分變長。\n\n**競程課程講義：樹題把子樹轉成區間或貢獻公式**\n\n**Pattern 1：Euler tour**\n\n訊號：子樹查詢、刪除子樹、判斷祖先。DFS 進入時間 `tin` 到離開前最大時間 `tout` 是子樹連續區間。\n\n```cpp\nstruct EulerTour {\n  vector<int> tin, tout, order;\n\n  EulerTour(const vector<vector<int>>& tree, int root = 0) {\n    int n = tree.size();\n    tin.assign(n, 0);\n    tout.assign(n, 0);\n    dfs(root, root, tree);\n  }\n\n  void dfs(int u, int parent, const vector<vector<int>>& tree) {\n    tin[u] = order.size();\n    order.push_back(u);\n    for (int v : tree[u]) {\n      if (v != parent) {\n        dfs(v, u, tree);\n      }\n    }\n    tout[u] = order.size();\n  }\n\n  bool is_ancestor(int u, int v) const { return tin[u] <= tin[v] && tin[v] < tout[u]; }\n\n  pair<int, int> subtree_range(int u) const {  // [tin[u], tout[u])\n    return {tin[u], tout[u]};\n  }\n};\n```\n\n**Pattern 2：後序子樹 DP**\n\n先算孩子，再合併 size、sum、height、best path。\n\n**Pattern 3：換根 DP**\n\n第一次 DFS 算根為 0 的答案；第二次把父答案推給孩子。核心是推導跨過一條邊時貢獻如何改變。\n\n**Pattern 4：Binary lifting**\n\n訊號：多次 LCA、向上跳 k 步、路徑查詢。預處理 `up[j][v]` 表示 v 的 `2^j` 祖先。\n\n```cpp\nclass BinaryLifting {\n  vector<vector<int>> up;\n  vector<int> depth;\n\n  void dfs(int u, int parent, const vector<vector<int>>& tree) {\n    up[0][u] = parent;\n    for (int j = 1; j < (int)up.size(); ++j) {\n      up[j][u] = up[j - 1][up[j - 1][u]];\n    }\n    for (int v : tree[u]) {\n      if (v == parent) {\n        continue;\n      }\n      depth[v] = depth[u] + 1;\n      dfs(v, u, tree);\n    }\n  }\n\n public:\n  BinaryLifting(const vector<vector<int>>& tree, int root = 0) {\n    int n = tree.size();\n    int log = 1;\n    while ((1 << log) <= n) {\n      log++;\n    }\n    up.assign(log, vector<int>(n));\n    depth.assign(n, 0);\n    dfs(root, root, tree);\n  }\n\n  int kth_ancestor(int node, int k) const {\n    for (int j = 0; j < (int)up.size(); ++j) {\n      if (k & (1 << j)) {\n        node = up[j][node];\n      }\n    }\n    return node;\n  }\n\n  int lca(int a, int b) const {\n    if (depth[a] < depth[b]) {\n      swap(a, b);\n    }\n    a = kth_ancestor(a, depth[a] - depth[b]);\n    if (a == b) {\n      return a;\n    }\n    for (int j = (int)up.size() - 1; j >= 0; --j) {\n      if (up[j][a] != up[j][b]) {\n        a = up[j][a];\n        b = up[j][b];\n      }\n    }\n    return up[0][a];\n  }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2246 | [相鄰字元不同的最長路徑](https://leetcode.cn/problems/longest-path-with-different-adjacent-characters) | 2126 |\n| 834 | [樹中距離之和](https://leetcode.cn/problems/sum-of-distances-in-tree) | 2197 |\n| 2458 | [移除子樹後的二叉樹高度](https://leetcode.cn/problems/height-of-binary-tree-after-subtree-removal-queries) | 2299 |\n| 2322 | [從樹中刪除邊的最小分數](https://leetcode.cn/problems/minimum-score-after-removals-on-a-tree) | 2392 |\n| 2440 | [建立價值相同的連通塊](https://leetcode.cn/problems/create-components-with-same-value) | 2460 |"
    },
    {
      "id": 1104,
      "title": "13. 字串演算法結合 DP/資料結構",
      "summary": "**觀念起點**\n\n字串演算法結合 DP 時，字串工具通常只負責產生快速轉移。Trie 可以告訴你從某個位置能接哪些字典詞，Z/hash 可以快速比較兩段，KMP 可以提供 border 或週期資訊。\n\n先把外層問題定義清楚：是最少切幾段、能否拼出 target、計算方案數，還是找字典序最小結果。外層若是 DP，字串工具只是讓每個狀態的轉移不必暴力比較。\n\n若 pattern 很多且 text 很長，逐一 KMP 可能不夠；這時才考慮 Trie 或 Aho-Corasick，把多 pattern 匹配合併成一次掃描。\n\n字串工具只負責加速轉移：Z 與 hash 比較任意兩段、KMP 提供 border 與週期、Trie 與 Aho-Corasick 處理多模式；外層仍是 DP（最少切段、計數、字典序最小等）。\n\n字串高階題通常是「字串演算法 + 另一層 DP/計數/資料結構」。KMP、Z 函式、Trie、rolling hash 只是取得比較資訊的工具，真正的答案常要再透過 DP 或資料結構累積。從零開始時，先找出題目反覆做的昂貴比較，再用字串工具把比較降到 `O(1)` 或均攤線性。\n\n**工具定位**：\n- Z 函式：快速知道 `s[i...]` 和整個字串前綴的 LCP。\n- KMP prefix function：處理 border、模式匹配、週期性。\n- rolling hash：快速比較任意兩段子串，但要注意碰撞。\n- Trie：處理多個字典詞與 target 的前綴匹配，常接 DP。\n\n**系統例題：用字典詞拼出 target 的最少詞數**\n\n**完整問題**：給一批 words 和 target，每次可取一個 word 完整接到目前字串後面，求拼出 target 的最少詞數。\n\n解題步驟：\n1. 暴力嘗試每個位置接每個 word 會很慢。\n2. 把 words 建成 Trie，從 target 的每個起點 i 沿 Trie 往後走，能走到 word 結尾就代表有一條轉移 `i -> j`。\n3. 定義 `dp[i]`：拼出 target 前 i 個字元的最少詞數。\n4. 從每個 i 出發沿 Trie 枚舉可接的詞，更新 `dp[j] = min(dp[j], dp[i] + 1)`。\n\n**C++：Trie + DP**\n\n```cpp\n// minimum number of dictionary words to form target\nstruct TrieNode {\n  int next[26];\n  bool isWord = false;\n  TrieNode() { fill(std::begin(next), std::end(next), -1); }\n};\n\nint minWords(vector<string>& words, string target) {\n  vector<TrieNode> trie(1);\n  for (const string& w : words) {\n    int node = 0;\n    for (char ch : w) {\n      int c = ch - 'a';\n      if (trie[node].next[c] == -1) {\n        trie[node].next[c] = trie.size();\n        trie.emplace_back();\n      }\n      node = trie[node].next[c];\n    }\n    trie[node].isWord = true;\n  }\n\n  const int kInf = 1e9;\n  int n = target.size();\n  vector<int> dp(n + 1, kInf);\n  dp[0] = 0;\n\n  for (int i = 0; i < n; ++i) {\n    if (dp[i] == kInf) {\n      continue;\n    }\n    int node = 0;\n    for (int j = i; j < n; ++j) {\n      int c = target[j] - 'a';\n      if (trie[node].next[c] == -1) {\n        break;\n      }\n      node = trie[node].next[c];\n      if (trie[node].isWord) {\n        dp[j + 1] = min(dp[j + 1], dp[i] + 1);\n      }\n    }\n  }\n  return dp[n] == kInf ? -1 : dp[n];\n}\n```\n\n如果 words 很多且 target 很長，可以再用 Aho-Corasick 或 hash 優化匹配；但面試中先把「Trie 產生轉移，DP 求最少段數」講清楚最重要。\n\n**競程課程講義：字串工具通常只是 DP 的加速器**\n\n**Pattern 1：Z + DP 刪除前綴**\n\n若要比較 `s[i...]` 和 `s[j...]` 的 LCP，可對後綴建 Z 或用 rolling hash + binary search。\n\n**Pattern 2：KMP border tree**\n\nprefix function 形成 border 鏈，可以統計每個 border 出現次數、週期長度、前後綴關係。\n\n```cpp\nstruct PrefixFunctionTools {\n  static vector<int> prefix_function(const string& s) {\n    vector<int> pi(s.size());\n    for (int i = 1; i < (int)s.size(); ++i) {\n      int j = pi[i - 1];\n      while (j > 0 && s[i] != s[j]) {\n        j = pi[j - 1];\n      }\n      if (s[i] == s[j]) {\n        j++;\n      }\n      pi[i] = j;\n    }\n    return pi;\n  }\n\n  static vector<int> border_counts(const string& s) {\n    vector<int> pi = prefix_function(s);\n    int n = s.size();\n    vector<int> count(n + 1, 0);\n    for (int x : pi) {\n      count[x]++;\n    }\n    for (int len = n; len > 0; --len) {\n      count[pi[len - 1]] += count[len];\n    }\n    for (int len = 0; len <= n; ++len) {\n      count[len]++;\n    }\n    return count;\n  }\n};\n```\n\n**Pattern 3：Hash LCP + DP**\n\n任意兩段比較時，用 hash 判斷長度 mid 是否相等，再二分 LCP。常用於 lexicographic DP 或一次 mismatch 判斷。\n\n**Pattern 4：Trie / Aho-Corasick + DP**\n\n多字典詞匹配 target 時，Trie 是基礎；若要從每個位置大量找所有詞，Aho-Corasick 能把匹配變成線性掃描。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3036 | [匹配模式陣列的子陣列數目 II](https://leetcode.cn/problems/number-of-subarrays-that-match-a-pattern-ii) | 1895 |\n| 3316 | [從原字串裡進行刪除操作的最多次數](https://leetcode.cn/problems/find-maximum-removals-from-source-string) | 2062 |\n| 2430 | [對字母串可執行的最大刪除數](https://leetcode.cn/problems/maximum-deletions-on-a-string) | 2102 |\n| 3045 | [統計前字尾下標對 II](https://leetcode.cn/problems/count-prefix-and-suffix-pairs-ii) | 2328 |\n| 3303 | [第一個幾乎相等子字串的下標](https://leetcode.cn/problems/find-the-occurrence-of-first-almost-equal-substring) | 2509 |"
    },
    {
      "id": 1105,
      "title": "14. 高階 DP 與單調性優化",
      "summary": "**觀念起點**\n\n高階 DP 優化從樸素式開始。先寫出 `dp[i]` 從哪些 `j` 轉移而來，再看找最佳 `j` 的過程是否能被資料結構或數學性質取代。\n\n若合法 j 是一段滑動窗口，可用 deque；若 j 是排序後的相容前驅，可用二分；若轉移是 `m*x+b` 的最小值，才考慮 convex hull trick 或 Li Chao tree。\n\n優化不能改變狀態語意。每次把枚舉消掉之前，都要確認被資料結構保留的候選和原本暴力枚舉的候選集合完全一致。\n\n辨認轉移形態再選工具：窗口最值用單調隊列，排序後相容前驅用二分，`min(m_j * x + b_j)` 用 convex hull trick 或李超線段樹；查詢 x 與斜率是否單調決定用 deque hull 還是 Li Chao。\n\n高階 DP 的訊號是狀態容易寫，但轉移太慢。從零開始時，不要一開始就套優化；先寫出正確但可能較慢的定義，再觀察轉移是否只需要某個前綴最佳值、滑動窗口最值、二分相容狀態、單調隊列或斜率優化。優化的本質是避免重複枚舉不可能成為最佳的決策點。\n\n**常見 Pattern**：\n- 排程/活動：排序 + 二分找到上一個相容狀態。\n- `dp[i] = min(dp[j] + cost(j, i))`：檢查 cost 是否能用前綴、單調隊列或 convex hull trick 優化。\n- 有 k 次選擇：通常是 `dp[i][used]`，再看每層能否優化。\n- 窗口限制：轉移只來自最近一段 j，可用 deque 維護候選最大/最小值。\n\n**系統例題：帶最多 k 個活動的最大收益**\n\n**完整問題**：每個活動有開始、結束、價值，最多選 k 個互不重疊活動，求最大價值。\n\n解題步驟：\n1. 按結束時間排序，讓前 i 個活動形成前綴。\n2. `dp[i][c]` 表示只看前 i 個活動、最多選 c 個時的最大價值。\n3. 不選第 i 個：`dp[i-1][c]`。\n4. 選第 i 個：找到最後一個結束時間小於目前開始時間的活動數 `j`，轉移為 `dp[j][c-1] + value`。\n5. 用二分找 j，避免每次線性往前掃。\n\n**C++：二分相容狀態的二維 DP**\n\n```cpp\n// maximum value from at most k non-overlapping events\nint max_value(vector<vector<int>>& events, int k) {\n  sort(events.begin(), events.end(), [](const auto& a, const auto& b) { return a[1] < b[1]; });\n\n  int n = events.size();\n  vector<int> ends(n);\n  for (int i = 0; i < n; ++i) {\n    ends[i] = events[i][1];\n  }\n\n  vector<vector<int>> dp(n + 1, vector<int>(k + 1, 0));\n  for (int i = 1; i <= n; ++i) {\n    int start = events[i - 1][0];\n    int value = events[i - 1][2];\n    int j = lower_bound(ends.begin(), ends.end(), start) - ends.begin();\n\n    for (int c = 1; c <= k; ++c) {\n      dp[i][c] = max(dp[i - 1][c], dp[j][c - 1] + value);\n    }\n  }\n  return dp[n][k];\n}\n```\n\n複雜度是 `O(n log n + n*k)`。如果 n 和 k 都很大，才需要再討論單調性、資料結構或不同狀態定義；先把可證明正確的 DP 建出來，是高階 DP 面試的基礎。\n\n**競程課程講義：DP 優化先保留樸素式，再消掉枚舉維度**\n\n**Pattern 1：排序 + 二分前驅**\n\n活動選擇、工作排程、帶 k 次選擇。把相容條件變成前綴 index。\n\n**Pattern 2：單調隊列優化**\n\n訊號：`dp[i] = best(dp[j] + value[j])`，且 j 只在滑動窗口內。deque 維護候選值單調。\n\n```cpp\nclass MonotonicMaxQueue {\n  deque<pair<int, long long>> dq;  // index, value; values decrease from front to back\n\n public:\n  void push(int index, long long value) {\n    while (!dq.empty() && dq.back().second <= value) {\n      dq.pop_back();\n    }\n    dq.push_back({index, value});\n  }\n\n  void expire_before(int minIndex) {\n    while (!dq.empty() && dq.front().first < minIndex) {\n      dq.pop_front();\n    }\n  }\n\n  long long max_value() const { return dq.front().second; }\n\n  bool empty() const { return dq.empty(); }\n};\n\n// dp[i] = nums[i] + max(dp[j]) for i-k <= j < i.\nint constrainedSubsetSum(vector<int>& nums, int k) {\n  MonotonicMaxQueue window;\n  vector<long long> dp(nums.size());\n  long long answer = nums[0];\n\n  for (int i = 0; i < (int)nums.size(); ++i) {\n    window.expire_before(i - k);\n    dp[i] = nums[i] + (window.empty() ? 0 : max(0LL, window.max_value()));\n    answer = max(answer, dp[i]);\n    window.push(i, dp[i]);\n  }\n  return (int)answer;\n}\n```\n\n**Pattern 3：前綴最佳值**\n\n若轉移只需要 `min(dp[j] - prefix[j])` 或 `max(dp[j] + value[j])`，用一個變數維護最佳候選即可。\n\n**Pattern 4：Convex Hull Trick**\n\n訊號：轉移形如 `dp[i] = min(m_j * x_i + b_j)`。每個 j 是一條線，查詢 x_i 的最小值。競程課中先要求學生辨認斜率與查詢 x 是否單調，再決定用 deque hull 或 Li Chao tree。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2008 | [計程車的最大盈利](https://leetcode.cn/problems/maximum-earnings-from-taxi) | 1872 |\n| 1751 | [最多可以參加的會議數目 II](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended-ii) | 2041 |\n| 1473 | [粉刷房子 III](https://leetcode.cn/problems/paint-house-iii) | 2056 |\n| 1883 | [準時抵達會議現場的最小跳過休息次數](https://leetcode.cn/problems/minimum-skips-to-arrive-at-meeting-on-time) | 2588 |\n| 1687 | [從倉庫到碼頭運輸箱子](https://leetcode.cn/problems/delivering-boxes-from-storage-to-ports) | 2610 |"
    },
    {
      "id": 9001,
      "title": "15. 常見面試模式",
      "summary": "面試官常用「題目訊號」反推考點，掌握下列通用模式可在 Google 風格的演算法面試中快速辨識題型並給出 `O(n log n)` 以內的解法。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 雙指針 | 已排序陣列、找配對或去重 | 左右指針相向逼近，依和的大小移動 | [兩數之和 II](https://leetcode.cn/problems/two-sum-ii-input-array-is-sorted) |\n| 滑動視窗 | 連續子陣列/子字串、最長或最短 | 右擴左縮維護視窗合法性，攤還 `O(n)` | [無重複字元的最長子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters) |\n| 二分搜尋 | 單調性、「最小可行值」 | 對答案二分，判定函式檢查可行性 | [在排序陣列中查找元素的第一個和最後一個位置](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array) |\n| BFS / DFS | 圖或網格的連通、最短步數 | 佇列做最短層數、遞迴或堆疊做連通塊 | [島嶼數量](https://leetcode.cn/problems/number-of-islands) |\n| 堆積 (優先佇列) | Top-K、第 K 大/小、合併多路 | 維護大小為 $k$ 的堆積，`O(n log k)` | [數組中的第K個最大元素](https://leetcode.cn/problems/kth-largest-element-in-an-array) |\n| 動態規劃 | 最優子結構、計數或最值 | 定義狀態與轉移，自底向上填表 | [最長遞增子序列](https://leetcode.cn/problems/longest-increasing-subsequence) |\n| 回溯 | 列舉所有組合/排列、$n \\\\le 20$ | 遞迴選擇 + 撤銷，搭配剪枝 | [全排列](https://leetcode.cn/problems/permutations) |\n\n小提醒：\n\n- 看到「已排序」先想雙指針或二分；看到「連續區間」先想滑動視窗。\n- 「第 K」「Top-K」優先考慮堆積，資料量大時再評估 `O(n)` 的快速選擇。\n- 回溯前先估搜尋空間大小，確認指數級規模在約束內，否則改用 DP。"
    }
  ]
} as TutorialData.Root;
