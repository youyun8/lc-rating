import type { TutorialData } from "@/types";

export const greedy = {
  id: 1,
  title: "貪心（基本貪心策略/反悔/區間/字典序/數學/思維/構造）",
  src: "https://leetcode.cn/circle/discuss/g6KTKL/",
  last_update: "2026-02-24T14:55:40.937799+00:00",
  summary:
    "**本題單結構**：\n- 前面：套路性貪心題（基本策略）\n- 後面：靈活思維題、構造題\n- 題目按難度排序\n\n**做題建議**：沒思路時參考第五章「思考清單」\n\n**本題單涵蓋**：\n- 基本貪心策略：從最小/最大、從最左/最右\n- 反悔貪心：反悔堆\n- 區間貪心：區間排序、區間合併\n- 字典序貪心：最小/最大字典序\n- 數學貪心：不等式、排序不等式\n- 思維與構造：腦筋急轉彎、構造答案\n\n:::example 區間排程貪心（最大不相交區間數）\n**貪心演算法核心**：\n- 優先處理當前最優選擇\n- 通常需要排序或使用優先佇列 (heap)\n- 關鍵在於證明貪心策略的正確性\n\n```cpp\n// 區間排程貪心（最大不相交區間數）\n// 按右端點排序，依次選擇不衝突的區間\nsort(intervals.begin(), intervals.end(),\n     [](auto& a, auto& b) { return a[1] < b[1]; });\nint cnt = 0, last = INT_MIN;\nfor (auto& it : intervals) {\n  if (it[0] >= last) {\n    ++cnt;\n    last = it[1];\n  }\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 從最小/最大開始貪心",
      summary:
        "**原理講解**\n\n本節要解決的場景是：你要從一堆元素裡「挑出一些」或「決定處理順序」，目標是讓某個總量（總和、總價值、可行數量）達到最好。直覺常常是「每一步都挑當下看起來最好的」，但這裡的關鍵字是「從最小／最大開始」——先把資料依大小排好，再從一端開始貪心。\n\n什麼叫「貪心（greedy）」？就是每一步都做一個當下最划算的選擇，且選了之後不反悔。問題是：當下最划算，整體就一定最好嗎？不一定。所以這個技巧真正的重點，不是「會排序」，而是「能說明為什麼從最小／最大開始挑是安全的」。\n\n最常用的證明工具叫「交換論證（exchange argument）」。意思是：假設有一個最優解沒有照我的規則挑，那我就把它的選擇「換」成我的規則挑的元素，並論證換完之後結果不會變差。既然能換到我的規則上，那我的規則就也是最優的。\n\n:::example 選 K 個數使總和最大：排序後從大到小取\n舉一個小例子：要從陣列裡選 K 個數，讓總和最大。規則很自然——排序後挑最大的 K 個。\n\n```cpp\n// 選 K 個數使總和最大：排序後從大到小取\nsort(a.begin(), a.end(), greater<int>());  // 由大到小\nlong long sum = 0;\nfor (int i = 0; i < k; ++i) {\n  sum += a[i];  // 取前 K 大\n}\n```\n:::\n\n\n\n為什麼安全？若某個最優解選了一個較小的 `x`、卻沒選某個更大、還沒被選的 `y`，把 `x` 換成 `y`，總和只會變大或不變，不會變差。這就是交換論證。像「2098. 長度為 K 的最大偶數和子序列」就是在這個基礎上多一個「總和要偶數」的限制。\n\n**實作方式**\n\n:::example 排序鍵：依題目目標決定升冪或降冪\n**1. 決定排序鍵與方向。** 先想清楚：要讓答案變大就通常從大的開始挑，要省成本就從小的開始挑。把元素依這個鍵排序，是整個技巧的地基。\n\n```cpp\n// 排序鍵：依題目目標決定升冪或降冪\nsort(a.begin(), a.end());  // 升冪：想優先用小的\n                           // 或 sort(a.begin(), a.end(), greater<int>()); //\n                           // 降冪：想優先用大的\n```\n:::\n\n\n\n:::example 從大到小貪心，遇到不合條件的就略過\n**2. 從一端開始掃，每步做安全選擇。** 沿著排好序的順序走，能拿就拿、能配就配，必要時跳過不合法的元素。\n\n```cpp\n// 從大到小貪心，遇到不合條件的就略過\nlong long best = 0;\nfor (int x : a) {\n  if (!ok(x)) {\n    continue;  // ok：這個元素現在能用嗎？\n  }\n  best += x;  // 安全選擇：採用它\n}\n```\n:::\n\n\n\n:::example 修正範例：和為奇數時，移除已選裡最小的奇數讓它變偶\n**3. 處理「奇偶／配額」之類的附加限制。** 很多題目不是純取最大，而是有額外條件（總和要偶數、最多選 K 個、要兩兩配對）。做法是先照貪心拿，再用最小代價修正。例如要偶數和，但目前和是奇數，就丟掉「已選中最小的奇數」或補進「未選中最大的奇數」，二者取較好。\n\n```cpp\n// 修正範例：和為奇數時，移除已選裡最小的奇數讓它變偶\nif (sum % 2 != 0 && !chosenOdd.empty()) {\n  sum -= chosenOdd.back();  // 代價最小的修正\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 主成本是排序，整體 `O(n log n)`；只掃一遍是 `O(n)`。最大陷阱是「沒驗證貪心是否安全」就直接寫——若交換論證不成立（例如選擇之間互相牽制），這題多半不是貪心，而要改用 DP 或搜尋。像「2214. 通關遊戲所需的最低生命值」就要先想清楚怎麼安排順序才安全，再貪心。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「從最小／最大開始貪心」想：\n\n- **題目要最大化／最小化某個總量，而元素之間相對獨立。** 一個元素選不選，不會改變另一個元素的價值——這正是貪心成立的溫床。\n- **答案明顯和「大小排序」有關。** 「最大距離」「最大偶數和」「最大價格」這類字眼，常暗示先排序、再從一端取。例如「624. 陣列列表中的最大距離」就是看最大減最小。\n- **資料量大到不能枚舉所有組合。** 暴力試所有子集是 `O(2^n)`，排序貪心把它壓成 `O(n log n)`。\n\n:::example 警訊：當下最大不一定全域最優\n但要小心反例：\n\n```cpp\n// 警訊：當下最大不一定全域最優\n// 若選了大的會「擋住」之後更好的選擇，貪心就不安全\n// 這種互相牽制的情況通常要改用 DP\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的排序鍵是什麼？為什麼是升冪或降冪？\n2. 我能不能用交換論證說明「換成我的選法不會更差」？\n3. 有沒有附加限制（奇偶、配額）需要在貪心後再修正？\n\n三題都答得出來，這個技巧才算套對。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 624 | 陣列列表中的最大距離 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2098 | 長度為 K 的最大偶數和子序列（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2214 | 通關遊戲所需的最低生命值（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2548 | 填滿背包的最大價格（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2557 | 從一個範圍內選擇最多整數 II（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 624 | [陣列列表中的最大距離](https://leetcode.cn/problems/maximum-distance-in-arrays) |  |\n| 2098 | [長度為 K 的最大偶數和子序列](https://leetcode.cn/problems/subsequence-of-size-k-with-the-largest-even-sum) |  |\n| 2214 | [通關遊戲所需的最低生命值](https://leetcode.cn/problems/minimum-health-to-beat-game) |  |\n| 2548 | [填滿背包的最大價格](https://leetcode.cn/problems/maximum-price-to-fill-a-bag) |  |\n| 2557 | [從一個範圍內選擇最多整數 II](https://leetcode.cn/problems/maximum-number-of-integers-to-choose-from-a-range-ii) |  |\n| 3119 | [最大數量的可修復坑窪](https://leetcode.cn/problems/maximum-number-of-potholes-that-can-be-fixed) |  |\n| 3476 | [最大化任務分配的利潤](https://leetcode.cn/problems/maximize-profit-from-task-assignment) |  |\n| 3730 | [跳躍燃燒的最大卡路里](https://leetcode.cn/problems/maximum-calories-burnt-from-jumps) |  |\n| 3763 | [帶閾值約束的最大總和](https://leetcode.cn/problems/maximum-total-sum-with-threshold-constraints) |  |\n| LCP 40 | [心算挑戰](https://leetcode.cn/problems/uOAnQW) |  |\n| LCS 02 | [完成一半題目](https://leetcode.cn/problems/WqXACV) |  |\n| 3074 | [重新分裝蘋果](https://leetcode.cn/problems/apple-redistribution-into-boxes) | 1198 |\n| 3545 | [不同字元數量最多為 K 時的最少刪除數](https://leetcode.cn/problems/minimum-deletions-for-at-most-k-distinct-characters) | 1211 |\n| 3745 | [三元素表示式的最大值](https://leetcode.cn/problems/maximize-expression-of-three-elements) | 1218 |\n| 1196 | [最多可以買到的蘋果數量](https://leetcode.cn/problems/how-many-apples-can-you-put-into-the-basket) | 1249 |\n| 2279 | [裝滿石頭的背包的最大數量](https://leetcode.cn/problems/maximum-bags-with-full-capacity-of-rocks) | 1249 |\n| 1833 | [雪糕的最大數量](https://leetcode.cn/problems/maximum-ice-cream-bars) | 1253 |\n| 3627 | [中位數之和的最大值](https://leetcode.cn/problems/maximum-median-sum-of-subsequences-of-size-3) | 1262 |\n| 1005 | [K 次取反後最大化的陣列和](https://leetcode.cn/problems/maximize-sum-of-array-after-k-negations) | 1275 |\n| 1481 | [不同整數的最少數目](https://leetcode.cn/problems/least-number-of-unique-integers-after-k-removals) | 1284 |\n| 1403 | [非遞增順序的最小子序列](https://leetcode.cn/problems/minimum-subsequence-in-non-increasing-order) | 1288 |\n| 3010 | [將陣列分成最小總代價的子陣列 I](https://leetcode.cn/problems/divide-an-array-into-subarrays-with-minimum-cost-i) | 1292 |\n| 1338 | [陣列大小減半](https://leetcode.cn/problems/reduce-array-size-to-the-half) | 1303 |\n| 1984 | [學生分數的最小差值](https://leetcode.cn/problems/minimum-difference-between-highest-and-lowest-of-k-scores) | 1306 |\n| 1710 | [卡車上的最大單元數](https://leetcode.cn/problems/maximum-units-on-a-truck) | 1310 |\n| 3075 | [幸福值最大化的選擇方案](https://leetcode.cn/problems/maximize-happiness-of-selected-children) | 1326 |\n| 2554 | [從一個範圍內選擇最多整數 I](https://leetcode.cn/problems/maximum-number-of-integers-to-choose-from-a-range-i) | 1333 |\n| 2126 | [摧毀小行星](https://leetcode.cn/problems/destroying-asteroids) | 1335 |\n| 2587 | [重排陣列以得到最大字首分數](https://leetcode.cn/problems/rearrange-array-to-maximize-prefix-score) | 1337 |\n| 976 | [三角形的最大周長](https://leetcode.cn/problems/largest-perimeter-triangle) | 1341 |\n| 1561 | [你可以獲得的最大硬幣數目](https://leetcode.cn/problems/maximum-number-of-coins-you-can-get) | 1406 |\n| 3462 | [提取至多 K 個元素的最大總和](https://leetcode.cn/problems/maximum-sum-with-at-most-k-elements) | 1416 |\n| 2099 | [找到和最大的長度為 K 的子序列](https://leetcode.cn/problems/find-subsequence-of-length-k-with-the-largest-sum) | 1447 |\n| 945 | [使陣列唯一的最小增量](https://leetcode.cn/problems/minimum-increment-to-make-array-unique) | 1448 |\n| 3301 | [高度互不相同的最大塔高和](https://leetcode.cn/problems/maximize-the-total-height-of-unique-towers) | 1448 |\n| 1846 | [減小和重新排列陣列後的最大元素](https://leetcode.cn/problems/maximum-element-after-decreasing-and-rearranging) | 1454 |\n| 3727 | [最大交替平方和](https://leetcode.cn/problems/maximum-alternating-sum-of-squares) | 1455 |\n| 1647 | [字元頻次唯一的最小刪除次數](https://leetcode.cn/problems/minimum-deletions-to-make-character-frequencies-unique) | 1510 |\n| 2971 | [找到最大周長的多邊形](https://leetcode.cn/problems/find-polygon-with-the-largest-perimeter) | 1521 |\n| 2178 | [拆分成最多數目的正偶數之和](https://leetcode.cn/problems/maximum-split-of-positive-even-integers) | 1538 |\n| 1578 | [使繩子變成彩色的最短時間](https://leetcode.cn/problems/minimum-time-to-make-rope-colorful) | 1574 |\n| 2567 | [修改兩個元素的最小分數](https://leetcode.cn/problems/minimum-score-by-changing-two-elements) | 1609 |\n| 1509 | [三次操作後最大值與最小值的最小差](https://leetcode.cn/problems/minimum-difference-between-largest-and-smallest-value-in-three-moves) | 1653 |\n| 2611 | [老鼠和乳酪](https://leetcode.cn/problems/mice-and-cheese) | 1663 |\n| 3397 | [執行操作後不同元素的最大數量](https://leetcode.cn/problems/maximum-number-of-distinct-elements-after-operations) | 1687 |\n| 3767 | [選擇 K 個任務的最大總分數](https://leetcode.cn/problems/maximize-points-after-choosing-k-tasks) | 1704 |\n| 3457 | [喫披薩](https://leetcode.cn/problems/eat-pizzas) | 1704 |\n| 1262 | [可被三整除的最大和](https://leetcode.cn/problems/greatest-sum-divisible-by-three) | 1762 |\n| 948 | [令牌放置](https://leetcode.cn/problems/bag-of-tokens) | 1762 |\n| 1775 | [透過最少操作次數使陣列的和相等](https://leetcode.cn/problems/equal-sum-arrays-with-minimum-number-of-operations) | 1850 |\n| 2333 | [最小差值平方和](https://leetcode.cn/problems/minimum-sum-of-squared-difference) | 2011 |\n| 3645 | [最優啟用順序得到的最大總和](https://leetcode.cn/problems/maximum-total-from-optimal-activation-order) | 2019 |\n| 3366 | [最小陣列和](https://leetcode.cn/problems/minimum-array-sum) | 2040 |\n| 910 | [最小差值 II](https://leetcode.cn/problems/smallest-range-ii) | 2135 |\n| 2835 | [使子序列的和等於目標的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-form-subsequence-with-target-sum) | 2207 |\n| 2141 | [同時執行 N 臺電腦的最長時間](https://leetcode.cn/problems/maximum-running-time-of-n-computers) | 2265 |",
    },
    {
      id: 4,
      title: "2. 單序列配對",
      summary:
        "**原理講解**\n\n本節處理一種很常見的貪心場景：你手上有「一個」序列，要把裡面的元素「兩兩配成一對」（或相鄰分組），讓配對結果最好。例如把人兩兩塞上船、把數字兩兩湊成一堆，目標是船最少、或最大那一對的和最小。\n\n這個技巧的核心直覺是：**排序之後，「最大」應該和「最小」搭、或「相鄰的」搭。** 哪一種對法是對的，由題目目標決定，而且都能用交換論證證明。\n\n:::example 頭尾配對：最小配最大，讓最大對和最小\n先看「頭尾配對」。題目「1877. 陣列中最大數對和的最小值」要你把 n 個數兩兩配對，讓所有對裡「最大的那一對和」越小越好。正確做法是排序後，最小配最大、第二小配第二大……\n\n```cpp\n// 頭尾配對：最小配最大，讓最大對和最小\nsort(a.begin(), a.end());\nint n = a.size(), best = 0;\nfor (int i = 0; i < n / 2; ++i) {\n  best = max(best, a[i] + a[n - 1 - i]);  // 兩端往中間夾\n}\n```\n:::\n\n\n\n為什麼安全？用交換論證：若某個最優解讓兩個大的配在一起，那一對的和會很大，把它拆開、各自去配小的，最大對和只會變小或不變。所以頭尾配是最優的。\n\n:::example 雙指標：最重者上船，能帶最輕者就一起帶\n再看「貪心塞容量」。題目「881. 救生艇」每艘船限重，最多載兩人，問最少幾艘船。排序後用雙指標：最重的一定要上船，看看能不能順便帶上最輕的。\n\n```cpp\n// 雙指標：最重者上船，能帶最輕者就一起帶\nint i = 0, j = n - 1, boats = 0;\nwhile (i <= j) {\n  if (a[i] + a[j] <= limit) {\n    ++i;  // 最輕者也搭上\n  }\n  j--;  // 最重者必上船\n  ++boats;\n}\n```\n:::\n\n\n\n兩題的共通骨架都是：**排序 + 雙指標從兩端往內掃**。\n\n**實作方式**\n\n:::example 第一步永遠是排序\n**1. 先排序，再決定配對方向。** 配對型問題幾乎都從排序開始。接著問自己：要「大配小」（平衡）還是「貪心地塞」（容量）。\n\n```cpp\n// 第一步永遠是排序\nsort(a.begin(), a.end());  // 升冪，方便用雙指標\n```\n:::\n\n\n\n:::example 雙指標初始化：i 指最小、j 指最大\n**2. 用雙指標代表「目前最小」與「目前最大」。** `i` 從左、`j` 從右，分別是還沒處理的最小與最大元素。\n\n```cpp\n// 雙指標初始化：i 指最小、j 指最大\nint i = 0, j = (int)a.size() - 1;\n```\n:::\n\n\n\n:::example 每步至少移動一個指標，更新答案\n**3. 每一步處理掉至少一個指標。** 根據能不能配、要不要配，移動 `i`、`j` 或兩者，並更新答案。重點是每步都讓區間縮小，保證 `O(n)` 掃完。\n\n```cpp\n// 每步至少移動一個指標，更新答案\nwhile (i < j) {\n  // ...依題目條件決定配對與移動...\n  ++i;\n  j--;  // 配成一對，兩端各前進\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 注意奇數長度時中間會剩一個元素（如 561 拆分）；`i == j` 是否要單獨處理也要想清楚。主成本是排序 `O(n log n)`，雙指標掃描 `O(n)`。像「2576. 求出最多標記下標」就是排序後用「小的去配後半段更大的」，是雙指標配對的進階變形。\n\n**題目線索（Pattern）**\n\n出現這些訊號，往「單序列配對」想：\n\n- **題目明說要「兩兩一組」「成對」「配對」「每組最多兩個」。** 這是最直接的訊號，像救生艇每船兩人、561 兩兩分組。\n- **要最小化「最大的那一組」或最大化「最小的那一組」。** 這種「極值的極值」最常用排序後頭尾配解掉，例如 1877。\n- **答案和「相鄰元素差」有關。** 例如「561. 陣列拆分」要讓每對較小值的總和最大，排序後相鄰兩兩配即可。\n\n:::example 想讓最大對和最小 → 頭尾配（小配大）\n判斷該用哪種配法，可記：\n\n```cpp\n// 想讓最大對和最小 → 頭尾配（小配大）\n// 想盡量塞滿容量   → 雙指標貪心塞\n// 想讓配對總收益最大 → 多半是相鄰配，排序後直接相鄰兩兩取\n```\n:::\n\n\n\n自我檢查三個問題：\n\n1. 我排序後，是「頭尾配」還是「相鄰配」？依據是什麼？\n2. 我能用交換論證說明「拆開重配不會更好」嗎？\n3. 序列長度是奇數時，多出來的那個元素怎麼辦？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 561 | 陣列拆分 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 881 | [救生艇](https://leetcode.cn/problems/boats-to-save-people/) | 1530 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1877 | [陣列中最大數對和的最小值](https://leetcode.cn/problems/minimize-maximum-pair-sum-in-array/) | 1301 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2144 | [打折購買糖果的最小開銷](https://leetcode.cn/problems/minimum-cost-of-buying-candies-with-discount/) | 1261 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2576 | [求出最多標記下標](https://leetcode.cn/problems/find-the-maximum-number-of-marked-indices/) | 1843 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 561 | [陣列拆分](https://leetcode.cn/problems/array-partition) |  |\n| 881 | [救生艇](https://leetcode.cn/problems/boats-to-save-people) | 1530 |\n| 1877 | [陣列中最大數對和的最小值](https://leetcode.cn/problems/minimize-maximum-pair-sum-in-array) | 1301 |\n| 2144 | [打折購買糖果的最小開銷](https://leetcode.cn/problems/minimum-cost-of-buying-candies-with-discount) | 1261 |\n| 2576 | [求出最多標記下標](https://leetcode.cn/problems/find-the-maximum-number-of-marked-indices) | 1843 |\n| 2592 | [最大化陣列的偉大值](https://leetcode.cn/problems/maximize-greatness-of-an-array) | 1569 |",
    },
    {
      id: 5,
      title: "3. 雙序列配對",
      summary:
        "**原理講解**\n\n本節的場景是：你手上有「兩個」序列，要把一邊的元素去配另一邊的元素，讓滿足條件的配對數最多、或總收益最好。例如把餅乾分給小孩、把工人分給工作、把我的牌去壓對方的牌。\n\n核心直覺是：**兩邊都排序，然後讓「剛剛好夠用」的去滿足「要求最低」的，把強的留給後面更難的需求。** 這樣不浪費，每一步都做最省的安全選擇。\n\n:::example 兩序列排序後雙指標配對：小餅乾優先餵小胃口\n先看「455. 分發餅乾」：每個小孩有胃口 `g[i]`，每塊餅乾有大小 `s[j]`，餅乾要 `>=` 胃口才能餵飽。目標是餵飽最多小孩。兩邊排序後用雙指標，從胃口最小的小孩開始，拿剛好夠大的餅乾餵他。\n\n```cpp\n// 兩序列排序後雙指標配對：小餅乾優先餵小胃口\nsort(g.begin(), g.end());\nsort(s.begin(), s.end());\nint i = 0, j = 0;  // i 指小孩、j 指餅乾\nwhile (i < g.size() && j < s.size()) {\n  if (s[j] >= g[i]) {\n    ++i;  // 餵飽一個小孩，換下一個\n  }\n  ++j;  // 這塊餅乾用掉（或太小丟棄）\n}\n// i 就是餵飽的人數\n```\n:::\n\n\n\n為什麼安全？交換論證：把「最小的餅乾」拿去餵「能被它餵飽、且胃口最小的小孩」絕不吃虧——因為更大的餅乾留著，能餵的小孩只會更多。若反而拿大餅乾餵小胃口，就浪費了它本可餵更大胃口的能力。\n\n「826. 安排工作以達到最大收益」是同一招的變形：把工人能力和工作難度都排序，能力越強的工人能做的工作只會更多，於是用一個「滑動的最大收益」記住目前所有他能勝任的工作裡收益最高的。\n\n**實作方式**\n\n:::example 兩邊都排序，方向通常一致（都升冪）\n**1. 兩個序列各自排序。** 雙序列配對的地基就是「兩邊都有序」，這樣雙指標才能單向前進。\n\n```cpp\n// 兩邊都排序，方向通常一致（都升冪）\nsort(a.begin(), a.end());\nsort(b.begin(), b.end());\n```\n:::\n\n\n\n:::example i 走需求方、j 走供給方\n**2. 用兩個指標分別走兩個序列。** 一個指「需求方」（小孩、工作），一個指「供給方」（餅乾、工人）。\n\n```cpp\n// i 走需求方、j 走供給方\nint i = 0, j = 0, matched = 0;\n```\n:::\n\n\n\n:::example 能配就配，不能配就讓供給方變強\n**3. 比較目前兩端，決定推進哪一邊。** 供給滿足需求 → 配成一對、兩邊都前進；供給不夠 → 只推進供給方去找更大的。\n\n```cpp\n// 能配就配，不能配就讓供給方變強\nwhile (i < a.size() && j < b.size()) {\n  if (b[j] >= a[i]) {\n    ++matched;\n    ++i;\n    ++j;\n  }  // 配成一對\n  else {\n    ++j;  // 供給太弱，換更強的\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 注意兩序列長度可能不同，任一邊走完就停。若不是「能不能配」而是「配了拿多少收益」，常需加一個堆或滑動最大值來挑最佳供給（如 826）。複雜度由排序主導，`O(n log n + m log m)`。像「870. 優勢洗牌（田忌賽馬）」就是：我的牌排序後，能贏就拿剛好能贏的去贏，贏不了就用最小的牌去墊。\n\n**題目線索（Pattern）**\n\n出現這些訊號，往「雙序列配對」想：\n\n- **題目有兩個獨立的清單，要把一邊配到另一邊。** 小孩 vs 餅乾、工人 vs 工作、我的隊伍 vs 對手隊伍（870、1433）。\n- **配對有「門檻條件」：A 要 `>=` 或 `<=` B 才合法。** 這種大小門檻，排序後雙指標最自然。\n- **目標是「最多配對數」或「總收益最大」。** 前者直接雙指標，後者常配一個堆挑最佳。\n\n:::example 需求被滿足 → 兩指標一起前進（消耗一對）\n判斷推進方向可記：\n\n```cpp\n// 需求被滿足 → 兩指標一起前進（消耗一對）\n// 供給不足   → 只推供給方，去找更強的\n// 想贏就用「剛好能贏」的，別浪費強者（田忌賽馬思路）\n```\n:::\n\n\n\n自我檢查三個問題：\n\n1. 哪一個序列是「需求方」、哪一個是「供給方」？\n2. 配對的合法條件（門檻）是什麼方向的不等式？\n3. 我要的是「配對數最多」還是「總收益最大」？後者要不要加堆？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 455 | 分發餅乾 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 826 | 安排工作以達到最大收益 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 870 | [優勢洗牌](https://leetcode.cn/problems/advantage-shuffle/) | 1648 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1433 | [檢查一個字串是否可以打破另一個字串](https://leetcode.cn/problems/check-if-a-string-can-break-another-string/) | 1436 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1889 | [裝包裹的最小浪費空間](https://leetcode.cn/problems/minimum-space-wasted-from-packaging/) | 2214 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 455 | [分發餅乾](https://leetcode.cn/problems/assign-cookies) |  |\n| 826 | [安排工作以達到最大收益](https://leetcode.cn/problems/most-profit-assigning-work) |  |\n| 870 | [優勢洗牌](https://leetcode.cn/problems/advantage-shuffle) | 1648 |\n| 1433 | [檢查一個字串是否可以打破另一個字串](https://leetcode.cn/problems/check-if-a-string-can-break-another-string) | 1436 |\n| 1889 | [裝包裹的最小浪費空間](https://leetcode.cn/problems/minimum-space-wasted-from-packaging) | 2214 |\n| 2037 | [使每位學生都有座位的最少移動次數](https://leetcode.cn/problems/minimum-number-of-moves-to-seat-everyone) | 1357 |\n| 2071 | [你可以安排的最多工數目](https://leetcode.cn/problems/maximum-number-of-tasks-you-can-assign) | 2648 |\n| 2323 | [完成所有工作的最短時間 II](https://leetcode.cn/problems/find-minimum-time-to-finish-all-jobs-ii) |  |\n| 2410 | [運動員和訓練師的最大匹配數](https://leetcode.cn/problems/maximum-matching-of-players-with-trainers) | 1381 |\n| 2449 | [使陣列相似的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-arrays-similar) | 2076 |\n| 2561 | [重排水果](https://leetcode.cn/problems/rearranging-fruits) | 2222 |",
    },
    {
      id: 6,
      title: "4. 從最左/最右開始貪心",
      summary:
        "**原理講解**\n\n本節的場景是：資料的順序本身有意義（不能隨便排序打亂），你要從序列的「一端」開始，一格一格往另一端掃，每到一格就立刻做決定、且不反悔。這叫「從最左／最右開始貪心」。\n\n它和前面「排序後貪心」最大的差別是：**這裡通常不能排序**，因為位置關係很重要（花壇相鄰、矩陣的欄位順序、字典序的先後）。所以我們改成沿著固定方向掃，靠「當下的局部資訊」就把這一格定下來。\n\n:::example 種花問題：從左到右，能種就種\n核心直覺是：**從一端開始，最早遇到的格子其選擇空間最受限制，先把它定死，反而能替後面留下最大彈性。** 例如種花，從左到右掃，只要「左空、右空、自己空」就種——因為越早能種就越不會擋到後面。\n\n```cpp\n// 種花問題：從左到右，能種就種\nint count = 0;\nfor (int i = 0; i < n; ++i) {\n  if (bed[i] == 0 && (i == 0 || bed[i - 1] == 0) &&  // 左邊空（或沒有左邊）\n      (i == n - 1 || bed[i + 1] == 0)) {  // 右邊空（或沒有右邊）\n    bed[i] = 1;                           // 種下，立刻佔位\n    ++count;\n  }\n}\n```\n:::\n\n\n\n為什麼從左到右地「能種就種」是安全的？交換論證：若某最優解在某個可種的位置選擇不種、卻在更右邊種，把它挪到左邊這個更早的位置種，絕不會減少總數，因為左邊種佔用的「右側資源」更少。這就是「越早決定越安全」的精神。「605. 種花問題」正是這個模型。\n\n**實作方式**\n\n:::example 方向二選一，視題目而定\n**1. 確定掃描方向。** 先想：是從左往右、還是從右往左更好決策？牽涉到「字典序最大／最小」「高位優先」時，方向選錯會做白工。\n\n```cpp\n// 方向二選一，視題目而定\nfor (int i = 0; i < n; ++i) { /* 從左到右 */\n}\nfor (int i = n - 1; i >= 0; i--) { /* 從右到左 */\n}\n```\n:::\n\n\n\n:::example 局部狀態：記住前一格的決定，供下一格判斷\n**2. 維護「決策所需的局部狀態」。** 每一格的決定常只依賴前一格或目前累積量（前一格種了沒、目前已用幾次操作）。把這個狀態一路帶著走。\n\n```cpp\n// 局部狀態：記住前一格的決定，供下一格判斷\nint prev = 0;                 // 例如前一格是否已種花/已翻轉\nfor (int i = 0; i < n; ++i) { /* 用 prev 決定，再更新 prev */\n}\n```\n:::\n\n\n\n:::example 鋸齒陣列：把當前峰值壓到比鄰居小（局部決策）\n**3. 每一格做不可反悔的安全選擇。** 用目前的局部狀態判斷這一格怎麼選，選完立刻定案。重點是「這一步不會傷害後面」。\n\n```cpp\n// 鋸齒陣列：把當前峰值壓到比鄰居小（局部決策）\n// 對偶數位 / 奇數位分別嘗試，取較少操作數\nops += max(0, a[i] - min(left, right) + 1);  // 需要壓多少\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 第一格、最後一格往往沒有「前一格／後一格」，要特判（如種花的邊界）。整個過程通常一次掃完，`O(n)`。像「861. 翻轉矩陣後的得分」就是「先固定第一欄全為 1（高位優先），再逐欄看 1 多還是 0 多」；「955. 刪列造序 II」則是從左到右逐欄決定刪不刪，維護「哪些列已嚴格遞增」的狀態。\n\n**題目線索（Pattern）**\n\n出現這些訊號，往「從最左／最右開始貪心」想：\n\n- **元素順序固定、不能排序。** 花壇位置、矩陣欄位、字串字元的先後都不能打亂——這排除了「先排序再貪心」。\n- **要最佳化字典序、或「高位優先」。** 數值的高位、字典序的前面字元影響最大，通常從那一端開始定死（如 861 先固定第一欄）。\n- **每一格的決定只看鄰居或目前累積量。** 像種花看左右鄰、鋸齒陣列看相鄰大小（1144），是典型的局部決策。\n\n:::example 不能排序 + 順序有意義 + 一格一格定案 → 從一端掃的貪心\n辨識小抄：\n\n```cpp\n// 不能排序 + 順序有意義 + 一格一格定案 → 從一端掃的貪心\n// 要字典序/高位最優 → 從影響最大的那一端開始\n```\n:::\n\n\n\n自我檢查三個問題：\n\n1. 這題能不能排序？若不能，是什麼讓順序變得重要？\n2. 我該從左還是從右掃？哪一端的決定影響最大？\n3. 每一格做決定時，我需要記住哪些「前面的局部狀態」？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 605 | 種花問題 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 861 | [翻轉矩陣後的得分](https://leetcode.cn/problems/score-after-flipping-matrix/) | 1818 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 955 | [刪列造序 II](https://leetcode.cn/problems/delete-columns-to-make-sorted-ii/) | 1876 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1144 | [遞減元素使陣列呈鋸齒狀](https://leetcode.cn/problems/decrease-elements-to-make-array-zigzag/) | 1559 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1488 | [避免洪水氾濫](https://leetcode.cn/problems/avoid-flood-in-the-city/) | 1974 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 605 | [種花問題](https://leetcode.cn/problems/can-place-flowers) |  |\n| 861 | [翻轉矩陣後的得分](https://leetcode.cn/problems/score-after-flipping-matrix) | 1818 |\n| 955 | [刪列造序 II](https://leetcode.cn/problems/delete-columns-to-make-sorted-ii) | 1876 |\n| 1144 | [遞減元素使陣列呈鋸齒狀](https://leetcode.cn/problems/decrease-elements-to-make-array-zigzag) | 1559 |\n| 1488 | [避免洪水氾濫](https://leetcode.cn/problems/avoid-flood-in-the-city) | 1974 |\n| 1529 | [最少的字尾翻轉次數](https://leetcode.cn/problems/minimum-suffix-flips) | 1393 |\n| 1536 | [排布二進位制網格的最少交換次數](https://leetcode.cn/problems/minimum-swaps-to-arrange-a-binary-grid) | 1881 |\n| 1827 | [最少操作使陣列遞增](https://leetcode.cn/problems/minimum-operations-to-make-the-array-increasing) | 1315 |\n| 2027 | [轉換字串的最少操作次數](https://leetcode.cn/problems/minimum-moves-to-convert-string) | 1346 |\n| 2086 | [餵食倉鼠的最小食物桶數](https://leetcode.cn/problems/minimum-number-of-food-buckets-to-feed-the-hamsters) | 1623 |\n| 2366 | [將陣列排序的最少替換次數](https://leetcode.cn/problems/minimum-replacements-to-sort-the-array) | 2060 |\n| 2422 | [使用合併操作將陣列轉換為迴文序列](https://leetcode.cn/problems/merge-operations-to-turn-array-into-a-palindrome) |  |\n| 2528 | [最大化城市的最小電量](https://leetcode.cn/problems/maximize-the-minimum-powered-city) | 2236 |\n| 2571 | [將整數減少到零需要的最少運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-an-integer-to-0) | 1649 |\n| 2673 | [使二叉樹所有路徑值相等的最小代價](https://leetcode.cn/problems/make-costs-of-paths-equal-in-a-binary-tree) | 1917 |\n| 2789 | [合併後陣列中的最大元素](https://leetcode.cn/problems/largest-element-in-an-array-after-merge-operations) | 1485 |\n| 2957 | [消除相鄰近似相等字元](https://leetcode.cn/problems/remove-adjacent-almost-equal-characters) | 1430 |\n| 3111 | [覆蓋所有點的最少矩形數目](https://leetcode.cn/problems/minimum-rectangles-to-cover-points) | 1401 |\n| 3191 | [使二進位制陣列全部等於 1 的最少操作次數 I](https://leetcode.cn/problems/minimum-operations-to-make-binary-array-elements-equal-to-one-i) | 1312 |\n| 3192 | [使二進位制陣列全部等於 1 的最少操作次數 II](https://leetcode.cn/problems/minimum-operations-to-make-binary-array-elements-equal-to-one-ii) | 1433 |\n| 3228 | [將 1 移動到末尾的最大操作次數](https://leetcode.cn/problems/maximum-number-of-operations-to-move-ones-to-the-end) | 1593 |\n| 3326 | [使陣列非遞減的最少除法操作次數](https://leetcode.cn/problems/minimum-division-operations-to-make-array-non-decreasing) | 1864 |\n| 3402 | [使每一列嚴格遞增的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-columns-strictly-increasing) | 1246 |\n| 3449 | [最大化遊戲分數的最小值](https://leetcode.cn/problems/maximize-the-minimum-game-score) | 2748 |\n| 3576 | [陣列元素相等轉換](https://leetcode.cn/problems/transform-array-to-all-equal-elements) | 1489 |\n| 3776 | [使迴圈陣列餘額非負的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-balance-circular-array) | 1740 |",
    },
    {
      id: 7,
      title: "5. 劃分型貪心",
      summary:
        "**原理講解**\n\n本節的場景是：要把一個序列「切成好幾段（劃分）」，每一段滿足某個條件，目標是段數最多、或切的代價最小。例如把字串切成一段段平衡子串、把陣列切成「最大減最小 `<=` K」的若干組。\n\n核心直覺是：**從左到右掃，只要當前這一段「剛好可以結束」，就立刻切一刀，然後從下一個元素開始全新的一段。** 越早切，剩下的元素越多、後面能切的段也越多——所以「能切就切」是安全的。\n\n:::example 平衡字串劃分：計數歸零就切一段\n先看「1221. 分割平衡字串」：字串只有 L 和 R，平衡指 L 和 R 數量相等，要切出最多段。做法是維護一個計數器，遇 L 加一、遇 R 減一，每當歸零就代表一段平衡子串結束，切一刀。\n\n```cpp\n// 平衡字串劃分：計數歸零就切一段\nint balance = 0, segments = 0;\nfor (char c : s) {\n  balance += (c == 'L') ? 1 : -1;  // L 加、R 減\n  if (balance == 0) {\n    ++segments;  // 歸零 → 可切一段\n  }\n}\n```\n:::\n\n\n\n為什麼「歸零就切」最優？交換論證：若某最優解在某次歸零時「不切、繼續併到後面」，那它把兩段合成一段，段數只會更少不會更多。所以每次能切就切，段數最大化。\n\n:::example 無重複字元劃分：遇到重複就切，開新段\n「2405. 子字串的最優劃分」是另一種味道：要切成「每段內字元都不重複」的最少段數。做法是從左掃，用一個集合記目前這段出現過哪些字元，一旦遇到重複，就在這裡切、開新的一段。\n\n```cpp\n// 無重複字元劃分：遇到重複就切，開新段\nunordered_set<char> seen;\nint segments = 1;\nfor (char c : s) {\n  if (seen.count(c)) {\n    ++segments;\n    seen.clear();\n  }  // 重複 → 切\n  seen.insert(c);\n}\n```\n:::\n\n\n\n**實作方式**\n\n:::example 段結束條件範例（依題目擇一）\n**1. 想清楚「一段合法」的條件。** 這是劃分型貪心的靈魂：平衡（計數歸零）、無重複（集合不衝突）、極差受限（最大減最小 `<=` K）。先把「何時一段可以結束」寫成一個明確判斷。\n\n```cpp\n// 段結束條件範例（依題目擇一）\n// balance == 0            // 平衡\n// !seen.count(c)          // 不重複\n// cur - segMin <= k       // 極差不超過 K\n```\n:::\n\n\n\n:::example 維護目前段的摘要（這裡以集合為例）\n**2. 從左到右掃，維護「目前這段」的摘要。** 摘要可能是一個計數、一個集合、或這段的最小值。每讀一個元素就更新摘要。\n\n```cpp\n// 維護目前段的摘要（這裡以集合為例）\nunordered_set<char> seen;\nfor (char c : s) { /* 用 seen 判斷，再更新 seen */\n}\n```\n:::\n\n\n\n:::example 切一刀：段數加一，摘要重置\n**3. 條件達成就「切一刀」並重置摘要。** 切的動作就是「段數 +1、把摘要清空、從目前元素重新開一段」。某些題目（如極差型）要先排序再分組。\n\n```cpp\n// 切一刀：段數加一，摘要重置\n++segments;\nseen.clear();  // 開始全新的一段\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 注意掃到結尾時「最後一段有沒有算到」（計數型常用歸零自動算到，集合型要記得初始 `segments = 1`）。掃描 `O(n)`；若需先排序（如「2294. 劃分陣列使最大差為 K」排序後貪心分組）則 `O(n log n)`。「1546. 和為目標值且不重疊的非空子陣列的最大數目」是用前綴和判斷「目前段和達標就切」，思路相同。\n\n**題目線索（Pattern）**\n\n出現這些訊號，往「劃分型貪心」想：\n\n- **題目要把序列「切成幾段／分成幾組」，且段內有局部條件。** 「最多分幾段」「最少分幾組」幾乎都是劃分貪心，如 1221、2405、2358。\n- **段與段彼此獨立，前一段切在哪不影響後一段能不能合法。** 這種獨立性讓「能切就切」成立。\n- **暴力枚舉所有切法是指數級的，而一次掃描就能解。** 從 `O(2^n)` 降到 `O(n)`。\n\n:::example 求「最多段」→ 條件一達成就立刻切（貪心）\n辨識小抄：\n\n```cpp\n// 求「最多段」→ 條件一達成就立刻切（貪心）\n// 求「最少組」→ 排序後讓每組盡量塞滿再開新組\n```\n:::\n\n\n\n自我檢查三個問題：\n\n1. 「一段合法／可以結束」的精確條件是什麼？\n2. 我目前這段需要維護什麼摘要（計數、集合、最小值）？\n3. 掃到結尾時，最後一段有沒有被正確算進去？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1221 | [分割平衡字串](https://leetcode.cn/problems/split-a-string-in-balanced-strings/) | 1220 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1546 | [和為目標值且不重疊的非空子陣列的最大數目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-subarrays-with-sum-equals-target/) | 1855 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2294 | [劃分陣列使最大差為 K](https://leetcode.cn/problems/partition-array-such-that-maximum-difference-is-k/) | 1416 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2358 | [分組的最大數量](https://leetcode.cn/problems/maximum-number-of-groups-entering-a-competition/) | 1503 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2405 | [子字串的最優劃分](https://leetcode.cn/problems/optimal-partition-of-string/) | 1355 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1221 | [分割平衡字串](https://leetcode.cn/problems/split-a-string-in-balanced-strings) | 1220 |\n| 1546 | [和為目標值且不重疊的非空子陣列的最大數目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-subarrays-with-sum-equals-target) | 1855 |\n| 2294 | [劃分陣列使最大差為 K](https://leetcode.cn/problems/partition-array-such-that-maximum-difference-is-k) | 1416 |\n| 2358 | [分組的最大數量](https://leetcode.cn/problems/maximum-number-of-groups-entering-a-competition) | 1503 |\n| 2405 | [子字串的最優劃分](https://leetcode.cn/problems/optimal-partition-of-string) | 1355 |\n| 2436 | [使子陣列最大公約數大於一的最小分割數](https://leetcode.cn/problems/minimum-split-into-subarrays-with-gcd-greater-than-one) |  |\n| 2522 | [將字串分割成值不超過 K 的子字串](https://leetcode.cn/problems/partition-string-into-substrings-with-values-at-most-k) | 1605 |\n| 2892 | [將相鄰元素相乘後得到最小化陣列](https://leetcode.cn/problems/minimizing-array-after-replacing-pairs-with-their-product) |  |\n| 3557 | [不相交子字串的最大數量](https://leetcode.cn/problems/find-maximum-number-of-non-intersecting-substrings) | 1720 |",
    },
    {
      id: 8,
      title: "6. 先列舉，再貪心",
      summary:
        "**原理講解**\n\n本節的場景是：一個答案同時受「兩個變數」牽動，直接同時最佳化兩者很難。技巧是——**先「列舉（枚舉）」其中一個變數，把它固定下來；一旦固定，另一個變數就只剩一個容易求的最佳值。** 列舉完所有可能，取整體最好的那個。這就是「先列舉，再貪心」。\n\n為什麼有效？因為「固定一個維度」之後，剩下的子問題往往退化成「在前綴／後綴裡找最大或最小」這種一眼就能貪心解的小問題。難的二維搜尋，被拆成「列舉一維 × 每次 O(1) 貪心」。\n\n:::example 列舉賣出點 j，貪心取左側最小作買入點\n舉個迷你例子：給一個陣列，求 `j > i` 時 `a[j] - a[i]` 的最大值（買賣股票一次）。列舉「賣出點 `j`」，那麼買入點當然要選「`j` 左邊的最小值」——這個最小值可以邊掃邊維護。\n\n```cpp\n// 列舉賣出點 j，貪心取左側最小作買入點\nint minLeft = a[0], best = 0;\nfor (int j = 1; j < n; ++j) {\n  best = max(best, a[j] - minLeft);  // 先用「歷史摘要」更新答案\n  minLeft = min(minLeft, a[j]);      // 再把 j 併入摘要\n}\n```\n:::\n\n\n\n注意上面兩行的順序：**先用「目前累積的摘要」算答案，再把目前元素加進摘要。** 這樣才不會「自己配自己」（買賣同一天），這是這個技巧最容易出錯的細節。\n\n「2171. 拿出最少數目的魔法豆」就是典型：排序後列舉「保留哪一個高度當作統一高度」，那麼答案的另一半（要拿走多少豆）就由前綴和直接算出。「1727. 重新排列後的最大子矩陣」則是列舉「以哪一欄為右界、哪一高度為底」，每次貪心地把夠高的欄排到一起。\n\n**實作方式**\n\n:::example 列舉哪一個量？選「固定後另一邊變簡單」的那個\n**1. 選對「列舉的維度」。** 通常選那個「固定後另一邊最好算」的維度。選錯維度會讓子問題仍然很難。\n\n```cpp\n// 列舉哪一個量？選「固定後另一邊變簡單」的那個\nfor (int j = 0; j < n; ++j) { /* 固定 j，求另一邊最佳 */\n}\n```\n:::\n\n\n\n:::example 摘要：前綴最小值，讓每個 j 都能 O(1) 取到左側最佳\n**2. 維護「另一側」的摘要，支援 O(1) 查最佳。** 摘要可以是前綴最小／最大、計數表、前綴和、或一個堆。它把「另一邊的最佳值」隨手存好。\n\n```cpp\n// 摘要：前綴最小值，讓每個 j 都能 O(1) 取到左側最佳\nint prefMin = INT_MAX;\n```\n:::\n\n\n\n:::example 固定順序：先更新答案，後加入摘要\n**3. 嚴守「先讀摘要、再更新摘要」的順序。** 對每個列舉點：先用現有摘要更新答案，再把目前點併入摘要。順序顛倒就會把自己也算進去。\n\n```cpp\n// 固定順序：先更新答案，後加入摘要\nfor (int j = 0; j < n; ++j) {\n  if (j > 0) {\n    best = max(best, a[j] - prefMin);  // 1) 讀摘要\n  }\n  prefMin = min(prefMin, a[j]);  // 2) 加自己\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 若需要「刪掉過期摘要」（例如只看最近 K 個），把前綴最小值換成 deque、heap 或 set。整體常是 `O(n)` 或 `O(n log n)`，遠勝枚舉所有配對的 `O(n^2)`。像「2910. 合法分組的最少組數」要列舉「每組大小」，再貪心檢查所有頻次能不能被切成這種大小；「2234. 花園的最大總美麗值」則是列舉「完成多少座滿級花園」，另一半用前綴和＋二分貪心地補。\n\n**題目線索（Pattern）**\n\n出現這些訊號，往「先列舉，再貪心」想：\n\n- **答案由兩個量共同決定，但固定其一後另一個就好求。** 「枚舉右端點，左端點取最值」「枚舉分組大小，其餘貪心驗證」都是這種味道。\n- **暴力是枚舉所有配對 `O(n^2)` 或更高，需要降一個維度。** 用摘要把「另一邊」壓成 O(1) 查詢。\n- **題目出現「對每個 i，找左邊／右邊符合某條件的最佳 j」。** 這幾乎一定是「列舉 i ＋ 維護另一側摘要」。\n\n:::example 兩個變數糾纏 → 固定一個（列舉），另一個變單純最值\n辨識小抄：\n\n```cpp\n// 兩個變數糾纏 → 固定一個（列舉），另一個變單純最值\n// 警惕順序：先用摘要算答案，再把自己加進摘要，避免自配自\n```\n:::\n\n\n\n自我檢查三個問題：\n\n1. 我要列舉哪一個維度？固定它之後，另一邊是不是真的變簡單了？\n2. 「另一側」的最佳值，我用什麼摘要 O(1) 取得（前綴最值／前綴和／堆）？\n3. 我有沒有守住「先讀摘要、再加入自己」的順序，避免重複或自配？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1727 | [重新排列後的最大子矩陣](https://leetcode.cn/problems/largest-submatrix-with-rearrangements/) | 1927 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2171 | [拿出最少數目的魔法豆](https://leetcode.cn/problems/removing-minimum-number-of-magic-beans/) | 1748 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2234 | [花園的最大總美麗值](https://leetcode.cn/problems/maximum-total-beauty-of-the-gardens/) | 2562 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2749 | [得到整數零需要執行的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-the-integer-zero/) | 2132 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2910 | [合法分組的最少組數](https://leetcode.cn/problems/minimum-number-of-groups-to-create-a-valid-assignment/) | 2132 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1727 | [重新排列後的最大子矩陣](https://leetcode.cn/problems/largest-submatrix-with-rearrangements) | 1927 |\n| 2171 | [拿出最少數目的魔法豆](https://leetcode.cn/problems/removing-minimum-number-of-magic-beans) | 1748 |\n| 2234 | [花園的最大總美麗值](https://leetcode.cn/problems/maximum-total-beauty-of-the-gardens) | 2562 |\n| 2749 | [得到整數零需要執行的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-the-integer-zero) | 2132 |\n| 2910 | [合法分組的最少組數](https://leetcode.cn/problems/minimum-number-of-groups-to-create-a-valid-assignment) | 2132 |\n| 3085 | [成為 K 特殊字串需要刪除的最少字元數](https://leetcode.cn/problems/minimum-deletions-to-make-string-k-special) | 1765 |",
    },
    {
      id: 9,
      title: "7. 交換論證法",
      summary:
        "**觀念起點**\n\n貪心題的重點是證明局部選擇不破壞最優性。僅靠「我覺得先選這個比較好」並不算解法，必須以交換論證、反悔或排序不等式加以說明。\n\n反悔貪心先暫時接受候選，待限制被破壞時再從已接受集合中移除最不值得保留者；heap 的角色即是快速找出該被反悔的元素。\n\n此類題常存在一個逐步擴張的限制，例如時間、資源、前綴和或可到達位置。掃描順序使限制單調變化，heap 或 set 則維護當下仍可保留的候選集合。\n\n貪心的正確性有三條常見路徑：交換論證、反悔（先選再撤回）與擬陣理論。能舉出一個反例即足以否定貪心，此時改用 DP 或搜尋枚舉決策。\n\n---\n\n**代表例題：LeetCode 1642 可以到達的最遠建築**\n\n**完整問題**：LeetCode 1642 給定建築高度 `heights`、磚塊數 `bricks` 與梯子數 `ladders`。由第 i 棟走到第 i+1 棟時，若高度增加 `diff > 0`，須消耗 `diff` 個磚塊或 1 把梯子；若高度不增加則不消耗資源。回傳在資源限制下能到達的最遠建築下標。\n\n本節以 LeetCode 1642「可以到達的最遠建築」 為主例題（rating 1962），並將同一套不變式延伸至本節其他題目。\n\n**題目訊號**\n\n- 僅於向上爬時消耗資源。\n- 梯子宜保留給目前遇過的最大爬升。\n- 掃描至某一步方知先前的資源分配是否需要反悔。\n\n**狀態、不變式與答案更新**\n\n- max-heap 保存目前暫時以磚塊支付的所有爬升高度。\n- `used_bricks` 為扣除反悔後仍由磚塊支付的爬升總和。\n- 當 `used_bricks > bricks` 時，將 heap 中最大爬升改由梯子支付，即一次反悔。\n- 若已無梯子可反悔且磚塊仍超限，當前邊無法跨過，答案即為前一棟建築。\n\n**從零建模**\n\n1. 由左至右掃描相鄰建築，略過高度未增加的邊。\n2. 先假設每個正爬升皆以磚塊支付，將 `diff` 加入 `used_bricks` 並放入 max-heap。\n3. 若磚塊超限，從 heap 取出目前最大爬升改以一把梯子支付。\n4. 反悔後若梯子用盡且磚塊仍超限，代表無法跨過當前邊。\n5. 能完成所有邊時，答案即為最後一棟建築的下標。\n\n**小範例手動模擬**\n\n以反悔貪心選最多工作（各有截止與耗時）為例：依截止排序逐一加入，並把耗時推入大根堆；一旦累積耗時超過當前截止，就彈出耗時最大的工作（反悔）。堆中數量即答案。\n\n**常見模式**\n\n- 反悔堆\n- 先用便宜資源承擔，再將最大代價改交予稀缺資源\n- 掃描到違規時移除已選集合中最值得替換的元素\n\n**常見錯誤**\n\n- 本題並非見最大爬升即立刻用梯子，而是先暫定、超限時方反悔。\n- heap 中應放爬升高度，而非建築高度或下標。\n- 僅 `diff > 0` 時方需消耗資源。\n- 排序比較器需為嚴格弱序（用 `<` 而非 `<=`），否則 `sort` 行為未定義。\n\n**複雜度**：每個正爬升至多進出 heap 一次，時間 `O(n log n)`，空間 `O(n)`。 排序加堆操作，時間 `O(n log n)`、空間 `O(n)`。\n\n---\n\n**本節題單中的延伸題**\n\n- LeetCode 179 最大數\n- LeetCode 1877 陣列中最大數對和的最小值，rating 1301\n- LeetCode 2895 最小處理時間，rating 1352\n- LeetCode 3309 連線二進位製表示可形成的最大數值，rating 1364\n- LeetCode 3457 喫披薩，rating 1704\n\n---\n\n**同一模板的套用要點**\n\n能用交換論證或反悔證明才套貪心；舉得出反例就改用 DP 或搜尋枚舉決策。\n\n---\n\n**C++ 實作骨架**\n\n```cpp\nclass Solution {\n public:\n  int furthestBuilding(vector<int>& heights, int bricks, int ladders) {\n    priority_queue<int> paid_by_bricks;\n    long long used_bricks = 0;\n\n    for (int i = 0; i + 1 < (int)heights.size(); ++i) {\n      int climb = heights[i + 1] - heights[i];\n      if (climb <= 0) {\n        continue;\n      }\n\n      used_bricks += climb;\n      paid_by_bricks.push(climb);\n\n      if (used_bricks > bricks) {\n        if (ladders == 0) {\n          return i;\n        }\n        used_bricks -= paid_by_bricks.top();\n        paid_by_bricks.pop();\n        ladders--;\n      }\n    }\n    return heights.size() - 1;\n  }\n};\n```",
      children: [
        {
          id: 43,
          title: "7.1 交換論證法",
          summary:
            "交換論證法（exchange argument）用於證明一類貪心演算法的正確性，也可以用來啟發思考。做法如下：\n1. 對於題目，猜想按照「某種順序」處理資料，可以得到最優解。\n2. 交換順序中的兩個元素 $a_i$ 和 $a_j$，計算交換後的答案。\n3. 對比交換前後的答案。如果交換後，答案沒有變得更優，則說明猜想成立。\n\n也可以不用猜想，而是計算「先 $a_i$ 後 $a_j$」和「先 $a_j$ 後 $a_i$」對應的答案，透過比較兩個答案誰更優，來確定按照何種順序處理資料。\n\n:::example 交換論證法：自定義排序規則\n[講解（以 2895 題為例）](https://leetcode.cn/problems/minimum-processing-time/solution/tan-xin-pythonjavacgo-by-endlesscheng-8fzf/)\n\n```cpp\n// 交換論證法：自定義排序規則\n// 假設相鄰兩元素 a, b，比較 a 在前 vs b 在前的結果\nsort(items.begin(), items.end(), [](auto& a, auto& b) {\n  // 根據題意推導排序不等式\n  return a.cost * b.weight < b.cost * a.weight;\n});\n```\n:::\n\n\n\n本節為「7. 交換論證法」的延伸與變形：題幹包裝更活，常混雜次要條件或需多步轉化。\n\n**練習流程**：\n1. 默寫上級節核心不變式與結束條件\n2. 列出與主模板不同的約束，逐條對應修改點\n3. 用小測資手跑，特別是空、單元素、全相等、極值\n\n**本主線加強**：把題意轉成「決策序列／區間／圖」其中一類，再對照主節模板逐項檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 179 | [最大數](https://leetcode.cn/problems/largest-number) |  |\n| 1877 | [陣列中最大數對和的最小值](https://leetcode.cn/problems/minimize-maximum-pair-sum-in-array) | 1301 |\n| 2895 | [最小處理時間](https://leetcode.cn/problems/minimum-processing-time) | 1352 |\n| 3309 | [連線二進位製表示可形成的最大數值](https://leetcode.cn/problems/maximum-possible-number-by-binary-concatenation) | 1364 |\n| 3457 | [喫披薩](https://leetcode.cn/problems/eat-pizzas) | 1704 |\n| 1665 | [完成所有任務的最少初始能量](https://leetcode.cn/problems/minimum-initial-energy-to-finish-tasks) | 1901 |\n| 3273 | [對 Bob 造成的最少傷害](https://leetcode.cn/problems/minimum-amount-of-damage-dealt-to-bob) | 2013 |\n| 2136 | [全部開花的最早一天](https://leetcode.cn/problems/earliest-possible-day-of-full-bloom) | 2033 |\n| 2561 | [重排水果](https://leetcode.cn/problems/rearranging-fruits) | 2222 |",
        },
        {
          id: 11,
          title: "7.2 相似題目",
          summary:
            "**原理講解**\n\n本節聚焦「交換論證法」的應用：當題目要你決定一堆事情的「處理順序」時，怎麼證明某個排序規則是最優的？工具就是交換論證（exchange argument）——這也是貪心類題目最核心的證明手法。\n\n交換論證的邏輯是這樣的：假設最優順序裡有「相鄰兩個」沒照我的規則排，我把它們對調，計算對調後結果是否變好（或不變）。如果「只要不照規則排、交換一下就不會更差」，那把所有逆序的相鄰對都換正，最後就得到「完全照規則排」的順序，且它一定最優。\n\n:::example 交換論證的精神：只比較相鄰 a、b 誰先比較好\n關鍵在於找出「兩個相鄰元素 a、b 該誰先」的比較式。方法是只比較這兩個的相對先後對結果的影響，其他元素不動。\n\n```cpp\n// 交換論證的精神：只比較相鄰 a、b 誰先比較好\n// 若「a 在前的代價」<=「b 在前的代價」，就讓 a 在前\nsort(items.begin(), items.end(), [](auto& a, auto& b) {\n  return costAfirst(a, b) < costBfirst(a, b);  // 自訂比較鍵\n});\n```\n:::\n\n\n\n來看本節的代表題「2412. 完成所有交易的初始最少錢數」。每筆交易有花費 `cost` 和回收 `cashback`，做完淨變化是 `cashback - cost`。你要安排做交易的順序，讓「過程中需要的初始本金」最少。這就是典型的「排序順序」問題：先做哪些、後做哪些，會決定本金需求的峰值。\n\n:::example 虧損交易先做（cashback 小者優先），盈利交易後做（cost 大者優先）\n直覺結論是：**會「虧錢」的交易（cost > cashback）先做，且其中 cashback 小的先做；會「賺錢」的交易後做，且其中 cost 大的先做。** 這個分組與排序規則，正是用交換論證一對一對比出來的。\n\n```cpp\n// 虧損交易先做（cashback 小者優先），盈利交易後做（cost 大者優先）\nsort(t.begin(), t.end(), [](auto& a, auto& b) {\n  bool la = a[0] > a[1], lb = b[0] > b[1];  // 是否虧損\n  if (la != lb) {\n    return la;  // 虧損的排前面\n  }\n  return la ? a[1] < b[1] : a[0] > b[0];  // 組內各自的鍵\n});\n```\n:::\n\n\n\n**實作方式**\n\n:::example 訊號：答案隨處理順序改變 → 考慮排序 + 交換論證\n**1. 確認題目是在問「順序」。** 交換論證只適用於「重排處理順序使某指標最優」的題目。先確定「換順序會改變答案」。\n\n```cpp\n// 訊號：答案隨處理順序改變 → 考慮排序 + 交換論證\n```\n:::\n\n\n\n:::example 比較式：a 先不比 b 先差 → a 排在前\n**2. 推出「相鄰兩元素誰該先」的比較式。** 取任意相鄰 a、b，寫出「a 先」與「b 先」兩種情況的代價，令前者不差於後者，整理成比較函式。這一步是全題的核心。\n\n```cpp\n// 比較式：a 先不比 b 先差 → a 排在前\nauto cmp = [](const T& a, const T& b) {\n  return keyAfirst(a, b) <= keyBfirst(a, b);\n};\n```\n:::\n\n\n\n:::example 排序後線性模擬，追蹤所需初始本金\n**3. 依比較式排序，再線性模擬求答案。** 排好序後，照順序掃一遍，邊做邊追蹤需要的指標（如目前現金、峰值需求）。\n\n```cpp\n// 排序後線性模擬，追蹤所需初始本金\nlong long need = 0, cur = 0;\nfor (auto& x : t) {\n  need = max(need, cur + x.cost);  // 做這筆前需要的本金峰值\n  cur += x.cashback - x.cost;      // 做完後現金的淨變化\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 一定要驗證你的比較式滿足「全序」（不會出現 a<b、b<c 卻 c<a 的矛盾），否則 `sort` 行為未定義。複雜度由排序主導，`O(n log n)`。若交換後「不一定不變差」，代表這題不是單純排序貪心，可能要 DP。\n\n**題目線索（Pattern）**\n\n出現這些訊號，往「交換論證法／相似題目」想：\n\n- **題目要你決定「先做哪個、後做哪個」的順序，使某指標最優。** 排程、做交易順序（2412）、拼接數字成最大數等都是。\n- **元素兩兩之間有明確的「誰先比較好」關係。** 能寫出相鄰兩個的比較式，就能排序。\n- **暴力是試所有 `n!` 種排列，顯然不可行。** 交換論證把它收斂成「一次排序」。\n\n:::example 問「最佳順序」+ 能比出相鄰兩個誰先 → 交換論證 + 自訂排序\n辨識小抄：\n\n```cpp\n// 問「最佳順序」+ 能比出相鄰兩個誰先 → 交換論證 + 自訂排序\n// 比較式務必是全序，否則 sort 會出錯\n```\n:::\n\n\n\n自我檢查三個問題：\n\n1. 這題的答案真的會隨「處理順序」改變嗎？\n2. 任取相鄰兩個元素，我能寫出「誰排前面更好」的比較式嗎？\n3. 我的比較式是合法的全序嗎（沒有循環矛盾）？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2412 | [完成所有交易的初始最少錢數](https://leetcode.cn/problems/minimum-money-required-before-transactions/) | 2092 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2412 | [完成所有交易的初始最少錢數](https://leetcode.cn/problems/minimum-money-required-before-transactions) | 2092 |",
        },
      ],
    },
    {
      id: 12,
      title: "8. 相鄰不同",
      summary:
        '**原理講解**\n\n本節要解決的場景是：你手上有一堆「同類元素」（例如同一個字母、同一種任務），規則要求**相同的東西不能緊挨著排在一起**，問你能不能排好、或最少要花多少時間排完。代表題如 767 重構字串、621 任務排程器、358 K 距離間隔重排字串。\n\n先建立直覺。如果某種元素特別多，多到「就算其他元素全塞進它們中間也隔不開」，那一定排不出來。例如字串 `aaab`，三個 `a` 至少需要兩個非 `a` 來當間隔（`a?a?a`），但只有一個 `b`，必敗。這給了我們第一個判斷式：設出現最多的元素數量為 `maxCnt`，總長為 `n`，可行的必要條件是 `maxCnt - 1 <= n - maxCnt`（中間的縫隙夠不夠塞）。\n\n:::example 用大根堆隨時取出「目前剩餘最多」的元素\n那要怎麼排才不會自打嘴巴？核心貪心規則是：**每一輪都優先放「目前剩餘數量最多」的那個元素**。原因是數量最多的最難安置，越晚處理越容易卡住，所以要趁早消化它。為了「隨時拿到剩餘最多者」，我們用大根堆（`priority_queue`）按剩餘次數排序。\n\n```cpp\n// 用大根堆隨時取出「目前剩餘最多」的元素\npriority_queue<pair<int, int>> pq;  // {剩餘次數, 元素}\nfor (auto [ch, cnt] : count) {\n  pq.push({cnt, ch});\n}\n```\n:::\n\n\n\n:::example 放完的元素先擱置，等下一輪再放回堆，避免相鄰\n但「最多者」剛放完，下一步不能馬上又放它（會相鄰）。所以技巧是：**放完一個就先把它擱到一旁「冷卻」，等放了別的元素後再放回堆裡。** 這樣就保證同一個元素之間一定隔著別人。\n\n```cpp\n// 放完的元素先擱置，等下一輪再放回堆，避免相鄰\nauto cur = pq.top();\npq.pop();  // 放一個目前最多的\nresult.push_back(cur.second);\ncur.first--;  // 用掉一個\n              // 先別 push 回去，等放完下一個元素再放回（見實作）\n```\n:::\n\n\n\n直覺上這保證最優：每一步都先消化最危險（最多）的元素，又用「冷卻」確保不相鄰，所以只要必要條件成立就一定能排出來。\n\n**實作方式**\n\n以「每一步放一個」的版本（767 / 358 通用）拆成四步：\n\n:::example 統計次數並建立大根堆\n**1. 統計與建堆。** 先數每個元素出現幾次，全部丟進大根堆，堆頂永遠是剩餘最多者。\n\n```cpp\n// 統計次數並建立大根堆\nunordered_map<char, int> cnt;\nfor (char c : s) {\n  cnt[c]++;\n}\npriority_queue<pair<int, int>> pq;\nfor (auto [c, k] : cnt) {\n  pq.push({k, c});\n}\n```\n:::\n\n\n\n:::example 一次處理兩個不同元素，天然滿足相鄰不同\n**2. 每輪取兩個。** 一次從堆頂拿出「最多」和「次多」兩個元素各放一個——因為這兩個必定不同，放在一起就保證相鄰不同。\n\n```cpp\n// 一次處理兩個不同元素，天然滿足相鄰不同\nauto a = pq.top();\npq.pop();\nres += a.second;\na.first--;\nif (!pq.empty()) {\n  auto b = pq.top();\n  pq.pop();\n  res += b.second;\n  b.first--;\n  if (b.first > 0) {\n    pq.push(b);  // 還有剩就放回\n  }\n}\nif (a.first > 0) {\n  pq.push(a);  // a 放回，下一輪才能再用（已隔一個）\n}\n```\n:::\n\n\n\n:::example 只剩一種卻還有剩 → 必相鄰 → 無解\n**3. 判斷無解。** 若迴圈中某次只剩一種元素卻還有剩（`pq` 只剩 1 個且次數 > 1），代表它一定會相鄰，回傳空字串表示失敗。\n\n```cpp\n// 只剩一種卻還有剩 → 必相鄰 → 無解\nif (a.first > 0 && pq.empty()) {\n  return "";\n}\n```\n:::\n\n\n\n**4. 帶冷卻時間的變體（621 任務排程器）。** 若要求相同元素至少間隔 `k`，把「擱置」改成一個固定長度為 `k` 的等待佇列：放完的元素帶著「下次可用時間」進佇列，時間到才放回堆；佇列沒人可放時就讓 CPU 待機（idle）。複雜度為 `O(n log m)`，`m` 是元素種類數。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「相鄰不同 / 最大堆貪心」想：\n\n- **限制是「相同的不能挨在一起」**，或「相同的兩次之間至少隔 `k` 個」。任務排程器的「冷卻時間」就是這種限制的偽裝。\n- **答案通常是「能否排出」「最短長度」或「重排後的字串」**，而排列的自由度來自「先放誰」。\n- **直覺是『先消化最多的最安全』**，因為最多的最難塞，這正是大根堆登場的理由：\n\n:::example 嗅到味道：你想反覆「拿出目前剩最多的那個」\n```cpp\n// 嗅到味道：你想反覆「拿出目前剩最多的那個」\n// 普通陣列每次都要重掃 O(n) 找最大 → 改用大根堆 O(log n)\n```\n:::\n\n\n\n- **暴力做法是枚舉所有排列再檢查**（階乘級，必爆），而本技巧把它壓成 `O(n log m)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「最多者優先」規則，為什麼不會把局面排死？（想清楚必要條件 `maxCnt - 1 <= n - maxCnt`）\n2. 我用什麼結構隨時取出剩餘最多的元素？（大根堆）\n3. 放完一個元素後，我怎麼確保它下次不會緊接著出現？（擱置／冷卻佇列）\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 358 | K 距離間隔重排字串（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 621 | 任務排程器 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 767 | 重構字串 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2335 | [裝滿杯子需要的最短總時長](https://leetcode.cn/problems/minimum-amount-of-time-to-fill-cups/) | 1360 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3659 | [陣列元素分組](https://leetcode.cn/problems/partition-array-into-k-distinct-groups/) | 1440 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 358 | [K 距離間隔重排字串](https://leetcode.cn/problems/rearrange-string-k-distance-apart) |  |\n| 621 | [任務排程器](https://leetcode.cn/problems/task-scheduler) |  |\n| 767 | [重構字串](https://leetcode.cn/problems/reorganize-string) |  |\n| 2335 | [裝滿杯子需要的最短總時長](https://leetcode.cn/problems/minimum-amount-of-time-to-fill-cups) | 1360 |\n| 3659 | [陣列元素分組](https://leetcode.cn/problems/partition-array-into-k-distinct-groups) | 1440 |\n| 984 | [不含 AAA 或 BBB 的字串](https://leetcode.cn/problems/string-without-aaa-or-bbb) | 1474 |\n| 1753 | [移除石子的最大得分](https://leetcode.cn/problems/maximum-score-from-removing-stones) | 1488 |\n| 1054 | [距離相等的條形碼](https://leetcode.cn/problems/distant-barcodes) | 1702 |\n| 2856 | [刪除數對後的最小陣列長度](https://leetcode.cn/problems/minimum-array-length-after-pair-removals) | 1750 |\n| 1953 | [你可以工作的最大週數](https://leetcode.cn/problems/maximum-number-of-weeks-for-which-you-can-work) | 1804 |\n| 1405 | [最長快樂字串](https://leetcode.cn/problems/longest-happy-string) | 1821 |\n| 3785 | [避免禁用值的最小交換次數](https://leetcode.cn/problems/minimum-swaps-to-avoid-forbidden-values) | 2052 |\n| 3664 | [兩個字母卡牌遊戲](https://leetcode.cn/problems/two-letter-card-game) | 2158 |\n| 3495 | [使陣列元素都變為零的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-array-elements-zero) | 2206 |\n| 3139 | [使陣列中所有元素相等的最小開銷](https://leetcode.cn/problems/minimum-cost-to-equalize-array) | 2666 |',
    },
    {
      id: 14,
      title: "9. 反悔貪心",
      summary:
        "**原理講解**\n\n本節談「反悔貪心（regret greedy）」。一般貪心是「一旦選了就不回頭」，但有些題目，前面看似最好的選擇，到後面才發現吃虧了。反悔貪心的精神是：**先大膽把每個能拿的都拿下，事後若違反限制，再從已拿的之中『退掉一個最划算的退法』。** 代表題如 871 最低加油次數、1642 可以到達的最遠建築、630 課程表 III、LCP 30 魔塔遊戲。\n\n用 871 加油來體會。你開著車往前開，每經過一個加油站就把「這站能加的油量」記下來但不一定要加。當油不夠走到下一站時，你才回頭問：「我經過的那些站裡，哪一站油最多？就在那站加！」這就是「反悔」——你把加油的決定延後到真正需要時，並且永遠選最有價值的那次來補。\n\n:::example 把沿途候選收進大根堆，需要時取出「最划算」的那個\n為什麼要用堆？因為「退掉/補上最划算的一項」就是「取出已收集候選中的最大（或最小）值」，這正是堆的拿手好戲。\n\n```cpp\n// 把沿途候選收進大根堆，需要時取出「最划算」的那個\npriority_queue<int> pq;  // 存每個加油站可加的油量\npq.push(station_fuel);   // 經過就先記下，不急著用\n```\n:::\n\n\n\n:::example 反悔的瞬間：違規了，就退掉「最不划算的已選項」換成更好的\n為什麼這樣是對的？關鍵不變式是：**「到目前為止，我所做的選擇，等價於『最優策略在這個前綴上會做的選擇』。」** 每次反悔都用一個更好的選擇替換一個較差的，總價值只會變好不會變差，所以掃完整個輸入時手上的就是最優解。這是一種「邊走邊修正」的交換論證。\n\n```cpp\n// 反悔的瞬間：違規了，就退掉「最不划算的已選項」換成更好的\nif (fuel < 0 && !pq.empty()) {\n  fuel += pq.top();  // 在油最多的那站補加\n  pq.pop();\n  ++refuels;\n}\n```\n:::\n\n\n\n**實作方式**\n\n反悔貪心通常可拆成四步，逐一配程式碼（以 871 為骨架）：\n\n:::example 狀態：目前油量 + 反悔次數 + 候選堆\n**1. 狀態表示：堆存「可反悔的候選」。** 已做選擇的摘要（剩餘資源、已用次數）用變數記，候選池用堆。\n\n```cpp\n// 狀態：目前油量 + 反悔次數 + 候選堆\nint fuel = startFuel, refuels = 0;\npriority_queue<int> pq;  // 沿途可加油量（候選）\n```\n:::\n\n\n\n:::example 掃描每個加油站：先記候選，再判斷夠不夠到這裡\n**2. 更新流程：按掃描順序，先推候選、再檢查限制。** 每經過一個元素，先把它的「可用價值」推進堆（代表「我有權選它」），再檢查目前狀態是否違規。\n\n```cpp\n// 掃描每個加油站：先記候選，再判斷夠不夠到這裡\nfor (auto& st : stations) {\n  fuel -= dist_to(st);  // 開到這站消耗\n  pq.push(st.fuel);     // 這站可加的油，先記下\n                        // 接下來若 fuel < 0 就進入反悔（見下）\n}\n```\n:::\n\n\n\n:::example 油不夠就一直在「最多油的站」補，直到夠或無油可補\n**3. 反悔修正：違規時退掉/補上最划算者，反覆直到合法。** 用 `while` 是因為一次反悔可能還不夠。\n\n```cpp\n// 油不夠就一直在「最多油的站」補，直到夠或無油可補\nwhile (fuel < 0 && !pq.empty()) {\n  fuel += pq.top();\n  pq.pop();\n  ++refuels;\n}\nif (fuel < 0) {\n  return -1;  // 補光了還是不夠 → 不可能到達\n}\n```\n:::\n\n\n\n**4. 答案讀取與複雜度。** 掃完後，反悔次數（871）、已選集合大小（630 能修幾門課）、或構造結果就是答案。每個候選最多進出堆一次，整體 `O(n log n)`。\n\n注意 630 課程表 III 的方向相反：先全收下每門課，**超過截止期限時退掉「耗時最長」的已選課**（大根堆存耗時），道理一樣是「退最划算的退法」。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「反悔貪心」想：\n\n- **可以「先收下再說」，違規時再撤銷某個之前的決定。** 限制通常是「資源有限」「次數有限」「期限到了」。\n- **撤銷時你想挑『最划算的一項』退掉/補上**——這就是堆要存的東西：\n\n:::example 嗅到味道：你想反覆「在已選的東西裡挑最大/最小來反悔」\n```cpp\n// 嗅到味道：你想反覆「在已選的東西裡挑最大/最小來反悔」\n// 線性掃描找最值太慢 → 用堆把每次反悔壓到 O(log n)\n```\n:::\n\n\n\n- **普通一刀切貪心會被反例打臉**（前面貪心選了反而害後面），於是改成「延後決定、可撤銷」。\n- **暴力做法是枚舉「哪些要選、哪些不選」**（指數級），反悔貪心把它壓成 `O(n log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我先「無條件收下」的是什麼？違規的判斷條件是什麼？\n2. 反悔時我要退掉/補上「最划算」的哪一項？它存在哪個堆裡（大根還小根）？\n3. 為什麼用更好的替換較差的，總解只會變好不會變差？（說得出交換論證才算套對）\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 30 | 魔塔遊戲 | N/A | 適合練反悔貪心；先接受候選，違法時用堆刪掉最不划算的一項。 |\n| 630 | 課程表 III | N/A | 適合練反悔貪心；先接受候選，違法時用堆刪掉最不划算的一項。 |\n| 871 | [最低加油次數](https://leetcode.cn/problems/minimum-number-of-refueling-stops/) | 2074 | 適合練反悔貪心；先接受候選，違法時用堆刪掉最不划算的一項。 |\n| 1642 | [可以到達的最遠建築](https://leetcode.cn/problems/furthest-building-you-can-reach/) | 1962 | 適合練反悔貪心；先接受候選，違法時用堆刪掉最不划算的一項。 |\n| 2599 | 使字首和陣列非負（Premium） | N/A | 適合練反悔貪心；先接受候選，違法時用堆刪掉最不划算的一項。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 630. 課程表 III 展示核心寫法。\n\n**完整題目**：每門課有修課時間與截止日，選最多課程，使每門被選課程都能在截止日前完成。\n\n:::example 貪心 + 最大堆：按截止日期排序，超時時替換掉耗時最長的課程\n**線索**：題目同時有「修課耗時」與「截止日」，掃描到某個截止日前若總時間超標，就必須從已選課程中反悔刪掉耗時最長者，這正是最大堆維護淘汰候選的線索。\n\n```cpp\nclass Solution {\n public:\n  // 貪心 + 最大堆：按截止日期排序，超時時替換掉耗時最長的課程\n  int scheduleCourse(vector<vector<int>>& courses) {\n    // 按截止日期升序排序，確保先考慮早截止的課\n    sort(courses.begin(), courses.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    priority_queue<int> pq;  // 最大堆，存已選課程的持續時間\n    int time = 0;            // 當前累計所用時間\n    for (auto& c : courses) {\n      time += c[0];\n      pq.push(c[0]);\n      // 若超過截止時間，捨棄堆頂（耗時最長）的課程以騰出時間\n      if (time > c[1]) {\n        time -= pq.top();\n        pq.pop();\n      }\n    }\n    return pq.size();  // 堆中剩餘課程即為最多可修數量\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 課程先按截止日排序，確保掃描到目前為止只需滿足當前及更早的截止日。\n- 最大堆保存已選課程的耗時；一旦總時間超過當前截止日，就刪掉耗時最長的一門課。\n- 刪掉最長課程後，已選課程數少一但總時間降最多，留下的集合最有機會容納後續課程。\n\n**常見變形**\n\n- 課程表、最低加油次數、魔塔遊戲。\n- 最大堆刪最大耗時，最小堆補最小代價。\n- 可與排序截止時間搭配。\n\n**實作注意事項**\n\n- 堆中存反悔依據，不一定是原值。\n- 每次違法只反悔到恢復合法。\n- 排序順序通常是截止時間或觸發順序。\n- 證明要說明被刪項在已選集合中最差。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 30 | [魔塔遊戲](https://leetcode.cn/problems/p0NxJO) |  |\n| 630 | [課程表 III](https://leetcode.cn/problems/course-schedule-iii) |  |\n| 871 | [最低加油次數](https://leetcode.cn/problems/minimum-number-of-refueling-stops) | 2074 |\n| 1642 | [可以到達的最遠建築](https://leetcode.cn/problems/furthest-building-you-can-reach) | 1962 |\n| 2599 | [使字首和陣列非負](https://leetcode.cn/problems/make-the-prefix-sum-non-negative) |  |\n| 2813 | [子序列最大優雅度](https://leetcode.cn/problems/maximum-elegance-of-a-k-length-subsequence) | 2582 |\n| 3049 | [標記所有下標的最早秒數 II](https://leetcode.cn/problems/earliest-second-to-mark-indices-ii) | 3111 |\n| 3362 | [零陣列變換 III](https://leetcode.cn/problems/zero-array-transformation-iii) | 2424 |\n| 3711 | [不出現負餘額的最大交易額](https://leetcode.cn/problems/maximum-transactions-without-negative-balance) |  |",
    },
    {
      id: 15,
      title: "10. 不相交區間",
      summary:
        "**原理講解**\n\n本節談「不相交區間（interval scheduling）」：給你一堆有頭有尾的區間（會議、任務、數對），要你**挑出盡量多個彼此不重疊的區間**，或反過來算「最少刪掉幾個才能讓剩下的都不重疊」。代表題如 435 無重疊區間、646 最長數對鏈、1520 最多的不重疊子字串。\n\n:::example 關鍵：按結束時間（右端點）由小到大排序\n核心貪心規則只有一句：**按「右端點（結束時間）」由小到大排序，能不重疊就貪心地選。** 為什麼是右端點而不是左端點？因為一個區間結束得越早，它「擋住」後面的時間就越少，留給後續區間的空間就越大。先選結束最早的，等於替後面保留了最多機會。\n\n```cpp\n// 關鍵：按結束時間（右端點）由小到大排序\nsort(a.begin(), a.end(), [](auto& x, auto& y) {\n  return x[1] < y[1];  // x[1] 是右端點\n});\n```\n:::\n\n\n\n:::example 不重疊就選：左端點 >= 上一個結束位置\n選的時候維護一個變數 `lastEnd`，記「上一個被選中區間的結束位置」。掃描時，只要當前區間的左端點 `>= lastEnd`，代表它和已選的都不重疊，就選它並更新 `lastEnd`。\n\n```cpp\n// 不重疊就選：左端點 >= 上一個結束位置\nif (a[i][0] >= lastEnd) {\n  ++count;            // 多選一個\n  lastEnd = a[i][1];  // 更新結束位置\n}\n```\n:::\n\n\n\n為什麼這樣是最優？用交換論證：假設最優解第一個選的區間結束時間比「我們選的最早結束區間」還晚，那把它換成我們這個更早結束的，不會和最優解的其他區間衝突（因為更早結束只會更安全），所以替換後仍是合法解且數量不變。一路換下去，貪心解就和最優解一樣多。\n\n**實作方式**\n\n以 435 / 646（求最多不重疊個數）為骨架，拆成四步：\n\n:::example 按右端點升序，這是不相交區間貪心的標準排序\n**1. 排序：以右端點為鍵。** 這是整個技巧的靈魂，排錯鍵就證不出最優。\n\n```cpp\n// 按右端點升序，這是不相交區間貪心的標準排序\nsort(intervals.begin(), intervals.end(),\n     [](auto& a, auto& b) { return a[1] < b[1]; });\n```\n:::\n\n\n\n:::example lastEnd 記上一個選中區間的右端點\n**2. 初始化：lastEnd 設為極小值。** 表示「還沒選任何區間」，第一個區間一定能選。\n\n```cpp\n// lastEnd 記上一個選中區間的右端點\nint lastEnd = INT_MIN, count = 0;\n```\n:::\n\n\n\n:::example 掃一遍：左端點不早於 lastEnd 就選\n**3. 掃描：不重疊才選，並更新 lastEnd。** 重疊的就跳過（等於「刪掉它」）。\n\n```cpp\n// 掃一遍：左端點不早於 lastEnd 就選\nfor (auto& it : intervals) {\n  if (it[0] >= lastEnd) {  // 視題意有時用 > （端點可共用時用 >=）\n    ++count;\n    lastEnd = it[1];\n  }\n}\n```\n:::\n\n\n\n:::example 最多不重疊 = count；最少刪除 = 總數 - count\n**4. 讀答案與邊界。** `count` 是「最多能選的不重疊個數」；若題目要「最少刪除數」，答案就是 `總數 - count`（435 正是如此）。\n\n```cpp\n// 最多不重疊 = count；最少刪除 = 總數 - count\nreturn (int)intervals.size() - count;\n```\n:::\n\n\n\n注意「端點相接算不算重疊」要看題意：若 `[1, 2]` 與 `[2, 3]` 視為不重疊，用 `>=`；若視為重疊，用 `>`。646 數對鏈的「鏈接」要求嚴格 `a[1] < b[0]`，就要用嚴格不等號。複雜度由排序主導，`O(n log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「不相交區間貪心」想：\n\n- **輸入是一堆 [start, end] 區間**（會議、任務、數對、子字串範圍），目標是「最多互不重疊」或「最少刪除使其不重疊」。\n- **你直覺想排序，但糾結該按起點還終點。** 答案幾乎都是「按終點」，因為終點早＝佔用少：\n\n:::example 嗅到味道：選了一個就想知道「下一個不衝突的最早是誰」\n```cpp\n// 嗅到味道：選了一個就想知道「下一個不衝突的最早是誰」\n// 按右端點排序後，從左到右掃一遍就能貪心選出最多個\n```\n:::\n\n\n\n- **這是「選最多」型；若題目是『最少刪除』就取補集**，兩者是同一個貪心的一體兩面。\n- **暴力做法是枚舉所有子集檢查是否互不重疊**（指數級），本技巧排序後一次掃描即 `O(n log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我按哪個端點排序？為什麼是右端點而不是左端點？\n2. 我用什麼變數記「上一個選中區間的結束位置」？更新時機在哪？\n3. 端點相接（`a[1] == b[0]`）在這題算不算重疊？我的不等號（`>` 還是 `>=`）對嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 435 | 無重疊區間 | N/A | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 646 | 最長數對鏈 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1520 | [最多的不重疊子字串](https://leetcode.cn/problems/maximum-number-of-non-overlapping-substrings/) | 2363 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3458 | [選擇 K 個互不重疊的特殊子字串](https://leetcode.cn/problems/select-k-disjoint-special-substrings/) | 2221 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 435 | [無重疊區間](https://leetcode.cn/problems/non-overlapping-intervals) |  |\n| 646 | [最長數對鏈](https://leetcode.cn/problems/maximum-length-of-pair-chain) |  |\n| 1520 | [最多的不重疊子字串](https://leetcode.cn/problems/maximum-number-of-non-overlapping-substrings) | 2363 |\n| 3458 | [選擇 K 個互不重疊的特殊子字串](https://leetcode.cn/problems/select-k-disjoint-special-substrings) | 2221 |",
    },
    {
      id: 16,
      title: "11. 區間分組",
      summary:
        "**原理講解**\n\n本節談「區間分組（interval partitioning）」：給你一堆有頭有尾的區間（會議、課程、任務），要把它們分成**最少的組數**，使得**同一組裡的區間彼此不重疊**。代表題如 253 會議室 II、2406 將區間分為最少組數。\n\n換個生活化說法：每組就像「一間會議室」，同一間會議室同時只能開一場會。問題就變成：最少需要幾間會議室，才能讓所有會議都開成？\n\n關鍵洞察是一個下界：**答案 = 任一時刻同時「正在進行」的區間數量的最大值。** 想像在數線上某個時間點切一刀，這一刻有 `k` 個區間同時覆蓋它，那這 `k` 個彼此都重疊，必須分到 `k` 個不同的組，所以至少要 `k` 組。而貪心可以證明「最大同時重疊數」這個下界一定能達到，所以它就是答案。\n\n:::example 小根堆存「各組最早結束時間」，堆大小就是當前用到的組數\n怎麼算「最大同時重疊」？兩種等價做法。第一種是**最小堆**：堆裡存「每個目前開著的組，最早何時結束」。新區間來時，若堆頂（最早結束的組）已經在它開始前結束了，就重用那個組；否則只能開新組。\n\n```cpp\n// 小根堆存「各組最早結束時間」，堆大小就是當前用到的組數\npriority_queue<int, vector<int>, greater<int>> pq;\n// 新區間 [s, e]：能接上最早結束的組就重用，否則開新組\nif (!pq.empty() && pq.top() <= s) {\n  pq.pop();  // 重用該組\n}\npq.push(e);  // 放進這一組\n             // 全部處理完，pq.size() 就是最少組數\n```\n:::\n\n\n\n:::example 掃描線：開始事件 +1、結束事件 -1，求同時開著的峰值\n第二種是**差分 / 掃描線**：把每個 `[s, e]` 拆成「`s` 時刻 +1，`e + 1` 時刻 -1」，排序後掃一遍求前綴和的最大值，那個峰值就是答案。\n\n```cpp\n// 掃描線：開始事件 +1、結束事件 -1，求同時開著的峰值\nvector<pair<int, int>> ev;  // {時間, +1 或 -1}\nfor (auto& it : a) {\n  ev.push_back({it[0], 1});\n  ev.push_back({it[1] + 1, -1});\n}\nsort(ev.begin(), ev.end());\nint cur = 0, best = 0;\nfor (auto& [t, d] : ev) {\n  cur += d;\n  best = max(best, cur);\n}\n```\n:::\n\n\n\n**實作方式**\n\n以最小堆版本（253 / 2406 通用）為骨架，拆成四步：\n\n:::example 按開始時間升序處理區間\n**1. 按左端點（開始時間）排序。** 這樣才能依時間先後處理，判斷「新會議來時哪些組已空出」。\n\n```cpp\n// 按開始時間升序處理區間\nsort(intervals.begin(), intervals.end(),\n     [](auto& a, auto& b) { return a[0] < b[0]; });\n```\n:::\n\n\n\n:::example 小根堆：堆頂 = 最早結束的那一組\n**2. 用小根堆追蹤各組的結束時間。** 堆頂是「最早會空出來的組」。\n\n```cpp\n// 小根堆：堆頂 = 最早結束的那一組\npriority_queue<int, vector<int>, greater<int>> ends;\n```\n:::\n\n\n\n:::example 新區間能接在最早結束的組後面就重用，否則新增一組\n**3. 逐一安置：能重用就重用，否則開新組。** 堆的大小即「目前需要的組數」。\n\n```cpp\n// 新區間能接在最早結束的組後面就重用，否則新增一組\nfor (auto& it : intervals) {\n  if (!ends.empty() && ends.top() <= it[0]) {\n    ends.pop();  // 重用\n  }\n  ends.push(it[1]);  // 安置進某組\n}\n```\n:::\n\n\n\n:::example 堆內元素數 = 同時開著的組的最大值 = 最少組數\n**4. 讀答案與邊界。** 全部安置完，堆的大小就是最少組數。\n\n```cpp\n// 堆內元素數 = 同時開著的組的最大值 = 最少組數\nreturn (int)ends.size();\n```\n:::\n\n\n\n注意「結束與開始相接算不算衝突」要看題意：若 `[1, 2]` 結束後 `[2, 3]` 可共用一間，用 `ends.top() <= s`；若不可共用要用 `<`。複雜度：排序 `O(n log n)`，堆操作 `O(n log n)`，總計 `O(n log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「區間分組」想：\n\n- **輸入是一堆 [start, end] 區間，要分成最少組／用最少『資源』，且同組不能重疊。** 「最少會議室」「最少車道」「最少分組」都是它。\n- **問的是『最少幾組』而非『最多選幾個』**——這是和「不相交區間（選最多）」最大的分野：分組是全部都要安置，不是挑出子集。\n- **你會想知道『某一刻最多幾個同時進行』**，這就是堆或掃描線登場的時機：\n\n:::example 嗅到味道：你想追蹤「此刻有幾個區間同時開著」的峰值\n```cpp\n// 嗅到味道：你想追蹤「此刻有幾個區間同時開著」的峰值\n// 小根堆存結束時間，或差分掃描線求前綴和最大值\n```\n:::\n\n\n\n- **暴力做法是枚舉所有分組方式**（指數級），本技巧排序後一次掃描即 `O(n log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能說清「答案 = 最大同時重疊數」這個下界，並相信貪心能達到它？\n2. 我用小根堆存什麼？堆的大小代表什麼意義？\n3. 「結束時間 == 下一個開始時間」在這題算衝突嗎？我的不等號對嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 253 | 會議室 II（Premium） | N/A | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 2406 | [將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups/) | 1713 | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 253 | [會議室 II](https://leetcode.cn/problems/meeting-rooms-ii) |  |\n| 2406 | [將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups) | 1713 |",
    },
    {
      id: 17,
      title: "12. 區間選點",
      summary:
        "**原理講解**\n\n本節談「區間選點（interval point selection）」：給你一堆有頭有尾的區間，要你在數線上**放下盡量少的「點」，使每個區間裡至少有一個點。** 代表題如 452 用最少數量的箭引爆氣球、757 設定交集大小至少為 2、LCP 32 批次處理任務、2589 完成所有任務的最少時間。\n\n用 452 射氣球體會：每個氣球橫跨一段區間 `[x_start, x_end]`，一支箭從某個 x 座標垂直射上去，能戳破所有「覆蓋這個 x」的氣球。問最少幾支箭能戳光全部？這就是「每個區間至少被一個點命中、求最少點數」。\n\n:::example 關鍵：按右端點升序排序\n核心貪心規則：**按「右端點」由小到大排序，每次把點放在『當前還沒被命中的區間的右端點』上。** 為什麼放右端點？因為點放得越靠右，越可能順便覆蓋到後面那些「左端點較小」的區間，一個點能命中越多區間越省。\n\n```cpp\n// 關鍵：按右端點升序排序\nsort(a.begin(), a.end(), [](auto& x, auto& y) {\n  return x[1] < y[1];  // x[1] 是右端點\n});\n```\n:::\n\n\n\n:::example 現有點戳不到這個區間 → 新放一個點在它的右端點\n掃描時維護「上一個放的點的位置 `pos`」。遇到一個區間，若它的左端點 `> pos`，代表現有的點戳不到它，必須新放一個點——放在它的右端點上，因為這個位置最可能兼顾後續區間。\n\n```cpp\n// 現有點戳不到這個區間 → 新放一個點在它的右端點\nif (a[i][0] > pos) {\n  ++arrows;\n  pos = a[i][1];  // 點放最右，盡量覆蓋後面的區間\n}\n```\n:::\n\n\n\n為什麼最優？交換論證：對「右端點最小」的那個區間，最優解必須在它之內放一個點；把那個點挪到它的右端點，只會讓覆蓋範圍往右擴大、不會漏掉任何已命中的區間，所以挪過去不吃虧。重複此論證，貪心就和最優一樣省。\n\n**實作方式**\n\n以 452（求最少點數 / 箭數）為骨架，拆成四步：\n\n:::example 按右端點升序，這是區間選點貪心的標準排序\n**1. 排序：以右端點為鍵。** 同「不相交區間」，右端點排序是這類技巧的標準動作。\n\n```cpp\n// 按右端點升序，這是區間選點貪心的標準排序\nsort(points.begin(), points.end(),\n     [](auto& a, auto& b) { return a[1] < b[1]; });\n```\n:::\n\n\n\n:::example 第一個點放在第一個區間的右端點\n**2. 初始化：放第一個點在第一個區間的右端點。** 至少需要一個點。\n\n```cpp\n// 第一個點放在第一個區間的右端點\nint arrows = 1;\nlong pos = points[0][1];\n```\n:::\n\n\n\n:::example 左端點超過現有點 → 這支點蓋不到，必須再放一個\n**3. 掃描：戳不到才新放點，位置取右端點。** 戳得到的就「免費覆蓋」，跳過。\n\n```cpp\n// 左端點超過現有點 → 這支點蓋不到，必須再放一個\nfor (auto& p : points) {\n  if (p[0] > pos) {  // 注意嚴格 > ：端點相接視為可命中\n    ++arrows;\n    pos = p[1];\n  }\n}\n```\n:::\n\n\n\n**4. 讀答案與邊界。** `arrows` 即最少點數。要小心「端點相接算不算命中」：452 中 `[1, 2]` 和 `[2, 3]` 共用 x=2 即可命中，所以用嚴格 `>`。757「交集至少為 2」需要每個區間命中兩個點，要維護「最後兩個放的點」並排序鍵更講究，但骨架仍是右端點貪心。複雜度由排序主導，`O(n log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「區間選點」想：\n\n- **要在數線上放最少的『點 / 箭 / 標記』，讓每個區間都被命中至少一次（或 757 的至少兩次）。** 關鍵字：最少箭、最少時刻、最少資源覆蓋全部。\n- **這是「不相交區間」的對偶問題**：那邊是「選最多互不重疊的區間」，這邊是「放最少的點戳穿所有區間」，神奇的是兩者排序鍵都是右端點。\n- **你會想『一個點盡量多覆蓋幾個區間』**，於是把點往右端點擺：\n\n:::example 嗅到味道：放一個點時，你想讓它順便覆蓋後面盡量多的區間\n```cpp\n// 嗅到味道：放一個點時，你想讓它順便覆蓋後面盡量多的區間\n// 按右端點排序後，點永遠放在右端點，貪心掃一遍即可\n```\n:::\n\n\n\n- **暴力做法是枚舉點的位置或子集**（指數級），本技巧排序後一次掃描即 `O(n log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我按右端點排序後，為什麼把點放在右端點而不是左端點或中間？\n2. 我用什麼變數記「上一個放的點的位置」？什麼條件下才需要再放一個點？\n3. 端點相接（`a[0] == pos`）算命中嗎？我的不等號（`>` 還是 `>=`）對嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 32 | 批次處理任務 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 452 | 用最少數量的箭引爆氣球 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 757 | 設定交集大小至少為2 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2589 | [完成所有任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-tasks/) | 2381 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 32 | [批次處理任務](https://leetcode.cn/problems/t3fKg1) |  |\n| 452 | [用最少數量的箭引爆氣球](https://leetcode.cn/problems/minimum-number-of-arrows-to-burst-balloons) |  |\n| 757 | [設定交集大小至少為2](https://leetcode.cn/problems/set-intersection-size-at-least-two) |  |\n| 2589 | [完成所有任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-tasks) | 2381 |",
    },
    {
      id: 18,
      title: "13. 區間覆蓋",
      summary:
        "**原理講解**\n\n本節談「區間覆蓋（interval covering）」：給你一個要被完整蓋住的「目標範圍」（例如 `[0, n]`），和一堆能各蓋一小段的區間，要你**用最少的區間把整個目標範圍蓋滿**。代表題如 45 跳躍遊戲 II、1024 影片拼接、1326 灌溉花園的最少水龍頭數目。\n\n換個說法：你要從起點走到終點，每個區間是一塊「踏板」，站在踏板上能踩到的最遠處由它的右端點決定。問最少踩幾塊踏板能到終點？這就是覆蓋。\n\n核心貪心規則：**在「目前已覆蓋到的位置」之內，能往右延伸最遠的那個區間就是下一步要選的。** 直覺是：既然非選不可，當然選那個讓覆蓋邊界跳最遠的，這樣後面要選的次數才最少。\n\n:::example 兩個邊界：curEnd 是本步覆蓋終點，farthest 是下一步能達到的最遠\n實作上維護兩個邊界：`curEnd`（目前這一「步」已確定能覆蓋到哪）和 `farthest`（在 `curEnd` 範圍內，下一步最遠能延伸到哪）。掃描所有起點 `<= curEnd` 的區間，更新 `farthest`；當位置走到 `curEnd` 時，就「踩下一塊踏板」：`curEnd = farthest`，計數加一。\n\n```cpp\n// 兩個邊界：curEnd 是本步覆蓋終點，farthest 是下一步能達到的最遠\nint curEnd = 0, farthest = 0, count = 0;\n// 在 curEnd 之內，不斷把 farthest 推到最遠（見實作）\n```\n:::\n\n\n\n:::example 跳躍遊戲 II：到達本步邊界就「起跳」一次，跳到目前能及的最遠\n這其實和 45 跳躍遊戲 II 一模一樣：`nums[i]` 表示從 i 能跳的最遠距離，等於區間 `[i, i + nums[i]]`，求最少跳幾次到終點。\n\n```cpp\n// 跳躍遊戲 II：到達本步邊界就「起跳」一次，跳到目前能及的最遠\nif (i == curEnd) {  // 走到本步邊界，必須再跳一次\n  ++count;\n  curEnd = farthest;  // 新邊界 = 之前累積的最遠\n}\n```\n:::\n\n\n\n為什麼最優？交換論證：第一步若不選「延伸最遠」的區間，把它換成延伸最遠的那個，覆蓋只會更廣、不會更窄，所需步數不會變多。逐步替換，貪心解就和最優一樣少。\n\n**實作方式**\n\n以「給一堆 [l, r] 區間，覆蓋 [0, T]」的通用版（1024 / 1326）為骨架，拆四步：\n\n:::example 灌溉花園：把每個水龍頭轉成「從 l 起，最遠能澆到 r」\n**1. 預處理：按左端點排序，或轉成「每個起點能達到的最遠」陣列。** 後者對 1326 灌溉花園很方便。\n\n```cpp\n// 灌溉花園：把每個水龍頭轉成「從 l 起，最遠能澆到 r」\nvector<int> reach(n + 1, 0);\nfor (int i = 0; i <= n; ++i) {\n  int l = max(0, i - ranges[i]), r = min(n, i + ranges[i]);\n  reach[l] = max(reach[l], r);  // 起點 l 能延伸到的最遠處\n}\n```\n:::\n\n\n\n:::example 逐位置推進：本步範圍內不斷更新最遠可達\n**2. 維護 curEnd / farthest 兩個邊界，逐格推進。** 在本步邊界內把 `farthest` 拉到最遠。\n\n```cpp\n// 逐位置推進：本步範圍內不斷更新最遠可達\nint curEnd = 0, farthest = 0, count = 0;\nfor (int i = 0; i < T; ++i) {  // 注意只掃到 T-1\n  farthest = max(farthest, reach[i]);\n  // 到邊界要「踩下一步」（見步驟 3）\n}\n```\n:::\n\n\n\n:::example 走到本步邊界：必須再用一個區間；若推不動則無法覆蓋\n**3. 到邊界就計數推進；推不動就失敗。** 若 `farthest` 沒超過 `i`，代表有縫隙蓋不住。\n\n```cpp\n// 走到本步邊界：必須再用一個區間；若推不動則無法覆蓋\nif (i == curEnd) {\n  if (farthest <= i) {\n    return -1;  // 卡住，蓋不滿\n  }\n  ++count;\n  curEnd = farthest;\n}\n```\n:::\n\n\n\n**4. 讀答案與邊界。** 覆蓋到終點所用的區間數即答案；中途出現 `farthest <= i` 表示有空隙，回傳 -1（或題目規定的失敗值）。複雜度 `O(n)`（已排序或可直接索引時）或 `O(n log n)`（需先排序時）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「區間覆蓋」想：\n\n- **目標是『用最少的區間 / 步數 / 跳躍蓋滿一段連續範圍或走到終點』。** 關鍵字：最少跳躍、最少影片片段、最少水龍頭、最短拼接。\n- **每個元素都能覆蓋『從某位置起、到某個更遠位置』的一段**，且你想盡量一步跨最遠。\n- **這和『區間分組／選點』不同**：那兩者是處理「重疊衝突」，覆蓋是要「連續無縫蓋滿」，重點在「下一步能到多遠」：\n\n:::example 嗅到味道：你關心的是「在目前能到的範圍內，下一步最遠能到哪」\n```cpp\n// 嗅到味道：你關心的是「在目前能到的範圍內，下一步最遠能到哪」\n// 維護 curEnd 與 farthest，到邊界才計數一次\n```\n:::\n\n\n\n- **暴力做法是 BFS / DP 枚舉每種覆蓋組合**（較慢），本技巧用雙邊界貪心一次掃描即 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有分清「本步已覆蓋到的 curEnd」和「下一步最遠可達的 farthest」？\n2. 我在什麼時機才把 count 加一、把 curEnd 更新成 farthest？\n3. 出現空隙（farthest 推不過當前位置）時，我怎麼判斷並回報「覆蓋失敗」？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 45 | 跳躍遊戲 II | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1024 | [影片拼接](https://leetcode.cn/problems/video-stitching/) | 1746 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1326 | [灌溉花園的最少水龍頭數目](https://leetcode.cn/problems/minimum-number-of-taps-to-open-to-water-a-garden/) | 1885 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 45 | [跳躍遊戲 II](https://leetcode.cn/problems/jump-game-ii) |  |\n| 1024 | [影片拼接](https://leetcode.cn/problems/video-stitching) | 1746 |\n| 1326 | [灌溉花園的最少水龍頭數目](https://leetcode.cn/problems/minimum-number-of-taps-to-open-to-water-a-garden) | 1885 |",
    },
    {
      id: 19,
      title: "14. 合併區間",
      summary:
        "**原理講解**\n\n本節談「合併區間（merge intervals）」：給你一堆可能彼此重疊的區間，要把所有**互相重疊或相接的區間合併成一個大區間**，輸出合併後的結果。代表題如 56 合併區間、57 插入區間、616 給字串新增加粗標籤、758 字串中的加粗單詞。\n\n直覺很簡單：把區間想成一條條重疊的色帶畫在數線上，凡是黏在一起的就連成一整條。例如 `[1, 3]`、`[2, 6]` 重疊，合併成 `[1, 6]`；再來 `[8, 10]` 和它們沒接觸，自成一條。\n\n:::example 關鍵：按左端點升序排序，讓重疊的區間在掃描中自然相鄰\n關鍵動作是排序：**先按左端點（起點）由小到大排序。** 排序後有一個漂亮的性質——你只要從左到右掃，每個新區間只可能和「目前正在累積的那一條」發生關係，不必回頭比對前面所有區間。\n\n```cpp\n// 關鍵：按左端點升序排序，讓重疊的區間在掃描中自然相鄰\nsort(intervals.begin(), intervals.end(),\n     [](auto& a, auto& b) { return a[0] < b[0]; });\n```\n:::\n\n\n\n掃描時維護「目前正在累積的區間」`cur = [curL, curR]`。看下一個區間 `[l, r]`：\n\n- 若 `l <= curR`（重疊或相接），就把右邊界延伸：`curR = max(curR, r)`——注意是取 max，因為下一個區間可能整個被包住（`r` 比 `curR` 還小）。\n- 若 `l > curR`（脫離了），代表 `cur` 已經完整，存進結果，並用 `[l, r]` 開一條新的。\n\n:::example 重疊就延伸右邊界（取 max 以防被完全包住）；脫離就另起一條\n```cpp\n// 重疊就延伸右邊界（取 max 以防被完全包住）；脫離就另起一條\nif (l <= curR) {\n  curR = max(curR, r);  // 合併\n} else {\n  res.push_back({curL, curR});\n  curL = l;\n  curR = r;\n}  // 開新區間\n```\n:::\n\n\n\n為什麼排序後只看「上一條」就夠？因為按左端點排序後，任何能與更早區間重疊的，必定也與「最近這一條累積區間」重疊，所以重疊關係具有傳遞性，一路滾雪球即可，不會漏。\n\n**實作方式**\n\n以 56 合併區間為骨架，拆成四步：\n\n:::example 按起點升序排序\n**1. 排序：以左端點為鍵。** 這是合併區間能「一次掃描」的前提。\n\n```cpp\n// 按起點升序排序\nsort(intervals.begin(), intervals.end(),\n     [](auto& a, auto& b) { return a[0] < b[0]; });\n```\n:::\n\n\n\n:::example 用第一個區間當作目前累積的區間\n**2. 初始化：用第一個區間開啟累積。**\n\n```cpp\n// 用第一個區間當作目前累積的區間\nvector<vector<int>> res;\nint curL = intervals[0][0], curR = intervals[0][1];\n```\n:::\n\n\n\n:::example 逐一處理後續區間：重疊延伸、脫離收尾\n**3. 掃描：重疊就延伸，脫離就收尾並另起。** 延伸右邊界務必取 max。\n\n```cpp\n// 逐一處理後續區間：重疊延伸、脫離收尾\nfor (int i = 1; i < intervals.size(); ++i) {\n  int l = intervals[i][0], r = intervals[i][1];\n  if (l <= curR) {\n    curR = max(curR, r);  // 合併進當前區間\n  } else {\n    res.push_back({curL, curR});\n    curL = l;\n    curR = r;\n  }\n}\nres.push_back({curL, curR});  // 別忘了把最後一條放進去\n```\n:::\n\n\n\n**4. 收尾與邊界。** 迴圈結束後最後一條累積區間還沒存，務必補一次 `push_back`（最常見的 bug）。「相接算不算合併」看題意：56 中 `[1, 4]` 與 `[4, 5]` 合併成 `[1, 5]`，用 `l <= curR`；若相接不合併則用 `<`。複雜度由排序主導，`O(n log n)`。\n\n對 57 插入區間，若原區間已排序，可不必重新排序：分「在新區間左邊、與新區間重疊、在右邊」三段線性處理，達到 `O(n)`。616 / 758 是把「字元位置區間」合併後再標記加粗範圍，骨架完全相同。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「合併區間」想：\n\n- **輸入是一堆可能重疊的 [start, end]，要求『把重疊的併起來』輸出合併結果**（而非選最多、分最少組）。\n- **答案本身就是一組區間**（或基於合併後區間做標記、計算總覆蓋長度），這是它和其他區間題最明顯的差別。\n- **你想『從左到右滾雪球』，每個新區間只跟上一條比**：\n\n:::example 嗅到味道：排序後，每個區間只會跟「目前累積的那一條」重疊\n```cpp\n// 嗅到味道：排序後，每個區間只會跟「目前累積的那一條」重疊\n// 重疊就延伸右邊界，脫離就把累積的存下、另起新的\n```\n:::\n\n\n\n- **暴力做法是兩兩比較找重疊再反覆合併**（最壞 `O(n^2)`），本技巧排序後一次掃描即 `O(n log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我按左端點排序後，為什麼每個區間只需跟「上一條累積區間」比較？\n2. 延伸右邊界時我有沒有用 `max`，避免被「完全被包住的小區間」覆寫？\n3. 迴圈結束後，我有沒有記得把最後一條累積區間 push 進結果？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 55 | 跳躍遊戲 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 56 | 合併區間 | N/A | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 57 | 插入區間 | N/A | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 616 | 給字串新增加粗標籤（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 758 | 字串中的加粗單詞（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 55 | [跳躍遊戲](https://leetcode.cn/problems/jump-game) |  |\n| 56 | [合併區間](https://leetcode.cn/problems/merge-intervals) |  |\n| 57 | [插入區間](https://leetcode.cn/problems/insert-interval) |  |\n| 616 | [給字串新增加粗標籤](https://leetcode.cn/problems/add-bold-tag-in-string) |  |\n| 758 | [字串中的加粗單詞](https://leetcode.cn/problems/bold-words-in-string) |  |\n| 759 | [員工空閒時間](https://leetcode.cn/problems/employee-free-time) |  |\n| 763 | [劃分字母區間](https://leetcode.cn/problems/partition-labels) |  |\n| 2580 | [統計將重疊區間合併成組的方案數](https://leetcode.cn/problems/count-ways-to-group-overlapping-ranges) | 1632 |\n| 2584 | [分割陣列使乘積互質](https://leetcode.cn/problems/split-the-array-to-make-coprime-products) | 2159 |\n| 2655 | [尋找最大長度的未覆蓋區間](https://leetcode.cn/problems/find-maximal-uncovered-ranges) |  |\n| 2848 | [與車相交的點](https://leetcode.cn/problems/points-that-intersect-with-cars) | 1230 |\n| 2963 | [統計好分割方案的數目](https://leetcode.cn/problems/count-the-number-of-good-partitions) | 1985 |\n| 3169 | [無需開會的工作日](https://leetcode.cn/problems/count-days-without-meetings) | 1483 |\n| 3323 | [透過插入區間最小化連通組](https://leetcode.cn/problems/minimize-connected-groups-by-inserting-interval) |  |\n| 3394 | [判斷網格圖能否被切割成塊](https://leetcode.cn/problems/check-if-grid-can-be-cut-into-sections) | 1916 |",
    },
    {
      id: 20,
      title: "15. 其他區間貪心",
      summary:
        "**原理講解**\n\n本節要處理的是「區間」題目：每筆資料是一段範圍，例如一個會議從第 2 天開到第 5 天、或一個區間 `[start, end]`。我們常被問：最多能選幾個不重疊的區間？哪些區間其實被別人完全包住了？或是在時間軸上每天只能做一件事時最多能參加幾場活動？\n\n這類題的核心想法叫「貪心（greedy）」：不去窮舉所有組合，而是找出一條「每一步都挑當下最安全的選擇」的規則，並且能說明這樣選不會比最優解差。對區間來說，最關鍵的一步幾乎都是「先排序」——選對排序鍵，題目就解一半了。\n\n:::example 區間排序：結束時間小的排前面（留最多空間給後面）\n舉個最直觀的例子：要選最多不重疊區間，就「按結束時間由小到大排序」，因為越早結束，留給後面的空間越多。\n\n```cpp\n// 區間排序：結束時間小的排前面（留最多空間給後面）\nsort(a.begin(), a.end(), [](auto& x, auto& y) {\n  return x[1] < y[1];  // x[1]、y[1] 是兩個區間的結束點\n});\n```\n:::\n\n\n\n:::example 掃描：上一個選到的結束時間是 last，能接上就選\n為什麼這樣選不吃虧？想像最優解裡第一個區間如果結束得比「最早結束的那個」還晚，我們把它換成最早結束的，後面所有選擇仍然成立——這就是「交換論證（exchange argument）」：把最優解的某一步換成貪心的選擇而不變差，於是貪心也是最優。\n\n```cpp\n// 掃描：上一個選到的結束時間是 last，能接上就選\nlong long last = LLONG_MIN;\nint cnt = 0;\nfor (auto& it : a) {\n  if (it[0] >= last) {\n    last = it[1];\n    ++cnt;\n  }  // it[0] 是起點\n}\n```\n:::\n\n\n\n不同題目的排序鍵不同（有時按起點、有時按長度），但骨架都是「排序 + 一次掃描 + 維護一個摘要狀態」。\n\n**實作方式**\n\n把任何區間貪心題拆成三件事，逐一搭配程式碼：\n\n:::example 狀態：記住目前看過的最大右端點\n**1. 狀態表示：只記必要的摘要。** 通常一個變數就夠：上一個選到的結束時間、目前覆蓋到的最右端、或還剩多少資源。像「刪除被覆蓋區間」（1288）只需記「目前最大的右端點」。\n\n```cpp\n// 狀態：記住目前看過的最大右端點\nint maxRight = INT_MIN;  // 被它包住的區間就是「被覆蓋」\n```\n:::\n\n\n\n:::example 1288：左端升冪，左端相同則右端降冪\n**2. 選排序鍵 + 掃描更新。** 排序方式由你想證明的規則決定。例如 1288 按左端點升冪、左端相同時右端降冪，這樣後面的區間若右端沒超過 `maxRight`，就一定被前面的包住。\n\n```cpp\n// 1288：左端升冪，左端相同則右端降冪\nsort(a.begin(), a.end(),\n     [](auto& x, auto& y) { return x[0] != y[0] ? x[0] < y[0] : x[1] > y[1]; });\n```\n:::\n\n\n\n:::example 1353：小根堆放可參加會議的結束日，每天挑最早結束的\n**3. 答案讀取。** 掃描完，答案就在狀態裡：選中的數量、剩下未被覆蓋的數量、或累積收益。「最多參加會議數」（1353）會在掃描每一天時，用一個小根堆放「還沒過期的會議結束時間」，每天參加結束最早的那場——這就是把「優先選最緊迫的」變成可維護狀態。\n\n```cpp\n// 1353：小根堆放可參加會議的結束日，每天挑最早結束的\npriority_queue<int, vector<int>, greater<int>> pq;\n// 每天：把今天能開始的會議結束日 push 進來，\n// 丟掉已過期的堆頂，再參加 pq.top() 那一場\n```\n:::\n\n\n\n**邊界與複雜度。** 排序是 `O(n log n)`，掃描 `O(n)`，用到堆則整體仍是 `O(n log n)`。注意：不能只憑直覺「選最大或最長」，例如「兩個最好的不重疊活動」（2054）要枚舉某事件後、用前綴最大值取另一邊最佳，單純貪最大會錯；若交換論證證不出來，就得改用 DP 或二分。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「區間貪心」想：\n\n- **輸入是一堆 `[start, end]` 區間或帶時間範圍的事件**，問最多選幾個、是否互相覆蓋、或最大收益。\n- **答案明顯和「順序」有關，而你還沒決定按什麼排序**——這正是區間貪心的第一步：試「按右端排」「按左端排」「按長度排」，看哪個能讓局部選擇可被證明安全。\n- **暴力做法是窮舉所有子集合（`O(2^n)`）或兩兩比較（`O(n^2)`）**，資料量一大就超時；排序加一次掃描能壓到 `O(n log n)`。\n- **每天/每個時刻只能處理一件事，且要挑「最緊迫」的**（如 1353、1705 喫蘋果），這通常要搭配一個堆來維護候選。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的排序鍵是什麼？我能用一句話說明「為什麼這樣排後，第一個選擇一定安全」嗎？\n2. 我掃描時維護的「摘要狀態」是哪一個變數（結束時間？最右端？堆？）？\n3. 我能用交換論證說明貪心選擇不會讓答案變差嗎？如果不能，是不是該換 DP？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1288 | [刪除被覆蓋區間](https://leetcode.cn/problems/remove-covered-intervals/) | 1375 | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 1353 | [最多可以參加的會議數目](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended/) | 2016 | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 1705 | [喫蘋果的最大數目](https://leetcode.cn/problems/maximum-number-of-eaten-apples/) | 1930 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2054 | [兩個最好的不重疊活動](https://leetcode.cn/problems/two-best-non-overlapping-events/) | 1883 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1288 | [刪除被覆蓋區間](https://leetcode.cn/problems/remove-covered-intervals) | 1375 |\n| 1353 | [最多可以參加的會議數目](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended) | 2016 |\n| 1705 | [喫蘋果的最大數目](https://leetcode.cn/problems/maximum-number-of-eaten-apples) | 1930 |\n| 2054 | [兩個最好的不重疊活動](https://leetcode.cn/problems/two-best-non-overlapping-events) | 1883 |",
    },
    {
      id: 21,
      title: "16. 字典序最小/最大",
      summary:
        "**觀念起點**\n\n貪心題的重點是證明局部選擇不破壞最優性。僅靠「我覺得先選這個比較好」並不算解法，必須以交換論證、反悔或排序不等式加以說明。\n\n反悔貪心先暫時接受候選，待限制被破壞時再從已接受集合中移除最不值得保留者；heap 的角色即是快速找出該被反悔的元素。\n\n此類題常存在一個逐步擴張的限制，例如時間、資源、前綴和或可到達位置。掃描順序使限制單調變化，heap 或 set 則維護當下仍可保留的候選集合。\n\n貪心的正確性有三條常見路徑：交換論證、反悔（先選再撤回）與擬陣理論。能舉出一個反例即足以否定貪心，此時改用 DP 或搜尋枚舉決策。\n\n---\n\n**代表例題：LeetCode 1642 可以到達的最遠建築**\n\n**完整問題**：LeetCode 1642 給定建築高度 `heights`、磚塊數 `bricks` 與梯子數 `ladders`。由第 i 棟走到第 i+1 棟時，若高度增加 `diff > 0`，須消耗 `diff` 個磚塊或 1 把梯子；若高度不增加則不消耗資源。回傳在資源限制下能到達的最遠建築下標。\n\n本節以 LeetCode 1642「可以到達的最遠建築」 為主例題（rating 1962），並將同一套不變式延伸至本節其他題目。\n\n**題目訊號**\n\n- 僅於向上爬時消耗資源。\n- 梯子宜保留給目前遇過的最大爬升。\n- 掃描至某一步方知先前的資源分配是否需要反悔。\n\n**狀態、不變式與答案更新**\n\n- max-heap 保存目前暫時以磚塊支付的所有爬升高度。\n- `used_bricks` 為扣除反悔後仍由磚塊支付的爬升總和。\n- 當 `used_bricks > bricks` 時，將 heap 中最大爬升改由梯子支付，即一次反悔。\n- 若已無梯子可反悔且磚塊仍超限，當前邊無法跨過，答案即為前一棟建築。\n\n**從零建模**\n\n1. 由左至右掃描相鄰建築，略過高度未增加的邊。\n2. 先假設每個正爬升皆以磚塊支付，將 `diff` 加入 `used_bricks` 並放入 max-heap。\n3. 若磚塊超限，從 heap 取出目前最大爬升改以一把梯子支付。\n4. 反悔後若梯子用盡且磚塊仍超限，代表無法跨過當前邊。\n5. 能完成所有邊時，答案即為最後一棟建築的下標。\n\n**小範例手動模擬**\n\n以反悔貪心選最多工作（各有截止與耗時）為例：依截止排序逐一加入，並把耗時推入大根堆；一旦累積耗時超過當前截止，就彈出耗時最大的工作（反悔）。堆中數量即答案。\n\n**常見模式**\n\n- 反悔堆\n- 先用便宜資源承擔，再將最大代價改交予稀缺資源\n- 掃描到違規時移除已選集合中最值得替換的元素\n\n**常見錯誤**\n\n- 本題並非見最大爬升即立刻用梯子，而是先暫定、超限時方反悔。\n- heap 中應放爬升高度，而非建築高度或下標。\n- 僅 `diff > 0` 時方需消耗資源。\n- 排序比較器需為嚴格弱序（用 `<` 而非 `<=`），否則 `sort` 行為未定義。\n\n**複雜度**：每個正爬升至多進出 heap 一次，時間 `O(n log n)`，空間 `O(n)`。 排序加堆操作，時間 `O(n log n)`、空間 `O(n)`。\n\n---\n\n**本節題單中的延伸題**\n\n- LeetCode 555 分割連線字串\n- LeetCode 564 尋找最近的迴文數\n- LeetCode 670 最大交換\n- LeetCode 738 單調遞增的數字\n- LeetCode 1053 交換一次的先前排列，rating 1633\n\n---\n\n**同一模板的套用要點**\n\n能用交換論證或反悔證明才套貪心；舉得出反例就改用 DP 或搜尋枚舉決策。\n\n---\n\n**C++ 實作骨架**\n\n```cpp\nclass Solution {\n public:\n  int furthestBuilding(vector<int>& heights, int bricks, int ladders) {\n    priority_queue<int> paid_by_bricks;\n    long long used_bricks = 0;\n\n    for (int i = 0; i + 1 < (int)heights.size(); ++i) {\n      int climb = heights[i + 1] - heights[i];\n      if (climb <= 0) {\n        continue;\n      }\n\n      used_bricks += climb;\n      paid_by_bricks.push(climb);\n\n      if (used_bricks > bricks) {\n        if (ladders == 0) {\n          return i;\n        }\n        used_bricks -= paid_by_bricks.top();\n        paid_by_bricks.pop();\n        ladders--;\n      }\n    }\n    return heights.size() - 1;\n  }\n};\n```",
      children: [
        {
          id: 44,
          title: "16.1 字典序最小/最大",
          summary:
            "**字典序**的定義如下：\n- 對於兩個字串 `a` 和 `b`，從左到右依次比較字元 `a[i]` 和 `b[i]` 的 `ASCII` 值大小。\n- 當 `a[i] != b[i]` 時，如果 `a[i] < b[i]`，那麼 `a` 的字典序更小，否則 `b` 的字典序更小。\n- 如果沒有出現 `a[i] != b[i]`，則較短的字串字典序更小。\n- 如果兩個字串的長度和內容均相同，那麼兩個字串的字典序一樣。\n\n:::example 字典序最小：單調堆疊貪心\n字典序的定義也可以推廣到陣列上，按照上述方法比較兩個陣列的字典序。\n\n```cpp\n// 字典序最小：單調堆疊貪心\n// Monotonic Stack Greedy for Lexicographically Smallest Subsequence\n//\n// 問題：從字串 s 中選 k 個字元（保持相對順序），使結果字典序最小\n//\n// 核心思路：\n//   遍歷字串，維護一個「單調遞增」的結果。\n//   遇到更小的字元時，把前面較大的字元彈出（如果還有刪除額度），\n//   這樣能讓結果盡可能以較小的字元開頭，達到字典序最小。\n\nstring res;  // 用作單調堆疊，存放目前的結果\nint remain = s.size() - k;  // 還可以刪除的字元數量（= 總長 - 需要保留的數量）\n\nfor (char c : s) {\n  // 貪心策略：如果堆疊頂端的字元比當前字元大，\n  // 且我們還有刪除額度（remain > 0），就彈出頂端\n  // → 用更小的字元取代，使字典序更小\n  while (!res.empty() && res.back() > c && remain > 0) {\n    res.pop_back();  // 彈出較大的字元\n    remain--;        // 消耗一次刪除額度\n  }\n  res += c;  // 將當前字元加入結果\n}\n\n// 如果刪除額度沒用完，結果可能超過 k 個字元，截斷到 k\nres.resize(k);\n```\n:::\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 555 | [分割連線字串](https://leetcode.cn/problems/split-concatenated-strings) |  |\n| 564 | [尋找最近的迴文數](https://leetcode.cn/problems/find-the-closest-palindrome) |  |\n| 670 | [最大交換](https://leetcode.cn/problems/maximum-swap) |  |\n| 738 | [單調遞增的數字](https://leetcode.cn/problems/monotone-increasing-digits) |  |\n| 1053 | [交換一次的先前排列](https://leetcode.cn/problems/previous-permutation-with-one-swap) | 1633 |\n| 1202 | [交換字串中的元素](https://leetcode.cn/problems/smallest-string-with-swaps) | 1855 |\n| 1323 | [和 9 組成的最大數字](https://leetcode.cn/problems/maximum-69-number) | 1194 |\n| 1328 | [破壞迴文串](https://leetcode.cn/problems/break-a-palindrome) | 1474 |\n| 1363 | [形成三的最大倍數](https://leetcode.cn/problems/largest-multiple-of-three) | 1823 |\n| 1432 | [改變一個整數能得到的最大差值](https://leetcode.cn/problems/max-difference-you-can-get-from-changing-an-integer) | 1427 |\n| 1505 | [最多 K 次交換相鄰數位後得到的最小整數](https://leetcode.cn/problems/minimum-possible-integer-after-at-most-k-adjacent-swaps-on-digits) | 2337 |\n| 1663 | [具有給定數值的最小字串](https://leetcode.cn/problems/smallest-string-with-a-given-numeric-value) | 1461 |\n| 1754 | [構造字典序最大的合併字串](https://leetcode.cn/problems/largest-merge-of-two-strings) | 1829 |\n| 1881 | [插入後的最大值](https://leetcode.cn/problems/maximum-value-after-insertion) | 1381 |\n| 1946 | [子字串突變後可能得到的最大整數](https://leetcode.cn/problems/largest-number-after-mutating-substring) | 1445 |\n| 2182 | [構造限制重複的字串](https://leetcode.cn/problems/construct-string-with-repeat-limit) | 1680 |\n| 2259 | [移除指定數字得到的最大結果](https://leetcode.cn/problems/remove-digit-from-number-to-maximize-result) | 1332 |\n| 2375 | [根據模式串構造最小數字](https://leetcode.cn/problems/construct-smallest-number-from-di-string) | 1642 |\n| 2434 | [使用機器人列印字典序最小的字串](https://leetcode.cn/problems/using-a-robot-to-print-the-lexicographically-smallest-string) | 1953 |\n| 2566 | [替換一個數字後的最大差值](https://leetcode.cn/problems/maximum-difference-by-remapping-a-digit) | 1396 |\n| 2697 | [字典序最小回文串](https://leetcode.cn/problems/lexicographically-smallest-palindrome) | 1304 |\n| 2734 | [執行子串操作後的字典序最小字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-substring-operation) | 1405 |\n| 2948 | [交換得到字典序最小的陣列](https://leetcode.cn/problems/make-lexicographically-smallest-array-by-swapping-elements) | 2047 |\n| 3088 | [使字串反迴文](https://leetcode.cn/problems/make-string-anti-palindrome) |  |\n| 3106 | [滿足距離約束且字典序最小的字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-operations-with-constraint) | 1515 |\n| 3170 | [刪除星號以後字典序最小的字串](https://leetcode.cn/problems/lexicographically-minimum-string-after-removing-stars) | 1772 |\n| 3216 | [交換後字典序最小的字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-a-swap) | 1243 |\n| 3403 | [從盒子中找出字典序最大的字串 I](https://leetcode.cn/problems/find-the-lexicographically-largest-string-from-the-box-i) | 1762 |\n| 3474 | [字典序最小的生成字串](https://leetcode.cn/problems/lexicographically-smallest-generated-string) | 2605 |\n| 3517 | [最小回文排列 I](https://leetcode.cn/problems/smallest-palindromic-rearrangement-i) | 1357 |\n| 3723 | [數位平方和的最大值](https://leetcode.cn/problems/maximize-sum-of-squares-of-digits) | 1537 |\n| 3752 | [字典序最小和為目標值且絕對值是排列的陣列](https://leetcode.cn/problems/lexicographically-smallest-negated-permutation-that-sums-to-target) | 1827 |",
        },
        {
          id: 22,
          title: "16.2 倒序貪心",
          summary:
            "**原理講解**\n\n本節談「倒序貪心」：很多構造題要我們造出一個「滿足某條件、又是字典序最小或數值最小」的答案，這時與其從左往右硬湊，不如**從右邊（最低位）往左掃，遇到第一個可以「往上加一點」的位置就動手，再把它右邊全部重設成最小**。\n\n為什麼要倒著走？因為「字典序大小」是由高位決定的：高位越小，整個字串/數字就越小。所以當我們要找「比目標大、但盡量小」的答案時，應該盡量保留高位不動，只在「不得不進位」的最右位置改一下。倒序掃描正好讓我們先碰到最低位、第一個能改的點就是「改動影響最小」的點。\n\n:::example 倒序找第一個能 +1 的位置（這裡示意：找非 9 的最右位）\n舉個直覺例子：要找「比 `12399` 大的最小數」，從右往左找第一個還能加一的位數，把它加一，右邊歸零（或歸成允許的最小值）。\n\n```cpp\n// 倒序找第一個能 +1 的位置（這裡示意：找非 9 的最右位）\nint i = s.size() - 1;\nwhile (i >= 0 && s[i] == '9') {\n  i--;  // 9 不能再加，繼續往左\n        // i 就是要 +1 的位置；它右邊全部之後重設為最小\n}\n```\n:::\n\n\n\n:::example 提升該位、右邊重設為最小（示意：數字情境）\n這個「先加最右可加位、再把右邊清成最小」的動作，正是倒序貪心的靈魂。它安全的理由是交換論證：任何比目標大的合法答案，其「第一個比目標大的高位」一定不會比我們找到的位置更靠右更好——我們已經在能保留最多高位的前提下做了最小幅度的提升。\n\n```cpp\n// 提升該位、右邊重設為最小（示意：數字情境）\ns[i]++;  // 在這一位往上加\nfor (int j = i + 1; j < (int)s.size(); ++j) {\n  s[j] = '0';  // 右邊全部歸最小\n}\n```\n:::\n\n\n\n「下一個更大的數值平衡數」（2048）、「下一個特殊迴文數」（3646）、「大於目標字串的最小字典序排列」（3720）都是這個骨架：找到可提升的最右位，提升後把右側填成「滿足條件下的最小」。\n\n**實作方式**\n\n把倒序貪心題拆成三步，逐一搭配程式碼：\n\n:::example 狀態：答案序列 + 從右往左的指標\n**1. 狀態表示：把答案當成一個可改寫的序列。** 通常是一個 `string` 或 `vector<int>`，從右端的指標開始往左移動，記住「目前考慮到第幾位」。\n\n```cpp\n// 狀態：答案序列 + 從右往左的指標\nstring s = ...;        // 目前的答案（常由目標複製而來）\nint i = s.size() - 1;  // 從最低位開始檢查\n```\n:::\n\n\n\n:::example 找最右可提升位（示意：字母題，'z' 是上限）\n**2. 倒序找可提升位 + 提升。** 從右往左找「還能往上換更大值」的第一位。判斷條件依題目而定（沒到上限、進位後仍合法等），找到就提升它。\n\n```cpp\n// 找最右可提升位（示意：字母題，'z' 是上限）\nwhile (i >= 0 && s[i] == 'z') {\n  i--;  // 'z' 不能再大，往左退\n}\nif (i < 0) {\n  return \"\";  // 全部到頂 → 無解\n}\ns[i]++;  // 提升這一位\n```\n:::\n\n\n\n:::example 右側逐位填最小合法值（示意：避開前一、二位形成迴文）\n**3. 右側重填成「合法的最小」。** 提升後右邊不必再保留，全部填成滿足題目條件的最小值。例如「字典序最小的美麗字串」（2663）要避免出現迴文，填最小字母時得跳過會和前一、二位相同的選擇。\n\n```cpp\n// 右側逐位填最小合法值（示意：避開前一、二位形成迴文）\nfor (int j = i + 1; j < (int)s.size(); ++j) {\n  for (char c = 'a';; ++c) {\n    if (c != s[j - 1] && (j < 2 || c != s[j - 2])) {\n      s[j] = c;\n      break;\n    }\n  }\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 整串只掃一兩遍，多為 `O(n)`（n 為位數/長度）；若每位要試常數種值，仍是 `O(n)`。要小心「找不到可提升位」代表無解（已是最大），以及進位可能讓長度改變（如 3348「最小可整除數位乘積 II」需補位）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「倒序貪心」想：\n\n- **題目要「下一個更大的…」「大於目標的最小…」「字典序最小且滿足某條件」**——關鍵字是「下一個」「最小」「大於目標」。\n- **答案是一個字串或數字，且大小由高位主導**：你會本能地想保留高位、只改低位，這正是倒序掃描的訊號。\n- **暴力做法是從目標 `+1` 一路枚舉到找到合法答案**，當數字很大時根本枚舉不完；倒序貪心一次定位就 `O(n)` 完成。\n- **改完一位後，右邊可以「自由重填成最小」**——若右邊有這種自由度，幾乎可以確定用倒序貪心。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是從哪一端開始掃描？我能說明「為什麼倒序能讓改動影響最小」嗎？\n2. 我「提升某一位」的合法條件是什麼？提升後右邊要填成什麼最小值？\n3. 什麼情況代表無解（掃到頭都不能提升）？我有處理進位／補位的邊界嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2048 | [下一個更大的數值平衡數](https://leetcode.cn/problems/next-greater-numerically-balanced-number/) | 1734 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2663 | [字典序最小的美麗字串](https://leetcode.cn/problems/lexicographically-smallest-beautiful-string/) | 2416 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 3348 | [最小可整除數位乘積 II](https://leetcode.cn/problems/smallest-divisible-digit-product-ii/) | 3101 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 3646 | [下一個特殊迴文數](https://leetcode.cn/problems/next-special-palindrome-number/) | 2445 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3720 | [大於目標字串的最小字典序排列](https://leetcode.cn/problems/lexicographically-smallest-permutation-greater-than-target/) | 1958 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2048 | [下一個更大的數值平衡數](https://leetcode.cn/problems/next-greater-numerically-balanced-number) | 1734 |\n| 2663 | [字典序最小的美麗字串](https://leetcode.cn/problems/lexicographically-smallest-beautiful-string) | 2416 |\n| 3348 | [最小可整除數位乘積 II](https://leetcode.cn/problems/smallest-divisible-digit-product-ii) | 3101 |\n| 3646 | [下一個特殊迴文數](https://leetcode.cn/problems/next-special-palindrome-number) | 2445 |\n| 3720 | [大於目標字串的最小字典序排列](https://leetcode.cn/problems/lexicographically-smallest-permutation-greater-than-target) | 1958 |\n| 3734 | [大於目標字串的最小字典序迴文排列](https://leetcode.cn/problems/lexicographically-smallest-palindromic-permutation-greater-than-target) | 2330 |",
        },
      ],
    },
    {
      id: 23,
      title: "17. 迴文串貪心",
      summary:
        "**原理講解**\n\n本節談「迴文串貪心」。先講清楚名詞：迴文串（palindrome）就是「正著讀、反著讀都一樣」的字串，例如 `aba`、`abba`。這類題的共同特徵是——答案的左右兩半互為鏡像，所以**每個字元幾乎都要「成對」出現**，最多只有正中央能放一個落單的字元（長度為奇數時）。抓住這個「成對」性質，很多題就能用貪心一步到位，不必窮舉。\n\n:::example 最長迴文串：統計每個字元的出現次數\n最經典的例子是「最長迴文串」（409）：給一堆字元，問能拼出的最長迴文有多長。想法是——每種字元能用的數量是「成對的部分」，亦即把出現次數無條件捨去到偶數；最後若還有任何字元有剩（出現過奇數次），就能挑一個放正中央，答案再 `+1`。\n\n```cpp\n// 最長迴文串：統計每個字元的出現次數\nint cnt[128] = {0};\nfor (char c : s) {\n  cnt[c]++;  // cnt[c] 是字元 c 的數量\n}\n```\n:::\n\n\n\n:::example 成對的全收，最後若有奇數次的字元，補一個放中間\n```cpp\n// 成對的全收，最後若有奇數次的字元，補一個放中間\nint ans = 0;\nbool hasOdd = false;\nfor (int c = 0; c < 128; ++c) {\n  ans += cnt[c] / 2 * 2;  // 取偶數部分（每兩個一對）\n  if (cnt[c] & 1) {\n    hasOdd = true;  // 還有落單的\n  }\n}\nans += hasOdd;  // 中央放一個\n```\n:::\n\n\n\n為什麼這樣選一定最優？因為迴文的對稱結構決定了「除了正中央，任何字元都得成對」，所以「能成對的全用上」不可能更好；而中央那一格放任何一個剩餘字元都等價。這就是貪心的安全性來源：規則由問題的結構直接保證，不需要交換論證的複雜推導。\n\n「迴文排列」（266，判斷能否重排成迴文）更單純：只要「出現奇數次的字元種類數 ≤ 1」即可。\n\n**實作方式**\n\n迴文串貪心題大致可拆成三步，逐一搭配程式碼：\n\n:::example 比對型狀態：左右指標往中間靠\n**1. 狀態表示：通常是字元計數，或左右雙指標。** 計數型（409、266）用一個 `cnt` 陣列；比對型（680「驗證迴文串 II」）則用左右兩個指標往中間夾。\n\n```cpp\n// 比對型狀態：左右指標往中間靠\nint l = 0, r = s.size() - 1;  // 兩端比對，遇到不同再決策\n```\n:::\n\n\n\n:::example 680：遇到不相等時，試跳過左邊或右邊一個字元\n**2. 依結構做局部決策。** 計數型直接套「成對全收 + 中央補一」。比對型則：兩端字元相同就一起內縮；遇到第一個不相同時，貪心地「試刪左邊一個」或「試刪右邊一個」，看其中一種能否讓剩下變迴文。\n\n```cpp\n// 680：遇到不相等時，試跳過左邊或右邊一個字元\nbool ok(const string& s, int l, int r) {\n  while (l < r) {\n    if (s[l] != s[r]) {\n      return false;\n    }\n    ++l;\n    r--;\n  }\n  return true;  // 剩下這段是迴文嗎？\n}\n// 主流程不相等時：return ok(s, l+1, r) || ok(s, l, r-1);\n```\n:::\n\n\n\n:::example 1328：前半找第一個非 'a' 改成 'a'（讓字典序最小）\n**3. 答案讀取。** 計數型直接回傳累積長度或可否（266）；比對型回傳布林或最少修改數。「破壞迴文串」（1328）則是貪心地「把前半段第一個非 a 的字元改成 a」讓字典序最小，若前半全是 a 就改最後一個字元。\n\n```cpp\n// 1328：前半找第一個非 'a' 改成 'a'（讓字典序最小）\nfor (int i = 0; i < n / 2; ++i) {\n  if (s[i] != 'a') {\n    s[i] = 'a';\n    return s;\n  }\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 計數與雙指標都是 `O(n)`、空間 `O(1)`（字元集固定）。特別注意奇偶長度：長度為奇數時中央那格不參與配對；空字串、單字元等退化情形要單獨確認。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「迴文串貪心」想：\n\n- **題目出現「迴文」「重排成迴文」「最長迴文」「驗證/破壞迴文」**——直接點名迴文，且問的是能否、最長、最小修改。\n- **答案只和「每個字元出現幾次」有關，而與順序無關**（如 409、266）——這是計數型貪心的明確訊號。\n- **暴力做法是枚舉所有排列或所有刪除方式**（指數或 `O(n^2)`），但靠「成對」結構可壓到一次線性掃描。\n- **要在「保持迴文」前提下讓字典序最小**（如 1328、3646）——通常從左往右或從中央往外，第一個能動的位置就動。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這題的答案只看「字元次數」，還是看「位置順序」？該用計數還是雙指標？\n2. 我的貪心規則是「成對全收、中央補一」，還是「遇到不同就試刪一邊」？我能用迴文的對稱性說明它安全嗎？\n3. 我有沒有處理奇偶長度、中央字元、以及空串等邊界？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 266 | 迴文排列（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 409 | 最長迴文串 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 680 | 驗證迴文串 II | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1147 | [段式迴文](https://leetcode.cn/problems/longest-chunked-palindrome-decomposition/) | 1912 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1328 | [破壞迴文串](https://leetcode.cn/problems/break-a-palindrome/) | 1474 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 266 | [迴文排列](https://leetcode.cn/problems/palindrome-permutation) |  |\n| 409 | [最長迴文串](https://leetcode.cn/problems/longest-palindrome) |  |\n| 680 | [驗證迴文串 II](https://leetcode.cn/problems/valid-palindrome-ii) |  |\n| 1147 | [段式迴文](https://leetcode.cn/problems/longest-chunked-palindrome-decomposition) | 1912 |\n| 1328 | [破壞迴文串](https://leetcode.cn/problems/break-a-palindrome) | 1474 |\n| 1400 | [構造 K 個迴文字串](https://leetcode.cn/problems/construct-k-palindrome-strings) | 1530 |\n| 1616 | [分割兩個字串得到迴文串](https://leetcode.cn/problems/split-two-strings-to-make-palindrome) | 1868 |\n| 1842 | [下個由相同數字構成的迴文串](https://leetcode.cn/problems/next-palindrome-using-same-digits) |  |\n| 2131 | [連線兩字母單詞得到的最長迴文串](https://leetcode.cn/problems/longest-palindrome-by-concatenating-two-letter-words) | 1557 |\n| 2193 | [得到迴文串的最少操作次數](https://leetcode.cn/problems/minimum-number-of-moves-to-make-palindrome) | 2091 |\n| 2384 | [最大回文數字](https://leetcode.cn/problems/largest-palindromic-number) | 1636 |\n| 2422 | [使用合併操作將陣列轉換為迴文序列](https://leetcode.cn/problems/merge-operations-to-turn-array-into-a-palindrome) |  |\n| 2697 | [字典序最小回文串](https://leetcode.cn/problems/lexicographically-smallest-palindrome) | 1304 |\n| 3035 | [迴文字串的最大數量](https://leetcode.cn/problems/maximum-palindromes-after-operations) | 1857 |\n| 3088 | [使字串反迴文](https://leetcode.cn/problems/make-string-anti-palindrome) |  |\n| 3517 | [最小回文排列 I](https://leetcode.cn/problems/smallest-palindromic-rearrangement-i) | 1357 |\n| 3646 | [下一個特殊迴文數](https://leetcode.cn/problems/next-special-palindrome-number) | 2445 |\n| 3734 | [大於目標字串的最小字典序迴文排列](https://leetcode.cn/problems/lexicographically-smallest-palindromic-permutation-greater-than-target) | 2330 |",
    },
    {
      id: 24,
      title: "18. 基礎",
      summary:
        "**原理講解**\n\n本節是「貪心基礎」：用幾道入門題，把「貪心（greedy）」這個想法本身講清楚。所謂貪心，就是**不窮舉所有可能，而是每一步都做「當下看起來最好、且能證明不會害到最終答案」的選擇**。它快、好寫，但前提是那個「局部最好」真的能拼成「全域最好」——不是每題都成立，所以「會證明」比「會寫」更重要。\n\n:::example 整數替換：偶數直接砍半（最划算的一步）\n最能體會貪心的例子是「整數替換」（397）：給一個數 n，每次可以對偶數除以 2、對奇數加一或減一，問最少幾步變成 1。直覺規則是：偶數就除以 2（一定不虧），奇數就看「加一還是減一更接近 2 的次方」。\n\n```cpp\n// 整數替換：偶數直接砍半（最划算的一步）\nif (n % 2 == 0) {\n  n /= 2;  // 偶數除以 2 一定不會更差\n  ++steps;\n}\n```\n:::\n\n\n\n:::example 奇數：n==3 特判減一；否則看末兩位決定加減\n奇數時為什麼不能亂選？因為加一或減一會影響後續能砍幾次半。貪心規則是：除了 `n == 3` 之外，看末兩位——`...11` 結尾加一（能連續砍更多），`...01` 結尾減一。\n\n```cpp\n// 奇數：n==3 特判減一；否則看末兩位決定加減\nelse if (n == 3 || (n & 2) == 0) n--;  // ...01 → 減一\nelse n++;  // ...11 → 加一（之後能多砍幾次）\n```\n:::\n\n\n\n這就是貪心的精髓：找到一條「每步可證明安全」的規則。判斷它對不對，最常用「交換論證」——假設最優解某一步和貪心不同，把它換成貪心的選擇而答案不變差，於是貪心也最優。\n\n:::example 1217：奇偶位置各算總和，答案取較小的那邊\n「玩籌碼」（1217）更直觀：移動偶數位置不花錢、移動奇數位置每步花 1，所以答案就是「奇數位置籌碼數」和「偶數位置籌碼數」取較小者。\n\n```cpp\n// 1217：奇偶位置各算總和，答案取較小的那邊\nint odd = 0, even = 0;\nfor (int x : pos) {\n  (x % 2 ? odd : even)++;\n}\nreturn min(odd, even);  // 把少的那群搬過去最省\n```\n:::\n\n\n\n**實作方式**\n\n把基礎貪心題拆成三步，逐一搭配程式碼：\n\n:::example 2244：先統計每種任務的數量\n**1. 狀態表示：先想清楚「哪些量會變、哪些要快速查」。** 有時是一個累加器，有時是計數。「完成所有任務的最少輪數」（2244）就先用 map 數每種任務各有幾個。\n\n```cpp\n// 2244：先統計每種任務的數量\nunordered_map<int, int> cnt;\nfor (int t : tasks) {\n  cnt[t]++;  // 同種任務一次能做 2 或 3 個\n}\n```\n:::\n\n\n\n:::example 2244：每種任務優先 3 個一組；只剩 1 個則無解\n**2. 按自然順序或排序後處理，每步只更新局部。** 規則要能一句話講明白「為什麼這步安全」。2244 對每種任務貪心地「盡量用 3 個一組」，餘數決定加幾輪。\n\n```cpp\n// 2244：每種任務優先 3 個一組；只剩 1 個則無解\nfor (auto [t, c] : cnt) {\n  if (c == 1) {\n    return -1;  // 落單無法湊組\n  }\n  ans += (c + 2) / 3;  // 向上取整成 3 的組數\n}\n```\n:::\n\n\n\n:::example 2160：四位數字排序，兩小的當十位\n**3. 答案讀取。** 答案就在你維護的狀態裡：累加的步數、輪數、或兩群取 min。「拆分數位後四位數字的最小和」（2160）則是把四個數字排序後，小的兩個各當十位、組成兩個兩位數，和最小。\n\n```cpp\n// 2160：四位數字排序，兩小的當十位\nsort(d, d + 4);\nint a = d[0] * 10 + d[2], b = d[1] * 10 + d[3];\nreturn a + b;  // 把小數字放到高位 → 總和最小\n```\n:::\n\n\n\n**邊界與複雜度。** 多數基礎貪心是排序 `O(n log n)` 或一遍掃描 `O(n)`。最重要的提醒：**若無法說清「為什麼這步安全」，貪心很可能是錯的**，這時回到暴力解（甚至 DP）再找可貪的部分。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「基礎貪心」想：\n\n- **問「最少步數 / 最小花費 / 最大數量」，而每一步的選擇看起來有個顯然的偏好**（砍半、選小的、搬少的）。\n- **答案只跟「次數、奇偶、排序後的位置」這種簡單統計有關**，不需要記住複雜歷史。\n- **暴力做法是枚舉所有操作序列或所有組合**（指數級），但一個簡單規則就能一步定案。\n- **你能用一句話說明選擇規則，並隱約覺得「換成別的選擇不會更好」**——這就是可以嘗試交換論證的訊號。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的貪心規則是什麼？能用一句話講出「為什麼這步不會害到最終答案」嗎？\n2. 我能不能舉出一個反例讓這規則失效？若舉不出，再試著用交換論證證明它。\n3. 如果證不出來，這題是不是其實該用 DP 或搜尋，而不是貪心？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCS 01 | 下載外掛 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 397 | 整數替換 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1217 | [玩籌碼](https://leetcode.cn/problems/minimum-cost-to-move-chips-to-the-same-position/) | 1408 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2160 | [拆分數位後四位數字的最小和](https://leetcode.cn/problems/minimum-sum-of-four-digit-number-after-splitting-digits/) | 1314 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2244 | [完成所有任務需要的最少輪數](https://leetcode.cn/problems/minimum-rounds-to-complete-all-tasks/) | 1372 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCS 01 | [下載外掛](https://leetcode.cn/problems/Ju9Xwi) |  |\n| 397 | [整數替換](https://leetcode.cn/problems/integer-replacement) |  |\n| 1217 | [玩籌碼](https://leetcode.cn/problems/minimum-cost-to-move-chips-to-the-same-position) | 1408 |\n| 2160 | [拆分數位後四位數字的最小和](https://leetcode.cn/problems/minimum-sum-of-four-digit-number-after-splitting-digits) | 1314 |\n| 2244 | [完成所有任務需要的最少輪數](https://leetcode.cn/problems/minimum-rounds-to-complete-all-tasks) | 1372 |\n| 2578 | [最小和分割](https://leetcode.cn/problems/split-with-minimum-sum) | 1351 |\n| 2870 | [使陣列為空的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-array-empty) | 1392 |\n| 3091 | [執行操作使資料元素之和大於等於 K](https://leetcode.cn/problems/apply-operations-to-make-sum-of-array-greater-than-or-equal-to-k) | 1522 |",
    },
    {
      id: 25,
      title: "19. 乘積貪心",
      summary:
        "**原理講解**\n\n本節談「乘積貪心」：當題目要的是「乘積最大／最小」而不是「和最大／最小」時，貪心的規則會和加法不一樣，關鍵全在**負號**。加法裡兩個負數相加更小，但乘法裡**兩個負數相乘會變正、而且絕對值越大乘積越大**，所以負數常常是「翻盤」的關鍵。\n\n:::example 三個數最大乘積：先排序，方便取最大/最小幾個\n最經典的是「三個數的最大乘積」（628）：從陣列裡選三個數使乘積最大。直覺是「選三個最大的」，但如果有很大的負數，「兩個最小的（最負的）負數 × 一個最大的正數」可能更大。所以答案是兩種候選取較大者。\n\n```cpp\n// 三個數最大乘積：先排序，方便取最大/最小幾個\nsort(a.begin(), a.end());\nint n = a.size();\n// 候選一：最大的三個   候選二：最小兩個(可能是負) × 最大的一個\n```\n:::\n\n\n\n:::example 兩種乘法候選取較大者\n```cpp\n// 兩種乘法候選取較大者\nlong long c1 = (long long)a[n - 1] * a[n - 2] * a[n - 3];\nlong long c2 = (long long)a[0] * a[1] * a[n - 1];\nreturn max(c1, c2);  // 負負得正可能反超\n```\n:::\n\n\n\n為什麼只需比這兩種？因為乘積的大小由「最大正數們」和「最大的兩個負數（最負）」主導，其他組合不可能更優——這是由乘法的符號規則直接保證的，不必窮舉所有 `C(n, 3)` 組合。\n\n:::example 1567：維護以 i 結尾、乘積為正/為負的最長長度\n另一種乘積題是「乘積為正數的最長子陣列」（1567）：在乘法世界裡，子陣列乘積正負只看「負數個數的奇偶」。所以我們一邊掃描，一邊維護「以目前位置結尾、乘積為正的最長長度」與「為負的最長長度」。\n\n```cpp\n// 1567：維護以 i 結尾、乘積為正/為負的最長長度\nint pos = 0, neg = 0, ans = 0;  // pos: 乘積為正; neg: 為負\n```\n:::\n\n\n\n碰到正數，正的延長、負的（若存在）也延長；碰到負數，正負互換；碰到 0，全部歸零重來。\n\n**實作方式**\n\n把乘積貪心題拆成三步，逐一搭配程式碼：\n\n:::example 取極值型常用：排序後關注 a[0],a[1] 與 a[n-1],a[n-2],a[n-3…\n**1. 狀態表示：排序後取兩端，或掃描維護正負長度。** 「取極值型」（628、3732）排序後只看頭尾幾個；「子陣列型」（1567）維護兩個長度。\n\n```cpp\n// 取極值型常用：排序後關注 a[0],a[1] 與 a[n-1],a[n-2],a[n-3]\nsort(a.begin(), a.end());  // 最負的在前、最大的在後\n```\n:::\n\n\n\n:::example 子陣列型更新（1567）：遇到負數時正負互換\n**2. 列出所有「符號組合」候選並比較。** 乘積題的核心是「別漏掉負負得正」。「一次替換後的三元素最大乘積」（3732）就要枚舉把某個元素替換後，再套類似 628 的取極值比較。\n\n```cpp\n// 子陣列型更新（1567）：遇到負數時正負互換\nif (x > 0) {\n  ++pos;\n  neg = neg ? neg + 1 : 0;\n} else if (x < 0) {\n  int t = pos;\n  pos = neg ? neg + 1 : 0;\n  neg = t + 1;\n} else {\n  pos = neg = 0;\n}  // 遇到 0：乘積斷掉，全歸零\nans = max(ans, pos);\n```\n:::\n\n\n\n:::example 取極值型：別忘了用 long long 防溢位\n**3. 答案讀取。** 取極值型回傳候選的 max；子陣列型回傳掃描過程中的最長 `pos`。\n\n```cpp\n// 取極值型：別忘了用 long long 防溢位\nlong long ans = max(c1, c2);  // int 相乘三次極易溢位\n```\n:::\n\n\n\n**邊界與複雜度。** 排序型 `O(n log n)`，掃描型 `O(n)`。最常見的坑有兩個：一是**整數溢位**，多個數相乘要轉 `long long`；二是**0 與負數的處理**——0 會讓乘積歸零、負數個數奇偶決定符號，務必各自特判。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「乘積貪心」想：\n\n- **目標是「乘積最大／最小」而非「總和」**，且輸入裡可能有負數或 0——符號就是解題重點。\n- **你發現「選最大的幾個」會被反例打臉**（兩個大負數相乘反而更大），這正是乘積題的標誌。\n- **暴力做法是枚舉所有 k 個元素的組合或所有子陣列**（`O(n^k)` 或 `O(n^2)`），但靠符號規則只需排序取端點或一遍掃描。\n- **子陣列乘積問題**，正負由「負數個數奇偶」決定——維護「正/負最長長度」即可。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有考慮「負負得正」這種被加法直覺忽略的候選？\n2. 0 和負數我各自怎麼處理？乘積會不會被 0 歸零、被負數翻號？\n3. 我的乘法有沒有用 `long long` 防溢位？候選是否已窮盡所有符號組合？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 628 | 三個數的最大乘積 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1567 | [乘積為正數的最長子陣列長度](https://leetcode.cn/problems/maximum-length-of-subarray-with-positive-product/) | 1710 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3732 | [一次替換後的三元素最大乘積](https://leetcode.cn/problems/maximum-product-of-three-elements-after-one-replacement/) | 1529 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 628 | [三個數的最大乘積](https://leetcode.cn/problems/maximum-product-of-three-numbers) |  |\n| 1567 | [乘積為正數的最長子陣列長度](https://leetcode.cn/problems/maximum-length-of-subarray-with-positive-product) | 1710 |\n| 3732 | [一次替換後的三元素最大乘積](https://leetcode.cn/problems/maximum-product-of-three-elements-after-one-replacement) | 1529 |",
    },
    {
      id: 26,
      title: "20. 排序不等式",
      summary:
        "**原理講解**\n\n本節談「排序不等式」：這是一個能用來指導貪心的數學事實。白話講就是——**當你要把兩組數字一一配對相乘再相加時，「大配大、小配小」（同序）總和最大；「大配小、小配大」（逆序）總和最小。** 中間任何亂配的順序，結果都介於這兩者之間。\n\n:::example 想要乘積和最大：兩組都升冪 → 同序相乘\n舉個生活例子：你有兩組權重要兩兩相乘求和，例如把 `a = [1, 2, 3]` 和 `b = [4, 5, 6]` 配對。同序配（1×4 + 2×5 + 3×6）最大，逆序配（1×6 + 2×5 + 3×4）最小。要最大就兩組都升冪，要最小就一組升冪、一組降冪。\n\n```cpp\n// 想要乘積和最大：兩組都升冪 → 同序相乘\nsort(a.begin(), a.end());\nsort(b.begin(), b.end());\nlong long s = 0;\nfor (int i = 0; i < (int)a.size(); ++i) {\n  s += (long long)a[i] * b[i];\n}\n```\n:::\n\n\n\n:::example 想要乘積和最小：一組升冪、一組降冪 → 逆序相乘\n```cpp\n// 想要乘積和最小：一組升冪、一組降冪 → 逆序相乘\nsort(a.begin(), a.end());\nsort(b.rbegin(), b.rend());  // b 由大到小\n```\n:::\n\n\n\n為什麼成立？用交換論證：若最優配對裡存在「大配小、小配大」這種交叉，把它們交換成不交叉，總和不會變差（甚至更好）。一路把所有交叉消掉，就回到同序/逆序，所以同序最大、逆序最小。\n\n:::example 2285：度數越大配越大的值（同序）\n這個原理常被「包裝」進題目。例如「道路的最大總重要性」（2285）：每個城市要分配一個 1..n 的不重複數值，每條路的重要性是它兩端城市值之和，求總和最大。每個城市的「度數（連幾條路）」就是它的權重——**度數大的城市要配大數值**，正是同序配最大。\n\n```cpp\n// 2285：度數越大配越大的值（同序）\nsort(deg.begin(), deg.end());  // 度數升冪\nlong long ans = 0;\nfor (int i = 0; i < n; ++i) {\n  ans += (long long)(i + 1) * deg[i];\n}\n```\n:::\n\n\n\n**實作方式**\n\n把排序不等式型題目拆成三步，逐一搭配程式碼：\n\n:::example 3016：先統計每個字母的出現頻率（這是要被配對的一組）\n**1. 狀態表示：找出「兩組要配對的量」。** 一組常是現成的（陣列），另一組可能要先算出來，例如出現頻率、度數、或位置成本。「輸入單詞最少按鍵次數 II」（3016）就要先數每個字母的出現頻率。\n\n```cpp\n// 3016：先統計每個字母的出現頻率（這是要被配對的一組）\nint cnt[26] = {0};\nfor (char c : word) {\n  cnt[c - 'a']++;\n}\n```\n:::\n\n\n\n:::example 3016：頻率由大到小，前 8 個按 1 次、次 8 個按 2 次...\n**2. 排序後同序或逆序配對。** 確定目標是最大或最小，決定兩組的排序方向。3016 要讓「高頻字母配少的按鍵次數」——頻率高的排前、按鍵成本（1,1,...,2,2,...）由小到大，逆序配以最小化。\n\n```cpp\n// 3016：頻率由大到小，前 8 個按 1 次、次 8 個按 2 次...\nsort(cnt, cnt + 26, greater<int>());\nlong long ans = 0;\nfor (int i = 0; i < 26; ++i) {\n  ans += (long long)(i / 8 + 1) * cnt[i];\n}\n```\n:::\n\n\n\n:::example 1402：升冪後從大的開始累加，只要前綴和為正就繼續加\n**3. 答案讀取。** 配對乘積和就是答案。「做菜順序」（1402）是變形：滿意度乘以「製作順序係數」，把大的滿意度排到後面（係數大）能讓總和最大，等價於排序後的後綴貪心。\n\n```cpp\n// 1402：升冪後從大的開始累加，只要前綴和為正就繼續加\nsort(s.begin(), s.end());\nlong long suf = 0, ans = 0;\nfor (int i = s.size() - 1; i >= 0; i--) {\n  suf += s[i];\n  if (suf > 0) {\n    ans += suf;\n  }\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 主成本是排序 `O(n log n)`，配對 `O(n)`。注意：要先看清「哪兩組在配對、要最大還是最小」才能決定排序方向；乘積累加常需 `long long` 防溢位。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「排序不等式」想：\n\n- **要把兩組量一一配對相乘（或加權求和），求總和最大或最小**——這是最直接的訊號（如 1874 兩陣列最小乘積和）。\n- **其中一組是「權重」（頻率、度數、成本、係數）**，你可以自由決定哪個值配給誰（如 2285、3016、2268）。\n- **暴力做法是枚舉所有配對方式**（`O(n!)`），但排序後一一對應就 `O(n log n)`。\n- **「越重要的東西要給越好的待遇」這句話講得通**——這幾乎就是同序配對。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要配對的「兩組量」分別是什麼？其中一組權重是不是要自己算（頻率/度數）？\n2. 我的目標是最大還是最小？所以該同序（都升冪）還是逆序（一升一降）？\n3. 我能用交換論證說明「消掉交叉配對不會變差」嗎？乘積累加有沒有用 `long long`？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1874 | 兩個陣列的最小乘積和（Premium） | N/A | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2268 | 最少按鍵次數（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2285 | [道路的最大總重要性](https://leetcode.cn/problems/maximum-total-importance-of-roads/) | 1496 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3016 | [輸入單詞需要的最少按鍵次數 II](https://leetcode.cn/problems/minimum-number-of-pushes-to-type-word-ii/) | 1534 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1402 | [做菜順序](https://leetcode.cn/problems/reducing-dishes/) | 1679 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1874 | [兩個陣列的最小乘積和](https://leetcode.cn/problems/minimize-product-sum-of-two-arrays) |  |\n| 2268 | [最少按鍵次數](https://leetcode.cn/problems/minimum-number-of-keypresses) |  |\n| 2285 | [道路的最大總重要性](https://leetcode.cn/problems/maximum-total-importance-of-roads) | 1496 |\n| 3016 | [輸入單詞需要的最少按鍵次數 II](https://leetcode.cn/problems/minimum-number-of-pushes-to-type-word-ii) | 1534 |\n| 1402 | [做菜順序](https://leetcode.cn/problems/reducing-dishes) | 1679 |\n| 2931 | [購買物品的最大開銷](https://leetcode.cn/problems/maximum-spending-after-buying-items) | 1822 |\n| 1589 | [所有排列中的最大和](https://leetcode.cn/problems/maximum-sum-obtained-of-any-permutation) | 1871 |\n| 2561 | [重排水果](https://leetcode.cn/problems/rearranging-fruits) | 2222 |\n| 3547 | [圖中邊值的最大和](https://leetcode.cn/problems/maximum-sum-of-edge-values-in-a-graph) | 2344 |",
    },
    {
      id: 28,
      title: "21. 均值不等式",
      summary:
        "**原理講解**\n\n本節談「均值不等式」：這是另一個能指導貪心的數學事實。最常用的版本是——**當幾個正數的「和」固定時，它們「越平均」乘積越大；越「極端」（差距越大）乘積越小。** 反過來，若要讓乘積小，就把資源集中到少數幾個、讓其他盡量極端。\n\n:::example 直覺驗證：和固定時，越平均乘積越大\n直覺例子：和固定為 10，拆成 `5 + 5` 乘積是 25，拆成 `1 + 9` 乘積只有 9。所以「平均分」乘積最大、「極端分」乘積最小。許多題目就是在問「該平均還是該極端」。\n\n```cpp\n// 直覺驗證：和固定時，越平均乘積越大\n// 5 * 5 = 25  >  1 * 9 = 9  （和都是 10）\nlong long balanced = 5LL * 5;  // 平均 → 大\nlong long extreme = 1LL * 9;   // 極端 → 小\n```\n:::\n\n\n\n:::example 1969：把可調整的數兩兩配成 (極小, 極大)，讓乘積最小化\n「陣列元素的最小非零乘積」（1969）就是反向應用：在某些位元交換規則下要讓乘積「最小」，於是策略是「把數值推向兩端極值」——盡量湊出最多的最小值，剩下集中成最大值。\n\n```cpp\n// 1969：把可調整的數兩兩配成 (極小, 極大)，讓乘積最小化\n// 例如值域 [1, M]，把大多數壓到 1（極小），少數頂到 M（極大）\n// 結論常是 1 的若干次方 × (大值的快速冪)，用快速冪算\n```\n:::\n\n\n\n:::example 2897：把位元盡量集中到少數元素（讓它們變很大）\n而「對陣列執行操作使平方和最大」（2897）是正向應用的變形：平方和要最大，反而要「極端」——因為平方放大大數，把數值集中（透過 AND/OR 操作把位元堆到少數元素）能讓平方和更大。\n\n```cpp\n// 2897：把位元盡量集中到少數元素（讓它們變很大）\n// 統計每個 bit 出現次數，再「重組」成盡量大的數，取前 k 大平方和\nint bitCnt[32] = {0};\nfor (int x : nums) {\n  for (int b = 0; b < 32; ++b) {\n    if (x >> b & 1) {\n      bitCnt[b]++;\n    }\n  }\n}\n```\n:::\n\n\n\n為什麼平均/極端能保證最優？同樣可用交換論證：若兩個數不平均，把它們各往中間挪一點（和不變），乘積會增大；反覆挪到相等為止。要最小化就反向操作。\n\n**實作方式**\n\n把均值不等式型題目拆成三步，逐一搭配程式碼：\n\n:::example 常見起手：統計可自由分配的資源（如各 bit 的數量）\n**1. 狀態表示：先確認「什麼量固定、什麼量要極值化」。** 通常「和」或「位元總數」固定，問乘積/平方和的極值。先把可重新分配的資源統計出來。\n\n```cpp\n// 常見起手：統計可自由分配的資源（如各 bit 的數量）\n// 之後再決定「平均分」還是「極端分」\n```\n:::\n\n\n\n:::example 3081：先數現有字母頻率，問號優先補給「目前最少」的字母\n**2. 依目標選擇平均或極端，並落實分配。** 要乘積/平方和「最大」通常往極端集中（平方場景）或平均（純乘積場景）；要「最小」則反向。3081「替換問號使分數最小」則是讓字母分布盡量平均，避免某字母過度集中而拉高分數。\n\n```cpp\n// 3081：先數現有字母頻率，問號優先補給「目前最少」的字母\n// 用小根堆每次取頻率最小的字母來填 '?'（讓分布平均）\npriority_queue<pair<int, char>, vector<pair<int, char>>, greater<>> pq;\n```\n:::\n\n\n\n:::example 快速冪：算 base^exp % mod，O(log exp)\n**3. 答案讀取，常配快速冪。** 乘積題答案往往極大需取模，用「快速冪（fast power）」高效計算次方。\n\n```cpp\n// 快速冪：算 base^exp % mod，O(log exp)\nlong long power(long long base, long long exp, long long mod) {\n  long long r = 1;\n  base %= mod;\n  while (exp) {\n    if (exp & 1) {\n      r = r * base % mod;\n    }\n    base = base * base % mod;\n    exp >>= 1;\n  }\n  return r;\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 統計與分配多為 `O(n)` 或 `O(n log n)`（用堆時），快速冪 `O(log exp)`。注意：取模下「最小非零乘積」不能直接比大小，要在「實際整數意義」上想清楚再套模；位元操作題務必確認位元能否自由搬移。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「均值不等式」想：\n\n- **某個量（和、位元總數、字母總數）固定，問乘積、平方和、或分數的最大/最小**——這是核心訊號。\n- **你能在不改變總量的前提下「重新分配」資源**（交換位元、替換問號、調整數值），且直覺告訴你「該平均」或「該極端」。\n- **暴力做法是枚舉所有分配方式**（指數級），但「平均或極端」一句話就定下最優分配。\n- **答案是巨大的乘積要取模**——常搭配快速冪，這也是均值不等式題的常見配套。\n\n確認自己有沒有套對，問三個問題：\n\n1. 哪個量是固定的？我要極值化的是乘積還是平方和？\n2. 我的目標要「平均分」還是「極端集中」？我能用交換論證說明往那個方向挪不會變差嗎？\n3. 答案需要取模／快速冪嗎？我在取模前是否已用真實數值意義想清楚最優分配？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1969 | [陣列元素的最小非零乘積](https://leetcode.cn/problems/minimum-non-zero-product-of-the-array-elements/) | 1967 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2897 | [對陣列執行操作使平方和最大](https://leetcode.cn/problems/apply-operations-on-array-to-maximize-sum-of-squares/) | 2301 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2939 | [最大異或乘積](https://leetcode.cn/problems/maximum-xor-product/) | 2128 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3081 | [替換字串中的問號使分數最小](https://leetcode.cn/problems/replace-question-marks-in-string-to-minimize-its-value/) | 1905 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 3723 | [數位平方和的最大值](https://leetcode.cn/problems/maximize-sum-of-squares-of-digits/) | 1537 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1969 | [陣列元素的最小非零乘積](https://leetcode.cn/problems/minimum-non-zero-product-of-the-array-elements) | 1967 |\n| 2897 | [對陣列執行操作使平方和最大](https://leetcode.cn/problems/apply-operations-on-array-to-maximize-sum-of-squares) | 2301 |\n| 2939 | [最大異或乘積](https://leetcode.cn/problems/maximum-xor-product) | 2128 |\n| 3081 | [替換字串中的問號使分數最小](https://leetcode.cn/problems/replace-question-marks-in-string-to-minimize-its-value) | 1905 |\n| 3723 | [數位平方和的最大值](https://leetcode.cn/problems/maximize-sum-of-squares-of-digits) | 1537 |",
    },
    {
      id: 29,
      title: "22. 中位數貪心",
      summary:
        "**原理講解**\n\n本節要解決的場景是：給定一堆數字，要把它們「全部變成同一個值」或「全部往某個目標靠攏」，而且每移動一步要付代價，問最少代價。直覺上你會想試各種目標值，但其實有個漂亮結論：**讓所有數靠到「中位數」上，總移動距離最小。** 這就是「中位數貪心」。\n\n先講什麼是中位數。把數字排序後，正中間那個就是中位數（偶數個時，中間兩個之間任何位置都行）。例如 `[1, 4, 10]` 的中位數是 4。\n\n:::example 中位數貪心：把所有數移到中位數，總距離最小\n為什麼選中位數最省？關鍵在「絕對值距離總和」這個代價形式。想像數線上有一群點，你站在某處，要算「你到每個點的距離總和」。如果你站的位置左邊有 3 個點、右邊有 5 個點，那你往右走一步：左邊 3 個點各遠了 1（多花 3），右邊 5 個點各近了 1（省了 5），淨賺 2，所以該往右走。**只要兩側點數不相等，往人多的那側移動一定更省**；移到「兩側點數相等」時（也就是中位數）才停下來，這就是最佳位置。\n\n```cpp\n// 中位數貪心：把所有數移到中位數，總距離最小\nsort(a.begin(), a.end());\nint mid = a[a.size() / 2];  // 取中位數\nlong long cost = 0;\nfor (int x : a) {\n  cost += abs(x - mid);  // 累加每個數到中位數的距離\n}\n```\n:::\n\n\n\n這個技巧的本質是：當代價是「∑|xᵢ − target|」這種絕對值和，最佳 target 永遠是中位數，不必枚舉每個可能的 target。\n\n**實作方式**\n\n**1. 確認代價是「絕對距離總和」。** 只有當「每個元素到目標的代價 = 它們差的絕對值」時，中位數結論才成立。若代價是平方（∑(xᵢ−target)²），最佳點變成平均數，不是中位數，要分清楚。\n\n:::example 排序並取中位數下標\n**2. 排序後取中位數。** 排序是為了找中間元素。注意偶數個時，下標 `n/2` 與 `n/2 - 1` 之間都最優，取任一個算出來的總代價相同。\n\n```cpp\n// 排序並取中位數下標\nsort(a.begin(), a.end());\nint n = a.size();\nint mid = a[n / 2];  // 偶數個取 n/2 也行，結果一樣\n```\n:::\n\n\n\n:::example 計算把所有數變成中位數的總操作次數\n**3. 累加總代價。** 掃一遍，把每個元素到中位數的距離加起來。如 LeetCode 462「最小操作次數使陣列元素相等 II」就是直接套這段。\n\n```cpp\n// 計算把所有數變成中位數的總操作次數\nlong long ops = 0;\nfor (int x : a) {\n  ops += abs(x - mid);\n}\n```\n:::\n\n\n\n**4. 變形：帶權或帶位移的中位數。** 有些題目（如 1703「得到連續 K 個 1 的最少相鄰交換」、1478「安排郵筒」）要先做「座標變換」把問題化成標準中位數模型，再套上面公式。把每個 1 的位置減去它的目標相對位置，剩下的就是一個普通的「靠攏到中位數」問題。\n\n**5. 複雜度。** 排序 `O(n log n)`，掃描 `O(n)`，整體 `O(n log n)`。若資料邊插入邊查中位數，才需要用兩個堆動態維護（小的一側大根堆、大的一側小根堆），但靜態題目排序就夠了。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「中位數貪心」想：\n\n- **要把一組數全部變成「同一個值」，每步代價是 ±1 或移動距離，求最小總代價。** 這幾乎是中位數貪心的招牌，如 462。\n- **代價式長得像 ∑|xᵢ − t|**，要你選一個 t 使它最小。答案就是中位數，不用枚舉 t。\n- **「碰頭」「集合」「對齊」類題目**：一群東西要聚到一點，求最短總路程，如 296「最佳的碰頭地點」（二維時 x、y 各自取中位數）。\n- 若代價是「平方和」而非「絕對值和」，停一下——那是平均數，不是這個技巧。\n\n確認自己套對了，問三個問題：\n\n1. 我的代價真的是「絕對值距離的總和」嗎？\n2. 排序後我取的是哪一個元素當目標？是不是正中間？\n3. 有沒有需要先做座標變換，才能化成標準中位數模型？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 24 | 數字遊戲 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 296 | 最佳的碰頭地點（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 462 | 最小操作次數使陣列元素相等 II | N/A | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 1478 | [安排郵筒](https://leetcode.cn/problems/allocate-mailboxes/) | 2190 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1703 | [得到連續 K 個 1 的最少相鄰交換次數](https://leetcode.cn/problems/minimum-adjacent-swaps-for-k-consecutive-ones/) | 2467 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 24 | [數字遊戲](https://leetcode.cn/problems/5TxKeK) |  |\n| 296 | [最佳的碰頭地點](https://leetcode.cn/problems/best-meeting-point) |  |\n| 462 | [最小操作次數使陣列元素相等 II](https://leetcode.cn/problems/minimum-moves-to-equal-array-elements-ii) |  |\n| 1478 | [安排郵筒](https://leetcode.cn/problems/allocate-mailboxes) | 2190 |\n| 1703 | [得到連續 K 個 1 的最少相鄰交換次數](https://leetcode.cn/problems/minimum-adjacent-swaps-for-k-consecutive-ones) | 2467 |\n| 2033 | [獲取單值網格的最小運算元](https://leetcode.cn/problems/minimum-operations-to-make-a-uni-value-grid) | 1672 |\n| 2448 | [使陣列相等的最小開銷](https://leetcode.cn/problems/minimum-cost-to-make-array-equal) | 2005 |\n| 2607 | [使子陣列元素和相等](https://leetcode.cn/problems/make-k-subarray-sums-equal) | 2071 |\n| 2967 | [使陣列成為等數陣列的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-array-equalindromic) | 2116 |\n| 2968 | [執行操作使頻率分數最大](https://leetcode.cn/problems/apply-operations-to-maximize-frequency-score) | 2444 |\n| 3086 | [拾起 K 個 1 需要的最少行動次數](https://leetcode.cn/problems/minimum-moves-to-pick-k-ones) | 2673 |\n| 3441 | [變成好標題的最少代價](https://leetcode.cn/problems/minimum-cost-good-caption) | 2765 |\n| 3762 | [使陣列元素相等的最小操作次數](https://leetcode.cn/problems/minimum-operations-to-equalize-subarrays) | 2497 |\n| 棋盤整理 | [棋盤整理](https://leetcode.cn/problems/1Hxnb6) |  |",
    },
    {
      id: 30,
      title: "23. 歸納法",
      summary:
        "**原理講解**\n\n本節要學的是「歸納法」貪心。名字聽起來抽象，但想法很生活化：**從最小、最簡單的情況開始,一步步把「目前能做到的範圍」往外擴。每加一個新元素，就看它能不能讓範圍變得更大；只要能,就吃下它。**\n\n最經典的例子是 LeetCode 330「按要求補齊陣列」與 2952「需要新增的硬幣的最小數量」。問題長這樣：給你一些硬幣（可重複使用一次每個），你希望能湊出 `1, 2, 3, ... target` 所有金額，最少還要再加幾枚硬幣？\n\n核心不變式（invariant，意思是「過程中始終成立的性質」）是：**我目前能連續湊出 `[1, reach]` 的所有金額。** 一開始 `reach = 0`（什麼都湊不出）。現在來看下一枚硬幣 `c`：\n\n- 如果 `c <= reach + 1`，代表這枚硬幣能無縫接上——原本能湊到 `reach`，加上 `c` 後能湊到 `reach + c`，範圍變大，吃下它。\n- 如果 `c > reach + 1`，代表 `reach + 1` 這個金額怎麼都湊不出來了（出現斷層）。這時只能自己補一枚 `reach + 1`，補完範圍直接翻倍到 `2 * reach + 1`。\n\n:::example 歸納法核心：reach 表示「目前能連續湊出 [1, reach]」\n```cpp\n// 歸納法核心：reach 表示「目前能連續湊出 [1, reach]」\nlong long reach = 0;\nint added = 0, i = 0;\nwhile (reach < target) {\n  if (i < coins.size() && coins[i] <= reach + 1) {\n    reach += coins[i++];  // 硬幣接得上，擴大範圍\n  } else {\n    reach += reach + 1;\n    ++added;\n  }  // 有斷層，補一枚 reach+1\n}\n```\n:::\n\n\n\n為什麼補 `reach + 1` 最划算？因為這是「能延伸範圍的最大一步」——補任何比它小的數，擴張幅度更小；補比它大的數，會留下 `reach + 1` 這個湊不出的洞。這就是貪心的「安全選擇」，可用歸納法嚴格證明：每一步都讓不變式保持成立，且擴張到極限。\n\n**實作方式**\n\n**1. 找出「可維護的範圍」不變式。** 先問自己：到目前為止，我「保證能達成」的東西是什麼?常見答案是一個區間 `[1, reach]` 或一個閾值。把它寫成一個變數。\n\n:::example 由小到大處理，才能逐步接龍\n**2. 排序輸入（若需要按大小逐一接龍）。** 像補齊陣列，要先把硬幣由小到大排序，才能保證「接得上就接，接不上才補」。\n\n```cpp\n// 由小到大處理，才能逐步接龍\nsort(coins.begin(), coins.end());\n```\n:::\n\n\n\n:::example 判斷接得上 vs 出現斷層\n**3. 逐元素更新範圍。** 每來一個元素，判斷它能否無縫擴張範圍。能就吸收；不能就執行「最划算的補強動作」，並記一次代價。\n\n```cpp\n// 判斷接得上 vs 出現斷層\nif (c <= reach + 1) {\n  reach += c;  // 接得上：直接擴張\n} else {\n  reach += reach + 1;\n  ++added;\n}  // 斷層：補 reach+1（翻倍）\n```\n:::\n\n\n\n**4. 在不變式成立時讀答案。** 迴圈跑到 `reach >= target`，累計的 `added` 就是答案。1798「你能構造出連續值的最大數目」更直接——不問補幾枚，直接回傳 `reach + 1`（能構造的連續值個數）。\n\n**5. 複雜度。** 排序 `O(n log n)`；之後每枚硬幣只看一次，每次補強讓 reach 至少翻倍（補的次數是 `O(log target)`），整體 `O(n log n + log target)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「歸納法」想：\n\n- **題目要你保證「能湊出 / 能覆蓋 / 能構造」連續的一段 `[1, X]`**，問最少要補多少，或最多能撐到多大。如 330、2952、1798。\n- **存在一個會單調成長的「目前可達範圍」**，而你每一步只關心「下一個元素能不能銜接這個範圍」。\n- **暴力做法是枚舉所有湊法（指數爆炸）**；歸納法把它壓成一次線性掃描，靠「範圍翻倍」快速逼近目標。\n- 關鍵直覺：**有沒有出現「斷層」？** 一旦最小湊不出的數比手上元素還小,就必須補。\n\n確認自己套對了，問三個問題：\n\n1. 我維護的「保證可達範圍」是哪個變數？初始值是多少？\n2. 一個新元素在什麼條件下能「無縫接上」範圍？\n3. 出現斷層時，補哪個值能讓範圍擴張最多、又不留洞？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 330 | 按要求補齊陣列 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1798 | [你能構造出連續值的最大數目](https://leetcode.cn/problems/maximum-number-of-consecutive-values-you-can-make/) | 1931 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2952 | [需要新增的硬幣的最小數量](https://leetcode.cn/problems/minimum-number-of-coins-to-be-added/) | 1784 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 330 | [按要求補齊陣列](https://leetcode.cn/problems/patching-array) |  |\n| 1798 | [你能構造出連續值的最大數目](https://leetcode.cn/problems/maximum-number-of-consecutive-values-you-can-make) | 1931 |\n| 2952 | [需要新增的硬幣的最小數量](https://leetcode.cn/problems/minimum-number-of-coins-to-be-added) | 1784 |",
    },
    {
      id: 31,
      title: "24. 其他數學貪心",
      summary:
        "**原理講解**\n\n本節是「其他數學貪心」——專收那些「看穿一條數學規律後，貪心就變得很短」的題目。它們沒有統一模板，共同點是：**先用一點數學觀察把問題簡化，再貪心地一步步逼近答案。**\n\n舉一個非常直觀的例子，LeetCode 1414「和為 K 的最少斐波那契數字數目」。斐波那契數列是 `1, 1, 2, 3, 5, 8, 13, ...`（每個數是前兩個的和）。問題：用最少幾個斐波那契數（可重複嗎？這題不可重複）加起來剛好等於 K？\n\n:::example 其他數學貪心：每次貪心減去「不超過 k 的最大斐波那契數」\n貪心規則只有一句：**每次都減掉「不超過 K 的最大斐波那契數」。** 例如 K = 19，最大不超過 19 的是 13，減掉剩 6；不超過 6 的最大是 5，減掉剩 1；再減 1，剩 0。用了 13 + 5 + 1，共 3 個。\n\n```cpp\n// 其他數學貪心：每次貪心減去「不超過 k 的最大斐波那契數」\nint count = 0;\nwhile (k > 0) {\n  int f = largestFibNotExceeding(k);  // 找 <= k 的最大斐波那契數\n  k -= f;                             // 貪心扣掉\n  ++count;\n}\n```\n:::\n\n\n\n為什麼這樣最少？這需要一點數學論證（叫 Zeckendorf 定理：任何正整數都能唯一表示成不相鄰斐波那契數之和），保證貪心取最大不會吃虧。**這正是「數學貪心」的精神——貪心步驟很簡單，但它的正確性要靠一個數學性質撐腰。**\n\n另一類常用工具是「交換論證」：假設最優解和貪心解不同，找出第一個不同處，證明把最優解「改成貪心的選法」不會變差，於是貪心也是最優。\n\n**實作方式**\n\n**1. 先做數學觀察，把問題化簡。** 這是本節最重要也最難的一步。常見化簡：發現答案有閉式公式、發現只與某個數的奇偶/餘數有關、發現可以唯一分解。如 754「到達終點數字」要先推出「累加 1+2+...+n 何時能蓋過目標、奇偶是否對得上」。\n\n:::example 例：貪心累加 1,2,3,... 直到總和 >= |target|\n**2. 設計「安全的局部選擇」。** 觀察清楚後，貪心規則通常就浮現了——每步取最大、取最近、或取某個極值。\n\n```cpp\n// 例：貪心累加 1,2,3,... 直到總和 >= |target|\nlong long sum = 0, n = 0, t = abs(target);\nwhile (sum < t || (sum - t) % 2 != 0) {  // 還沒蓋過，或奇偶對不上\n  ++n;\n  sum += n;  // 再走一步\n}\n```\n:::\n\n\n\n**3. 用交換論證或不變式證明它不吃虧。** 寫程式前先說服自己:「如果有更好的選擇，我能把它換成貪心的選擇而不變差」。3107「使陣列中位數等於 K」就要論證「只動需要動的元素、且每次動最划算的那個」。\n\n**4. 累加代價，掃完讀答案。** 把每步代價或計數累加，迴圈結束就是答案。如 1058「最小化舍入誤差」需在「向上取整 vs 向下取整」間做數學權衡後排序取捨。\n\n**5. 複雜度。** 多半是 `O(n)` 或 `O(n log n)`（含排序或預先生成數列）。斐波那契類因數字成長極快，步數只有 `O(log k)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「其他數學貪心」想：\n\n- **題目背後藏著一個數學結構**（斐波那契、等差數列和、奇偶性、唯一分解），先把它挖出來，貪心步驟就會變得很短。\n- **答案像是「最少用幾個 / 最少操作幾次」**，而每一步都有一個明顯「最划算」的選擇，如 1414、3107。\n- **暴力是搜尋所有組合（指數級）**；數學觀察 + 貪心把它壓成線性或對數。\n- 警訊：**如果你的貪心只憑直覺、卻說不出為什麼不吃虧，多半要改用 DP 或搜尋。** 數學貪心的門檻就是「能證明」。\n\n確認自己套對了，問三個問題：\n\n1. 我發現了哪條數學規律，讓問題大幅簡化？\n2. 我每一步的「安全局部選擇」是什麼？\n3. 我能用交換論證或已知定理說明它不會錯過最優解嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 754 | 到達終點數字 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1058 | 最小化舍入誤差以滿足目標（Premium） | N/A | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 1414 | [和為 K 的最少斐波那契數字數目](https://leetcode.cn/problems/find-the-minimum-number-of-fibonacci-numbers-whose-sum-is-k/) | 1466 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3107 | [使陣列中位數等於 K 的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-median-of-array-equal-to-k/) | 1605 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 754 | [到達終點數字](https://leetcode.cn/problems/reach-a-number) |  |\n| 1058 | [最小化舍入誤差以滿足目標](https://leetcode.cn/problems/minimize-rounding-error-to-meet-target) |  |\n| 1414 | [和為 K 的最少斐波那契數字數目](https://leetcode.cn/problems/find-the-minimum-number-of-fibonacci-numbers-whose-sum-is-k) | 1466 |\n| 3107 | [使陣列中位數等於 K 的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-median-of-array-equal-to-k) | 1605 |",
    },
    {
      id: 32,
      title: "25. 從特殊到一般",
      summary:
        "**原理講解**\n\n本節介紹一種解題「思考方法」：**從特殊到一般**。意思是：當一個問題太大、太複雜、看不出頭緒時，先把它縮小到最特殊、最簡單的情形手算一遍，從中找出規律，再把規律推廣到一般情況。這不是某個固定演算法，而是一種找出貪心規則的途徑。\n\n:::example 從特殊到一般：先想兩人情形，推得「按 costA - costB 排序」\n舉 LeetCode 1029「兩地排程」為例。公司要派 2N 個人，一半去 A 城、一半去 B 城，每人去 A、去 B 各有不同花費，求最小總花費。一開始很難，但先想「特殊情況」：如果只有兩個人怎麼辦？顯然讓「去 A 比去 B 省更多的人」去 A。把這個直覺一般化：**對每個人算出 `costA − costB`（去 A 相對去 B 多花多少），這個差越小代表他越「適合去 A」。** 按差排序，前一半去 A、後一半去 B。\n\n```cpp\n// 從特殊到一般：先想兩人情形，推得「按 costA - costB 排序」\nsort(costs.begin(), costs.end(), [](auto& x, auto& y) {\n  return (x[0] - x[1]) < (y[0] - y[1]);  // 差越小越該去 A\n});\nint n = costs.size() / 2, total = 0;\nfor (int i = 0; i < costs.size(); ++i) {\n  total += (i < n) ? costs[i][0] : costs[i][1];  // 前半去 A，後半去 B\n}\n```\n:::\n\n\n\n這個方法的價值在於：**特殊情況通常能用手算或暴力跑出答案，你從幾個小例子裡「看出」規律，再大膽推廣，最後補上證明。** 像 517「超級洗衣機」也是先想「只有兩台、三台時怎麼搬」，再歸納出「答案由最大瓶頸決定」。\n\n**實作方式**\n\n**1. 把問題縮到最小規模手算。** n = 1、n = 2、只有兩種元素……先算出這些小情況的答案，記下你是怎麼決策的。\n\n:::example 提煉出排序鍵後，把一般情況按這個鍵排序\n**2. 從小例子中提煉「決策依據」。** 觀察：你在小情況裡是依據什麼挑選的？是某個差值、某個比值、還是某個排序鍵？把它寫成明確的規則。\n\n```cpp\n// 提煉出排序鍵後，把一般情況按這個鍵排序\nsort(items.begin(), items.end(), [](auto& a, auto& b) {\n  return key(a) < key(b);  // key 就是你從小例子歸納出的決策依據\n});\n```\n:::\n\n\n\n**3. 把規則推廣並掃描一遍。** 一般情況通常就是「按這個鍵排序後，依序做小情況裡的同一個決策」。\n\n**4. 驗證推廣是否正確。** 用幾個你沒算過的中等例子代入，看答案對不對；再嘗試交換論證證明排序鍵的合理性（為什麼這個鍵越小越優先）。2202「K 次操作後最大化頂端元素」也要先分 K 與棧大小的幾種特殊關係，再合併成統一公式。\n\n**5. 注意特殊邊界。** 推廣後常有「退化情況」要單獨處理，例如只剩一個元素、K 大於總長度、全部相同等。2350「不可能得到的最短骰子序列」就要從「湊齊一輪所有面」這個特殊觀察推廣到「湊齊幾輪」。\n\n**複雜度** 多半是排序主導的 `O(n log n)`，或線性掃描 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「從特殊到一般」想：\n\n- **題目規模一上來就讓人不知如何下手**，但縮到 n = 1、n = 2 後變得一目了然。\n- **答案似乎由某個排序順序或某個極值決定**，但規則不明顯，需要從小例子「逆推」出來，如 1029、517。\n- **暴力是枚舉所有分配 / 所有順序（指數級）**；找到規律後變成一次排序加掃描。\n- 技巧心法：**卡住時不要硬想一般解，先把最小的例子算到爛熟，規律自己會浮出來。**\n\n確認自己套對了，問三個問題：\n\n1. 我試過的最小特殊情況是什麼？它的最優決策是怎麼做的？\n2. 我從小例子歸納出的「排序鍵 / 決策依據」是什麼？\n3. 推廣到一般時，有哪些退化邊界要單獨處理？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 517 | 超級洗衣機 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1029 | [兩地排程](https://leetcode.cn/problems/two-city-scheduling/) | 1348 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1702 | [修改後的最大二進位制字串](https://leetcode.cn/problems/maximum-binary-string-after-change/) | 1825 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2202 | [K 次操作後最大化頂端元素](https://leetcode.cn/problems/maximize-the-topmost-element-after-k-moves/) | 1717 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2350 | [不可能得到的最短骰子序列](https://leetcode.cn/problems/shortest-impossible-sequence-of-rolls/) | 1961 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 517 | [超級洗衣機](https://leetcode.cn/problems/super-washing-machines) |  |\n| 1029 | [兩地排程](https://leetcode.cn/problems/two-city-scheduling) | 1348 |\n| 1702 | [修改後的最大二進位制字串](https://leetcode.cn/problems/maximum-binary-string-after-change) | 1825 |\n| 2202 | [K 次操作後最大化頂端元素](https://leetcode.cn/problems/maximize-the-topmost-element-after-k-moves) | 1717 |\n| 2350 | [不可能得到的最短骰子序列](https://leetcode.cn/problems/shortest-impossible-sequence-of-rolls) | 1961 |\n| 2499 | [讓陣列不相等的最小總代價](https://leetcode.cn/problems/minimum-total-cost-to-make-arrays-unequal) | 2633 |\n| 2568 | [最小無法得到的或值](https://leetcode.cn/problems/minimum-impossible-or) | 1754 |\n| 2611 | [老鼠和乳酪](https://leetcode.cn/problems/mice-and-cheese) | 1663 |\n| 2645 | [構造有效字串的最少插入數](https://leetcode.cn/problems/minimum-additions-to-make-valid-string) | 1478 |\n| 2745 | [構造最長的新字串](https://leetcode.cn/problems/construct-the-longest-new-string) | 1607 |\n| 3012 | [透過操作使陣列長度最小](https://leetcode.cn/problems/minimize-length-of-array-using-operations) | 1833 |\n| 3357 | [最小化相鄰元素的最大差值](https://leetcode.cn/problems/minimize-the-maximum-adjacent-element-difference) | 3077 |\n| 3431 | [對數字排序的最小解鎖下標](https://leetcode.cn/problems/minimum-unlocked-indices-to-sort-nums) |  |\n| 3660 | [跳躍遊戲 IX](https://leetcode.cn/problems/jump-game-ix) | 2187 |",
    },
    {
      id: 33,
      title: "26. 腦筋急轉彎",
      summary:
        "**原理講解**\n\n本節是「腦筋急轉彎」類題目。它們的共同特色是：**正面硬解看起來很難、甚至像要模擬一大堆操作，但只要換個角度看穿背後的關鍵性質，答案往往是一條簡短公式或一兩行掃描。** 重點不在演算法多複雜，而在「想通那一層」。\n\n:::example 腦筋急轉彎：周長 = 陸地數*4 - 相鄰邊*2\n舉 LeetCode 463「島嶼的周長」為例。網格裡有一塊陸地，要算它的周長。新手會想沿著邊界一格格繞圈走，很麻煩。換個角度：**每一格陸地本身貢獻 4 條邊，但每當兩塊陸地相鄰，它們之間那條共用邊就要從周長扣掉 2（兩塊各扣 1）。** 於是只要數陸地格數和相鄰對數即可。\n\n```cpp\n// 腦筋急轉彎：周長 = 陸地數*4 - 相鄰邊*2\nint perimeter = 0;\nfor (int i = 0; i < m; ++i) {\n  for (int j = 0; j < n; ++j) {\n    if (grid[i][j] == 1) {\n      perimeter += 4;  // 每格先給 4 條邊\n      if (i > 0 && grid[i - 1][j]) {\n        perimeter -= 2;  // 上方相鄰，扣共用邊\n      }\n      if (j > 0 && grid[i][j - 1]) {\n        perimeter -= 2;  // 左方相鄰，扣共用邊\n      }\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 看穿關鍵後，一行解決\n另一個極端例子是 521「最長特殊序列 Ⅰ」：給兩個字串，求「是其中一個的子序列、但不是另一個的子序列」的最長字串。看似要枚舉子序列，其實一句話搞定——**若兩字串不同，答案就是較長那個的長度；相同則回傳 −1。** 因為一個字串永遠是自己的子序列，較長的不可能是較短那個的子序列。\n\n```cpp\n// 看穿關鍵後，一行解決\nreturn a == b ? -1 : max(a.size(), b.size());\n```\n:::\n\n\n\n這類題目的「原理」其實是：**先別急著寫程式，問自己「這個問題真正在問什麼？有沒有一個不變量、對稱性或計數技巧能繞過模擬？」**\n\n**實作方式**\n\n**1. 先寫出暴力 / 模擬解，看它在算什麼。** 把笨方法想清楚，常能發現「其實大部分計算是重複或可省略的」。\n\n:::example 計數代替模擬的通用骨架：掃一遍，邊掃邊累計關鍵量\n**2. 尋找關鍵性質。** 常見的突破口：\n- **計數代替模擬**：像 463 用「總邊數 − 共用邊」取代繞邊界。\n- **對稱 / 極端**：像 1145「二叉樹著色遊戲」只需比較「對手能拿到的三塊區域中最大的一塊」和「其餘」誰多。\n- **特例直接出答案**：像 521。\n\n```cpp\n// 計數代替模擬的通用骨架：掃一遍，邊掃邊累計關鍵量\nint total = 0;\nfor (auto& cell : items) {\n  total += contribution(cell);  // 每個元素貢獻多少\n}\n```\n:::\n\n\n\n**3. 把性質寫成公式或單次掃描。** 一旦看穿，程式碼通常極短。598「區間加法 II」就是看穿「最大值只出現在所有操作的交集區域」，答案 = 所有操作 `ai` 的最小值乘以所有 `bi` 的最小值。\n\n**4. 用小例子驗證你的「靈感」。** 腦筋急轉彎最怕想歪，務必拿 2~3 個手算例子核對公式，尤其是邊界（空輸入、全相同、只有一格）。1007「行相等的最少多米諾旋轉」就要把「只可能以第一張牌的上或下值作為目標」這個觀察驗證清楚。\n\n**5. 複雜度。** 看穿之後通常是 `O(n)` 或 `O(mn)`（網格），極少需要更高。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「腦筋急轉彎」想：\n\n- **題目讀起來像要模擬一連串操作，但資料規模又大到模擬會超時**——暗示有捷徑。\n- **答案疑似是一條公式、一個極值、或某個特例**，而不是複雜流程，如 463、521、598。\n- **暴力枚舉所有可能（子序列、所有操作、所有路徑）會爆炸**；正解卻短得出奇。\n- 心法：**卡住時問「我能不能用『數』的，而不是『做』的？」「有沒有對稱性或不變量？」**\n\n確認自己套對了，問三個問題：\n\n1. 暴力解到底在重複算什麼？哪些可以一次數清？\n2. 有沒有某個不變量、對稱性或特例能讓我跳過模擬？\n3. 我的簡短公式在所有邊界（空、全同、單格）都成立嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 463 | 島嶼的周長 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 521 | 最長特殊序列 Ⅰ | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 598 | 區間加法 II | N/A | 先選排序鍵；練習用右端點、左端點或覆蓋範圍證明局部選擇不吃虧。 |\n| 1007 | [行相等的最少多米諾旋轉](https://leetcode.cn/problems/minimum-domino-rotations-for-equal-row/) | 1541 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1145 | [二叉樹著色遊戲](https://leetcode.cn/problems/binary-tree-coloring-game/) | 1741 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 463 | [島嶼的周長](https://leetcode.cn/problems/island-perimeter) |  |\n| 521 | [最長特殊序列 Ⅰ](https://leetcode.cn/problems/longest-uncommon-subsequence-i) |  |\n| 598 | [區間加法 II](https://leetcode.cn/problems/range-addition-ii) |  |\n| 1007 | [行相等的最少多米諾旋轉](https://leetcode.cn/problems/minimum-domino-rotations-for-equal-row) | 1541 |\n| 1145 | [二叉樹著色遊戲](https://leetcode.cn/problems/binary-tree-coloring-game) | 1741 |\n| 1297 | [子串的最大出現次數](https://leetcode.cn/problems/maximum-number-of-occurrences-of-a-substring) | 1748 |\n| 1332 | [刪除迴文子序列](https://leetcode.cn/problems/remove-palindromic-subsequences) | 1629 |\n| 1503 | [所有螞蟻掉下來前的最後一刻](https://leetcode.cn/problems/last-moment-before-all-ants-fall-out-of-a-plank) | 1619 |\n| 1689 | [十-二進位制數的最少數目](https://leetcode.cn/problems/partitioning-into-minimum-number-of-deci-binary-numbers) | 1355 |\n| 1708 | [長度為 K 的最大子陣列](https://leetcode.cn/problems/largest-subarray-length-k) |  |\n| 1794 | [統計距離最小的子串對個數](https://leetcode.cn/problems/count-pairs-of-equal-substrings-with-minimum-difference) |  |\n| 1903 | [字串中的最大奇數](https://leetcode.cn/problems/largest-odd-number-in-string) | 1249 |\n| 1975 | [最大方陣和](https://leetcode.cn/problems/maximum-matrix-sum) | 1648 |\n| 1992 | [找到所有的農場組](https://leetcode.cn/problems/find-all-groups-of-farmland) | 1539 |\n| 2211 | [統計道路上的碰撞次數](https://leetcode.cn/problems/count-collisions-on-a-road) | 1581 |\n| 2311 | [小於等於 K 的最長二進位制子序列](https://leetcode.cn/problems/longest-binary-subsequence-less-than-or-equal-to-k) | 1840 |\n| 2332 | [坐上公交的最晚時間](https://leetcode.cn/problems/the-latest-time-to-catch-a-bus) | 1841 |\n| 2396 | [嚴格迴文的數字](https://leetcode.cn/problems/strictly-palindromic-number) | 1329 |\n| 2419 | [按位與最大的最長子陣列](https://leetcode.cn/problems/longest-subarray-with-maximum-bitwise-and) | 1496 |\n| 2546 | [執行逐位運算使字串相等](https://leetcode.cn/problems/apply-bitwise-operations-to-make-strings-equal) | 1605 |\n| 2549 | [統計桌面上的不同數字](https://leetcode.cn/problems/count-distinct-numbers-on-board) | 1266 |\n| 2556 | [二進位制矩陣中翻轉最多一次使路徑不連通](https://leetcode.cn/problems/disconnect-path-in-a-binary-matrix-by-at-most-one-flip) | 2369 |\n| 2680 | [最大或值](https://leetcode.cn/problems/maximum-or) | 1912 |\n| 2712 | [使所有字元相等的最小成本](https://leetcode.cn/problems/minimum-cost-to-make-all-characters-equal) | 1791 |\n| 2731 | [移動機器人](https://leetcode.cn/problems/movement-of-robots) | 1923 |\n| 2733 | [既不是最小值也不是最大值](https://leetcode.cn/problems/neither-minimum-nor-maximum) | 1148 |\n| 2811 | [判斷是否能拆分陣列](https://leetcode.cn/problems/check-if-it-is-possible-to-split-array) | 1543 |\n| 2860 | [讓所有學生保持開心的分組方法數](https://leetcode.cn/problems/happy-students) | 1626 |\n| 3125 | [使得按位與結果為 0 的最大數字](https://leetcode.cn/problems/maximum-number-that-makes-result-of-bitwise-and-zero) |  |\n| 3148 | [矩陣中的最大得分](https://leetcode.cn/problems/maximum-difference-score-in-a-grid) | 1820 |\n| 3207 | [與敵人戰鬥後的最大分數](https://leetcode.cn/problems/maximum-points-after-enemy-battles) | 1591 |\n| 3227 | [字串母音遊戲](https://leetcode.cn/problems/vowels-game-in-a-string) | 1452 |\n| 3282 | [到達陣列末尾的最大得分](https://leetcode.cn/problems/reach-end-of-array-with-max-score) | 1772 |\n| 3353 | [最小總運算元](https://leetcode.cn/problems/minimum-total-operations) |  |\n| 3424 | [將陣列變相同的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-arrays-identical) | 1503 |\n| 3432 | [統計元素和差值為偶數的分割槽方案](https://leetcode.cn/problems/count-partitions-with-even-sum-difference) | 1200 |\n| 3471 | [找出最大的幾近缺失整數](https://leetcode.cn/problems/find-the-largest-almost-missing-integer) | 1308 |\n| 3496 | [最大化配對刪除後的得分](https://leetcode.cn/problems/maximize-score-after-pair-deletions) |  |\n| 3577 | [統計計算機解鎖順序排列數](https://leetcode.cn/problems/count-the-number-of-computer-unlocking-permutations) | 1750 |\n| 3596 | [最小花費路徑交替方向 I](https://leetcode.cn/problems/minimum-cost-path-with-alternating-directions-i) |  |\n| 3638 | [平衡裝運的最大數量](https://leetcode.cn/problems/maximum-balanced-shipments) | 1463 |\n| 3644 | [排序排列](https://leetcode.cn/problems/maximum-k-to-sort-a-permutation) | 1775 |\n| 3674 | [陣列元素相等的最小操作次數](https://leetcode.cn/problems/minimum-operations-to-equalize-array) | 1369 |\n| 3675 | [轉換字串的最小操作次數](https://leetcode.cn/problems/minimum-operations-to-transform-string) | 1414 |\n| 3689 | [最大子陣列總值 I](https://leetcode.cn/problems/maximum-total-subarray-value-i) | 1371 |\n| 3702 | [按位異或非零的最長子序列](https://leetcode.cn/problems/longest-subsequence-with-non-zero-bitwise-xor) | 1489 |\n| 3746 | [等量移除後的字串最小長度](https://leetcode.cn/problems/minimum-string-length-after-balanced-removals) | 1327 |\n| 3760 | [不同首字母的子字串數目](https://leetcode.cn/problems/maximum-substrings-with-distinct-start) | 1364 |\n| 3810 | [變成目標陣列的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-reach-target-array) | 1492 |\n| 3828 | [刪除子陣列後的最終元素](https://leetcode.cn/problems/final-element-after-subarray-deletions) | 1591 |",
    },
    {
      id: 34,
      title: "27. 等價轉換",
      summary:
        "**原理講解**\n\n本節介紹「等價轉換」這個思考技巧。意思是：**把一個難算的問題，換成一個跟它「答案完全一樣、但好算很多」的等價問題。** 換了個說法後，原本卡住的題目可能就變成你熟悉的套路了。\n\n:::example 等價轉換：把單字排序後當作分組的 key\n最經典的例子是 LeetCode 49「字母異位詞分組」。要把「字母組成相同、只是順序不同」的單字分成一組，例如 `eat`、`tea`、`ate` 是一組。怎麼判斷兩個單字是同一組？直接兩兩比較很慢。**等價轉換：把每個單字的字母「排序」當作它的指紋——同一組單字排序後會完全一樣。** 於是「分組」就轉成「按指紋字串分桶」。\n\n```cpp\n// 等價轉換：把單字排序後當作分組的 key\nunordered_map<string, vector<string>> groups;\nfor (string& s : strs) {\n  string key = s;\n  sort(key.begin(), key.end());  // 排序後的字串就是這組的指紋\n  groups[key].push_back(s);      // 指紋相同 → 丟進同一桶\n}\n```\n:::\n\n\n\n:::example 等價轉換：「n-1 個加 1」等價於「1 個減 1」\n另一個漂亮例子是 453「最小操作次數使陣列元素相等」：每次操作可讓「n−1 個元素各加 1」，求把所有元素變相等的最少次數。「n−1 個加 1」聽起來很怪。**等價轉換：把『n−1 個加 1』看成『1 個減 1』**——因為兩者對「元素之間的相對差距」效果完全一樣（整體平移不影響相等與否）。問題瞬間變成：每次把一個元素減 1，要花幾次讓全部相等？答案就是「∑(每個數 − 最小值)」。\n\n```cpp\n// 等價轉換：「n-1 個加 1」等價於「1 個減 1」\nint mn = *min_element(a.begin(), a.end());\nlong long ops = 0;\nfor (int x : a) {\n  ops += x - mn;  // 每個數要減到最小值的總次數\n}\n```\n:::\n\n\n\n這個技巧的精神是：**原問題的形式騙了你，換一個等價的、座標移過的、或對偶的說法，難度就崩塌了。**\n\n**實作方式**\n\n**1. 寫下原問題，找出「卡住的點」。** 通常是某個操作很怪、某個判定很慢、或維度太高。\n\n:::example 指紋化的另一種寫法：用 26 個字母的計數當 key\n**2. 尋找一個保持答案不變的轉換。** 常見手法：\n- **指紋化**：把「等價類」用一個正規化的 key 表示（排序、字元計數、移位差），如 49、249「移位字串分組」。\n\n```cpp\n// 指紋化的另一種寫法：用 26 個字母的計數當 key\narray<int, 26> cnt{};\nfor (char c : s) {\n  cnt[c - 'a']++;  // 字母出現次數相同 → 同一組\n}\n```\n:::\n\n\n\n- **相對化 / 平移**：操作只影響相對關係時，把絕對量換成相對量，如 453（「全加」等價「單減」）。\n- **對偶 / 逆向**：把「最大化 A」換成「最小化補集 B」。\n\n**3. 在轉換後的問題上套標準解法。** 轉換完通常就是排序、雜湊分桶、或前綴和等熟套路。\n\n**4. 確認轉換真的「等價」。** 這是關鍵也是陷阱：必須說清楚「原問題答案 = 新問題答案」。391「完美矩形」就把「小矩形拼成大矩形」等價成「只有四個角各出現一次、其餘角成對抵消，且面積總和相符」。1040「移動石子直到連續 II」也是把石子位置轉成「間隙」問題再算。\n\n**5. 複雜度。** 取決於轉換後的問題，通常是 `O(n log n)`（含排序或建指紋）或 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「等價轉換」想：\n\n- **題目的操作描述得很奇怪或很繞**（「n−1 個加 1」「移位」「拼接」），直接照做很難，如 453、49、391。\n- **要判斷一堆東西「是否同一類」**，而「同類」有某種可正規化的特徵——做成指紋去分桶。\n- **操作只改變「相對關係」而非絕對值**——平移到相對座標往往大幅簡化。\n- 心法：**「能不能換個說法，讓它變成我會的題目？」** 排序當 key、減去最小值、改算補集，都是常見招數。\n\n確認自己套對了，問三個問題：\n\n1. 原問題哪裡卡住？是操作怪、判定慢、還是維度高？\n2. 我用的等價轉換是什麼（指紋 / 平移 / 對偶）？\n3. 我能說清楚「轉換前後答案完全相同」嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 49 | 字母異位詞分組 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 249 | 移位字串分組（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 391 | 完美矩形 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 453 | 最小操作次數使陣列元素相等 | N/A | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 1040 | [移動石子直到連續 II](https://leetcode.cn/problems/moving-stones-until-consecutive-ii/) | 2456 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 49 | [字母異位詞分組](https://leetcode.cn/problems/group-anagrams) |  |\n| 249 | [移位字串分組](https://leetcode.cn/problems/group-shifted-strings) |  |\n| 391 | [完美矩形](https://leetcode.cn/problems/perfect-rectangle) |  |\n| 453 | [最小操作次數使陣列元素相等](https://leetcode.cn/problems/minimum-moves-to-equal-array-elements) |  |\n| 1040 | [移動石子直到連續 II](https://leetcode.cn/problems/moving-stones-until-consecutive-ii) | 2456 |\n| 1183 | [矩陣中 1 的最大數量](https://leetcode.cn/problems/maximum-number-of-ones) | 2367 |\n| 1585 | [檢查字串是否可以透過排序子字串得到另一個字串](https://leetcode.cn/problems/check-if-string-is-transformable-with-substring-sort-operations) | 2333 |\n| 1657 | [確定兩個字串是否接近](https://leetcode.cn/problems/determine-if-two-strings-are-close) | 1530 |\n| 2551 | [將珠子放入背包中](https://leetcode.cn/problems/put-marbles-in-bags) | 2042 |\n| 2914 | [使二進位制字串變美麗的最少修改次數](https://leetcode.cn/problems/minimum-number-of-changes-to-make-binary-string-beautiful) | 1480 |\n| 3365 | [重排子字串以形成目標字串](https://leetcode.cn/problems/rearrange-k-substrings-to-form-target-string) | 1514 |\n| 3375 | [使陣列的值全部為 K 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-array-values-equal-to-k) | 1383 |",
    },
    {
      id: 35,
      title: "28. 逆向思維",
      summary:
        "**觀念起點**\n\n區間貪心的第一步是決定排序鍵。若目標是選最多不重疊區間，按右端點排序最合理，因為越早結束越能留下更多後續空間。\n\n貪心不能停留在「看起來最好」，必須能用交換論證：任一最優解若首選不是最早結束的區間，把它替換為最早結束區間後，後續可選空間不會變差。\n\n區間題要先釐清端點語意。閉區間、半開區間、是否允許端點相接，直接決定判斷式使用 `>` 還是 `>=`。\n\n排序鍵依目標而異：選最多不重疊區間與用最少點覆蓋所有區間都按右端排序，而合併區間或求最大重疊數則常按左端排序。\n\n---\n\n**代表例題：LeetCode 2589 完成所有任務的最少時間**\n\n**完整問題**：LeetCode 2589 給定多個任務 `[start, end, duration]`。任務可於區間內任選 duration 個整數時間點執行，同一時間可同時服務多個任務；求完成所有任務所需開啟的最少時間點數。\n\n本節以 LeetCode 2589「完成所有任務的最少時間」 為主例題（rating 2381），並將同一套不變式延伸至本節其他題目。\n\n**題目訊號**\n\n- 每個任務為區間需求。\n- 按右端點排序後，先滿足早結束的任務。\n- 新開時間點應盡量靠右，以利覆蓋後續任務。\n\n**狀態、不變式與答案更新**\n\n- 處理至某任務時，所有更早結束的任務皆已滿足。\n- 已選時間點越靠右對未來區間越有利。\n- 若當前任務仍缺 need 個時間點，自 end 往 start 補最安全。\n\n**從零建模**\n\n1. 依 end 由小至大排序任務。\n2. 對每個任務先計算 `[start, end]` 中已開啟多少時間點。\n3. 若不足 duration，自 end 向左選尚未開啟的時間點補足。\n4. 最終已開啟時間點數即為答案。\n\n**小範例手動模擬**\n\n以區間 [1,3]、[2,4]、[3,5] 求最多不重疊：按右端排序後選 [1,3]；下一個左端 >= 3 的是 [3,5]，選之；[2,4] 與 [1,3] 重疊 → 跳過。共 2 個。\n\n**常見模式**\n\n- 區間右端點貪心\n- 最少點覆蓋帶需求區間\n- 靠右補點\n\n**常見錯誤**\n\n- 不可按 start 排序，否則將浪費可覆蓋未來的右側位置。\n- 同一時間點可服務多個任務，故並非累加 duration。\n- 排序比較器需為嚴格弱序（用 `<` 而非 `<=`），否則 `sort` 行為未定義。\n\n**複雜度**：簡單實作為 `O(n * U)`，可用線段樹優化至 `O(n log U)`。 排序主導，時間 `O(n log n)`、空間 `O(1)`。\n\n---\n\n**本節題單中的延伸題**\n\n- LeetCode 417 太平洋大西洋水流問題\n- LeetCode 554 磚牆\n- LeetCode 683 K 個關閉的燈泡\n- LeetCode 780 到達終點\n- LeetCode 782 變為棋盤\n\n---\n\n**同一模板的套用要點**\n\n「最少移除使不重疊」= 總數 - 最多不重疊；會議室類（最多同時重疊數）改用掃描線或最小堆維護結束時間。\n\n---\n\n**C++ 實作骨架**\n\n```cpp\nclass Solution {\n public:\n  int findMinimumTime(vector<vector<int>>& tasks) {\n    sort(tasks.begin(), tasks.end(),\n         [](const auto& lhs, const auto& rhs) { return lhs[1] < rhs[1]; });\n    vector<int> used(2001, 0);\n    int answer = 0;\n    for (const auto& task : tasks) {\n      int start = task[0], end = task[1], duration = task[2];\n      for (int time = start; time <= end; ++time) {\n        duration -= used[time];\n      }\n      for (int time = end; duration > 0; --time) {\n        if (used[time]) {\n          continue;\n        }\n        used[time] = 1;\n        duration--;\n        ++answer;\n      }\n    }\n    return answer;\n  }\n};\n```",
      children: [
        {
          id: 45,
          title: "28.1 逆向思維",
          summary:
            "從終態或答案反推操作序列、或把刪除改為反向插入；可降低前綴約束的難度。\n\n**常見**：時間倒流、反向併查集、從右往左掃描。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 417 | [太平洋大西洋水流問題](https://leetcode.cn/problems/pacific-atlantic-water-flow) |  |\n| 554 | [磚牆](https://leetcode.cn/problems/brick-wall) |  |\n| 683 | [K 個關閉的燈泡](https://leetcode.cn/problems/k-empty-slots) |  |\n| 780 | [到達終點](https://leetcode.cn/problems/reaching-points) |  |\n| 782 | [變為棋盤](https://leetcode.cn/problems/transform-to-chessboard) |  |\n| 869 | [重新排序得到 2 的冪](https://leetcode.cn/problems/reordered-power-of-2) | 1505 |\n| 936 | [戳印序列](https://leetcode.cn/problems/stamping-the-sequence) | 2583 |\n| 991 | [壞了的計算器](https://leetcode.cn/problems/broken-calculator) | 1909 |\n| 1354 | [多次求和構造目標陣列](https://leetcode.cn/problems/construct-target-array-with-multiple-sums) | 2015 |\n| 1558 | [得到目標陣列的最少函式呼叫次數](https://leetcode.cn/problems/minimum-numbers-of-function-calls-to-make-target-array) | 1637 |\n| 1611 | [使整數變為 0 的最少操作次數](https://leetcode.cn/problems/minimum-one-bit-operations-to-make-integers-zero) | 2345 |\n| 2139 | [得到目標值的最少行動次數](https://leetcode.cn/problems/minimum-moves-to-reach-target-score) | 1417 |\n| 2227 | [加密解密字串](https://leetcode.cn/problems/encrypt-and-decrypt-strings) | 1945 |\n| 2543 | [判斷一個點是否可以到達](https://leetcode.cn/problems/check-if-point-is-reachable) | 2221 |\n| 3141 | [最大漢明距離](https://leetcode.cn/problems/maximum-hamming-distances) |  |\n| 3147 | [從魔法師身上吸取的最大能量](https://leetcode.cn/problems/taking-maximum-energy-from-the-mystic-dungeon) | 1460 |\n| 3307 | [找出第 K 個字元 II](https://leetcode.cn/problems/find-the-k-th-character-in-string-game-ii) | 2232 |\n| 3396 | [使陣列元素互不相同所需的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-elements-in-array-distinct) | 1299 |\n| 3419 | [圖的最大邊權的最小值](https://leetcode.cn/problems/minimize-the-maximum-edge-weight-of-graph) | 2243 |\n| 3609 | [到達目標點的最小移動次數](https://leetcode.cn/problems/minimum-moves-to-reach-target-in-grid) | 2419 |\n| 3614 | [用特殊操作處理字串 II](https://leetcode.cn/problems/process-string-with-special-operations-ii) | 2011 |\n| 3779 | [得到互不相同元素的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-have-distinct-elements) | 1444 |",
        },
        {
          id: 36,
          title: "28.2 時光倒流",
          summary:
            "**原理講解**\n\n本節介紹「時光倒流」這個技巧。它專門對付一種題目：**過程中不斷「刪除 / 破壞」東西，每刪一次就要查詢當下的某個結果。** 刪除很難處理——因為刪掉一個元素後，要重新計算它對連通性、區段、結構的影響，通常很費勁。「時光倒流」的核心想法是：**把整段時間倒過來看。原本的「刪除」，倒著看就變成「新增」；而新增比刪除好處理太多了。**\n\n:::example 時光倒流骨架：把刪除順序反過來，變成依序加回\n為什麼新增比較好做？以「連通性」為例。如果你用「並查集（Union-Find，一種能快速判斷兩點是否連通、並把兩群合併的資料結構）」，它天生支援「合併（加邊）」，卻不支援「拆開（刪邊）」。所以遇到刪邊題，就把所有刪除先記下來，從「全部刪光後的最終狀態」開始，倒著把元素一個個加回去。\n\n```cpp\n// 時光倒流骨架：把刪除順序反過來，變成依序加回\n// 1) 先把所有「要被刪的」標記掉，得到最終殘局\n// 2) 從殘局開始，反向把元素一個個加回，每加一次更新答案\nvector<int> ans(queries.size());\nfor (int t = queries.size() - 1; t >= 0; t--) {  // 時間倒著走\n  addBack(queries[t]);       // 把這次「刪除」當成「加回」\n  ans[t] = currentResult();  // 記下加回後的當下結果\n}\n```\n:::\n\n\n\n最具代表性的是 LeetCode 803「打磚塊」：先有一面磚牆（黏在頂部的才穩固），然後依序敲掉某些磚，每敲一次問「有幾塊磚因此掉落」。正著模擬「敲掉一塊會連帶崩塌多少」非常難。倒過來：先把所有要敲的磚都拿走得到殘局，再「把磚一塊塊貼回去」，每貼回一塊，看它讓多少原本懸空的磚重新連回頂部——那個數量就是當初敲掉它造成的掉落數。\n\n**實作方式**\n\n**1. 辨認「只刪不加」的離線結構。** 題目給出一串刪除 / 破壞操作，且你可以「先看完所有操作再回答」（這叫離線 offline），就具備倒流條件。\n\n:::example 標記所有將被刪除的元素，建立最終殘局\n**2. 先施加所有刪除，建出「最終殘局」。** 把每個會被刪的元素標記掉，在殘局上初始化你的資料結構（並查集 / 計數 / 區段和）。\n\n```cpp\n// 標記所有將被刪除的元素，建立最終殘局\nvector<bool> removed(n, false);\nfor (auto& q : queries) {\n  removed[idx(q)] = true;  // 先全部移除\n}\nbuildStructure(removed);  // 在殘局上建好資料結構\n```\n:::\n\n\n\n:::example 反向加回，用並查集合併相鄰，記錄連通變化\n**3. 反向逐步「加回」，同時維護答案。** 從最後一個刪除開始往前，每次把該元素加回，更新結構（如並查集 union 相鄰、把區段補回），並把「此刻的查詢結果」存到對應時間點。1970「你能穿過矩陣的最後一天」就把「逐天加水淹格」倒成「逐天把陸地加回，問哪天上下/左右連通」。\n\n```cpp\n// 反向加回，用並查集合併相鄰，記錄連通變化\nfor (int t = m - 1; t >= 0; t--) {\n  activate(t);  // 把第 t 次刪的元素加回\n  for (auto nb : neighbors(t)) {\n    if (isActive(nb)) {\n      dsu.unite(t, nb);  // 與已存在的鄰居合併\n    }\n  }\n  ans[t] = query();  // 記錄此時的結果\n}\n```\n:::\n\n\n\n**4. 輸出時記得答案是「倒著填」的。** 因為你從後往前處理，答案陣列要按原始時間順序對應好。2382「刪除操作後的最大子段和」就是倒著把元素加回、用並查集合併相鄰段並維護每段和的最大值。\n\n**5. 複雜度。** 並查集近乎 `O(α(n))` 每次操作（α 是極慢成長的反阿克曼函數，可當常數），整體常見 `O((n + q) α)`，遠快於每次刪除都重算的 `O(nq)`。2718、3607 也是同型，靠倒流把刪除轉成可合併操作。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「時光倒流」想：\n\n- **操作是一連串「刪除 / 摧毀 / 斷開 / 淹沒」**，每次操作後要查詢連通性、最大子段、或某種聚合量，如 803、1970、2382。\n- **你的資料結構（並查集等）只會「合併」不會「拆分」**——倒過來就把拆分變合併了。\n- **可以離線**：所有操作一開始就全給你，允許先讀完再回答。\n- **暴力是每次刪除都重新掃全圖 / 全陣列（`O(nq)`）**；倒流把它降到接近線性。\n\n確認自己套對了，問三個問題：\n\n1. 題目的「刪除」反過來看，是不是就變成好處理的「新增 / 合併」？\n2. 我能不能先施加所有刪除、建出最終殘局當起點？\n3. 我反向加回時，每一步維護的是哪個量？答案有沒有對回正確的時間點？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 803 | 打磚塊 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1970 | [你能穿過矩陣的最後一天](https://leetcode.cn/problems/last-day-where-you-can-still-cross/) | 2124 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2382 | [刪除操作後的最大子段和](https://leetcode.cn/problems/maximum-segment-sum-after-removals/) | 2136 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2718 | [查詢後矩陣的和](https://leetcode.cn/problems/sum-of-matrix-after-queries/) | 1769 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3607 | [電網維護](https://leetcode.cn/problems/power-grid-maintenance/) | 1700 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 803 | [打磚塊](https://leetcode.cn/problems/bricks-falling-when-hit) |  |\n| 1970 | [你能穿過矩陣的最後一天](https://leetcode.cn/problems/last-day-where-you-can-still-cross) | 2124 |\n| 2382 | [刪除操作後的最大子段和](https://leetcode.cn/problems/maximum-segment-sum-after-removals) | 2136 |\n| 2718 | [查詢後矩陣的和](https://leetcode.cn/problems/sum-of-matrix-after-queries) | 1769 |\n| 3607 | [電網維護](https://leetcode.cn/problems/power-grid-maintenance) | 1700 |\n| 3639 | [變為活躍狀態的最小時間](https://leetcode.cn/problems/minimum-time-to-activate-string) | 1853 |",
        },
      ],
    },
    {
      id: 37,
      title: "29. 貢獻法",
      summary:
        "**原理講解**\n\n本節要解決的場景是：題目要你「把所有子陣列、所有子序列、或所有配對的某個量加總起來」，例如「所有子序列的寬度之和」。直覺做法是把每一個子陣列／子序列都列出來、各算一次再相加，但子陣列有 `O(n^2)` 個、子序列更有 `O(2^n)` 個，逐一列舉一定超時。\n\n「貢獻法（contribution technique）」換了一個看世界的角度：**不要一個一個「組合」去算，而是站在每個「元素」的立場，問它總共被算進答案幾次、每次貢獻多少。** 把每個元素的總貢獻加起來，就是最終答案。\n\n:::example 貢獻法的核心一問：第 i 個元素出現在幾個子陣列中？\n舉個最小的例子。陣列 `[a, b, c]`，所有子陣列的元素總和是多少？與其列出 6 個子陣列再加，不如問「`b` 出現在幾個子陣列裡」：左端點可選 a 或 b（2 種），右端點可選 b 或 c（2 種），所以 `b` 被算了 `2 * 2 = 4` 次。\n\n```cpp\n// 貢獻法的核心一問：第 i 個元素出現在幾個子陣列中？\n// 左端點有 (i + 1) 種選法（0..i），右端點有 (n - i) 種選法（i..n-1）\nlong long times = (long long)(i + 1) * (n - i);\nlong long contribution = (long long)nums[i] * times;  // 這個元素的總貢獻\n```\n:::\n\n\n\n:::example 所有子陣列的元素總和 = 每個元素的貢獻之和\n把每個 `i` 的 `contribution` 加總，一個 `O(n)` 迴圈就得到答案，省去了列舉所有子陣列。\n\n```cpp\n// 所有子陣列的元素總和 = 每個元素的貢獻之和\nlong long ans = 0;\nfor (int i = 0; i < n; ++i) {\n  ans += (long long)nums[i] * (i + 1) * (n - i);\n}\n```\n:::\n\n\n\n為什麼一定對？因為「對所有組合求和」和「對所有元素求貢獻再求和」算的是同一堆數字，只是換了相加的順序（這在數學上叫交換求和次序）。順序換了，總和不變，但複雜度從 `O(2^n)` 降到 `O(n)`。\n\n**實作方式**\n\n任何貢獻法題目都可拆成三個步驟：\n\n:::example 例：2063「所有子字串中的母音數」\n**1. 鎖定「貢獻的主體」。** 先想清楚答案是「對什麼東西求和」，再決定要算誰的貢獻。常見主體是「每個元素值」（如 2063 母音字母）或「每一對最大最小值之差」（如 891 子序列寬度）。\n\n```cpp\n// 例：2063「所有子字串中的母音數」\n// 主體 = 每個母音字母；它在多少個子字串中出現，就被數幾次\nif (isVowel(s[i])) {\n  ans += (long long)(i + 1) * (n - i);  // 同樣的左右端點計數\n}\n```\n:::\n\n\n\n:::example 例：891 排序後，nums[i] 當「最大值」的子序列有 2^i 個，\n**2. 算出每個主體被計入的「次數」與「權重」。** 次數通常是組合計數（左端 × 右端、或前面有幾個比它小的數）；權重是它本身的值或差值。子序列題常需要排序後配上 `2^k` 這類冪次。\n\n```cpp\n// 例：891 排序後，nums[i] 當「最大值」的子序列有 2^i 個，\n// 當「最小值」的子序列也有 2^i 個（前/後各 i 個元素可自由選或不選）\nlong long asMax = power2[i];          // nums[i] 作為最大值的次數\nlong long asMin = power2[n - 1 - i];  // nums[i] 作為最小值的次數\nans += (long long)nums[i] * (asMax - asMin);  // 寬度 = 最大 - 最小\n```\n:::\n\n\n\n:::example 計數很容易爆 int，全程用 long long，必要時取模\n**3. 一次掃描累加，注意溢位與取模。** 次數常是指數級的大數，務必用 `long long`，題目要求取模時每一步都要 `% MOD`。\n\n```cpp\n// 計數很容易爆 int，全程用 long long，必要時取模\nconst int MOD = 1e9 + 7;\nans = (ans % MOD + MOD) % MOD;  // 有減法時補上一個 MOD 再取模，避免負數\n```\n:::\n\n\n\n樹上的貢獻法（如 979 分配硬幣、2477 到達首都油耗）形式略不同：把「答案」換成「每條邊被經過幾次」，用一次 DFS 回傳子樹資訊，在回溯時累加每條邊的貢獻，整體仍是 `O(n)`。\n\n複雜度：排序版 `O(n log n)`，純掃描或樹形版 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「貢獻法」想：\n\n- **答案是「對所有子陣列／子序列／路徑／配對」求某個量的總和」。** 關鍵詞是「所有…之和（sum of all …）」。例如 1588 所有奇數長度子陣列的和、2063 所有子字串中的母音。\n- **暴力列舉所有組合會爆炸**（子陣列 `O(n^2)`、子序列 `O(2^n)`），但你只需要「總和」而不需要每個組合的細節——這時就可以拆成元素貢獻。\n\n:::example 嗅到味道的瞬間：你寫出了這種雙重（甚至指數級）迴圈\n```cpp\n// 嗅到味道的瞬間：你寫出了這種雙重（甚至指數級）迴圈\n// for (每個子陣列 [l, r]) ans += f(子陣列);   // 太多子陣列，超時\n// 改問：每個元素被多少個子陣列包含？把貢獻一次加總即可\n```\n:::\n\n\n\n- **每個元素的貢獻可以「獨立計算」**，不依賴別的元素怎麼選——這是能拆開求和的前提。\n- **樹上版本**：題目問「所有節點兩兩距離之和」「移動代價之和」，就把答案攤到「每條邊被經過幾次」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的答案是在對「哪一類組合」求和？\n2. 我選定的貢獻主體是誰？它的「次數」怎麼數、「權重」是什麼？\n3. 次數會不會大到爆 `int`？要不要取模、要不要處理減法產生的負數？\n\n三題都答得出來，貢獻法就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 891 | [子序列寬度之和](https://leetcode.cn/problems/sum-of-subsequence-widths/) | 2183 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 979 | [在二叉樹中分配硬幣](https://leetcode.cn/problems/distribute-coins-in-binary-tree/) | 1709 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1588 | [所有奇數長度子陣列的和](https://leetcode.cn/problems/sum-of-all-odd-length-subarrays/) | 1409 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2063 | [所有子字串中的母音](https://leetcode.cn/problems/vowels-of-all-substrings/) | 1663 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital/) | 2012 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 891 | [子序列寬度之和](https://leetcode.cn/problems/sum-of-subsequence-widths) | 2183 |\n| 979 | [在二叉樹中分配硬幣](https://leetcode.cn/problems/distribute-coins-in-binary-tree) | 1709 |\n| 1588 | [所有奇數長度子陣列的和](https://leetcode.cn/problems/sum-of-all-odd-length-subarrays) | 1409 |\n| 2063 | [所有子字串中的母音](https://leetcode.cn/problems/vowels-of-all-substrings) | 1663 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital) | 2012 |\n| 2681 | [英雄的力量](https://leetcode.cn/problems/power-of-heroes) | 2060 |\n| 2763 | [所有子陣列中不平衡數字之和](https://leetcode.cn/problems/sum-of-imbalance-numbers-of-all-subarrays) | 2278 |\n| 3426 | [所有安放棋子方案的曼哈頓距離](https://leetcode.cn/problems/manhattan-distances-of-all-arrangements-of-pieces) | 2443 |\n| 3428 | [最多 K 個元素的子序列的最值之和](https://leetcode.cn/problems/maximum-and-minimum-sums-of-at-most-size-k-subsequences) | 2028 |\n| 3771 | [探索地牢的得分](https://leetcode.cn/problems/total-score-of-dungeon-runs) | 1981 |\n| 3786 | [樹組的互動代價總和](https://leetcode.cn/problems/total-sum-of-interaction-cost-in-tree-groups) | 2139 |",
    },
    {
      id: 38,
      title: "30. 兩次掃描",
      summary:
        "**原理講解**\n\n本節要解決的場景是：每個位置的答案同時被「左邊的鄰居」和「右邊的鄰居」牽制，你想一次掃描就定案，卻發現顧得了左邊就顧不了右邊。\n\n最經典的例子是 135 分發糖果：每個小孩至少 1 顆糖，且「評分比鄰居高的人要拿得比鄰居多」。如果只從左掃到右，你能保證「比左鄰高就多給」，卻管不到右邊；反過來只從右掃到左也一樣。\n\n:::example 分發糖果：先處理「左邊」這一半約束\n「兩次掃描（two passes）」的核心想法只有一句：**把一個雙向的約束，拆成兩個單向約束，各掃一遍，最後把兩次結果合併。** 第一遍只滿足「左邊的條件」，第二遍只滿足「右邊的條件」，再取兩者的綜合（常是取最大值或相加）。\n\n```cpp\n// 分發糖果：先處理「左邊」這一半約束\nvector<int> candy(n, 1);  // 每人先給 1 顆\nfor (int i = 1; i < n; ++i) {\n  if (rating[i] > rating[i - 1]) {  // 比左鄰評分高\n    candy[i] = candy[i - 1] + 1;    // 就比左鄰多 1 顆\n  }\n}\n```\n:::\n\n\n\n:::example 第二遍：從右往左處理「右邊」約束，用 max 保住第一遍的結果\n第一遍跑完，所有「比左鄰高」的條件都滿足了，但「比右鄰高」可能還沒。於是反方向再掃一次，這次要同時保住第一遍的成果，所以用 `max` 合併。\n\n```cpp\n// 第二遍：從右往左處理「右邊」約束，用 max 保住第一遍的結果\nfor (int i = n - 2; i >= 0; i--) {\n  if (rating[i] > rating[i + 1]) {\n    candy[i] = max(candy[i], candy[i + 1] + 1);\n  }\n}\n```\n:::\n\n\n\n為什麼合併用 `max` 就對？因為每個小孩的糖數只要「同時 ≥ 左邊要求、≥ 右邊要求」即可，取兩個下界的較大值，就是滿足兩邊的最小可行值。兩遍各 `O(n)`，整體 `O(n)`，遠快於反覆來回調整。\n\n**實作方式**\n\n兩次掃描題目可拆成三個步驟：\n\n:::example 心法：left[i] 只看左側資訊，right[i] 只看右側資訊\n**1. 辨認「方向相反的兩個約束」。** 先問：某位置的答案被哪些方向影響？若同時依賴左、右兩側，且兩者互不衝突，就適合拆成兩遍。\n\n```cpp\n// 心法：left[i] 只看左側資訊，right[i] 只看右側資訊\nvector<int> left(n), right(n);\n// 之後 ans[i] 由 left[i] 與 right[i] 合併得到\n```\n:::\n\n\n\n:::example 通用骨架：左遍存 left[i]，右遍邊掃邊合併出答案\n**2. 兩遍分別維護前綴／後綴資訊。** 第一遍由左往右，累積「到 i 為止從左邊能得到的值」；第二遍由右往左，累積「從右邊能得到的值」。很多題不需要存陣列，邊掃邊合併即可。\n\n```cpp\n// 通用骨架：左遍存 left[i]，右遍邊掃邊合併出答案\nfor (int i = 0; i < n; ++i) {\n  left[i] = combineLeft(i, left);\n}\nint suf = INIT;\nfor (int i = n - 1; i >= 0; i--) {\n  suf = combineRight(i, suf);    // 後綴資訊\n  ans = mergeAns(left[i], suf);  // 與左遍合併\n}\n```\n:::\n\n\n\n:::example 例：3494 釀造藥水這類「左能到的高度 vs 右能到的高度」題\n**3. 選對合併方式並檢查邊界。** 兩個下界取 `max`、兩段距離相加、左右乘積……依題意而定。第一個與最後一個元素只有單側鄰居，初始值要設對（糖果題初始全 1）。\n\n```cpp\n// 例：3494 釀造藥水這類「左能到的高度 vs 右能到的高度」題\n// 兩次掃描各算一邊的可達上界，逐位取較小者才同時合法\nans[i] = min(fromLeft[i], fromRight[i]);\n```\n:::\n\n\n\n進階變形（如 1840 最高建築、3796 帶限制序列）本質一樣：限制是「相鄰位置差不超過 1」這種雙向耦合，就用左、右兩遍把上界分別壓下來再取最小。\n\n複雜度：每遍 `O(n)`，合計 `O(n)`，空間可優化到 `O(1)`（只存一個後綴變數）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「兩次掃描」想：\n\n- **每個位置的合法性同時取決於左、右兩邊的鄰居**，單向掃描總有一邊照顧不到。分發糖果是模板題。\n- **約束形如「相鄰差不超過某值」「比鄰居大就要更…」**，且左、右兩個約束彼此獨立、可以分開滿足後再合併。\n\n:::example 嗅到味道的瞬間：你只掃一遍，發現「右邊還沒滿足」，想再回頭修\n```cpp\n// 嗅到味道的瞬間：你只掃一遍，發現「右邊還沒滿足」，想再回頭修\n// 別來回反覆修（可能死循環）；改成正向一遍 + 反向一遍，用 max/min 合併\n```\n:::\n\n\n\n- **暴力做法要反覆來回調整直到穩定**（每次掃 `O(n)`，掃很多輪），而兩次掃描把它壓成固定兩遍。\n- 合併動作通常是 `max`（取較強的下界）、`min`（取較緊的上界）或相加（兩側距離／代價之和）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這個位置的答案，到底被「左邊」和「右邊」分別約束了什麼？\n2. 我的左遍維護什麼前綴量、右遍維護什麼後綴量？\n3. 兩遍結果該用 `max`、`min` 還是相加合併？頭尾邊界的初始值對嗎？\n\n三題都答得出來，兩次掃描就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 135 | 分發糖果 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1840 | [最高建築高度](https://leetcode.cn/problems/maximum-building-height/) | 2374 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3494 | [釀造藥水需要的最少總時間](https://leetcode.cn/problems/find-the-minimum-amount-of-time-to-brew-potions/) | 2042 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3796 | [找到帶限制序列的最大值](https://leetcode.cn/problems/find-maximum-value-in-a-constrained-sequence/) | 1833 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 135 | [分發糖果](https://leetcode.cn/problems/candy) |  |\n| 1840 | [最高建築高度](https://leetcode.cn/problems/maximum-building-height) | 2374 |\n| 3494 | [釀造藥水需要的最少總時間](https://leetcode.cn/problems/find-the-minimum-amount-of-time-to-brew-potions) | 2042 |\n| 3796 | [找到帶限制序列的最大值](https://leetcode.cn/problems/find-maximum-value-in-a-constrained-sequence) | 1833 |",
    },
    {
      id: 39,
      title: "31. 分類討論",
      summary:
        "**原理講解**\n\n本節要解決的場景是：題目沒有一條漂亮的通用公式，正確答案會因為「輸入長什麼樣」而走完全不同的處理路線。這時硬套單一模板必錯，正確策略是「分類討論（case analysis）」——**把所有可能的輸入情況切成幾個互不重疊、又合起來涵蓋全部的類別，每一類給出對應的處理方式。**\n\n舉個生活化的例子：判斷一個密碼「至少還要改幾個字元才合格」（420 強密碼檢驗器）。長度太短、長度剛好、長度太長，三種情況的最佳補救手段完全不同——短的要「插入」、長的要「刪除並順便修缺陷」。沒有一條公式能同時覆蓋，只能分情況談。\n\n分類討論的關鍵不在程式技巧，而在「分類本身要做到兩件事」：\n\n- **不重複（互斥）**：任何一個輸入只落進一個類別，避免重複處理。\n- **不遺漏（完備）**：所有可能輸入加起來剛好被類別蓋滿，沒有漏網之魚。\n\n:::example 分類討論的骨架：依關鍵特徵切成互斥且完備的幾類\n```cpp\n// 分類討論的骨架：依關鍵特徵切成互斥且完備的幾類\nif (n < 6) {\n  // 情況 A：長度太短\n} else if (n <= 20) {\n  // 情況 B：長度合適\n} else {\n  // 情況 C：長度太長\n}\n```\n:::\n\n\n\n:::example 例：838 推多米諾，逐段看左右兩張牌的方向組合來決定中間如何倒\n每一類內部，再針對該類的特性算出答案。難點往往是「找到那個能把問題切乾淨的關鍵特徵」——可能是長度、奇偶、正負、是否相鄰、首尾關係等等。\n\n```cpp\n// 例：838 推多米諾，逐段看左右兩張牌的方向組合來決定中間如何倒\n// 左 'R' 右 'L'：兩邊往中間擠 → 中間對半，奇數中點不動\n// 左 'R' 右 'R'：整段都往右倒\n// 左 'L' 右 'L'：整段都往左倒\n// 把「左右方向」當分類關鍵，四種組合各自有明確結果\n```\n:::\n\n\n\n只要分類切得乾淨，每類處理又正確，整體就一定正確；複雜度通常就是掃一遍輸入的 `O(n)`。\n\n**實作方式**\n\n分類討論題目可拆成三個步驟：\n\n:::example 先把「決定走哪條路」的特徵抽出來命名清楚\n**1. 找出「分類關鍵特徵」。** 先觀察樣例，問「是什麼讓答案不一樣？」。常見關鍵：規模大小（420 長度）、相對位置（838 左右兩端的牌）、奇偶、正負、邊界是否相接。\n\n```cpp\n// 先把「決定走哪條路」的特徵抽出來命名清楚\nchar left = dominoes[i - 1], right = dominoes[i];\n// 接著針對 (left, right) 的組合分類\n```\n:::\n\n\n\n:::example 把四種方向組合一一列清楚（保證完備、互斥）\n**2. 列出互斥且完備的所有情況，逐一處理。** 建議用紙筆先把情況表列出來，確認每個輸入恰好落入一類。每一類寫出它的答案或轉移，別讓任兩類條件重疊。\n\n```cpp\n// 把四種方向組合一一列清楚（保證完備、互斥）\nif (left == 'R' && right == 'L') {        /* 對撞，中間對半 */\n} else if (left == 'R' && right == 'R') { /* 全往右 */\n} else if (left == 'L' && right == 'L') { /* 全往左 */\n} else {                                  /* 'L'..'R' 中間不動 */\n}\n```\n:::\n\n\n\n:::example 對每條分支的臨界值各測一筆，例如長度恰好 6、恰好 20\n**3. 重點測邊界與「夾縫」案例。** 分類討論最常錯在邊界：`<` 還是 `<=`、空輸入、單一元素、奇偶交界。針對每個 `if` 的臨界值各造一個測資。\n\n```cpp\n// 對每條分支的臨界值各測一筆，例如長度恰好 6、恰好 20\n// n == 6  → 落入「合適」而非「太短」\n// n == 21 → 落入「太長」\n```\n:::\n\n\n\n像 LCP 21 追逐遊戲、LCP 48 無限棋局這類題，分類更細：要先把棋盤／圖的局面歸納成「幾種拓撲型態」，每型態各有勝負結論——但流程一樣是「切情況、逐類給答案、嚴測邊界」。\n\n複雜度：多半是掃一遍輸入 `O(n)`；圖論型態題視具體結構而定。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「分類討論」想：\n\n- **找不到一條統一公式，樣例之間答案的「成因」明顯不同。** 你會發現「這組這樣算，那組卻要那樣算」。\n- **題目本身就帶有天然的幾種型態**：長度區間（420）、方向組合（838）、勝負局面（LCP 21）、路徑是否自交（335 路徑交叉）。\n\n:::example 嗅到味道的瞬間：你的程式裡 if/else 越加越多還是有反例\n```cpp\n// 嗅到味道的瞬間：你的程式裡 if/else 越加越多還是有反例\n// 通常是分類沒切乾淨——某個輸入同時符合兩條、或都不符合\n// 退一步把「情況表」列完整，確保互斥且完備\n```\n:::\n\n\n\n- **暴力或單一貪心會在某些角落案例失效**，補丁越打越多——這代表問題天生需要分情況。\n\n確認自己有沒有套對，問三個問題：\n\n1. 是哪個特徵讓答案走不同路線？（長度？方向？奇偶？正負？）\n2. 我列的這幾類，是否互斥（不重疊）又完備（不漏）？\n3. 每條分支的臨界值我都測過了嗎？（`<` vs `<=`、空、單一元素）\n\n三題都答得出來，分類討論就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 21 | 追逐遊戲 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| LCP 48 | 無限棋局 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 335 | 路徑交叉 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 420 | 強密碼檢驗器 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 838 | [推多米諾](https://leetcode.cn/problems/push-dominoes/) | 1638 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 21 | [追逐遊戲](https://leetcode.cn/problems/Za25hA) |  |\n| LCP 48 | [無限棋局](https://leetcode.cn/problems/fsa7oZ) |  |\n| 335 | [路徑交叉](https://leetcode.cn/problems/self-crossing) |  |\n| 420 | [強密碼檢驗器](https://leetcode.cn/problems/strong-password-checker) |  |\n| 838 | [推多米諾](https://leetcode.cn/problems/push-dominoes) | 1638 |\n| 860 | [檸檬水找零](https://leetcode.cn/problems/lemonade-change) | 1286 |\n| 1033 | [移動石子直到連續](https://leetcode.cn/problems/moving-stones-until-consecutive) | 1421 |\n| 1040 | [移動石子直到連續 II](https://leetcode.cn/problems/moving-stones-until-consecutive-ii) | 2456 |\n| 1247 | [交換字元使得字串相同](https://leetcode.cn/problems/minimum-swaps-to-make-strings-equal) | 1597 |\n| 1330 | [翻轉子陣列得到最大的陣列值](https://leetcode.cn/problems/reverse-subarray-to-maximize-array-value) | 2482 |\n| 1900 | [最佳運動員的比拼回合](https://leetcode.cn/problems/the-earliest-and-latest-rounds-where-players-compete) | 2455 |\n| 2078 | [兩棟顏色不同且距離最遠的房子](https://leetcode.cn/problems/two-furthest-houses-with-different-colors) | 1241 |\n| 2091 | [從陣列中移除最大值和最小值](https://leetcode.cn/problems/removing-minimum-and-maximum-from-array) | 1384 |\n| 2131 | [連線兩字母單詞得到的最長迴文串](https://leetcode.cn/problems/longest-palindrome-by-concatenating-two-letter-words) | 1557 |\n| 2162 | [設定時間的最少代價](https://leetcode.cn/problems/minimum-cost-to-set-cooking-time) | 1852 |\n| 2165 | [重排數字的最小值](https://leetcode.cn/problems/smallest-value-of-the-rearranged-number) | 1362 |\n| 2170 | [使陣列變成交替陣列的最少運算元](https://leetcode.cn/problems/minimum-operations-to-make-the-array-alternating) | 1663 |\n| 2202 | [K 次操作後最大化頂端元素](https://leetcode.cn/problems/maximize-the-topmost-element-after-k-moves) | 1717 |\n| 2423 | [刪除字元使頻率相同](https://leetcode.cn/problems/remove-letter-to-equalize-frequency) | 1648 |\n| 2468 | [根據限制分割訊息](https://leetcode.cn/problems/split-message-based-on-limit) | 2382 |\n| 2499 | [讓陣列不相等的最小總代價](https://leetcode.cn/problems/minimum-total-cost-to-make-arrays-unequal) | 2633 |\n| 2508 | [新增邊使所有節點度數都為偶數](https://leetcode.cn/problems/add-edges-to-make-degrees-of-all-nodes-even) | 2060 |\n| 2525 | [根據規則將箱子分類](https://leetcode.cn/problems/categorize-box-according-to-criteria) | 1301 |\n| 2591 | [將錢分給最多的兒童](https://leetcode.cn/problems/distribute-money-to-maximum-children) | 1531 |\n| 2844 | [生成特殊數字的最少操作](https://leetcode.cn/problems/minimum-operations-to-make-a-special-number) | 1588 |\n| 2918 | [陣列的最小相等和](https://leetcode.cn/problems/minimum-equal-sum-of-two-arrays-after-replacing-zeros) | 1526 |\n| 2934 | [最大化陣列末位元素的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-maximize-last-elements-in-arrays) | 1803 |\n| 2982 | [找出出現至少三次的最長特殊子字串 II](https://leetcode.cn/problems/find-longest-special-substring-that-occurs-thrice-ii) | 1773 |\n| 2983 | [迴文串重新排列查詢](https://leetcode.cn/problems/palindrome-rearrangement-queries) | 2780 |\n| 3001 | [捕獲黑皇后需要的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-capture-the-queen) | 1797 |\n| 3017 | [按距離統計房屋對數目 II](https://leetcode.cn/problems/count-the-number-of-houses-at-a-certain-distance-ii) | 2709 |\n| 3139 | [使陣列中所有元素相等的最小開銷](https://leetcode.cn/problems/minimum-cost-to-equalize-array) | 2666 |\n| 3197 | [包含所有 1 的最小矩形面積 II](https://leetcode.cn/problems/find-the-minimum-area-to-cover-all-ones-ii) | 2541 |\n| 3240 | [最少翻轉次數使二進位制矩陣迴文 II](https://leetcode.cn/problems/minimum-number-of-flips-to-make-binary-grid-palindromic-ii) | 2080 |\n| 3311 | [構造符合圖結構的二維矩陣](https://leetcode.cn/problems/construct-2d-grid-matching-graph-layout) | 2664 |\n| 3357 | [最小化相鄰元素的最大差值](https://leetcode.cn/problems/minimize-the-maximum-adjacent-element-difference) | 3077 |\n| 3366 | [最小陣列和](https://leetcode.cn/problems/minimum-array-sum) | 2040 |\n| 3395 | [唯一中間眾數子序列 I](https://leetcode.cn/problems/subsequences-with-a-unique-middle-mode-i) | 2800 |\n| 3440 | [重新安排會議得到最多空餘時間 II](https://leetcode.cn/problems/reschedule-meetings-for-maximum-free-time-ii) | 1998 |\n| 3548 | [等和矩陣分割 II](https://leetcode.cn/problems/equal-sum-grid-partition-ii) | 2245 |\n| 3609 | [到達目標點的最小移動次數](https://leetcode.cn/problems/minimum-moves-to-reach-target-in-grid) | 2419 |\n| 3724 | [轉換陣列的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-transform-array) | 1790 |\n| 3789 | [採購的最小花費](https://leetcode.cn/problems/minimum-cost-to-acquire-required-items) | 1580 |\n| 3800 | [使二進位制字串相等的最小成本](https://leetcode.cn/problems/minimum-cost-to-make-two-binary-strings-equal) | 1841 |",
    },
    {
      id: 40,
      title: "32. 構造題",
      summary:
        "**原理講解**\n\n本節要解決的場景是：題目不問「最大值是多少」「有幾種方案」，而是要你**親手「造出」一個符合一堆規則的具體答案**——一個排列、一個字串、一個陣列。例如 324 擺動排序 II 要你重排成 `nums[0] < nums[1] > nums[2] < ...`、667 優美的排列 II 要你造出「相鄰差剛好有 k 種」的排列。\n\n這類「構造題（constructive problem）」的特點是：合法答案通常有很多個，你不必找最優，只要找出「任何一個合法的」即可。難點不是搜尋，而是**設計一條簡單、可證明一定合法的「建造規則」**，照著規則一步步填，填完就對。\n\n:::example 擺動排序 II 的構造思路：排序後分大、小兩半交錯放\n核心心法：**找一個你能說清楚「為什麼這樣填一定不違規」的模式，然後機械化地套用。** 以 324 擺動排序為例，把陣列排序後，想讓大小交錯，一個漂亮的構造是「把較小的一半放奇偶位的某邊、較大的一半放另一邊」。\n\n```cpp\n// 擺動排序 II 的構造思路：排序後分大、小兩半交錯放\n// 排序後前半是小數、後半是大數\nsort(nums.begin(), nums.end());\nint n = nums.size();\n// 偶數位放小數、奇數位放大數，且都「由大到小」倒著取，避免中位數相鄰相等\n```\n:::\n\n\n\n:::example 倒序填入：偶數位由小數的尾端取、奇數位由大數的尾端取\n為什麼倒著取？因為若有大量重複的中位數，順著放可能讓兩個相等的數相鄰、破壞嚴格不等式；從兩半各自的尾端往前取，能把相等的數拉到最遠，這就是「可證明安全」的細節。\n\n```cpp\n// 倒序填入：偶數位由小數的尾端取、奇數位由大數的尾端取\nint idx = 0;\nfor (int i = (n - 1) / 2; i >= 0; i--) {\n  result[idx] = nums[i], idx += 2;  // 偶數位\n}\nfor (int i = n - 1; i > (n - 1) / 2; i--) {\n  result[idx] = nums[i], idx += 2;  // 奇數位\n}\n```\n:::\n\n\n\n很多構造題還有更巧的「規律型」解。如 667：先把 `1` 和 `k + 1` 放好，前 `k + 1` 個數用「一大一小往中間夾」造出 `k` 種不同差值，剩下的數順排（差都是 1，不增加新差值）。一旦找到規律，程式就是 `O(n)` 的直接填寫。\n\n**實作方式**\n\n構造題可拆成三個步驟：\n\n:::example 例：667 優美的排列 II，前 k+1 個位置「兩端往中間夾」造出 k 種差\n**1. 先用最小例子手算，找出可重複的模式。** 拿 n=3、4、5 在紙上排一排，觀察合法答案長什麼樣，歸納出「第 i 位該放什麼」的規則。\n\n```cpp\n// 例：667 優美的排列 II，前 k+1 個位置「兩端往中間夾」造出 k 種差\n// 序列：1, k+1, 2, k, 3, k-1, ...   差值依序是 k, k-1, k-2, ...（剛好 k 種）\nint lo = 1, hi = k + 1, idx = 0;\nwhile (lo <= hi) {\n  ans[idx++] = lo++;\n  if (lo <= hi) {\n    ans[idx++] = hi--;\n  }\n}\n```\n:::\n\n\n\n:::example 667 後半：剩下的數 (k+2 .. n) 順排，相鄰差都是 1，不會新增差值種類\n**2. 把規則寫成一個確定性的填寫迴圈。** 構造通常不需要回溯或搜尋，照規律順填即可。處理好「分兩半」「兩端夾擊」「先放關鍵元素再補其餘」這幾種常見骨架。\n\n```cpp\n// 667 後半：剩下的數 (k+2 .. n) 順排，相鄰差都是 1，不會新增差值種類\nfor (int v = k + 2; v <= n; ++v) {\n  ans[idx++] = v;\n}\n```\n:::\n\n\n\n:::example 自我檢查：把造出來的答案再驗一遍是否真的合法（debug 時很值得）\n**3. 證明合法 + 嚴測邊界。** 構造題最容易「看似對、其實有反例」，務必論證每條規則為何不違規，並測極端輸入：全相等、n=1、k 取最大／最小。\n\n```cpp\n// 自我檢查：把造出來的答案再驗一遍是否真的合法（debug 時很值得）\nfor (int i = 1; i < n; ++i) {\n  assert(/* 你要求的相鄰關係 */ true);\n}\n```\n:::\n\n\n\n像 932 漂亮陣列用「分治構造」：奇數放一邊、偶數放一邊，遞迴地保證任意 `2 * nums[k] != nums[i] + nums[j]`，本質仍是「找到可證明安全的遞迴模式再機械套用」。\n\n複雜度：規律型多為 `O(n)`，需排序的 `O(n log n)`，分治構造 `O(n log n)` 上下。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「構造題」想：\n\n- **要求輸出一個具體物件**（排列／字串／陣列／矩陣），而且滿足若干結構性條件，「任一合法解皆可」。題幹常出現「請構造一個…」「返回任意一個滿足…的…」。\n- **不問最大／最小／計數**，所以不是 DP、不是貪心求最優，而是「想出一個漂亮排法」。\n\n:::example 嗅到味道的瞬間：你開始想「用回溯枚舉所有排列再檢查」\n```cpp\n// 嗅到味道的瞬間：你開始想「用回溯枚舉所有排列再檢查」\n// for (每種排列) if (合法) return it;   // n 一大就爆炸\n// 構造題的目標是「直接造一個」，跳過搜尋\n```\n:::\n\n\n\n- **小例子手算後能看出明顯規律**（兩端夾擊、分大小兩半、奇偶分組）——這通常就是出題者預期的構造法。\n- 280/324 擺動排序、484 尋找排列、667 優美排列、932 漂亮陣列都是經典構造範本。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要造的物件長什麼樣？它必須滿足哪些結構條件？\n2. 我在小例子上看出的「填寫規律」是什麼？能機械化重複嗎？\n3. 我能論證這條規律「為何永遠不違規」嗎？極端輸入（全相等、n=1）也成立嗎？\n\n三題都答得出來，構造題就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 280 | 擺動排序（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 324 | 擺動排序 II | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 484 | 尋找排列（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 667 | 優美的排列 II | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 932 | [漂亮陣列](https://leetcode.cn/problems/beautiful-array/) | 2294 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 280 | [擺動排序](https://leetcode.cn/problems/wiggle-sort) |  |\n| 324 | [擺動排序 II](https://leetcode.cn/problems/wiggle-sort-ii) |  |\n| 484 | [尋找排列](https://leetcode.cn/problems/find-permutation) |  |\n| 667 | [優美的排列 II](https://leetcode.cn/problems/beautiful-arrangement-ii) |  |\n| 932 | [漂亮陣列](https://leetcode.cn/problems/beautiful-array) | 2294 |\n| 942 | [增減字串匹配](https://leetcode.cn/problems/di-string-match) | 1444 |\n| 969 | [煎餅排序](https://leetcode.cn/problems/pancake-sorting) | 1591 |\n| 1253 | [重構 2 行二進位制矩陣](https://leetcode.cn/problems/reconstruct-a-2-row-binary-matrix) | 1506 |\n| 1304 | [和為零的 N 個不同整數](https://leetcode.cn/problems/find-n-unique-integers-sum-up-to-zero) | 1167 |\n| 1317 | [將整數轉換為兩個無零整數的和](https://leetcode.cn/problems/convert-integer-to-the-sum-of-two-no-zero-integers) | 1278 |\n| 1605 | [給定行和列的和求可行矩陣](https://leetcode.cn/problems/find-valid-matrix-given-row-and-column-sums) | 1868 |\n| 1968 | [構造元素不等於兩相鄰元素平均值的陣列](https://leetcode.cn/problems/array-with-elements-not-equal-to-average-of-neighbors) | 1499 |\n| 1980 | [找出不同的二進位制字串](https://leetcode.cn/problems/find-unique-binary-string) | 1362 |\n| 1982 | [從子集的和還原陣列](https://leetcode.cn/problems/find-array-given-subset-sums) | 2872 |\n| 2122 | [還原原陣列](https://leetcode.cn/problems/recover-the-original-array) | 2159 |\n| 2182 | [構造限制重複的字串](https://leetcode.cn/problems/construct-string-with-repeat-limit) | 1680 |\n| 2375 | [根據模式串構造最小數字](https://leetcode.cn/problems/construct-smallest-number-from-di-string) | 1642 |\n| 2573 | [找出對應 LCP 矩陣的字串](https://leetcode.cn/problems/find-the-string-with-lcp) | 2682 |\n| 2790 | [長度遞增組的最大數目](https://leetcode.cn/problems/maximum-number-of-groups-with-increasing-length) | 2620 |\n| 3311 | [構造符合圖結構的二維矩陣](https://leetcode.cn/problems/construct-2d-grid-matching-graph-layout) | 2664 |\n| 3513 | [不同 XOR 三元組的數目 I](https://leetcode.cn/problems/number-of-unique-xor-triplets-i) | 1663 |\n| 3592 | [硬幣面值還原](https://leetcode.cn/problems/inverse-coin-change) | 1701 |\n| 3680 | [生成賽程](https://leetcode.cn/problems/generate-schedule) | 2378 |",
    },
    {
      id: 41,
      title: "33. 互動題",
      summary:
        "**原理講解**\n\n本節要解決的場景跟一般題目很不一樣：你看不到完整輸入。資料藏在系統手裡，你只能透過幾個「詢問函式（API）」一次問一點點，系統回你答案，你再根據回答決定下一步——這就是「互動題（interactive problem）」。例如 374 猜數字大小，系統選了一個數，你每次猜一個值，它只告訴你「太大、太小、還是猜中」。\n\n互動題的核心目標是：**用「最少的詢問次數」逼出答案。** 因為每次詢問都有成本（題目常限制總次數），你不能亂猜，要設計一個能「每問一次就大幅縮小未知範圍」的策略。\n\n:::example 374 猜數字：每問一次，範圍砍半，最多 log2(n) 次\n最常用的武器是二分搜尋（binary search）：每次詢問把「還可能是答案的範圍」砍掉一半。\n\n```cpp\n// 374 猜數字：每問一次，範圍砍半，最多 log2(n) 次\nint lo = 1, hi = n;\nwhile (lo <= hi) {\n  int mid = lo + (hi - lo) / 2;  // 用減法寫法避免溢位\n  int res = guess(mid);          // 向系統詢問\n  if (res == 0) {\n    return mid;  // 猜中\n  } else if (res < 0) {\n    hi = mid - 1;  // 太大 → 答案在左半\n  } else {\n    lo = mid + 1;  // 太小 → 答案在右半\n  }\n}\n```\n:::\n\n\n\n為什麼這樣快？因為每次回答都讓搜尋區間減半，問 `O(log n)` 次就能定位，遠優於一個一個試的 `O(n)`。278 第一個錯誤的版本也是同一招：找「第一個讓 `isBadVersion()` 變 true 的位置」。\n\n:::example 702：先指數式擴張找到一個上界，再在 [lo, hi] 內二分\n當範圍「未知有多大」時（702 搜尋長度未知的有序陣列），先用「倍增（exponential search）」快速框出上界，再二分：\n\n```cpp\n// 702：先指數式擴張找到一個上界，再在 [lo, hi] 內二分\nint lo = 0, hi = 1;\nwhile (reader.get(hi) < target) {  // 還沒越過 target 就把範圍翻倍\n  lo = hi;\n  hi *= 2;\n}\n// 接著在 [lo, hi] 內做標準二分\n```\n:::\n\n\n\n不是所有互動題都靠二分。277 搜尋名人靠「每問一次淘汰一個候選」把 n 個人縮到 1 個再驗證；489 掃地機器人靠 DFS 配合「走過就回退」探索未知房間。共同精神都是：**每次詢問都要換到最大資訊量，並嚴守次數上限。**\n\n**實作方式**\n\n互動題可拆成三個步驟：\n\n:::example 先把可用的詢問與限制寫下來，例如\n**1. 認清「能問什麼」與「能問幾次」。** 先讀清題目給的 API 簽名、回傳值意義，以及詢問次數／其他資源的上限——上限往往暗示了該用哪種策略（`log n` 次 → 二分）。\n\n```cpp\n// 先把可用的詢問與限制寫下來，例如：\n// int guess(int num);  // 回 -1/0/1\n// 限制：盡量少問 → 提示走二分（O(log n) 次）\n```\n:::\n\n\n\n:::example 277 搜尋名人：一遍掃描淘汰，候選最後只剩一人\n**2. 設計「每問一次就大幅縮小未知」的策略。** 有序／單調 → 二分；範圍未知 → 先倍增再二分；要從 n 個候選選 1 → 每問淘汰一個；探索地圖 → DFS + 回退。\n\n```cpp\n// 277 搜尋名人：一遍掃描淘汰，候選最後只剩一人\nint cand = 0;\nfor (int i = 1; i < n; ++i) {\n  if (knows(cand, i)) {\n    cand = i;  // cand 認識 i → cand 不可能是名人，換 i\n               // 之後再花一輪驗證 cand 是否真的是名人\n  }\n}\n```\n:::\n\n\n\n:::example 機器人 DFS 探索：每進一格做事，回溯時務必「轉身走回去」還原位置\n**3. 收尾驗證並嚴守邊界。** 縮到單一候選後，互動題常要再「確認」一次（名人題要驗證 cand 不認識任何人、且所有人都認識他）。注意 `mid` 計算用 `lo + (hi - lo) / 2` 防溢位，回退要確實還原狀態。\n\n```cpp\n// 機器人 DFS 探索：每進一格做事，回溯時務必「轉身走回去」還原位置\nrobot.move();  // 前進一格\ndfs();         // 探索\nrobot.turnRight();\nrobot.turnRight();  // 掉頭\nrobot.move();       // 退回原格，保持狀態一致\nrobot.turnRight();\nrobot.turnRight();  // 轉回原朝向\n```\n:::\n\n\n\n複雜度：二分型詢問 `O(log n)` 次；候選淘汰型 `O(n)` 次詢問；探索型與可達格子數成正比。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「互動題」想：\n\n- **你拿不到完整輸入，只能呼叫題目給的函式去「問」。** 題幹會提供 `guess()`、`isBadVersion()`、`knows()` 之類的 API，並限制呼叫次數。\n- **目標是「用最少次詢問定位答案」**，而不是處理一份現成資料。\n\n:::example 嗅到味道的瞬間：題目沒給陣列，只給你一個可呼叫的函式\n```cpp\n// 嗅到味道的瞬間：題目沒給陣列，只給你一個可呼叫的函式\n// 你想 for (i = 1..n) if (check(i)) ...   // 但這會問太多次，超過上限\n// 改設計「每問一次資訊量最大」的策略（多半是二分）\n```\n:::\n\n\n\n- **資料具單調性／有序性**（猜大小、壞版本之後全壞）→ 強烈暗示二分，`O(log n)` 次搞定。\n- **未知範圍大小** → 先倍增框界再二分（702）。**從多個候選挑一個** → 每問淘汰一個（277）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能呼叫哪些詢問函式？回傳值各代表什麼？總共能問幾次？\n2. 每問一次，我能把「未知的部分」縮小多少？（砍半？淘汰一個？）\n3. 縮到單一候選後，需不需要再做一次驗證才能回傳？\n\n三題都答得出來，互動題就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 277 | 搜尋名人（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 278 | 第一個錯誤的版本 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 374 | 猜數字大小 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 489 | 掃地機器人（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 702 | 搜尋長度未知的有序陣列（Premium） | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 277 | [搜尋名人](https://leetcode.cn/problems/find-the-celebrity) |  |\n| 278 | [第一個錯誤的版本](https://leetcode.cn/problems/first-bad-version) |  |\n| 374 | [猜數字大小](https://leetcode.cn/problems/guess-number-higher-or-lower) |  |\n| 489 | [掃地機器人](https://leetcode.cn/problems/robot-room-cleaner) |  |\n| 702 | [搜尋長度未知的有序陣列](https://leetcode.cn/problems/search-in-a-sorted-array-of-unknown-size) |  |\n| 843 | [猜猜這個單詞](https://leetcode.cn/problems/guess-the-word) | 2078 |\n| 1095 | [山脈陣列中查詢目標值](https://leetcode.cn/problems/find-in-mountain-array) | 1827 |\n| 1237 | [找出給定方程的正整數解](https://leetcode.cn/problems/find-positive-integer-solution-for-a-given-equation) | 1405 |\n| 1274 | [矩形內船隻的數目](https://leetcode.cn/problems/number-of-ships-in-a-rectangle) | 1997 |\n| 1428 | [至少有一個 1 的最左端列](https://leetcode.cn/problems/leftmost-column-with-at-least-a-one) |  |\n| 1533 | [找到最大整數的索引](https://leetcode.cn/problems/find-the-index-of-the-large-integer) |  |\n| 1538 | [找出隱藏陣列中出現次數最多的元素](https://leetcode.cn/problems/guess-the-majority-in-a-hidden-array) |  |\n| 1618 | [找出適應螢幕的最大字號](https://leetcode.cn/problems/maximum-font-to-fit-a-sentence-in-a-screen) |  |\n| 1778 | [未知網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-hidden-grid) |  |\n| 1810 | [隱藏網格下的最小消耗路徑](https://leetcode.cn/problems/minimum-path-cost-in-a-hidden-grid) |  |\n| 2728 | [計算一個環形街道上的房屋數量](https://leetcode.cn/problems/count-houses-in-a-circular-street) |  |\n| 2782 | [唯一類別的數量](https://leetcode.cn/problems/number-of-unique-categories) |  |\n| 2936 | [包含相等值數字塊的數量](https://leetcode.cn/problems/number-of-equal-numbers-blocks) |  |\n| 3064 | [使用按位查詢猜測數字 I](https://leetcode.cn/problems/guess-the-number-using-bitwise-questions-i) |  |\n| 3094 | [使用按位查詢猜測數字 II](https://leetcode.cn/problems/guess-the-number-using-bitwise-questions-ii) |  |",
    },
    {
      id: 42,
      title: "34. 其他",
      summary:
        "**原理講解**\n\n本節是貪心大章節的「其他」雜項：收錄那些用得到貪心精神、但不剛好落入「區間排程／反悔／字典序」等標準小節的題目。它們的共同點是——**先觀察出一條「可證明安全的局部選擇規則」，照著一路走到底，就能得到全域最優或唯一可行解。** 沒有花俏的資料結構，難在「看穿那條規則為什麼成立」。\n\n:::example 加油站的貪心：一旦累積油量轉負，就放棄之前所有起點，從下一站重來\n最具代表性的是 134 加油站：一圈加油站，每站可加 `gas[i]`、開到下一站耗 `cost[i]`，問能否找到一個起點繞完整圈。暴力是「每個起點都試著跑一圈」共 `O(n^2)`，但有一條漂亮的貪心觀察：**若從 A 出發，走到 B 時油箱第一次變負，那麼 A 到 B 之間的任何一站都不可能當起點**——因為從 A 起步時油更充足都撐不到 B，從中間起步只會更慘。\n\n```cpp\n// 加油站的貪心：一旦累積油量轉負，就放棄之前所有起點，從下一站重來\nint total = 0, tank = 0, start = 0;\nfor (int i = 0; i < n; ++i) {\n  int diff = gas[i] - cost[i];  // 這一站淨賺/淨賠的油\n  total += diff;                // 全程總淨油（判斷整圈是否可行）\n  tank += diff;                 // 從目前 start 起的累積油\n  if (tank < 0) {               // 撐不下去了\n    start = i + 1;              // 起點直接跳到下一站\n    tank = 0;                   // 油箱歸零重新算\n  }\n}\nreturn total >= 0 ? start : -1;  // 總油 >= 0 才有解\n```\n:::\n\n\n\n為什麼最後 `start` 一定對？因為「總淨油 ≥ 0」保證有解，而被跳過的起點都已被論證不可行，所以剩下的 `start` 就是答案。一遍掃描 `O(n)`，省掉外層的重試。\n\n:::example 659：用兩個計數，優先把當前數字接到已有序列的尾巴\n另一種雜項是「結構觀察 + 貪心配對」，如 659 分割陣列為連續子序列：把數字盡量接到已存在的連續序列尾端，接不上才另起新序列——同樣是「當下能接就接」的安全局部選擇。\n\n```cpp\n// 659：用兩個計數，優先把當前數字接到已有序列的尾巴\n// need[x]：結尾需要 x 的序列數量；freq[x]：x 還剩幾個沒用\nif (need[x] > 0) {\n  need[x]--;\n  need[x + 1]++;\n}  // 接到舊序列尾端\nelse if (freq[x + 1] > 0 && freq[x + 2] > 0) { /* 新開一條長度 3 的序列 */\n} else {\n  return false;  // 兩者都不行 → 無解\n}\n```\n:::\n\n\n\n**實作方式**\n\n雜項貪心沒有單一模板，但有共通的攻法：\n\n:::example 暴力骨架（先寫出來找規律）：每個起點各跑一圈\n**1. 先寫暴力，再找「能砍掉重複工作」的觀察。** 把 `O(n^2)` 的重試列出來，問「失敗時，能不能一次排除一整批候選？」加油站的「轉負就跳過整段」正是這種觀察。\n\n```cpp\n// 暴力骨架（先寫出來找規律）：每個起點各跑一圈\n// for (int s = 0; s < n; ++s) if (canFinish(s)) return s;   // O(n^2)\n// 觀察：失敗點之前的所有起點都可一次排除 → 降為 O(n)\n```\n:::\n\n\n\n:::example 通用骨架：維護摘要，逐元素做安全的局部決策\n**2. 把局部規則寫成一遍掃描，並維護必要的摘要狀態。** 摘要可能是「累積油量」「各序列尾端的需求計數」「目前前綴和」等，邊掃邊更新。\n\n```cpp\n// 通用骨架：維護摘要，逐元素做安全的局部決策\nState st = init;\nfor (auto& x : arr) {\n  st = applyGreedyChoice(st, x);  // 當下最安全的選擇\n}\nreturn readAnswer(st);\n```\n:::\n\n\n\n:::example 加油站邊界：total 恰為 0 仍有解；total < 0 必無解\n**3. 證明「局部選擇不傷全域」並嚴測邊界。** 雜項題的反例常藏在邊界：空輸入、全部相等、剛好臨界（加油站總油恰為 0）。每個假設都造一筆測資。\n\n```cpp\n// 加油站邊界：total 恰為 0 仍有解；total < 0 必無解\n// 測：所有 gas == cost、單一站、起點落在最後一站\n```\n:::\n\n\n\n像 LCP 03 機器人大冒險、LCP 26 導航裝置、LCP 70 沙地治理這類題，外殼是模擬或圖論，但解題關鍵仍是「找出那條讓你不必窮舉的觀察」。\n\n複雜度：抓對觀察後多半是 `O(n)` 或 `O(n log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「雜項貪心 / 結構觀察」想：\n\n- **暴力是「每個起點／每種選擇都重試一遍」的 `O(n^2)`**，但你隱約覺得「失敗時應該能一次排除一大批」。加油站是典型。\n- **存在一個直覺的局部選擇**（能接就接、轉負就重來、當下取最划算），而且你能講出「為什麼這樣不會錯過更好的解」。\n\n:::example 嗅到味道的瞬間：你寫了雙層迴圈反覆重算，且內層大量重複\n```cpp\n// 嗅到味道的瞬間：你寫了雙層迴圈反覆重算，且內層大量重複\n// 退一步問：某次失敗能不能讓我「跳過一整段候選」而非只跳一個？\n```\n:::\n\n\n\n- **題目要的是「可行性／唯一答案」而非複雜計數**，常可用一遍掃描配上幾個計數器解決（659）。\n- 若局部規則想不出、交換論證不成立，就要懷疑它其實是 DP 或搜尋，而非貪心。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的暴力解在重複做什麼？失敗時能不能一次排除一批候選？\n2. 我的局部選擇規則是什麼？我能論證它「不會錯過更優解」嗎？\n3. 我維護了哪個摘要狀態？邊界（全相等、臨界、空輸入）都測過了嗎？\n\n三題都答得出來，這類雜項貪心就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 03 | 機器人大冒險 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| LCP 26 | 導航裝置 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| LCP 70 | 沙地治理 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 134 | 加油站 | N/A | 適合練反悔貪心；先接受候選，違法時用堆刪掉最不划算的一項。 |\n| 659 | 分割陣列為連續子序列 | N/A | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(),\n         [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        ++kept;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 03 | [機器人大冒險](https://leetcode.cn/problems/programmable-robot) |  |\n| LCP 26 | [導航裝置](https://leetcode.cn/problems/hSRGyL) |  |\n| LCP 70 | [沙地治理](https://leetcode.cn/problems/XxZZjK) |  |\n| 134 | [加油站](https://leetcode.cn/problems/gas-station) |  |\n| 659 | [分割陣列為連續子序列](https://leetcode.cn/problems/split-array-into-consecutive-subsequences) |  |\n| 781 | [森林中的兔子](https://leetcode.cn/problems/rabbits-in-forest) |  |\n| 1733 | [需要教語言的最少人數](https://leetcode.cn/problems/minimum-number-of-people-to-teach) | 1984 |\n| 1864 | [構成交替字串需要的最小交換次數](https://leetcode.cn/problems/minimum-number-of-swaps-to-make-the-binary-string-alternating) | 1601 |\n| 1899 | [合併若干三元組以形成目標三元組](https://leetcode.cn/problems/merge-triplets-to-form-target-triplet) | 1636 |\n| 2340 | [生成有效陣列的最少交換次數](https://leetcode.cn/problems/minimum-adjacent-swaps-to-make-a-valid-array) |  |\n| 2498 | [青蛙過河 II](https://leetcode.cn/problems/frog-jump-ii) | 1759 |\n| 2732 | [找到矩陣中的好子集](https://leetcode.cn/problems/find-a-good-subset-of-the-matrix) | 2240 |\n| 2740 | [找出分割槽值](https://leetcode.cn/problems/find-the-value-of-the-partition) | 1302 |\n| 2753 | [計算一個環形街道上的房屋數量 II](https://leetcode.cn/problems/count-houses-in-a-circular-street-ii) |  |\n| 3002 | [移除後集合的最多元素數](https://leetcode.cn/problems/maximum-size-of-a-set-after-removals) | 1917 |\n| 3443 | [K 次修改後的最大曼哈頓距離](https://leetcode.cn/problems/maximum-manhattan-distance-after-k-changes) | 1856 |\n| 3460 | [最多刪除一次後的最長公共字首](https://leetcode.cn/problems/longest-common-prefix-after-at-most-one-removal) |  |\n| 3523 | [非遞減陣列的最大長度](https://leetcode.cn/problems/make-array-non-decreasing) | 1435 |\n| 3587 | [最小相鄰交換至奇偶交替](https://leetcode.cn/problems/minimum-adjacent-swaps-to-alternate-parity) | 1548 |\n| 3635 | [最早完成陸地和水上游樂設施的時間 II](https://leetcode.cn/problems/earliest-finish-time-for-land-and-water-rides-ii) | 1870 |",
    },
    {
      id: 9009,
      title: "35. 常見面試模式",
      summary:
        "**常見面試模式**\n\n貪心是面試裡最常露臉的解題骨架之一：許多看似不同的題目，骨子裡都是「先排序，再用一條清楚的規則做局部最優選擇」。掌握下面幾種固定模式，遇到新題時就能快速辨認出它屬於哪一類、該套用哪種貪心。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 區間排程 | 要選最多不重疊的活動／區間 | 按右端點排序，貪心地選結束最早者 | [435 無重疊區間](https://leetcode.cn/problems/non-overlapping-intervals/) |\n| 區間合併 | 把重疊或相接的區間併成一條 | 按左端點排序，掃描時延伸右邊界 | [56 合併區間](https://leetcode.cn/problems/merge-intervals/) |\n| 堆積貪心 | 反覆取「目前最大／最小」 | 用優先佇列維護當前極值 | [1046 最後一塊石頭的重量](https://leetcode.cn/problems/last-stone-weight/) |\n| 反悔貪心 | 先全拿，超限時退回最差的選擇 | 用最小堆記錄已選項，必要時反悔 | [871 最低加油次數](https://leetcode.cn/problems/minimum-number-of-refueling-stops/) |\n| 可達性跳躍 | 在陣列上判斷能否抵達或最少步數 | 維護目前能到達的最遠位置 | [55 跳躍遊戲](https://leetcode.cn/problems/jump-game/) |\n| 排序後雙指針 | 兩組資料配對、滿足某條件 | 排序後用兩端指針貪心配對 | [455 分發餅乾](https://leetcode.cn/problems/assign-cookies/) |\n| 兩側兩遍掃描 | 某位置同時受左右鄰居約束 | 左到右、右到左各掃一遍取 max | [135 分發糖果](https://leetcode.cn/problems/candy/) |\n\n**小提醒**\n\n- 看到「最多／最少」「不重疊」「最早結束」這類字眼，先想想能不能排序後貪心。\n- 排序鍵（左端點 vs 右端點）往往決定了整題對錯，先想清楚要按哪個端點排。\n- 貪心若不確定正確性，務必用交換論證或反例驗證，別只憑直覺。",
    },
    {
      id: 9103,
      title: "36. 區間（Intervals）",
      summary:
        "**原理講解**\n\n本章講「區間（intervals）」這一大類問題。輸入通常是一堆形如 `[start, end]` 的區間，要求你做合併、插入、選最多不重疊、用最少資源覆蓋，或統計某時刻的重疊數量。這類題的共通骨架是：**先排序（按左端點或右端點），再線性掃描**，把整體問題化成一連串簡單的局部判斷。時間複雜度幾乎都由排序主導，為 `O(n log n)`。\n\n**何時使用**\n\n看到下列訊號，多半就是區間題：\n\n- 區間之間會**重疊**，要判斷或計算重疊的範圍。\n- 要把重疊／相接的區間**合併**成更大的區間。\n- 要在區間中做**排程**，選出最多互不衝突的活動。\n- 問「最少需要幾間**會議室**」「最少幾支**箭**能戳爆所有氣球」這類最少資源問題。\n\n**依起點排序後合併（合併區間 LC 56）**\n\n:::example 標註：按左端點排序後線性合併，重疊取 max 延伸右界\n把所有區間按左端點升序排序後，重疊的區間必定相鄰，於是只要維護「目前累積的區間」，重疊就把右邊界取 max 延伸，脫離就收尾另起一條。\n\n```cpp\n// 標註：按左端點排序後線性合併，重疊取 max 延伸右界\nvector<vector<int>> merge(vector<vector<int>>& a) {\n  sort(a.begin(), a.end());\n  vector<vector<int>> res;\n  for (auto& it : a) {\n    if (res.empty() || res.back()[1] < it[0])\n      res.push_back(it);  // 脫離：另起一條\n    else\n      res.back()[1] = max(res.back()[1], it[1]);  // 重疊：延伸\n  }\n  return res;\n}\n```\n:::\n\n\n\n**插入區間（LC 57）**\n\n:::example 標註：左段直接放、重疊段合併、右段直接放\n給一個已按起點排好且互不重疊的區間清單，插入一個新區間並保持結果有序、不重疊。分三段處理：在新區間左側（完全不重疊）的直接加入；與新區間重疊的不斷合併進去；右側剩餘的再依序加入。\n\n```cpp\n// 標註：左段直接放、重疊段合併、右段直接放\nvector<vector<int>> insert(vector<vector<int>>& a, vector<int> ni) {\n  vector<vector<int>> res;\n  int i = 0, n = a.size();\n  while (i < n && a[i][1] < ni[0]) res.push_back(a[i++]);  // 左段\n  while (i < n && a[i][0] <= ni[1]) {                      // 重疊段\n    ni[0] = min(ni[0], a[i][0]);\n    ni[1] = max(ni[1], a[i][1]);\n    ++i;\n  }\n  res.push_back(ni);\n  while (i < n) res.push_back(a[i++]);  // 右段\n  return res;\n}\n```\n:::\n\n\n\n**區間排程／最多不重疊（無重疊區間 LC 435、用最少箭 LC 452）**\n\n:::example 標註：按右端點排序，貪心選結束最早者以最大化不重疊數\n要選出最多互不重疊的區間（或反過來，移除最少的區間），關鍵是**按右端點升序排序**，每次貪心地選結束最早的，這樣替後面留下最多空間。記錄上一個被選中區間的右端點 `last$_R$`，下一個區間若起點 $> last_R$ 就能選。LC 452「用最少箭引爆氣球」是同一招的對偶：能被同一支箭射穿的氣球，相當於可重疊地歸成一組。\n\n```cpp\n// 標註：按右端點排序，貪心選結束最早者以最大化不重疊數\nint eraseOverlapIntervals(vector<vector<int>>& a) {\n  sort(a.begin(), a.end(), [](auto& x, auto& y) { return x[1] < y[1]; });\n  int cnt = 0, lastR = INT_MIN;\n  for (auto& it : a) {\n    if (it[0] >= lastR)\n      lastR = it[1];  // 不重疊：選它\n    else\n      ++cnt;  // 重疊：移除\n  }\n  return cnt;  // 需移除的最少數量\n}\n```\n:::\n\n\n\n**掃描線／差分（會議室 II LC 253、拼車 LC 1094）**\n\n:::example 標註：差分陣列，start 處 +1、end 處 -1，前綴和峰值即同時重疊數\n要問「同一時刻最多有幾個區間同時存在」，用**掃描線**：把每個區間拆成 `(start, +1)` 與 `(end, -1)` 兩個事件，按時間排序後依序累加，過程中的最大值就是答案。若座標範圍小且為整數，可改用**差分陣列**：在 `start` 處 `+1`、在 `end` 處 `-1`，前綴和的最大值即為峰值。LC 1094「拼車」就是差分的典型應用。\n\n```cpp\n// 標註：差分陣列，start 處 +1、end 處 -1，前綴和峰值即同時重疊數\nbool carPooling(vector<vector<int>>& trips, int capacity) {\n  int diff[1001] = {0};\n  for (auto& t : trips) {  // t = {numPassengers, from, to}\n    diff[t[1]] += t[0];\n    diff[t[2]] -= t[0];\n  }\n  int cur = 0;\n  for (int i = 0; i <= 1000; ++i) {\n    cur += diff[i];  // 前綴和：當前車上人數\n    if (cur > capacity) return false;\n  }\n  return true;\n}\n```\n:::\n\n\n\n**模式總覽**\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 合併 | 重疊／相接要併成一條 | 按左端點排序後掃描延伸右界 | [56 合併區間](https://leetcode.cn/problems/merge-intervals/) |\n| 插入 | 在有序不重疊清單插一段 | 左段、重疊段合併、右段三段處理 | [57 插入區間](https://leetcode.cn/problems/insert-interval/) |\n| 排程 | 選最多不重疊／移除最少 | 按右端點排序貪心選最早結束 | [435 無重疊區間](https://leetcode.cn/problems/non-overlapping-intervals/) |\n| 覆蓋／戳穿 | 用最少箭／點覆蓋全部 | 按右端點排序，能共用就共用 | [452 用最少數量的箭引爆氣球](https://leetcode.cn/problems/minimum-number-of-arrows-to-burst-balloons/) |\n| 掃描線／差分 | 統計某時刻的重疊數 | 事件 ±1 排序累加取峰值 | [253 會議室 II](https://leetcode.cn/problems/meeting-rooms-ii/) |\n\n**代表題**\n\n- [56 合併區間](https://leetcode.cn/problems/merge-intervals/)\n- [57 插入區間](https://leetcode.cn/problems/insert-interval/)\n- [435 無重疊區間](https://leetcode.cn/problems/non-overlapping-intervals/)\n- [452 用最少數量的箭引爆氣球](https://leetcode.cn/problems/minimum-number-of-arrows-to-burst-balloons/)\n- [253 會議室 II](https://leetcode.cn/problems/meeting-rooms-ii/)\n- [1094 拼車](https://leetcode.cn/problems/car-pooling/)\n- [1109 航班預訂統計](https://leetcode.cn/problems/corporate-flight-bookings/)\n- [763 劃分字母區間](https://leetcode.cn/problems/partition-labels/)\n- [986 區間列表的交集](https://leetcode.cn/problems/interval-list-intersections/)\n- [759 員工空閒時間](https://leetcode.cn/problems/employee-free-time/)\n- [218 天際線問題](https://leetcode.cn/problems/the-skyline-problem/)\n\n**小提醒**\n\n- 注意**端點開閉**：`[1, 2]` 與 `[2, 3]` 在「相接算不算重疊」上，不同題定義不同，動手前先確認。\n- **排序鍵**要選對：合併題按左端點，排程／戳穿題按右端點，選錯往往整題崩盤。\n- 別忘了處理**空輸入**：區間清單為空時應直接回傳空結果，避免存取 `intervals[0]` 越界。",
    },
  ],
} as TutorialData.Root;
