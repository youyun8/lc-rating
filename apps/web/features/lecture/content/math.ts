import type { TutorialData } from "@/types";

export const math = {
  id: 1,
  title: "數學（數論/組合/機率期望/博弈/計算幾何/隨機演算法）",
  src: "https://leetcode.cn/circle/discuss/IYT3ss/",
  last_update: "2026-02-24T14:34:02.963631+00:00",
  summary:
    "**本題單涵蓋**：\n- 數論：質數、GCD/LCM、質因數分解、尤拉函式\n- 組合數學：排列組合、容斥原理、卡特蘭數\n- 機率期望：期望計算、機率 DP\n- 博弈論：Nim 遊戲、SG 函式\n- 計算幾何：點線面、凸包\n- 隨機演算法：蒙特卡羅、拒絕抽樣\n\n**取模相關**：部分題目涉及取模運算，詳見 [模運算教程](https://leetcode.cn/circle/discuss/mDfnkW/)\n\n:::example 埃氏篩：篩出 [2, n] 的所有質數\n**C++ 質數篩模板**：\n\n```cpp\n// 埃氏篩：篩出 [2, n] 的所有質數\nvector<int> sieve(int n) {\n    vector<int> primes;\n    vector<bool> is_prime(n + 1, true);\n    is_prime[0] = is_prime[1] = false;\n    for (int p = 2; p <= n; ++p) {\n        if (is_prime[p]) {\n            primes.push_back(p);\n            for (long long i = (long long)p * p; i <= n; i += p) {  // 從 p² 開始篩 {\n                is_prime[i] = false;\n            }\n        }\n    }\n}\nreturn primes;\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 判斷質數",
      summary:
        "**原理講解**\n\n本節要解決的問題很單純：給一個整數 `n`，判斷它是不是「質數（prime）」。質數的定義是：大於 1，而且除了 1 和它自己以外，沒有別的正整數能整除它（例如 2、3、5、7 是質數；4 = 2×2、9 = 3×3 不是）。\n\n最直覺的做法是：拿 2、3、4 …一路試到 `n − 1`，只要有任何一個能整除 `n`，它就不是質數。但這要試 `n` 次，太慢。\n\n:::example 根號枚舉：只試到 sqrt(n) 就能判斷質數\n這個技巧的關鍵性質是：**因數是成對出現的**。如果 `n = a × b`，那麼 `a` 和 `b` 一定有一個 `≤ √n`、另一個 `≥ √n`。換句話說，只要 `n` 有比較小的因數，這個小因數必定不超過 `√n`。所以我們只要試到 `√n` 就夠了，沒找到因數就代表是質數。\n\n```cpp\n// 根號枚舉：只試到 sqrt(n) 就能判斷質數\nbool isPrime(int n) {\n    if (n < 2) {\n        return false;  // 0、1 不是質數\n    }\n    for (int i = 2; (long long)i * i <= n; ++i) {  // i*i<=n 等價於 i<=sqrt(n)\n        if (n % i == 0) {\n            return false;  // 找到因數，不是質數\n        }\n    }\n    return true;  // 都沒整除，是質數\n}\n```\n:::\n\n\n\n注意 `i * i <= n` 這個寫法：用乘法比 `sqrt()` 更安全（避免浮點誤差），但 `i * i` 可能溢位，所以轉成 `long long`。這樣單次判斷只要 `O(√n)`。\n\n**實作方式**\n\n:::example 邊界：小於 2 一律不是質數\n**1. 處理邊界。** 先把 `n < 2` 擋掉（0 和 1 都不是質數），這是最常被漏掉的地方。\n\n```cpp\n// 邊界：小於 2 一律不是質數\nif (n < 2) {\n    return false;\n}\n```\n:::\n\n\n\n:::example 試除：找到任何因數就不是質數\n**2. 從 2 枚舉到 √n，逐一試除。** 只要任何一個 `i` 能整除 `n`，立刻回傳 false。\n\n```cpp\n// 試除：找到任何因數就不是質數\nfor (int i = 2; (long long)i * i <= n; ++i) {\n    if (n % i == 0) {\n        return false;\n    }\n}\n```\n:::\n\n\n\n**3. 撐到最後就是質數。** 迴圈跑完都沒被整除，代表只有 1 和自己能整除它。\n\n**4. 邊界與複雜度。**\n\n- 單次 `O(√n)`；若題目要對很多數字反覆判斷（例如掃整個陣列），總和是 `O(總個數 × √上限)`。\n- 如果要判斷的是「一個範圍內的每個數是否為質數」，就不該一個一個判，改用下一節的「篩法」一次預處理會快得多。\n- 用 `i * i <= n` 而非 `i <= sqrt(n)`，並小心 `i * i` 的溢位。\n\n**題目線索（Pattern）**\n\n看到這些訊號，就往「判斷質數」想：\n\n- **題目直接出現「質數」兩個字**，要你檢查某個數、或統計符合質數條件的數量。例如 3115「質數的最大距離」要先把陣列中哪些是質數標出來；2614「對角線上的質數」要逐格判斷對角線上的數是不是質數。\n- **數字本身要先經過某種轉換再判質數。** 例如 762「二進位表示中質數個計算置位」是先數出二進位中 1 的個數，再判斷那個個數是不是質數；3044「出現頻率最高的質數」要對矩陣中組出的數字逐一判質數。\n- **數值上限不大（通常 ≤ 約 10⁶）**，每個數用 `O(√n)` 判斷可以接受。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有先擋掉 `n < 2` 這個邊界？\n2. 我的迴圈是試到 `√n`（即 `i * i <= n`）還是不小心試到 `n`？\n3. 同一批數字要判很多次嗎？如果是，是不是該改用篩法？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 762 | 二進製表示中質數個計算置位 | N/A | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 866 | [迴文質數](https://leetcode.cn/problems/prime-palindrome/) | 1938 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2614 | [對角線上的質數](https://leetcode.cn/problems/prime-in-diagonal/) | 1375 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 3044 | [出現頻率最高的質數](https://leetcode.cn/problems/most-frequent-prime/) | 1737 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 3115 | [質數的最大距離](https://leetcode.cn/problems/maximum-prime-difference/) | 1294 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 762 | [二進製表示中質數個計算置位](https://leetcode.cn/problems/prime-number-of-set-bits-in-binary-representation) |  |\n| 866 | [迴文質數](https://leetcode.cn/problems/prime-palindrome) | 1938 |\n| 2614 | [對角線上的質數](https://leetcode.cn/problems/prime-in-diagonal) | 1375 |\n| 3044 | [出現頻率最高的質數](https://leetcode.cn/problems/most-frequent-prime) | 1737 |\n| 3115 | [質數的最大距離](https://leetcode.cn/problems/maximum-prime-difference) | 1294 |\n| 3556 | [最大質數子字串之和](https://leetcode.cn/problems/sum-of-largest-prime-substrings) | 1440 |\n| 3765 | [完全質數](https://leetcode.cn/problems/complete-prime-number) | 1378 |",
    },
    {
      id: 3,
      title: "2. 預處理質數（篩質數）",
      summary:
        "**原理講解**\n\n本節要解決的場景是：題目給一個範圍（例如 0 到 `n`），需要知道「這個範圍裡每個數是不是質數」，而且會反覆查很多次。\n\n如果對每個數都單獨用根號試除，總成本是 `O(n√n)`，當 `n` 很大時太慢。這個技巧改用「篩法（sieve）」：**一次把整個範圍內所有質數都標記出來，之後查任何一個數都是 `O(1)`。**\n\n:::example 埃氏篩：把每個質數的倍數標記為非質數\n核心想法叫「埃拉托斯特尼篩法（Eratosthenes）」，反過來想：與其問「每個數是不是質數」，不如**主動把每個質數的倍數全部劃掉**。從 2 開始，2 是質數，那 4、6、8…（2 的倍數）都不是質數；3 是質數，那 6、9、12…都不是質數。被劃掉的就是合數，沒被劃掉的就是質數。\n\n```cpp\n// 埃氏篩：把每個質數的倍數標記為非質數\nvector<bool> isPrime(n + 1, true);\nisPrime[0] = isPrime[1] = false;  // 0、1 不是質數\nfor (int i = 2; i <= n; ++i) {\n    if (isPrime[i]) {                        // i 還沒被劃掉 → 它是質數\n        for (int j = i * i; j <= n; j += i) {  // 從 i*i 開始劃它的倍數\n            isPrime[j] = false;\n        }\n    }\n}\n```\n:::\n\n\n\n為什麼內層從 `i * i` 而不是 `2 * i` 開始？因為比 `i * i` 小的 `i` 的倍數（如 `2i`、`3i`）早就被更小的質數（2、3）劃掉了，不必重劃。整體複雜度約 `O(n log log n)`，幾乎是線性，非常快。\n\n**實作方式**\n\n:::example 質數表：true 代表目前仍視為質數\n**1. 開一個布林陣列當「質數表」，預設全為質數。** 先把 0、1 標成非質數。\n\n```cpp\n// 質數表：true 代表目前仍視為質數\nvector<bool> isPrime(n + 1, true);\nisPrime[0] = isPrime[1] = false;\n```\n:::\n\n\n\n:::example 用每個質數 i 去劃它的倍數\n**2. 由小到大掃，遇到還活著的數就劃掉它的倍數。** 沒被劃掉代表它是質數，用它去劃。\n\n```cpp\n// 用每個質數 i 去劃它的倍數\nfor (int i = 2; (long long)i * i <= n; ++i) {\n    if (isPrime[i]) {\n        for (int j = i * i; j <= n; j += i) {\n            isPrime[j] = false;\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 收集所有質數，方便之後列舉\n**3. 查詢與後續使用。** 篩完後，`isPrime[x]` 直接 `O(1)` 回答；也可順手把質數收集成清單供後續使用。\n\n```cpp\n// 收集所有質數，方便之後列舉\nvector<int> primes;\nfor (int i = 2; i <= n; ++i) {\n    if (isPrime[i]) {\n        primes.push_back(i);\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 預處理 `O(n log log n)`，之後每次查詢 `O(1)`。\n- 內層用 `i * i` 起步、注意 `i * i` 溢位（必要時轉 `long long`）。\n- 空間 `O(n)`：範圍上限決定陣列大小，太大時要小心記憶體。\n\n**題目線索（Pattern）**\n\n看到這些特徵，就往「篩質數」想：\n\n- **需要在一個範圍內反覆判斷質數，或統計質數個數。** 例如 204「計數質數」要算小於 `n` 的質數有幾個，正是篩法的標準題；2523「範圍內最接近的兩個質數」要先把範圍內質數全找出來再比較。\n- **解題前需要一份「現成的質數清單」當素材。** 例如 2601「質數減法運算」、2761「和等於目標值的質數對」都需要先有範圍內所有質數，再去配對或選取。\n- **數值上限固定且不會太大**（陣列開得下），但查詢次數很多——一次篩、多次查最划算。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是「反覆查同一範圍」嗎？（若只判斷一個數，用上一節的根號試除即可。）\n2. 我的內層迴圈是從 `i * i` 開始劃倍數嗎？\n3. 我需要的是「布林表」還是「質數清單」，或兩者都要？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 204 | 計數質數 | N/A | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2523 | [範圍內最接近的兩個質數](https://leetcode.cn/problems/closest-prime-numbers-in-range/) | 1650 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2601 | [質數減法運算](https://leetcode.cn/problems/prime-subtraction-operation/) | 1779 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2761 | [和等於目標值的質數對](https://leetcode.cn/problems/prime-pairs-with-target-sum/) | 1505 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 3233 | [統計不是特殊數字的數字數量](https://leetcode.cn/problems/find-the-count-of-numbers-which-are-not-special/) | 1509 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 204 | [計數質數](https://leetcode.cn/problems/count-primes) |  |\n| 2523 | [範圍內最接近的兩個質數](https://leetcode.cn/problems/closest-prime-numbers-in-range) | 1650 |\n| 2601 | [質數減法運算](https://leetcode.cn/problems/prime-subtraction-operation) | 1779 |\n| 2761 | [和等於目標值的質數對](https://leetcode.cn/problems/prime-pairs-with-target-sum) | 1505 |\n| 3233 | [統計不是特殊數字的數字數量](https://leetcode.cn/problems/find-the-count-of-numbers-which-are-not-special) | 1509 |\n| 3591 | [檢查元素頻次是否為質數](https://leetcode.cn/problems/check-if-any-element-has-prime-frequency) | 1235 |\n| 3618 | [根據質數下標分割陣列](https://leetcode.cn/problems/split-array-by-prime-indices) | 1227 |\n| 3770 | [可表示為連續質數和的最大質數](https://leetcode.cn/problems/largest-prime-from-consecutive-prime-sum) | 1547 |",
    },
    {
      id: 4,
      title: "3. 質因數分解",
      summary:
        "**原理講解**\n\n本節要解決的是「質因數分解（prime factorization）」：把一個整數寫成質數相乘的形式。例如 `12 = 2 × 2 × 3`、`60 = 2² × 3 × 5`。算術基本定理保證：每個大於 1 的整數，這種分解方式是唯一的（不計順序）。很多數論題的核心，其實都是先把數字拆成它的質因數。\n\n:::example 質因數分解：從小到大除乾淨每個質因數\n做法和判斷質數一樣利用「因數成對」的性質：從小的質因數開始試除。**每找到一個因數 `i`，就把 `n` 裡所有的 `i` 都除乾淨**，這樣後面再遇到的因數一定也是質數（合數因數早就被它的小質因數除掉了）。同樣只需要試到 `√n`。\n\n```cpp\n// 質因數分解：從小到大除乾淨每個質因數\nvoid factorize(int n) {\n    for (int i = 2; (long long)i * i <= n; ++i) {\n        while (n % i == 0) {  // i 是質因數，全部除掉\n            // 這裡 i 就是一個質因數，可累計次數\n            n /= i;\n        }\n    }\n    if (n > 1) {  // 剩下的 n 本身是個大質因數\n                                // n 也是一個質因數\n    }\n}\n```\n:::\n\n\n\n最後那個 `if (n > 1)` 很關鍵：迴圈只試到 `√n`，所以如果 `n` 含有一個大於 `√n` 的質因數（例如 14 = 2 × 7，7 > √14），它會剩在 `n` 裡，要記得補上。單次分解是 `O(√n)`。\n\n**實作方式**\n\n:::example 狀態：質因數 -> 次數（指數）\n**1. 狀態表示：用一個結構記錄每個質因數的次數。** 通常是 `map` 或 `vector<pair>`，存「質因數 → 出現幾次」。\n\n```cpp\n// 狀態：質因數 -> 次數（指數）\nmap<int, int> factors;  // 例如 60 → {2:2, 3:1, 5:1}\n```\n:::\n\n\n\n:::example 逐個質因數除乾淨，邊除邊計數\n**2. 從 2 試到 √n，把每個質因數除乾淨並累計次數。**\n\n```cpp\n// 逐個質因數除乾淨，邊除邊計數\nfor (int i = 2; (long long)i * i <= n; ++i) {\n    while (n % i == 0) {\n        factors[i]++;  // 記一次因數 i\n        n /= i;\n    }\n}\n```\n:::\n\n\n\n:::example 別忘了最後殘留的大質因數\n**3. 補上殘留的大質因數。** 迴圈結束後若 `n > 1`，它就是一個超過 `√n` 的質因數。\n\n```cpp\n// 別忘了最後殘留的大質因數\nif (n > 1) {\n    factors[n]++;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 單次 `O(√n)`；若要對很多數分解，可先用篩法預處理「每個數的最小質因數（SPF）」，把單次分解降到 `O(log n)`。\n- 大數相乘要防溢位，必要時用 `long long`。\n- `n = 1` 時沒有質因數，注意這個空集合的情況。\n\n**題目線索（Pattern）**\n\n看到這些特徵，就往「質因數分解」想：\n\n- **題目和「乘積、因數結構、整除」深度相關，需要看清一個數由哪些質數組成。** 例如 1735「生成乘積陣列的方案數」要把目標乘積分解成質因數再做組合計數；2338「統計理想陣列的數目」也以質因數的指數分配為核心。\n- **要重複把一個數變小或替換，每步依賴它的質因數。** 例如 2507「使用質因數之和替換後可以取到的最小值」每一步都要先分解再求和。\n- **牽涉 gcd / 整除分組、或把數字按公因數歸類**，如 1998「陣列的最大公因數排序」常需要從質因數角度理解連通關係。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要的是「有哪些質因數」還是「每個質因數的次數」？\n2. 我有沒有記得補上迴圈結束後殘留的大質因數（`n > 1`）？\n3. 要分解的數很多嗎？如果是，是否該預處理最小質因數表？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 14 | 切分陣列 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1735 | [生成乘積陣列的方案數](https://leetcode.cn/problems/count-ways-to-make-array-with-product/) | 2500 | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 1998 | [陣列的最大公因數排序](https://leetcode.cn/problems/gcd-sort-of-an-array/) | 2429 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2338 | [統計理想陣列的數目](https://leetcode.cn/problems/count-the-number-of-ideal-arrays/) | 2615 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2507 | [使用質因數之和替換後可以取到的最小值](https://leetcode.cn/problems/smallest-value-after-replacing-with-sum-of-prime-factors/) | 1500 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 14 | [切分陣列](https://leetcode.cn/problems/qie-fen-shu-zu) |  |\n| 1735 | [生成乘積陣列的方案數](https://leetcode.cn/problems/count-ways-to-make-array-with-product) | 2500 |\n| 1998 | [陣列的最大公因數排序](https://leetcode.cn/problems/gcd-sort-of-an-array) | 2429 |\n| 2338 | [統計理想陣列的數目](https://leetcode.cn/problems/count-the-number-of-ideal-arrays) | 2615 |\n| 2507 | [使用質因數之和替換後可以取到的最小值](https://leetcode.cn/problems/smallest-value-after-replacing-with-sum-of-prime-factors) | 1500 |\n| 2521 | [陣列乘積中的不同質因數數目](https://leetcode.cn/problems/distinct-prime-factors-of-product-of-array) | 1413 |\n| 2584 | [分割陣列使乘積互質](https://leetcode.cn/problems/split-the-array-to-make-coprime-products) | 2159 |\n| 2709 | [最大公約數遍歷](https://leetcode.cn/problems/greatest-common-divisor-traversal) | 2172 |\n| 2818 | [操作使得分最大](https://leetcode.cn/problems/apply-operations-to-maximize-score) | 2397 |\n| 2862 | [完全子集的最大元素和](https://leetcode.cn/problems/maximum-element-sum-of-a-complete-subset-of-indices) | 2292 |\n| 3326 | [使陣列非遞減的最少除法操作次數](https://leetcode.cn/problems/minimum-division-operations-to-make-array-non-decreasing) | 1864 |\n| 3629 | [透過質數傳送到達終點的最少跳躍次數](https://leetcode.cn/problems/minimum-jumps-to-reach-end-via-prime-teleportation) | 2139 |\n| 3715 | [完全平方數的祖先個數總和](https://leetcode.cn/problems/sum-of-perfect-square-ancestors) | 2235 |",
    },
    {
      id: 5,
      title: "4. 階乘分解",
      summary:
        "**原理講解**\n\n本節要解決的是「階乘分解」：`n!` 表示 `1 × 2 × 3 × … × n`，它的數值會大到爆炸（`20!` 就快接近 long long 上限），所以我們幾乎不直接算出 `n!`，而是分析它的「質因數結構」——特別是某個質數 `p` 在 `n!` 裡出現幾次。\n\n最經典的應用是「`n!` 末尾有幾個零」。一個末尾零來自一個 `10 = 2 × 5`，而在 `1` 到 `n` 的乘積裡，因數 2 永遠比 5 多，所以**末尾零的個數 = `n!` 裡質因數 5 的個數**。\n\n:::example Legendre 公式：質數 p 在 n! 中出現的次數\n關鍵公式叫 Legendre 公式：質數 `p` 在 `n!` 中的次數是\n\n```cpp\n// Legendre 公式：質數 p 在 n! 中出現的次數\nint legendre(int n, int p) {\n    int cnt = 0;\n    while (n > 0) {\n        n /= p;    // 每次把 n 整除 p\n        cnt += n;  // 累加「是 p 的倍數、p^2 的倍數…」的個數\n    }\n    return cnt;\n}\n```\n:::\n\n\n\n直覺：1~n 裡是 5 的倍數有 `n/5` 個（各貢獻一個 5），是 25 的倍數的再多貢獻一個（`n/25`），是 125 的倍數的再多一個…全加起來。這樣不用算出 `n!`，只要 `O(log_p n)` 就得到答案。\n\n**實作方式**\n\n:::example 末尾零個數 = n! 中質因數 5 的個數\n**1. 確定要數的是哪個質數。** 末尾零問題只需數 5（因為 2 一定更多）。\n\n```cpp\n// 末尾零個數 = n! 中質因數 5 的個數\nint trailingZeroes(int n) {\n    int cnt = 0;\n    for (long long pw = 5; pw <= n; pw *= 5) {\n        cnt += n / pw;  // 加上 5、25、125… 的倍數個數\n    }\n    return cnt;\n}\n```\n:::\n\n\n\n:::example 零的個數對 n 單調 → 可用二分找門檻\n**2. 反向問題用二分搜尋。** 「末尾恰好有 K 個零的 `n!` 存在嗎 / 有幾個 `n`」這類問題，因為零的個數隨 `n` 單調不減，可對 `n` 二分。\n\n```cpp\n// 零的個數對 n 單調 → 可用二分找門檻\n// 計算 f(n)=末尾零個數，再二分找 f(n)>=K 的最小 n\n```\n:::\n\n\n\n**3. 注意「跳號」現象。** 末尾零個數每跨過一個 5 的倍數就一次跳 1（甚至跳更多，如跨過 25 會一次多 2），所以「恰好 K 個零」可能不存在，答案會是 0 或固定的 5。\n\n**4. 邊界與複雜度。**\n\n- 數零個數 `O(log₅ n)`；配二分搜尋是 `O(log n × log₅ n)`。\n- 用 `long long` 累乘 `pw`（5、25、125…）以免溢位。\n- `n = 0` 時 `0!= 1`，末尾零為 0。\n\n**題目線索（Pattern）**\n\n看到這些特徵，就往「階乘分解」想：\n\n- **出現 `n!`、又問「末尾零」「能被某數整除幾次」「某質因數的次數」。** 例如 172「階乘後的零」就是直接數 `n!` 裡 5 的個數。\n- **反過來給定零的個數，問符合的 `n`。** 例如 793「階乘函式後 K 個零」問有幾個 `n` 使 `n!` 末尾恰好 K 個零——關鍵是發現答案只會是 0 或 5，搭配二分。\n- **數值大到不可能直接算階乘**，必須改成分析質因數的次數。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我真正要數的是哪個質數在 `n!` 裡的次數（末尾零就是數 5）？\n2. 我有用 Legendre 公式「除 p、除 p²…累加」而不是硬算 `n!` 嗎？\n3. 這是正向（給 n 求個數）還是反向（給個數求 n）問題？反向是否要二分？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 172 | 階乘後的零 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 793 | 階乘函式後 K 個零 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 172 | [階乘後的零](https://leetcode.cn/problems/factorial-trailing-zeroes) |  |\n| 793 | [階乘函式後 K 個零](https://leetcode.cn/problems/preimage-size-of-factorial-zeroes-function) |  |",
    },
    {
      id: 6,
      title: "5. 因子",
      summary:
        "**原理講解**\n\n本節要解決的是「找出一個數的所有因數（divisors）」：能整除 `n` 的所有正整數。例如 12 的因數是 1、2、3、4、6、12。很多題目需要列舉因數、數因數個數、或判斷因數之間的關係（例如完美數）。\n\n:::example 根號枚舉因數：找到 i 的同時取得搭檔 n/i\n最笨的做法是從 1 試到 `n`，逐個檢查能不能整除，要 `O(n)`。這個技巧同樣利用「**因數成對**」：若 `i` 是 `n` 的因數，那 `n / i` 也是因數，而且這兩個一個 `≤ √n`、一個 `≥ √n`。所以只要枚舉到 `√n`，每找到一個小因數 `i`，就同時得到它的搭檔 `n / i`，把兩個都收下。\n\n```cpp\n// 根號枚舉因數：找到 i 的同時取得搭檔 n/i\nvector<int> divisors(int n) {\n    vector<int> res;\n    for (int i = 1; (long long)i * i <= n; ++i) {\n        if (n % i == 0) {\n            res.push_back(i);        // 小因數\n            if (i != n / i) {        // 避免完全平方數重複（如 36 的 6）\n                res.push_back(n / i);  // 搭檔大因數\n            }\n        }\n    }\n    return res;\n}\n```\n:::\n\n\n\n注意 `if (i != n / i)`：當 `n` 是完全平方數（如 36 = 6×6），`i` 和 `n/i` 會相等，只能收一次。這樣列舉因數從 `O(n)` 降到 `O(√n)`。\n\n**實作方式**\n\n:::example 成對收集，注意平方根去重\n**1. 從 1 枚舉到 √n，成對收因數。** 每個能整除的 `i` 帶來 `i` 和 `n / i` 兩個因數。\n\n```cpp\n// 成對收集，注意平方根去重\nfor (int i = 1; (long long)i * i <= n; ++i) {\n    if (n % i == 0) {\n        small.push_back(i);\n        if (i != n / i) {\n            large.push_back(n / i);\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 真因數和（不含 n 自己），用於判斷完美數\n**2. 視需要只求「個數」或「總和」，不必存清單。** 例如完美數要的是「真因數之和」，邊枚舉邊累加即可。\n\n```cpp\n// 真因數和（不含 n 自己），用於判斷完美數\nlong long sum = 1;  // 1 一定是真因數（n>1 時）\nfor (int i = 2; (long long)i * i <= n; ++i) {\n    if (n % i == 0) {\n        sum += i;\n        if (i != n / i) {\n            sum += n / i;\n        }\n    }\n}\n// 完美數：sum == n\n```\n:::\n\n\n\n**3. 若要對「很多數」一起算因數，改用篩法思路。** 讓每個 `d` 去加到它所有倍數上（`d, 2d, 3d…`），一次預處理全部數的因數和 / 因數個數，總成本 `O(n log n)`。\n\n**4. 邊界與複雜度。**\n\n- 單個數列舉因數 `O(√n)`；完全平方數記得去重。\n- 求和容易溢位，用 `long long`。\n- `n = 1` 的因數只有 1，注意這個特例。\n\n**題目線索（Pattern）**\n\n看到這些特徵，就往「因子」想：\n\n- **題目要列舉、計數或加總一個數的因數。** 例如 507「完美數」要判斷真因數之和是否等於自己；1362「最接近的因數」要找一對因數使乘積等於 `n` 或 `n + 1` 且兩數最接近——正是利用 `√n` 附近的因數對。\n- **把問題拆成「找一組因數 / 約數」。** 例如 829「連續整數求和」可轉化成對 `2n` 的因數做枚舉；625「最小因式分解」也要從因數角度組數字。\n- **大量數字依公因數分組或連通。** 例如 952「按公因數計算最大元件大小」需對每個數列舉因數，再以因數為橋把數字併在一起。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要的是「所有因數」、「因數個數」還是「因數和」？\n2. 我有沒有處理完全平方數時 `i == n/i` 的重複？\n3. 是單一個數，還是一大批數？批量是否該用篩法式預處理？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 507 | 完美數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 625 | 最小因式分解（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 829 | [連續整數求和](https://leetcode.cn/problems/consecutive-numbers-sum/) | 1694 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 952 | [按公因數計算最大元件大小](https://leetcode.cn/problems/largest-component-size-by-common-factor/) | 2272 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 1362 | [最接近的因數](https://leetcode.cn/problems/closest-divisors/) | 1534 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 507 | [完美數](https://leetcode.cn/problems/perfect-number) |  |\n| 625 | [最小因式分解](https://leetcode.cn/problems/minimum-factorization) |  |\n| 829 | [連續整數求和](https://leetcode.cn/problems/consecutive-numbers-sum) | 1694 |\n| 952 | [按公因數計算最大元件大小](https://leetcode.cn/problems/largest-component-size-by-common-factor) | 2272 |\n| 1362 | [最接近的因數](https://leetcode.cn/problems/closest-divisors) | 1534 |\n| 1390 | [四因數](https://leetcode.cn/problems/four-divisors) | 1478 |\n| 1492 | [n 的第 k 個因子](https://leetcode.cn/problems/the-kth-factor-of-n) | 1232 |\n| 1627 | [帶閾值的圖連通性](https://leetcode.cn/problems/graph-connectivity-with-threshold) | 2221 |\n| 1952 | [三除數](https://leetcode.cn/problems/three-divisors) | 1204 |\n| 2176 | [統計陣列中相等且可以被整除的數對](https://leetcode.cn/problems/count-equal-and-divisible-pairs-in-an-array) | 1216 |\n| 2183 | [統計可以被 K 整除的下標對數目](https://leetcode.cn/problems/count-array-pairs-divisible-by-k) | 2246 |\n| 2198 | [單因數三元組](https://leetcode.cn/problems/number-of-single-divisor-triplets) |  |\n| 2427 | [公因子的數目](https://leetcode.cn/problems/number-of-common-factors) | 1172 |\n| 2847 | [給定數字乘積的最小數字](https://leetcode.cn/problems/smallest-number-with-given-digit-product) |  |\n| 3164 | [優質數對的總數 II](https://leetcode.cn/problems/find-the-number-of-good-pairs-ii) | 1777 |\n| 3447 | [將元素分配給有約束條件的組](https://leetcode.cn/problems/assign-elements-to-groups-with-constraints) | 1731 |",
    },
    {
      id: 7,
      title: "6. 最大公約數（GCD）",
      summary:
        "**原理講解**\n\n本節要解決的是「最大公約數（GCD, greatest common divisor）」：兩個整數共同的因數中最大的那個。例如 `gcd(12, 18) = 6`。GCD 是數論的基礎工具，很多看似無關的題目（分組、約分、判斷可達性）底層都是它。\n\n求 GCD 不必把兩數的因數全列出來比對，而用「歐幾里得演算法（輾轉相除法）」，它基於一條漂亮的性質：\n\n> `gcd(a, b) = gcd(b, a mod b)`\n\n:::example 歐幾里得演算法：輾轉相除求 GCD\n直覺是：`a` 和 `b` 的公因數，和 `b` 與「`a` 除以 `b` 的餘數」的公因數完全一樣。於是把大的數不斷對小的取餘數，數字快速變小，餘數變 0 時，另一個就是答案。\n\n```cpp\n// 歐幾里得演算法：輾轉相除求 GCD\nint gcd(int a, int b) {\n    while (b != 0) {\n        int r = a % b;  // 取餘數\n        a = b;\n        b = r;  // 用 (b, a mod b) 取代 (a, b)\n    }\n    return a;  // b 變 0 時，a 就是最大公約數\n}\n```\n:::\n\n\n\n這個過程非常快，是 `O(log(min(a, b)))`。C++ 也內建 `std::__gcd(a, b)` 或 C++17 的 `std::gcd`。另外，「最小公倍數」可由 GCD 求得：`lcm(a, b) = a / gcd(a, b) * b`（先除再乘，避免溢位）。\n\n**實作方式**\n\n:::example 遞迴版：更精簡\n**1. 直接用歐幾里得演算法求兩數 GCD。** 記住遞迴版更短。\n\n```cpp\n// 遞迴版：更精簡\nint gcd(int a, int b) { return b == 0 ? a : gcd(b, a % b); }\n```\n:::\n\n\n\n:::example 整個陣列的 GCD\n**2. 多個數的 GCD：逐個累積。** 整個陣列的 GCD 可以一個一個併進去。\n\n```cpp\n// 整個陣列的 GCD\nint g = a[0];\nfor (int x : a) {\n    g = gcd(g, x);  // 一旦 g==1 可提早結束\n}\n```\n:::\n\n\n\n:::example 最小公倍數：先除以 gcd 再乘\n**3. 需要 LCM 時先除後乘。** 防止 `a * b` 先乘起來溢位。\n\n```cpp\n// 最小公倍數：先除以 gcd 再乘\nlong long lcm(long long a, long long b) { return a / gcd(a, b) * b; }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `gcd(a, 0) = a`；`gcd(0, 0)` 通常定義為 0，注意題目語意。\n- 求 LCM 易溢位，用 `long long` 且「先除後乘」。\n- 單次 GCD `O(log(min(a, b)))`；整個陣列 `O(n log(max))`。\n\n**題目線索（Pattern）**\n\n看到這些特徵，就往「GCD」想：\n\n- **題目談到「共同的倍數 / 約數」「能否整除全部」「約分」。** 例如 1071「字串的最大公因子」要找能同時整除兩字串的最長重複單元，本質是對長度取 GCD；914「卡牌分組」要看所有牌數的 GCD 是否 ≥ 2。\n- **判斷「能否用若干個數湊出某值」。** 例如 1250「檢查好陣列」用到一條定理：一組數能湊出 1（互質）⟺ 它們的 GCD 為 1。\n- **幾何/比例中要把斜率、向量約成最簡形式。** 例如 149「直線上最多的點數」用 GCD 把 `(dx, dy)` 約分當作斜率的鍵；365「水壺問題」也用 GCD 判斷可量出的容量。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是兩數、還是一整組數的 GCD？\n2. 有沒有把問題轉成「GCD 是否為某值（如 1）」這種判定？\n3. 若需要 LCM，我有沒有「先除後乘」防溢位？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 149 | 直線上最多的點數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 365 | 水壺問題 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 914 | [卡牌分組](https://leetcode.cn/problems/x-of-a-kind-in-a-deck-of-cards/) | 1371 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1071 | [字串的最大公因子](https://leetcode.cn/problems/greatest-common-divisor-of-strings/) | 1398 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1250 | [檢查「好陣列」](https://leetcode.cn/problems/check-if-it-is-a-good-array/) | 1983 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 149 | [直線上最多的點數](https://leetcode.cn/problems/max-points-on-a-line) |  |\n| 365 | [水壺問題](https://leetcode.cn/problems/water-and-jug-problem) |  |\n| 914 | [卡牌分組](https://leetcode.cn/problems/x-of-a-kind-in-a-deck-of-cards) | 1371 |\n| 1071 | [字串的最大公因子](https://leetcode.cn/problems/greatest-common-divisor-of-strings) | 1398 |\n| 1250 | [檢查「好陣列」](https://leetcode.cn/problems/check-if-it-is-a-good-array) | 1983 |\n| 1625 | [執行操作後字典序最小的字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-applying-operations) | 1992 |\n| 1819 | [序列中不同最大公約數的數目](https://leetcode.cn/problems/number-of-different-subsequences-gcds) | 2540 |\n| 1979 | [找出陣列的最大公約數](https://leetcode.cn/problems/find-greatest-common-divisor-of-array) | 1184 |\n| 2169 | [得到 0 的運算元](https://leetcode.cn/problems/count-operations-to-obtain-zero) | 1200 |\n| 2344 | [使陣列可以被整除的最少刪除次數](https://leetcode.cn/problems/minimum-deletions-to-make-array-divisible) | 1641 |\n| 2436 | [使子陣列最大公約數大於一的最小分割數](https://leetcode.cn/problems/minimum-split-into-subarrays-with-gcd-greater-than-one) |  |\n| 2447 | [最大公因數等於 K 的子陣列數目](https://leetcode.cn/problems/number-of-subarrays-with-gcd-equal-to-k) | 1603 |\n| 2464 | [有效分割中的最少子陣列數目](https://leetcode.cn/problems/minimum-subarrays-in-a-valid-split) |  |\n| 2543 | [判斷一個點是否可以到達](https://leetcode.cn/problems/check-if-point-is-reachable) | 2221 |\n| 2607 | [使子陣列元素和相等](https://leetcode.cn/problems/make-k-subarray-sums-equal) | 2071 |\n| 2654 | [使陣列所有元素變成 1 的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-all-array-elements-equal-to-1) | 1929 |\n| 2941 | [子陣列的最大 GCD-Sum](https://leetcode.cn/problems/maximum-gcd-sum-of-a-subarray) |  |\n| 3312 | [查詢排序後的最大公約數](https://leetcode.cn/problems/sorted-gcd-pair-queries) | 2533 |\n| 3574 | [最大子陣列 GCD 分數](https://leetcode.cn/problems/maximize-subarray-gcd-score) | 2258 |\n| 3658 | [奇數和與偶數和的最大公約數](https://leetcode.cn/problems/gcd-of-odd-and-even-sums) | 1220 |\n| 3671 | [子序列美麗值求和](https://leetcode.cn/problems/sum-of-beautiful-subsequences) | 2647 |\n| 3725 | [統計每一行選擇互質整數的方案數](https://leetcode.cn/problems/count-ways-to-choose-coprime-integers-from-rows) | 1982 |",
    },
    {
      id: 8,
      title: "7. 最大公約數（GCD）擴展",
      summary:
        "**原理講解**\n\n本節在上一節「最大公約數」的基礎上再進一步。普通 GCD 只回答「最大公約數是多少」；而「擴展歐幾里得演算法（extended GCD）」多回答一件事：**找出整數 `x`、`y`，使得 `a·x + b·y = gcd(a, b)`。** 這條等式叫貝祖等式（Bézout's identity），是解「同餘方程」「求模逆元」「判斷不定方程有沒有整數解」的鑰匙。\n\n更基礎的層面是：很多題目其實只需要 GCD 的「性質」，而不需要解 `x, y`。最常用的性質是：**`a·x + b·y` 能取到的所有正整數，恰好是 `gcd(a, b)` 的倍數。** 例如兩個水壺 3L 與 5L，`gcd = 1`，所以理論上能量出任何整數公升數。\n\n:::example 連結串列 / 陣列相鄰元素取 GCD（2807 的核心）\n先看普通需求——對「一串數」連續取 GCD：\n\n```cpp\n// 連結串列 / 陣列相鄰元素取 GCD（2807 的核心）\nint g = gcd(a, b);  // 在每對相鄰節點之間插入它們的 GCD\n```\n:::\n\n\n\n:::example 擴展歐幾里得：求出 x, y 使 a*x + b*y = gcd(a, b)\n再看擴展版本，它在求 GCD 的同時把係數一路回推回來：\n\n```cpp\n// 擴展歐幾里得：求出 x, y 使 a*x + b*y = gcd(a, b)\nint extgcd(int a, int b, int& x, int& y) {\n    if (b == 0) {\n        x = 1;\n        y = 0;\n        return a;\n    }  // 邊界：gcd=a, 1*a+0*b=a\n    int x1, y1;\n    int g = extgcd(b, a % b, x1, y1);  // 先解較小的子問題\n    x = y1;\n    y = x1 - (a / b) * y1;  // 由子問題回推當前 x, y\n    return g;\n}\n```\n:::\n\n\n\n它和普通歐幾里得一樣是 `O(log(min(a, b)))`，只是多帶回了係數。\n\n**實作方式**\n\n:::example 相鄰兩數插入其 GCD（鏈結串列遍歷）\n**1. 只要 GCD 本身時，用基本版即可。** 大多數 LeetCode 題（包含本節例題）其實停在這一步。\n\n```cpp\n// 相鄰兩數插入其 GCD（鏈結串列遍歷）\nwhile (cur && cur->next) {\n    int g = gcd(cur->val, cur->next->val);\n    cur->next = new ListNode(g, cur->next);  // 插入 GCD 節點\n    cur = cur->next->next;                   // 跳過剛插入的節點\n}\n```\n:::\n\n\n\n:::example 模逆元：a 在 mod m 下的逆元（需 gcd(a,m)=1）\n**2. 需要係數 / 模逆元時，用擴展版。** 解 `a·x ≡ 1 (mod m)` 時，對 `extgcd(a, m, x, y)` 求出的 `x`（再對 `m` 取正）就是逆元。\n\n```cpp\n// 模逆元：a 在 mod m 下的逆元（需 gcd(a,m)=1）\nint x, y;\nextgcd(a, m, x, y);\nint inv = (x % m + m) % m;  // 調成 [0, m) 範圍\n```\n:::\n\n\n\n**3. 判斷不定方程是否有解。** `a·x + b·y = c` 有整數解 ⟺ `c` 是 `gcd(a, b)` 的倍數。\n\n**4. 邊界與複雜度。**\n\n- 回推 `x, y` 時注意 `a / b` 是整數除法；數值大時用 `long long` 防溢位。\n- 模逆元只在 `gcd(a, m) = 1` 時存在，先檢查。\n- 複雜度同歐幾里得 `O(log(min(a, b)))`。\n\n**題目線索（Pattern）**\n\n看到這些特徵，就往「GCD 擴展」想：\n\n- **需要對序列「相鄰元素插入 / 計算 GCD」。** 例如 2807「在連結串列中插入最大公約數」就是逐對相鄰節點插入其 GCD，屬於基本 GCD 的直接應用。\n- **要解同餘方程、求模逆元、組合數取模的除法。** 這時普通 GCD 不夠，得用擴展版求係數。\n- **判斷「能不能用 `a` 與 `b` 的整數組合湊出 `c`」。** 用貝祖性質：看 `c` 是否為 `gcd(a, b)` 的倍數。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我只需要 GCD 的「值」，還是需要係數 `x, y`？\n2. 如果是求逆元，我有先確認 `gcd(a, m) = 1` 嗎？\n3. 我的目標值是不是「`gcd` 的倍數」才有解？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2807 | [在連結串列中插入最大公約數](https://leetcode.cn/problems/insert-greatest-common-divisors-in-linked-list/) | 1279 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2807 | [在連結串列中插入最大公約數](https://leetcode.cn/problems/insert-greatest-common-divisors-in-linked-list) | 1279 |",
    },
    {
      id: 9,
      title: "8. 最小公倍數（LCM）",
      summary:
        "**原理講解**\n\n本節要解決的是「最小公倍數（LCM）」這類問題。先把名詞講清楚：兩個正整數 a 和 b 的「公倍數」是同時被 a、b 整除的數（例如 4 和 6 的公倍數有 12、24、36…），其中最小的那個就是 LCM（這裡是 12）。\n\n:::example LCM 與 GCD 的關係：a 與 b 的乘積 = gcd × lcm\n直覺上你可能會從 a、b 之間較大者開始一個一個往上試，看哪個同時被兩者整除。但這太慢。真正的關鍵是一條公式，它把 LCM 和「最大公因數（GCD，兩數共同的最大因數）」連在一起：\n\n```cpp\n// LCM 與 GCD 的關係：a 與 b 的乘積 = gcd × lcm\n// 所以 lcm(a, b) = a / gcd(a, b) * b   （先除再乘，避免溢位）\n```\n:::\n\n\n\n:::example 歐幾里得演算法求 GCD：不斷用「餘數」取代較大數\n為什麼成立？把 a、b 做質因數分解，GCD 取「每個質數的較小次方」，LCM 取「較大次方」，兩者相乘剛好補回 a×b 的所有次方。所以只要會算 GCD，就會算 LCM。而 GCD 用輾轉相除法（歐幾里得演算法）非常快，是 `O(log(min(a, b)))`：\n\n```cpp\n// 歐幾里得演算法求 GCD：不斷用「餘數」取代較大數\nlong long gcd(long long a, long long b) {\n    while (b) {\n        a %= b;\n        swap(a, b);\n    }\n    return a;  // b 變 0 時，a 就是最大公因數\n}\n```\n:::\n\n\n\nC++17 起 `std::gcd` 與 `std::lcm`（在 `<numeric>`）已內建，可直接使用，但理解上面這條原理才能在變形題裡靈活運用。\n\n**實作方式**\n\n:::example 由 gcd 推出 lcm：務必「先除後乘」防止中途溢位\n**1. 先寫好 GCD，再由它推出 LCM。** 這是所有 LCM 題的地基。\n\n```cpp\n// 由 gcd 推出 lcm：務必「先除後乘」防止中途溢位\nlong long lcm(long long a, long long b) { return a / gcd(a, b) * b; }\n```\n:::\n\n\n\n:::example 整個陣列的 lcm：用前綴結果不斷併入下一個數\n**2. 多個數的 LCM：兩兩滾動合併。** LCM 有結合律，把整個陣列 `reduce` 起來即可。\n\n```cpp\n// 整個陣列的 lcm：用前綴結果不斷併入下一個數\nlong long ans = nums[0];\nfor (int i = 1; i < n; ++i) {\n    ans = lcm(ans, nums[i]);\n}\n```\n:::\n\n\n\n:::example 子陣列 lcm 一旦超過目標 k，再加元素也不可能等於 k，提前停\n**3. 警惕溢位與「LCM 爆炸」。** 多個數的 LCM 會成長極快。像「最小公倍數為 K 的子陣列數目」(2470) 中，一旦滾動 LCM 超過 K 就不可能再等於 K，可提早剪枝；用 `long long`，並隨時和題目上界比較。\n\n```cpp\n// 子陣列 lcm 一旦超過目標 k，再加元素也不可能等於 k，提前停\nif (cur > k) {\n    break;  // cur 是目前子陣列的 lcm\n}\n```\n:::\n\n\n\n**4. 有時答案是公式而非直接算 LCM。** 像「最小偶倍數」(2413) 其實就是 `lcm(2, n)`：n 是偶數答案就是 n，否則 2n。先把題意翻成 GCD/LCM 公式，往往一兩行就解決。複雜度方面，單次 GCD/LCM 是 `O(log V)`（V 為數值大小），掃陣列合計 `O(n log V)`。\n\n**題目線索（Pattern）**\n\n看到以下訊號，就往「GCD / LCM」想：\n\n- **題目出現「同時被…整除」「共同週期」「最小的公倍數 / 倍數」字眼。** 例如「最小偶倍數」「最小公倍數為 K 的子陣列」，名稱直接點名。\n- **兩個（或多個）量要「對齊」「同時回到原點」。** 像「鏡面反射」(1881) 中光線在房間反彈，何時打到角落，本質是邊長的 LCM 對齊問題。\n- **需要約分、判斷互質、合併因數。** 「替換陣列中的非互質數」(2197) 反覆把相鄰非互質的數合併成它們的 LCM，核心就是 GCD/LCM 運算。\n- **暴力逐一枚舉倍數會超時**，而用 `lcm = a / gcd * b` 一步到位。\n\n確認自己套對，問三個問題：\n\n1. 我要的是「共同的最小倍數」還是「共同的最大因數」？（前者 LCM、後者 GCD）\n2. 數值會不會在連乘後溢位？我有沒有用 `long long` 並先除後乘？\n3. 答案能不能直接寫成一條 GCD/LCM 公式，省去枚舉？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 858 | [鏡面反射](https://leetcode.cn/problems/mirror-reflection/) | 1881 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2197 | [替換陣列中的非互質數](https://leetcode.cn/problems/replace-non-coprime-numbers-in-array/) | 2057 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2413 | [最小偶倍數](https://leetcode.cn/problems/smallest-even-multiple/) | 1145 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2470 | [最小公倍數為 K 的子陣列數目](https://leetcode.cn/problems/number-of-subarrays-with-lcm-equal-to-k/) | 1560 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3334 | [陣列的最大因子得分](https://leetcode.cn/problems/find-the-maximum-factor-score-of-array/) | 1519 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 858 | [鏡面反射](https://leetcode.cn/problems/mirror-reflection) | 1881 |\n| 2197 | [替換陣列中的非互質數](https://leetcode.cn/problems/replace-non-coprime-numbers-in-array) | 2057 |\n| 2413 | [最小偶倍數](https://leetcode.cn/problems/smallest-even-multiple) | 1145 |\n| 2470 | [最小公倍數為 K 的子陣列數目](https://leetcode.cn/problems/number-of-subarrays-with-lcm-equal-to-k) | 1560 |\n| 3334 | [陣列的最大因子得分](https://leetcode.cn/problems/find-the-maximum-factor-score-of-array) | 1519 |",
    },
    {
      id: 10,
      title: "9. 互質",
      summary:
        "**原理講解**\n\n本節的主題是「互質（coprime）」。先定義：兩個正整數如果除了 1 以外沒有共同因數，就稱為互質，也就是它們的最大公因數（GCD，兩數共同的最大因數）等於 1。例如 8 和 15 互質（沒有共同因數），但 8 和 12 不互質（共同有因數 4）。\n\n:::example 判斷 a、b 是否互質：gcd 等於 1 就互質\n所以判斷互質的核心，就是算 GCD 再看是不是 1：\n\n```cpp\n// 判斷 a、b 是否互質：gcd 等於 1 就互質\nbool coprime(int a, int b) {\n    return std::gcd(a, b) == 1;  // <numeric> 內建，O(log min(a,b))\n}\n```\n:::\n\n\n\n`std::gcd` 用的是歐幾里得演算法（輾轉相除），每一步用餘數取代較大數，速度是 `O(log(min(a, b)))`，非常快。\n\n:::example 值域小的關鍵觀察：相同的值互質性也相同，可按值統計而非按位置\n這個技巧難的不是「判斷一對數互質」，而是**當題目要你統計「有多少對互質」時，直接兩兩枚舉是 `O(n²)`，常常會超時**。這時要善用一個事實：題目裡的數值範圍通常不大（例如 ≤ 50 或 ≤ 100）。值域小，就可以用「按值分桶」或「預處理因數」把成對判斷壓下來：\n\n```cpp\n// 值域小的關鍵觀察：相同的值互質性也相同，可按值統計而非按位置\n// cnt[v] = 值 v 出現幾次；只需對「不同的值」兩兩判斷，再乘上次數\n```\n:::\n\n\n\n也就是說，重複的值只算一次互質判斷，再用出現次數做乘法，這就把 `O(n²)` 降成 `O(V² log V)`（V 是值域大小），當 V 遠小於 n 時大幅加速。\n\n**實作方式**\n\n:::example 互質判斷\n**1. 基本判斷：一行 GCD。** 所有互質題的底層都是它。\n\n```cpp\n// 互質判斷\nbool ok = std::gcd(x, y) == 1;\n```\n:::\n\n\n\n:::example 值域小時統計互質對：對每對不同值判斷一次，乘上出現次數\n**2. 統計互質對：按值域分桶，避免逐對枚舉。** 先數每個值出現幾次，再對「不同值的配對」做判斷。\n\n```cpp\n// 值域小時統計互質對：對每對不同值判斷一次，乘上出現次數\nlong long ans = 0;\nfor (int a = 1; a <= V; ++a) {\n    for (int b = a; b <= V; ++b) {\n        if (!cnt[a] || !cnt[b] || std::gcd(a, b) != 1) {\n            continue;\n        }\n        ans += (a == b) ? 1LL * cnt[a] * (cnt[a] - 1) / 2  // 同值內部配對\n                                        : 1LL * cnt[a] * cnt[b];           // 不同值交叉配對\n    }\n}\n```\n:::\n\n\n\n:::example 預處理：cop[v] = 所有與 v 互質的值清單（值域 ≤ 50 才划算）\n**3. 樹上 / 路徑上的互質：邊走邊維護「祖先資訊」。** 像「互質樹」(1766) 要找每個節點最近的、與它互質的祖先。值域小（≤ 50），可預先算好哪些值互質，DFS 時為每個值維護「最近出現的深度」。\n\n```cpp\n// 預處理：cop[v] = 所有與 v 互質的值清單（值域 ≤ 50 才划算）\nfor (int i = 1; i <= 50; ++i) {\n    for (int j = 1; j <= 50; ++j) {\n        if (std::gcd(i, j) == 1) {\n            cop[i].push_back(j);\n        }\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** GCD 單次 `O(log V)`；分桶法 `O(V² log V)`；樹上法因為值域固定，整體接近 `O(n)`。注意 0 與任意數的 GCD 是那個數本身，題目若含 0 要特別釐清定義。\n\n**題目線索（Pattern）**\n\n出現以下特徵，往「互質 / GCD = 1」想：\n\n- **題目明確提到「互質」「最簡分數」「沒有共同因數」。** 例如「最簡分數」(1447) 要列出所有 `gcd(分子, 分母) == 1` 的分數；「美麗下標對的數目」(2748) 判斷首尾數字是否互質。\n- **要統計「滿足某種數論關係的數對」，而數值範圍很小。** 這是把 `O(n²)` 降為按值域處理的信號，如「美麗下標對的數目」。\n- **在樹 / 序列上找「與當前元素互質」的鄰居或祖先。** 如「互質樹」(1766)。\n- **涉及乘積、因數結構的子陣列判定。** 如「最長乘積等價子陣列」(3411) 需要 GCD/LCM 與乘積的關係。\n\n確認套對，問自己：\n\n1. 「互質」在這題等價於哪個條件？（通常是 `gcd == 1`）\n2. 值域有多大？能不能按值分桶把兩兩枚舉省掉？\n3. 我需要的是「判斷一對」還是「統計所有對」？兩者實作差很多。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1447 | [最簡分數](https://leetcode.cn/problems/simplified-fractions/) | 1269 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1766 | [互質樹](https://leetcode.cn/problems/tree-of-coprimes/) | 2232 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2748 | [美麗下標對的數目](https://leetcode.cn/problems/number-of-beautiful-pairs/) | 1301 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3411 | [最長乘積等價子陣列](https://leetcode.cn/problems/maximum-subarray-with-equal-products/) | 1443 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1447 | [最簡分數](https://leetcode.cn/problems/simplified-fractions) | 1269 |\n| 1766 | [互質樹](https://leetcode.cn/problems/tree-of-coprimes) | 2232 |\n| 2748 | [美麗下標對的數目](https://leetcode.cn/problems/number-of-beautiful-pairs) | 1301 |\n| 3411 | [最長乘積等價子陣列](https://leetcode.cn/problems/maximum-subarray-with-equal-products) | 1443 |",
    },
    {
      id: 11,
      title: "10. 同餘",
      summary:
        "**原理講解**\n\n本節的主題是「同餘（modular arithmetic）」。先講白話：兩個數「除以同一個數 m 後餘數相同」，就說它們對 m 同餘。例如 17 和 5 除以 6 餘數都是 5，所以它們對 6 同餘。我們最常用的工具是取餘運算子 `%`。\n\n:::example 同餘的好處：先取餘再算，和先算再取餘結果一樣（防止數字爆大）\n同餘最有用的性質是：**餘數在加、減、乘之下會「保持一致」**，也就是可以先取餘再運算，結果不變：\n\n```cpp\n// 同餘的好處：先取餘再算，和先算再取餘結果一樣（防止數字爆大）\n(a + b) % m == ((a % m) + (b % m)) % m;\n(a* b) % m == ((a % m) * (b % m)) % m;\n```\n:::\n\n\n\n:::example 前綴和的餘數：子陣列和能被 m 整除 ⇔ 兩端前綴和的餘數相等\n這在處理大數、避免溢位時極關鍵。但同餘在解題上更厲害的用法是**配合「前綴和」找子陣列**。核心觀察：一段子陣列 `[i + 1, j]` 的和能被 m 整除，等價於前綴和 `pre[j]` 和 `pre[i]` 對 m 同餘。於是「找和為 m 倍數的子陣列」就變成「找前綴和餘數相同的兩個位置」：\n\n```cpp\n// 前綴和的餘數：子陣列和能被 m 整除 ⇔ 兩端前綴和的餘數相等\n// pre[j] % m == pre[i] % m  ⇒  (pre[j] - pre[i]) 是 m 的倍數\n```\n:::\n\n\n\n於是我們只要用一個雜湊表（`unordered_map`）記錄「每種餘數最近 / 最早出現在哪」，掃一遍就能回答問題，把 `O(n²)` 枚舉所有子陣列降到 `O(n)`。處理 C++ 負數取餘要小心：`-3 % 5` 在 C++ 是 -3，要用 `((x % m) + m) % m` 修正成非負。\n\n**實作方式**\n\n:::example 找「最短／某種」和為 m 倍數的子陣列：記錄每個餘數的位置\n**1. 前綴和餘數 + 雜湊表，是同餘題的主力套路。** 用 map 記錄某個餘數第一次出現的位置。\n\n```cpp\n// 找「最短／某種」和為 m 倍數的子陣列：記錄每個餘數的位置\nunordered_map<int, int> pos;  // 餘數 -> 出現的下標\npos[0] = -1;                  // 前綴和為 0 視為下標 -1（空前綴）\n```\n:::\n\n\n\n:::example 維護前綴和餘數，遇到相同餘數就找到一段「整除子陣列」\n**2. 邊掃邊查同餘的另一端。** 計算當前前綴和的餘數，看之前有沒有出現過同樣餘數。\n\n```cpp\n// 維護前綴和餘數，遇到相同餘數就找到一段「整除子陣列」\nint r = ((pre + nums[i]) % m + m) % m;  // 修正負餘數為非負\npre = r;\nif (pos.count(r)) {\n    updateAns(i - pos[r]);  // 兩端餘數相同 → 子陣列可被 m 整除\n} else {\n    pos[r] = i;  // 第一次見到此餘數才記\n}\n```\n:::\n\n\n\n:::example 按餘數分組：同餘的元素歸為一類\n**3. 用餘數做分組計數。** 像「摧毀一系列目標」(2453) 把每個數按 `value % space` 分組，同組的能被同一次操作清掉，於是用 map 統計每個餘數的數量與最小值。\n\n```cpp\n// 按餘數分組：同餘的元素歸為一類\nunordered_map<int, int> group;  // 餘數 -> 該組元素個數\ngroup[v % space]++;\n```\n:::\n\n\n\n**4. 邊界與複雜度。** C++ 的 `%` 對負數結果為負，務必修正；模數 m 為 0 要排除。前綴和餘數法整體 `O(n)`，空間 `O(min(n, m))`。涉及連乘取模時用 `long long` 防溢位。\n\n**題目線索（Pattern）**\n\n看到下列特徵，往「同餘 / 取餘分組」想：\n\n- **題目要求「能被某數整除」「餘數相同」「是某數的倍數」。** 例如「使陣列和能被 P 整除」(1590) 要刪最短子陣列讓剩餘和被 P 整除——正是前綴和同餘的經典題。\n- **要按「除以某數的餘數」把元素分類。** 如「摧毀一系列目標」(2453) 依 `% space` 分組。\n- **答案和「週期、循環、輪流」有關。** 餘數本質就是週期位置。\n- **數字會連乘、連加到很大，需要全程取模。** 如「執行操作後的最大 MEX」(2598) 把元素 `% value` 後統計每個餘數出現次數，判斷能補出多大的 MEX。\n\n確認套對，問自己：\n\n1. 我關心的是「餘數本身」還是「餘數相等的兩個位置」？\n2. 有沒有負數取餘要修正成非負？\n3. 能不能用一個 map 記錄餘數，把雙重迴圈降成單次掃描？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1590 | [使陣列和能被 P 整除](https://leetcode.cn/problems/make-sum-divisible-by-p/) | 2039 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 2453 | [摧毀一系列目標](https://leetcode.cn/problems/destroy-sequential-targets/) | 1762 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2598 | [執行操作後的最大 MEX](https://leetcode.cn/problems/smallest-missing-non-negative-integer-after-operations/) | 1846 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1590 | [使陣列和能被 P 整除](https://leetcode.cn/problems/make-sum-divisible-by-p) | 2039 |\n| 2453 | [摧毀一系列目標](https://leetcode.cn/problems/destroy-sequential-targets) | 1762 |\n| 2598 | [執行操作後的最大 MEX](https://leetcode.cn/problems/smallest-missing-non-negative-integer-after-operations) | 1846 |",
    },
    {
      id: 12,
      title: "11. 數論分塊",
      summary:
        "**原理講解**\n\n本節介紹「數論分塊（整除分塊）」。它解決一類特定問題：要計算像 `∑ floor(n / i)`（i 從 1 到 n）這種「對 `n` 除以 i 後取整（向下取整）再加總」的式子。直接讓 i 從 1 跑到 n 是 `O(n)`，當 n 很大（例如 10⁹）就太慢。\n\n:::example 觀察：floor(n/i) 只會有 O(√n) 種不同的值\n關鍵觀察是：`floor(n / i)` 這個值，在 i 連續變化時**會一段一段地保持不變**。例如 n = 10 時，i = 4、5 都讓 `floor(10/i) = 2`；i = 6,7,8,9,10 都讓它等於 1。所以與其逐個 i 計算，不如「一整塊相同值一起處理」：\n\n```cpp\n// 觀察：floor(n/i) 只會有 O(√n) 種不同的值\n// floor(10/i)：i=1→10, 2→5, 3→3, 4→2, 5→2, 6..10→1\n// 相同值的 i 連成一段，可以整段一起算\n```\n:::\n\n\n\n:::example 給定左端點 l，這一塊（floor(n/i) 相同）的右端點是\n更妙的是，對於某個起點 i，能讓 `floor(n/i)` 保持不變的「最大 i」有公式可直接求出：`r = n / (n / i)`。於是我們每次跳一整段，段數總共只有 `O(√n)` 個，複雜度從 `O(n)` 降到 `O(√n)`：\n\n```cpp\n// 給定左端點 l，這一塊（floor(n/i) 相同）的右端點是：\nint r = n / (n / l);  // [l, r] 內 floor(n/i) 都等於 n/l\n```\n:::\n\n\n\n直覺解釋：當 i ≤ √n 時，每個 i 給的值幾乎都不同（至多 √n 個 i）；當 i > √n 時，`floor(n/i)` < √n（至多 √n 種值）。兩邊合起來不同值就只有 `O(√n)` 種，這正是分塊能加速的根本原因。\n\n**實作方式**\n\n:::example 數論分塊：計算 sum of floor(n/i)，i = 1..n\n**1. 標準分塊框架：用 l 跳到 r，整段一起算。** 這是固定模板。\n\n```cpp\n// 數論分塊：計算 sum of floor(n/i)，i = 1..n\nlong long sum = 0;\nfor (int l = 1, r; l <= n; l = r + 1) {\n    int v = n / l;                 // 這一塊內 floor(n/i) 的共同值\n    r = n / v;                     // 這一塊的右端點\n    sum += 1LL * v * (r - l + 1);  // v 重複出現 (r-l+1) 次\n}\n```\n:::\n\n\n\n:::example 每塊 i 從 l 到 r 的等差和，乘上共同值 v\n**2. 變形：值不是直接相加，而是配上權重。** 若是 `∑ i * floor(n/i)`，每塊的 i 是等差數列，用首尾相加公式整段算。\n\n```cpp\n// 每塊 i 從 l 到 r 的等差和，乘上共同值 v\nlong long blockI = 1LL * (l + r) * (r - l + 1) / 2;  // l..r 的和\nsum += v * blockI;\n```\n:::\n\n\n\n:::example 根號枚舉：只跑到 √limit，再判斷另一邊是否為完全平方\n**3. 用「√n 枚舉因數」是它的近親。** 像「統計平方和三元組的數目」(1925) 要找 a²+b²=c²，可枚舉到 √上界 並利用整除/平方根判定，避免三重迴圈。核心同樣是「只需走根號級別的範圍」。\n\n```cpp\n// 根號枚舉：只跑到 √limit，再判斷另一邊是否為完全平方\nfor (int a = 1; 1LL * a * a < limit; ++a) { /* 檢查 b、c */\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 注意 `n / l` 為 0 的情形（l > n 時迴圈已停）；用 `long long` 防乘法溢位。整體 `O(√n)`，相較暴力 `O(n)` 是質的飛躍。\n\n**題目線索（Pattern）**\n\n出現以下特徵，往「數論分塊 / 根號枚舉」想：\n\n- **要計算含 `floor(n / i)` 的總和或計數，且 n 很大。** 這是分塊的招牌式子。\n- **暴力枚舉是 `O(n)` 但 n 達到 10⁸~10⁹，明顯需要降到 `O(√n)`。**\n- **問題涉及因數、倍數、平方關係，且只需走到根號範圍。** 如「統計平方和三元組的數目」(1925)，枚舉到 √limit 即可。\n- **觀察到「某個取整值在連續區間內不變」**——這正是分塊能整段處理的信號。\n\n確認套對，問自己：\n\n1. 我的式子裡有沒有 `floor(n/i)` 這種會「分段不變」的項？\n2. 對某個 l，這一塊的右端點 `r = n / (n / l)` 是多少？\n3. 暴力是 `O(n)`，分塊 / 根號枚舉能不能把它壓到 `O(√n)`？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1925 | [統計平方和三元組的數目](https://leetcode.cn/problems/count-square-sum-triples/) | 1324 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1925 | [統計平方和三元組的數目](https://leetcode.cn/problems/count-square-sum-triples) | 1324 |",
    },
    {
      id: 13,
      title: "12. 其他",
      summary:
        "**原理講解**\n\n本節是「其他」數論雜項，集合了幾個常見又不好歸類的小技巧。它們的共通精神是：**把問題翻成一條可證明的數學性質或公式，再用快速的計算手段（特別是快速冪、平方判定）求解。** 這裡介紹兩個最常考的工具。\n\n:::example 快速冪：用「平方折半」把 a^b 降到 O(log b)，全程取模防溢位\n第一個是「快速冪（fast power）」。要算 `a ^ b`，若一個一個乘要 `O(b)` 次；快速冪利用「指數可以拆成二進位」的事實，把次數降到 `O(log b)`。核心是：`a^b = (a^(b/2))²`，偶數折半、奇數多乘一個 a：\n\n```cpp\n// 快速冪：用「平方折半」把 a^b 降到 O(log b)，全程取模防溢位\nlong long qpow(long long a, long long b, long long mod) {\n    long long r = 1;\n    a %= mod;\n    while (b) {\n        if (b & 1) {\n            r = r * a % mod;  // 這一位是 1，乘上當前的 a\n        }\n        a = a * a % mod;  // a 平方，對應指數翻倍\n        b >>= 1;          // 處理下一個二進位位\n    }\n    return r;\n}\n```\n:::\n\n\n\n:::example 判斷 c 是否為兩平方數之和：枚舉 a，看 c - a² 是否為完全平方\n第二個是「完全平方 / 平方和判定」。要判斷一個數是否能寫成兩個平方數之和，可用數論定理；但入門做法是「一邊枚舉、另一邊用平方根驗證」：\n\n```cpp\n// 判斷 c 是否為兩平方數之和：枚舉 a，看 c - a² 是否為完全平方\nlong long b = (long long)sqrt(c - a * a);\nif (b * b + a * a == c) {\n    return true;  // sqrt 有誤差，務必回乘驗證\n}\n```\n:::\n\n\n\n這兩招的共同點：先找到數學性質（指數結構、平方關係），再用 `O(log)` 或 `O(√n)` 的計算取代暴力。\n\n**實作方式**\n\n:::example 處理超大指數：a^(10*k + d) = (a^k)^10 * a^d，逐位快速冪\n**1. 凡是「大指數取模」一律快速冪。** 像「超級次方」(372) 指數是一個陣列表示的巨大數，用快速冪 + 逐位處理。\n\n```cpp\n// 處理超大指數：a^(10*k + d) = (a^k)^10 * a^d，逐位快速冪\n// 對指數陣列每一位：res = qpow(res, 10) * qpow(a, digit)\n```\n:::\n\n\n\n:::example 完全平方判定：取整數平方根後回乘比對\n**2. 平方相關：枚舉 + 平方根驗證。** 「平方數之和」(633) 雙指針或枚舉一邊；`sqrt` 結果要回乘檢查，避免浮點誤差。\n\n```cpp\n// 完全平方判定：取整數平方根後回乘比對\nlong long r = (long long)sqrtl(n);\nbool isSquare = (r * r == n || (r + 1) * (r + 1) == n);\n```\n:::\n\n\n\n:::example dp[i]：湊出 i 最少需要幾個完全平方數\n**3. 「最少幾個完全平方數湊成 n」用 DP 或四平方定理。** 「完全平方數」(279) 經典做法是 `dp[i] = min(dp[i - j * j] + 1)`。\n\n```cpp\n// dp[i]：湊出 i 最少需要幾個完全平方數\nfor (int i = 1; i <= n; ++i) {\n    for (int j = 1; j * j <= i; ++j) {\n        dp[i] = min(dp[i], dp[i - j * j] + 1);\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 快速冪全程 `long long` 取模；判斷「n 的冪」(326) 可用「最大的 3 的冪是否整除 n」這種常數技巧。複雜度：快速冪 `O(log b)`、平方枚舉 `O(√n)`、平方數 DP `O(n√n)`。\n\n**題目線索（Pattern）**\n\n出現以下特徵，往這些雜項數論技巧想：\n\n- **要算非常大的次方並取模。** 如「超級次方」(372)、「可被 K 整除的最小整數」(1015，本質是對 K 取餘的循環判定）——指數大就上快速冪。\n- **問「是不是某數的冪」「能否寫成平方和」「最少幾個平方數」。** 如「的冪」(326)、「平方數之和」(633)、「完全平方數」(279)。\n- **題目能化成一條閉式公式或整除條件，暴力枚舉太慢。**\n- **看到浮點 `sqrt`**，記得回乘驗證，別直接相信浮點結果。\n\n確認套對，問自己：\n\n1. 我需要的是「大指數取模」（快速冪）還是「平方關係判定」（√n 枚舉）？\n2. 有沒有用 `long long` 並全程取模防溢位？\n3. `sqrt` 的結果我有沒有回乘檢查、避免浮點誤差？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 279 | 完全平方數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 326 | 的冪 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 372 | 超級次方 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 633 | 平方數之和 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1015 | [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k/) | 1875 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 279 | [完全平方數](https://leetcode.cn/problems/perfect-squares) |  |\n| 326 | [的冪](https://leetcode.cn/problems/power-of-three) |  |\n| 372 | [超級次方](https://leetcode.cn/problems/super-pow) |  |\n| 633 | [平方數之和](https://leetcode.cn/problems/sum-of-square-numbers) |  |\n| 1015 | [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k) | 1875 |\n| 1018 | [可被 5 整除的二進位制字首](https://leetcode.cn/problems/binary-prefix-divisible-by-5) | 1376 |\n| 1387 | [將整數按權重排序](https://leetcode.cn/problems/sort-integers-by-the-power-value) | 1507 |\n| 2240 | [買鋼筆和鉛筆的方案數](https://leetcode.cn/problems/number-of-ways-to-buy-pens-and-pencils) | 1400 |\n| 3790 | [最小全 1 倍數](https://leetcode.cn/problems/smallest-all-ones-multiple) | 1593 |",
    },
    {
      id: 14,
      title: "13. 乘法原理",
      summary:
        "**原理講解**\n\n本節介紹「乘法原理」——組合計數最基礎的一條規則。它說的是：**如果完成一件事要經過幾個獨立的步驟，第 1 步有 a 種選法、第 2 步有 b 種、…，那麼總方案數就是把每一步的選法相乘 `a × b × …`。**\n\n:::example 乘法原理：各步驟獨立時，總方案 = 各步驟選法數的乘積\n舉個生活例子：早餐有 3 種飲料、2 種主食，搭配方式就是 `3 × 2 = 6` 種。關鍵詞是「獨立」——每一步的選擇不互相影響時，才能直接相乘：\n\n```cpp\n// 乘法原理：各步驟獨立時，總方案 = 各步驟選法數的乘積\n// 例如 4 位數密碼，每位 0~9：總數 = 10 * 10 * 10 * 10\nlong long total = 1;\nfor (int i = 0; i < positions; ++i) {\n    total = total * choices[i];\n}\n```\n:::\n\n\n\n:::example 連乘取模：避免方案數爆炸，每乘一次就 % MOD\n在演算法題裡，乘法原理常和「取模」一起出現，因為方案數會大到爆掉 `long long`，題目要求對 `1e9 + 7` 取餘。連乘時每一步都取模即可：\n\n```cpp\n// 連乘取模：避免方案數爆炸，每乘一次就 % MOD\nconst long long MOD = 1e9 + 7;\nlong long ans = 1;\nfor (int x : factors) {\n    ans = ans * x % MOD;\n}\n```\n:::\n\n\n\n另一個常見搭檔是「快速冪」：當某種選法重複很多次（例如某位有 4 種選法、共 n 位），就是 `4^n`，用快速冪在 `O(log n)` 算出 `4^n % MOD`，而不是乘 n 次。乘法原理的威力在於——它把「需要列舉所有組合」（指數爆炸）變成「幾個數相乘」（極快）。\n\n**實作方式**\n\n:::example 把答案拆成「各位置的選法」相乘\n**1. 拆步驟，確認每步獨立，再相乘。** 這是套用乘法原理的第一動作。\n\n```cpp\n// 把答案拆成「各位置的選法」相乘\n// 例如奇數位有 a 種、偶數位有 b 種、各有若干位 → a^(奇位數) * b^(偶位數)\n```\n:::\n\n\n\n:::example 快速冪求 base^exp % MOD（乘法原理裡算「重複選法」的標配）\n**2. 大量重複的選法用快速冪。** 「統計好數字的數目」(1922) 偶數位有 5 種選法、奇數位有 4 種，答案就是 `5^偶位數 * 4^奇位數`，用快速冪算冪次。\n\n```cpp\n// 快速冪求 base^exp % MOD（乘法原理裡算「重複選法」的標配）\nlong long qpow(long long base, long long exp, long long mod) {\n    long long r = 1;\n    base %= mod;\n    for (; exp; exp >>= 1, base = base * base % mod) {\n        if (exp & 1) {\n            r = r * base % mod;\n        }\n    }\n    return r;\n}\n```\n:::\n\n\n\n:::example 元素 nums[i] 的貢獻：作為最大值的子序列數 2^i，作為最小值 2^(n-1-i…\n**3. 配合前後綴拆貢獻。** 「子序列寬度之和」(891) 排序後，每個元素當「最大值」與「最小值」的次數由它前後有幾個元素決定，貢獻是 `2^前 - 2^後` 之類的冪——本質是乘法原理數「有多少子序列包含它」。\n\n```cpp\n// 元素 nums[i] 的貢獻：作為最大值的子序列數 2^i，作為最小值 2^(n-1-i)\nans = (ans + (qpow(2, i) - qpow(2, n - 1 - i)) % MOD * nums[i]) % MOD;\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 取模減法可能變負，要 `(x % MOD + MOD) % MOD` 修正；全程 `long long`。單純相乘 `O(n)`，含快速冪則 `O(n log V)`。\n\n**題目線索（Pattern）**\n\n出現以下特徵，往「乘法原理」想：\n\n- **答案是「方案數 / 排列數 / 組合數」，且各部分選擇互相獨立。** 如「統計好數字的數目」(1922) 各位數獨立決定。\n- **要對 `1e9 + 7` 取模**，幾乎都是計數題，乘法原理常是骨架。\n- **每個元素 / 位置的「貢獻」可獨立計算再相加 / 相乘。** 如「子序列寬度之和」(891)、「所有奇數長度子陣列的和」(1588) 用「每個元素被多少子陣列包含」乘上自身。\n- **某種選法重複很多次** → 化成冪，用快速冪。如「分割字串的方案數」(1573)。\n\n確認套對，問自己：\n\n1. 我能不能把答案拆成「幾個獨立步驟的選法」相乘？\n2. 各步驟真的彼此獨立嗎（一步的選擇不限制另一步）？\n3. 連乘會不會溢位？要不要每步取模、用快速冪算重複的冪次？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 891 | [子序列寬度之和](https://leetcode.cn/problems/sum-of-subsequence-widths/) | 2183 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1573 | [分割字串的方案數](https://leetcode.cn/problems/number-of-ways-to-split-a-string/) | 1591 | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 1588 | [所有奇數長度子陣列的和](https://leetcode.cn/problems/sum-of-all-odd-length-subarrays/) | 1409 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1617 | [統計子樹中城市之間最大距離](https://leetcode.cn/problems/count-subtrees-with-max-distance-between-cities/) | 2309 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1922 | [統計好數字的數目](https://leetcode.cn/problems/count-good-numbers/) | 1675 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 891 | [子序列寬度之和](https://leetcode.cn/problems/sum-of-subsequence-widths) | 2183 |\n| 1573 | [分割字串的方案數](https://leetcode.cn/problems/number-of-ways-to-split-a-string) | 1591 |\n| 1588 | [所有奇數長度子陣列的和](https://leetcode.cn/problems/sum-of-all-odd-length-subarrays) | 1409 |\n| 1617 | [統計子樹中城市之間最大距離](https://leetcode.cn/problems/count-subtrees-with-max-distance-between-cities) | 2309 |\n| 1922 | [統計好數字的數目](https://leetcode.cn/problems/count-good-numbers) | 1675 |\n| 2063 | [所有子字串中的元音](https://leetcode.cn/problems/vowels-of-all-substrings) | 1663 |\n| 2147 | [分隔長廊的方案數](https://leetcode.cn/problems/number-of-ways-to-divide-a-long-corridor) | 1915 |\n| 2306 | [公司命名](https://leetcode.cn/problems/naming-a-company) | 2305 |\n| 2316 | [統計無向圖中無法互相到達點對數](https://leetcode.cn/problems/count-unreachable-pairs-of-nodes-in-an-undirected-graph) | 1604 |\n| 2450 | [應用操作後不同二進位制字串的數量](https://leetcode.cn/problems/number-of-distinct-binary-strings-after-applying-operations) |  |\n| 2550 | [猴子碰撞的方法數](https://leetcode.cn/problems/count-collisions-of-monkeys-on-a-polygon) | 1663 |\n| 2681 | [英雄的力量](https://leetcode.cn/problems/power-of-heroes) | 2060 |\n| 2750 | [將陣列劃分成若干好子陣列的方式](https://leetcode.cn/problems/ways-to-split-array-into-good-subarrays) | 1598 |\n| 2763 | [所有子陣列中不平衡數字之和](https://leetcode.cn/problems/sum-of-imbalance-numbers-of-all-subarrays) | 2278 |\n| 2867 | [統計樹中的合法路徑數目](https://leetcode.cn/problems/count-valid-paths-in-a-tree) | 2428 |\n| 2963 | [統計好分割方案的數目](https://leetcode.cn/problems/count-the-number-of-good-partitions) | 1985 |\n| 3067 | [在帶權樹網路中統計可連線伺服器對數目](https://leetcode.cn/problems/count-pairs-of-connectable-servers-in-a-weighted-tree-network) | 1909 |\n| 3128 | [直角三角形](https://leetcode.cn/problems/right-triangles) | 1541 |\n| 3426 | [所有安放棋子方案的曼哈頓距離](https://leetcode.cn/problems/manhattan-distances-of-all-arrangements-of-pieces) | 2443 |\n| 3428 | [最多 K 個元素的子序列的最值之和](https://leetcode.cn/problems/maximum-and-minimum-sums-of-at-most-size-k-subsequences) | 2028 |",
    },
    {
      id: 15,
      title: "14. 組合計數",
      summary:
        "**原理講解**\n\n本節介紹「組合計數」——回答「有幾種選法 / 排法」的數學。核心是兩個量：排列數與組合數。\n\n:::example 組合數定義：C(n, k) = n! / (k! * (n-k)!)\n「排列」在意順序：從 n 個東西取 k 個排成一列，有 `n × (n - 1) × … × (n - k + 1)` 種。「組合」不在意順序：從 n 個取 k 個（誰先誰後不算）記作 `C(n, k)`，公式是 `n!/ (k!× (n - k)!)`。例如從 5 人選 2 人組隊，是 `C(5, 2) = 10`：\n\n```cpp\n// 組合數定義：C(n, k) = n! / (k! * (n-k)!)\n// 例：C(5, 2) = 120 / (2 * 6) = 10\n```\n:::\n\n\n\n:::example 模逆元（MOD 為質數）：inv(b) = b^(MOD-2) % MOD，再用乘法代替…\n實作上最大的坑是**取模除法**。計數題要對 `1e9 + 7` 取餘，但 `%` 不能直接做除法（`(a/b) % MOD ≠ a%MOD / b%MOD`）。解法是用「模逆元」：在取模世界裡，「除以 b」等於「乘上 b 的逆元」。由於 MOD 是質數，可用費馬小定理求逆元 `b ^ (MOD - 2)`，配快速冪算出：\n\n```cpp\n// 模逆元（MOD 為質數）：inv(b) = b^(MOD-2) % MOD，再用乘法代替除法\nlong long inv(long long b) { return qpow(b, MOD - 2, MOD); }\n// 於是 C(n,k) % MOD = fac[n] * inv(fac[k]) % MOD * inv(fac[n-k]) % MOD\n```\n:::\n\n\n\n實務上會「預處理階乘表」`fac[]` 和「逆元階乘表」`invFac[]`，之後每個組合數 `O(1)` 取出。這就是組合計數的標準骨架：先建表，再查表。\n\n**實作方式**\n\n:::example 預處理：階乘 fac 與其逆元 invFac\n**1. 預處理階乘與逆元表。** 一次建好，往後所有 `C(n, k)` 都是常數時間。\n\n```cpp\n// 預處理：階乘 fac 與其逆元 invFac\nfac[0] = 1;\nfor (int i = 1; i <= N; ++i) {\n    fac[i] = fac[i - 1] * i % MOD;\n}\ninvFac[N] = qpow(fac[N], MOD - 2, MOD);\nfor (int i = N; i > 0; i--) {\n    invFac[i - 1] = invFac[i] * i % MOD;\n}\n```\n:::\n\n\n\n:::example 查表取 C(n, k)，邊界 k<0 或 k>n 回 0\n**2. O(1) 取組合數。** 套公式 = 階乘 × 兩個逆元階乘。\n\n```cpp\n// 查表取 C(n, k)，邊界 k<0 或 k>n 回 0\nlong long C(int n, int k) {\n    if (k < 0 || k > n) {\n        return 0;\n    }\n    return fac[n] * invFac[k] % MOD * invFac[n - k] % MOD;\n}\n```\n:::\n\n\n\n:::example 網格不同路徑：總步數中選「向下」的步 → 組合數\n**3. 把題目翻成計數模型。** 「不同路徑」(62) 從左上到右下走 m+n-2 步、選 m-1 步向下，答案就是 `C(m + n - 2, m - 1)`。先想清楚「在選什麼、要不要順序」。\n\n```cpp\n// 網格不同路徑：總步數中選「向下」的步 → 組合數\nlong long ans = C(m + n - 2, m - 1);\n```\n:::\n\n\n\n**4. 需要排除重複時用容斥；小範圍可直接 DP。** 「統計各位數字都不同的數字個數」(357) 是按位排列相乘；「質數排列」(1175) 是 `(質數個數)!× (非質數個數)!`，純階乘相乘。複雜度：建表 `O(N)`，每次查詢 `O(1)`。\n\n**題目線索（Pattern）**\n\n出現以下特徵，往「組合計數」想：\n\n- **問「有幾種方案 / 路徑 / 排法 / 選法」，答案是大數要取模。** 如「不同路徑」(62)、「質數排列」(1175)。\n- **能描述成「從 n 個裡選 k 個」或「把東西排成一列」。** 在意順序用排列，不在意用組合。\n- **公式裡出現階乘 / 除法，需要模逆元。** 看到要對 `1e9 + 7` 取餘又有除法，就是逆元的信號。\n- **要扣掉不合法的情形** → 容斥；像「尋找陣列的錯位排列」(634) 是經典錯排計數。\n\n確認套對，問自己：\n\n1. 我在數的東西「在意順序」嗎？（決定用排列還是組合）\n2. 能不能把它寫成 `C(n, k)` 或階乘相乘的閉式？\n3. 取模時的除法，我有沒有改用模逆元（質數模 + 費馬小定理）？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 25 | 古董鍵盤 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 62 | 不同路徑 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 357 | 統計各位數字都不同的數字個數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 634 | 尋找陣列的錯位排列（Premium） | N/A | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 1175 | [質數排列](https://leetcode.cn/problems/prime-arrangements/) | 1489 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 25 | [古董鍵盤](https://leetcode.cn/problems/Uh984O) |  |\n| 62 | [不同路徑](https://leetcode.cn/problems/unique-paths) |  |\n| 357 | [統計各位數字都不同的數字個數](https://leetcode.cn/problems/count-numbers-with-unique-digits) |  |\n| 634 | [尋找陣列的錯位排列](https://leetcode.cn/problems/find-the-derangement-of-an-array) |  |\n| 1175 | [質數排列](https://leetcode.cn/problems/prime-arrangements) | 1489 |\n| 1359 | [有效的快遞序列數目](https://leetcode.cn/problems/count-all-valid-pickup-and-delivery-options) | 1723 |\n| 1467 | [兩個盒子中球的顏色數相同的機率](https://leetcode.cn/problems/probability-of-a-two-boxes-having-the-same-number-of-distinct-balls) | 2357 |\n| 1569 | [將子陣列重新排序得到同一個二叉搜索樹的方案數](https://leetcode.cn/problems/number-of-ways-to-reorder-array-to-get-same-bst) | 2288 |\n| 1575 | [統計所有可行路徑](https://leetcode.cn/problems/count-all-possible-routes) | 2055 |\n| 1643 | [第 K 條最小指令](https://leetcode.cn/problems/kth-smallest-instructions) | 2080 |\n| 1692 | [計算分配糖果的不同方式](https://leetcode.cn/problems/count-ways-to-distribute-candies) |  |\n| 1830 | [使字串有序的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-string-sorted) | 2620 |\n| 1866 | [恰有 K 根木棍可以看到的排列數目](https://leetcode.cn/problems/number-of-ways-to-rearrange-sticks-with-k-sticks-visible) | 2333 |\n| 1916 | [統計為蟻羣構築房間的不同順序](https://leetcode.cn/problems/count-ways-to-build-rooms-in-an-ant-colony) | 2486 |\n| 2221 | [陣列的三角和](https://leetcode.cn/problems/find-triangular-sum-of-an-array) | 1317 |\n| 2400 | [恰好移動 k 步到達某一位置的方法數目](https://leetcode.cn/problems/number-of-ways-to-reach-a-position-after-exactly-k-steps) | 1751 |\n| 2514 | [統計同位異構字串數目](https://leetcode.cn/problems/count-anagrams) | 2070 |\n| 2539 | [好子序列的個數](https://leetcode.cn/problems/count-the-number-of-good-subsequences) |  |\n| 2842 | [統計一個字串的 k 子序列美麗值最大的數目](https://leetcode.cn/problems/count-k-subsequences-of-a-string-with-maximum-beauty) | 2092 |\n| 2954 | [統計感冒序列的數目](https://leetcode.cn/problems/count-the-number-of-infection-sequences) | 2645 |\n| 3154 | [到達第 K 級臺階的方案數](https://leetcode.cn/problems/find-number-of-ways-to-reach-the-k-th-stair) | 2071 |\n| 3179 | [K 秒後第 N 個元素的值](https://leetcode.cn/problems/find-the-n-th-value-after-k-seconds) | 1369 |\n| 3251 | [單調陣列對的數目 II](https://leetcode.cn/problems/find-the-count-of-monotonic-pairs-ii) | 2323 |\n| 3272 | [統計好整數的數目](https://leetcode.cn/problems/find-the-count-of-good-integers) | 2382 |\n| 3317 | [安排活動的方案數](https://leetcode.cn/problems/find-the-number-of-possible-ways-for-an-event) | 2414 |\n| 3343 | [統計平衡排列的數目](https://leetcode.cn/problems/count-number-of-balanced-permutations) | 2615 |\n| 3395 | [唯一中間眾數子序列 I](https://leetcode.cn/problems/subsequences-with-a-unique-middle-mode-i) | 2800 |\n| 3405 | [統計恰好有 K 個相等相鄰元素的陣列數目](https://leetcode.cn/problems/count-the-number-of-arrays-with-k-matching-adjacent-elements) | 2310 |\n| 3463 | [判斷操作後字串中的數字是否相等 II](https://leetcode.cn/problems/check-if-digits-are-equal-in-string-after-operations-ii) | 2286 |\n| 3470 | [全排列 IV](https://leetcode.cn/problems/permutations-iv) | 2474 |\n| 3518 | [最小回文排列 II](https://leetcode.cn/problems/smallest-palindromic-rearrangement-ii) | 2375 |\n| 3519 | [統計逐位非遞減的整數](https://leetcode.cn/problems/count-numbers-with-non-decreasing-digits) | 2246 |\n| 3539 | [魔法序列的陣列乘積之和](https://leetcode.cn/problems/find-sum-of-array-product-of-magical-sequences) | 2694 |\n| 3558 | [給邊賦權值的方案數 I](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-i) | 1845 |\n| 3821 | [二進位制中恰好K個1的第N小整數](https://leetcode.cn/problems/find-nth-smallest-integer-with-k-one-bits) | 2069 |",
    },
    {
      id: 16,
      title: "15. 放球問題",
      summary:
        '**原理講解**\n\n本節要解決的場景是：要把若干個「球」放進若干個「盒子」，問有幾種放法。這類「計數」問題的核心，是把一句中文敘述翻譯成一條可以直接算的公式，而不是真的去一個一個排列出來數。\n\n:::example 隔板法：n 顆相同球放進 k 個盒子（可空）的方法數\n最經典的工具叫「隔板法（stars and bars）」。想像你要把 `n` 顆一模一樣的球，放進 `k` 個不同的盒子，盒子可以空。把球排成一列，再插入 `k - 1` 塊「隔板」把它們切成 `k` 段，每一段就是一個盒子分到的數量。於是「放法數」就等於「在 `n + k - 1` 個位置裡挑 `k - 1` 個放隔板」的方法數，也就是組合數 `C(n + k - 1, k - 1)`。\n\n```cpp\n// 隔板法：n 顆相同球放進 k 個盒子（可空）的方法數\n// 答案 = C(n + k - 1, k - 1)\nlong long ballsIntoBoxes(int n, int k) {\n    return C(n + k - 1, k - 1);  // C 是組合數，下面會說怎麼算\n}\n```\n:::\n\n\n\n這裡的關鍵字是「相同的球、不同的盒子、可不可以空」。球相不相同、盒子能不能空，會換成不同公式，但精神一樣：先想清楚「在數什麼東西」，再找對應的組合公式。\n\n:::example 小資料先驗證公式：把 n=2, k=2 手算一遍\n因為答案常常是天文數字，題目通常要求對一個質數（多半是 `1e9 + 7`）取餘數。組合數 `C(a, b) = a!/ (b!(a - b)!)` 含除法，取餘數時不能直接除，要用「模逆元」把除法換成乘法（之後實作會說明）。先記住一句話：**計數題 = 找公式 + 預處理階乘 + 取模。**\n\n```cpp\n// 小資料先驗證公式：把 n=2, k=2 手算一遍\n// 放法：(2,0)(1,1)(0,2) 共 3 種；C(2+2-1, 2-1)=C(3,1)=3，吻合\n```\n:::\n\n\n\n**實作方式**\n\n計數題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 公式化：先打表觀察，再猜閉式\n**1. 公式化：先用小資料把規律試出來。** 動手算 `n = 1, 2, 3` 的幾個小例子，列出答案，再去湊公式或上網對組合恆等式。像 1641「統計字典序母音字串的數目」其實就是「把字串長度當球、5 個母音當盒子」的隔板模型。\n\n```cpp\n// 公式化：先打表觀察，再猜閉式\nfor (int n = 0; n <= 4; ++n) {\n    cout << n << ": " << brute(n) << "\\n";  // 暴力跑出小答案，找規律\n}\n```\n:::\n\n\n\n:::example 預處理：階乘 fac[] 與其逆元 inv[]（模 MOD）\n**2. 預處理階乘與逆元。** 既然要反覆算組合數，先一次把 `0!` 到 `N!` 以及它們的逆元都算好，之後每個 `C(a, b)` 都是 `O(1)`。\n\n```cpp\n// 預處理：階乘 fac[] 與其逆元 inv[]（模 MOD）\nconst long long MOD = 1e9 + 7;\nlong long fac[N], inv[N];\nfac[0] = 1;\nfor (int i = 1; i < N; ++i) {\n    fac[i] = fac[i - 1] * i % MOD;\n}\ninv[N - 1] = power(fac[N - 1], MOD - 2, MOD);  // 費馬小定理求逆元\nfor (int i = N - 1; i > 0; i--) {\n    inv[i - 1] = inv[i] * i % MOD;\n}\n```\n:::\n\n\n\n:::example 組合數 C(a, b) = a! * (b!)^-1 * ((a-b)!)^-1 (mo…\n**3. 用公式組出答案。** 把組合數封裝成函式，主邏輯就只是套公式。\n\n```cpp\n// 組合數 C(a, b) = a! * (b!)^-1 * ((a-b)!)^-1 (mod MOD)\nlong long C(int a, int b) {\n    if (b < 0 || b > a) {\n        return 0;\n    }\n    return fac[a] * inv[b] % MOD * inv[a - b] % MOD;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 乘法會溢位：每乘一步都要 `% MOD`，且用 `long long`。\n- 取模下不能用普通除法，一定要走逆元。\n- 預處理 `O(N)`，之後每次查詢 `O(1)`；若像 2338「統計理想陣列的數目」需要疊加多個隔板模型，總複雜度仍由查詢次數決定。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「放球／計數公式」想：\n\n- **題目在問「有幾種方法 / 多少種方案 / 數目」，而且答案要對 `1e9 + 7` 取模。** 取模幾乎等於明示「答案很大，請用公式而非枚舉」，像 1735「生成乘積陣列的方案數」、1621「大小為 K 的不重疊線段的數目」都是。\n- **可以重新敘述成「把東西分配到格子裡」。** 一旦你能說出「這是把 n 個 X 放進 k 個 Y」，隔板法或組合數通常就在不遠處。\n- **暴力會爆炸。** 直接枚舉所有放法是指數級，根本跑不完，這逼你去找閉式：\n\n:::example 嗅到味道的瞬間：暴力枚舉所有分配是指數級，不可行\n```cpp\n// 嗅到味道的瞬間：暴力枚舉所有分配是指數級，不可行\n// dfs 枚舉每顆球放哪個盒子 → O(k^n)，n 一大就 TLE\n// 於是改用組合公式，一步算出總數\n```\n:::\n\n\n\n- **解法重點寫著「先公式化、用小資料驗證規律」。** 這就是叫你打表猜公式再補證明。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我到底在數「什麼東西的排列或組合」？球是誰、盒子是誰？\n2. 我猜的公式，在 `n = 1, 2, 3` 的小例子上對得起來嗎？\n3. 我有沒有處理好取模？除法是不是換成逆元了？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1641 | [統計字典序母音字串的數目](https://leetcode.cn/problems/count-sorted-vowel-strings/) | 1519 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3669 | [K 因數分解](https://leetcode.cn/problems/balanced-k-factor-decomposition/) | 1917 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 1621 | [大小為 K 的不重疊線段的數目](https://leetcode.cn/problems/number-of-sets-of-k-non-overlapping-line-segments/) | 2198 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1735 | [生成乘積陣列的方案數](https://leetcode.cn/problems/count-ways-to-make-array-with-product/) | 2500 | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 2338 | [統計理想陣列的數目](https://leetcode.cn/problems/count-the-number-of-ideal-arrays/) | 2615 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1641 | [統計字典序母音字串的數目](https://leetcode.cn/problems/count-sorted-vowel-strings) | 1519 |\n| 3669 | [K 因數分解](https://leetcode.cn/problems/balanced-k-factor-decomposition) | 1917 |\n| 1621 | [大小為 K 的不重疊線段的數目](https://leetcode.cn/problems/number-of-sets-of-k-non-overlapping-line-segments) | 2198 |\n| 1735 | [生成乘積陣列的方案數](https://leetcode.cn/problems/count-ways-to-make-array-with-product) | 2500 |\n| 2338 | [統計理想陣列的數目](https://leetcode.cn/problems/count-the-number-of-ideal-arrays) | 2615 |',
    },
    {
      id: 18,
      title: "16. 容斥原理",
      summary:
        "**原理講解**\n\n本節要解決的場景是：要數「滿足某些條件的東西有幾個」，但這些條件之間會「重複算到」。容斥原理（inclusion-exclusion）就是一套修正重複的記帳法。\n\n:::example 容斥最小例：1..n 中能被 2 或 3 整除的數量\n先看最小的例子。問「1 到 n 裡，能被 2 或被 3 整除的數有幾個」。直覺是「被 2 的個數」加「被 3 的個數」，但 6、12、18… 這些「同時被 2 和 3」的數被算了兩次，所以要再減掉一份。\n\n```cpp\n// 容斥最小例：1..n 中能被 2 或 3 整除的數量\n// |A∪B| = |A| + |B| - |A∩B|\nlong long countDiv2or3(long long n) {\n    return n / 2 + n / 3 - n / 6;  // 6 = lcm(2,3)，被重複算，要減回去\n}\n```\n:::\n\n\n\n:::example 三個集合的容斥（看正負號交替的規律）\n核心公式就是這條：**並集的大小 = 各集合相加 − 兩兩交集 + 三三交集 − …，奇數個取正、偶數個取負，正負交替。** 集合一多，這個「加加減減」會延伸成一長串，但規則永遠是「項裡集合的個數是奇數就加、偶數就減」。\n\n```cpp\n// 三個集合的容斥（看正負號交替的規律）\n// |A∪B∪C| = |A|+|B|+|C| - |AB|-|AC|-|BC| + |ABC|\n```\n:::\n\n\n\n容斥之所以有用，是因為很多題目「直接數很難，但數交集很容易」。例如「能被某些數整除」這種條件，交集就是「能被它們的最小公倍數 lcm 整除」，一行就算得出來。像 1201「醜數 III」要數「能被 a、b、c 之一整除的第 N 個數」、2652「倍數求和」、878「第 N 個神奇數字」，骨子裡都是這套整除 + 容斥。\n\n**實作方式**\n\n容斥題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 定義：cnt(d) = 1..n 中能被 d 整除的個數\n**1. 定義集合。** 先說清楚每個集合代表什麼條件，以及它的大小怎麼快速算。整除類問題中，「能被 d 整除的個數」就是 `n / d`。\n\n```cpp\n// 定義：cnt(d) = 1..n 中能被 d 整除的個數\nauto cnt = [&](long long d) { return n / d; };\n```\n:::\n\n\n\n:::example 枚舉子集做容斥（以「整除」為例）\n**2. 列出所有子集，按大小定正負號。** 若有 `m` 個基本集合，就枚舉 `2^m - 1` 種非空子集；子集裡元素個數是奇數加、偶數減。多個數的交集 = 它們的 lcm。\n\n```cpp\n// 枚舉子集做容斥（以「整除」為例）\nlong long ans = 0;\nfor (int s = 1; s < (1 << m); ++s) {\n    long long L = 1;\n    int bits = __builtin_popcount(s);\n    for (int i = 0; i < m; ++i) {\n        if (s >> i & 1) {\n            L = lcm(L, a[i]);  // 交集 = lcm\n        }\n    }\n    ans += (bits & 1 ? 1 : -1) * (n / L);  // 奇加偶減\n}\n```\n:::\n\n\n\n:::example 安全 lcm：一旦超過 n 就可以提前當 0 處理\n**3. 注意 lcm 的溢位。** lcm 可能比 `n` 還大，這時 `n / L` 直接是 0；但 lcm 連乘本身可能爆 `long long`，要邊算邊判斷是否已超過上限。\n\n```cpp\n// 安全 lcm：一旦超過 n 就可以提前當 0 處理\nlong long safeLcm(long long a, long long b, long long cap) {\n    long long g = __gcd(a, b);\n    if (a / g > cap / b) {\n        return cap + 1;  // 會溢位/超界，標記為無效\n    }\n    return a / g * b;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 容斥常配二分搜尋：像 878、1201 是「二分一個答案 x，用容斥數出 ≤ x 有幾個合格數」，複雜度約 `O(2^m log n)`。\n- 正負號弄反、漏掉某個交集，是最常見的錯；用三個集合的小例子手算對照。\n- 取模題（如 920「播放列表的數量」）的容斥要小心減出負數，記得 `(x % MOD + MOD) % MOD`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「容斥原理」想：\n\n- **要數「滿足 A 或 B 或 C…」的數量，而條件彼此會重疊。** 直接相加會重複，這正是容斥的主場。\n- **單獨數「同時滿足多個條件」（交集）反而簡單。** 例如整除類，交集就是 lcm，一行算完。\n- **基本條件個數很少（通常 ≤ 20），但組合爆炸。** 少量集合才枚舉得起 `2^m` 個子集：\n\n:::example 嗅到味道的瞬間：條件只有幾個，但「或」起來很難直接數\n```cpp\n// 嗅到味道的瞬間：條件只有幾個，但「或」起來很難直接數\n// 直接數並集很煩，但每個交集都好算 → 改用加減交替的容斥\n```\n:::\n\n\n\n- **題目像「第 N 個能被 … 整除的數」。** 多半是「二分答案 + 容斥計數」的組合，例如 1201、878。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「基本集合」各是什麼？每個集合大小怎麼 `O(1)` 算？\n2. 兩個集合的交集是什麼？（整除題就是 lcm）\n3. 我的正負號規則對嗎？奇數個集合加、偶數個減，有沒有弄反？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 878 | [第 N 個神奇數字](https://leetcode.cn/problems/nth-magical-number/) | 1897 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 920 | [播放列表的數量](https://leetcode.cn/problems/number-of-music-playlists/) | 2400 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1201 | [醜數 III](https://leetcode.cn/problems/ugly-number-iii/) | 2039 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2513 | [最小化兩個陣列中的最大值](https://leetcode.cn/problems/minimize-the-maximum-of-two-arrays/) | 2302 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2652 | [倍數求和](https://leetcode.cn/problems/sum-multiples/) | 1182 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 878 | [第 N 個神奇數字](https://leetcode.cn/problems/nth-magical-number) | 1897 |\n| 920 | [播放列表的數量](https://leetcode.cn/problems/number-of-music-playlists) | 2400 |\n| 1201 | [醜數 III](https://leetcode.cn/problems/ugly-number-iii) | 2039 |\n| 2513 | [最小化兩個陣列中的最大值](https://leetcode.cn/problems/minimize-the-maximum-of-two-arrays) | 2302 |\n| 2652 | [倍數求和](https://leetcode.cn/problems/sum-multiples) | 1182 |\n| 2927 | [給小朋友們分糖果 III](https://leetcode.cn/problems/distribute-candies-among-children-iii) |  |\n| 2929 | [給小朋友們分糖果 II](https://leetcode.cn/problems/distribute-candies-among-children-ii) | 1701 |\n| 2930 | [重新排列後包含指定子字串的字串數目](https://leetcode.cn/problems/number-of-strings-which-can-be-rearranged-to-contain-substring) | 2227 |\n| 3116 | [單面值組合的第 K 小金額](https://leetcode.cn/problems/kth-smallest-amount-with-single-denomination-combination) | 2388 |\n| 3130 | [找出所有穩定的二進位制陣列 II](https://leetcode.cn/problems/find-all-possible-stable-binary-arrays-ii) | 2825 |\n| 3336 | [最大公約數相等的子序列數量](https://leetcode.cn/problems/find-the-number-of-subsequences-with-equal-gcd) | 2403 |\n| 3733 | [完成所有送貨任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-deliveries) | 1973 |\n| 3757 | [有效子序列的數量](https://leetcode.cn/problems/number-of-effective-subsequences) | 2519 |",
    },
    {
      id: 19,
      title: "17. 生成函式（母函式）",
      summary:
        "**原理講解**\n\n本節要解決的場景是：把「有幾種組合方式」的計數問題，變成「多項式相乘」來做。這個技巧叫生成函式（generating function，又稱母函式）。聽起來抽象，但它其實就是一句話：**用多項式的「次數」記錄數量，用「係數」記錄方法數。**\n\n:::example 一顆骰子的生成函式：x^1 + x^2 + ... + x^6\n舉個具體例子。一顆骰子能擲出 1~6 點，我們把它寫成一個多項式：\n\n```cpp\n// 一顆骰子的生成函式：x^1 + x^2 + ... + x^6\n// x 的「次方」代表點數，係數 1 代表「擲出這個點數有 1 種方式」\n// poly[p] = 1 表示點數 p 出現 1 種方法，p = 1..6\n```\n:::\n\n\n\n:::example 多項式相乘 = 卷積：c[i+j] += a[i] * b[j]\n神奇的地方在於：**兩顆骰子的總和分布，就是兩個多項式相乘。** 因為相乘時，`x ^ a * x ^ b = x ^ (a + b)`，自然把「點數相加」對應成「次方相加」，而係數會自動累加成「湊出這個總和的方法數」。1155「擲骰子等於目標和的方法數」正是這個模型：把 `k` 顆骰子的多項式連乘，看最後 `x ^ target` 的係數就是答案。\n\n```cpp\n// 多項式相乘 = 卷積：c[i+j] += a[i] * b[j]\nvector<long long> mul(vector<long long>& a, vector<long long>& b) {\n    vector<long long> c(a.size() + b.size() - 1, 0);\n    for (int i = 0; i < (int)a.size(); ++i) {\n        for (int j = 0; j < (int)b.size(); ++j) {\n            c[i + j] += a[i] * b[j];  // 次方相加、係數相乘並累計\n        }\n    }\n    return c;\n}\n```\n:::\n\n\n\n你會發現，這跟「分組背包 / 計數 DP」本質完全一樣——生成函式只是給了我們一個乾淨的語言去描述「把多個選擇合併」。所以實作上，多半就是寫一個係數陣列，反覆做卷積。\n\n**實作方式**\n\n生成函式題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 物件 → 多項式：以骰子為例，poly[1..6] = 1，其餘 0\n**1. 把每個「物件」寫成一個多項式（係數陣列）。** 想清楚 `x` 的次方代表什麼量（總和？個數？逆序對數量？），係數代表方法數。3193「統計逆序對的數目」就是讓次方代表「目前累積的逆序對數」。\n\n```cpp\n// 物件 → 多項式：以骰子為例，poly[1..6] = 1，其餘 0\nvector<long long> die(7, 0);\nfor (int p = 1; p <= 6; ++p) {\n    die[p] = 1;\n}\n```\n:::\n\n\n\n:::example 連乘 k 顆骰子，ans[s] = 總和為 s 的方法數\n**2. 連乘合併（卷積）。** 把所有物件的多項式乘起來。乘的過程就是計數 DP 的轉移；用一維陣列滾動即可。\n\n```cpp\n// 連乘 k 顆骰子，ans[s] = 總和為 s 的方法數\nvector<long long> ans = {1};  // 空積：總和 0 有 1 種\nfor (int t = 0; t < k; ++t) {\n    ans = mul(ans, die);\n}\n```\n:::\n\n\n\n:::example 讀答案：總和 = target 的方法數 = ans[target]\n**3. 讀係數當答案。** 答案就是某個特定次方的係數。\n\n```cpp\n// 讀答案：總和 = target 的方法數 = ans[target]\nlong long getAns(vector<long long>& ans, int target) {\n    return target < (int)ans.size() ? ans[target] : 0;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 係數要取模（多半 `1e9 + 7`），每次相乘後 `% MOD`，並用 `long long`。\n- 樸素卷積是 `O(次方範圍^2 * 物件數)`，題目資料不大時夠用；範圍很大才需要 NTT 等進階技巧（多數 LeetCode 題不需要）。\n- 別忘了「空積」初始為 `{1}`（代表選 0 個物件、總量為 0 有 1 種方法）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「生成函式」想：\n\n- **問「湊出某個總量有幾種方法」，而總量是由多個獨立選擇「相加」而成。** 總和、總點數、總逆序對數——只要是相加，就能用次方相加來模擬，像 1155、3193。\n- **每個物件的可選值範圍不大，且彼此獨立。** 這正好對應「每個物件一個小多項式，連乘合併」，例如 3082「求出所有子序列的能量和」。\n- **你已經想到計數 DP，但轉移寫起來像在「把兩堆方案數合併」。** 那其實就是卷積：\n\n:::example 嗅到味道的瞬間：DP 轉移長得像「a[i] * b[j] 累加到 c[i+j]」\n```cpp\n// 嗅到味道的瞬間：DP 轉移長得像「a[i] * b[j] 累加到 c[i+j]」\n// 這就是多項式相乘；用生成函式的語言會更清楚\n```\n:::\n\n\n\n- **答案要對質數取模、且是「方法數 / 方案數」。** 這類計數幾乎都能套到多項式係數上，例如 2518「好分割槽的數目」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我讓多項式的「次方」代表哪個會相加的量？\n2. 每個物件的多項式（係數陣列）長什麼樣？\n3. 我要的答案，是最終多項式的「哪一項係數」？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 629 | K 個逆序對陣列 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1155 | [擲骰子等於目標和的方法數](https://leetcode.cn/problems/number-of-dice-rolls-with-target-sum/) | 1654 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2518 | [好分割槽的數目](https://leetcode.cn/problems/number-of-great-partitions/) | 2415 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3082 | [求出所有子序列的能量和](https://leetcode.cn/problems/find-the-sum-of-the-power-of-all-subsequences/) | 2242 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3193 | [統計逆序對的數目](https://leetcode.cn/problems/count-the-number-of-inversions/) | 2266 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 629 | [K 個逆序對陣列](https://leetcode.cn/problems/k-inverse-pairs-array) |  |\n| 1155 | [擲骰子等於目標和的方法數](https://leetcode.cn/problems/number-of-dice-rolls-with-target-sum) | 1654 |\n| 2518 | [好分割槽的數目](https://leetcode.cn/problems/number-of-great-partitions) | 2415 |\n| 3082 | [求出所有子序列的能量和](https://leetcode.cn/problems/find-the-sum-of-the-power-of-all-subsequences) | 2242 |\n| 3193 | [統計逆序對的數目](https://leetcode.cn/problems/count-the-number-of-inversions) | 2266 |\n| 3333 | [找到初始輸入字串 II](https://leetcode.cn/problems/find-the-original-typed-string-ii) | 2629 |",
    },
    {
      id: 20,
      title: "18. 機率期望",
      summary:
        "**原理講解**\n\n本節要解決的場景是：題目裡有「隨機」與「平均」，問「期望值是多少」「機率是多少」。期望（expectation）可以白話理解成「如果重複玩很多很多次，平均會得到的結果」。\n\n:::example 期望定義：擲一顆公平骰子的點數期望\n最基本的工具是兩條規則。第一，期望的定義：**每個結果的值，乘上它發生的機率，全部加起來。**\n\n```cpp\n// 期望定義：擲一顆公平骰子的點數期望\n// E = Σ (值 × 機率) = 1*(1/6)+2*(1/6)+...+6*(1/6) = 3.5\ndouble diceExpectation() {\n    double e = 0;\n    for (int v = 1; v <= 6; ++v) {\n        e += v * (1.0 / 6);\n    }\n    return e;  // 3.5\n}\n```\n:::\n\n\n\n:::example 機率遞推：第 k 步落在 (r,c) 的機率 = 各前驅格機率 / 8 之和\n第二，也是解題最常用的：**期望可以用「遞推」算。** 一個狀態的期望，等於「往各個下一步走的機率」乘上「下一步的期望」，再加上這一步本身的代價。這讓我們可以像走迷宮一樣，從「已知結果的終點」往回推。688「騎士在棋盤上的機率」就是：第 `k` 步在某格的機率，等於「上一步在相鄰各格的機率，各乘 `1/8` 再相加」。\n\n```cpp\n// 機率遞推：第 k 步落在 (r,c) 的機率 = 各前驅格機率 / 8 之和\n// f[k][r][c] = Σ f[k-1][pr][pc] / 8   （pr,pc 是能跳到 (r,c) 的格子）\n```\n:::\n\n\n\n關鍵心法是：**先把「狀態」定義好（通常是某個局面），再寫出「這個狀態的期望/機率 = 由它能轉移到的狀態加權而來」。** 808「分湯」、LCR 185「統計結果機率」都是先定狀態、再列轉移。會列轉移式，題目就解了一大半。\n\n**實作方式**\n\n機率期望題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 狀態：f[剩餘步數 k][位置 r][位置 c] = 仍留在棋盤上的機率\n**1. 定義狀態。** 把「目前局面」濃縮成幾個變數：第幾步、在哪、剩多少湯…。狀態就是 DP 表的索引。\n\n```cpp\n// 狀態：f[剩餘步數 k][位置 r][位置 c] = 仍留在棋盤上的機率\n// 終止：k = 0 時，只要還在棋盤上，機率記為 1\ndouble f[K + 1][N][N];\n```\n:::\n\n\n\n:::example 轉移：每一步等機率往 8 個方向跳，越界就掉出棋盤（貢獻 0）\n**2. 寫轉移式（期望/機率的遞推）。** 從一個狀態，列出它會走到哪些狀態、各自機率多少，加權求和。\n\n```cpp\n// 轉移：每一步等機率往 8 個方向跳，越界就掉出棋盤（貢獻 0）\nfor (auto [dr, dc] : dirs) {\n    int nr = r + dr, nc = c + dc;\n    if (inBoard(nr, nc)) {\n        f[k][r][c] += f[k - 1][nr][nc] / 8.0;\n    }\n}\n```\n:::\n\n\n\n:::example 讀答案：從起點 (sr,sc) 走 K 步後仍在棋盤上的機率\n**3. 讀答案。** 答案通常是某個起始狀態的期望/機率值。期望類題目若要對質數取模，浮點數要換成「分數 + 模逆元」表示。\n\n```cpp\n// 讀答案：從起點 (sr,sc) 走 K 步後仍在棋盤上的機率\ndouble ans = f[K][sr][sc];\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 終止狀態的值要先填對（例如「步數用完且還在盤上 → 機率 1」），否則整條遞推都錯。\n- 浮點精度：相加很多小機率時誤差會累積，必要時改用 `double` 並避免反覆相除。\n- 取模期望（機率寫成分數時）要用模逆元，不能直接寫小數。\n- 複雜度通常是「狀態數 × 每個狀態的轉移數」。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「機率期望」想：\n\n- **題目出現「期望」「機率」「平均」「隨機」等字眼。** 像 688「騎士在棋盤上的機率」、808「分湯」、LCR 185「統計結果機率」幾乎是明示。\n- **過程是一步步隨機進行，且「下一步只跟現在的局面有關」。** 這種「無記憶」特性正好能用狀態 + 遞推描述：\n\n:::example 嗅到味道的瞬間：下一步只取決於「現在在哪」，與怎麼來的無關\n```cpp\n// 嗅到味道的瞬間：下一步只取決於「現在在哪」，與怎麼來的無關\n// → 定義狀態 + 寫「期望 = Σ 機率 × 下一步期望」的遞推\n```\n:::\n\n\n\n- **能找到明確的「終止局面」，其期望/機率是已知的。** 有了終點才能往回推，例如「步數用完」或「湯分完」。\n- **解法重點提到「先公式化、用小資料驗證」。** 期望題很適合先手算 1~2 步的小例子核對遞推式。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「狀態」是什麼？它能不能完整描述目前局面？\n2. 從一個狀態，我能列出「以多少機率走到哪些狀態」嗎？\n3. 我的終止狀態（邊界）期望/機率填對了嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 九坤-04 | 籌碼遊戲 | N/A | 要先定義必勝/必敗態；練習從終止局面反推轉移。 |\n| LCP 11 | 期望個數統計 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| LCR 185 | 統計結果機率 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 688 | 騎士在棋盤上的機率 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 808 | 分湯 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 九坤-04 | [籌碼遊戲](https://leetcode.cn/problems/I3Gm2h) |  |\n| LCP 11 | [期望個數統計](https://leetcode.cn/problems/qi-wang-ge-shu-tong-ji) |  |\n| LCR 185 | [統計結果機率](https://leetcode.cn/problems/nge-tou-zi-de-dian-shu-lcof) |  |\n| 688 | [騎士在棋盤上的機率](https://leetcode.cn/problems/knight-probability-in-chessboard) |  |\n| 808 | [分湯](https://leetcode.cn/problems/soup-servings) |  |\n| 837 | [新 21 點](https://leetcode.cn/problems/new-21-game) | 2350 |\n| 1227 | [飛機座位分配機率](https://leetcode.cn/problems/airplane-seat-assignment-probability) |  |\n| 1230 | [拋擲硬幣](https://leetcode.cn/problems/toss-strange-coins) | 1809 |\n| 1467 | [兩個盒子中球的顏色數相同的機率](https://leetcode.cn/problems/probability-of-a-two-boxes-having-the-same-number-of-distinct-balls) | 2357 |",
    },
    {
      id: 21,
      title: "19. 機率期望隨機資料下顯著更快的演算法",
      summary:
        "**原理講解**\n\n本節要解決的場景是：一個演算法在「最壞情況」可能很慢，但只要輸入是隨機的（或我們主動把它打亂、用隨機策略），它在「平均情況」就會明顯變快。換句話說，我們用「期望時間」而不是「最壞時間」來衡量，並接受極小機率的慢。\n\n:::example 隨機快速選擇：每次隨機挑基準，期望 O(n)\n最直觀的例子是隨機化的「快速選擇」：要找第 k 小的數，每次隨機挑一個基準把資料一切兩半，只往含答案的那半繼續找。最壞是 `O(n^2)`，但因為基準是隨機挑的，期望只要 `O(n)`。\n\n```cpp\n// 隨機快速選擇：每次隨機挑基準，期望 O(n)\n// 最壞 O(n^2)，但隨機基準讓最壞幾乎不會發生\nint pivot = a[rand() % a.size()];  // 隨機，避免被特意構造的輸入卡住\n```\n:::\n\n\n\n:::example 只推進到「關鍵時刻」，跳過中間無事發生的時間\n另一種更貼近本節例題的味道是：**很多「過程模擬」題，看似要把所有時間點都跑一遍很慢，但只要抓住「真正會改變答案的關鍵時刻」，就能跳過大量無用步驟。** 3494「釀造藥水需要的最少總時間」就是這類：直接逐毫秒模擬會爆掉，但只要追蹤「每個工人何時真正開始下一瓶」這個關鍵量，往前推進即可，均攤下來非常快。\n\n```cpp\n// 只推進到「關鍵時刻」，跳過中間無事發生的時間\n// 與其逐格走，不如直接算出「下一個會變化的點」一步跳過去\nlong long nextStart = max(workerFree[i], prevPotionStart);  // 直接跳到關鍵點\n```\n:::\n\n\n\n核心心法兩條：**(1) 用隨機性把「被惡意輸入卡死」的機率壓到趨近於 0；(2) 用「只處理關鍵事件、跳過無關步驟」把均攤複雜度壓下來。** 兩者都在追求「平均/期望意義下快」。\n\n**實作方式**\n\n這類題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 先有正確基準：逐步模擬，雖慢但保證對\n**1. 先寫出正確但可能慢的暴力解。** 隨機化與「跳關鍵點」都是在「已知正確做法」之上做加速，所以先確保邏輯對。\n\n```cpp\n// 先有正確基準：逐步模擬，雖慢但保證對\nfor (int t = 0; t < totalTime; ++t) {\n    simulateStep(t);  // 慢，但先確認正確\n}\n```\n:::\n\n\n\n:::example 隨機打亂，破壞最壞輸入結構，讓期望變快\n**2. 找出可加速的點：要嘛打亂輸入，要嘛只跳關鍵時刻。** 若怕被特殊輸入卡，就隨機打亂或隨機選基準；若是模擬題，就找出「答案唯一會變的那些時刻」。\n\n```cpp\n// 隨機打亂，破壞最壞輸入結構，讓期望變快\nshuffle(a.begin(), a.end(), mt19937(random_device{}()));\n```\n:::\n\n\n\n:::example 事件推進：算出下一個關鍵時刻，一步到位\n**3. 只在關鍵點推進狀態。** 維護「下一個關鍵時刻」，每次直接跳過去，而不是一格一格走。\n\n```cpp\n// 事件推進：算出下一個關鍵時刻，一步到位\nlong long t = 0;\nfor (auto& ev : events) {\n    t = max(t, ev.ready);  // 直接跳到事件真正能發生的時刻\n    process(ev, t);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 隨機化要說清楚是「期望快」，最壞仍可能慢；競賽/面試需保證正確性不受隨機影響。\n- 「跳關鍵點」要證明每個關鍵點只處理 `O(1)` 次，均攤才成立。\n- 用 `long long` 防溢位（時間/數量級常很大），隨機種子用 `mt19937` 較佳。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「隨機化 / 跳關鍵點加速」想：\n\n- **暴力是逐時間/逐元素模擬，步數巨大但「大多數步什麼都沒發生」。** 這就是只跳關鍵時刻的訊號，像 3494。\n- **最壞情況會被特殊輸入卡死，但題目資料像是隨機或允許隨機策略。** 此時隨機打亂/隨機基準能把期望壓低：\n\n:::example 嗅到味道的瞬間：演算法本身對，但「被排好的輸入」會退化成 O(n^2)\n```cpp\n// 嗅到味道的瞬間：演算法本身對，但「被排好的輸入」會退化成 O(n^2)\n// → 加一次隨機打亂，期望就回到接近線性\n```\n:::\n\n\n\n- **你只關心「平均表現」或「期望步數」，能容忍極低機率的慢。**\n- **狀態變化是稀疏的：兩個關鍵事件之間可以一口氣跳過。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的暴力解是什麼？它慢在「重複跑很多無事發生的步」嗎？\n2. 哪些是「真正會改變答案」的關鍵時刻，我能直接算出下一個嗎？\n3. 若怕被卡，我能不能用隨機打亂讓期望變快、而不影響正確性？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3494 | [釀造藥水需要的最少總時間](https://leetcode.cn/problems/find-the-minimum-amount-of-time-to-brew-potions/) | 2042 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3494 | [釀造藥水需要的最少總時間](https://leetcode.cn/problems/find-the-minimum-amount-of-time-to-brew-potions) | 2042 |",
    },
    {
      id: 22,
      title: "20. 博弈論",
      summary:
        "**原理講解**\n\n本節要解決的場景是：兩個玩家輪流操作，雙方都「絕頂聰明、只想自己贏」，問先手能不能贏、或最後誰贏。這類題叫博弈論（game theory）。它的解法核心，是把每個「局面」標記成「必勝態」或「必敗態」。\n\n定義超級簡單，只有兩條：\n\n- **必敗態（P）**：輪到你、但你不管怎麼走都會輸的局面（包含「無路可走」這種終止局面）。\n- **必勝態（N）**：輪到你、且你存在某一步能讓對手陷入「必敗態」的局面。\n\n:::example 兩條核心規則\n```cpp\n// 兩條核心規則\n// 一個局面是「必勝」← 只要存在一步能走到「對手的必敗局面」\n// 一個局面是「必敗」← 它能走到的所有局面，對手都是必勝\n```\n:::\n\n\n\n:::example Nim 遊戲（每次取 1~3 顆）的結論：n % 4 == 0 先手必敗\n換句話說：**你贏，當且僅當你能把對手丟進一個他必輸的局面。** 這就把「誰會贏」變成一個可以從終點往回推的標記問題。292「Nim 遊戲」就是經典：一堆 n 顆石頭、每次拿 1~3 顆、拿到最後一顆者贏；手算小局面會發現 `n % 4 == 0` 時先手必敗，其餘必勝。\n\n```cpp\n// Nim 遊戲（每次取 1~3 顆）的結論：n % 4 == 0 先手必敗\nbool firstWins(int n) {\n    return n % 4 != 0;  // 先用 dp 推小局面，再觀察出這個規律\n}\n```\n:::\n\n\n\n實作上有兩條路：局面少時，直接用記憶化遞迴/DP 把每個局面標 P 或 N（如 486「預測贏家」、464「我能贏嗎」）；局面有結構時，手算小例子找規律湊出公式（如 Nim）。兩條路本質一樣，都是「從終止局面往回判定」。\n\n**實作方式**\n\n博弈題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 狀態：以「剩餘可選數字集合」為例，用 bitmask 表示\n**1. 定義局面（狀態）。** 把「現在輪到誰、盤面長怎樣」濃縮成索引。常見是「區間 `[l, r]`」「剩餘集合（用 bitmask）」「剩餘數量」。\n\n```cpp\n// 狀態：以「剩餘可選數字集合」為例，用 bitmask 表示\n// used 的第 i 位 = 1 代表數字 i 已被選走（464 我能贏嗎）\nint memo[1 << 20];  // -1 未算, 0 必敗, 1 必勝\n```\n:::\n\n\n\n:::example 判定：當前玩家是否必勝\n**2. 寫「必勝/必敗」的遞迴判定。** 嘗試每一個合法走法；只要有一步能讓對手變必敗，自己就是必勝。\n\n```cpp\n// 判定：當前玩家是否必勝\nbool win(int state) {\n    if (memo[state] != -1) {\n        return memo[state];\n    }\n    for (int move : legalMoves(state)) {\n        if (!win(apply(state, move))) {  // 對手變必敗 → 我必勝\n            return memo[state] = 1;\n        }\n    }\n    return memo[state] = 0;  // 所有走法對手都必勝 → 我必敗\n}\n```\n:::\n\n\n\n:::example 區間博弈：dp[l][r] = 在 [l,r] 上，先手淨領先分數\n**3. 區間博弈用「對手會最小化我的所得」表達。** 像 486「預測贏家」是雙方輪流從兩端拿，要記「先手能比後手多拿多少」。\n\n```cpp\n// 區間博弈：dp[l][r] = 在 [l,r] 上，先手淨領先分數\n// dp[l][r] = max(a[l] - dp[l+1][r], a[r] - dp[l][r-1]);\n// 拿走一端後，下一輪「對手」變先手，故減去其 dp\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 終止局面要先填對：無合法走法者為必敗。\n- 狀態用 bitmask 時注意 `1 << n` 別溢位，記憶化陣列開夠大。\n- 複雜度 ≈ 狀態數 × 每態走法數；若狀態爆炸（如 Nim 的 n 很大），就改找公式。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「博弈論」想：\n\n- **兩人輪流操作，且雙方都「最優策略」。** 「Alice、Bob 輪流」「兩人都絕頂聰明」幾乎是明示，像 292、486、464、LCP 48「無限棋局」。\n- **問的是「先手會贏嗎 / 最後誰贏」這種勝負判定。**\n- **每個局面的合法走法有限，能往回推。** 這時就用「必勝 ← 能走到對手必敗」判定：\n\n:::example 嗅到味道的瞬間：問「誰會贏」而非「最大得分」\n```cpp\n// 嗅到味道的瞬間：問「誰會贏」而非「最大得分」\n// → 把每個局面標成必勝/必敗，從終止局面往回推\n```\n:::\n\n\n\n- **資料量小（局面數可枚舉）→ 記憶化 DP；資料量大但有結構 → 手算小例子找公式（如 Nim 的 mod 規律）。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「局面狀態」是什麼？終止局面（無路可走）是必勝還是必敗？\n2. 我能列出每個局面的所有合法走法嗎？\n3. 我是用記憶化判定 P/N，還是手算小局面找到了公式？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 48 | 無限棋局 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 292 | Nim 遊戲 | N/A | 要先定義必勝/必敗態；練習從終止局面反推轉移。 |\n| 294 | 翻轉遊戲 II（Premium） | N/A | 要先定義必勝/必敗態；練習從終止局面反推轉移。 |\n| 464 | 我能贏嗎 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 486 | 預測贏家 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 292. Nim 遊戲 展示核心寫法。\n\n**完整題目**：兩人輪流從一堆石子中拿 1 到 3 顆，拿到最後一顆者勝，判斷先手在 `n` 顆石子時是否必勝。\n\n:::example Nim 博弈：只要石頭數不是 4 的倍數，先手必勝\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        // Nim 博弈：只要石頭數不是 4 的倍數，先手必勝\n        bool canWinNim(int n) { return n % 4 != 0; }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 必敗態是能被 4 整除的局面；先手若不是必敗態，就能拿走若干石子把對手送到必敗態。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- Nim 異或和判勝負。\n- 區間博弈用 DP 比較雙方分差。\n- 可拆成獨立子遊戲時用 SG 異或。\n\n**實作注意事項**\n\n- 明確正常玩法或反常玩法。\n- 終止態先定義清楚。\n- 記憶化避免重複搜尋局面。\n- 分數型博弈與勝負型博弈狀態不同。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 48 | [無限棋局](https://leetcode.cn/problems/fsa7oZ) |  |\n| 292 | [Nim 遊戲](https://leetcode.cn/problems/nim-game) |  |\n| 294 | [翻轉遊戲 II](https://leetcode.cn/problems/flip-game-ii) |  |\n| 464 | [我能贏嗎](https://leetcode.cn/problems/can-i-win) |  |\n| 486 | [預測贏家](https://leetcode.cn/problems/predict-the-winner) |  |\n| 810 | [黑板異或遊戲](https://leetcode.cn/problems/chalkboard-xor-game) |  |\n| 877 | [石子游戲](https://leetcode.cn/problems/stone-game) | 1590 |\n| 913 | [貓和老鼠](https://leetcode.cn/problems/cat-and-mouse) | 2567 |\n| 1025 | [除數博弈](https://leetcode.cn/problems/divisor-game) | 1435 |\n| 1140 | [石子游戲 II](https://leetcode.cn/problems/stone-game-ii) | 2035 |\n| 1406 | [石子游戲 III](https://leetcode.cn/problems/stone-game-iii) | 2027 |\n| 1510 | [石子游戲 IV](https://leetcode.cn/problems/stone-game-iv) | 1787 |\n| 1563 | [石子游戲 V](https://leetcode.cn/problems/stone-game-v) | 2087 |\n| 1686 | [石子游戲 VI](https://leetcode.cn/problems/stone-game-vi) | 2001 |\n| 1690 | [石子游戲 VII](https://leetcode.cn/problems/stone-game-vii) | 1951 |\n| 1728 | [貓和老鼠 II](https://leetcode.cn/problems/cat-and-mouse-ii) | 2849 |\n| 1872 | [石子游戲 VIII](https://leetcode.cn/problems/stone-game-viii) | 2440 |\n| 1908 | [Nim 遊戲 II](https://leetcode.cn/problems/game-of-nim) |  |\n| 1927 | [求和遊戲](https://leetcode.cn/problems/sum-game) | 2005 |\n| 2005 | [斐波那契樹的移除子樹遊戲](https://leetcode.cn/problems/subtree-removal-game-with-fibonacci-tree) |  |\n| 2029 | [石子游戲 IX](https://leetcode.cn/problems/stone-game-ix) | 2277 |\n| 2038 | [如果相鄰兩個顏色均相同則刪除當前顏色](https://leetcode.cn/problems/remove-colored-pieces-if-both-neighbors-are-the-same-color) | 1468 |\n| 2868 | [單詞遊戲](https://leetcode.cn/problems/the-wording-game) |  |\n| 3227 | [字串母音遊戲](https://leetcode.cn/problems/vowels-game-in-a-string) | 1452 |\n| 3828 | [刪除子陣列後的最終元素](https://leetcode.cn/problems/final-element-after-subarray-deletions) | 1591 |",
    },
    {
      id: 23,
      title: "21. 點、線",
      summary:
        "**原理講解**\n\n本節要解決的場景是：題目給你平面上的一些「點」與「線」，問共線、相交、角度、面積之類的幾何關係。這類題的核心，是把幾何問題翻成「向量與座標的算術」，並盡量只用加減乘、避開除法與浮點誤差。\n\n:::example 叉積：(x1,y1) × (x2,y2) = x1*y2 - x2*y1\n最常用的兩個工具是「向量」和「叉積」。從點 A 到點 B 的向量就是座標相減 `(Bx - Ax, By - Ay)`。而兩個向量的叉積，能一口氣告訴你它們是否平行、誰在誰的左右側。\n\n```cpp\n// 叉積：(x1,y1) × (x2,y2) = x1*y2 - x2*y1\n// = 0 → 兩向量平行（共線）；>0 / <0 → 在左側 / 右側\nlong long cross(long long x1, long long y1, long long x2, long long y2) {\n    return x1 * y2 - x2 * y1;\n}\n```\n:::\n\n\n\n:::example 三點 A,B,C 是否共線：向量 AB × 向量 AC == 0\n最常見的應用是「判斷三點共線」：A、B、C 共線，等價於向量 AB 與向量 AC 平行，也就是它們的叉積為 0。1232「綴點成線」就是檢查所有點是否都和前兩點共線。\n\n```cpp\n// 三點 A,B,C 是否共線：向量 AB × 向量 AC == 0\nbool collinear(Point A, Point B, Point C) {\n        return cross(B.x - A.x, B.y - A.y, C.x - A.x, C.y - A.y) ==\n                                    0;  // 用乘法判斜率相等，避免除法\n}\n```\n:::\n\n\n\n為什麼不直接比斜率 `(y2 - y1) / (x2 - x1)`？因為除法會遇到「分母為 0（垂直線）」和「浮點誤差」兩個大坑。**改用叉積（純乘減）就能用整數精確判斷，這是幾何題最重要的習慣。** 1610「可見點的最大數目」進一步用「角度 + 滑動視窗」處理視野範圍，但底層仍是把點轉成方向來比較。\n\n**實作方式**\n\n點線題可以拆成四個固定步驟，逐一搭配程式碼。\n\n:::example 點與向量：向量 = 終點 - 起點\n**1. 把點存成座標，把線段/方向存成向量。** 向量一律用「終點減起點」得到。\n\n```cpp\n// 點與向量：向量 = 終點 - 起點\nstruct P {\n    long long x, y;\n};\nP vec(P a, P b) { return {b.x - a.x, b.y - a.y}; }\n```\n:::\n\n\n\n:::example 共線判定（綴點成線 1232 的核心）：所有點都和 p0->p1 共線\n**2. 用叉積做核心判定（共線/左右側/平行）。** 幾乎所有點線判斷都能歸結到叉積的正負或是否為零。\n\n```cpp\n// 共線判定（綴點成線 1232 的核心）：所有點都和 p0->p1 共線\nP d = vec(pts[0], pts[1]);\nfor (auto& p : pts) {\n    if (cross(d.x, d.y, p.x - pts[0].x, p.y - pts[0].y) != 0) {\n        return false;  // 出現不平行 → 不共線\n    }\n}\n```\n:::\n\n\n\n:::example 方向轉角度（弧度），用於角度排序 / 視野範圍\n**3. 需要角度時，用 atan2 把方向轉成角度。** 像 1610 要找「某視角能看到最多幾個點」，就把每個點相對觀察者的方向算成角度再排序。\n\n```cpp\n// 方向轉角度（弧度），用於角度排序 / 視野範圍\ndouble ang = atan2(p.y - eye.y, p.x - eye.x);\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 能用整數叉積就別用浮點斜率：避免除以零與精度誤差。\n- 座標相乘可能溢位，叉積用 `long long`。\n- 角度題注意「環狀」邊界（0 與 2π 相接），常把角度陣列複製一份 +2π 接在後面再滑動視窗。\n- 退化情形要想到：重合的點、垂直線、共點。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「點、線幾何」想：\n\n- **輸入是座標 `(x, y)`，問共線、平行、相交、角度、面積。** 像 1232「綴點成線」、面試題 16.03「交點」、LCP 37「最小矩形面積」。\n- **你想比較「斜率是否相等」。** 別真的去除，改叉積：\n\n:::example 嗅到味道的瞬間：想寫 (y2-y1)/(x2-x1) 比斜率\n```cpp\n// 嗅到味道的瞬間：想寫 (y2-y1)/(x2-x1) 比斜率\n// → 改成 cross 判定，整數精確、無除零無誤差\n```\n:::\n\n\n\n- **題目談「視野 / 角度範圍 / 能看到幾個」。** 多半要把點轉成角度後排序，再配滑動視窗，例如 1610。\n- **解法重點寫「需要先公式化」。** 提醒你先用向量/叉積把幾何關係寫成算式，再下手。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是不是把點之間的關係，化成了「向量」？\n2. 我的核心判定能不能用叉積（純乘減、整數）取代除法？\n3. 若牽涉角度，我有沒有處理好環狀邊界與精度？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 37 | 最小矩形面積 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1232 | [綴點成線](https://leetcode.cn/problems/check-if-it-is-a-straight-line/) | 1247 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 面試題 16.03 | 交點 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1610 | [可見點的最大數目](https://leetcode.cn/problems/maximum-number-of-visible-points/) | 2147 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 面試題 16.13 | 平分正方形 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 37 | [最小矩形面積](https://leetcode.cn/problems/zui-xiao-ju-xing-mian-ji) |  |\n| 1232 | [綴點成線](https://leetcode.cn/problems/check-if-it-is-a-straight-line) | 1247 |\n| 面試題 16.03 | [交點](https://leetcode.cn/problems/intersection-lcci) |  |\n| 1610 | [可見點的最大數目](https://leetcode.cn/problems/maximum-number-of-visible-points) | 2147 |\n| 面試題 16.13 | [平分正方形](https://leetcode.cn/problems/bisect-squares-lcci) |  |\n| 面試題 16.14 | [最佳直線](https://leetcode.cn/problems/best-line-lcci) |  |\n| 2152 | [穿過所有點的所需最少直線數量](https://leetcode.cn/problems/minimum-number-of-lines-to-cover-points) |  |\n| 2280 | [表示一個折線圖的最少線段數](https://leetcode.cn/problems/minimum-lines-to-represent-a-line-chart) | 1681 |",
    },
    {
      id: 24,
      title: "22. 圓",
      summary:
        "**原理講解**\n\n本節要處理的是「圓」相關的計算幾何問題。圓在程式裡其實只需要三個數字就能完整描述：圓心座標 `(cx, cy)` 加上半徑 `r`。所有關於圓的判斷，幾乎都能化簡成「比較某個距離和半徑誰大誰小」。\n\n:::example 判斷點是否落在圓內：比平方，不開根號\n關鍵觀念只有一句話：**一個點 `(px, py)` 在圓內，等價於它到圓心的距離 `≤ r`**。而比距離時，千萬不要真的開根號（`sqrt` 會帶來浮點誤差），而是把兩邊平方，改比「距離的平方」和「半徑的平方」。這樣全程都是整數運算，沒有精度問題。\n\n```cpp\n// 判斷點是否落在圓內：比平方，不開根號\nbool inCircle(long long px, long long py, long long cx, long long cy,\n                            long long r) {\n    long long dx = px - cx, dy = py - cy;\n    return dx * dx + dy * dy <= r * r;  // 距離平方 ≤ 半徑平方\n}\n```\n:::\n\n\n\n為什麼可以平方比較？因為距離永遠 `≥ 0`，半徑也 `≥ 0`，兩個非負數比大小時平方不會改變大小關係。這招幾乎是所有圓題的地基。\n\n:::example 把圓心座標夾進矩形 [x1,x2] x [y1,y2]，得到矩形上最近點\n另一個常見場景是「圓和矩形是否重疊」（如 LeetCode 1401）。技巧是：先把圓心「夾」到矩形範圍內，得到矩形上離圓心最近的點，再判斷這個最近點是否落在圓內。\n\n```cpp\n// 把圓心座標夾進矩形 [x1,x2] x [y1,y2]，得到矩形上最近點\nlong long nx = max(x1, min(cx, x2));  // clamp 到 x 範圍\nlong long ny =\n                max(y1, min(cy, y2));  // clamp 到 y 範圍\n                                                                                                            // 接著用 inCircle 判斷 (nx, ny) 是否在圓內即可\n```\n:::\n\n\n\n**實作方式**\n\n圓題雖然花樣多，但都能拆成幾個固定步驟：\n\n:::example 兩圓是否相交（含相切）：圓心距離平方介於半徑差與半徑和之間\n**1. 把幾何條件翻成「平方距離」的不等式。** 先想清楚「什麼情況算成立」，再用平方距離表達，避免 `sqrt`。\n\n```cpp\n// 兩圓是否相交（含相切）：圓心距離平方介於半徑差與半徑和之間\nlong long d2 = (c1x - c2x) * (c1x - c2x) + (c1y - c2y) * (c1y - c2y);\nbool intersect = d2 <= (r1 + r2) * (r1 + r2) && d2 >= (r1 - r2) * (r1 - r2);\n```\n:::\n\n\n\n**2. 用 `long long` 防溢位。** 座標相減再平方很容易超過 `int` 範圍（例如座標到 1e4，平方就到 1e8，再相加可能逼近上限），統一用 `long long` 最保險。\n\n:::example 枚舉點對 -> 算候選圓心 -> 用 inCircle 計數\n**3. 需要「枚舉圓」時，固定半徑、列舉圓心候選。** 像 1453「圓形靶內最大飛鏢數」這類題，半徑 `r` 固定，最佳圓的邊界一定通過至少兩個點；於是枚舉每一對點，算出以它們為邊界、半徑 `r` 的候選圓心，再數有多少點落在圓內。\n\n```cpp\n// 枚舉點對 -> 算候選圓心 -> 用 inCircle 計數\nfor (int i = 0; i < n; ++i) {\n    for (int j = i + 1; j < n; ++j) {\n        // 由 points[i], points[j] 與半徑 r 解出候選圓心 (cx, cy)\n        int cnt = 0;\n        for (auto& p : points) {\n            if (inCircle(p.x, p.y, cx, cy, r)) {\n                ++cnt;  // 數圓內點\n            }\n        }\n        ans = max(ans, cnt);\n    }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 「相切」算不算重疊？題目通常算，所以不等式用 `<=`，務必看清題意。\n- 需要解圓心座標時會用到浮點與 `sqrt`，這時保留適當精度（如比較時加一個小 `eps`）即可。\n- 枚舉點對的解法是 `O(n^3)`（點對 `O(n^2)` 乘以每次計數 `O(n)`），在 `n` 不大時可接受。\n\n**題目線索（Pattern）**\n\n看到下面這些訊號，就往「圓」這類計算幾何想：\n\n- **題目直接出現圓、半徑、圓心、距離。** 例如 1401「圓和矩形是否有重疊」、1453「圓形靶內的最大飛鏢數量」。\n- **要判斷「點在不在某範圍內」**，而範圍是用「離某中心多遠」定義的——這就是圓的判定，記得用平方距離。\n- **要找一個能覆蓋最多點的固定大小圓盤**：最佳圓的邊界必通過至少兩個點，於是枚舉點對定圓心。\n- **看到座標數值偏大**，第一反應就是換 `long long`、避免 `sqrt`，先把溢位與精度問題堵掉（這也是 LCP 42、3235 這類題的共同陷阱）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要比較的「距離」能不能改成「距離平方」來避開開根號？\n2. 我的座標相乘後會不會超過 `int`？要不要換 `long long`？\n3. 如果要找最佳圓，它的邊界由哪些點決定，我要枚舉什麼？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 42 | 玩具套圈 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1401 | [圓和矩形是否有重疊](https://leetcode.cn/problems/circle-and-rectangle-overlapping/) | 1709 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1453 | [圓形靶內的最大飛鏢數量](https://leetcode.cn/problems/maximum-number-of-darts-inside-of-a-circular-dartboard/) | 2202 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1924 | 安裝柵欄 II（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3235 | [判斷矩形的兩個角落是否可達](https://leetcode.cn/problems/check-if-the-rectangle-corner-is-reachable/) | 3774 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 42 | [玩具套圈](https://leetcode.cn/problems/vFjcfV) |  |\n| 1401 | [圓和矩形是否有重疊](https://leetcode.cn/problems/circle-and-rectangle-overlapping) | 1709 |\n| 1453 | [圓形靶內的最大飛鏢數量](https://leetcode.cn/problems/maximum-number-of-darts-inside-of-a-circular-dartboard) | 2202 |\n| 1924 | [安裝柵欄 II](https://leetcode.cn/problems/erect-the-fence-ii) |  |\n| 3235 | [判斷矩形的兩個角落是否可達](https://leetcode.cn/problems/check-if-the-rectangle-corner-is-reachable) | 3774 |",
    },
    {
      id: 25,
      title: "23. 矩形、多邊形",
      summary:
        "**原理講解**\n\n本節處理「矩形、多邊形」的幾何判斷，例如算重疊面積、判斷兩矩形是否相交、檢查四個點是否構成正方形等。和圓一樣，核心是把幾何問題化成座標上的整數比較，能不用浮點就不用。\n\n:::example 兩個軸對齊矩形是否重疊（只碰到邊界不算）\n先看最常用的工具：**軸對齊矩形（邊都平行座標軸）只要記四個數**——左下角 `(x1, y1)` 與右上角 `(x2, y2)`。兩個這種矩形「是否重疊」有個極簡判斷：它們在 x 方向要有交集，y 方向也要有交集，缺一不可。\n\n```cpp\n// 兩個軸對齊矩形是否重疊（只碰到邊界不算）\nbool overlap(int ax1, int ay1, int ax2, int ay2, int bx1, int by1, int bx2,\n                                                    int by2) {\n        bool xCross = ax1 < bx2 && bx1 < ax2;  // x 區間有重疊\n        bool yCross = ay1 < by2 && by1 < ay2;  // y 區間有重疊\n        return xCross && yCross;               // 兩方向都重疊才算\n}\n```\n:::\n\n\n\n理解這條判斷的訣竅：與其去想「怎樣會重疊」（情況很多），不如反過來想「怎樣不會重疊」——一個在另一個的左邊、右邊、上面或下面其中之一即可分開。把「不重疊」取反，就得到上面簡潔的條件。\n\n:::example 重疊矩形的寬與高（交集長度，負數代表沒交集）\n算「重疊面積」也類似：重疊區域本身仍是一個矩形，它的寬是兩矩形 x 區間的交集長度，高是 y 區間的交集長度。\n\n```cpp\n// 重疊矩形的寬與高（交集長度，負數代表沒交集）\nint w = min(ax2, bx2) - max(ax1, bx1);  // x 方向交集\nint h = min(ay2, by2) - max(ay1, by1);  // y 方向交集\nlong long area = (w > 0 && h > 0) ? (long long)w * h : 0;\n```\n:::\n\n\n\n**實作方式**\n\n矩形、多邊形題可依下面步驟拆解：\n\n**1. 選定矩形的表示法。** 軸對齊矩形用「左下 + 右上」兩個角最方便；一般四邊形 / 多邊形則存頂點陣列，按順序（順時針或逆時針）排好。\n\n**2. 把判斷拆成 x、y 兩個一維問題。** 軸對齊的好處就是 x 和 y 互不干擾，可以分開處理重疊、相交、包含等關係（如上面的 `overlap`）。\n\n:::example 用平方距離判正方形：6 個距離平方裡，最小值出現 4 次、最大值 2 次\n**3. 處理一般四邊形時，用「邊長 + 對角線」或向量。** 例如 593「有效的正方形」：四個點兩兩之間有 6 段距離，正方形恰好是「4 段短邊相等、2 段對角線相等、且對角線 = 短邊 ×√2」。一樣用平方距離避免開根號。\n\n```cpp\n// 用平方距離判正方形：6 個距離平方裡，最小值出現 4 次、最大值 2 次\nlong long d2(int x1, int y1, int x2, int y2) {\n    long long dx = x1 - x2, dy = y1 - y2;\n    return dx * dx + dy * dy;  // 距離平方，整數運算\n}\n// 收集 4 點兩兩的 6 個 d2，排序後檢查相等關係即可\n```\n:::\n\n\n\n:::example 鞋帶公式：頂點需按順序排列，結果取絕對值除以 2\n**4. 算多邊形面積用鞋帶公式（Shoelace）。** 任意簡單多邊形，按頂點順序套公式即可，無需切三角形。\n\n```cpp\n// 鞋帶公式：頂點需按順序排列，結果取絕對值除以 2\nlong long twiceArea = 0;\nfor (int i = 0; i < n; ++i) {\n    auto& a = poly[i];\n    auto& b = poly[(i + 1) % n];  // 下一個頂點，最後一個接回第 0 個\n    twiceArea += (long long)a.x * b.y - (long long)b.x * a.y;\n}\ndouble area = abs(twiceArea) / 2.0;\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 「碰到邊算不算重疊」要看題意：836 不算（用 `<`），有些題算（用 `<=`）。\n- 距離、面積乘起來容易溢位，統一 `long long`。\n- 上述判斷多半是 `O(1)`～`O(n)`，瓶頸通常在外層枚舉（例如 939「最小面積矩形」要枚舉點對找對角，配雜湊表約 `O(n^2)`）。\n\n**題目線索（Pattern）**\n\n出現以下訊號，就往「矩形、多邊形」想：\n\n- **題目明說矩形、正方形、多邊形、頂點座標。** 例如 223「矩形面積」、836「矩形重疊」、593「有效的正方形」、939「最小面積矩形」。\n- **要判斷「相交 / 包含 / 重疊」**：若是軸對齊矩形，拆成 x、y 兩個一維區間判斷，最快也最不易錯。\n- **要算面積**：軸對齊用區間交集相乘；任意多邊形用鞋帶公式。\n- **要驗證幾何形狀（正方形、矩形）**：改用「兩兩平方距離」的多重集合特徵來判斷，避開浮點。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的矩形是不是軸對齊？能不能拆成 x、y 兩個一維問題？\n2. 我要的關係（重疊 / 包含）用「不成立的反面」是不是更好寫？\n3. 我所有乘法 / 面積會不會溢位，要不要 `long long`？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 223 | 矩形面積 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 469 | 凸多邊形（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 593 | 有效的正方形 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 836 | [矩形重疊](https://leetcode.cn/problems/rectangle-overlap/) | 1443 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 939 | [最小面積矩形](https://leetcode.cn/problems/minimum-area-rectangle/) | 1752 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 223 | [矩形面積](https://leetcode.cn/problems/rectangle-area) |  |\n| 469 | [凸多邊形](https://leetcode.cn/problems/convex-polygon) |  |\n| 593 | [有效的正方形](https://leetcode.cn/problems/valid-square) |  |\n| 836 | [矩形重疊](https://leetcode.cn/problems/rectangle-overlap) | 1443 |\n| 939 | [最小面積矩形](https://leetcode.cn/problems/minimum-area-rectangle) | 1752 |\n| 963 | [最小面積矩形 II](https://leetcode.cn/problems/minimum-area-rectangle-ii) | 1991 |\n| 3625 | [統計梯形的數目 II](https://leetcode.cn/problems/count-number-of-trapezoids-ii) | 2643 |",
    },
    {
      id: 26,
      title: "24. 凸包",
      summary:
        "**原理講解**\n\n本節要解決的是「凸包（convex hull）」：給一堆平面上的點，找出能把它們全部圍住的「最小凸多邊形」。想像把所有點當成釘在木板上的釘子，拿一條橡皮筋套住最外圈再放開，橡皮筋勒出的形狀就是凸包。\n\n:::example 叉積：判斷 O->A->B 的轉向\n要算凸包，得先掌握一個最基礎的工具——**叉積（cross product），它能判斷三個點的轉向是左轉、右轉還是共線**。對於從 `O` 走到 `A` 再走到 `B`：\n\n```cpp\n// 叉積：判斷 O->A->B 的轉向\n// > 0 左轉(逆時針)，< 0 右轉(順時針)，= 0 三點共線\nlong long cross(const Point& O, const Point& A, const Point& B) {\n        return (long long)(A.x - O.x) * (B.y - O.y) -\n                                    (long long)(A.y - O.y) * (B.x - O.x);\n}\n```\n:::\n\n\n\n:::example 加入新點 p 時，若會造成內凹(非左轉)，就彈出棧頂\n凸包的邊界恰好是「一路都往同一個方向轉」的折線：例如沿著下半圈走時，每一步都應該左轉（逆時針）。一旦發現某個點讓我們「轉錯方向」（往內凹），就代表前一個點其實不在凸包上，應該被丟掉。這個「丟掉內凹點」的動作，正好用一個堆疊（stack）來做，就是經典的 **Andrew's monotone chain（單調鏈）演算法**。\n\n```cpp\n// 加入新點 p 時，若會造成內凹(非左轉)，就彈出棧頂\nwhile (k >= 2 && cross(hull[k - 2], hull[k - 1], p) <= 0) {\n    k--;  // 棧頂那個點不在凸包上，丟掉\n}\nhull[k++] = p;  // 再把 p 放進來\n```\n:::\n\n\n\n整個流程：先把點按座標排序，從左到右掃一遍建「下半凸包」，再從右到左掃一遍建「上半凸包」，兩半接起來就是完整凸包。因為只是排序加兩趟線性掃描，總複雜度 `O(n log n)`（瓶頸在排序）。\n\n**實作方式**\n\n凸包題可拆成下面步驟：\n\n:::example 按 x 為主、y 為輔排序，作為單調鏈的掃描順序\n**1. 準備叉積與點的排序。** 叉積用整數避免精度問題；排序以 x 為主、y 為輔。\n\n```cpp\n// 按 x 為主、y 為輔排序，作為單調鏈的掃描順序\nsort(pts.begin(), pts.end(), [](const Point& a, const Point& b) {\n    return a.x < b.x || (a.x == b.x && a.y < b.y);\n});\n```\n:::\n\n\n\n:::example 下半凸包：由左至右\n**2. 建下半凸包：從左到右掃。** 維護一個棧，新點若讓折線不再左轉就彈出。\n\n```cpp\n// 下半凸包：由左至右\nfor (int i = 0; i < n; ++i) {\n    while (k >= 2 && cross(hull[k - 2], hull[k - 1], pts[i]) <= 0) {\n        k--;\n    }\n    hull[k++] = pts[i];\n}\n```\n:::\n\n\n\n:::example 上半凸包：由右至左；lower 記住下半結束時的棧高\n**3. 建上半凸包：從右到左掃。** 邏輯一模一樣，只是反向再走一遍，把「上面那條邊界」補齊。\n\n```cpp\n// 上半凸包：由右至左；lower 記住下半結束時的棧高\nint lower = k + 1;\nfor (int i = n - 2; i >= 0; i--) {\n    while (k >= lower && cross(hull[k - 2], hull[k - 1], pts[i]) <= 0) {\n        k--;\n    }\n    hull[k++] = pts[i];\n}\n// 最後 hull[0..k-1] 即凸包頂點（首尾點重複，視需要去掉一個）\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 共線點要不要保留？若題目要「最少頂點」就用嚴格 `<` 過濾共線（叉積 `== 0` 視為內凹丟掉）；若要「圍籬上所有釘子」（如 587 安裝柵欄）則保留共線點，把判斷改成 `< 0`。\n- 點數太少（少於 3 個）要特判，直接全部回傳。\n- 整體 `O(n log n)`，排序是瓶頸，兩趟掃描各 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到以下訊號就往「凸包」想：\n\n- **要找「能把所有點包住的最外圈邊界 / 最小凸多邊形」。** 例如 587「安裝柵欄」就是直接求凸包。\n- **要找「面積最大」或「最外側」的點組合**：最大三角形、最遠點對等，最優解的頂點通常都落在凸包上（如 812「最大三角形面積」可先求凸包再枚舉）。\n- **題目給一堆平面座標，且關心整體輪廓 / 形狀**，而不是單點關係。\n- **你發現需要反覆判斷「三點是左轉還是右轉」**——這就是叉積與凸包的招牌動作。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我真正要的是「最外圈輪廓」嗎？還是只是某種距離關係？\n2. 我的叉積能不能用整數算，避開浮點誤差？\n3. 共線的點，我這題要保留還是丟掉？這決定 `<=` 還是 `<`。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 15 | 遊樂園的迷宮 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 587 | 安裝柵欄 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 812 | 最大三角形面積 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 15 | [遊樂園的迷宮](https://leetcode.cn/problems/you-le-yuan-de-mi-gong) |  |\n| 587 | [安裝柵欄](https://leetcode.cn/problems/erect-the-fence) |  |\n| 812 | [最大三角形面積](https://leetcode.cn/problems/largest-triangle-area) |  |",
    },
    {
      id: 27,
      title: "25. 隨機數",
      summary:
        "**原理講解**\n\n本節談「隨機數」相關的設計題，主軸是兩件事：一是設計支援「`O(1)` 等機率隨機取元素」的資料結構（如 380、381），二是「等機率取樣 / 洗牌」的演算法（如 382 連結串列隨機節點、384 打亂陣列、398 隨機數索引）。\n\n:::example O(1) 刪除：把目標和尾端交換，再砍掉尾端\n先看第一個重點。要「`O(1)` 隨機取一個元素」，最自然的容器是 `vector`，因為 `vec[randomIndex]` 是 `O(1)`。但 `vector` 中間刪除是 `O(n)`。這個技巧的核心招數是：**刪除時不要真的搬移，而是把「要刪的元素」和「最後一個元素」交換，再 `pop_back`。** 配一個雜湊表記「值 → 在 vector 裡的下標」，就能 `O(1)` 完成插入、刪除、隨機取。\n\n```cpp\n// O(1) 刪除：把目標和尾端交換，再砍掉尾端\nint i = pos[val];       // 找到 val 的下標\nnums[i] = nums.back();  // 把尾端元素搬到這個洞\npos[nums[i]] = i;       // 更新被搬動元素的下標\nnums.pop_back();        // 砍掉尾端，O(1)\npos.erase(val);\n```\n:::\n\n\n\n:::example 蓄水池抽樣：邊掃邊以 1/i 機率替換，保證最後每個元素等機率\n第二個重點是**蓄水池抽樣（reservoir sampling）**：在「資料邊讀邊來、總數未知、且不能全部存下來」時，仍能讓每個元素被選中的機率相等。規則是看到第 `i` 個元素（從 1 數起）時，以 `1/i` 的機率用它替換目前保留的答案。\n\n```cpp\n// 蓄水池抽樣：邊掃邊以 1/i 機率替換，保證最後每個元素等機率\nint chosen = -1, i = 0;\nfor (int x : stream) {\n    ++i;\n    if (rand() % i == 0) {\n        chosen = x;  // 以 1/i 的機率選 x\n    }\n}\n```\n:::\n\n\n\n為什麼公平？第 `k` 個元素最後留下的機率 = 它一開始被選中（`1/k`）× 之後每一步都沒被替換掉，乘開後恰好等於 `1/n`，對所有 `k` 都一樣，所以等機率。\n\n**實作方式**\n\n依題型分別套對應步驟：\n\n:::example 結構：vector 存值、map 存「值 -> 下標」\n**1.「O(1) 增刪 + 隨機取」用「vector + 雜湊表」雙結構。** vector 負責隨機索引，map 負責 `O(1)` 定位。\n\n```cpp\n// 結構：vector 存值、map 存「值 -> 下標」\nvector<int> nums;\nunordered_map<int, int> pos;  // val -> index in nums\n// getRandom：直接抽下標\nint getRandom() { return nums[rand() % nums.size()]; }\n```\n:::\n\n\n\n**2. 插入只往尾端加，刪除靠「換尾再彈出」。** 插入 `O(1)`，刪除用上面那段交換技巧。允許重複的 381 則把 `pos` 改成 `unordered_map < int, unordered_set < int >>`（一個值對應多個下標）。\n\n**3. 串流 / 連結串列隨機取用蓄水池抽樣。** 382（連結串列隨機節點）、398（隨機數索引）都適用，邊走邊以 `1/i` 替換，不需先知道長度、不需額外大空間。\n\n:::example Fisher–Yates 洗牌：每個排列等機率出現\n**4. 等機率洗牌用 Fisher–Yates。** 384「打亂陣列」就是它：從後往前，每個位置和它「自己或前面任一位置」隨機交換。\n\n```cpp\n// Fisher–Yates 洗牌：每個排列等機率出現\nfor (int i = n - 1; i > 0; i--) {\n    int j = rand() % (i + 1);  // 從 [0, i] 等機率挑一個\n    swap(a[i], a[j]);\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 機率邊界要小心：蓄水池是 `rand() % i`（從 1 起算），洗牌是 `rand() % (i + 1)`（含自己），少一位就不再等機率。\n- 380/381 所有操作均攤 `O(1)`；蓄水池與洗牌都是 `O(n)` 時間、`O(1)` 額外空間。\n\n**題目線索（Pattern）**\n\n看到以下訊號往「隨機數」想：\n\n- **要設計一個資料結構，同時要 `O(1)` 插入、刪除、隨機取一個元素。** 這是 380/381 的招牌——立刻想到「vector + 雜湊表 + 換尾刪除」。\n- **資料像水流一樣一個一個來，長度未知或不能整個存下**，卻要等機率取樣：用蓄水池抽樣（382、398）。\n- **要「公平地打亂 / 重排」一個陣列**：用 Fisher–Yates（384）。\n- **題目反覆出現「等機率」「隨機」「random」字眼**，重點往往在「怎麼保證每個元素機率相同」，而非演算法多難。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要的是「快速隨機取」還是「公平取樣 / 洗牌」？對應不同模板。\n2. 若要 `O(1)` 刪除，我能不能用「和尾端交換再 pop」避開搬移？\n3. 我的隨機範圍（`% i` 還是 `% (i + 1)`）有沒有讓每個元素機率真的相等？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 380 | $O(1)$ 時間插入、刪除和獲取隨機元素 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 381 | $O(1)$ 時間插入、刪除和獲取隨機元素 - 允許重複 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 382 | 連結串列隨機節點 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 384 | 打亂陣列 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 398 | 隨機數索引 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 380 | [$O(1)$ 時間插入、刪除和獲取隨機元素](https://leetcode.cn/problems/insert-delete-getrandom-o1) |  |\n| 381 | [$O(1)$ 時間插入、刪除和獲取隨機元素 - 允許重複](https://leetcode.cn/problems/insert-delete-getrandom-o1-duplicates-allowed) |  |\n| 382 | [連結串列隨機節點](https://leetcode.cn/problems/linked-list-random-node) |  |\n| 384 | [打亂陣列](https://leetcode.cn/problems/shuffle-an-array) |  |\n| 398 | [隨機數索引](https://leetcode.cn/problems/random-pick-index) |  |\n| 470 | [用 Rand7() 實現 Rand10()](https://leetcode.cn/problems/implement-rand10-using-rand7) |  |\n| 478 | [在圓內隨機生成點](https://leetcode.cn/problems/generate-random-point-in-a-circle) |  |\n| 497 | [非重疊矩形中的隨機點](https://leetcode.cn/problems/random-point-in-non-overlapping-rectangles) |  |\n| 519 | [隨機翻轉矩陣](https://leetcode.cn/problems/random-flip-matrix) |  |\n| 528 | [按權重隨機選擇](https://leetcode.cn/problems/random-pick-with-weight) |  |\n| 710 | [黑名單中的隨機數](https://leetcode.cn/problems/random-pick-with-blacklist) |  |",
    },
    {
      id: 28,
      title: "26. 隨機化技巧",
      summary:
        "**原理講解**\n\n本節談「隨機化技巧」：在演算法裡刻意加入隨機，用「猜 + 驗證」換取又快又短的解。它和上一節「隨機數設計題」不同——這裡的隨機不是題目要求，而是我們主動拿來「賭一把」的工具。\n\n核心想法是：**當答案在資料裡佔比夠高時，隨機抽一個就很可能抽中；抽中與否可以 `O(n)` 快速驗證，沒中就再抽一次。** 因為每次抽中的機率不低，期望只要抽幾次就會成功，整體仍然很快。\n\n:::example 隨機猜眾數：抽一個 -> 數出現次數 -> 過半就回傳\n最典型的例子是「找出現次數過半的元素」。既然超過一半都是答案，隨機抽一個有超過 50% 機率就是答案；抽到後數一遍驗證即可。\n\n```cpp\n// 隨機猜眾數：抽一個 -> 數出現次數 -> 過半就回傳\nwhile (true) {\n    int cand = nums[rand() % nums.size()];  // 隨機抽一個當候選\n    int cnt = 0;\n    for (int x : nums) {\n        if (x == cand) {\n            ++cnt;  // O(n) 驗證\n        }\n    }\n    if (cnt * 2 > nums.size()) {\n        return cand;  // 過半 -> 確定是答案\n    }\n}\n```\n:::\n\n\n\n為什麼夠快？每次抽中的機率 `> 1/2`，抽 `k` 次都沒中的機率小於 `1/2^k`，幾次之後就趨近於零。所以期望抽不到兩次就成功，每次驗證 `O(n)`，期望總時間是 `O(n)`。961「找重複 N 次的元素」（2N 長度裡有 N 個相同）也是同理：隨機抽兩個位置看是否相等，命中率極高。\n\n:::example 隨機構造：隨機生成 -> 檢查 -> 不合法就重試\n第二類隨機化是「**隨機建構（randomized construction）**」：題目要你「構造一個滿足某條件的答案」，而符合的答案很多、隨便弄一個多半就行——那就隨機生成再檢查，不行就重來。\n\n```cpp\n// 隨機構造：隨機生成 -> 檢查 -> 不合法就重試\nwhile (true) {\n    auto ans = randomBuild();  // 隨機產生一個候選解\n    if (valid(ans)) {\n        return ans;  // 驗證合法就回傳\n    }\n}\n```\n:::\n\n\n\n**實作方式**\n\n隨機化題的通用骨架是「猜 → 驗 → 重試」，可拆成下面步驟：\n\n**1. 確認「答案佔比高」或「合法解很多」。** 這是隨機化能成立的前提；佔比太低（例如只有一個特解）就不適合硬賭。\n\n:::example 驗證器：給候選，O(n) 判斷對不對\n**2. 寫一個快速的「驗證器」。** 隨機猜本身很快，重點是「驗證一次要夠便宜」（通常 `O(n)`），否則重試多次會拖慢。\n\n```cpp\n// 驗證器：給候選，O(n) 判斷對不對\nbool check(const vector<int>& a) {\n    // 依題意檢查，例如「沒有相鄰元素等於兩側平均」\n    for (int i = 1; i + 1 < a.size(); ++i) {\n        if (2 * a[i] == a[i - 1] + a[i + 1]) {\n            return false;\n        }\n    }\n    return true;\n}\n```\n:::\n\n\n\n:::example 主流程：隨機種子 + 重試迴圈\n**3. 包成 while 迴圈：抽 / 造 → 驗 → 不過就重試。** 加上隨機種子讓每次執行不同。\n\n```cpp\n// 主流程：隨機種子 + 重試迴圈\nsrand((unsigned)chrono::steady_clock::now().time_since_epoch().count());\nwhile (true) {\n    auto cand = guessOrBuild();  // 隨機猜或構造\n    if (check(cand)) {\n        return cand;  // 通過就結束\n    }\n}\n```\n:::\n\n\n\n**4. 有時「打亂輸入」就直接破題。** 像 1968「構造相鄰不等於兩側平均」，把陣列隨機洗牌幾乎一定就滿足條件，驗證後不行再洗一次。\n\n**5. 邊界與複雜度。**\n\n- 要保證迴圈「幾乎一定」會結束：先在腦中估算單次成功機率，太低就別用隨機化。\n- 期望複雜度通常是「單次驗證成本 ÷ 成功機率」，眾數類約 `O(n)`。\n- 用較好的隨機源（如 `mt19937` 配時間種子），避免每次跑出相同序列。\n\n**題目線索（Pattern）**\n\n看到以下訊號可考慮「隨機化」：\n\n- **答案在資料中佔比很高（過半、N/2N 等），而精確解法又繁瑣。** 隨機抽一個來猜再驗證，往往最短最快：961「重複 N 次的元素」、1157「子陣列絕大多數元素」。\n- **題目要「構造任意一個」滿足條件的解，且合法解明顯很多。** 隨機生成 / 洗牌再檢查即可：1968、3680「生成賽程」。\n- **正解難寫但「驗證一個候選」很簡單。** 這種「驗證易、求解難」的落差，正是隨機化發揮的地方（如 1317 隨機湊兩個無零整數，驗證秒判）。\n- **你發現「隨便試一個多半就成功」**——那就把「隨便試」正式寫成「隨機 + 重試」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的答案，在資料裡佔比是不是夠高，使得隨機抽很容易命中？\n2. 我能不能寫出一個 `O(n)` 的快速驗證器？\n3. 單次成功的機率大概多少？重試迴圈會不會幾乎一定結束？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 961 | [在長度 2N 的陣列中找出重複 N 次的元素](https://leetcode.cn/problems/n-repeated-element-in-size-2n-array/) | 1162 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1157 | [子陣列中佔絕大多數的元素](https://leetcode.cn/problems/online-majority-element-in-subarray/) | 2205 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1317 | [將整數轉換為兩個無零整數的和](https://leetcode.cn/problems/convert-integer-to-the-sum-of-two-no-zero-integers/) | 1278 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1968 | [構造元素不等於兩相鄰元素平均值的陣列](https://leetcode.cn/problems/array-with-elements-not-equal-to-average-of-neighbors/) | 1499 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3680 | [生成賽程](https://leetcode.cn/problems/generate-schedule/) | 2378 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 961 | [在長度 2N 的陣列中找出重複 N 次的元素](https://leetcode.cn/problems/n-repeated-element-in-size-2n-array) | 1162 |\n| 1157 | [子陣列中佔絕大多數的元素](https://leetcode.cn/problems/online-majority-element-in-subarray) | 2205 |\n| 1317 | [將整數轉換為兩個無零整數的和](https://leetcode.cn/problems/convert-integer-to-the-sum-of-two-no-zero-integers) | 1278 |\n| 1968 | [構造元素不等於兩相鄰元素平均值的陣列](https://leetcode.cn/problems/array-with-elements-not-equal-to-average-of-neighbors) | 1499 |\n| 3680 | [生成賽程](https://leetcode.cn/problems/generate-schedule) | 2378 |",
    },
    {
      id: 29,
      title: "27. 迴文數",
      summary:
        "**原理講解**\n\n本節談「迴文數」：正著讀和反著讀一樣的數，例如 121、1331。重點不只是「判斷一個數是不是迴文」，更常見的是「在某範圍內找迴文數」或「生成所有迴文數」。\n\n:::example 判斷 x 是否為迴文數：只反轉後半段\n先看最基礎的「判斷」。技巧是只反轉一半的數字：從尾巴一位位拆下來拼成 `rev`，當 `rev` 追上剩下的前半段時就停。這樣不必反轉整個數，也自然避開溢位。\n\n```cpp\n// 判斷 x 是否為迴文數：只反轉後半段\nif (x < 0 || (x % 10 == 0 && x != 0)) {\n    return false;  // 負數、結尾0(非0本身)不是\n}\nint rev = 0;\nwhile (x > rev) {\n    rev = rev * 10 + x % 10;  // 把 x 的個位接到 rev 尾端\n    x /= 10;                  // x 去掉個位\n}\nreturn x == rev || x == rev / 10;  // 偶數位 x==rev；奇數位中間那位丟掉\n```\n:::\n\n\n\n:::example 由「前半段」生成偶數長度的迴文數，例如 half=123 -> 123321\n但更關鍵的觀念是：**找 / 數迴文數時，不要逐一檢查每個數，而是「直接生成」迴文數。** 因為一個迴文數其實由「前半段」決定——後半段是前半段的鏡像。所以只要枚舉前半段，就能拼出迴文數，數量比逐一掃描少非常多（例如所有 6 位迴文數只有約 900 個，由 3 位前半決定）。\n\n```cpp\n// 由「前半段」生成偶數長度的迴文數，例如 half=123 -> 123321\nlong long makeEven(long long half) {\n    long long pal = half;\n    for (long long t = half; t > 0; t /= 10) {\n        pal = pal * 10 + t % 10;  // 把 half 反過來接到後面\n    }\n    return pal;\n}\n```\n:::\n\n\n\n奇數長度只要在拼接前先把中間那位「留下不鏡像」即可。這個「枚舉一半、鏡像生成」的想法，是迴文數題的精髓。\n\n**實作方式**\n\n迴文數題依目標拆步驟：\n\n**1. 只是判斷單一數字：反轉一半比大小。** 用上面那段即可，記得處理負數與結尾 0。\n\n:::example 枚舉奇數長度迴文：half 是前半含中間位，鏡像時跳過中間位\n**2. 要在範圍內列舉 / 計數：枚舉前半段並鏡像生成。** 想要長度 `L` 的迴文數，就枚舉它的前 `⌈L/2⌉` 位，再鏡像補齊。\n\n```cpp\n// 枚舉奇數長度迴文：half 是前半含中間位，鏡像時跳過中間位\nlong long makeOdd(long long half) {\n    long long pal = half;\n    for (long long t = half / 10; t > 0; t /= 10) {  // 注意 /10 跳過中間\n        pal = pal * 10 + t % 10;\n    }\n    return pal;\n}\n```\n:::\n\n\n\n**3. 要「最近的迴文數」：只需比較少數候選。** 如 564「尋找最近的迴文數」，最近的迴文一定在「前半保持 / 前半 +1 / 前半 -1」以及「全 9」「100…01」這幾個候選裡，逐一算距離取最近即可，不必掃整個數線。\n\n:::example 生成迴文 -> 再加條件篩，例如質數\n**4. 結合其他性質時，先生成迴文再篩。** 如 866「迴文質數」：先用鏡像生成迴文數（注意偶數位迴文除 11 外都能被 11 整除，可直接跳過），再判質數；906「超級迴文數」：枚舉「根的前半」生成迴文根，平方後再驗證是否也是迴文。\n\n```cpp\n// 生成迴文 -> 再加條件篩，例如質數\nfor (long long h = 1;; ++h) {\n    long long pal = makeOdd(h);  // 先得到迴文候選\n    if (pal > limit) {\n        break;\n    }\n    if (isPrime(pal)) {\n        collect(pal);  // 再套額外條件\n    }\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 反轉數字時改用 `long long` 防溢位（例如 479「最大回文數乘積」乘起來很大）。\n- 長度奇偶要分開枚舉，別漏掉其一。\n- 「枚舉一半」把規模從 `O(N)` 降到約 `O(√N)`（前半段位數是全長一半），這正是它快的原因。\n\n**題目線索（Pattern）**\n\n看到以下訊號往「迴文數」想：\n\n- **題目明說迴文、回文、palindrome，且對象是「數字」。** 例如 9「迴文數」、564「尋找最近的迴文數」。\n- **要在很大的範圍內找 / 數迴文數**：別逐一掃描，改「枚舉前半段、鏡像生成」把規模砍半（866、906）。\n- **迴文 + 其他性質（質數、平方、乘積）**：先生成迴文，再套額外條件篩，比反過來快得多（479、866、906）。\n- **要「最近的迴文」**：只比較「前半 ±1」與全 9 / 10…01 等少量候選（564）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是「判斷一個」還是「找 / 數很多個」？後者就該用鏡像生成而非逐一檢查。\n2. 我要枚舉的是「前半段」嗎？長度的奇偶我都處理了嗎？\n3. 反轉 / 平方時會不會溢位，要不要 `long long`？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 9 | 迴文數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 479 | 最大回文數乘積 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 564 | 尋找最近的迴文數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 866 | [迴文質數](https://leetcode.cn/problems/prime-palindrome/) | 1938 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 906 | [超級迴文數](https://leetcode.cn/problems/super-palindromes/) | 2140 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 9 | [迴文數](https://leetcode.cn/problems/palindrome-number) |  |\n| 479 | [最大回文數乘積](https://leetcode.cn/problems/largest-palindrome-product) |  |\n| 564 | [尋找最近的迴文數](https://leetcode.cn/problems/find-the-closest-palindrome) |  |\n| 866 | [迴文質數](https://leetcode.cn/problems/prime-palindrome) | 1938 |\n| 906 | [超級迴文數](https://leetcode.cn/problems/super-palindromes) | 2140 |\n| 2081 | [k 映象數字的和](https://leetcode.cn/problems/sum-of-k-mirror-numbers) | 2210 |\n| 2217 | [找到指定長度的迴文數](https://leetcode.cn/problems/find-palindrome-with-fixed-length) | 1822 |\n| 2396 | [嚴格迴文的數字](https://leetcode.cn/problems/strictly-palindromic-number) | 1329 |\n| 2967 | [使陣列成為等數陣列的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-array-equalindromic) | 2116 |\n| 3260 | [找出最大的 N 位 K 迴文數](https://leetcode.cn/problems/find-the-largest-palindrome-divisible-by-k) | 2370 |\n| 3272 | [統計好整數的數目](https://leetcode.cn/problems/find-the-count-of-good-integers) | 2382 |\n| 3646 | [下一個特殊迴文數](https://leetcode.cn/problems/next-special-palindrome-number) | 2445 |\n| 3677 | [統計二進位制迴文數字的數目](https://leetcode.cn/problems/count-binary-palindromic-numbers) | 2223 |\n| 3766 | [將數字變成二進位制迴文數的最少操作](https://leetcode.cn/problems/minimum-operations-to-make-binary-palindrome) | 1657 |",
    },
    {
      id: 30,
      title: "28. 整數拆分",
      summary:
        "**原理講解**\n\n本節談「整數拆分」：把一個正整數 `n` 拆成若干個正整數的和，使這些數的「乘積最大」。例如 `n = 10` 拆成 `3 + 3 + 4`，乘積 `36` 最大。代表題就是 343「整數拆分」和 1808「好因子的最大數目」。\n\n這個技巧有趣的地方是它有兩種看法：一種是穩紮穩打的動態規劃（DP），一種是一眼看穿的數學結論。先講 DP，因為它最不需要靈感。\n\n:::example 整數拆分 DP：dp[i] = 拆 i 的最大乘積\n定義 `dp[i]` = 把 `i` 拆開後能得到的最大乘積。要拆 `i`，就枚舉「第一刀切出 `j`」，剩下的 `i - j` 要嘛不再拆（值就是 `i - j`），要嘛繼續拆（值是 `dp[i - j]`）。兩者取大，再乘上 `j`。\n\n```cpp\n// 整數拆分 DP：dp[i] = 拆 i 的最大乘積\nvector<int> dp(n + 1, 1);\nfor (int i = 2; i <= n; ++i) {\n    for (int j = 1; j < i; ++j) {\n        // 切出 j 後，剩下 i-j 可拆(dp[i-j])或不拆(i-j)，取大\n        dp[i] = max(dp[i], j * max(i - j, dp[i - j]));\n    }\n}\n// dp[n] 即答案，時間 O(n^2)\n```\n:::\n\n\n\n:::example 整數拆分的數學解：盡量用 3，餘數用 2 收尾\n接著是漂亮的數學結論：**最佳拆法就是「盡量拆成一堆 3，餘數用 2 補」。** 直覺是：把一個數拆成更小的因子相乘通常更大（如 `6` → `3×3 = 9 > 6`），而在所有切法中 3 的「每單位貢獻」最高；唯一例外是餘 1 時，與其留一個沒用的 1，不如把它和一個 3 併成 `2 + 2`。\n\n```cpp\n// 整數拆分的數學解：盡量用 3，餘數用 2 收尾\nint integerBreak(int n) {\n    if (n <= 3) {\n        return n - 1;  // n=2->1, n=3->2（題目要求至少拆兩塊）\n    }\n    int prod = 1;\n    while (n > 4) {\n        prod *= 3;\n        n -= 3;\n    }                 // 不斷切 3，留下 n∈{2,3,4}\n    return prod * n;  // 剩 2/3/4 直接乘上（4 = 2×2 最佳）\n}\n```\n:::\n\n\n\n為什麼是 3 不是 2？因為 `3 × 3 = 9 > 2 × 2 × 2 = 8`，同樣花掉 6，三個 2 比兩個 3 差。所以優先用 3。\n\n**實作方式**\n\n整數拆分題可這樣拆步驟：\n\n**1. 先用 DP 確保正確、再考慮數學優化。** DP 不需要洞察、最不易錯，適合當保底解法（上面那段 `O(n^2)`）。\n\n**2. 數學解：先處理小情況，再貪心拆 3。** `n ≤ 3` 要特判（題目通常要求「至少拆成兩個數」，所以 `n = 3` 答案是 `1×2 = 2` 而非 3）。\n\n**3. 餘數規則記牢。** 不斷減 3 直到 `n ≤ 4`：剩 2 就乘 2，剩 3 就乘 3，剩 4 就乘 4（因為 `4 = 2×2`，不需再拆）。讓 `while` 在 `n > 4` 停下正好涵蓋這三種收尾。\n\n:::example 快速冪：算 base^exp % MOD，O(log exp)\n**4. 要取模時改用快速冪。** 如 1808「好因子的最大數目」，答案是 `3^k` 之類的大數須對 `1e9 + 7` 取模，這時別真的連乘，用快速冪算 `3` 的次方。\n\n```cpp\n// 快速冪：算 base^exp % MOD，O(log exp)\nlong long qpow(long long base, long long exp, long long MOD) {\n    long long r = 1;\n    base %= MOD;\n    while (exp) {\n        if (exp & 1) {\n            r = r * base % MOD;  // 這一位是 1 就乘進來\n        }\n        base = base * base % MOD;  // 底數自乘\n        exp >>= 1;\n    }\n    return r;\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- 「至少拆兩塊」與「可以不拆」題意不同，會改變 `n ≤ 3` 的答案，務必看清。\n- 數學解是 `O(1)`（或取模時 `O(log n)`），DP 解是 `O(n^2)`。\n- 取模題切勿先連乘再取模（會溢位），要邊乘邊取模或直接快速冪。\n\n**題目線索（Pattern）**\n\n看到以下訊號往「整數拆分」想：\n\n- **把一個整數拆成若干正整數之和，要最大化它們的乘積。** 這就是 343「整數拆分」的標準型。\n- **答案結構是「一堆相同因子相乘」**，且因子越接近 3 越好——看到「乘積最大 / 因子數目最大」可往 3 的冪聯想（1808「好因子的最大數目」）。\n- **拆解問題有「子問題重疊」**（拆 `i` 會用到拆 `i - j` 的結果）：先想 DP，保證對；行有餘力再換數學 `O(1)` 解。\n- **數字很大、要對質數取模**：搭配快速冪計算次方。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要最大化的是「和」固定下的「乘積」嗎？\n2. 題目是「至少拆兩塊」還是「可以不拆」？這影響小數的特判。\n3. 答案會不會大到要取模？要不要用快速冪而非直接連乘？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 343 | 整數拆分 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1808 | [好因子的最大數目](https://leetcode.cn/problems/maximize-number-of-nice-divisors/) | 2070 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 343 | [整數拆分](https://leetcode.cn/problems/integer-break) |  |\n| 1808 | [好因子的最大數目](https://leetcode.cn/problems/maximize-number-of-nice-divisors) | 2070 |",
    },
    {
      id: 31,
      title: "29. 曼哈頓距離與切比雪夫距離",
      summary:
        "**原理講解**\n\n本節要處理的是平面上「點與點之間的距離」，但用的不是直線距離，而是兩種特別的距離。\n\n:::example 曼哈頓距離：橫向差 + 縱向差\n第一種叫**曼哈頓距離（Manhattan distance）**。它的名字來自曼哈頓的棋盤式街道：你不能斜著穿過大樓，只能沿著橫向和縱向的路走。兩點 `(x1, y1)` 和 `(x2, y2)` 的曼哈頓距離就是橫向走的距離加上縱向走的距離：\n\n```cpp\n// 曼哈頓距離：橫向差 + 縱向差\nint manhattan(int x1, int y1, int x2, int y2) {\n    return abs(x1 - x2) + abs(y1 - y2);  // abs 是取絕對值\n}\n```\n:::\n\n\n\n:::example 切比雪夫距離：橫向差與縱向差取較大值\n第二種叫**切比雪夫距離（Chebyshev distance）**。想像國際象棋的「國王」，它一步可以走到周圍 8 格（含斜角），所以從一格走到另一格需要的步數，是橫向差和縱向差中**較大的那一個**：\n\n```cpp\n// 切比雪夫距離：橫向差與縱向差取較大值\nint chebyshev(int x1, int y1, int x2, int y2) {\n    return max(abs(x1 - x2), abs(y1 - y2));\n}\n```\n:::\n\n\n\n這兩種距離看似無關，卻有一個漂亮的橋樑：**把座標旋轉 45 度，曼哈頓距離就會變成切比雪夫距離。** 具體做法是把每個點 `(x, y)` 換成新座標 `(x + y, x - y)`，那麼原本兩點的曼哈頓距離，剛好等於新座標下的切比雪夫距離。\n\n:::example 轉換：曼哈頓座標 -> 切比雪夫座標\n為什麼這很有用？因為曼哈頓距離裡的 `abs` 很難一次處理（你不知道哪個大哪個小），但換成切比雪夫之後，橫向（`x + y`）和縱向（`x - y`）就**完全獨立**了，可以各自取最大、最小，分開算，難題瞬間變簡單。\n\n```cpp\n// 轉換：曼哈頓座標 -> 切比雪夫座標\nint u = x + y;  // 新的「橫軸」\nint v = x - y;  // 新的「縱軸」\n                // 之後在 u、v 上各自獨立求極值即可\n```\n:::\n\n\n\n**實作方式**\n\n**1. 先想清楚題目用的是哪種距離。** 題目若描述「上下左右移動」「街區步數」，多半是曼哈頓；若是「八方向移動」「西洋棋國王」，則是切比雪夫。\n\n:::example 最大曼哈頓距離 = max( (x+y)的極差 , (x-y)的極差 )\n**2. 若要最大化曼哈頓距離，拆絕對值。** `abs(x1 - x2) + abs(y1 - y2)` 可以展開成四種正負號組合，取其中最大的一種。常見技巧是分別維護 `x + y` 與 `x - y` 的最大值與最小值。\n\n```cpp\n// 最大曼哈頓距離 = max( (x+y)的極差 , (x-y)的極差 )\nint maxU = INT_MIN, minU = INT_MAX, maxV = INT_MIN, minV = INT_MAX;\nfor (auto& p : points) {\n    int u = p[0] + p[1], v = p[0] - p[1];\n    maxU = max(maxU, u);\n    minU = min(minU, u);\n    maxV = max(maxV, v);\n    minV = min(minV, v);\n}\nint ans = max(maxU - minU, maxV - minV);  // 一次掃描就求出最大距離\n```\n:::\n\n\n\n**3. 若需要把曼哈頓改成切比雪夫，整批轉座標。** 把所有點 `(x, y)` 換成 `(x + y, x - y)`，之後橫縱兩維可以完全分開處理，例如分別排序、分別做前綴和。\n\n:::example 提醒：x、y 很大時，x + y 可能溢位，用 long long 比較保險\n**4. 邊界與複雜度。** 座標相加相減可能超出 `int` 範圍，記得視情況用 `long long`。掃描型解法通常是 `O(n)`，若需排序則 `O(n log n)`。\n\n```cpp\n// 提醒：x、y 很大時，x + y 可能溢位，用 long long 比較保險\nlong long u = (long long)x + y;\n```\n:::\n\n\n\n**題目線索（Pattern）**\n\n看到以下特徵，就可以往「曼哈頓／切比雪夫距離」想：\n\n- **題目出現絕對值之和**，像 `|x1 - x2| + |y1 - y2|`，或要你最大化／最小化這種式子。LeetCode 1131「絕對值表示式的最大值」就是要你最大化 `|a - b| + |i - j|`，標準拆絕對值技巧。\n- **題目在平面上移動且限定方向。** LeetCode 1266「訪問所有點的最小時間」中，可以斜走，每兩點間的時間正是切比雪夫距離。\n- **看到 `abs` 巢狀很難下手，想辦法「消掉絕對值」。** LeetCode 1330「翻轉子陣列得到最大的陣列值」就需要把絕對值拆成正負組合來分析。\n- **暴力做法是兩兩枚舉所有點對（`O(n^2)`）算距離取極值；** 用維護極值或座標轉換可降到 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這題的距離定義是曼哈頓還是切比雪夫？方向限制是什麼？\n2. 我能不能把 `abs` 拆成 `x + y` 與 `x - y` 兩個獨立的量？\n3. 我要的是「最大距離」「最小距離」還是「全部距離和」？該維護哪些極值或前綴量？\n\n三題都答得出來，方向就對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1131 | [絕對值表示式的最大值](https://leetcode.cn/problems/maximum-of-absolute-value-expression/) | 2059 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1266 | [訪問所有點的最小時間](https://leetcode.cn/problems/minimum-time-visiting-all-points/) | 1303 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1330 | [翻轉子陣列得到最大的陣列值](https://leetcode.cn/problems/reverse-subarray-to-maximize-array-value/) | 2482 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1956 | 感染 K 種病毒所需的最短時間（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2613 | 美數對（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1131 | [絕對值表示式的最大值](https://leetcode.cn/problems/maximum-of-absolute-value-expression) | 2059 |\n| 1266 | [訪問所有點的最小時間](https://leetcode.cn/problems/minimum-time-visiting-all-points) | 1303 |\n| 1330 | [翻轉子陣列得到最大的陣列值](https://leetcode.cn/problems/reverse-subarray-to-maximize-array-value) | 2482 |\n| 1956 | [感染 K 種病毒所需的最短時間](https://leetcode.cn/problems/minimum-time-for-k-virus-variants-to-spread) |  |\n| 2613 | [美數對](https://leetcode.cn/problems/beautiful-pairs) |  |\n| 3102 | [最小化曼哈頓距離](https://leetcode.cn/problems/minimize-manhattan-distances) | 2216 |\n| 3443 | [K 次修改後的最大曼哈頓距離](https://leetcode.cn/problems/maximum-manhattan-distance-after-k-changes) | 1856 |\n| 3464 | [正方形上的點之間的最大距離](https://leetcode.cn/problems/maximize-the-distance-between-points-on-a-square) | 2806 |",
    },
    {
      id: 32,
      title: "30. 多項式",
      summary:
        "**原理講解**\n\n本節談的「多項式」在競賽題裡，多半不是要你背高深的公式，而是指**把一個計數問題或幾何問題，整理成一條可以逐項計算的式子，再想辦法把暴力的多層迴圈壓下來**。\n\n:::example 固定中間元素 j，左右各自的數量相乘，就是經過 j 的三元組數\n最常見的形態是「枚舉其中一個量，把另外的量用前面累積好的資訊算出來」。舉個最直觀的例子：要數陣列裡有多少組 `i < j < k` 滿足某條件，最笨的是三層迴圈 `O(n^3)`。但如果固定中間的 `j`，左邊有多少合格的 `i`、右邊有多少合格的 `k` 都能事先算好，乘起來就是以 `j` 為中心的組數：\n\n```cpp\n// 固定中間元素 j，左右各自的數量相乘，就是經過 j 的三元組數\nlong long total = 0;\nfor (int j = 0; j < n; ++j) {\n    long long left = countLeft[j];    // j 左邊合格的個數\n    long long right = countRight[j];  // j 右邊合格的個數\n    total += left * right;            // 乘法原理：左右自由組合\n}\n```\n:::\n\n\n\n這裡用到的核心是**乘法原理**：兩件互相獨立的事，各有 `a`、`b` 種選法，合起來就有 `a * b` 種。把問題拆成「幾個獨立部分相乘、再把各情況相加」，就是這類題的骨架。\n\n:::example 排序後，固定最大的 nums[k]，雙指針找有多少對 (i, j) 滿足條件\n另一個常見招式是**排序後用雙指針或二分**，把「對每個元素去找有多少個搭配它」從 `O(n)` 降到 `O(log n)` 甚至均攤 `O(1)`：\n\n```cpp\n// 排序後，固定最大的 nums[k]，雙指針找有多少對 (i, j) 滿足條件\nsort(nums.begin(), nums.end());\nfor (int k = n - 1; k >= 2; k--) {\n    int i = 0, j = k - 1;\n    while (i < j) {\n        if (nums[i] + nums[j] > nums[k]) { /* 統計 */\n            j--;\n        } else {\n            ++i;\n        }\n    }\n}\n```\n:::\n\n\n\n關鍵心法：**先把答案寫成一條式子（哪些東西相乘、哪些相加），再針對式子找可以預處理或單調掃描的部分**，而不是一開始就硬寫多層迴圈。\n\n**實作方式**\n\n**1. 先寫出暴力式子。** 不管多慢，先把答案表達成「對哪些變數做枚舉、每組怎麼計算」。例如「對所有三元組求和」就先寫三層迴圈，看清結構。\n\n:::example 預處理：countRight[v] 表示某種值還剩多少個在右側\n**2. 找出可以固定的那一維。** 通常固定中間或最大的那個元素，讓另外兩維變成「左邊有幾個、右邊有幾個」這種能預先算好的量。\n\n```cpp\n// 預處理：countRight[v] 表示某種值還剩多少個在右側\nunordered_map<int, int> countRight;\nfor (int x : nums) {\n    countRight[x]++;  // 之後一邊掃一邊把元素從右移到左\n}\n```\n:::\n\n\n\n**3. 用乘法原理把獨立部分相乘、相加。** 確認「左選法」與「右選法」彼此獨立，才能直接相乘；若有重疊或順序限制，要特別小心別重複計算。\n\n:::example 計數題常見收尾：乘積可能很大，記得開 long long 或取模\n**4. 邊界與複雜度。** 計數題很容易溢位，數量大時用 `long long`，必要時對某個質數取模。排序加雙指針通常是 `O(n log n)`，預處理計數則是 `O(n)`。\n\n```cpp\n// 計數題常見收尾：乘積可能很大，記得開 long long 或取模\nconst int MOD = 1e9 + 7;\nans = (ans + left * right) % MOD;\n```\n:::\n\n\n\n**題目線索（Pattern）**\n\n看到以下特徵，可以往「公式化 + 計數」想：\n\n- **要你數「有多少組」滿足條件，而組的大小固定**（兩數、三數、四數）。LeetCode 259「較小的三數之和」、611「有效三角形的個數」、923「三數之和的多種可能」都是這型，固定一個端點後左右計數。\n- **答案可以寫成「幾個量相乘再相加」的式子。** 一旦你能寫出這種式子，通常就能預處理某些量來加速。\n- **暴力是多層巢狀迴圈（`O(n^2)` 或 `O(n^3)`），明顯會超時；** 固定一維 + 排序雙指針 / 計數，常能降一個次方。LeetCode 835「影象重疊」則是枚舉所有平移量，對每個平移量計數重疊格子。\n- **元素值有重複，需要用「個數」而非「位置」來算組合數**，這時乘法原理特別好用。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把答案寫成一條明確的式子（哪些相乘、哪些相加）？\n2. 固定哪一維之後，其他維度能變成「事先算好的數量」？\n3. 我的計數有沒有重複算或漏算？要不要取模防溢位？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 259 | 較小的三數之和（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 611 | 有效三角形的個數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 835 | [影象重疊](https://leetcode.cn/problems/image-overlap/) | 1970 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 923 | [三數之和的多種可能](https://leetcode.cn/problems/3sum-with-multiplicity/) | 1711 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3078 | 矩陣中的字母數字模式匹配 I（Premium） | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 259 | [較小的三數之和](https://leetcode.cn/problems/3sum-smaller) |  |\n| 611 | [有效三角形的個數](https://leetcode.cn/problems/valid-triangle-number) |  |\n| 835 | [影象重疊](https://leetcode.cn/problems/image-overlap) | 1970 |\n| 923 | [三數之和的多種可能](https://leetcode.cn/problems/3sum-with-multiplicity) | 1711 |\n| 3078 | [矩陣中的字母數字模式匹配 I](https://leetcode.cn/problems/match-alphanumerical-pattern-in-matrix-i) |  |\n| 3400 | [右移後的最大匹配索引數](https://leetcode.cn/problems/maximum-number-of-matching-indices-after-right-shifts) |  |\n| 3549 | [兩個多項式相乘](https://leetcode.cn/problems/multiply-two-polynomials) |  |",
    },
    {
      id: 33,
      title: "31. 快速沃爾什變換（FWT）",
      summary:
        "**原理講解**\n\n:::example 我們想算的東西（暴力版，O(n^2)）：對所有 i, j 把貢獻加到 c[i ^ j]\n本節要解決一類特別的問題：給兩個陣列 `a` 和 `b`，想算出一個新陣列 `c`，其中 `c[k]` 等於「所有 `i 與 j 經過某種位元運算等於 k` 的 `a[i] * b[j]` 的總和」。最常見的是 XOR（互斥或）卷積：\n\n```cpp\n// 我們想算的東西（暴力版，O(n^2)）：對所有 i, j 把貢獻加到 c[i ^ j]\nfor (int i = 0; i < n; ++i) {\n    for (int j = 0; j < n; ++j) {\n        c[i ^ j] += a[i] * b[j];  // i ^ j 是按位元 XOR\n    }\n}\n```\n:::\n\n\n\n這個暴力是 `O(n^2)`，當 `n` 達到 `2^20` 這種規模就會超時。**快速沃爾什變換（Fast Walsh-Hadamard Transform，FWT）** 就是把它加速到 `O(n log n)` 的工具，思路和傅立葉變換（FFT）非常像。\n\n核心想法分三步：**正變換、逐位相乘、逆變換**。先把 `a` 和 `b` 各自做一次「FWT 變換」，變換後兩個陣列只要**對應位置直接相乘**（變成 `O(n)`），再做一次「逆變換」還原，就得到答案 `c`。\n\n:::example XOR 版 FWT 的核心蝴蝶操作（對每一段長度為 len 的區塊兩兩配對）\n為什麼可以這樣？因為 FWT 把「XOR 卷積」這個麻煩的運算，轉換到一個新空間，在新空間裡卷積變成了單純的逐項相乘。就像把兩個很難相乘的數字取對數後變成加法一樣。\n\n```cpp\n// XOR 版 FWT 的核心蝴蝶操作（對每一段長度為 len 的區塊兩兩配對）\n// x、y 是配對的兩個值，變換後變成 x+y 與 x-y\nint nx = x + y;\nint ny = x - y;  // 逆變換時改成 (x + y) / 2 與 (x - y) / 2\n```\n:::\n\n\n\n對初學者來說，重點不是背下證明，而是記住**這個三步驟框架**和**它能把位元卷積從 `O(n^2)` 降到 `O(n log n)`**。AND、OR、XOR 各有自己版本的蝴蝶操作，但框架完全一樣。\n\n**實作方式**\n\n:::example 確認長度是 2 的次方（值域上限決定）\n**1. 把陣列大小補成 2 的次方。** FWT 要求長度是 `2^k`，不足的補 0。位元運算的範圍決定了要開多大，例如值域到 `2^20` 就要開 `1 << 20`。\n\n```cpp\n// 確認長度是 2 的次方（值域上限決定）\nint n = 1 << K;                      // 例如 K = 20\nvector<long long> a(n, 0), b(n, 0);  // 不足補 0\n```\n:::\n\n\n\n:::example XOR 版 FWT：inv 為 true 時做逆變換\n**2. 寫出對應運算的變換函式。** 以 XOR 為例，由外而內依序處理每個區塊長度：\n\n```cpp\n// XOR 版 FWT：inv 為 true 時做逆變換\nvoid fwtXor(vector<long long>& f, bool inv) {\n    int n = f.size();\n    for (int len = 1; len < n; len <<= 1) {\n        for (int i = 0; i < n; i += len << 1) {\n            for (int j = i; j < i + len; ++j) {\n                long long x = f[j], y = f[j + len];\n                f[j] = x + y;\n                f[j + len] = x - y;\n                if (inv) {\n                    f[j] /= 2;\n                    f[j + len] /= 2;\n                }  // 逆變換除以 2\n            }\n        }\n    }\n}\n```\n:::\n\n\n\n:::example 主流程：兩邊各變換，逐項乘，再逆變換還原\n**3. 三步驟組合出答案。** 變換 → 逐項相乘 → 逆變換：\n\n```cpp\n// 主流程：兩邊各變換，逐項乘，再逆變換還原\nfwtXor(a, false);\nfwtXor(b, false);\nfor (int i = 0; i < n; ++i) {\n    a[i] *= b[i];  // 此處乘法即完成卷積\n}\nfwtXor(a, true);  // a 現在就是答案 c\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 時間是 `O(n log n)`，`n` 是補齊後的長度。數值會在變換中放大，務必用 `long long`，必要時取模（取模時逆變換的「除以 2」要改成乘以 2 的模逆元）。\n\n**題目線索（Pattern）**\n\n看到以下特徵，可以往「FWT」想：\n\n- **答案需要對所有元素對做位元運算（XOR / AND / OR）後分類統計**，而且值域是 `2^k` 等級。LeetCode 3514「不同 XOR 三元組的數目 II」就是要統計 XOR 結果的分布，正適合用 XOR 卷積疊加。\n- **暴力是雙重（或多重）迴圈枚舉所有組合算位元結果（`O(n^2)`），但值域只有幾百萬，**這種「組合多、值域中等」的結構正是 FWT 的舞台。\n- **題目可以轉成「子集和」或「位元卷積」的形式。** 例如 LeetCode 1723「完成所有工作的最短時間」用狀態壓縮 DP 時，子集枚舉可由類似的位元變換思想加速。\n- **你發現要把多個陣列「用 XOR 反覆合併」**，每次合併都是一次卷積，FWT 能讓每次合併都很快。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的合併運算是 XOR、AND 還是 OR？要用哪一版蝴蝶操作？\n2. 值域補齊成 `2^k` 後，`n` 多大？`O(n log n)` 能過嗎？\n3. 數值會不會溢位或需要取模？逆變換的「除以 2」該怎麼處理？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1723 | [完成所有工作的最短時間](https://leetcode.cn/problems/find-minimum-time-to-finish-all-jobs/) | 2284 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3514 | [不同 XOR 三元組的數目 II](https://leetcode.cn/problems/number-of-unique-xor-triplets-ii/) | 1884 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1723 | [完成所有工作的最短時間](https://leetcode.cn/problems/find-minimum-time-to-finish-all-jobs) | 2284 |\n| 3514 | [不同 XOR 三元組的數目 II](https://leetcode.cn/problems/number-of-unique-xor-triplets-ii) | 1884 |",
    },
    {
      id: 34,
      title: "32. 摩爾投票法",
      summary:
        "**原理講解**\n\n本節的「摩爾投票法（Boyer-Moore Majority Vote）」要解決一個經典問題：在一個陣列裡找出**出現次數超過一半的元素**（稱為「多數元素」）。最直覺的做法是用一個 map 統計每個數出現幾次，但那要額外 `O(n)` 空間。摩爾投票法厲害的地方是：**只用一個候選變數和一個計數器，`O(n)` 時間、`O(1)` 空間**就能找出來。\n\n核心想法用一句話說：**讓不同的元素互相抵消，最後活下來的就是多數。** 想像每個元素都是一張選票，相同的票互相支持，不同的票互相消滅。因為多數元素的票數超過一半，就算其他所有票全部來抵消它，它也消不完，最後剩下的一定是它。\n\n:::example 摩爾投票核心：相同就加票，不同就抵消，票數歸零就換人\n具體維護兩個東西：一個「目前候選人 `cand`」，一個「票數 `count`」。掃描陣列時：\n\n```cpp\n// 摩爾投票核心：相同就加票，不同就抵消，票數歸零就換人\nint cand = 0, count = 0;\nfor (int x : nums) {\n    if (count == 0) {\n        cand = x;  // 沒有候選人了，當前這張票成為新候選\n    }\n    count += (x == cand) ? 1 : -1;  // 跟候選一致加票，不一致抵消一票\n}\n// 迴圈結束，cand 就是「可能的」多數元素\n```\n:::\n\n\n\n:::example 驗證：因為投票只給「候選」，不保證它真的過半\n要特別注意：摩爾投票法找出的只是「**最有可能**的候選」。如果題目**保證**多數元素一定存在，`cand` 就是答案；若不保證，需要**再掃一遍驗證** `cand` 的真實次數是否真的過半。\n\n```cpp\n// 驗證：因為投票只給「候選」，不保證它真的過半\nint real = 0;\nfor (int x : nums) {\n    if (x == cand) {\n        ++real;\n    }\n}\nbool ok = real > nums.size() / 2;  // 真的過半才算數\n```\n:::\n\n\n\n這個方法還能推廣：要找「出現次數超過 `1/3`」的元素時，最多有兩個，就維護**兩組**候選與計數，原理完全一樣。\n\n**實作方式**\n\n:::example 一次掃描找多數候選\n**1. 標準版：一個候選、一個計數。** 適用「超過 `1/2`」的題目，例如 LeetCode 169「多數元素」。\n\n```cpp\n// 一次掃描找多數候選\nint majorityElement(vector<int>& nums) {\n    int cand = 0, count = 0;\n    for (int x : nums) {\n        if (count == 0) {\n            cand = x;\n        }\n        count += (x == cand) ? 1 : -1;\n    }\n    return cand;  // 題目保證存在，直接回傳\n}\n```\n:::\n\n\n\n:::example 兩個候選的抵消：先比是否等於某候選，再看能否補空位，最後一起減票\n**2. 推廣版：找超過 `1/3` 的元素，維護兩個候選。** 超過 `1/3` 的數最多兩個，所以開兩組 (候選, 票數)。\n\n```cpp\n// 兩個候選的抵消：先比是否等於某候選，再看能否補空位，最後一起減票\nif (x == c1) {\n    ++cnt1;\n} else if (x == c2) {\n    ++cnt2;\n} else if (cnt1 == 0) {\n    c1 = x;\n    cnt1 = 1;\n} else if (cnt2 == 0) {\n    c2 = x;\n    cnt2 = 1;\n} else {\n    cnt1--;\n    cnt2--;\n}  // 與兩候選都不同，各抵消一票\n```\n:::\n\n\n\n**3. 一定要驗證（除非題目保證）。** 推廣版尤其要驗證，因為兩個候選不一定都真的過 `1/3`，最後重數一次再決定收哪些。\n\n:::example 收尾驗證候選真的超過 n/3 才放進答案\n**4. 邊界與複雜度。** 時間 `O(n)`，空間 `O(1)`。注意空陣列、全部相同、剛好等於一半（不算過半）等邊界。\n\n```cpp\n// 收尾驗證候選真的超過 n/3 才放進答案\nfor (int x : nums) {\n    if (x == c1) {\n        ++r1;\n    } else if (x == c2) {\n        ++r2;\n    }\n}\nif (r1 > n / 3) {\n    ans.push_back(c1);\n}\nif (r2 > n / 3) {\n    ans.push_back(c2);\n}\n```\n:::\n\n\n\n**題目線索（Pattern）**\n\n看到以下特徵，可以往「摩爾投票法」想：\n\n- **要找「出現次數超過一半 / 超過 1/3」的元素。** LeetCode 169「多數元素」、229「多數元素 II」是最直接的兩題。\n- **題目強調 `O(1)` 額外空間、不准用雜湊表計數。** 這正是摩爾投票相對於 map 計數的最大賣點。\n- **隱含「某元素佔絕對多數」的結構。** LeetCode 961「在長度 2N 的陣列中找出重複 N 次的元素」、2780「合法分割的最小下標」都圍繞「主要元素」展開，可用投票思想定位候選。\n- **暴力是用 map 統計次數（`O(n)` 時間、`O(n)` 空間），** 摩爾投票把空間壓到 `O(1)`；若資料是在線、區間查詢（如 LeetCode 1157），則需配合其他結構但候選思想仍適用。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的是超過 `1/2` 還是 `1/k` 的元素？要維護幾組候選？\n2. 題目有沒有保證多數元素一定存在？沒有的話我有沒有最後驗證？\n3. 抵消邏輯寫對了嗎？「票數歸零才換候選」這條有沒有漏？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 169 | 多數元素 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 229 | 多數元素 II | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 961 | [在長度 2N 的陣列中找出重複 N 次的元素](https://leetcode.cn/problems/n-repeated-element-in-size-2n-array/) | 1162 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1157 | [子陣列中佔絕大多數的元素](https://leetcode.cn/problems/online-majority-element-in-subarray/) | 2205 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2780 | [合法分割的最小下標](https://leetcode.cn/problems/minimum-index-of-a-valid-split/) | 1550 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 169. 多數元素 展示核心寫法。\n\n**完整題目**：給定陣列，找出出現次數超過 `n / 2` 的多數元素；題目保證答案存在。\n\n:::example Boyer–Moore 投票法：多數元素出現次數超過一半，最終候選必為答案\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int majorityElement(vector<int>& nums) {\n                // Boyer–Moore 投票法：多數元素出現次數超過一半，最終候選必為答案\n                int cand = 0, cnt = 0;\n                for (int x : nums) {\n                        if (cnt == 0) {\n                                cand = x;  // 計數歸零時更換候選人\n                        }\n                        cnt += (x == cand ? 1 : -1);  // 相同加票，相異抵消\n                }\n                return cand;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 摩爾投票把候選值與不同值兩兩抵消；若多數元素一定存在，最後留下的候選就是答案。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- n/2 只需一個候選。\n- n/3 需要兩個候選。\n- 沒有保證存在時，最後要二次計數驗證。\n\n**實作注意事項**\n\n- 抵消後的候選不一定有效，除非題目保證存在。\n- 更新順序很重要：先匹配候選，再處理空槽。\n- 多候選版本要避免兩個候選相同。\n- 二次驗證時計數清零重算。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 169 | [多數元素](https://leetcode.cn/problems/majority-element) |  |\n| 229 | [多數元素 II](https://leetcode.cn/problems/majority-element-ii) |  |\n| 961 | [在長度 2N 的陣列中找出重複 N 次的元素](https://leetcode.cn/problems/n-repeated-element-in-size-2n-array) | 1162 |\n| 1157 | [子陣列中佔絕大多數的元素](https://leetcode.cn/problems/online-majority-element-in-subarray) | 2205 |\n| 2780 | [合法分割的最小下標](https://leetcode.cn/problems/minimum-index-of-a-valid-split) | 1550 |",
    },
    {
      id: 35,
      title: "33. 其他",
      summary:
        "**原理講解**\n\n本節是數學分類的「其他」，收的是一些不好歸進前面類別、但都圍繞同一種心法的雜項題：**先把問題化成一條清楚的數學式或構造規則，再按照式子直接算出答案，而不是用搜尋或模擬硬湊。**\n\n:::example 輾轉相除法求最大公因數：a、b 的 GCD\n這類題的共同特徵是「答案有公式或可一步步推導出來」。最典型的例子是**分數運算**和**進位制／序號轉換**。先看一個基礎工具——求最大公因數（GCD），它是化簡分數的關鍵：\n\n```cpp\n// 輾轉相除法求最大公因數：a、b 的 GCD\nint gcd(int a, int b) {\n    while (b) {\n        int t = a % b;\n        a = b;\n        b = t;\n    }\n    return a;  // b 變 0 時，a 就是最大公因數\n}\n```\n:::\n\n\n\n:::example 化簡分數 a / b 為最簡分數\n有了 GCD，要把分數 `a/b` 化成最簡，只要分子分母同除以它們的 GCD：\n\n```cpp\n// 化簡分數 a / b 為最簡分數\nint g = gcd(abs(a), abs(b));\na /= g;\nb /= g;  // 例如 6/8 -> 3/4\n```\n:::\n\n\n\n:::example 用階乘定位：第一位應該選第幾個剩餘數字\n另一種常見題型是**「第 k 個」問題**：不要真的把前面所有東西列出來，而是用數學一步步「定位」。例如要找排列中字典序第 k 個，可以利用「固定第一位後，剩下的排列共有 `(n - 1)!` 種」這個事實，直接算出每一位該放誰：\n\n```cpp\n// 用階乘定位：第一位應該選第幾個剩餘數字\n// f[n - 1] 是 (n-1)! ；k 從 0 開始算\nint idx = k / f[n - 1];  // 商 = 第一位的選擇\nk %= f[n - 1];           // 餘數 = 在剩下排列中的新名次\n```\n:::\n\n\n\n心法就是：**遇到「算出某個量」「轉換表示法」「找第 k 個」這類題，先問有沒有公式或遞推能直接算，避免暴力枚舉。**\n\n**實作方式**\n\n**1. 先把問題寫成數學式或規則。** 像分數化簡寫成「同除 GCD」、找第 k 項寫成「用階乘逐位確定」。寫不出式子時，先手算幾個小例子找規律。\n\n:::example 處理長除法的小數部分：用 map 記每個餘數第一次出現的位置，偵測循環節\n**2. 備好常用數學工具。** GCD、快速冪、組合數、模逆元等是這類題的零件，先確認需要哪個。\n\n```cpp\n// 處理長除法的小數部分：用 map 記每個餘數第一次出現的位置，偵測循環節\nunordered_map<long long, int> seen;  // 餘數 -> 它在結果字串中的位置\n                                                                                                                                                    // 同一個餘數再次出現，代表小數開始循環\n```\n:::\n\n\n\n**3. 一步步把答案算出來。** 例如 LeetCode 166「分數到小數」：整數部分用除法，小數部分反覆「餘數乘 10 再除」，並用 map 偵測循環。LeetCode 60「排列序列」則用階乘逐位定位。\n\n:::example 常見地雷：相乘前先轉 long long，避免中途溢位\n**4. 邊界與複雜度。** 這類題最容易栽在邊界：負號、除以零、溢位、循環小數、`k` 從 0 還是 1 開始。數值大時用 `long long`。複雜度通常很低（`O(log n)` 或 `O(n)`），因為是直接計算而非搜尋。\n\n```cpp\n// 常見地雷：相乘前先轉 long long，避免中途溢位\nlong long num = (long long)numerator;\n```\n:::\n\n\n\n**題目線索（Pattern）**\n\n看到以下特徵，可以往「公式化／直接構造」想：\n\n- **題目要求「計算某個量」或「轉換表示法」，而非搜尋最優解。** LeetCode 166「分數到小數」要把分數轉成字串、處理循環節；LCP 02「分式化簡」要化簡連分數，都是純計算。\n- **要找「第 k 個」「字典序第 k 大」，但 `k` 很大不能逐一列舉。** LeetCode 60「排列序列」用階乘直接定位，避免生成全部排列。\n- **暴力是模擬或枚舉所有可能（指數級或階乘級），明顯不可行；** 改用公式 / 遞推 / 數論工具一步算出。LCP 29「樂團站位」、LCP 46「志願者調配」都能用遞推式直接求第 n 項。\n- **手算幾個小例子後看得出明顯規律或週期**，這通常意味著背後有閉式公式或循環結構。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這題的答案能不能寫成一條式子或一段遞推，而不必搜尋？\n2. 我需要哪些數學零件（GCD、階乘、快速冪、模逆元）？\n3. 邊界處理好了嗎？負號、除零、溢位、`k` 的起算值、循環節有沒有漏？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 02 | 分式化簡 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| LCP 29 | 樂團站位 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| LCP 46 | 志願者調配 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 60 | 排列序列 | N/A | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 166 | 分數到小數 | N/A | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n    public:\n        int countPrimes(int n) {\n                // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n                vector<char> isPrime(n, true);\n                int ans = 0;\n                for (int i = 2; i < n; ++i) {\n                        if (!isPrime[i]) {\n                                continue;\n                        }\n                        ++ans;\n                        // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n                        if ((long long)i * i < n) {\n                                for (long long j = 1LL * i * i; j < n; j += i) {\n                                        isPrime[j] = false;\n                                }\n                        }\n                }\n                return ans;\n        }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i * i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| LCP 02 | [分式化簡](https://leetcode.cn/problems/deep-dark-fraction) |  |\n| LCP 29 | [樂團站位](https://leetcode.cn/problems/SNJvJP) |  |\n| LCP 46 | [志願者調配](https://leetcode.cn/problems/05ZEDJ) |  |\n| 60 | [排列序列](https://leetcode.cn/problems/permutation-sequence) |  |\n| 166 | [分數到小數](https://leetcode.cn/problems/fraction-to-recurring-decimal) |  |\n| 319 | [燈泡開關](https://leetcode.cn/problems/bulb-switcher) |  |\n| 390 | [消除遊戲](https://leetcode.cn/problems/elimination-game) |  |\n| 458 | [可憐的小豬](https://leetcode.cn/problems/poor-pigs) |  |\n| 483 | [最小好進位制](https://leetcode.cn/problems/smallest-good-base) |  |\n| 660 | [移除 9](https://leetcode.cn/problems/remove-9) |  |\n| 800 | [相似 RGB 顏色](https://leetcode.cn/problems/similar-rgb-color) |  |\n| 972 | [相等的有理數](https://leetcode.cn/problems/equal-rational-numbers) | 2121 |\n| 1017 | [負二進位制轉換](https://leetcode.cn/problems/convert-to-base-2) | 1698 |\n| 1073 | [負二進位制數相加](https://leetcode.cn/problems/adding-two-negabinary-numbers) | 1807 |\n| 1414 | [和為 K 的最少斐波那契數字數目](https://leetcode.cn/problems/find-the-minimum-number-of-fibonacci-numbers-whose-sum-is-k) | 1466 |\n| 1502 | [判斷能否形成等差數列](https://leetcode.cn/problems/can-make-arithmetic-progression-from-sequence) | 1155 |\n| 1515 | [服務中心的最佳位置](https://leetcode.cn/problems/best-position-for-a-service-centre) | 2157 |\n| 1523 | [在區間範圍內統計奇數數目](https://leetcode.cn/problems/count-odd-numbers-in-an-interval-range) | 1209 |\n| 1716 | [計算力扣銀行的錢](https://leetcode.cn/problems/calculate-money-in-leetcode-bank) | 1295 |\n| 1739 | [放置盒子](https://leetcode.cn/problems/building-boxes) | 2198 |\n| 1780 | [判斷一個數字是否可以表示成三的冪的和](https://leetcode.cn/problems/check-if-number-is-a-sum-of-powers-of-three) | 1506 |\n| 1806 | [還原排列的最少操作步數](https://leetcode.cn/problems/minimum-number-of-operations-to-reinitialize-a-permutation) | 1491 |\n| 1823 | [找出遊戲的獲勝者](https://leetcode.cn/problems/find-the-winner-of-the-circular-game) | 1412 |\n| 1862 | [向下取整數對和](https://leetcode.cn/problems/sum-of-floored-pairs) | 2170 |\n| 1954 | [收集足夠蘋果的最小花園周長](https://leetcode.cn/problems/minimum-garden-perimeter-to-collect-enough-apples) | 1759 |\n| 2117 | [一個區間內所有數乘積的縮寫](https://leetcode.cn/problems/abbreviating-the-product-of-a-range) | 2477 |\n| 2145 | [統計隱藏陣列數目](https://leetcode.cn/problems/count-the-hidden-sequences) | 1614 |\n| 2195 | [向陣列中追加 K 個整數](https://leetcode.cn/problems/append-k-integers-with-minimal-sum) | 1659 |\n| 2310 | [個位數字為 K 的整數之和](https://leetcode.cn/problems/sum-of-numbers-with-units-digit-k) | 1559 |\n| 2443 | [反轉之後的數字和](https://leetcode.cn/problems/sum-of-number-and-its-reverse) | 1376 |\n| 2457 | [美麗整數的最小增量](https://leetcode.cn/problems/minimum-addition-to-make-integer-beautiful) | 1680 |\n| 2541 | [使陣列中所有元素相等的最小運算元 II](https://leetcode.cn/problems/minimum-operations-to-make-array-equal-ii) | 1620 |\n| 2579 | [統計染色格子數](https://leetcode.cn/problems/count-total-number-of-colored-cells) | 1356 |\n| 2647 | [把三角形染成紅色](https://leetcode.cn/problems/color-the-triangle-red) |  |\n| 2829 | [k-avoiding 陣列的最小總和](https://leetcode.cn/problems/determine-the-minimum-sum-of-a-k-avoiding-array) | 1347 |\n| 2834 | [找出美麗陣列的最小和](https://leetcode.cn/problems/find-the-minimum-possible-sum-of-a-beautiful-array) | 1409 |\n| 2844 | [生成特殊數字的最少操作](https://leetcode.cn/problems/minimum-operations-to-make-a-special-number) | 1588 |\n| 2979 | [最貴的無法購買的商品](https://leetcode.cn/problems/most-expensive-item-that-can-not-be-bought) |  |\n| 3012 | [透過操作使陣列長度最小](https://leetcode.cn/problems/minimize-length-of-array-using-operations) | 1833 |\n| 3091 | [執行操作使資料元素之和大於等於 K](https://leetcode.cn/problems/apply-operations-to-make-sum-of-array-greater-than-or-equal-to-k) | 1522 |\n| 3468 | [可行陣列的數目](https://leetcode.cn/problems/find-the-number-of-copy-arrays) | 1545 |\n| 3648 | [覆蓋網格的最少感測器數目](https://leetcode.cn/problems/minimum-sensors-to-cover-grid) | 1396 |\n| 3649 | [完美對的數目](https://leetcode.cn/problems/number-of-perfect-pairs) | 1716 |\n| 3666 | [使二進位制字串全為 1 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-equalize-binary-string) | 2477 |\n| 3782 | [交替刪除操作後最後剩下的整數](https://leetcode.cn/problems/last-remaining-integer-after-alternating-deletion-operations) | 2074 |",
    },
    {
      id: 9011,
      title: "34. 常見面試模式",
      summary:
        "數學類題目在面試中常考冪取模、質數預處理、最大公約數與組合計數取模等基本功。辨識題目對應的數學模式後，往往能把暴力的 `O(n)` 或更糟降到 `O(log n)` 或預處理後 `O(1)`。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 快速冪 | 需算 $a^b$ 或 $a^b \\bmod p$，指數很大 | 二進位拆解指數逐次平方，`O(log b)` | [Pow(x, n)](https://leetcode.cn/problems/powx-n) |\n| 質數篩 | 需多次判質數或列舉範圍內所有質數 | 埃氏篩或線性篩預處理，整體 `O(n log log n)` | [計數質數](https://leetcode.cn/problems/count-primes) |\n| GCD / Bézout | 涉及最大公約數、線性組合可達性 | 輾轉相除求 GCD，擴展歐幾里得求係數 | [水壺問題](https://leetcode.cn/problems/water-and-jug-problem) |\n| 組合數取模 | 求方案數且要對大質數取模 | 預處理階乘與逆元，逆元用費馬小定理 $a^{p-2}$ | [不同路徑](https://leetcode.cn/problems/unique-paths) |\n| 數位處理 | 與十進位每一位有關（迴文、數位和、計數） | 逐位拆解或數位 DP，按位統計 | [回文數](https://leetcode.cn/problems/palindrome-number) |\n| 容斥 | 求「至少滿足其一」或多條件並集計數 | 加上單條件、減去兩兩交集、再加回… | [醜數 II](https://leetcode.cn/problems/ugly-number-ii) |\n\n**小提醒**\n\n- 模逆元僅在模數 $p$ 為質數且 $\\gcd(a, p) = 1$ 時可用費馬小定理 $a^{p-2}$，否則改用擴展歐幾里得。\n- 快速冪與連乘取模時，中間乘積可能溢位 32 位，務必用 64 位型別累乘。\n- 線性篩除了得到質數表，還能順便得到最小質因數，方便後續質因數分解。",
    },
  ],
} as TutorialData.Root;
