import type { TutorialData } from "@/types";

export const rating_2100 = {
  id: 1,
  title: "Rating 2100 攻略計畫",
  src: "",
  last_update: "2026-05-29T04:54:36.866176+00:00",
  summary:
    "這是一條面向已具備約 1700 rating 基礎的進階路線，目標是在 1700-2100 區間建立穩定解題輸出。題目按 rating 分成四個階段，每個階段再按主題分類，方便你在保持綜合訓練的同時補強弱項。\n\n**使用建議：**\n- 每週選 2-3 個主題交替練習，避免只刷熟悉類型\n- 1700-1900 題控制在 35-45 分鐘內，1900+ 題控制在 60 分鐘內\n- 每道錯題記錄三件事：題型訊號、卡住原因、下次可復用的判斷規則\n- 每完成一個 phase，做 2-3 場 Virtual Contest 檢查是否能在比賽中調用技巧\n- 已經穩定解出 2000+ 題後，再銜接 Rating 2400 精進計畫",
  children: [
    {
      id: 2,
      title: "1. Phase 1：穩住 1700 基本盤（1700-1800）",
      summary:
        "用中高難度題校準基本功：題目通常不靠冷門技巧，而是考驗模型轉換、邊界處理與模板熟練度。建議以 35 分鐘為單題上限，卡住後先寫出狀態、單調性或不變式，再看提示。",
      children: [
        {
          id: 3,
          title: "1.1 二分搜尋",
          summary:
            "**原理講解**\n\n本節要解決的場景是：答案是一個「數值」，而且這個數值有一條清楚的分界線——小於某個門檻時某條件成立，大於它就不成立（或反過來）。這種「單調」的結構，正是「二分搜尋（binary search）」能發揮威力的地方。\n\n:::example 在有序陣列裡找 target，回傳第一個 >= target 的位置\n先複習最基本的形式：在一個**已排序**的陣列裡找某個值。因為陣列有序，每次看中間元素就能判斷「目標在左半還是右半」，於是一次砍掉一半範圍。\n\n```cpp\n// 在有序陣列裡找 target，回傳第一個 >= target 的位置\nint lo = 0, hi = a.size();  // 區間 [lo, hi)\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;  // 防溢位寫法\n  if (a[mid] >= target) {\n    hi = mid;  // 中間夠大 → 答案在左半（含 mid）\n  } else {\n    lo = mid + 1;  // 中間太小 → 答案在右半\n  }\n}\n// 結束時 lo 就是第一個 >= target 的位置\n```\n:::\n\n\n\n但這個技巧真正強大的地方，是它不只能用在「找陣列裡的值」。只要你能定義一個**布林判斷函式** `check(x)`，而且它隨著 `x` 變大會從「假」變「真」（或從真變假）只跳變一次，你就能用二分去找那個「臨界點」。這叫做「二分答案」。\n\n:::example 二分答案的骨架：找「最小的可行 x」\n像「在 D 天內送達包裹的能力」（題 1011），就是猜一個「每天運載能力 `cap`」：`cap` 太小天數不夠（假），`cap` 夠大就能在 D 天內運完（真），而且 `cap` 越大越容易達成——完美的單調性。於是我們不去枚舉每個 `cap`，而是二分它。\n\n```cpp\n// 二分答案的骨架：找「最小的可行 x」\nint lo = minX, hi = maxX;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;  // mid 可行 → 試更小\n  } else {\n    lo = mid + 1;  // mid 不可行 → 必須更大\n  }\n}\n// lo 即為最小可行值；check 通常是一次 O(n) 的掃描\n```\n:::\n\n\n\n關鍵體會：二分搜尋把「逐一嘗試每個候選值（`O(範圍)`）」壓成 `O(log 範圍)` 次嘗試，每次嘗試再花 `check` 的成本。\n\n**實作方式**\n\n把任何二分搜尋題拆成三個固定步驟，逐一搭配程式碼：\n\n**1. 定義候選值與它的語意。** 先想清楚「我在二分什麼數字」，以及這個數字代表什麼。是陣列下標？還是某個答案量（容量、天數、最小值）？「統計公平數對」（題 2563）二分的是「另一個元素的下標範圍」；「最大美麗值」（題 2070）二分的是「價格門檻」。\n\n:::example check：給定門檻 x，需要幾天才能運完？是否 <= D？\n**2. 畫出真假分界，寫出 `check`。** 確認「條件隨候選值單調變化」，再決定要找「第一個為真」還是「最後一個為真」。這一步決定邊界怎麼收。\n\n```cpp\n// check：給定門檻 x，需要幾天才能運完？是否 <= D？\nbool check(int cap, vector<int>& w, int D) {\n  int days = 1, cur = 0;\n  for (int x : w) {\n    if (x > cap) {\n      return false;  // 單件就超過能力 → 不可行\n    }\n    if (cur + x > cap) {\n      days++;\n      cur = 0;\n    }  // 開新的一天\n    cur += x;\n  }\n  return days <= D;\n}\n```\n:::\n\n\n\n:::example 找第一個可行容量\n**3. 收斂邊界、讀出答案。** 用 `[lo, hi)` 半開區間最不易出錯：找「第一個為真」時，真就 `hi = mid`、假就 `lo = mid + 1`；迴圈結束 `lo == hi` 即答案。\n\n```cpp\n// 找第一個可行容量\nint lo = *max_element(w.begin(), w.end());   // 容量至少要裝得下最大件\nint hi = accumulate(w.begin(), w.end(), 0);  // 至多一天運完全部\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid, w, D)) {\n    hi = mid;\n  } else {\n    lo = mid + 1;\n  }\n}\nreturn lo;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定用 `mid = lo + (hi - lo) / 2` 避免相加溢位。\n- 想清楚「找第一個真」還是「最後一個真」，兩者的更新方式相反，寫反會差一格。\n- 複雜度：二分本身 `O(log 範圍)`，乘上每次 `check` 的成本。若 `check` 是 `O(n)`，整體就是 `O(n log 範圍)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「二分搜尋」想：\n\n- **資料已經排序，要快速定位某個值或某個邊界。** 例如「區間內查詢數字的頻率」（題 2080）把每個值出現的下標排好，再二分查區間端點。\n- **答案是一個數值，且「越大越容易成立、越小越難成立」（或相反）。** 這種單調性是二分答案的訊號。「在 D 天內送達」「分割正方形」（題 3453）都屬此類——直接枚舉答案太慢：\n\n:::example 嗅到味道的瞬間：暴力是枚舉每個候選 x\n```cpp\n// 嗅到味道的瞬間：暴力是枚舉每個候選 x\n// for (int x = lo; x <= hi; x++) if (check(x)) return x;  // O(範圍) 太慢\n// 改成二分：O(log 範圍) 次 check 就夠\n```\n:::\n\n\n\n- **題目問「最小的能…」「最大的使得…」「第 k 個…」。** 這類「最值＋可行性判斷」往往就是二分答案。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我二分的「候選值 `x`」是什麼？它的範圍 `[lo, hi]` 是多少？\n2. 我的 `check(x)` 怎麼寫？它隨 `x` 變大是從假變真，還是從真變假？\n3. 我要的是「第一個為真」還是「最後一個為真」？邊界更新對得上嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2080 | [區間內查詢數字的頻率](https://leetcode.cn/problems/range-frequency-queries/) | 1702 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2563 | [統計公平數對的數目](https://leetcode.cn/problems/count-the-number-of-fair-pairs/) | 1721 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2070 | [每一個查詢的最大美麗值](https://leetcode.cn/problems/most-beautiful-item-for-each-query/) | 1724 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1011 | [在 D 天內送達包裹的能力](https://leetcode.cn/problems/capacity-to-ship-packages-within-d-days/) | 1725 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 3453 | [分割正方形 I](https://leetcode.cn/problems/separate-squares-i/) | 1735 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n:::example 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  // 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;  // 閉區間 [l, r]\n    while (l <= r) {\n      int mid = l + (r - l) / 2;  // 取中點，避免 l+r 溢位\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;  // 目標在右半段\n      } else {\n        r = mid - 1;  // 目標在左半段\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2080 | [區間內查詢數字的頻率](https://leetcode.cn/problems/range-frequency-queries) | 1702 |\n| 2563 | [統計公平數對的數目](https://leetcode.cn/problems/count-the-number-of-fair-pairs) | 1721 |\n| 2070 | [每一個查詢的最大美麗值](https://leetcode.cn/problems/most-beautiful-item-for-each-query) | 1724 |\n| 1011 | [在 D 天內送達包裹的能力](https://leetcode.cn/problems/capacity-to-ship-packages-within-d-days) | 1725 |\n| 3453 | [分割正方形 I](https://leetcode.cn/problems/separate-squares-i) | 1735 |\n| 875 | [愛喫香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) | 1766 |\n| 3281 | [範圍內整數的最大得分](https://leetcode.cn/problems/maximize-score-of-numbers-in-ranges) | 1768 |\n| 1146 | [快照陣列](https://leetcode.cn/problems/snapshot-array) | 1771 |\n| 2982 | [找出出現至少三次的最長特殊子字串 II](https://leetcode.cn/problems/find-longest-special-substring-that-occurs-thrice-ii) | 1773 |",
        },
        {
          id: 4,
          title: "1.2 位元運算",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目的核心藏在「數字的二進位位元」裡。當你看到 XOR、AND、OR 這些位元運算，往往可以把一個整數拆成一排 0/1 的位元，**每一位獨立處理**，問題瞬間變簡單。\n\n:::example 位元運算的基本工具\n先建立直覺。電腦裡的整數就是一串二進位位元，C++ 提供了直接操作它們的工具：\n\n```cpp\n// 位元運算的基本工具\nint x = 13;                     // 二進位 1101\nbool b = (x >> 2) & 1;          // 取第 2 位（從 0 算起）：1\nint y = x | (1 << 0);           // 把第 0 位設成 1\nint z = x & ~(1 << 0);          // 把第 0 位清成 0\nint c = __builtin_popcount(x);  // 數有幾個 1：3\n```\n:::\n\n\n\n這個技巧的精髓是兩條觀察：\n\n:::example 逐位獨立：分別統計每一位上有幾個 1，再各自決定那一位的答案\n第一，**XOR / AND / OR 都是「逐位獨立」的**。`a XOR b` 的第 k 位，只跟 `a`、`b` 的第 k 位有關，跟其他位無關。所以很多看似複雜的題目，可以「一位一位地單獨統計或決策」，互不干擾。\n\n```cpp\n// 逐位獨立：分別統計每一位上有幾個 1，再各自決定那一位的答案\nfor (int bit = 0; bit < 30; bit++) {\n  int cnt = 0;\n  for (int v : nums) {\n    cnt += (v >> bit) & 1;  // 這一位有幾個 1\n                            // ... 根據 cnt 決定答案的第 bit 位\n  }\n}\n```\n:::\n\n\n\n第二，**位元運算具有單調性**。例如 OR 只會「把 0 變 1，不會把 1 變 0」，所以子陣列越長，OR 值只增不減；AND 則相反，越長只減不增。這種單調性常和貪心或滑動視窗搭配，像「最長優雅子陣列」（題 2401）就是利用「AND 為 0 代表沒有共用的 1」。\n\n關鍵體會：與其把整數當成一個整體硬算，不如「拆成位元看」，讓每一位的決策變成獨立的小問題。\n\n**實作方式**\n\n把位元運算題拆成幾個固定步驟，逐一搭配程式碼：\n\n**1. 決定要不要拆位。** 若答案的每一位可以獨立決定（典型是 XOR/AND/OR 的計數題），就用「逐位迴圈」；若是利用單調性掃陣列，則維護一個累積的位元集合。\n\n:::example 逐位決策範例：讓某個位元目標最大化（示意）\n**2. 逐位統計或決策。** 對每一位 `bit`，掃過所有數字統計它的貢獻，再決定答案那一位該是 0 還是 1。\n\n```cpp\n// 逐位決策範例：讓某個位元目標最大化（示意）\nint ans = 0;\nfor (int bit = 29; bit >= 0; bit--) {  // 從高位到低位，高位優先\n  if (canSetBit(bit)) {\n    ans |= (1 << bit);  // 能設就設，貪心拿高位\n  }\n}\nreturn ans;\n```\n:::\n\n\n\n:::example AND 為 0 的最長子陣列：used 記錄視窗內已出現的位元\n**3. 用單調性配合滑動視窗。** 像「最長優雅子陣列」要求區間內任兩數 AND 為 0，等價於「各位元至多被用一次」。用一個 `used` 整數記錄視窗內已用的位元，衝突就收縮左界。\n\n```cpp\n// AND 為 0 的最長子陣列：used 記錄視窗內已出現的位元\nint used = 0, l = 0, ans = 0;\nfor (int r = 0; r < n; r++) {\n  while (used & nums[r]) {  // 與視窗有共用位元 → 收縮\n    used ^= nums[l++];      // 移出左端，清掉它的位元\n  }\n  used |= nums[r];  // 加入右端的位元\n  ans = max(ans, r - l + 1);\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 位數要取夠：題目值到 `10^9` 大約要看 30 位，到 `10^18` 要看 60 位並用 `long long`。\n- 移位前注意型別：`1 << 31` 在 `int` 會溢位，需要時用 `1LL << bit`。\n- 複雜度：逐位法通常是 `O(n × 位數)`，對 30 位來說近似 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「位元運算」想：\n\n- **題目明確出現 XOR / AND / OR / 位元 等字眼。** 例如「構造最小位運算陣列」（題 3315）、「最小無法得到的或值」（題 2568）。\n- **答案或限制可以「一位一位地」獨立判斷。** 這是拆位法的訊號：\n\n:::example 嗅到味道的瞬間：每一位互不影響，能分開算\n```cpp\n// 嗅到味道的瞬間：每一位互不影響，能分開算\n// 第 k 位的答案只取決於各數第 k 位 → 拆成 30 個獨立子問題\n```\n:::\n\n\n\n- **要找「分割成最多段使各段 OR/AND 滿足某條件」。** 像「將陣列分割成最多數目的子陣列」（題 2871）利用 AND 的單調性貪心分段。\n- **涉及二進位表示的構造或排列。** 如「迴圈碼排列」（題 1238）用格雷碼性質構造。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把問題「拆成每一位獨立」來看？\n2. 我用到的是哪種運算的單調性（OR 只增、AND 只減）？\n3. 位數與型別取夠了嗎（會不會溢位）？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3315 | [構造最小位運算陣列 II](https://leetcode.cn/problems/construct-the-minimum-bitwise-array-ii/) | 1715 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2871 | [將陣列分割成最多數目的子陣列](https://leetcode.cn/problems/split-array-into-maximum-number-of-subarrays/) | 1750 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2401 | [最長優雅子陣列](https://leetcode.cn/problems/longest-nice-subarray/) | 1750 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2568 | [最小無法得到的或值](https://leetcode.cn/problems/minimum-impossible-or/) | 1754 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 1238 | [迴圈碼排列](https://leetcode.cn/problems/circular-permutation-in-binary-representation/) | 1775 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1310. 子陣列異或查詢 展示核心寫法。\n\n**完整題目**：給定陣列與多個 `[l, r]` 查詢，回傳每段子陣列的 XOR 值。\n\n:::example 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR…\n**線索**：題目出現 XOR/AND/OR、mask 或二進位；先判斷每一位是否能獨立統計或從高位貪心。\n\n```cpp\nclass Solution {\n public:\n  // 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR 值\n  vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {\n    vector<int> pre(arr.size() + 1);  // pre[i] 為 arr[0..i-1] 的 XOR 前綴\n    for (int i = 0; i < (int)arr.size(); ++i) {\n      pre[i + 1] = pre[i] ^ arr[i];\n    }\n    vector<int> ans;\n    for (auto& q : queries) {\n      ans.push_back(pre[q[1] + 1] ^ pre[q[0]]);  // 區間 [q[0], q[1]] 的 XOR\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- XOR 具有自反性 `x ^ x = 0`，因此區間 XOR 可用前綴 XOR 相消。\n- 程式把整數轉成位元狀態或前綴 XOR，利用位元運算的可逆性/獨立性更新答案。\n- 涉及高位貪心時，迴圈通常從最高位往低位試，因為高位決定主要大小關係。\n\n**常見變形**\n\n- 前綴 XOR 處理區間異或。\n- 試填最高位處理最大 XOR/最大值。\n- SOS DP 處理子集包含關係。\n\n**實作注意事項**\n\n- 位移用 `1LL << b` 避免溢位。\n- 負數右移與符號位要小心。\n- 按位貢獻要確認每位獨立。\n- `__builtin_popcountll` 對 long long 使用 ll 版本。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3315 | [構造最小位運算陣列 II](https://leetcode.cn/problems/construct-the-minimum-bitwise-array-ii) | 1715 |\n| 2871 | [將陣列分割成最多數目的子陣列](https://leetcode.cn/problems/split-array-into-maximum-number-of-subarrays) | 1750 |\n| 2401 | [最長優雅子陣列](https://leetcode.cn/problems/longest-nice-subarray) | 1750 |\n| 2568 | [最小無法得到的或值](https://leetcode.cn/problems/minimum-impossible-or) | 1754 |\n| 1238 | [迴圈碼排列](https://leetcode.cn/problems/circular-permutation-in-binary-representation) | 1775 |\n| 3644 | [排序排列](https://leetcode.cn/problems/maximum-k-to-sort-a-permutation) | 1775 |",
        },
        {
          id: 5,
          title: "1.3 資料結構",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目本身不難，難的是「資料來來去去，你得隨時維護某個摘要並快速查詢」。例如隨時要拿最大／最小值、隨時要查某個東西出現幾次、隨時要把同類的東西配對掉。這時選對「資料結構（data structure）」就是解題的關鍵——同一題用對結構是 `O(n log n)`，用錯就是 `O(n²)`。\n\n這個技巧的核心心法是：**先問「我每一步要做什麼操作」，再挑「能把這些操作都做快」的結構**。最常用的兩個主力是「堆（priority_queue）」和「雜湊表（unordered_map）」。\n\n:::example 堆：隨時取出最大值（大根堆）\n堆讓你隨時 `O(1)` 取最大／最小、`O(log n)` 加入或移除堆頂，適合「每次都要拿目前最極端的那個」。\n\n```cpp\n// 堆：隨時取出最大值（大根堆）\npriority_queue<int> pq;\npq.push(5);\npq.push(8);\npq.push(2);\nint mx = pq.top();  // 8，O(1)\npq.pop();           // 移除最大值，O(log n)\n```\n:::\n\n\n\n:::example 雜湊表：統計每個值出現幾次\n雜湊表讓你 `O(1)` 記錄「某個鍵對應的數量／狀態」，適合計數、查重、配對。\n\n```cpp\n// 雜湊表：統計每個值出現幾次\nunordered_map<int, int> cnt;\nfor (int v : nums) {\n  cnt[v]++;  // 出現次數 +1，平均 O(1)\n}\nint times = cnt[target];  // 直接查，平均 O(1)\n```\n:::\n\n\n\n把這兩者組合，就能解很多題。例如「距離相等的條形碼」（題 1054）要把相同的數排得彼此不相鄰——策略是「每次都先放目前剩最多的那種」，這就用堆按「剩餘數量」排序，每次拿出最多的擺下。又如「積壓訂單」（題 1801）買賣撮合，用「兩個堆」分別維護買單（大根）與賣單（小根），新單來時和對面堆頂撮合。\n\n關鍵體會：題目的操作模式決定資料結構。「要極值」→堆；「要計數／查存在」→雜湊表；「要配對抵銷」→常是兩個堆或一個堆＋雜湊。\n\n**實作方式**\n\n把這類題拆成三個固定步驟，逐一搭配程式碼：\n\n:::example 例：區間分組（題 2406）——維護各組「目前最右端點」的最小值\n**1. 狀態表示：列出你需要的操作，挑對應結構。** 先寫下「我要反覆做哪些事」：取極值？計數？配對？再對號入座。\n\n```cpp\n// 例：區間分組（題 2406）——維護各組「目前最右端點」的最小值\n// 操作是「找最小結束點 + 更新它」→ 小根堆\npriority_queue<int, vector<int>, greater<int>> ends;  // 各組結束時間\n```\n:::\n\n\n\n:::example 區間分組：能接續就重用一組，否則新開一組\n**2. 更新流程：每來一筆資料，按規則動結構。** 把進來的元素依規則 push / pop。區間分組：把區間按開始排序，逐一處理，能接到舊組就更新堆頂，否則開新組。\n\n```cpp\n// 區間分組：能接續就重用一組，否則新開一組\nsort(intervals.begin(), intervals.end());\nfor (auto& [l, r] : intervals) {\n  if (!ends.empty() && ends.top() < l) {\n    ends.pop();  // 舊組空出來了\n  }\n  ends.push(r);  // 放進（重用或新開）\n}\n// ends.size() 就是最少組數\n```\n:::\n\n\n\n**3. 答案讀取：從結構直接讀摘要。** 維護到最後，答案常常就是 `堆的大小`、`堆頂值`、或某個累加計數。\n\n**4. 邊界與複雜度。**\n\n- 堆預設是大根堆；要小根堆得寫 `greater<int>` 或存負數。\n- 計數題注意鍵的設計：若要區分「值相同但位置不同」，鍵要帶上位置或時間戳。像「K 個可整除元素的子陣列」（題 2261）要對「子陣列內容」去重，常用雜湊把序列編碼成鍵。\n- 複雜度：堆操作 `O(log n)`，n 筆資料合計 `O(n log n)`；雜湊平均 `O(1)`，合計 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「選對資料結構」想：\n\n- **要反覆取「目前最大／最小／最頻繁」的元素。** 像「條形碼」每步都拿剩最多的 → 堆。\n- **有「加入、移除、撮合」交錯發生的動態流程。** 像「積壓訂單」邊下單邊撮合 → 兩個堆：\n\n:::example 嗅到味道的瞬間：要一邊加入、一邊取極值並移除\n```cpp\n// 嗅到味道的瞬間：要一邊加入、一邊取極值並移除\n// 暴力每次掃全部找極值是 O(n²)；堆把每步壓成 O(log n)\n```\n:::\n\n\n\n- **要快速回答「某東西出現過嗎／幾次」。** → 雜湊表計數或查重，如「K 個可整除元素的子陣列」。\n- **要對「字串前綴」做批量查詢／計分。** 如「字串的字首分數和」（題 2416）用 Trie（字典樹）讓共同前綴只算一次。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每一步反覆做的「核心操作」是什麼（取極值？計數？配對？）？\n2. 哪個結構能把這個操作做到 `O(log n)` 或 `O(1)`？\n3. 我的「鍵」或「排序依據」設計對了嗎（會不會把該區分的東西混在一起）？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1054 | [距離相等的條形碼](https://leetcode.cn/problems/distant-barcodes/) | 1702 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 1801 | [積壓訂單中的訂單總數](https://leetcode.cn/problems/number-of-orders-in-the-backlog/) | 1711 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 2406 | [將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups/) | 1713 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 2261 | [含最多 K 個可整除元素的子陣列](https://leetcode.cn/problems/k-divisible-elements-subarrays/) | 1724 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 2416 | [字串的字首分數和](https://leetcode.cn/problems/sum-of-prefix-scores-of-strings/) | 1725 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n\n**C++ 範例講解**\n\n以下用 2406. 將區間分為最少組數 展示資料結構（掃描線 + 堆）的核心寫法。\n\n**完整題目**：給若干區間 `intervals[i] = [left, right]`，要把它們分成最少的組，使同一組內任意兩區間都不重疊（端點相同也算重疊）。求最少組數。\n\n:::example 貪心 + 最小堆：按起點排序後，嘗試將區間接到最早結束的組\n**線索**：最少組數等於「任一時刻最多有幾個區間同時覆蓋」。把區間按左端點排序，用最小堆保存每組目前的最後結束時間：新區間若能接在某組之後就重用該組，否則新開一組。\n\n```cpp\nclass Solution {\n public:\n  int minGroups(vector<vector<int>>& intervals) {\n    // 貪心 + 最小堆：按起點排序後，嘗試將區間接到最早結束的組\n    sort(intervals.begin(), intervals.end());\n    priority_queue<int, vector<int>, greater<>> pq;  // 堆頂為當前各組的最小結束時間\n    for (auto& it : intervals) {\n      // 若最早結束的組在當前區間開始前已結束，可重用該組\n      if (!pq.empty() && pq.top() < it[0]) {\n        pq.pop();\n      }\n      pq.push(it[1]);  // 將當前區間的結束時間加入（新開一組或接在舊組後）\n    }\n    return pq.size();  // 堆的大小即所需最少組數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 區間依左端點排序，確保處理順序與時間軸一致。\n- 堆中每個元素代表一組目前的「最後結束時間」。\n- 若堆頂（最早結束的組）已結束於新區間開始前，就重用該組（pop 再 push）。\n- 否則必須新開一組（直接 push）；最終堆大小即最少組數。\n\n**常見變形**\n\n- 會議室 II（253）：求同時進行的最大會議數。\n- 用差分陣列 / 掃描線在端點 +1 / -1，取前綴最大值同樣可解。\n- 航班預訂、車輛調度等「同時佔用數」問題皆同構。\n\n**實作注意事項**\n\n- 端點相同視為重疊，判斷要用 `pq.top() < it[0]`（嚴格小於）。\n- 只需重用「最早結束」的一組，所以用最小堆而非掃全部。\n- 此題不必真的記錄分組內容，堆大小就是答案。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1054 | [距離相等的條形碼](https://leetcode.cn/problems/distant-barcodes) | 1702 |\n| 1801 | [積壓訂單中的訂單總數](https://leetcode.cn/problems/number-of-orders-in-the-backlog) | 1711 |\n| 2406 | [將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups) | 1713 |\n| 2261 | [含最多 K 個可整除元素的子陣列](https://leetcode.cn/problems/k-divisible-elements-subarrays) | 1724 |\n| 2416 | [字串的字首分數和](https://leetcode.cn/problems/sum-of-prefix-scores-of-strings) | 1725 |\n| 1014 | [最佳觀光組合](https://leetcode.cn/problems/best-sightseeing-pair) | 1730 |\n| 1292 | [元素和小於等於閾值的正方形的最大邊長](https://leetcode.cn/problems/maximum-side-length-of-a-square-with-sum-less-than-or-equal-to-threshold) | 1735 |\n| 1814 | [統計一個陣列中好對子的數目](https://leetcode.cn/problems/count-nice-pairs-in-an-array) | 1738 |\n| 2502 | [設計記憶體分配器](https://leetcode.cn/problems/design-memory-allocator) | 1746 |\n| 1111 | [有效括號的巢狀深度](https://leetcode.cn/problems/maximum-nesting-depth-of-two-valid-parentheses-strings) | 1749 |\n| 3478 | [選出和最大的 K 個元素](https://leetcode.cn/problems/choose-k-elements-with-maximum-sum) | 1753 |\n| 1541 | [平衡括號字串的最少插入次數](https://leetcode.cn/problems/minimum-insertions-to-balance-a-parentheses-string) | 1759 |\n| 3584 | [子序列首尾元素的最大乘積](https://leetcode.cn/problems/maximum-product-of-first-and-last-elements-of-a-subsequence) | 1763 |\n| 2462 | [僱傭 K 位工人的總代價](https://leetcode.cn/problems/total-cost-to-hire-k-workers) | 1764 |\n| 2905 | [找出滿足差值條件的下標 II](https://leetcode.cn/problems/find-indices-with-index-and-value-difference-ii) | 1764 |\n| 3170 | [刪除星號以後字典序最小的字串](https://leetcode.cn/problems/lexicographically-minimum-string-after-removing-stars) | 1772 |\n| 1895 | [最大的幻方](https://leetcode.cn/problems/largest-magic-square) | 1781 |\n| 2353 | [設計食物評分系統](https://leetcode.cn/problems/design-a-food-rating-system) | 1782 |\n| 2653 | [滑動子陣列的美麗值](https://leetcode.cn/problems/sliding-subarray-beauty) | 1786 |\n| 1497 | [檢查陣列對是否可以被 k 整除](https://leetcode.cn/problems/check-if-array-pairs-are-divisible-by-k) | 1787 |\n| 3092 | [最高頻率的 ID](https://leetcode.cn/problems/most-frequent-ids) | 1793 |\n| 2615 | [等值距離和](https://leetcode.cn/problems/sum-of-distances) | 1793 |\n| 2381 | [字母移位 II](https://leetcode.cn/problems/shifting-letters-ii) | 1793 |\n| 1834 | [單執行緒 CPU](https://leetcode.cn/problems/single-threaded-cpu) | 1798 |",
        },
        {
          id: 6,
          title: "1.4 動態規劃",
          summary:
            "**原理講解**\n\n本節要解決的場景是：一個大問題可以拆成許多「重疊的小問題」，而且大問題的最優解，能由小問題的最優解組合出來。這種結構就用「動態規劃（dynamic programming, DP）」——把每個小問題只算一次、把答案存起來，避免重複計算。\n\n先抓住兩個前提條件：\n\n1. **最優子結構**：大問題的最佳解，可以由更小子問題的最佳解拼出來。\n2. **重疊子問題**：同一個子問題會被反覆用到，所以值得「記下來」。\n\n:::example 狀態：dp[j] = 能否湊出金額 j（布林）\nDP 的精髓是定義一個「狀態」`dp[...]`，代表某個子問題的答案，再寫出「轉移方程」描述它怎麼由更小的狀態推出來。以「最接近目標價格的甜點成本」（題 1774）那類「能不能湊出某個總額」的問題為例，最經典的入門模型是「能否用硬幣湊出金額 j」：\n\n```cpp\n// 狀態：dp[j] = 能否湊出金額 j（布林）\nvector<char> dp(target + 1, false);\ndp[0] = true;             // 湊出 0 永遠可行（什麼都不選）\nfor (int coin : coins) {  // 逐一考慮每種硬幣\n  for (int j = coin; j <= target; j++) {\n    if (dp[j - coin]) {\n      dp[j] = true;  // j-coin 可行 → 再加一枚就湊出 j\n                     // dp[target] 即答案\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 最優值 DP 的通用形狀：當前狀態 = 由若干前態取最優\n如果題目要的是「最優值」（最大／最小），狀態就存數值而非布林，轉移時取 `min` 或 `max`。「最大相容性評分和」（題 1947）、「相同分數的最大運算次數」（題 3040）都屬於這類「最優值 DP」。\n\n```cpp\n// 最優值 DP 的通用形狀：當前狀態 = 由若干前態取最優\n// dp[i] = max(dp[i], dp[前態] + 這一步的收益)\n```\n:::\n\n\n\n關鍵體會：DP 的難點不在程式碼，而在**想清楚「狀態代表什麼」**。狀態定得好，轉移自然浮現；定不好，怎麼寫都繞。\n\n**實作方式**\n\n把任何 DP 題拆成四個固定步驟，逐一搭配程式碼：\n\n**1. 定義狀態：寫下 `dp[...]` 的精確含義。** 用一句話講清楚每個下標代表的子問題。例如「dp[j] = 湊出金額 j 的最少硬幣數」「dp[i][j] = 前 i 個人配前 j 個工作的最大相容分」。含義模糊是所有 DP bug 的根源。\n\n:::example 完全背包：dp[j] = 湊出 j 的最少硬幣數，由「最後放哪一枚」推來\n**2. 由「最後一步」推轉移。** 想像最優解的最後一個決策：它把問題縮小成哪個子問題？把所有可能的「最後一步」列出來取最優。\n\n```cpp\n// 完全背包：dp[j] = 湊出 j 的最少硬幣數，由「最後放哪一枚」推來\nvector<int> dp(target + 1, INF);\ndp[0] = 0;\nfor (int coin : coins) {\n  for (int j = coin; j <= target; j++) {\n    dp[j] = min(dp[j], dp[j - coin] + 1);  // 最後放一枚 coin\n  }\n}\n```\n:::\n\n\n\n:::example 計算順序要與依賴方向一致：這裡 dp[j] 依賴更小的 j，故 j 由小到大\n**3. 確定初始值與計算順序。** 起點（如 `dp[0]`）要先填對；遍歷順序要保證「算 `dp[i]` 時，它依賴的前態都已算好」。\n\n```cpp\n// 計算順序要與依賴方向一致：這裡 dp[j] 依賴更小的 j，故 j 由小到大\n// 若是 0/1 背包（每樣只能用一次），則 j 要由大到小，避免重複使用\n```\n:::\n\n\n\n**4. 讀出答案、注意邊界與複雜度。**\n\n- 答案位置：常是 `dp[target]` 或 `dp[n][...]`；無解時用哨兵值（如 `INF`）標記。\n- 邊界：陣列開夠大、起點填對、無解情況要能判斷。\n- 複雜度：通常是「狀態數 × 每個狀態的轉移成本」。一維金額 DP 是 `O(n × target)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「動態規劃」想：\n\n- **問「有幾種方法／能不能湊出／最多最少」，且每步有「選或不選、選哪個」的決策。** 像「硬幣面值還原」（題 3592）、「甜點成本」（題 1774）。\n- **暴力是「枚舉所有組合」會指數爆炸，但子問題會重複出現。** 這正是 DP 取代暴力的時機：\n\n:::example 嗅到味道的瞬間：遞迴枚舉所有選法 → 同一個子問題被算很多遍\n```cpp\n// 嗅到味道的瞬間：遞迴枚舉所有選法 → 同一個子問題被算很多遍\n// 把子問題的答案存進 dp[...]，每個只算一次 → 從指數降到多項式\n```\n:::\n\n\n\n- **問「最大／最小的某個值」，且最優解能由更小規模的最優解推出。** 像「最大相容性評分和」（題 1947）、「適合野炊的日子」（題 2100，先用前綴 DP 算每天往前往後的連續趨勢）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `dp[...]` 一句話含義是什麼？下標代表哪個子問題？\n2. 最優解的「最後一步」有哪些可能？轉移怎麼從前態取 `min/max`？\n3. 初始值填對了嗎？遍歷順序保證依賴都先算好了嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3592 | [硬幣面值還原](https://leetcode.cn/problems/inverse-coin-change/) | 1701 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 1774 | [最接近目標價格的甜點成本](https://leetcode.cn/problems/closest-dessert-cost/) | 1702 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 2100 | [適合野炊的日子](https://leetcode.cn/problems/find-good-days-to-rob-the-bank/) | 1702 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 1947 | [最大相容性評分和](https://leetcode.cn/problems/maximum-compatibility-score-sum/) | 1704 | 這是最優值 DP；練習把最後一步拆成若干前態，並確認取 `min/max` 的狀態已完整。 |\n| 3040 | [相同分數的最大運算元目 II](https://leetcode.cn/problems/maximum-number-of-operations-with-the-same-score-ii/) | 1709 | 這是最優值 DP；練習把最後一步拆成若干前態，並確認取 `min/max` 的狀態已完整。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1425. 帶限制的子序列和 展示核心寫法。\n\n**完整題目**：選擇一個非空子序列，使相鄰被選元素下標差不超過 k，並最大化元素和。\n\n:::example 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以…\n**線索**：題目能拆成前綴、位置或選擇序列，且不同路徑會到達相同子問題；需要把影響未來的資訊寫成狀態。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以 i 結尾的最大子集和\n  int constrainedSubsetSum(vector<int>& nums, int k) {\n    deque<int> dq;  // 單調遞減 deque，儲存下標，保證 dp[dq.front()] 為窗口最大值\n    vector<int> dp(nums.size());\n    int ans = INT_MIN;\n    for (int i = 0; i < (int)nums.size(); ++i) {\n      // 移除已超出 k 範圍的窗口頭部\n      while (!dq.empty() && dq.front() < i - k) {\n        dq.pop_front();\n      }\n      // dp[i]：選 nums[i] 加上前 k 個位置中最大的正 dp 值（若最大值為負則不取前段）\n      dp[i] = nums[i] + (dq.empty() ? 0 : max(0, dp[dq.front()]));\n      // 維護單調遞減性：彈出所有 dp 值不大於 dp[i] 的尾部\n      while (!dq.empty() && dp[dq.back()] <= dp[i]) {\n        dq.pop_back();\n      }\n      dq.push_back(i);\n      ans = max(ans, dp[i]);\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `dp[i]` 需要查前 k 個位置的最大 dp 值；單調佇列把這個區間最大值查詢降成均攤 O(1)。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 前綴和優化。\n- 單調佇列/堆疊優化。\n- 樹狀陣列、線段樹、斜率優化。\n\n**實作注意事項**\n\n- 先保留樸素式方便驗證。\n- 資料結構查的是哪個前驅集合要寫清楚。\n- 單調性不成立時不能硬套。\n- 可用小範圍對拍確認優化等價。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3592 | [硬幣面值還原](https://leetcode.cn/problems/inverse-coin-change) | 1701 |\n| 1774 | [最接近目標價格的甜點成本](https://leetcode.cn/problems/closest-dessert-cost) | 1702 |\n| 2100 | [適合野炊的日子](https://leetcode.cn/problems/find-good-days-to-rob-the-bank) | 1702 |\n| 1947 | [最大相容性評分和](https://leetcode.cn/problems/maximum-compatibility-score-sum) | 1704 |\n| 3040 | [相同分數的最大運算元目 II](https://leetcode.cn/problems/maximum-number-of-operations-with-the-same-score-ii) | 1709 |\n| 2944 | [購買水果需要的最少金幣數](https://leetcode.cn/problems/minimum-number-of-coins-for-fruits) | 1709 |\n| 2140 | [解決智力問題](https://leetcode.cn/problems/solving-questions-with-brainpower) | 1709 |\n| 1567 | [乘積為正數的最長子陣列長度](https://leetcode.cn/problems/maximum-length-of-subarray-with-positive-product) | 1710 |\n| 2385 | [感染二叉樹需要的總時間](https://leetcode.cn/problems/amount-of-time-for-binary-tree-to-be-infected) | 1711 |\n| 2826 | [將三個組排序](https://leetcode.cn/problems/sorting-three-groups) | 1721 |\n| 1220 | [統計母音字母序列的數目](https://leetcode.cn/problems/count-vowels-permutation) | 1730 |\n| 2786 | [訪問陣列中的位置使分數最大](https://leetcode.cn/problems/visit-array-positions-to-maximize-score) | 1733 |\n| 2707 | [字串中的額外字元](https://leetcode.cn/problems/extra-characters-in-a-string) | 1736 |\n| 1079 | [活字印刷](https://leetcode.cn/problems/letter-tile-possibilities) | 1741 |\n| 1191 | [K 次串聯後最大子陣列之和](https://leetcode.cn/problems/k-concatenation-maximum-sum) | 1748 |\n| 2400 | [恰好移動 k 步到達某一位置的方法數目](https://leetcode.cn/problems/number-of-ways-to-reach-a-position-after-exactly-k-steps) | 1751 |\n| 3628 | [插入一個字母的最大子序列數](https://leetcode.cn/problems/maximum-number-of-subsequences-after-one-inserting) | 1754 |\n| 1027 | [最長等差數列](https://leetcode.cn/problems/longest-arithmetic-subsequence) | 1759 |\n| 1262 | [可被三整除的最大和](https://leetcode.cn/problems/greatest-sum-divisible-by-three) | 1762 |\n| 3573 | [買賣股票的最佳時機 V](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-v) | 1777 |\n| 918 | [環形子陣列的最大和](https://leetcode.cn/problems/maximum-sum-circular-subarray) | 1777 |\n| 1824 | [最少側跳次數](https://leetcode.cn/problems/minimum-sideway-jumps) | 1778 |\n| 2369 | [檢查陣列是否存在有效劃分](https://leetcode.cn/problems/check-if-there-is-a-valid-partition-for-the-array) | 1780 |\n| 1911 | [最大交替子序列和](https://leetcode.cn/problems/maximum-alternating-subsequence-sum) | 1786 |\n| 983 | [最低票價](https://leetcode.cn/problems/minimum-cost-for-tickets) | 1786 |\n| 1312 | [讓字串成為迴文串的最少插入次數](https://leetcode.cn/problems/minimum-insertion-steps-to-make-a-string-palindrome) | 1787 |\n| 1510 | [石子游戲 IV](https://leetcode.cn/problems/stone-game-iv) | 1787 |\n| 2321 | [拼接陣列的最大分數](https://leetcode.cn/problems/maximum-score-of-spliced-array) | 1791 |\n| 2771 | [構造最長非遞減子陣列](https://leetcode.cn/problems/longest-non-decreasing-subarray-from-two-arrays) | 1792 |\n| 3376 | [破解鎖的最少時間 I](https://leetcode.cn/problems/minimum-time-to-break-locks-i) | 1793 |\n| 1653 | [使字串平衡的最少刪除次數](https://leetcode.cn/problems/minimum-deletions-to-make-string-balanced) | 1794 |\n| 2998 | [使 X 和 Y 相等的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-x-and-y-equal) | 1795 |\n| 3418 | [機器人可以獲得的最大金幣數](https://leetcode.cn/problems/maximum-amount-of-money-robot-can-earn) | 1798 |\n| 1186 | [刪除一次得到子陣列最大和](https://leetcode.cn/problems/maximum-subarray-sum-with-one-deletion) | 1799 |",
        },
        {
          id: 7,
          title: "1.5 圖論演算法",
          summary:
            "**原理講解**\n\n本節要解決的場景是：資料之間有「連接關係」——城市與道路、課程與先修、節點與邊。一旦你能把問題畫成「點 + 邊」的圖，就能套用「圖論演算法（graph algorithms）」來回答「能不能到達」「最短要多久」「怎麼分類」這類問題。\n\n:::example 鄰接表：g[u] 存所有 u 能直接走到的點\n第一步永遠是**建圖**：想清楚「點代表什麼、邊代表什麼」。最常用的存法是鄰接表。\n\n```cpp\n// 鄰接表：g[u] 存所有 u 能直接走到的點\nvector<vector<int>> g(n);\nfor (auto& e : edges) {\n  g[e[0]].push_back(e[1]);\n  g[e[1]].push_back(e[0]);  // 無向圖兩邊都加；有向圖只加一邊\n}\n```\n:::\n\n\n\n:::example BFS：從 start 一層層擴散，順便算出到各點的「步數」\n建好圖後，最基礎的兩種「走訪」是 DFS（深度優先，沿一條路走到底再回頭）和 BFS（廣度優先，一圈一圈往外擴）。它們能解「從某點出發能到達哪些點」「連通分量」「染色」等問題。例如「移除可疑的方法」（題 3310）就是從可疑點出發 DFS，標記所有受牽連的方法。\n\n```cpp\n// BFS：從 start 一層層擴散，順便算出到各點的「步數」\nqueue<int> q;\nq.push(start);\nvector<int> dist(n, -1);\ndist[start] = 0;\nwhile (!q.empty()) {\n  int u = q.front();\n  q.pop();\n  for (int v : g[u]) {\n    if (dist[v] == -1) {      // 還沒走過\n      dist[v] = dist[u] + 1;  // 多走一步\n      q.push(v);\n    }\n  }\n}\n```\n:::\n\n\n\nBFS 有個關鍵性質：**邊權都是 1 時，它第一次碰到某點的距離就是最短距離**。所以「找到離兩個節點最近的節點」（題 2359）可以從兩點各做一次 BFS，再合併。\n\n:::example Dijkstra 的核心：用小根堆每次取目前最近的點\n但若每條邊的「代價」不一樣（不再是 1），BFS 就不夠了，要用「Dijkstra」——本質是「用小根堆，每次取出目前距離最小的點去鬆弛它的鄰居」。「到達最後一個房間的最少時間」（題 3341）、「訪問消失節點的最少時間」（題 3112）都是這類帶權最短路。\n\n```cpp\n// Dijkstra 的核心：用小根堆每次取目前最近的點\npriority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;\npq.push({0, start});  // {距離, 點}\n                      // 取出堆頂 → 對每條邊 (u->v, w) 嘗試 dist[u]+w 更新 dist[v]\n```\n:::\n\n\n\n關鍵體會：圖論題的一半工夫在「建模」——把題目翻譯成點和邊；另一半在「選對走訪法」——無權最短路用 BFS，帶權非負用 Dijkstra。\n\n**實作方式**\n\n把圖論題拆成四個固定步驟，逐一搭配程式碼：\n\n**1. 建模：定義點、邊與額外狀態。** 點是什麼？邊是什麼？有沒有額外維度（如「剩餘時間」「鑰匙數」）要併進狀態裡？把這些併進去就叫「狀態圖」。\n\n:::example 判斷標準：邊權是 1 還是 0/1 還是任意非負\n**2. 選走訪法。** 只問連通／可達 → DFS 或 BFS；邊權都是 1 的最短路 → BFS；邊權為非負一般值 → Dijkstra。\n\n```cpp\n// 判斷標準：邊權是 1 還是 0/1 還是任意非負\n// 1     → BFS\n// 0/1   → 0-1 BFS（雙端佇列）\n// 非負  → Dijkstra（小根堆）\n```\n:::\n\n\n\n:::example Dijkstra 主迴圈：跳過過期記錄，鬆弛鄰居\n**3. 維護 visited / dist，避免重複。** 走過的點要標記，否則會無限繞圈。BFS/Dijkstra 用 `dist` 陣列兼當 visited。\n\n```cpp\n// Dijkstra 主迴圈：跳過過期記錄，鬆弛鄰居\nwhile (!pq.empty()) {\n  auto [d, u] = pq.top();\n  pq.pop();\n  if (d > dist[u]) {\n    continue;  // 過期的舊記錄，跳過\n  }\n  for (auto& [v, w] : g[u]) {\n    if (dist[u] + w < dist[v]) {  // 找到更短的路\n      dist[v] = dist[u] + w;\n      pq.push({dist[v], v});\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 答案讀取、邊界與複雜度。**\n\n- 染色／二分圖問題（如「不鄰接植花」題 1042）用 DFS/BFS 給每點塗色，相鄰必異色。\n- 起點忘了初始化、無向圖忘了加雙向邊，是最常見的兩個 bug。\n- 複雜度：BFS/DFS 是 `O(V + E)`；Dijkstra 用堆是 `O(E log V)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「圖論演算法」想：\n\n- **資料天然有「連接」關係：依賴、道路、相鄰、引用。** 像「移除可疑的方法」（題 3310）的呼叫關係、「不鄰接植花」（題 1042）的花園相鄰。\n- **問「從 A 能不能到 B」「最少幾步／最少時間到」。** 無權用 BFS、帶權用 Dijkstra：\n\n:::example 嗅到味道的瞬間：問「最短 / 最少步」且有點邊結構\n```cpp\n// 嗅到味道的瞬間：問「最短 / 最少步」且有點邊結構\n// 暴力枚舉所有路徑會指數爆炸；BFS/Dijkstra 是多項式時間\n```\n:::\n\n\n\n- **狀態除了「位置」還要記別的（時間、鑰匙、是否消失）。** 把它併進節點變成「狀態圖」，如「訪問消失節點」（題 3112）要在節點消失前抵達。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「點」和「邊」各代表什麼？要不要加額外狀態維度？\n2. 邊權是 1、0/1、還是一般非負？對應該選 BFS 還是 Dijkstra？\n3. 我有沒有正確初始化起點、標記走訪、處理無向圖的雙向邊？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3310 | [移除可疑的方法](https://leetcode.cn/problems/remove-methods-from-project/) | 1711 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 1042 | [不鄰接植花](https://leetcode.cn/problems/flower-planting-with-no-adjacent/) | 1712 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 2359 | [找到離給定兩個節點最近的節點](https://leetcode.cn/problems/find-closest-node-to-given-two-nodes/) | 1715 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 3341 | [到達最後一個房間的最少時間 I](https://leetcode.cn/problems/find-minimum-time-to-reach-last-room-i/) | 1721 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n| 3112 | [訪問消失節點的最少時間](https://leetcode.cn/problems/minimum-time-to-visit-disappearing-nodes/) | 1757 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 743. 網路延遲時間 展示核心寫法。\n\n**完整題目**：給定有向帶權邊、節點數與起點，求訊號從起點傳到所有節點所需的最長最短路時間；若有節點不可達則回傳 `-1`。\n\n:::example Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n**線索**：題目描述可達、路徑、依賴或關係傳遞；把物件當節點、操作當邊後，就能選遍歷或最短路。\n\n```cpp\nclass Solution {\n public:\n  // Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n  int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    vector<vector<pair<int, int>>> g(n + 1);  // 鄰接表，g[u] = {(v, w)}\n    for (auto& e : times) {\n      g[e[0]].push_back({e[1], e[2]});\n    }\n    const int INF = 1e9;\n    vector<int> dist(n + 1, INF);\n    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;  // 最小堆：{距離, 節點}\n    dist[k] = 0;\n    pq.push({0, k});\n    while (!pq.empty()) {\n      auto [d, u] = pq.top();\n      pq.pop();\n      if (d != dist[u]) {\n        continue;  // 已有更短路徑，跳過過期條目\n      }\n      for (auto [v, w] : g[u]) {\n        if (dist[v] > d + w) {\n          dist[v] = d + w;  // 鬆弛操作\n          pq.push({dist[v], v});\n        }\n      }\n    }\n    // 所有節點最短距離的最大值即為信號傳播時間\n    int ans = *max_element(dist.begin() + 1, dist.end());\n    return ans == INF ? -1 : ans;  // 若有節點不可達則返回 -1\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 非負權最短路使用 Dijkstra；堆中舊距離要跳過，避免重複鬆弛造成常數過大。\n- 先建出鄰接關係，再用 `visited`、`dist`、`indeg` 等狀態控制每個節點何時處理。\n- 如果同一節點在不同資源狀態下未來不同，就不能只用節點編號當 visited 鍵。\n\n**常見變形**\n\n- 無權最短路用 BFS。\n- 0/1 權用 0-1 BFS。\n- 非負權用 Dijkstra；有負權需 Bellman-Ford/SPFA 類思路。\n\n**實作注意事項**\n\n- 先估點數與邊數，決定鄰接表或矩陣。\n- Dijkstra 不適用負權邊。\n- 多源 BFS 要把所有源點同時入隊。\n- 狀態圖要包含資源、鑰匙、次數等額外維度。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3310 | [移除可疑的方法](https://leetcode.cn/problems/remove-methods-from-project) | 1711 |\n| 1042 | [不鄰接植花](https://leetcode.cn/problems/flower-planting-with-no-adjacent) | 1712 |\n| 2359 | [找到離給定兩個節點最近的節點](https://leetcode.cn/problems/find-closest-node-to-given-two-nodes) | 1715 |\n| 3341 | [到達最後一個房間的最少時間 I](https://leetcode.cn/problems/find-minimum-time-to-reach-last-room-i) | 1721 |\n| 3112 | [訪問消失節點的最少時間](https://leetcode.cn/problems/minimum-time-to-visit-disappearing-nodes) | 1757 |\n| 2685 | [統計完全連通分量的數量](https://leetcode.cn/problems/count-the-number-of-complete-components) | 1769 |\n| 1129 | [顏色交替的最短路徑](https://leetcode.cn/problems/shortest-path-with-alternating-colors) | 1780 |\n| 851 | [喧鬧和富有](https://leetcode.cn/problems/loud-and-rich) | 1783 |\n| 3387 | [兩天自由外匯交易後的最大貨幣數](https://leetcode.cn/problems/maximize-amount-after-two-days-of-conversions) | 1788 |\n| 2192 | [有向無環圖中一個節點的所有祖先](https://leetcode.cn/problems/all-ancestors-of-a-node-in-a-directed-acyclic-graph) | 1788 |\n| 3219 | [切蛋糕的最小總開銷 II](https://leetcode.cn/problems/minimum-cost-for-cutting-cake-ii) | 1789 |\n| 886 | [可能的二分法](https://leetcode.cn/problems/possible-bipartition) | 1795 |",
        },
        {
          id: 8,
          title: "1.6 貪心",
          summary:
            "**原理講解**\n\n本節要解決的場景是：問題需要做出一連串選擇以達到最優，而你發現「每一步都直接挑當下看起來最好的那個」，最後竟然就能得到全域最優解。這種策略叫「貪心（greedy）」——不回頭、不嘗試所有組合，每步只做局部最佳決策。\n\n貪心最吸引人的地方是簡單又快，但**危險也在這裡**：不是所有題目「局部最好」都能拼成「全域最好」。所以貪心真正的功夫，不在寫程式，而在**證明這個貪心策略是對的**。常用的證明方式是「交換論證（exchange argument）」：假設有一個最優解和你的貪心選擇不同，證明把它「換成」貪心的選擇後不會變差，於是貪心解也是最優。\n\n:::example 選最多不相交區間：按結束點排序，貪心地選結束最早的\n舉一個典型例子：要選不相交的區間使數量最多（「不相交子字串的最大數量」題 3557 即此類）。貪心策略是「按結束位置排序，每次選結束最早且不衝突的」。\n\n```cpp\n// 選最多不相交區間：按結束點排序，貪心地選結束最早的\nsort(iv.begin(), iv.end(), [](auto& a, auto& b) { return a[1] < b[1]; });\nint cnt = 0, lastEnd = -1;\nfor (auto& x : iv) {\n  if (x[0] > lastEnd) {  // 與上一個不衝突\n    cnt++;\n    lastEnd = x[1];  // 更新「已佔用到哪」\n  }\n}\n```\n:::\n\n\n\n為什麼選「結束最早」而不是「最長」或「最早開始」？因為結束越早，留給後面的空間越多——這就是交換論證的直覺：任何最優解的第一個區間，都能換成「結束最早的那個」而不變差。\n\n:::example 排序 + 堆：在符合條件的候選裡，隨時取目前收益最大的\n另一個常見模式是「排序 + 堆」：先排序確立處理順序，再用堆動態維護「目前最值得拿的東西」。「選擇 K 個任務的最大總分數」（題 3767）、「喫披薩」（題 3457）多屬此類。\n\n```cpp\n// 排序 + 堆：在符合條件的候選裡，隨時取目前收益最大的\npriority_queue<int> pq;  // 大根堆，存可選項的收益\n                         // 依某種順序掃描，把「現在可選」的丟進堆，需要時取堆頂最大收益\n```\n:::\n\n\n\n關鍵體會：貪心 = 找到一個「正確的排序／選擇規則」，並能說服自己「每步拿最好的不會錯」。寫不出證明時，要警惕貪心可能是錯的，改用 DP。\n\n**實作方式**\n\n把貪心題拆成四個固定步驟，逐一搭配程式碼：\n\n**1. 找出貪心規則（按什麼排序、每步選什麼）。** 這是最關鍵的一步。常見規則：按結束時間排（區間題）、按代價／收益比排、從高位或極端位置先決策。\n\n:::example 心裡的證明草稿（不是程式）\n**2. 用交換論證或歸納說服自己規則正確。** 問：「若把最優解的第一步換成我的貪心選擇，會更差嗎？」答案是「不會更差」，規則才成立。\n\n```cpp\n// 心裡的證明草稿（不是程式）：\n// 設最優解 OPT 第一步選了 X，貪心會選 G。\n// 證明把 OPT 的 X 換成 G 後仍合法且不更差 → 貪心可達最優。\n```\n:::\n\n\n\n:::example 樹上貪心：分配硬幣（題 979）——每條邊的搬運量就是子樹盈虧的絕對值\n**3. 依規則一次掃描（常需先排序）。** 排好序後，多半一個 for 迴圈就能跑完，邊掃邊維護「目前狀態」。\n\n```cpp\n// 樹上貪心：分配硬幣（題 979）——每條邊的搬運量就是子樹盈虧的絕對值\nint moves = 0;\nfunction<int(TreeNode*)> dfs = [&](TreeNode* node) -> int {\n  if (!node) {\n    return 0;\n  }\n  int l = dfs(node->left), r = dfs(node->right);\n  moves += abs(l) + abs(r);      // 經過這條邊搬的硬幣數\n  return node->val + l + r - 1;  // 本子樹的盈餘（多餘往上送/不足往上要）\n};\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 「K 次操作最大化頂端元素」（題 2202）這類要小心 K 的奇偶、K 與長度的關係等邊界，貪心常在這些角落出錯。\n- 若想不出正確性證明、或找到反例，貪心就不適用，回頭考慮 DP。\n- 複雜度：排序主導，通常 `O(n log n)`；純掃描部分 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「貪心」想：\n\n- **要在一堆選項裡「選最多／價值最大」，且存在明顯的「優先順序」。** 像區間題按結束排（題 3557）、任務題按收益排（題 3767）。\n- **每一步的最佳選擇不依賴未來，當下就能定。** 這是貪心可行的核心訊號：\n\n:::example 嗅到味道的瞬間：當前決策不需要回頭改，未來也不會推翻它\n```cpp\n// 嗅到味道的瞬間：當前決策不需要回頭改，未來也不會推翻它\n// 若「現在的選擇會影響未來且需權衡」→ 多半是 DP 而非貪心\n```\n:::\n\n\n\n- **從高位 / 極端位置往下決策能逐步逼近答案。** 如「K 次操作最大化頂端元素」（題 2202）。\n- **樹上的盈虧需要沿邊「搬運」並累加成本。** 如「分配硬幣」（題 979）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的貪心規則是什麼（按什麼排序、每步選什麼）？\n2. 我能用交換論證或歸納說明「每步選最好不會更差」嗎？\n3. 有沒有反例能推翻我的規則？若有，是不是該改用 DP？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3767 | [選擇 K 個任務的最大總分數](https://leetcode.cn/problems/maximize-points-after-choosing-k-tasks/) | 1704 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3457 | [喫披薩](https://leetcode.cn/problems/eat-pizzas/) | 1704 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 979 | [在二叉樹中分配硬幣](https://leetcode.cn/problems/distribute-coins-in-binary-tree/) | 1709 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2202 | [K 次操作後最大化頂端元素](https://leetcode.cn/problems/maximize-the-topmost-element-after-k-moves/) | 1717 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3557 | [不相交子字串的最大數量](https://leetcode.cn/problems/find-maximum-number-of-non-intersecting-substrings/) | 1720 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(), [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        kept++;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3767 | [選擇 K 個任務的最大總分數](https://leetcode.cn/problems/maximize-points-after-choosing-k-tasks) | 1704 |\n| 3457 | [喫披薩](https://leetcode.cn/problems/eat-pizzas) | 1704 |\n| 979 | [在二叉樹中分配硬幣](https://leetcode.cn/problems/distribute-coins-in-binary-tree) | 1709 |\n| 2202 | [K 次操作後最大化頂端元素](https://leetcode.cn/problems/maximize-the-topmost-element-after-k-moves) | 1717 |\n| 3557 | [不相交子字串的最大數量](https://leetcode.cn/problems/find-maximum-number-of-non-intersecting-substrings) | 1720 |\n| 2048 | [下一個更大的數值平衡數](https://leetcode.cn/problems/next-greater-numerically-balanced-number) | 1734 |\n| 3776 | [使迴圈陣列餘額非負的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-balance-circular-array) | 1740 |\n| 1145 | [二叉樹著色遊戲](https://leetcode.cn/problems/binary-tree-coloring-game) | 1741 |\n| 1024 | [影片拼接](https://leetcode.cn/problems/video-stitching) | 1746 |\n| 2171 | [拿出最少數目的魔法豆](https://leetcode.cn/problems/removing-minimum-number-of-magic-beans) | 1748 |\n| 1297 | [子串的最大出現次數](https://leetcode.cn/problems/maximum-number-of-occurrences-of-a-substring) | 1748 |\n| 3577 | [統計計算機解鎖順序排列數](https://leetcode.cn/problems/count-the-number-of-computer-unlocking-permutations) | 1750 |\n| 2856 | [刪除數對後的最小陣列長度](https://leetcode.cn/problems/minimum-array-length-after-pair-removals) | 1750 |\n| 2498 | [青蛙過河 II](https://leetcode.cn/problems/frog-jump-ii) | 1759 |\n| 3403 | [從盒子中找出字典序最大的字串 I](https://leetcode.cn/problems/find-the-lexicographically-largest-string-from-the-box-i) | 1762 |\n| 948 | [令牌放置](https://leetcode.cn/problems/bag-of-tokens) | 1762 |\n| 3085 | [成為 K 特殊字串需要刪除的最少字元數](https://leetcode.cn/problems/minimum-deletions-to-make-string-k-special) | 1765 |\n| 2718 | [查詢後矩陣的和](https://leetcode.cn/problems/sum-of-matrix-after-queries) | 1769 |\n| 3282 | [到達陣列末尾的最大得分](https://leetcode.cn/problems/reach-end-of-array-with-max-score) | 1772 |\n| 2952 | [需要新增的硬幣的最小數量](https://leetcode.cn/problems/minimum-number-of-coins-to-be-added) | 1784 |\n| 3724 | [轉換陣列的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-transform-array) | 1790 |\n| 2712 | [使所有字元相等的最小成本](https://leetcode.cn/problems/minimum-cost-to-make-all-characters-equal) | 1791 |\n| 3001 | [捕獲黑皇后需要的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-capture-the-queen) | 1797 |",
        },
        {
          id: 9,
          title: "1.7 網格圖",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目給你一個二維方格（grid），每個格子可以走到上下左右的鄰居，問你「能不能從 A 走到 B」「到每格的最短距離」「有幾塊連通區域」。這類「網格圖（grid graph）」其實就是圖論的特例——**把每個格子當成一個點，相鄰格子之間連一條邊**，於是 BFS / DFS 全都能直接套用。\n\n:::example 方向陣列：一次處理上下左右四個鄰居\n第一個關鍵技巧是用「方向陣列」優雅地枚舉四個鄰居，避免寫四份重複程式碼。\n\n```cpp\n// 方向陣列：一次處理上下左右四個鄰居\nint dx[4] = {-1, 1, 0, 0};\nint dy[4] = {0, 0, -1, 1};\nfor (int d = 0; d < 4; d++) {\n  int nx = x + dx[d], ny = y + dy[d];\n  if (nx < 0 || nx >= m || ny < 0 || ny >= n) {\n    continue;  // 越界\n  }\n  // 在邊界內 → (nx, ny) 是一個合法鄰居\n}\n```\n:::\n\n\n\n:::example 網格 BFS：從 start 擴散，dist 記錄到各格的最短步數\n第二個關鍵是：**當每步移動的代價都一樣（都是 1）時，用 BFS 就能得到「到每格的最短步數」**。BFS 一圈一圈往外擴，第一次碰到某格時的層數就是最短距離。\n\n```cpp\n// 網格 BFS：從 start 擴散，dist 記錄到各格的最短步數\nqueue<pair<int, int>> q;\nq.push({sx, sy});\ndist[sx][sy] = 0;\nwhile (!q.empty()) {\n  auto [x, y] = q.front();\n  q.pop();\n  for (int d = 0; d < 4; d++) {\n    int nx = x + dx[d], ny = y + dy[d];\n    if (inside(nx, ny) && dist[nx][ny] == -1) {  // 在界內且沒走過\n      dist[nx][ny] = dist[x][y] + 1;\n      q.push({nx, ny});\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 多源 BFS：一開始就把所有起點都放進佇列\n特別實用的變形是「多源 BFS」：把**所有起點同時**放進佇列再一起擴散，就能一次算出「每格到最近某類格子的距離」。「地圖中的最高點」（題 1765）正是如此：所有水域（高度 0）一起當起點，向外 BFS，每格的高度就是它到最近水域的步數。\n\n```cpp\n// 多源 BFS：一開始就把所有起點都放進佇列\nfor (所有起點(x, y)) {\n  dist[x][y] = 0;\n  q.push({x, y});\n}\n// 之後的擴散邏輯和單源 BFS 完全一樣\n```\n:::\n\n\n\n關鍵體會：網格題的本質是圖論，只是「點 = 格子、邊 = 相鄰」。掌握方向陣列、visited、單源／多源 BFS，大部分網格題就迎刃而解。\n\n**實作方式**\n\n把網格圖題拆成四個固定步驟，逐一搭配程式碼：\n\n:::example 八連通就把方向陣列擴成 8 個\n**1. 確定走訪規則：方向、障礙、起點數。** 是四連通還是八連通？哪些格子是牆（不能走）？起點是一個還是多個（決定單源還是多源 BFS）？\n\n```cpp\n// 八連通就把方向陣列擴成 8 個\nint dx[8] = {-1, -1, -1, 0, 0, 1, 1, 1};\nint dy[8] = {-1, 0, 1, -1, 1, -1, 0, 1};\n```\n:::\n\n\n\n**2. 準備 visited（或用 dist 兼任）避免重複。** 網格題最容易因為「走回頭路」而無限迴圈或超時，務必標記走過的格子。\n\n:::example DFS 數連通塊：把整塊相連的格子一次標記掉\n**3. 選 BFS 或 DFS。** 要最短步數 → BFS；只問連通／數區塊 → DFS 或 BFS 都行。注意 DFS 遞迴深度可能爆棧，大網格偏好 BFS。\n\n```cpp\n// DFS 數連通塊：把整塊相連的格子一次標記掉\nvoid dfs(int x, int y) {\n  vis[x][y] = true;\n  for (int d = 0; d < 4; d++) {\n    int nx = x + dx[d], ny = y + dy[d];\n    if (inside(nx, ny) && !vis[nx][ny] && grid[nx][ny] == '1') {\n      dfs(nx, ny);\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 答案讀取、邊界與複雜度。**\n\n- 邊界檢查（越界、是不是牆、走過沒）要在「進佇列前」就做完，順序別搞錯。\n- 像「檢查網格中是否存在有效路徑」（題 1391）這種「格子內有特定通道方向」的題，鄰接關係不是單純四連通，要按通道形狀判斷雙向是否互通。\n- 複雜度：每格進出一次，BFS/DFS 都是 `O(m × n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「網格圖」想：\n\n- **輸入是一個二維陣列，且可以「在格子間移動」。** 像「地圖中的最高點」（題 1765）、「有效路徑」（題 1391）。\n- **問「最短幾步到達」「到最近某類格子的距離」。** 步代價相同就用 BFS；多個起點就用多源 BFS：\n\n:::example 嗅到味道的瞬間：二維方格 + 問最短步數\n```cpp\n// 嗅到味道的瞬間：二維方格 + 問最短步數\n// 暴力對每格各跑一次搜尋是 O((mn)²)；一次（多源）BFS 是 O(mn)\n```\n:::\n\n\n\n- **問「有幾塊相連的區域／島嶼」。** 用 DFS/BFS 把每塊一次掃掉計數。\n- **格子有方向通道或障礙物。** 要按題意定義「兩格之間是否真的相通」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「點」是格子、「邊」是哪種相鄰（四連通？八連通？特定通道？）？\n2. 起點是一個還是多個？該用單源還是多源 BFS？\n3. 我有沒有正確做越界、障礙、visited 三項檢查，避免重複與超界？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1391 | [檢查網格中是否存在有效路徑](https://leetcode.cn/problems/check-if-there-is-a-valid-path-in-a-grid/) | 1746 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 1765 | [地圖中的最高點](https://leetcode.cn/problems/map-of-highest-peak/) | 1783 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n:::example DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\n public:\n  int numIslands(vector<vector<char>>& grid) {\n    int m = grid.size(), n = grid[0].size(), ans = 0;\n    int dirs[5] = {1, 0, -1, 0, 1};  // 方向陣列，相鄰兩元素構成上下左右四個方向向量\n    // DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n    function<void(int, int)> dfs = [&](int r, int c) {\n      grid[r][c] = '0';  // 標記已訪問，避免重複計算\n      for (int k = 0; k < 4; ++k) {\n        int nr = r + dirs[k], nc = c + dirs[k + 1];\n        if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n          dfs(nr, nc);\n        }\n      }\n    };\n    // 掃描整個網格，每遇到一個未訪問的 '1' 就是一座新島嶼\n    for (int r = 0; r < m; ++r) {\n      for (int c = 0; c < n; ++c) {\n        if (grid[r][c] == '1') {\n          ans++, dfs(r, c);\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1391 | [檢查網格中是否存在有效路徑](https://leetcode.cn/problems/check-if-there-is-a-valid-path-in-a-grid) | 1746 |\n| 1765 | [地圖中的最高點](https://leetcode.cn/problems/map-of-highest-peak) | 1783 |",
        },
        {
          id: 10,
          title: "1.8 數學",
          summary:
            '**原理講解**\n\n本節要解決的場景是：題目問「有幾種方法」「總和是多少」「第 N 個答案長怎樣」，而且資料量大到不能一個一個列舉。這時不該硬掃，而要先把答案寫成一條**數學公式或遞推式**，再用程式把公式算出來。這個技巧就叫「公式化（找規律 + 推導）」。\n\n:::example 找規律：把前幾項手算出來，觀察相鄰項的比例\n核心想法是：先用很小的例子手算幾組答案，觀察它們之間的關係，猜出一條通式，再用乘法、組合數、排列或等差級數把它寫死，最後直接代入。例如 LeetCode 1359「有效的快遞序列數目」要算 n 對取貨/送貨的合法排列數，暴力列舉是 `O((2n)!)`，但推導後會發現答案是 `1×3×5×…×(2n-1)` 連乘，一個迴圈就算完。\n\n```cpp\n// 找規律：把前幾項手算出來，觀察相鄰項的比例\n// n=1 -> 1; n=2 -> 1*3=3; n=3 -> 3*5=15; 規律：每多一對乘上 (2n-1)\nlong long count(int n) {\n  const int MOD = 1e9 + 7;\n  long long ans = 1;\n  for (int i = 1; i <= n; ++i) {\n    ans = ans * (2 * i - 1) % MOD;  // 邊乘邊取模，避免溢位\n  }\n  return ans;\n}\n```\n:::\n\n\n\n:::example 等差求和：1+2+...+k 不要用迴圈，直接套公式 O(1)\n這類題的關鍵不是寫迴圈，而是「想清楚數學關係」。一旦關係正確，程式通常只有幾行；難點全在推導與處理大數。\n\n```cpp\n// 等差求和：1+2+...+k 不要用迴圈，直接套公式 O(1)\nlong long sumTo(long long k) {\n  return k * (k + 1) / 2;  // 高斯求和公式\n}\n```\n:::\n\n\n\n**實作方式**\n\n把一道數學題拆成四步，逐一搭配程式碼：\n\n**1. 先用小資料手算，找出規律。** 代入 n=0,1,2,3，把答案列成一張表，找相鄰項的差、比例，或對照組合數。這一步在紙上做，不要急著寫程式。\n\n:::example 計數：每個值出現 cnt 次，從中選 2 個的組合數 C(cnt, 2)\n**2. 把規律寫成公式，再翻成程式。** 常見工具是乘法原理、加法原理、組合數、等差/等比級數。例如 LeetCode 923「三數之和的多種可能」要對每種數值組合算「能選幾組」，本質是排列組合的計數。\n\n```cpp\n// 計數：每個值出現 cnt 次，從中選 2 個的組合數 C(cnt, 2)\nlong long choose2(long long cnt) {\n  return cnt * (cnt - 1) / 2;  // C(cnt,2) = cnt*(cnt-1)/2\n}\n```\n:::\n\n\n\n:::example 取模乘法：先轉成 long long 再乘，再取模\n**3. 處理溢位與取模。** 數學題答案常常爆 `int`，要用 `long long`，題目要求對 `1e9+7` 取模時，每一步乘法後都要取模。\n\n```cpp\n// 取模乘法：先轉成 long long 再乘，再取模\nconst long long MOD = 1e9 + 7;\nlong long mul(long long a, long long b) {\n  return a % MOD * (b % MOD) % MOD;  // 防止中途溢位\n}\n```\n:::\n\n\n\n**4. 邊界與驗證。** 公式常在 n=0 或 n=1 時失效，要單獨檢查；推完公式後，務必拿手算的小例子回頭驗算，確認程式輸出與手算一致。複雜度通常是 `O(n)` 甚至 `O(1)`，遠快過暴力。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「公式化」想：\n\n- **題目問「方案數」「總共幾種」「總和」，且 n 很大（如 `1e9`）。** 例如 LeetCode 2929「給小朋友們分糖果 II」，數據範圍大到不能列舉，必須用組合計數扣掉超量的情況。\n- **答案似乎有規律，但你說不清。** 這正是該手算小例子找規律的訊號：\n\n:::example 嗅到味道的瞬間：暴力能跑出小答案，就用它來「對答案」找規律\n```cpp\n// 嗅到味道的瞬間：暴力能跑出小答案，就用它來「對答案」找規律\n// for (int n = 1; n <= 6; ++n) cout << n << ": " << brute(n) << "\\n";\n// 把輸出列成表，找相鄰項關係，再猜通式\n```\n:::\n\n\n\n- **題目是幾何判斷或數值關係**，例如 LeetCode 1401「圓和矩形是否有重疊」，靠的是把幾何條件化成一條不等式，而不是模擬。\n- **暴力是列舉所有組合（指數或階乘級），明顯超時；** 公式化能把它壓到線性或常數。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我手算了哪幾組小例子？它們之間的規律是什麼？\n2. 我推出的公式，代回 n=0 / n=1 還成立嗎？\n3. 中途的乘法會不會溢位、需不需要取模？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2929 | [給小朋友們分糖果 II](https://leetcode.cn/problems/distribute-candies-among-children-ii/) | 1701 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1401 | [圓和矩形是否有重疊](https://leetcode.cn/problems/circle-and-rectangle-overlapping/) | 1709 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 923 | [三數之和的多種可能](https://leetcode.cn/problems/3sum-with-multiplicity/) | 1711 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3649 | [完美對的數目](https://leetcode.cn/problems/number-of-perfect-pairs/) | 1716 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1359 | [有效的快遞序列數目](https://leetcode.cn/problems/count-all-valid-pickup-and-delivery-options/) | 1723 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n public:\n  int countPrimes(int n) {\n    // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n    vector<char> isPrime(n, true);\n    int ans = 0;\n    for (int i = 2; i < n; ++i) {\n      if (!isPrime[i]) {\n        continue;\n      }\n      ans++;\n      // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n      if ((long long)i * i < n) {\n        for (long long j = 1LL * i * i; j < n; j += i) {\n          isPrime[j] = false;\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i*i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2929 | [給小朋友們分糖果 II](https://leetcode.cn/problems/distribute-candies-among-children-ii) | 1701 |\n| 1401 | [圓和矩形是否有重疊](https://leetcode.cn/problems/circle-and-rectangle-overlapping) | 1709 |\n| 923 | [三數之和的多種可能](https://leetcode.cn/problems/3sum-with-multiplicity) | 1711 |\n| 3649 | [完美對的數目](https://leetcode.cn/problems/number-of-perfect-pairs) | 1716 |\n| 1359 | [有效的快遞序列數目](https://leetcode.cn/problems/count-all-valid-pickup-and-delivery-options) | 1723 |\n| 3447 | [將元素分配給有約束條件的組](https://leetcode.cn/problems/assign-elements-to-groups-with-constraints) | 1731 |\n| 3044 | [出現頻率最高的質數](https://leetcode.cn/problems/most-frequent-prime) | 1737 |\n| 939 | [最小面積矩形](https://leetcode.cn/problems/minimum-area-rectangle) | 1752 |\n| 1954 | [收集足夠蘋果的最小花園周長](https://leetcode.cn/problems/minimum-garden-perimeter-to-collect-enough-apples) | 1759 |\n| 2453 | [摧毀一系列目標](https://leetcode.cn/problems/destroy-sequential-targets) | 1762 |\n| 3164 | [優質數對的總數 II](https://leetcode.cn/problems/find-the-number-of-good-pairs-ii) | 1777 |\n| 2601 | [質數減法運算](https://leetcode.cn/problems/prime-subtraction-operation) | 1779 |',
        },
        {
          id: 11,
          title: "1.9 單調堆疊",
          summary:
            "**原理講解**\n\n本節要解決的場景是：對陣列中的每個元素，問「它左邊（或右邊）第一個比它大／比它小的是誰」，或是「以它為最大／最小值的區間有多長」。暴力做法是每個元素都往兩邊掃一遍找邊界，那是 `O(n^2)`；這個技巧「單調堆疊（monotonic stack）」能把它壓到 `O(n)`。\n\n:::example 找每個元素右邊「第一個更大」的下標（遞減棧）\n核心想法是：用一個堆疊保存「還沒找到邊界」的元素下標，並讓堆疊裡的值維持遞增或遞減。當新元素出現、破壞了這個單調性時，被彈出的元素「右邊第一個更大／更小者」就剛好是這個新元素——一次結算。\n\n```cpp\n// 找每個元素右邊「第一個更大」的下標（遞減棧）\nvector<int> nextGreater(vector<int>& a) {\n  int n = a.size();\n  vector<int> res(n, -1);  // 預設 -1：右邊沒有更大的\n  stack<int> stk;          // 存下標，對應的值由棧底到棧頂遞減\n  for (int i = 0; i < n; ++i) {\n    while (!stk.empty() && a[stk.top()] < a[i]) {\n      res[stk.top()] = i;  // a[i] 就是棧頂的「右邊第一個更大」\n      stk.pop();\n    }\n    stk.push(i);\n  }\n  return res;\n}\n```\n:::\n\n\n\n:::example 棧裡存「下標」而非值，因為算寬度／距離時需要位置資訊\n為什麼是 `O(n)`？因為每個下標最多被 push 一次、pop 一次，總操作量是線性的。LeetCode 901「股票價格跨度」就是典型：每天問「往前連續幾天股價 ≤ 今天」，用遞減棧維護候選的高價日即可。\n\n```cpp\n// 棧裡存「下標」而非值，因為算寬度／距離時需要位置資訊\nstk.push(i);  // 對：之後能用 i - stk.top() 算距離\n              // stk.push(a[i]);        // 不夠：只有值，算不出區間長度\n```\n:::\n\n\n\n**實作方式**\n\n把單調堆疊題拆成四步：\n\n**1. 決定棧的單調方向與存什麼。** 找「更大」用遞減棧，找「更小」用遞增棧；棧裡存**下標**（值可由 `a[下標]` 取回，還能算距離）。\n\n:::example 彈出瞬間結算：a[i] 是棧頂元素右邊第一個更大者\n**2. 掃描時持續彈出破壞單調性的棧頂。** 彈出的那一刻，就能確定被彈元素的某一側邊界。\n\n```cpp\n// 彈出瞬間結算：a[i] 是棧頂元素右邊第一個更大者\nwhile (!stk.empty() && a[stk.top()] < a[i]) {\n  int idx = stk.top();\n  stk.pop();\n  // 在這裡用 idx 與 i 計算貢獻、寬度或答案\n}\n```\n:::\n\n\n\n:::example 以某元素為最小值的子陣列寬度 = (i - 左邊界) * (右邊界 - i)\n**3. 把答案從邊界算出來。** 左邊界是「彈出後新的棧頂」，右邊界是「當前的 i」，矩形寬度或子陣列個數就由這兩個邊界決定。\n\n```cpp\n// 以某元素為最小值的子陣列寬度 = (i - 左邊界) * (右邊界 - i)\nint left = stk.empty() ? -1 : stk.top();  // 左邊第一個更小者的位置\nint width = i - left;                     // 它能往左延伸的長度\n```\n:::\n\n\n\n**4. 邊界與複雜度：相等值的處理最容易出錯。** 計數或貢獻法題（如 LeetCode 2962「統計最大元素出現至少 K 次的子陣列」相關的貢獻技巧）若有重複值，必須「一側用嚴格 `<`、另一側用非嚴格 `<=`」才不會重複計算。複雜度固定是 `O(n)`，每個下標進出棧各一次。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「單調堆疊」想：\n\n- **要對每個元素找「最近的更大／更小元素」。** 例如 LeetCode 901「股票價格跨度」要找往前第一個比今天高的價格，正是 next/previous greater 的變形。\n- **要算「以某元素為最大／最小值」的區間數量或矩形面積。** 這需要左右兩側的邊界，單調棧能在一次掃描中同時提供。\n- **暴力是每個元素往兩邊掃找邊界（`O(n^2)`），明顯太慢：**\n\n:::example 嗅到味道的瞬間：你正想寫這種雙層迴圈找邊界\n```cpp\n// 嗅到味道的瞬間：你正想寫這種雙層迴圈找邊界\n// for (int i = 0; i < n; ++i)\n//     for (int j = i+1; j < n && a[j] <= a[i]; ++j) ...  // O(n^2)，改用單調棧\n```\n:::\n\n\n\n- **題目像「下一個更大元素」「接雨水」「柱狀圖最大矩形」「在預算下選最大容量」（LeetCode 3814）**，都能歸到這個模型。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的是更大還是更小？對應該用遞減棧還是遞增棧？\n2. 棧裡存的是下標還是值？我之後要不要算距離？\n3. 有重複值時，左右邊界各該用嚴格還是非嚴格比較？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2962 | [統計最大元素出現至少 K 次的子陣列](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times/) | 1701 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 901 | [股票價格跨度](https://leetcode.cn/problems/online-stock-span/) | 1709 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 3814 | [預算下的最大總容量](https://leetcode.cn/problems/maximum-capacity-within-budget/) | 1796 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n    vector<int> st;\n    for (int x : nums2) {\n      // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2962 | [統計最大元素出現至少 K 次的子陣列](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times) | 1701 |\n| 901 | [股票價格跨度](https://leetcode.cn/problems/online-stock-span) | 1709 |\n| 3814 | [預算下的最大總容量](https://leetcode.cn/problems/maximum-capacity-within-budget) | 1796 |",
        },
        {
          id: 12,
          title: "1.10 滑動視窗",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在一個陣列或字串上，找「連續一段（子陣列／子字串）」滿足某個條件，並問最長、最短、或有幾段。暴力是列舉所有區間再各自檢查，那是 `O(n^2)` 甚至 `O(n^3)`；這個技巧「滑動視窗（sliding window）」能把它壓到 `O(n)`。\n\n:::example 最長不含重複字元的子字串：right 擴張、left 收縮\n核心想法是：用兩個指標 `left` 和 `right` 框出一段視窗。`right` 一路往右擴張、把新元素納入；一旦視窗「違規」，就移動 `left` 把左邊元素吐出來，直到視窗重新合法。整個過程兩個指標都只往右走，不回頭，所以是線性。\n\n```cpp\n// 最長不含重複字元的子字串：right 擴張、left 收縮\nint longestUnique(string s) {\n  unordered_map<char, int> cnt;  // 視窗內每個字元出現次數\n  int left = 0, ans = 0;\n  for (int right = 0; right < s.size(); ++right) {\n    cnt[s[right]]++;             // 納入新字元\n    while (cnt[s[right]] > 1) {  // 出現重複 → 違規\n      cnt[s[left]]--;\n      ++left;  // 從左吐出，直到合法\n    }\n    ans = max(ans, right - left + 1);  // 更新最長視窗長度\n  }\n  return ans;\n}\n```\n:::\n\n\n\n:::example 判斷單調性：能不能擴張，取決於合法性是否隨視窗變大而單向變壞\n這招能成立的前提是「單調性」：如果一個視窗合法，那縮小它通常仍合法；如果一個視窗違規，那擴大它只會更糟。有了這個性質，`left` 才能放心地只往一個方向移動而不漏解。例如 LeetCode 2134「最少交換次數組合所有的 1」用固定長度的環形視窗找「1 最多」的位置。\n\n```cpp\n// 判斷單調性：能不能擴張，取決於合法性是否隨視窗變大而單向變壞\n// 視窗變大 -> 條件只會更難滿足  ✓ 適合滑動視窗\n// 視窗變大 -> 條件忽好忽壞        ✗ 不適合，需換別的方法\n```\n:::\n\n\n\n**實作方式**\n\n把滑動視窗題拆成四步：\n\n:::example 視窗狀態：用一個變數或計數表，支援 O(1) 加入與移除\n**1. 想清楚視窗要維護什麼狀態。** 通常是計數（`unordered_map` 或陣列）、總和、或不同元素的個數，讓「加入／移除一個元素」都能 `O(1)` 更新。\n\n```cpp\n// 視窗狀態：用一個變數或計數表，支援 O(1) 加入與移除\nlong long sum = 0;  // 例如維護視窗總和\nsum += a[right];    // 加入右端\nsum -= a[left];     // 移除左端\n```\n:::\n\n\n\n**2. right 擴張、納入新元素。** 每一輪先把 `a[right]` 加進狀態。\n\n:::example 求最短滿足條件的視窗：一旦合法就盡量縮短並更新答案\n**3. 違規時 left 收縮。** 用 `while` 把左端元素一個個吐出，直到視窗重新合法（求最長），或剛好滿足條件（求最短）。\n\n```cpp\n// 求最短滿足條件的視窗：一旦合法就盡量縮短並更新答案\nwhile (sum >= target) {              // 視窗已滿足條件\n  ans = min(ans, right - left + 1);  // 嘗試更短\n  sum -= a[left];\n  ++left;  // 縮左端，看還合不合法\n}\n```\n:::\n\n\n\n**4. 在正確時機更新答案，並注意邊界。** 求最長在「視窗合法時」更新；求最短在「視窗剛滿足時」更新。複雜度 `O(n)`：`left`、`right` 各最多走 n 步。環形陣列題（如 LeetCode 2134）可把陣列接兩份或用取模處理。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「滑動視窗」想：\n\n- **要找「連續」的子陣列／子字串**，且問最長、最短、或計數。不連續（可跳著選）的題目不適用。例如 LeetCode 3694「刪除子字串後不同的終點」處理的就是連續區段。\n- **合法性隨視窗大小單調變化**：視窗越大越容易違規（或越容易滿足）。這保證 `left` 只需往右移：\n\n:::example 嗅到味道的瞬間：你想列舉所有區間檢查，但發現有單調性\n```cpp\n// 嗅到味道的瞬間：你想列舉所有區間檢查，但發現有單調性\n// for (int i = 0; i < n; ++i)\n//     for (int j = i; j < n; ++j) check(i, j);  // O(n^2)，改用滑動視窗\n```\n:::\n\n\n\n- **題目給「至多 / 至少 K 個」「總和不超過 / 不小於 target」「不含重複」這類約束**，例如 LeetCode 3439「重新安排會議得到最多空餘時間」用視窗框出可調整的會議段。\n- **暴力列舉所有區間明顯超時；** 滑動視窗用雙指標把它降成線性。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是「連續」一段嗎？合法性會隨視窗變大而單調變壞嗎？\n2. 視窗內要維護什麼狀態？加入／移除一個元素能 `O(1)` 更新嗎？\n3. 我該在「視窗合法時」還是「剛滿足時」更新答案？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3439 | [重新安排會議得到最多空餘時間 I](https://leetcode.cn/problems/reschedule-meetings-for-maximum-free-time-i/) | 1729 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 3499 | [操作後最大活躍區段數 I](https://leetcode.cn/problems/maximize-active-section-with-trade-i/) | 1729 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 3694 | [刪除子字串後不同的終點](https://leetcode.cn/problems/distinct-points-reachable-after-substring-removal/) | 1739 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2134 | [最少交換次數來組合所有的 1 II](https://leetcode.cn/problems/minimum-swaps-to-group-all-1s-together-ii/) | 1748 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 1016 | [子串能表示從 1 到 N 數字的二進位制串](https://leetcode.cn/problems/binary-string-with-substrings-representing-1-to-n/) | 1779 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 3. 無重複字元的最長子串 展示核心寫法。\n\n**完整題目**：給定字串 `s`，回傳不含重複字元的最長子字串長度。\n\n:::example 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass Solution {\n public:\n  int lengthOfLongestSubstring(string s) {\n    // 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n    vector<int> cnt(256);\n    int ans = 0, left = 0;\n    for (int right = 0; right < (int)s.size(); ++right) {\n      cnt[s[right]]++;\n      // 若新字元造成重複，縮小左邊界直到視窗內無重複\n      while (cnt[s[right]] > 1) {\n        cnt[s[left++]]--;\n      }\n      ans = max(ans, right - left + 1);  // 更新無重複子字串最大長度\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 右端加入新元素後，只在違反條件時移動左端；因此每個位置最多進出視窗一次，總複雜度為 O(n)。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n**常見變形**\n\n- 越短越合法：違法時收縮，更新最長。\n- 越長越合法：合法時收縮，更新最短。\n- 恰好型可轉成 `atMost(k)-atMost(k-1)`。\n\n**實作注意事項**\n\n- 先判斷合法性方向，再寫 while 條件。\n- 答案計數時常用 `right-left+1` 或 `left`，兩者語意不同。\n- 頻次表字元集可用陣列，值域大時用 map。\n- 含負數時滑窗的單調性可能失效。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3439 | [重新安排會議得到最多空餘時間 I](https://leetcode.cn/problems/reschedule-meetings-for-maximum-free-time-i) | 1729 |\n| 3499 | [操作後最大活躍區段數 I](https://leetcode.cn/problems/maximize-active-section-with-trade-i) | 1729 |\n| 3694 | [刪除子字串後不同的終點](https://leetcode.cn/problems/distinct-points-reachable-after-substring-removal) | 1739 |\n| 2134 | [最少交換次數來組合所有的 1 II](https://leetcode.cn/problems/minimum-swaps-to-group-all-1s-together-ii) | 1748 |\n| 1016 | [子串能表示從 1 到 N 數字的二進位制串](https://leetcode.cn/problems/binary-string-with-substrings-representing-1-to-n) | 1779 |\n| 1156 | [單字元重複子串的最大長度](https://leetcode.cn/problems/swap-for-longest-repeated-character-substring) | 1787 |",
        },
        {
          id: 13,
          title: "1.11 樹和二元樹",
          summary:
            "**原理講解**\n\n本節要解決的場景是：資料長成一棵「二元樹」——每個節點最多有左、右兩個子節點——而題目要你算路徑長度、判斷形狀、或從遍歷結果重建這棵樹。處理樹最自然的工具是**遞迴（recursion）**：因為「左子樹」「右子樹」本身又各是一棵更小的樹，問題天然有自相似結構。\n\n:::example 樹的高度：左右子樹高度的較大者 + 1\n核心想法是：定義一個遞迴函式處理「以某節點為根的子樹」，先遞迴解決左、右子樹，再用它們的結果合併出當前答案。這叫「分而治之」。空節點（`nullptr`）就是遞迴的終點（base case）。\n\n```cpp\n// 樹的高度：左右子樹高度的較大者 + 1\nint height(TreeNode* root) {\n  if (!root) {\n    return 0;  // 空節點高度為 0（終止條件）\n  }\n  int L = height(root->left);   // 先解左子樹\n  int R = height(root->right);  // 再解右子樹\n  return max(L, R) + 1;         // 合併：加上自己這一層\n}\n```\n:::\n\n\n\n:::example 遞迴常回傳「給父節點用的資訊」，同時用全域變數記錄整體答案\n很多樹的題目都是這個骨架的變形：把「合併」那一步換成題目要的邏輯就好。例如 LeetCode 1372「二元樹中的最長交錯路徑」，遞迴回傳「從這個節點往左走、往右走各能延伸多長」，父節點再據此更新答案。\n\n```cpp\n// 遞迴常回傳「給父節點用的資訊」，同時用全域變數記錄整體答案\nint best = 0;  // 全域：目前最長路徑\n               // 在遞迴中：best = max(best, 左長 + 右長);\n```\n:::\n\n\n\n:::example 層序遍歷骨架：用佇列一層層往下走\n判斷形狀的題目（如 LeetCode 958「二元樹的完全性檢驗」）則常改用一層一層走的**層序遍歷（BFS，用佇列）**，這是處理樹的另一個基本工具。\n\n```cpp\n// 層序遍歷骨架：用佇列一層層往下走\nqueue<TreeNode*> q;\nq.push(root);\nwhile (!q.empty()) {\n  TreeNode* cur = q.front();\n  q.pop();\n  if (cur->left) {\n    q.push(cur->left);  // 把孩子排進佇列\n  }\n  if (cur->right) {\n    q.push(cur->right);\n  }\n}\n```\n:::\n\n\n\n**實作方式**\n\n把樹的題目拆成四步：\n\n**1. 選遍歷方式。** 算路徑、求和、自底向上合併 → 用 DFS 遞迴；判斷層次形狀、找最短層數 → 用 BFS 層序。\n\n:::example 終止條件永遠先寫：碰到空節點直接回傳\n**2. 寫好終止條件。** 遞迴第一行幾乎都是處理空節點，漏掉就會對 `nullptr` 取值而崩潰。\n\n```cpp\n// 終止條件永遠先寫：碰到空節點直接回傳\nif (!root) {\n  return 0;  // 或 return nullptr / return true，視題目而定\n}\n```\n:::\n\n\n\n:::example 後序：先拿到左右結果，再決定回傳與更新全域答案\n**3. 想清楚「回傳什麼」與「順便更新什麼」。** 回傳值是給父節點用的；若整體答案不等於根的回傳值（例如最長路徑可能在中間），就另外用一個全域變數記錄。\n\n```cpp\n// 後序：先拿到左右結果，再決定回傳與更新全域答案\nint L = dfs(root->left), R = dfs(root->right);\nbest = max(best, L + R);  // 更新整體答案\nreturn max(L, R) + 1;     // 回傳給父節點的「單邊延伸長度」\n```\n:::\n\n\n\n**4. 重建類題目用「分割區間」思路。** 如 LeetCode 889「根據前序和後序遍歷構造二元樹」，靠前序的第一個元素定根、再把剩餘區間切給左右子樹遞迴建立。複雜度多為 `O(n)`（每個節點訪問一次），用雜湊表記錄值的位置可避免重複搜尋。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「樹的遞迴／遍歷」想：\n\n- **輸入是 `TreeNode*` 或明顯的樹狀結構**，問路徑、深度、節點關係或形狀。例如 LeetCode 1372 的最長交錯路徑。\n- **答案可由「左子樹的答案 + 右子樹的答案」合併而來。** 這正是分治遞迴的訊號：\n\n:::example 嗅到味道的瞬間：當前答案 = f(左子樹結果, 右子樹結果)\n```cpp\n// 嗅到味道的瞬間：當前答案 = f(左子樹結果, 右子樹結果)\n// 只要能寫出這種合併關係，就用遞迴\n```\n:::\n\n\n\n- **要判斷「逐層」性質**（如是否為完全二元樹、每層最大值、最小深度），改用 BFS 層序，例如 LeetCode 958。\n- **要從遍歷序列重建樹**，用「定根 + 切區間遞迴」，例如 LeetCode 889。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的遞迴終止條件（空節點）寫了嗎？\n2. 遞迴要「回傳給父節點什麼」？整體答案需不需要另開全域變數？\n3. 這題該用自底向上的 DFS，還是逐層的 BFS？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 958 | [二叉樹的完全性檢驗](https://leetcode.cn/problems/check-completeness-of-a-binary-tree/) | 1703 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1372 | [二叉樹中的最長交錯路徑](https://leetcode.cn/problems/longest-zigzag-path-in-a-binary-tree/) | 1713 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2397 | [被列覆蓋的最多行數](https://leetcode.cn/problems/maximum-rows-covered-by-columns/) | 1719 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1239 | [串聯字串的最大長度](https://leetcode.cn/problems/maximum-length-of-a-concatenated-string-with-unique-characters/) | 1720 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 889 | [根據前序和後序遍歷構造二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-postorder-traversal/) | 1732 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 958 | [二叉樹的完全性檢驗](https://leetcode.cn/problems//check-completeness-of-a-binary-tree/) | 1703 |\n| 1372 | [二叉樹中的最長交錯路徑](https://leetcode.cn/problems//longest-zigzag-path-in-a-binary-tree/) | 1713 |\n| 2397 | [被列覆蓋的最多行數](https://leetcode.cn/problems//maximum-rows-covered-by-columns/) | 1719 |\n| 1239 | [串聯字串的最大長度](https://leetcode.cn/problems//maximum-length-of-a-concatenated-string-with-unique-characters/) | 1720 |\n| 889 | [根據前序和後序遍歷構造二叉樹](https://leetcode.cn/problems//construct-binary-tree-from-preorder-and-postorder-traversal/) | 1732 |\n| 1593 | [拆分字串使唯一子字串的數目最大](https://leetcode.cn/problems//split-a-string-into-the-max-number-of-unique-substrings/) | 1740 |\n| 1530 | [好葉子節點對的數量](https://leetcode.cn/problems//number-of-good-leaf-nodes-pairs/) | 1746 |\n| 1849 | [將字串拆分為遞減的連續值](https://leetcode.cn/problems//splitting-a-string-into-descending-consecutive-values/) | 1747 |\n| 1171 | [從連結串列中刪去總和值為零的連續節點](https://leetcode.cn/problems//remove-zero-sum-consecutive-nodes-from-linked-list/) | 1782 |\n| 971 | [翻轉二叉樹以匹配先序遍歷](https://leetcode.cn/problems//flip-binary-tree-to-match-preorder-traversal/) | 1787 |\n| 1028 | [從先序遍歷還原二叉樹](https://leetcode.cn/problems//recover-a-tree-from-preorder-traversal/) | 1797 |",
        },
      ],
    },
    {
      id: 14,
      title: "2. Phase 2：打穿 1800 題型牆（1800-1900）",
      summary:
        "這一段開始需要把兩個技巧拼起來，例如二分加貪心、圖論加 DP、資料結構加離線。每題做完都要記錄最早可以識別出的關鍵訊號，訓練比賽中的選型速度。",
      children: [
        {
          id: 15,
          title: "2.1 二分搜尋",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在一個「有序」的範圍裡找某個目標值或臨界點。最熟悉的版本是在已排序陣列裡找數，但更強的用法是「在答案的可能範圍上二分」——當答案越大越容易（或越難）滿足條件時，可以猜一個答案 `x`，驗證它可不可行，再據此縮小範圍。這個技巧「二分搜尋（binary search）」每次砍掉一半，複雜度是 `O(log n)`。\n\n:::example 在已排序陣列找 target 的位置，找不到回傳 -1\n核心想法是維持一個「還可能是答案」的區間 `[lo, hi]`，每次取中點 `mid`，判斷答案在左半還是右半，把區間砍半。\n\n```cpp\n// 在已排序陣列找 target 的位置，找不到回傳 -1\nint binarySearch(vector<int>& a, int target) {\n  int lo = 0, hi = a.size() - 1;\n  while (lo <= hi) {\n    int mid = lo + (hi - lo) / 2;  // 防溢位寫法\n    if (a[mid] == target) {\n      return mid;\n    }\n    if (a[mid] < target) {\n      lo = mid + 1;  // 答案在右半\n    } else {\n      hi = mid - 1;  // 答案在左半\n    }\n  }\n  return -1;\n}\n```\n:::\n\n\n\n:::example 二分答案的骨架：找「最小的可行 x」\n更常考的是「二分答案」：把問題改寫成一個布林判斷 `check(x)`——「答案取 x 時可不可行？」如果 `check` 隨 x 單調（一旦可行，更大／更小的 x 也都可行），就能對 x 二分。例如 LeetCode 2064「分配給商店的最多商品的最小值」，二分「每家店最多分到幾件」，驗證需要的店數夠不夠。\n\n```cpp\n// 二分答案的骨架：找「最小的可行 x」\nint lo = 1, hi = MAX_X;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;  // mid 可行，答案 ≤ mid\n  } else {\n    lo = mid + 1;  // mid 不可行，答案 > mid\n  }\n}\n// 迴圈結束時 lo == hi，就是最小的可行答案\n```\n:::\n\n\n\n關鍵在於先畫出「真假分界線」：x 小到某處都不可行、大到某處全可行，二分要找的就是那條界。\n\n**實作方式**\n\n把二分題拆成四步：\n\n**1. 確認單調性，定義候選值 x 的語意。** 先問清楚「答案是什麼量」，並確認「x 變大時，可行性是單向變化的」。沒有單調性就不能二分。\n\n:::example check：給定每家店上限 x，計算需要幾家店，夠不夠 n 家\n**2. 寫出 check(x)。** 這是二分答案的靈魂：給定 x，用一次線性掃描判斷可不可行。\n\n```cpp\n// check：給定每家店上限 x，計算需要幾家店，夠不夠 n 家\nbool check(vector<int>& q, int n, int x) {\n  long long need = 0;\n  for (int v : q) {\n    need += (v + x - 1) / x;  // 進位除法：每店上限 x 需幾家\n  }\n  return need <= n;  // 店數夠 → x 可行\n}\n```\n:::\n\n\n\n:::example 找「最大的可行 x」：mid 要偏右，否則 lo=mid 會卡死\n**3. 決定找「第一個」還是「最後一個」可行值，並寫對邊界。** 求最小可行值用「可行就收右界 `hi = mid`」；求最大可行值用「可行就收左界 `lo = mid + 1`」並注意 `mid` 取上中點避免死迴圈。\n\n```cpp\n// 找「最大的可行 x」：mid 要偏右，否則 lo=mid 會卡死\nint mid = lo + (hi - lo + 1) / 2;  // 上中點\nif (check(mid)) {\n  lo = mid;  // 可行 → 答案 ≥ mid\n} else {\n  hi = mid - 1;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** `lo/hi` 的初值要涵蓋所有可能答案；`mid` 用 `lo + (hi-lo)/2` 防溢位。整體複雜度是 `O(log(範圍) × check 的成本)`，通常是 `O(n log n)` 等級。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「二分搜尋」想：\n\n- **資料已排序或部分有序**，要找某值或插入位置。例如 LeetCode 1095「山脈陣列中查詢目標值」，先二分找山頂，再在兩段各自二分。\n- **題目要「最大化最小值」或「最小化最大值」。** 這種「最小的最大」字眼幾乎是二分答案的招牌，例如 LeetCode 2064。\n- **答案有明顯單調性：x 可行則更大（或更小）的 x 也可行。** 嗅到這個就試二分：\n\n:::example 嗅到味道的瞬間：你能寫出一個布林函式且它隨 x 單調\n```cpp\n// 嗅到味道的瞬間：你能寫出一個布林函式且它隨 x 單調\n// check(x) = false false false TRUE TRUE TRUE  → 找這條分界線\n```\n:::\n\n\n\n- **暴力是從小到大逐一試答案（`O(範圍)`），太慢；** 二分把它降成 `O(log 範圍)`。例如 LeetCode 2576「求出最多標記下標」可二分「能標記幾對」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要二分的「候選值 x」代表什麼？它的可行性隨 x 單調嗎？\n2. 我要找第一個可行還是最後一個可行？邊界該怎麼收？\n3. 我的 `check(x)` 怎麼寫？一次能在線性時間內驗證嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1095 | [山脈陣列中查詢目標值](https://leetcode.cn/problems/find-in-mountain-array/) | 1827 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2576 | [求出最多標記下標](https://leetcode.cn/problems/find-the-maximum-number-of-marked-indices/) | 1843 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 3508 | [設計路由器](https://leetcode.cn/problems/implement-router/) | 1851 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 3639 | [變為活躍狀態的最小時間](https://leetcode.cn/problems/minimum-time-to-activate-string/) | 1853 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2064 | [分配給商店的最多商品的最小值](https://leetcode.cn/problems/minimized-maximum-of-products-distributed-to-any-store/) | 1886 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n:::example 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  // 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;  // 閉區間 [l, r]\n    while (l <= r) {\n      int mid = l + (r - l) / 2;  // 取中點，避免 l+r 溢位\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;  // 目標在右半段\n      } else {\n        r = mid - 1;  // 目標在左半段\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1095 | [山脈陣列中查詢目標值](https://leetcode.cn/problems/find-in-mountain-array) | 1827 |\n| 2576 | [求出最多標記下標](https://leetcode.cn/problems/find-the-maximum-number-of-marked-indices) | 1843 |\n| 3508 | [設計路由器](https://leetcode.cn/problems/implement-router) | 1851 |\n| 3639 | [變為活躍狀態的最小時間](https://leetcode.cn/problems/minimum-time-to-activate-string) | 1853 |\n| 2064 | [分配給商店的最多商品的最小值](https://leetcode.cn/problems/minimized-maximum-of-products-distributed-to-any-store) | 1886 |\n| 878 | [第 N 個神奇數字](https://leetcode.cn/problems/nth-magical-number) | 1897 |",
        },
        {
          id: 16,
          title: "2.2 位元運算",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目牽涉到 AND、OR、XOR 這些「位元運算」，或要你對二進位的某幾位做決策。處理這類題的核心心法是：**把整數拆成一個個二進位位（bit），對每一位獨立思考。** 因為 AND、OR、XOR 都是「逐位獨立」計算的——第 k 位的結果只跟所有數的第 k 位有關，跟其他位無關。\n\n:::example 位元基本操作：取出第 k 位、把第 k 位設為 1\n先複習工具。一個整數在電腦裡是一串 0/1，我們常用三個運算子取出或操作某一位：\n\n```cpp\n// 位元基本操作：取出第 k 位、把第 k 位設為 1\nint bit = (x >> k) & 1;  // 取出 x 的第 k 位（0 或 1）\nx = x | (1 << k);        // 把 x 的第 k 位設成 1\nint low = x & (-x);      // 取出最低位的 1（lowbit）\n```\n:::\n\n\n\n:::example 逐位統計：對第 k 位，算有幾個數在這一位是 1\n有了「逐位獨立」這個觀念，很多看似複雜的題就能拆開算。例如統計「所有數對 AND 的 XOR 總和」(LeetCode 1835)：與其枚舉所有數對，不如逐位問「這一位在多少個數對裡是 1？再看這個個數的奇偶決定 XOR 該位」。\n\n```cpp\n// 逐位統計：對第 k 位，算有幾個數在這一位是 1\nint cnt = 0;\nfor (int x : nums) {\n  if ((x >> k) & 1) {\n    ++cnt;  // 統計第 k 位為 1 的數量\n            // 之後用 cnt 對這一位做計數或貪心，與其他位互不干擾\n  }\n}\n```\n:::\n\n\n\n:::example XOR 前綴：prefix[i] ^ prefix[j] 就是區間 (j, i] 的 X…\n另一條常見路線是 XOR 的特殊性質：`a ^ a = 0`、`a ^ 0 = a`、可交換可結合。它讓「成對的東西互相抵消」，常用來找唯一出現的數或子陣列的 XOR 前綴。\n\n```cpp\n// XOR 前綴：prefix[i] ^ prefix[j] 就是區間 (j, i] 的 XOR\nint pre = 0;\npre ^= nums[i];  // 累積前綴 XOR；相同的部分會自動抵消\n```\n:::\n\n\n\n**實作方式**\n\n把位元運算題拆成四步：\n\n:::example 逐位框架：對每一位獨立統計或決策，再組回答案\n**1. 判斷能不能「逐位拆開」。** 若答案的每一位只取決於輸入的同一位，就對 0..30（或 0..63）每一位各算一次，最後組合起來。\n\n```cpp\n// 逐位框架：對每一位獨立統計或決策，再組回答案\nlong long ans = 0;\nfor (int k = 0; k <= 30; ++k) {\n  // ... 統計這一位的貢獻 contribution ...\n  // ans |= (something << k);  或  ans += contribution;\n}\n```\n:::\n\n\n\n**2. 善用 XOR 抵消性質。** 遇到「成對抵消」「找唯一」「區間 XOR」就用前綴 XOR 或直接累加。\n\n:::example 高位貪心：從最高位往下，盡量讓結果這一位為 1\n**3. 高位貪心。** 要讓結果的 XOR/數值最大時，從最高位往低位貪：每一位都優先嘗試讓它是 1，能成立就鎖定。例如 LeetCode 3514「不同 XOR 三元組的數目」需要分析每一位能取到的組合。\n\n```cpp\n// 高位貪心：從最高位往下，盡量讓結果這一位為 1\nfor (int k = 30; k >= 0; --k) {\n  if (canSet(k)) {\n    ans |= (1 << k);  // 這一位能設成 1 就設\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 注意位數上限（`int` 約 30 位、`long long` 約 63 位）與 `1 << k` 在大 k 時要寫成 `1LL << k` 防溢位。複雜度通常是 `O(n × 位數)`，遠快過枚舉所有數對／三元組的 `O(n^2)`、`O(n^3)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「位元運算」想：\n\n- **題目直接出現 AND / OR / XOR**，或要你比較、組合二進位數。例如 LeetCode 1835「所有數對按位與結果的異或和」。\n- **暴力是枚舉所有數對或三元組，明顯超時，但你發現「各位互不影響」。** 這就是逐位拆解的訊號：\n\n:::example 嗅到味道的瞬間：第 k 位的答案只跟大家的第 k 位有關\n```cpp\n// 嗅到味道的瞬間：第 k 位的答案只跟大家的第 k 位有關\n// 就能把 O(n^2) 的枚舉，換成 O(n * 30) 的逐位統計\n```\n:::\n\n\n\n- **要「最大化 XOR / 數值」，且可以從高位往低位決定。** 這指向高位貪心，例如 LeetCode 3097「或值至少為 K 的最短子陣列」要逐位維護 OR 的貢獻。\n- **出現「只有一個數出現奇數次」「區間 XOR」** 這類描述，直接用 XOR 抵消性質。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這題的答案能不能「對每一位獨立計算再組合」？\n2. 用得上 XOR 的抵消性質（成對歸零）嗎？\n3. 要最大化結果時，能不能從最高位開始貪心鎖定？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1835 | [所有數對按位與結果的異或和](https://leetcode.cn/problems/find-xor-sum-of-all-pairs-bitwise-and/) | 1825 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 3514 | [不同 XOR 三元組的數目 II](https://leetcode.cn/problems/number-of-unique-xor-triplets-ii/) | 1884 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 3097 | [或值至少為 K 的最短子陣列 II](https://leetcode.cn/problems/shortest-subarray-with-or-at-least-k-ii/) | 1891 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1310. 子陣列異或查詢 展示核心寫法。\n\n**完整題目**：給定陣列與多個 `[l, r]` 查詢，回傳每段子陣列的 XOR 值。\n\n:::example 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR…\n**線索**：題目出現 XOR/AND/OR、mask 或二進位；先判斷每一位是否能獨立統計或從高位貪心。\n\n```cpp\nclass Solution {\n public:\n  // 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR 值\n  vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {\n    vector<int> pre(arr.size() + 1);  // pre[i] 為 arr[0..i-1] 的 XOR 前綴\n    for (int i = 0; i < (int)arr.size(); ++i) {\n      pre[i + 1] = pre[i] ^ arr[i];\n    }\n    vector<int> ans;\n    for (auto& q : queries) {\n      ans.push_back(pre[q[1] + 1] ^ pre[q[0]]);  // 區間 [q[0], q[1]] 的 XOR\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- XOR 具有自反性 `x ^ x = 0`，因此區間 XOR 可用前綴 XOR 相消。\n- 程式把整數轉成位元狀態或前綴 XOR，利用位元運算的可逆性/獨立性更新答案。\n- 涉及高位貪心時，迴圈通常從最高位往低位試，因為高位決定主要大小關係。\n\n**常見變形**\n\n- 前綴 XOR 處理區間異或。\n- 試填最高位處理最大 XOR/最大值。\n- SOS DP 處理子集包含關係。\n\n**實作注意事項**\n\n- 位移用 `1LL << b` 避免溢位。\n- 負數右移與符號位要小心。\n- 按位貢獻要確認每位獨立。\n- `__builtin_popcountll` 對 long long 使用 ll 版本。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1835 | [所有數對按位與結果的異或和](https://leetcode.cn/problems/find-xor-sum-of-all-pairs-bitwise-and) | 1825 |\n| 3514 | [不同 XOR 三元組的數目 II](https://leetcode.cn/problems/number-of-unique-xor-triplets-ii) | 1884 |\n| 3097 | [或值至少為 K 的最短子陣列 II](https://leetcode.cn/problems/shortest-subarray-with-or-at-least-k-ii) | 1891 |",
        },
        {
          id: 17,
          title: "2.3 資料結構",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目本身不難「看懂」，但暴力做法會反覆重算同一批資訊而超時。關鍵在於**選對一個輔助資料結構**，把需要反覆查詢的東西「事先整理好、邊走邊維護」，讓每次查詢或更新都很快。這個技巧的精神就是：先問「我每一步需要快速拿到什麼？」再挑能 `O(1)` 或 `O(log n)` 提供它的結構。\n\n常見的幾種武器，各擅長一件事：\n\n- **堆疊（stack）**：處理「最近一次」「成對抵消」「括號／配對」。例如 LeetCode 3703「移除 K-平衡子字串」，用棧記錄連續字元，湊滿就整段彈掉。\n- **堆（priority_queue）**：隨時取最大／最小值。例如 LeetCode 1792「最大平均通過率」，每次挑「提升幅度最大」的班級。\n- **雜湊表（unordered_map）**：`O(1)` 查「某個鍵之前出現過嗎／在哪」。例如 LeetCode 3026「最大好子陣列和」配合前綴和。\n- **前綴和（prefix sum）**：預處理一次，之後任意區間和 `O(1)` 算出。例如 LeetCode 2055「蠟燭之間的盤子」。\n\n:::example 前綴和：pre[i] = a[0]+...+a[i-1]，區間 [l, r] 的和 =…\n```cpp\n// 前綴和：pre[i] = a[0]+...+a[i-1]，區間 [l, r] 的和 = pre[r+1]-pre[l]\nvector<long long> pre(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n  pre[i + 1] = pre[i] + a[i];  // 預處理 O(n)\n}\nlong long sum = pre[r + 1] - pre[l];  // 之後每次查詢 O(1)\n```\n:::\n\n\n\n:::example 用雜湊表記「某前綴和第一次出現的位置」，方便快速回查\n核心觀念是把問題拆成三件事：**狀態怎麼表示、怎麼更新、答案怎麼讀。** 選定的資料結構必須讓這三件事都便宜。\n\n```cpp\n// 用雜湊表記「某前綴和第一次出現的位置」，方便快速回查\nunordered_map<long long, int> firstSeen;  // 前綴和 -> 最早下標\nfirstSeen[pre] = i;                       // 更新：只在第一次出現時記\n```\n:::\n\n\n\n**實作方式**\n\n把這類題拆成四步：\n\n**1. 確認「每一步要快速取得什麼」。** 是「目前最值」？「最近一個配對」？「某鍵是否出現過」？「某段區間和」？答案決定你該選堆、棧、雜湊表還是前綴和。\n\n:::example 例：用堆維護「目前提升幅度最大的班級」（最大平均通過率）\n**2. 選結構並定義狀態。** 把「真相」放進選定的結構，想清楚它存什麼鍵、什麼值。\n\n```cpp\n// 例：用堆維護「目前提升幅度最大的班級」（最大平均通過率）\n// 比較子讓「加一名學生後通過率提升最多」者排在堆頂\npriority_queue<pair<double, pair<int, int>>> pq;  // {提升幅度, {pass, total}}\n```\n:::\n\n\n\n:::example 棧的成對抵消：湊滿一段就整段移除（移除 K-平衡子字串的核心）\n**3. 寫更新與查詢，讓兩者都便宜。** 更新只動結構內部；查詢直接讀堆頂、查表或減前綴和。\n\n```cpp\n// 棧的成對抵消：湊滿一段就整段移除（移除 K-平衡子字串的核心）\nst.push_back({c, 1});\nif (!st.empty() && st.back().ch == c && st.back().cnt == k) {\n  st.pop_back();  // 滿 k 個就彈掉，避免重複掃描\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 前綴和要小心 `long long` 溢位與索引 off-by-one；堆題要確認比較子方向正確（大根還是小根）；雜湊表查不到鍵要有預設處理。複雜度視結構而定：前綴和查詢 `O(1)`、堆操作 `O(log n)`、雜湊表平均 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「選對資料結構」想：\n\n- **要反覆查詢「目前最大／最小」並伴隨增刪** → 用堆。例如 LeetCode 3408「設計工作管理員」，隨時取優先度最高的任務。\n- **要反覆查「某段區間的和」** → 前綴和。例如 LeetCode 2055「蠟燭之間的盤子」多次問兩根蠟燭間的盤子數。\n- **要處理「最近一次」「成對消除」「括號配對」** → 棧。例如 LeetCode 3703。\n- **暴力是「每次查詢都重新掃一遍」，明顯重算：**\n\n:::example 嗅到味道的瞬間：你正想為每個查詢都重掃整個陣列\n```cpp\n// 嗅到味道的瞬間：你正想為每個查詢都重掃整個陣列\n// for each query: for (int i = l; i <= r; ++i) sum += a[i];  // O(nq)\n// 改成預處理一次（前綴和 / 堆 / 雜湊表），查詢就變很快\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每一步最需要「快速拿到」的是什麼資訊？\n2. 哪個結構能讓「更新」和「查詢」都便宜（`O(1)` 或 `O(log n)`）？\n3. 我的暴力為什麼慢？是不是在重算可以預先整理好的東西？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3703 | [移除K-平衡子字串](https://leetcode.cn/problems/remove-k-balanced-substrings/) | 1802 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 3408 | [設計工作管理員](https://leetcode.cn/problems/design-task-manager/) | 1807 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 3026 | [最大好子陣列和](https://leetcode.cn/problems/maximum-good-subarray-sum/) | 1817 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 1792 | [最大平均透過率](https://leetcode.cn/problems/maximum-average-pass-ratio/) | 1818 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 2055 | [蠟燭之間的盤子](https://leetcode.cn/problems/plates-between-candles/) | 1819 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n\n**C++ 範例講解**\n\n以下用 3026. 最大好子陣列和 展示資料結構（前綴和 + 雜湊表）的核心寫法。\n\n**完整題目**：給陣列 nums 與正整數 k。一個子陣列是「好的」當且僅當其首尾元素之差的絕對值恰為 k。求所有好子陣列的最大元素和；若不存在則回傳 0。\n\n:::example 前綴和 + 雜湊表：找滿足 |nums[i]-nums[j]|==k 且子陣列和最大的非…\n**線索**：子陣列和可用前綴和表示為「右端前綴 − 左端前綴」。要最大化，就對每個右端，找滿足 `|nums[左]-nums[右]|=k` 的左端中、前綴最小者。用雜湊表記每個值對應的最小前綴和即可 O(n)。\n\n```cpp\nclass Solution {\n public:\n  long long maximumSubarraySum(vector<int>& nums, int k) {\n    // 前綴和 + 雜湊表：找滿足 |nums[i]-nums[j]|==k 且子陣列和最大的非空子陣列\n    // minPrefix[v] 儲存值為 v 的元素在當前位置之前出現時的最小前綴和\n    unordered_map<long long, long long> minPrefix;\n    long long prefix = 0, ans = LLONG_MIN;\n    for (int i = 0; i < (int)nums.size(); ++i) {\n      // 查找左端點值為 nums[i]-k 或 nums[i]+k 的情況（差的絕對值恰為 k）\n      for (long long need : {(long long)nums[i] - k, (long long)nums[i] + k}) {\n        auto it = minPrefix.find(need);\n        if (it != minPrefix.end()) {\n          // 子陣列和 = prefix[i+1] - prefix[j]，最大化需最小化 prefix[j]\n          ans = max(ans, prefix + nums[i] - it->second);\n        }\n      }\n      // 更新 minPrefix：僅在尚未記錄或找到更小前綴和時才更新\n      auto it = minPrefix.find(nums[i]);\n      if (it == minPrefix.end() || prefix < it->second) {\n        minPrefix[nums[i]] = prefix;\n      }\n      prefix += nums[i];\n    }\n    return ans == LLONG_MIN ? 0 : ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `prefix` 是 `nums[0..i-1]` 的和（不含目前元素）。\n- 子陣列 `[左..i]` 的和 = `prefix + nums[i] - 左端前綴`，故需最小化左端前綴。\n- 對每個 i，查 `nums[i] ± k` 這兩個首元素值對應的最小前綴更新答案。\n- 再以 `nums[i]` 為鍵記錄 / 更新最小前綴，供後續右端使用。\n\n**常見變形**\n\n- 和為 k 的最長 / 最多子陣列：前綴和 + 雜湊表的同類手法。\n- 連續子陣列和能被 k 整除：以餘數為鍵。\n- 把「差為 k」換成其他配對條件，框架不變。\n\n**實作注意事項**\n\n- 必須先用既有 map 計算答案，再把目前位置存入，避免左右端是同一個。\n- 數值與和可能很大，前綴與鍵都用 `long long`。\n- 無解時要回傳 0，用 `LLONG_MIN` 當未更新哨兵。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3703 | [移除K-平衡子字串](https://leetcode.cn/problems/remove-k-balanced-substrings) | 1802 |\n| 3408 | [設計工作管理員](https://leetcode.cn/problems/design-task-manager) | 1807 |\n| 3026 | [最大好子陣列和](https://leetcode.cn/problems/maximum-good-subarray-sum) | 1817 |\n| 1792 | [最大平均透過率](https://leetcode.cn/problems/maximum-average-pass-ratio) | 1818 |\n| 2055 | [蠟燭之間的盤子](https://leetcode.cn/problems/plates-between-candles) | 1819 |\n| 3148 | [矩陣中的最大得分](https://leetcode.cn/problems/maximum-difference-score-in-a-grid) | 1820 |\n| 1405 | [最長快樂字串](https://leetcode.cn/problems/longest-happy-string) | 1821 |\n| 2931 | [購買物品的最大開銷](https://leetcode.cn/problems/maximum-spending-after-buying-items) | 1822 |\n| 3781 | [二進位制交換後的最大分數](https://leetcode.cn/problems/maximum-score-after-binary-swaps) | 1823 |\n| 2034 | [股票價格波動](https://leetcode.cn/problems/stock-price-fluctuation) | 1832 |\n| 995 | [K 連續位的最小翻轉次數](https://leetcode.cn/problems/minimum-number-of-k-consecutive-bit-flips) | 1835 |\n| 1177 | [構建迴文串檢測](https://leetcode.cn/problems/can-make-palindrome-from-substring) | 1848 |\n| 1477 | [找兩個和為目標值且不重疊的子陣列](https://leetcode.cn/problems/find-two-non-overlapping-sub-arrays-each-with-target-sum) | 1851 |\n| 3815 | [設計拍賣系統](https://leetcode.cn/problems/design-auction-system) | 1854 |\n| 1546 | [和為目標值且不重疊的非空子陣列的最大數目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-subarrays-with-sum-equals-target) | 1855 |\n| 1202 | [交換字串中的元素](https://leetcode.cn/problems/smallest-string-with-swaps) | 1855 |\n| 1744 | [你能在你最喜歡的那天喫到你最喜歡的糖果嗎？](https://leetcode.cn/problems/can-you-eat-your-favorite-candy-on-your-favorite-day) | 1859 |\n| 1717 | [刪除子字串的最大得分](https://leetcode.cn/problems/maximum-score-from-removing-substrings) | 1868 |\n| 1589 | [所有排列中的最大和](https://leetcode.cn/problems/maximum-sum-obtained-of-any-permutation) | 1871 |\n| 1526 | [形成目標陣列的子陣列最少增加次數](https://leetcode.cn/problems/minimum-number-of-increments-on-subarrays-to-form-a-target-array) | 1872 |\n| 1106 | [解析布林表示式](https://leetcode.cn/problems/parsing-a-boolean-expression) | 1880 |\n| 1722 | [執行交換操作後的最小漢明距離](https://leetcode.cn/problems/minimize-hamming-distance-after-swap-operations) | 1892 |\n| 3608 | [包含 K 個連通分量需要的最小時間](https://leetcode.cn/problems/minimum-time-for-k-connected-components) | 1893 |\n| 2327 | [知道祕密的人數](https://leetcode.cn/problems/number-of-people-aware-of-a-secret) | 1894 |\n| 1878 | [矩陣中最大的三個菱形和](https://leetcode.cn/problems/get-biggest-three-rhombus-sums-in-a-grid) | 1898 |",
        },
        {
          id: 18,
          title: "2.4 動態規劃",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目問「最大／最小／有幾種方案」，而且答案是「一步一步累積」出來的——例如在網格裡一格一格走、在陣列裡一個一個元素決定要不要選。這類問題用「動態規劃（DP）」最對味。\n\n動態規劃的核心想法只有一句話：**把大問題拆成同型的小問題，把小問題的答案記下來，大問題就靠它們拼出來，不重算。**\n\n:::example 最優值 DP：走到 (i,j) 的最佳分數 = 最好的來源 + 本格\n舉個最直覺的例子：[網格中得分最大的路徑](https://leetcode.cn/problems/maximum-path-score-in-a-grid/)。你從左上走到右下，每步只能往右或往下。要走到某一格 `(i, j)`，上一步只可能來自上面 `(i-1, j)` 或左邊 `(i, j-1)`。所以「走到 `(i,j)` 的最佳分數」就等於「兩個來源中比較好的那個，再加上這一格」：\n\n```cpp\n// 最優值 DP：走到 (i,j) 的最佳分數 = 最好的來源 + 本格\ndp[i][j] = max(dp[i - 1][j], dp[i][j - 1]) + grid[i][j];\n```\n:::\n\n\n\n這裡的 `dp[i][j]` 就是「狀態」——它代表一個被定義清楚的小問題的答案。把每個狀態算過一次就存起來，後面要用直接查表，這就是 DP 比暴力枚舉快的原因：暴力會把同一條子路徑重算無數次，DP 每個狀態只算一次。\n\n:::example 計數型 DP：方案數 = 不選這個數 + 選這個數\n另一種常見口味是「計數型 DP」，問的是「有幾種方法」。例如 [將一個數字表示成冪的和的方案數](https://leetcode.cn/problems/ways-to-express-an-integer-as-sum-of-powers/)，狀態存的不是最大值，而是方案數量，轉移時用「加法」把各種選擇的方案數累加：\n\n```cpp\n// 計數型 DP：方案數 = 不選這個數 + 選這個數\ndp[cap] = dp[cap] + dp[cap - x];  // 與 0/1 背包同型\n```\n:::\n\n\n\n**實作方式**\n\n任何 DP 題都可以拆成三個固定問題，逐一搭配程式碼：\n\n:::example 狀態定義（以最長非遞減子陣列為例）\n**1. 定義狀態：先把「`dp[...]` 代表什麼」用一句話講清楚。** 這是最重要的一步，狀態定錯後面全錯。最優值題就定義成「到某位置的最佳值」，計數題就定義成「達到某條件的方案數」。\n\n```cpp\n// 狀態定義（以最長非遞減子陣列為例）\n// dp[i] = 以第 i 個元素「結尾」的最長非遞減子陣列長度\nvector<int> dp(n, 1);\n```\n:::\n\n\n\n:::example 轉移：由前一步推現在（最長非遞減子陣列）\n**2. 寫轉移：把「最後一步」拆開。** 想清楚「要算出 `dp[i]`，它的前一步可能從哪些狀態來」，從那些前態推出現在。這是 DP 的引擎。\n\n```cpp\n// 轉移：由前一步推現在（最長非遞減子陣列）\nif (a[i] >= a[i - 1]) {\n  dp[i] = dp[i - 1] + 1;  // 接在前面後面\n}  // 否則 dp[i] 維持 1（自己重開一段）\n```\n:::\n\n\n\n:::example 讀答案：答案不一定在 dp 末尾，常是整段最大值\n**3. 初始化與讀答案：定好起點，再決定答案在哪。** 起點是「最小的子問題」（如第一格、空集合）；答案有時是 `dp` 的最後一格，有時要取整段的最大值。\n\n```cpp\n// 讀答案：答案不一定在 dp 末尾，常是整段最大值\nint ans = *max_element(dp.begin(), dp.end());\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 計數題幾乎都要「取模」（題目給的 `mod`），每次相加後立刻 `% mod`，否則溢位。\n- 狀態若只依賴上一排（如網格 DP），可以用「滾動陣列」把空間從 `O(mn)` 壓到 `O(n)`。\n- 複雜度＝「狀態數 × 每個狀態的轉移成本」。例如 [不相交的線](https://leetcode.cn/problems/uncrossed-lines/) 有 `O(mn)` 個狀態、每個 `O(1)` 轉移，合計 `O(mn)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「動態規劃」想：\n\n- **問的是「最大／最小值」或「有幾種方案」，而且答案能由「前面的選擇」一步步累積。** 像 [矩陣的最大非負積](https://leetcode.cn/problems/maximum-non-negative-product-in-a-matrix/) 要算最大乘積，[不相交的線](https://leetcode.cn/problems/uncrossed-lines/) 本質是兩字串的最長公共子序列。\n- **「當下這一步」只跟「前一兩步」有關，不需要回顧全部歷史。** 這代表狀態維度很少，DP 表畫得出來。\n- **暴力做法會把同一個子問題重算很多次（指數級爆炸）。** DP 用一張表把每個子問題的答案記住，避免重算：\n\n:::example 嗅到味道的瞬間：暴力遞迴重複算同一個 (i,j)，於是改成查表\n```cpp\n// 嗅到味道的瞬間：暴力遞迴重複算同一個 (i,j)，於是改成查表\n// return solve(i-1,j) ... 會被呼叫無數次 → 加 memo 或改填表，就是 DP\n```\n:::\n\n\n\n- **計數題常伴隨 `對 1e9+7 取模`，最優值題常伴隨 `min/max`。** 看到這些關鍵字幾乎可確認是 DP。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `dp[...]` 到底代表「哪個小問題的答案」？能用一句話說清楚嗎？\n2. 要算 `dp[i]`，它的「最後一步」可能來自哪些前態？\n3. 起點（初始值）是什麼？最終答案要讀 `dp` 的哪一格、還是整段最大值？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3742 | [網格中得分最大的路徑](https://leetcode.cn/problems/maximum-path-score-in-a-grid/) | 1804 | 這是最優值 DP；練習把最後一步拆成若干前態，並確認取 `min/max` 的狀態已完整。 |\n| 1035 | [不相交的線](https://leetcode.cn/problems/uncrossed-lines/) | 1806 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 1594 | [矩陣的最大非負積](https://leetcode.cn/problems/maximum-non-negative-product-in-a-matrix/) | 1807 | 這是最優值 DP；練習把最後一步拆成若干前態，並確認取 `min/max` 的狀態已完整。 |\n| 3738 | [替換至多一個元素後最長非遞減子陣列](https://leetcode.cn/problems/longest-non-decreasing-subarray-after-replacing-at-most-one-element/) | 1811 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 2787 | [將一個數字表示成冪的和的方案數](https://leetcode.cn/problems/ways-to-express-an-integer-as-sum-of-powers/) | 1818 | 這是計數型 DP；先說清 `dp` 計的是前綴、容量還是狀態集合，並處理模數與初始化。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1425. 帶限制的子序列和 展示核心寫法。\n\n**完整題目**：選擇一個非空子序列，使相鄰被選元素下標差不超過 k，並最大化元素和。\n\n:::example 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以…\n**線索**：題目能拆成前綴、位置或選擇序列，且不同路徑會到達相同子問題；需要把影響未來的資訊寫成狀態。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以 i 結尾的最大子集和\n  int constrainedSubsetSum(vector<int>& nums, int k) {\n    deque<int> dq;  // 單調遞減 deque，儲存下標，保證 dp[dq.front()] 為窗口最大值\n    vector<int> dp(nums.size());\n    int ans = INT_MIN;\n    for (int i = 0; i < (int)nums.size(); ++i) {\n      // 移除已超出 k 範圍的窗口頭部\n      while (!dq.empty() && dq.front() < i - k) {\n        dq.pop_front();\n      }\n      // dp[i]：選 nums[i] 加上前 k 個位置中最大的正 dp 值（若最大值為負則不取前段）\n      dp[i] = nums[i] + (dq.empty() ? 0 : max(0, dp[dq.front()]));\n      // 維護單調遞減性：彈出所有 dp 值不大於 dp[i] 的尾部\n      while (!dq.empty() && dp[dq.back()] <= dp[i]) {\n        dq.pop_back();\n      }\n      dq.push_back(i);\n      ans = max(ans, dp[i]);\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `dp[i]` 需要查前 k 個位置的最大 dp 值；單調佇列把這個區間最大值查詢降成均攤 O(1)。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 前綴和優化。\n- 單調佇列/堆疊優化。\n- 樹狀陣列、線段樹、斜率優化。\n\n**實作注意事項**\n\n- 先保留樸素式方便驗證。\n- 資料結構查的是哪個前驅集合要寫清楚。\n- 單調性不成立時不能硬套。\n- 可用小範圍對拍確認優化等價。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3742 | [網格中得分最大的路徑](https://leetcode.cn/problems/maximum-path-score-in-a-grid) | 1804 |\n| 1035 | [不相交的線](https://leetcode.cn/problems/uncrossed-lines) | 1806 |\n| 1594 | [矩陣的最大非負積](https://leetcode.cn/problems/maximum-non-negative-product-in-a-matrix) | 1807 |\n| 3738 | [替換至多一個元素後最長非遞減子陣列](https://leetcode.cn/problems/longest-non-decreasing-subarray-after-replacing-at-most-one-element) | 1811 |\n| 2787 | [將一個數字表示成冪的和的方案數](https://leetcode.cn/problems/ways-to-express-an-integer-as-sum-of-powers) | 1818 |\n| 1363 | [形成三的最大倍數](https://leetcode.cn/problems/largest-multiple-of-three) | 1823 |\n| 1458 | [兩個子序列的最大點積](https://leetcode.cn/problems/max-dot-product-of-two-subsequences) | 1824 |\n| 3332 | [旅客可以得到的最多點數](https://leetcode.cn/problems/maximum-points-tourist-can-earn) | 1828 |\n| 980 | [不同路徑 III](https://leetcode.cn/problems/unique-paths-iii) | 1830 |\n| 2370 | [最長理想子序列](https://leetcode.cn/problems/longest-ideal-subsequence) | 1835 |\n| 3186 | [施咒的最大總傷害](https://leetcode.cn/problems/maximum-total-damage-with-spell-casting) | 1841 |\n| 1411 | [給 N x 3 網格圖塗色的方案數](https://leetcode.cn/problems/number-of-ways-to-paint-n-3-grid) | 1845 |\n| 3825 | [按位與結果非零的最長上升子序列](https://leetcode.cn/problems/longest-strictly-increasing-subsequence-with-non-zero-bitwise-and) | 1846 |\n| 3196 | [最大化子陣列的總成本](https://leetcode.cn/problems/maximize-total-cost-of-alternating-subarrays) | 1847 |\n| 3747 | [統計移除零後不同整數的數目](https://leetcode.cn/problems/count-distinct-integers-after-removing-zeros) | 1848 |\n| 3180 | [執行操作可獲得的最大總獎勵 I](https://leetcode.cn/problems/maximum-total-reward-using-operations-i) | 1849 |\n| 3176 | [求出最長好子序列 I](https://leetcode.cn/problems/find-the-maximum-length-of-a-good-subsequence-i) | 1849 |\n| 2059 | [轉化數字的最小運算數](https://leetcode.cn/problems/minimum-operations-to-convert-number) | 1850 |\n| 2830 | [銷售利潤最大化](https://leetcode.cn/problems/maximize-the-profit-as-the-salesman) | 1851 |\n| 1301 | [最大得分的路徑數目](https://leetcode.cn/problems/number-of-paths-with-max-score) | 1853 |\n| 1269 | [停在原地的方案數](https://leetcode.cn/problems/number-of-ways-to-stay-in-the-same-place-after-some-steps) | 1854 |\n| 2266 | [統計打字方案數](https://leetcode.cn/problems/count-number-of-texts) | 1857 |\n| 2767 | [將字串分割為最少的美麗子字串](https://leetcode.cn/problems/partition-string-into-minimum-beautiful-substrings) | 1865 |\n| 1340 | [跳躍遊戲 V](https://leetcode.cn/problems/jump-game-v) | 1866 |\n| 2002 | [兩個迴文子序列長度的最大乘積](https://leetcode.cn/problems/maximum-product-of-the-length-of-two-palindromic-subsequences) | 1869 |\n| 2008 | [計程車的最大盈利](https://leetcode.cn/problems/maximum-earnings-from-taxi) | 1872 |\n| 3665 | [統計鏡子反射路徑數目](https://leetcode.cn/problems/twisted-mirror-path-count) | 1883 |\n| 2054 | [兩個最好的不重疊活動](https://leetcode.cn/problems/two-best-non-overlapping-events) | 1883 |\n| 3472 | [至多 K 次操作後的最長迴文子序列](https://leetcode.cn/problems/longest-palindromic-subsequence-after-at-most-k-operations) | 1884 |\n| 2305 | [公平分發餅乾](https://leetcode.cn/problems/fair-distribution-of-cookies) | 1887 |\n| 1871 | [跳躍遊戲 VII](https://leetcode.cn/problems/jump-game-vii) | 1896 |\n| 3250 | [單調陣列對的數目 I](https://leetcode.cn/problems/find-the-count-of-monotonic-pairs-i) | 1898 |\n| 2901 | [最長相鄰不相等子序列 II](https://leetcode.cn/problems/longest-unequal-adjacent-groups-subsequence-ii) | 1899 |",
        },
        {
          id: 19,
          title: "2.5 圖論演算法",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目裡有「東西」和「東西之間的連結」，問你「從 A 到 B 最少要幾步」「能不能到」「最短要多久」。這類問題的通用模型就是「圖（graph）」——把「東西」當成節點、把「連結」當成邊，再用圖論演算法去走。\n\n最關鍵、也最常考的工具是「最短路」。它又分兩種，選哪一種取決於「邊有沒有權重」：\n\n:::example BFS：每步成本都是 1，一層一層往外擴\n第一種是 **BFS（廣度優先搜尋）**，適用於「每走一步成本都是 1」的情況。它像水波一樣，從起點一圈一圈往外擴，第一次碰到目標時走過的層數就是最短步數。\n\n```cpp\n// BFS：每步成本都是 1，一層一層往外擴\nqueue<int> q;\nq.push(start);\nvector<int> dist(n, -1);\ndist[start] = 0;\nwhile (!q.empty()) {\n  int u = q.front();\n  q.pop();\n  for (int v : adj[u]) {\n    if (dist[v] == -1) {      // 還沒走過\n      dist[v] = dist[u] + 1;  // 第一次到，就是最短\n      q.push(v);\n    }\n  }\n}\n```\n:::\n\n\n\n:::example Dijkstra：邊權非負但不一定為 1，用小根堆永遠先擴最近的\n第二種是 **Dijkstra**，適用於「每條邊成本不同、但都非負」的情況。它用一個「小根堆」永遠先處理「目前已知距離最小」的節點，確保每個節點第一次被取出時拿到的就是最短距離。例如 [設計可以求最短路徑的圖類](https://leetcode.cn/problems/design-graph-with-shortest-path-calculator/) 邊權各異，就要用它。\n\n```cpp\n// Dijkstra：邊權非負但不一定為 1，用小根堆永遠先擴最近的\npriority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;\npq.push({0, start});  // {距離, 節點}\nwhile (!pq.empty()) {\n  auto [d, u] = pq.top();\n  pq.pop();\n  if (d > dist[u]) {\n    continue;  // 過期紀錄，跳過\n  }\n  for (auto [v, w] : adj[u]) {\n    if (d + w < dist[v]) {\n      dist[v] = d + w;\n      pq.push({dist[v], v});\n    }\n  }\n}\n```\n:::\n\n\n\n圖論題真正的難點，往往不是這些套路本身，而是**「怎麼把題目翻譯成圖」**——也就是「圖建模」。節點不一定是看得見的城市，也可能是一個抽象「狀態」。\n\n**實作方式**\n\n任何圖論最短路題都可拆成四步，逐一搭配程式碼：\n\n:::example 圖建模（狀態壓成整數）：把整個盤面編碼成一個 int 當節點\n**1. 圖建模：先想清楚「節點是什麼、邊是什麼」。** 這是成敗關鍵。例如 [跳躍遊戲 IV](https://leetcode.cn/problems/jump-game-iv/) 的節點是「下標」，邊是「能跳到的位置」；[轉化為全零矩陣的最少反轉次數](https://leetcode.cn/problems/minimum-number-of-flips-to-convert-binary-matrix-to-zero-matrix/) 的節點則是「整個矩陣的某個 0/1 盤面」，邊是「按一下某格」。\n\n```cpp\n// 圖建模（狀態壓成整數）：把整個盤面編碼成一個 int 當節點\nint code = 0;\nfor (int cell : flatten(grid)) {\n  code = code * 2 + cell;  // 0/1 盤面 -> 一個數\n}\n```\n:::\n\n\n\n:::example 鄰接表：adj[u] 存所有從 u 出發、(鄰居, 邊權)\n**2. 建鄰接表：把邊整理成「每個節點能去哪」。**\n\n```cpp\n// 鄰接表：adj[u] 存所有從 u 出發、(鄰居, 邊權)\nvector<vector<pair<int, int>>> adj(n);\nadj[u].push_back({v, w});\n```\n:::\n\n\n\n**3. 選演算法跑最短路：邊權全是 1 用 BFS，否則用 Dijkstra。** 同時用 `dist`（或 `visited`）記錄已知最短距離，避免重複處理。\n\n**4. 邊界與複雜度。**\n\n- 額外狀態要併進節點：若「同一格但帶不同道具/鑰匙」算不同情況（如 [你能從盒子裡獲得的最大糖果數](https://leetcode.cn/problems/maximum-candies-you-can-get-from-boxes/)），就要把那些資訊一起編進節點，否則會誤判已訪問。\n- `visited` 一定要在「入隊時或第一次定下距離時」標記，漏掉會超時。\n- 複雜度：BFS 是 `O(V + E)`；Dijkstra 用堆是 `O(E log V)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「圖論最短路」想：\n\n- **問「最少幾步」「最短時間」「最少操作」「能不能到達」。** 像 [有向圖中到達終點的最少時間](https://leetcode.cn/problems/minimum-time-to-reach-destination-in-directed-graph/) 直接就是最短路。\n- **元素之間有「可以從這個變到那個」的轉換關係。** 即使題目沒提到「圖」字，只要能畫出「節點 + 邊」就是圖題：\n\n:::example 嗅到味道的瞬間：問題能描述成「從一個狀態走到另一個狀態」\n```cpp\n// 嗅到味道的瞬間：問題能描述成「從一個狀態走到另一個狀態」\n// state_now ---一次操作---> state_next   → 這就是一條邊，整體就是圖\n```\n:::\n\n\n\n- **狀態空間不大、可枚舉**（如矩陣小到能把整個盤面當一個節點），就能用 BFS/Dijkstra 在狀態圖上跑。\n- **要先判斷邊權**：全是 1 → BFS；非負但不同 → Dijkstra。判斷錯就會用錯演算法。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「節點」代表什麼？是看得見的位置，還是一個抽象狀態？\n2. 兩個節點之間「有邊」的條件是什麼？邊權是 1 還是一般非負數？\n3. 有沒有「額外狀態」（道具、剩餘次數）必須一起編進節點，才不會誤判訪問？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1345 | [跳躍遊戲 IV](https://leetcode.cn/problems/jump-game-iv/) | 1810 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 2642 | [設計可以求最短路徑的圖類](https://leetcode.cn/problems/design-graph-with-shortest-path-calculator/) | 1811 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n| 1284 | [轉化為全零矩陣的最少反轉次數](https://leetcode.cn/problems/minimum-number-of-flips-to-convert-binary-matrix-to-zero-matrix/) | 1811 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n| 1298 | [你能從盒子裡獲得的最大糖果數](https://leetcode.cn/problems/maximum-candies-you-can-get-from-boxes/) | 1825 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 3604 | [有向圖中到達終點的最少時間](https://leetcode.cn/problems/minimum-time-to-reach-destination-in-directed-graph/) | 1845 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 743. 網路延遲時間 展示核心寫法。\n\n**完整題目**：給定有向帶權邊、節點數與起點，求訊號從起點傳到所有節點所需的最長最短路時間；若有節點不可達則回傳 `-1`。\n\n:::example Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n**線索**：題目描述可達、路徑、依賴或關係傳遞；把物件當節點、操作當邊後，就能選遍歷或最短路。\n\n```cpp\nclass Solution {\n public:\n  // Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n  int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    vector<vector<pair<int, int>>> g(n + 1);  // 鄰接表，g[u] = {(v, w)}\n    for (auto& e : times) {\n      g[e[0]].push_back({e[1], e[2]});\n    }\n    const int INF = 1e9;\n    vector<int> dist(n + 1, INF);\n    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;  // 最小堆：{距離, 節點}\n    dist[k] = 0;\n    pq.push({0, k});\n    while (!pq.empty()) {\n      auto [d, u] = pq.top();\n      pq.pop();\n      if (d != dist[u]) {\n        continue;  // 已有更短路徑，跳過過期條目\n      }\n      for (auto [v, w] : g[u]) {\n        if (dist[v] > d + w) {\n          dist[v] = d + w;  // 鬆弛操作\n          pq.push({dist[v], v});\n        }\n      }\n    }\n    // 所有節點最短距離的最大值即為信號傳播時間\n    int ans = *max_element(dist.begin() + 1, dist.end());\n    return ans == INF ? -1 : ans;  // 若有節點不可達則返回 -1\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 非負權最短路使用 Dijkstra；堆中舊距離要跳過，避免重複鬆弛造成常數過大。\n- 先建出鄰接關係，再用 `visited`、`dist`、`indeg` 等狀態控制每個節點何時處理。\n- 如果同一節點在不同資源狀態下未來不同，就不能只用節點編號當 visited 鍵。\n\n**常見變形**\n\n- 無權最短路用 BFS。\n- 0/1 權用 0-1 BFS。\n- 非負權用 Dijkstra；有負權需 Bellman-Ford/SPFA 類思路。\n\n**實作注意事項**\n\n- 先估點數與邊數，決定鄰接表或矩陣。\n- Dijkstra 不適用負權邊。\n- 多源 BFS 要把所有源點同時入隊。\n- 狀態圖要包含資源、鑰匙、次數等額外維度。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1345 | [跳躍遊戲 IV](https://leetcode.cn/problems/jump-game-iv) | 1810 |\n| 2642 | [設計可以求最短路徑的圖類](https://leetcode.cn/problems/design-graph-with-shortest-path-calculator) | 1811 |\n| 1284 | [轉化為全零矩陣的最少反轉次數](https://leetcode.cn/problems/minimum-number-of-flips-to-convert-binary-matrix-to-zero-matrix) | 1811 |\n| 1298 | [你能從盒子裡獲得的最大糖果數](https://leetcode.cn/problems/maximum-candies-you-can-get-from-boxes) | 1825 |\n| 3604 | [有向圖中到達終點的最少時間](https://leetcode.cn/problems/minimum-time-to-reach-destination-in-directed-graph) | 1845 |\n| 1514 | [機率最大的路徑](https://leetcode.cn/problems/path-with-maximum-probability) | 1846 |\n| 3650 | [邊反轉的最小路徑總成本](https://leetcode.cn/problems/minimum-cost-path-with-edge-reversals) | 1854 |\n| 1334 | [閾值距離內鄰居最少的城市](https://leetcode.cn/problems/find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance) | 1855 |\n| 1584 | [連線所有點的最小費用](https://leetcode.cn/problems/min-cost-to-connect-all-points) | 1858 |\n| 3342 | [到達最後一個房間的最少時間 II](https://leetcode.cn/problems/find-minimum-time-to-reach-last-room-ii) | 1862 |\n| 2039 | [網路空閒的時刻](https://leetcode.cn/problems/the-time-when-the-network-becomes-idle) | 1865 |\n| 924 | [儘量減少惡意軟體的傳播](https://leetcode.cn/problems/minimize-malware-spread) | 1869 |\n| 2101 | [引爆最多的炸彈](https://leetcode.cn/problems/detonate-the-maximum-bombs) | 1880 |\n| 2976 | [轉換字串的最小成本 I](https://leetcode.cn/problems/minimum-cost-to-convert-string-i) | 1882 |\n| 2360 | [圖中的最長環](https://leetcode.cn/problems/longest-cycle-in-a-graph) | 1897 |",
        },
        {
          id: 20,
          title: "2.6 貪心",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目要你「最大化／最小化某個結果」，而你有一連串選擇要做。「貪心（greedy）」的想法很大膽：**每一步都直接挑「當下看起來最好」的選項，不回頭、不反悔，最後就能得到全域最優解。**\n\n聽起來太天真了——憑什麼局部最好就保證全域最好？這正是貪心的精髓與陷阱：**它不總是對的**。貪心能用的前提是，題目具有某種特殊結構，使得「先把眼前最有利的決定做掉，不會擋住後面更好的選擇」。所以寫貪心題，真正的工作不是寫程式（程式通常很短），而是**想出正確的選擇規則，並說服自己它真的對**。\n\n:::example 貪心選擇：高位優先，能放越小越好（前提是後面還湊得出目標）\n舉個例子，[字典序最小和為目標值且絕對值是排列的陣列](https://leetcode.cn/problems/lexicographically-smallest-negated-permutation-that-sums-to-target/) 要字典序最小，貪心策略是「從最前面那一位開始，在合法前提下盡量放最小的數」。因為字典序由高位主導，前面一位的影響遠大於後面所有位元的總和，所以先把高位壓到最小一定不虧：\n\n```cpp\n// 貪心選擇：高位優先，能放越小越好（前提是後面還湊得出目標）\nfor (int pos = 0; pos < n; pos++) {\n  for (int v = smallest; v <= largest; v++) {\n    if (feasible(pos, v)) {\n      place(pos, v);\n      break;\n    }  // 找到可行的最小值就定下\n  }\n}\n```\n:::\n\n\n\n:::example 交換論證的直覺（非程式，是思路）\n怎麼確認規則正確？最常用的證明叫「交換論證」：假設有個最優解沒按我的貪心選，那我把它「換」成貪心的選法，證明換完之後結果不會變差。既然能換而不虧，貪心解就和最優解一樣好。\n\n```cpp\n// 交換論證的直覺（非程式，是思路）\n// 假設最優解某處沒選貪心想選的 x，把它換成 x →\n// 證明「目標值不變、其他約束仍滿足」→ 貪心解 >= 最優解 → 貪心正確\n```\n:::\n\n\n\n**實作方式**\n\n任何貪心題都可拆成四步：\n\n:::example 決策維度：很多貪心要先排序，讓「最該先處理的」排到前面\n**1. 確定優化目標與「決策維度」。** 先問清楚要最大化還是最小化什麼，以及「我是在沿哪個維度逐步做決定」——是從高位到低位、從大到小、還是某種排序後的順序。\n\n```cpp\n// 決策維度：很多貪心要先排序，讓「最該先處理的」排到前面\nsort(items.begin(), items.end(), cmp);  // cmp 體現你的貪心優先級\n```\n:::\n\n\n\n**2. 寫出明確的選擇規則。** 用一句話說死「每一步我挑誰」。例如 [你可以工作的最大週數](https://leetcode.cn/problems/maximum-number-of-weeks-for-which-you-can-work/) 的關鍵觀察是「最大的那一堆會不會超過其餘全部之和」，由此推出能完成的最大週數。\n\n**3. 證明（至少說服自己）規則保留最優。** 用交換論證或歸納法，確認「先做這步不會讓後面變差」。[最大化陣列末位元素的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-maximize-last-elements-in-arrays/) 就要小心：不能只憑局部最大，要論證所選規則確實全域最優。\n\n:::example 典型貪心主體：掃一遍，邊走邊根據規則更新答案\n**4. 實作並處理邊界。** 貪心程式通常一個 `for` 迴圈掃過去，邊掃邊累積答案。\n\n```cpp\n// 典型貪心主體：掃一遍，邊走邊根據規則更新答案\nint ans = 0;\nfor (auto& x : items) {\n  if (canTake(x)) {\n    take(x);\n    ans += gain(x);\n  }  // 符合規則就拿\n}\n```\n:::\n\n\n\n**邊界與複雜度。** 多數貪心是 `O(n)` 或排序主導的 `O(n log n)`。常見陷阱是「比較規則寫反」「相等時的取捨」「位元/翻轉題的進位影響」（如 [翻轉矩陣後的得分](https://leetcode.cn/problems/score-after-flipping-matrix/)、[修改後的最大二進位制字串](https://leetcode.cn/problems/maximum-binary-string-after-change/) 都要從高位的價值出發決策）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「貪心」想：\n\n- **要最大化／最小化某結果，且每一步都能獨立做一個「當下最佳」的選擇。** 像 [翻轉矩陣後的得分](https://leetcode.cn/problems/score-after-flipping-matrix/)：每一列、每一欄是否翻，從「讓高位變 1」這個當下最有利角度逐一決定。\n- **存在一個「明顯該優先處理」的維度**：高位優先（二進位/字典序題）、大的優先或小的優先（排序後處理）。\n- **你能講出一個簡單規則，並用交換論證說明它不會吃虧。** 反過來，如果你想不出規則、或舉得出反例，就別硬上貪心，可能要改 DP：\n\n:::example 嗅到味道的瞬間：你能說出「每步就選 ___ 一定不虧」並能論證\n```cpp\n// 嗅到味道的瞬間：你能說出「每步就選 ___ 一定不虧」並能論證\n// 若怎麼想都有反例 → 退回 DP，把所有選擇都試過\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每一步的「選擇規則」是什麼？能用一句話說死嗎？\n2. 我沿哪個維度做決策（高位優先？排序後從大到小？）？\n3. 我能不能用交換論證說明「先做這步不會讓後面變差」？想得出反例嗎？\n\n前兩題答得出、第三題說服得了自己，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2934 | [最大化陣列末位元素的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-maximize-last-elements-in-arrays/) | 1803 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1953 | [你可以工作的最大週數](https://leetcode.cn/problems/maximum-number-of-weeks-for-which-you-can-work/) | 1804 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 861 | [翻轉矩陣後的得分](https://leetcode.cn/problems/score-after-flipping-matrix/) | 1818 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1702 | [修改後的最大二進位制字串](https://leetcode.cn/problems/maximum-binary-string-after-change/) | 1825 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3752 | [字典序最小和為目標值且絕對值是排列的陣列](https://leetcode.cn/problems/lexicographically-smallest-negated-permutation-that-sums-to-target/) | 1827 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(), [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        kept++;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2934 | [最大化陣列末位元素的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-maximize-last-elements-in-arrays) | 1803 |\n| 1953 | [你可以工作的最大週數](https://leetcode.cn/problems/maximum-number-of-weeks-for-which-you-can-work) | 1804 |\n| 861 | [翻轉矩陣後的得分](https://leetcode.cn/problems/score-after-flipping-matrix) | 1818 |\n| 1702 | [修改後的最大二進位制字串](https://leetcode.cn/problems/maximum-binary-string-after-change) | 1825 |\n| 3752 | [字典序最小和為目標值且絕對值是排列的陣列](https://leetcode.cn/problems/lexicographically-smallest-negated-permutation-that-sums-to-target) | 1827 |\n| 1754 | [構造字典序最大的合併字串](https://leetcode.cn/problems/largest-merge-of-two-strings) | 1829 |\n| 3012 | [透過操作使陣列長度最小](https://leetcode.cn/problems/minimize-length-of-array-using-operations) | 1833 |\n| 3796 | [找到帶限制序列的最大值](https://leetcode.cn/problems/find-maximum-value-in-a-constrained-sequence) | 1833 |\n| 2311 | [小於等於 K 的最長二進位制子序列](https://leetcode.cn/problems/longest-binary-subsequence-less-than-or-equal-to-k) | 1840 |\n| 2332 | [坐上公交的最晚時間](https://leetcode.cn/problems/the-latest-time-to-catch-a-bus) | 1841 |\n| 3800 | [使二進位制字串相等的最小成本](https://leetcode.cn/problems/minimum-cost-to-make-two-binary-strings-equal) | 1841 |\n| 1775 | [透過最少操作次數使陣列的和相等](https://leetcode.cn/problems/equal-sum-arrays-with-minimum-number-of-operations) | 1850 |\n| 2162 | [設定時間的最少代價](https://leetcode.cn/problems/minimum-cost-to-set-cooking-time) | 1852 |\n| 3443 | [K 次修改後的最大曼哈頓距離](https://leetcode.cn/problems/maximum-manhattan-distance-after-k-changes) | 1856 |\n| 3035 | [迴文字串的最大數量](https://leetcode.cn/problems/maximum-palindromes-after-operations) | 1857 |\n| 3326 | [使陣列非遞減的最少除法操作次數](https://leetcode.cn/problems/minimum-division-operations-to-make-array-non-decreasing) | 1864 |\n| 1605 | [給定行和列的和求可行矩陣](https://leetcode.cn/problems/find-valid-matrix-given-row-and-column-sums) | 1868 |\n| 1616 | [分割兩個字串得到迴文串](https://leetcode.cn/problems/split-two-strings-to-make-palindrome) | 1868 |\n| 3635 | [最早完成陸地和水上游樂設施的時間 II](https://leetcode.cn/problems/earliest-finish-time-for-land-and-water-rides-ii) | 1870 |\n| 955 | [刪列造序 II](https://leetcode.cn/problems/delete-columns-to-make-sorted-ii) | 1876 |\n| 1536 | [排布二進位制網格的最少交換次數](https://leetcode.cn/problems/minimum-swaps-to-arrange-a-binary-grid) | 1881 |\n| 1326 | [灌溉花園的最少水龍頭數目](https://leetcode.cn/problems/minimum-number-of-taps-to-open-to-water-a-garden) | 1885 |",
        },
        {
          id: 21,
          title: "2.7 網格圖",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目給你一個二維方格（grid），格子之間上下左右相鄰，問你「最短要走幾步」「有幾塊連在一起的區域」「能不能走到」之類的問題。「網格圖」的核心觀念其實只有一句話：**把每個格子當成圖的一個節點，相鄰的格子之間連一條邊，於是整張網格就變成一張圖，可以直接套 BFS / DFS。**\n\n:::example 方向陣列：一次列舉上下左右四個鄰居\n走網格時有兩個幾乎每題都會用到的小工具。第一個是「方向陣列」，用它一次處理上下左右四個鄰居，不用寫四段重複程式：\n\n```cpp\n// 方向陣列：一次列舉上下左右四個鄰居\nint dx[4] = {-1, 1, 0, 0};\nint dy[4] = {0, 0, -1, 1};\nfor (int d = 0; d < 4; d++) {\n  int nx = x + dx[d], ny = y + dy[d];  // 鄰居座標\n  // 接著檢查 nx, ny 是否越界 / 是障礙 / 已訪問\n}\n```\n:::\n\n\n\n:::example 網格 BFS：從起點一層一層往外擴，第一次到目標即最短\n第二個是 `visited` 標記，避免同一格被重複走進去而無限繞圈。網格題的最短路因為「每步成本都是 1」，所以幾乎都用 BFS——它一圈一圈往外擴，第一次碰到目標時就是最短步數。例如 [最短的橋](https://leetcode.cn/problems/shortest-bridge/) 要找兩座島之間最短填海距離，就是典型的網格 BFS：\n\n```cpp\n// 網格 BFS：從起點一層一層往外擴，第一次到目標即最短\nqueue<pair<int, int>> q;\nq.push({sx, sy});\nvisited[sx][sy] = true;\nint step = 0;\nwhile (!q.empty()) {\n  int sz = q.size();\n  while (sz--) { /* 取出一格，把合法鄰居入隊並標記 visited */\n  }\n  step++;  // 擴完一整圈，步數 +1\n}\n```\n:::\n\n\n\n「找有幾塊連通區域」「把一整塊染色」則常用 DFS 或 BFS 從一格出發把整塊走完。\n\n**實作方式**\n\n任何網格圖題都可拆成四步：\n\n:::example 多源 BFS：把所有起點先一起入隊，再一起往外擴\n**1. 想清楚「起點」與「搜尋型態」。** 是從一個起點出發（單源），還是同時從很多起點一起擴（多源 BFS）？例如 [最短的橋](https://leetcode.cn/problems/shortest-bridge/) 要先把整座島的所有格子一次全放進佇列當起點，這就是多源。\n\n```cpp\n// 多源 BFS：把所有起點先一起入隊，再一起往外擴\nfor (auto [x, y] : sources) {\n  q.push({x, y});\n  visited[x][y] = true;\n}\n```\n:::\n\n\n\n:::example 鄰居合法性檢查：越界、障礙、已訪問 都要擋掉\n**2. 走訪時嚴格檢查每個鄰居的合法性。** 順序通常是：越界？是障礙（如牆、海）？已經訪問過？三關都過才能走。\n\n```cpp\n// 鄰居合法性檢查：越界、障礙、已訪問 都要擋掉\nbool ok(int x, int y) { return x >= 0 && x < m && y >= 0 && y < n && grid[x][y] != WALL && !visited[x][y]; }\n```\n:::\n\n\n\n**3. 標記 visited 的時機要對。** 在「入隊的瞬間」就標記，不要等出隊才標，否則同一格會被重複入隊導致超時。\n\n**4. 邊界與複雜度。**\n\n- 連通塊題（如 [二維網格圖中探測環](https://leetcode.cn/problems/detect-cycles-in-2d-grid/)）要對「每個還沒訪問的格子」都嘗試啟動一次搜尋，才不會漏掉孤立的塊。\n- 帶條件的網格（如 [價格範圍內最高排名的 K 樣物品](https://leetcode.cn/problems/k-highest-ranked-items-within-a-price-range/)）BFS 時要同時帶上「步數」並按題目規則排序候選。\n- 複雜度：每格最多進出佇列一次，整體 `O(mn)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「網格圖」想：\n\n- **輸入是一個二維陣列／矩陣，格子之間上下左右（有時含斜向）相鄰。** 像 [最短的橋](https://leetcode.cn/problems/shortest-bridge/)、[價格範圍內最高排名的 K 樣物品](https://leetcode.cn/problems/k-highest-ranked-items-within-a-price-range/) 都直接給你一張網格。\n- **問「最短距離 / 最少步數」「連通塊數量」「能否到達」「有沒有環」。** 這些都是圖的基本問題，搬到網格上就是網格圖：\n\n:::example 嗅到味道的瞬間：把「格子」想成「節點」，「相鄰」想成「邊」\n```cpp\n// 嗅到味道的瞬間：把「格子」想成「節點」，「相鄰」想成「邊」\n// (i,j) 與 (i,j+1) 相鄰 → 它們之間有一條邊 → 整張 grid 就是一張圖\n```\n:::\n\n\n\n- **「最短」且「每步成本都是 1」→ 用 BFS**；「數區域 / 染色 / 找環」→ 用 DFS 或 BFS 走滿整塊。\n- 看到「島嶼」「牆」「障礙」「填海」「擴散」這類字眼，幾乎就是網格搜尋。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的起點是一個還是很多個（單源還是多源 BFS）？\n2. 一個鄰居「能走」的條件是什麼（越界、障礙、已訪問都檢查了嗎）？\n3. 我在「入隊時」就標記 visited 了嗎？會不會有格子被重複處理？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 934 | [最短的橋](https://leetcode.cn/problems/shortest-bridge/) | 1826 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 2146 | [價格範圍內最高排名的 K 樣物品](https://leetcode.cn/problems/k-highest-ranked-items-within-a-price-range/) | 1837 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 1559 | [二維網格圖中探測環](https://leetcode.cn/problems/detect-cycles-in-2d-grid/) | 1838 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n:::example DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\n public:\n  int numIslands(vector<vector<char>>& grid) {\n    int m = grid.size(), n = grid[0].size(), ans = 0;\n    int dirs[5] = {1, 0, -1, 0, 1};  // 方向陣列，相鄰兩元素構成上下左右四個方向向量\n    // DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n    function<void(int, int)> dfs = [&](int r, int c) {\n      grid[r][c] = '0';  // 標記已訪問，避免重複計算\n      for (int k = 0; k < 4; ++k) {\n        int nr = r + dirs[k], nc = c + dirs[k + 1];\n        if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n          dfs(nr, nc);\n        }\n      }\n    };\n    // 掃描整個網格，每遇到一個未訪問的 '1' 就是一座新島嶼\n    for (int r = 0; r < m; ++r) {\n      for (int c = 0; c < n; ++c) {\n        if (grid[r][c] == '1') {\n          ans++, dfs(r, c);\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 934 | [最短的橋](https://leetcode.cn/problems/shortest-bridge) | 1826 |\n| 2146 | [價格範圍內最高排名的 K 樣物品](https://leetcode.cn/problems/k-highest-ranked-items-within-a-price-range) | 1837 |\n| 1559 | [二維網格圖中探測環](https://leetcode.cn/problems/detect-cycles-in-2d-grid) | 1838 |",
        },
        {
          id: 22,
          title: "2.8 數學",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目本質是一個「數學問題」——它要你找規律、推公式、處理進位與進位制、或在「除法、餘數、整除」上做文章。這類題的特點是：**只要把背後的數學想通，程式碼往往短得驚人；想不通就會卡死或暴力超時。** 所以本節真正的工夫在「動筆推導」，而不是寫迴圈。\n\n:::example 進位制加法：逐位相加，把「進位 carry」傳到下一位\n數學題沒有單一萬用套路，但有幾個反覆出現的工具。第一個是「處理進位制 / 進位」。例如 [負二進位制數相加](https://leetcode.cn/problems/adding-two-negabinary-numbers/)，底數是 `-2` 而非 `2`，加法時的進位規則跟一般二進位不同，要小心地一位一位處理：\n\n```cpp\n// 進位制加法：逐位相加，把「進位 carry」傳到下一位\nint carry = 0;\nfor (int i = 0; i < len; i++) {\n  int sum = a[i] + b[i] + carry;  // 本位先加總\n  digit[i] = mod(sum);            // 取出本位數字\n  carry = computeCarry(sum);      // 算出要往下一位帶多少\n}\n```\n:::\n\n\n\n:::example 用餘數找答案：餘數只有 K 種，最多走 K 步必出結果或判定無解\n第二個常見工具是「靠餘數找循環」。很多「最小的滿足某條件的數」題目，狀態其實就是「目前對某個數取餘的結果」，而餘數只有有限種，所以遲早會循環。例如 [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k/) 要找最短的全 1 數（111…1）能被 K 整除，只要追蹤「目前餘數」即可：\n\n```cpp\n// 用餘數找答案：餘數只有 K 種，最多走 K 步必出結果或判定無解\nint rem = 0;\nfor (int len = 1; len <= k; len++) {\n  rem = (rem * 10 + 1) % k;  // 在末尾再接一個 1，更新餘數\n  if (rem == 0) {\n    return len;  // 整除了！長度就是答案\n  }\n}\nreturn -1;  // 走完 K 步沒歸零 → 永遠不可能\n```\n:::\n\n\n\n第三個是「先用小資料找規律、再公式化」，以及計數題常見的「容斥 / 排列組合 / 取模」。\n\n**實作方式**\n\n數學題沒有固定模板，但有固定的「攻法」：\n\n:::example 由規律直接構造：迴文數的後半 = 前半反過來貼上\n**1. 先用小資料手算，找出規律。** 拿 `n = 1, 2, 3` 親手算出答案，列出來找模式。例如 [找到指定長度的迴文數](https://leetcode.cn/problems/find-palindrome-with-fixed-length/)，觀察「第 k 個固定長度迴文數」其實由它的「前半段」直接決定，於是不必枚舉。\n\n```cpp\n// 由規律直接構造：迴文數的後半 = 前半反過來貼上\nstring half = to_string(base + index);  // 前半段\nstring s = half;\nreverse(half.begin(), half.end());\ns += half.substr(skipMiddle);  // 鏡像補上後半\n```\n:::\n\n\n\n**2. 把規律寫成公式或遞推。** 計數題尤其要先講清「在數什麼」，再決定要不要容斥、是否考慮排列順序。例如 [給邊賦權值的方案數 I](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-i/) 是計數模型，先拆清楚每條邊的選擇彼此獨立。\n\n:::example 取模：每一步乘法後立刻取模，避免 long long 也裝不下\n**3. 處理「邊界 / 溢位 / 取模 / 精度」。** 這是數學題最容易掉分的地方。\n\n```cpp\n// 取模：每一步乘法後立刻取模，避免 long long 也裝不下\nconst int MOD = 1e9 + 7;\nans = (long long)ans * base % MOD;\n```\n:::\n\n\n\n**4. 用「根號枚舉 / 預處理」避免逐一暴力。** 找因數只需枚舉到 `sqrt(n)`；多次詢問可先用篩法預處理。複雜度常見為 `O(sqrt(n))` 或 `O(n)`，遠勝暴力的 `O(n^2)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「數學」想：\n\n- **答案疑似有「閉式公式」或明顯規律，暴力枚舉會爆。** 像 [找到指定長度的迴文數](https://leetcode.cn/problems/find-palindrome-with-fixed-length/) 不可能真的把每個迴文數列出來找第 k 個，必須用構造。\n- **題目圍繞「整除、餘數、進位、進位制」打轉。** 如 [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k/)、[負二進位制數相加](https://leetcode.cn/problems/adding-two-negabinary-numbers/)：\n\n:::example 嗅到味道的瞬間：狀態其實只有「對某數取餘的結果」這有限幾種\n```cpp\n// 嗅到味道的瞬間：狀態其實只有「對某數取餘的結果」這有限幾種\n// 餘數會循環 → 不必真的枚舉大數，追蹤 rem 即可\n```\n:::\n\n\n\n- **問「有幾種方案」且數字很大要取模**，多半是組合計數（如 [給邊賦權值的方案數 I](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-i/)），考慮排列組合、容斥、模逆元。\n- **題目可以「先操作再求極值」且帶數論性質**（如 [執行操作後的最大 MEX](https://leetcode.cn/problems/smallest-missing-non-negative-integer-after-operations/) 牽涉同餘分類）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有先用 `n = 1, 2, 3` 手算、找出規律？\n2. 這題的核心是不是「餘數 / 進位制 / 整除」這類數論結構？能不能避免枚舉大數？\n3. 如果是計數題，我有沒有處理好「取模、排列順序、是否要容斥」？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1073 | [負二進位制數相加](https://leetcode.cn/problems/adding-two-negabinary-numbers/) | 1807 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2217 | [找到指定長度的迴文數](https://leetcode.cn/problems/find-palindrome-with-fixed-length/) | 1822 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3558 | [給邊賦權值的方案數 I](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-i/) | 1845 | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 2598 | [執行操作後的最大 MEX](https://leetcode.cn/problems/smallest-missing-non-negative-integer-after-operations/) | 1846 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1015 | [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k/) | 1875 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n public:\n  int countPrimes(int n) {\n    // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n    vector<char> isPrime(n, true);\n    int ans = 0;\n    for (int i = 2; i < n; ++i) {\n      if (!isPrime[i]) {\n        continue;\n      }\n      ans++;\n      // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n      if ((long long)i * i < n) {\n        for (long long j = 1LL * i * i; j < n; j += i) {\n          isPrime[j] = false;\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i*i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1073 | [負二進位制數相加](https://leetcode.cn/problems/adding-two-negabinary-numbers) | 1807 |\n| 2217 | [找到指定長度的迴文數](https://leetcode.cn/problems/find-palindrome-with-fixed-length) | 1822 |\n| 3558 | [給邊賦權值的方案數 I](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-i) | 1845 |\n| 2598 | [執行操作後的最大 MEX](https://leetcode.cn/problems/smallest-missing-non-negative-integer-after-operations) | 1846 |\n| 1015 | [可被 K 整除的最小整數](https://leetcode.cn/problems/smallest-integer-divisible-by-k) | 1875 |\n| 858 | [鏡面反射](https://leetcode.cn/problems/mirror-reflection) | 1881 |",
        },
        {
          id: 23,
          title: "2.9 單調堆疊",
          summary:
            "**原理講解**\n\n本節要解決的場景是：對陣列裡的每個元素，你想知道「它左邊（或右邊）第一個比它大（或小）的是誰」，或者要算「以某元素為最矮的矩形能多寬」。暴力做法是對每個元素往兩邊掃一遍找邊界，這是 `O(n^2)`。「單調堆疊（monotonic stack）」能把它壓到 `O(n)`。\n\n核心想法是：**用一個堆疊保存「還沒找到邊界的元素」，並讓堆疊裡的元素始終保持遞增（或遞減）。當新元素破壞了這個單調性，就代表它正是某些舊元素要找的邊界——趁這個瞬間把那些舊元素一次結算掉。**\n\n:::example 單調遞減棧：求每個元素「右邊第一個更大的值」\n為什麼會省時間？因為每個元素只會「進堆一次、出堆一次」，被彈出的當下它的邊界就確定了，不必再重掃。先看最經典的「下一個更大元素」：\n\n```cpp\n// 單調遞減棧：求每個元素「右邊第一個更大的值」\nstack<int> st;  // 存「下標」，棧底到棧頂對應值遞減\nvector<int> ans(n, -1);\nfor (int i = 0; i < n; i++) {\n  while (!st.empty() && a[i] > a[st.top()]) {\n    ans[st.top()] = a[i];  // a[i] 就是 st.top() 右邊第一個更大的\n    st.pop();              // 結算完，彈出\n  }\n  st.push(i);\n}\n```\n:::\n\n\n\n:::example 彈出瞬間，左右邊界同時確定（用於算寬度 / 貢獻）\n注意一個關鍵習慣：**棧裡存的是「下標」而不是「值」**。因為很多題（矩形寬度、子陣列長度）需要用「位置差」算貢獻，存下標才方便。彈出某個元素時：它「右邊的邊界」是當前的 `i`，它「左邊的邊界」是彈出後新的棧頂——左右邊界一次到手：\n\n```cpp\n// 彈出瞬間，左右邊界同時確定（用於算寬度 / 貢獻）\nint height = a[st.top()];\nst.pop();\nint left = st.empty() ? -1 : st.top();  // 左邊第一個破壞單調的\nint width = i - left - 1;               // 以 height 為高的矩形寬度\n```\n:::\n\n\n\n**實作方式**\n\n任何單調棧題都可拆成四步：\n\n:::example 狀態表示：棧存下標，並約定維持遞減（求下一個更大）\n**1. 決定棧的單調方向，以及存什麼。** 求「更大」用遞減棧、求「更小」用遞增棧；棧裡通常存下標。\n\n```cpp\n// 狀態表示：棧存下標，並約定維持遞減（求下一個更大）\nstack<int> st;  // a[st.bottom()] >= ... >= a[st.top()]\n```\n:::\n\n\n\n:::example 更新流程：破壞單調性就一路彈，彈出即結算\n**2. 掃描並維護單調性：新元素破壞單調就彈。** 每讀到一個新元素，就把所有「不再滿足單調」的棧頂彈出，彈出的當下結算它。\n\n```cpp\n// 更新流程：破壞單調性就一路彈，彈出即結算\nwhile (!st.empty() && breaksMonotonic(a[i], a[st.top()])) {\n  settle(st.top(), i);  // 結算被彈元素（記答案 / 累加貢獻）\n  st.pop();\n}\nst.push(i);\n```\n:::\n\n\n\n**3. 讀答案：在彈出時記錄。** 「下一個更大／更小」直接在彈出時寫入；矩形面積、子陣列數則用左右邊界算寬度後累加。例如 [統計全 1 子矩形](https://leetcode.cn/problems/count-submatrices-with-all-ones/) 把每列壓成高度後對每列跑單調棧累加貢獻。\n\n**4. 邊界與複雜度。**\n\n- 相等元素要小心用 `<` 還是 `<=`：它決定相等時要不要彈出，直接影響「去重」與「左右邊界歸誰」。**貢獻法常需一側嚴格、一側非嚴格**，否則同樣的子陣列會被重複數或漏數。\n- 可在陣列尾端補一個「哨兵」（極大或極小值）逼出棧裡剩下的元素，省去最後的清空程式。\n- 複雜度：每個元素進出棧各一次，整體 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「單調堆疊」想：\n\n- **要對每個元素找「左／右第一個更大或更小的位置」。** 像 [找出最具競爭力的子序列](https://leetcode.cn/problems/find-the-most-competitive-subsequence/)（用單調棧維護字典序最小的候選）、[將所有元素變為 0 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-convert-all-elements-to-zero/)。\n- **題目和「最近的更大／更小邊界」「矩形寬度」「以某元素為最值的子陣列數」有關。** 像 [統計全 1 子矩形](https://leetcode.cn/problems/count-submatrices-with-all-ones/)、[碗子陣列的數目](https://leetcode.cn/problems/count-bowl-subarrays/)：\n\n:::example 嗅到味道的瞬間：暴力要對每個 i 往兩邊掃找邊界 → O(n^2) 會超時\n```cpp\n// 嗅到味道的瞬間：暴力要對每個 i 往兩邊掃找邊界 → O(n^2) 會超時\n// 改用「進出各一次」的單調棧 → O(n)\n```\n:::\n\n\n\n- **暴力是「對每個元素向兩側掃描找邊界」，明顯會 `O(n^2)`。** 這正是單調棧把重複掃描攤平成 `O(n)` 的典型場合。\n- 看到「下一個更大」「最大矩形」「貢獻法」這類關鍵字，幾乎可確認。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的是「更大」還是「更小」邊界？所以棧該維持遞增還遞減？\n2. 棧裡存的是值還是下標？我後面需不需要用位置差算寬度？\n3. 相等元素時我該用 `<` 還 `<=`？會不會造成重複數或漏數？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1673 | [找出最具競爭力的子序列](https://leetcode.cn/problems/find-the-most-competitive-subsequence/) | 1802 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1504 | [統計全 1 子矩形](https://leetcode.cn/problems/count-submatrices-with-all-ones/) | 1845 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 3676 | [碗子陣列的數目](https://leetcode.cn/problems/count-bowl-subarrays/) | 1848 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 3542 | [將所有元素變為 0 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-convert-all-elements-to-zero/) | 1890 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n    vector<int> st;\n    for (int x : nums2) {\n      // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1673 | [找出最具競爭力的子序列](https://leetcode.cn/problems/find-the-most-competitive-subsequence) | 1802 |\n| 1504 | [統計全 1 子矩形](https://leetcode.cn/problems/count-submatrices-with-all-ones) | 1845 |\n| 3676 | [碗子陣列的數目](https://leetcode.cn/problems/count-bowl-subarrays) | 1848 |\n| 3542 | [將所有元素變為 0 的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-convert-all-elements-to-zero) | 1890 |",
        },
        {
          id: 24,
          title: "2.10 滑動視窗",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目要你在一個陣列或字串裡，找「連續的一段」（子陣列／子字串）滿足某條件，並問「最長／最短／有幾個」。暴力做法是枚舉所有起點和終點，是 `O(n^2)`。「滑動視窗（sliding window）」能把它壓到 `O(n)`。\n\n核心想法是：**用左右兩個指標框住一段連續區間（視窗），右指標不斷往右擴大視窗、把新元素納入；一旦視窗「違規」，就移動左指標縮小視窗、把舊元素移出。兩個指標都只往右走、不回頭，所以整體只掃一遍。**\n\n:::example 滑動視窗（最長型）：右擴納入新元素，違規就縮左端\n之所以能成立，靠的是一個關鍵性質：**單調性**——視窗變大時，「某個量」會單調變化。例如 [統計得分小於 K 的子陣列數目](https://leetcode.cn/problems/count-subarrays-with-score-less-than-k/)，視窗越長分數越大，所以「太大了就縮左端」是有道理的。看一個「最長」型的骨架：\n\n```cpp\n// 滑動視窗（最長型）：右擴納入新元素，違規就縮左端\nint left = 0, ans = 0;\nfor (int right = 0; right < n; right++) {\n  add(a[right]);             // 把新元素納入視窗\n  while (windowInvalid()) {  // 視窗違規（如超過上限）\n    remove(a[left]);         // 移出左端元素\n    left++;                  // 左指標右移，縮小視窗\n  }\n  ans = max(ans, right - left + 1);  // 此刻視窗合法，更新答案\n}\n```\n:::\n\n\n\n:::example 滑動視窗（計數型）：每個 right 貢獻 (right-left+1) 個合法子陣列\n另一種常見口味是「計數型」：問「有幾個合法子陣列」。技巧是：當右端固定、視窗合法時，「以 right 結尾的合法子陣列」共有 `right - left + 1` 個，把它累加即可：\n\n```cpp\n// 滑動視窗（計數型）：每個 right 貢獻 (right-left+1) 個合法子陣列\nadd(a[right]);\nwhile (windowInvalid()) {\n  remove(a[left]);\n  left++;\n}\nans += right - left + 1;  // 以 right 結尾的所有合法區間\n```\n:::\n\n\n\n**實作方式**\n\n任何滑動視窗題都可拆成四步：\n\n**1. 確認「合法性」隨視窗大小單調變化。** 這是能用滑動視窗的前提：視窗變大，違規程度只增不減（或反之）。確認不了就不能用，可能要改前綴和 + 雜湊等別的方法。\n\n:::example 狀態量：用一個計數表維護視窗內各元素出現次數\n**2. 設計視窗的「狀態量」與加入／移出操作。** 想清楚視窗裡要維護什麼：和、字元計數、不同元素個數……並寫好 `add` 與 `remove`。\n\n```cpp\n// 狀態量：用一個計數表維護視窗內各元素出現次數\nunordered_map<int, int> cnt;\nauto add = [&](int x) { cnt[x]++; };\nauto remove = [&](int x) {\n  if (--cnt[x] == 0) {\n    cnt.erase(x);\n  }\n};\n```\n:::\n\n\n\n:::example 最短型：合法時持續縮左端，逼出最小長度\n**3. 決定「何時縮左端」與「何時更新答案」。**「最長」型在違規時縮、合法時更新最大長度；「最短」型則在合法時盡量縮、同時更新最小長度。例如 [將 x 減到 0 的最小運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-x-to-zero/) 可轉成「找和為定值的最長中間段」。\n\n```cpp\n// 最短型：合法時持續縮左端，逼出最小長度\nwhile (windowValid()) {\n  ans = min(ans, right - left + 1);\n  remove(a[left]);\n  left++;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 計數題若要求「恰好 K 個」不同元素，常用「至多 K 個」減「至多 K-1 個」的差（如 [統計好子陣列的數目](https://leetcode.cn/problems/count-the-number-of-good-subarrays/) 的對偶思路），因為「至多」才滿足單調性。\n- 含負數時和不再單調遞增，普通滑動視窗會失效，要另尋他法。\n- 複雜度：左右指標各最多走 n 步，整體 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「滑動視窗」想：\n\n- **要找「連續」的子陣列／子字串，問最長、最短或數量。** 像 [統計得分小於 K 的子陣列數目](https://leetcode.cn/problems/count-subarrays-with-score-less-than-k/)、[最高頻元素的頻數](https://leetcode.cn/problems/frequency-of-the-most-frequent-element/)（排序後用視窗維護「把區間補成同值的成本」）。\n- **條件具單調性：視窗越長越容易違規（或越容易合法）。** 這保證「擴右、縮左」走一遍就夠：\n\n:::example 嗅到味道的瞬間：右端右移時，視窗的「違規程度」只會單調往一個方向變\n```cpp\n// 嗅到味道的瞬間：右端右移時，視窗的「違規程度」只會單調往一個方向變\n// → 左端永遠不必回頭 → 兩指標各掃一次 → O(n)\n```\n:::\n\n\n\n- **暴力是枚舉所有 (left, right) 區間的 `O(n^2)`，明顯會超時。**\n- 變形如 [替換子串得到平衡字串](https://leetcode.cn/problems/replace-the-substring-for-balanced-string/)，把問題轉成「找一個最短視窗，使視窗外的字元仍能湊成平衡」，本質仍是滑動視窗。\n\n確認自己有沒有套對，問三個問題：\n\n1. 題目要的是「連續」的一段嗎？合法性會隨視窗變大而單調變化嗎？\n2. 視窗內要維護什麼狀態量？我的 `add` 和 `remove` 怎麼寫？\n3. 這是「最長」「最短」還是「計數」型？我該在什麼時機縮左端、什麼時機更新答案？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2302 | [統計得分小於 K 的子陣列數目](https://leetcode.cn/problems/count-subarrays-with-score-less-than-k/) | 1808 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 1658 | [將 x 減到 0 的最小運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-x-to-zero/) | 1817 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 1838 | [最高頻元素的頻數](https://leetcode.cn/problems/frequency-of-the-most-frequent-element/) | 1876 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 1234 | [替換子串得到平衡字串](https://leetcode.cn/problems/replace-the-substring-for-balanced-string/) | 1878 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2537 | [統計好子陣列的數目](https://leetcode.cn/problems/count-the-number-of-good-subarrays/) | 1892 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 3. 無重複字元的最長子串 展示核心寫法。\n\n**完整題目**：給定字串 `s`，回傳不含重複字元的最長子字串長度。\n\n:::example 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass Solution {\n public:\n  int lengthOfLongestSubstring(string s) {\n    // 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n    vector<int> cnt(256);\n    int ans = 0, left = 0;\n    for (int right = 0; right < (int)s.size(); ++right) {\n      cnt[s[right]]++;\n      // 若新字元造成重複，縮小左邊界直到視窗內無重複\n      while (cnt[s[right]] > 1) {\n        cnt[s[left++]]--;\n      }\n      ans = max(ans, right - left + 1);  // 更新無重複子字串最大長度\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 右端加入新元素後，只在違反條件時移動左端；因此每個位置最多進出視窗一次，總複雜度為 O(n)。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n**常見變形**\n\n- 越短越合法：違法時收縮，更新最長。\n- 越長越合法：合法時收縮，更新最短。\n- 恰好型可轉成 `atMost(k)-atMost(k-1)`。\n\n**實作注意事項**\n\n- 先判斷合法性方向，再寫 while 條件。\n- 答案計數時常用 `right-left+1` 或 `left`，兩者語意不同。\n- 頻次表字元集可用陣列，值域大時用 map。\n- 含負數時滑窗的單調性可能失效。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2302 | [統計得分小於 K 的子陣列數目](https://leetcode.cn/problems/count-subarrays-with-score-less-than-k) | 1808 |\n| 1658 | [將 x 減到 0 的最小運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-x-to-zero) | 1817 |\n| 1838 | [最高頻元素的頻數](https://leetcode.cn/problems/frequency-of-the-most-frequent-element) | 1876 |\n| 1234 | [替換子串得到平衡字串](https://leetcode.cn/problems/replace-the-substring-for-balanced-string) | 1878 |\n| 2537 | [統計好子陣列的數目](https://leetcode.cn/problems/count-the-number-of-good-subarrays) | 1892 |",
        },
        {
          id: 25,
          title: "2.11 字串",
          summary:
            "**原理講解**\n\n本節的主題是「字串比對」。最容易踩到的坑是：當你想判斷「兩個字串是否相等」或「某段子字串有沒有在別處出現過」，直覺會一個字元一個字元慢慢比；一旦字串很長、又要比很多次，這種重複比對就會慢到爆。\n\n這節要學的核心想法是：**先把已經比對過的資訊濃縮成一個可以重複利用的狀態，之後就不必再從頭比一次。** 兩個最常用的工具是「字串雜湊（hashing）」和「前綴函數（border / 失配指標）」。\n\n:::example 多項式雜湊：預處理前綴雜湊與冪次\n先看雜湊。它把一段字串壓成一個數字（像指紋）：只要兩段字串的指紋不同，它們一定不同；指紋相同，幾乎可以當作相同。關鍵在於可以用「前綴和」的方式 `O(1)` 算出任一子字串的指紋：\n\n```cpp\n// 多項式雜湊：預處理前綴雜湊與冪次\nconst long long B = 131, MOD = 1e9 + 7;\nvector<long long> h(n + 1, 0), p(n + 1, 1);\nfor (int i = 0; i < n; i++) {\n  h[i + 1] = (h[i] * B + s[i]) % MOD;  // 前綴指紋\n  p[i + 1] = p[i] * B % MOD;           // B 的冪次\n}\n```\n:::\n\n\n\n:::example 取出子字串 [l, r] 的雜湊（左閉右閉）\n有了上面這兩個陣列，任一段 `s[l..r]` 的指紋就能直接算出來，比對兩段是否相等變成 `O(1)`：\n\n```cpp\n// 取出子字串 [l, r] 的雜湊（左閉右閉）\nauto sub = [&](int l, int r) { return ((h[r + 1] - h[l] * p[r - l + 1]) % MOD + MOD) % MOD; };\n```\n:::\n\n\n\n像「不同的迴圈子字串」（1316）就是要判斷某段是不是由前半段重複兩次組成，用 `sub(l, mid) == sub(mid + 1, r)` 一次比對就完成，整體 `O(n^2)`。\n\n另一個工具是「前綴函數」。對字串每個位置，記錄「最長的、同時是前綴又是後綴的長度（border）」。它能讓你在比對失敗時，不用退回開頭，而是跳到下一個還有機會的位置。「最長快樂字首」（1392）要找最長的非整串 border，正是前綴函數的直接答案。\n\n**實作方式**\n\n**1. 先想清楚「要重複比什麼」。** 把問題化成「反覆判斷某兩段是否相等／某段是否為某段的前後綴」。若是「相等比對」就走雜湊；若是「前後綴 / 失配跳轉」就走前綴函數。\n\n:::example 判斷兩段子字串是否相等\n**2. 雜湊：預處理一次，之後查詢都 `O(1)`。** 上面的 `h`、`p` 只算一次。要比兩段是否相等，比它們的 `sub` 即可：\n\n```cpp\n// 判斷兩段子字串是否相等\nbool equal(int l1, int r1, int l2, int r2) {\n  if (r1 - l1 != r2 - l2) {\n    return false;  // 長度不同直接否\n  }\n  return sub(l1, r1) == sub(l2, r2);  // 指紋相同視為相等\n}\n```\n:::\n\n\n\n:::example 前綴函數（KMP 的核心）\n**3. 前綴函數：一次線性掃描建出 border 陣列。** `pi[i]` 表示 `s[0..i]` 最長的 border 長度：\n\n```cpp\n// 前綴函數（KMP 的核心）\nvector<int> pi(n, 0);\nfor (int i = 1; i < n; i++) {\n  int j = pi[i - 1];\n  while (j && s[i] != s[j]) {\n    j = pi[j - 1];  // 失配就往回跳\n  }\n  if (s[i] == s[j]) {\n    j++;\n  }\n  pi[i] = j;\n}\n```\n:::\n\n\n\n**4. 答案讀取。** 雜湊題：在掃描或二分過程中比對指紋取結果。前綴函數題：`pi[n-1]` 就是整串的最長 border（即「最長快樂字首」的答案）。\n\n**5. 邊界與複雜度。** 雜湊要選大質數模數、最好用雙雜湊避免碰撞；減法後記得 `+ MOD` 再取模。前綴函數是 `O(n)`，雜湊預處理 `O(n)`、單次比對 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「字串比對」想：\n\n- **題目反覆問「這兩段一不一樣」或「這段有沒有出現過」**，而且字串夠長，一格一格比會超時——這是雜湊的招牌場景，例如「不同的迴圈子字串」（1316）要判斷前後兩半是否相同。\n- **題目和「前綴同時也是後綴」有關**，例如「最長快樂字首」（1392）要找最長的非整串 border，直接用前綴函數。\n- **要在一段文字裡找很多個模式串的位置**，例如「多次搜尋」（1001），可以對每個模式串做比對或建立 Trie/自動機。\n- **要比較子字串的字典序大小**，例如「按字典序排在最後的子串」（1163），可用雜湊加二分快速比較兩段的大小關係。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我重複在比的，是「兩段相等」還是「前後綴關係」？\n2. 如果用雜湊，我的前綴指紋和冪次陣列準備好了嗎？單次比對真的是 `O(1)` 嗎？\n3. 我有沒有處理碰撞（雙雜湊）和取模後變負數的邊界？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1316 | [不同的迴圈子字串](https://leetcode.cn/problems/distinct-echo-substrings/) | 1837 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 2800 | [包含三個字串的最短字串](https://leetcode.cn/problems/shortest-string-that-contains-three-strings/) | 1856 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1163 | [按字典序排在最後的子串](https://leetcode.cn/problems/last-substring-in-lexicographical-order/) | 1864 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1001 | [面試題 17.17. 多次搜尋](https://leetcode.cn/problems/grid-illumination/) | 1873 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n| 1392 | [最長快樂字首](https://leetcode.cn/problems/longest-happy-prefix/) | 1876 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\n public:\n  // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n  int strStr(string haystack, string needle) {\n    int m = needle.size();\n    vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n    // 建構 pi 陣列\n    for (int i = 1; i < m; ++i) {\n      int j = pi[i - 1];\n      // 若失配，利用 pi 回退到上一個可能匹配的位置\n      while (j && needle[i] != needle[j]) {\n        j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n      }\n      if (needle[i] == needle[j]) {\n        j++;\n      }\n      pi[i] = j;\n    }\n    // 在 haystack 中用 pi 輔助匹配\n    for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n      while (j && haystack[i] != needle[j]) {\n        j = pi[j - 1];\n      }\n      if (haystack[i] == needle[j]) {\n        j++;\n      }\n      if (j == m) {  // 完整匹配，返回起始位置\n        return i - m + 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n**常見變形**\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n**實作注意事項**\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1316 | [不同的迴圈子字串](https://leetcode.cn/problems//distinct-echo-substrings/) | 1837 |\n| 2800 | [包含三個字串的最短字串](https://leetcode.cn/problems//shortest-string-that-contains-three-strings/) | 1856 |\n| 1163 | [按字典序排在最後的子串](https://leetcode.cn/problems//last-substring-in-lexicographical-order/) | 1864 |\n| 1001 | [面試題 17.17. 多次搜尋](https://leetcode.cn/problems//multi-search-lcci/) | 1873 |\n| 1392 | [最長快樂字首](https://leetcode.cn/problems//longest-happy-prefix/) | 1876 |",
        },
        {
          id: 26,
          title: "2.12 樹和二元樹",
          summary:
            "**原理講解**\n\n本節的主題是「樹和二元樹上的遞迴」。樹是一種「一個根，往下分岔、沒有環」的結構；二元樹則是每個節點最多兩個子節點（左、右）。處理樹的問題，幾乎都靠同一招：**遞迴（DFS）——先把「整棵樹」的問題，拆成「處理當前節點 + 兩棵子樹的同樣問題」。**\n\n為什麼這樣可行？因為子樹本身也是一棵樹，結構跟原問題一模一樣，所以可以用同一個函數去解。這就是「分治」的精神：相信遞迴函數對子樹會給出正確答案，你只要想清楚「拿到左右子樹的結果後，怎麼合出當前節點的答案」。\n\n:::example 樹 DFS 的骨架：先處理子樹，再合併\n最基本的骨架長這樣：\n\n```cpp\n// 樹 DFS 的骨架：先處理子樹，再合併\nint dfs(TreeNode* node) {\n  if (!node) {\n    return 0;  // 邊界：空節點\n  }\n  int L = dfs(node->left);   // 相信它算對左子樹\n  int R = dfs(node->right);  // 相信它算對右子樹\n  return /* 用 L、R 與 node->val 合成當前答案 */;\n}\n```\n:::\n\n\n\n舉例：「子樹中標籤相同的節點數」（1519），就是讓 `dfs` 回傳「以當前節點為根的子樹裡，各標籤各出現幾次」，父節點把左右子樹的計數加起來、再加上自己，就得到自己子樹的統計。每個節點只被走一次，整體 `O(n)`。\n\n有些題目要的是「從根到某節點的路徑」，這時在 DFS 過程中順手記下走過的路徑即可。「二叉樹節點間的方向」（2096）就是分別求出根到起點、根到終點的路徑，再去掉共同前綴。\n\n**實作方式**\n\n**1. 想清楚 `dfs` 回傳什麼。** 這是樹題最關鍵的一步。回傳值通常是「以當前節點為根的子樹」的某個彙總量：高度、節點數、某種計數、是否合法等。\n\n**2. 寫好邊界。** 空節點要回傳「不影響合併的中性值」（求和回傳 0、求高度回傳 0、求最小值回傳極大值）。\n\n:::example 後序：用左右子樹的回傳值決定當前節點\n**3. 後序合併：先遞迴子樹，再算自己。** 大多數樹題是「後序」——先拿到左右結果，再決定當前節點：\n\n```cpp\n// 後序：用左右子樹的回傳值決定當前節點\nauto dfs = [&](auto&& self, TreeNode* node) -> int {\n  if (!node) {\n    return 0;\n  }\n  int L = self(self, node->left);\n  int R = self(self, node->right);\n  return max(L, R) + 1;  // 例：求樹高\n};\n```\n:::\n\n\n\n:::example 在 DFS 中維護根到目前節點的路徑\n**4. 需要路徑時，用一個可變的 path 邊走邊回溯。** 進入節點時記錄、離開時還原（恢復現場）：\n\n```cpp\n// 在 DFS 中維護根到目前節點的路徑\nvoid dfs(TreeNode* node, string& path) {\n  if (!node) {\n    return;\n  }\n  path.push_back('X');  // 進入：記錄一步\n  dfs(node->left, path);\n  dfs(node->right, path);\n  path.pop_back();  // 離開：還原（回溯）\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。** 每個節點各訪問一次，時間 `O(n)`；遞迴深度等於樹高，最壞（鏈狀樹）`O(n)`，平衡時 `O(log n)`。注意極深的樹可能爆遞迴堆疊。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「樹 DFS」想：\n\n- **輸入是一棵二元樹或一般樹**（給 `root`，或給邊建出鄰接表），要對「每個子樹」算某個量——例如「子樹中標籤相同的節點數」（1519）。\n- **答案可以由「當前節點 + 左右子樹的答案」合成**：這就是後序遞迴的訊號，例如「根到葉路徑上的不足節點」（1080）要根據子樹是否全被刪來決定當前節點去留。\n- **問的是「從某節點到某節點的路徑 / 方向」**，例如「節點間的方向」（2096），用 DFS 記錄根到目標的路徑。\n- **一般樹上要做擴散 / 計步**（例如「T 秒後青蛙的位置」1377），先用邊建鄰接表，再 DFS/BFS 從根往下走。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `dfs` 對「一棵子樹」回傳什麼？這個回傳值夠不夠父節點合出答案？\n2. 空節點的回傳值是什麼？它會不會破壞合併？\n3. 我需要的是「後序合併子樹結果」，還是「邊走邊記錄路徑」？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2096 | [從二叉樹一個節點到另一個節點每一步的方向](https://leetcode.cn/problems/step-by-step-directions-from-a-binary-tree-node-to-another/) | 1805 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1080 | [根到葉路徑上的不足節點](https://leetcode.cn/problems/insufficient-nodes-in-root-to-leaf-paths/) | 1805 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1519 | [子樹中標籤相同的節點數](https://leetcode.cn/problems/number-of-nodes-in-the-sub-tree-with-the-same-label/) | 1809 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1377 | [T 秒後青蛙的位置](https://leetcode.cn/problems/frog-position-after-t-seconds/) | 1824 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2212 | [射箭比賽中的最大得分](https://leetcode.cn/problems/maximum-points-in-an-archery-competition/) | 1869 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2096 | [從二叉樹一個節點到另一個節點每一步的方向](https://leetcode.cn/problems//step-by-step-directions-from-a-binary-tree-node-to-another/) | 1805 |\n| 1080 | [根到葉路徑上的不足節點](https://leetcode.cn/problems//insufficient-nodes-in-root-to-leaf-paths/) | 1805 |\n| 1519 | [子樹中標籤相同的節點數](https://leetcode.cn/problems//number-of-nodes-in-the-sub-tree-with-the-same-label/) | 1809 |\n| 1377 | [T 秒後青蛙的位置](https://leetcode.cn/problems//frog-position-after-t-seconds/) | 1824 |\n| 2212 | [射箭比賽中的最大得分](https://leetcode.cn/problems//maximum-points-in-an-archery-competition/) | 1869 |\n| 1255 | [得分最高的單詞集合](https://leetcode.cn/problems//maximum-score-words-formed-by-letters/) | 1882 |",
        },
      ],
    },
    {
      id: 27,
      title: "3. Phase 3：建立 2000 分解題節奏（1900-2000）",
      summary:
        "目標是把 Q3 後段和 Q4 入門題變成可攻克題。練習時要完整寫出複雜度證明，並用小範圍暴力或手算樣例驗證貪心、DP 轉移與資料結構維護。",
      children: [
        {
          id: 28,
          title: "3.1 二分搜尋",
          summary:
            "**原理講解**\n\n本節的主題是「二分搜尋答案（binary search on answer）」。它跟「在排好序的陣列裡找一個數」不一樣，更強大：當答案是一個數字、而且具有「單調性」時，我們可以直接對「答案本身」做二分。\n\n什麼叫單調性？想像答案有一個分界線：小於等於某個值都「可行」，大於它都「不可行」（或反過來）。例如「兩球之間的磁力」（1552）問「相鄰兩球最小距離最大能是多少」：距離要求 `d` 越小越容易塞下所有球，越大越難——這就是一條「可行 / 不可行」的分界。我們要找的就是這條分界上「最後一個可行」或「第一個可行」的值。\n\n:::example check(x)：答案取 x 時是否可行（單調：true 一段、false 一段）\n核心想法：**與其逐一嘗試每個候選答案，不如每次砍掉一半。** 只要能寫出一個 `check(x)` 函數回答「答案取 `x` 時可不可行」，就能用 `O(log)` 次嘗試逼出分界。\n\n```cpp\n// check(x)：答案取 x 時是否可行（單調：true 一段、false 一段）\nbool check(int x) {\n  // ... 根據題意判斷，回傳 true / false\n}\n```\n:::\n\n\n\n:::example 找「最後一個讓 check 為 true」的 x\n二分的骨架（這裡示範「找最後一個可行值」，可行在左側）：\n\n```cpp\n// 找「最後一個讓 check 為 true」的 x\nint lo = L, hi = R, ans = L;\nwhile (lo <= hi) {\n  int mid = lo + (hi - lo) / 2;  // 防溢位的取中點\n  if (check(mid)) {\n    ans = mid;\n    lo = mid + 1;\n  }  // 可行 → 往大的試\n  else {\n    hi = mid - 1;  // 不可行 → 往小的縮\n  }\n}\n// ans 即為最後一個可行值\n```\n:::\n\n\n\n只要 `check` 正確且具單調性，這個迴圈一定收斂到分界。整體複雜度是 `O(log(值域) × check 的成本)`。\n\n**實作方式**\n\n**1. 定義候選值 `x` 的語意。** 先說清楚「`x` 代表什麼」。例如 1552 的 `x` = 相鄰兩球的最小距離；「修車的最少時間」（2594）的 `x` = 給定的總時間。這一步定錯，後面全錯。\n\n:::example check：相鄰距離至少 d 時，能否放下 m 顆球\n**2. 寫 `check(x)` 並確認單調。** `check` 把「答案是 `x`」翻譯成一個可以貪心或計數驗證的判斷。以 1552 為例：座標排序後，貪心地從左往右放球，看在「相鄰距離至少 `x`」的條件下能不能放滿 `m` 顆：\n\n```cpp\n// check：相鄰距離至少 d 時，能否放下 m 顆球\nbool canPlace(vector<int>& pos, int m, int d) {\n  int cnt = 1, last = pos[0];\n  for (int i = 1; i < pos.size(); i++) {\n    if (pos[i] - last >= d) {\n      cnt++;\n      last = pos[i];\n    }\n  }\n  return cnt >= m;  // 放得下 m 顆就可行\n}\n```\n:::\n\n\n\n:::example 找「第一個讓 check 為 true」的 x（可行在右側）\n**3. 定好二分邊界與「找哪一端」。** 想清楚要的是「第一個可行」還是「最後一個可行」。1552 是距離越大越好、且 `d` 小才可行，要找**最後一個可行**；2594 是時間越大越可行，要找**第一個可行**（最少時間）。\n\n```cpp\n// 找「第一個讓 check 為 true」的 x（可行在右側）\nint lo = L, hi = R, ans = R;\nwhile (lo <= hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    ans = mid;\n    hi = mid - 1;\n  }  // 可行 → 往小的試\n  else {\n    lo = mid + 1;  // 不可行 → 往大的擴\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 取中點用 `lo + (hi - lo) / 2` 避免溢位；邊界要含蓋真正的最小可行與最大可行值。複雜度 = `O(log(值域))` 次 `check`，每次 `check` 通常 `O(n)`，合計 `O(n log(值域))`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「二分答案」想：\n\n- **題目在問「最大化某個最小值」或「最小化某個最大值」**，例如 1552「最小磁力的最大值」、2594「修車的最少時間」——這是二分答案最經典的訊號。\n- **答案具有單調性**：假設答案是 `x`，「可行 / 不可行」隨 `x` 增大只切換一次。若你能說出「`x` 越大越容易（或越難）」，就有單調性。\n- **直接求答案很難，但「給定一個答案、驗證可不可行」很簡單**（通常用貪心或計數一次 `O(n)` 掃完），例如 1898「可移除字元的最大數目」中，移除前 `k` 個後驗證子序列是否還在。\n- **答案是一個落在已知範圍內的整數**（距離、時間、長度、某個下標的值），讓你有明確的 `lo`、`hi` 可二分，例如 1802「有界陣列指定下標的最大值」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的候選值 `x` 代表的具體量是什麼？\n2. 我的 `check(x)` 是不是單調？是「真在左、假在右」還是相反？\n3. 我要找的是「第一個可行」還是「最後一個可行」？邊界與更新方向對應上了嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1898 | [可移除字元的最大數目](https://leetcode.cn/problems/maximum-number-of-removable-characters/) | 1913 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2594 | [修車的最少時間](https://leetcode.cn/problems/minimum-time-to-repair-cars/) | 1915 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1552 | [兩球之間的磁力](https://leetcode.cn/problems/magnetic-force-between-two-balls/) | 1920 | 排序座標後，判斷能否放下 `m` 顆球且相鄰距離至少為 `d`；`d` 越小越容易，答案要找最後一個可行值。 |\n| 1802 | [有界陣列中指定下標處的最大值](https://leetcode.cn/problems/maximum-value-at-a-given-index-in-a-bounded-array/) | 1929 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1818 | [絕對差值和](https://leetcode.cn/problems/minimum-absolute-sum-difference/) | 1934 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n:::example 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  // 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;  // 閉區間 [l, r]\n    while (l <= r) {\n      int mid = l + (r - l) / 2;  // 取中點，避免 l+r 溢位\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;  // 目標在右半段\n      } else {\n        r = mid - 1;  // 目標在左半段\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1898 | [可移除字元的最大數目](https://leetcode.cn/problems/maximum-number-of-removable-characters) | 1913 |\n| 2594 | [修車的最少時間](https://leetcode.cn/problems/minimum-time-to-repair-cars) | 1915 |\n| 1552 | [兩球之間的磁力](https://leetcode.cn/problems/magnetic-force-between-two-balls) | 1920 |\n| 1802 | [有界陣列中指定下標處的最大值](https://leetcode.cn/problems/maximum-value-at-a-given-index-in-a-bounded-array) | 1929 |\n| 1818 | [絕對差值和](https://leetcode.cn/problems/minimum-absolute-sum-difference) | 1934 |\n| 1760 | [袋子裡最少數目的球](https://leetcode.cn/problems/minimum-limit-of-balls-in-a-bag) | 1940 |\n| 1482 | [製作 m 束花所需的最少天數](https://leetcode.cn/problems/minimum-number-of-days-to-make-m-bouquets) | 1946 |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort) | 1948 |\n| 1642 | [可以到達的最遠建築](https://leetcode.cn/problems/furthest-building-you-can-reach) | 1962 |\n| 2439 | [最小化陣列中的最大值](https://leetcode.cn/problems/minimize-maximum-of-array) | 1965 |\n| 3733 | [完成所有送貨任務的最少時間](https://leetcode.cn/problems/minimum-time-to-complete-all-deliveries) | 1973 |\n| 2861 | [最大合金數](https://leetcode.cn/problems/maximum-number-of-alloys) | 1981 |\n| 3620 | [恢復網路路徑](https://leetcode.cn/problems/network-recovery-pathways) | 1998 |",
        },
        {
          id: 29,
          title: "3.2 位元運算",
          summary:
            "**原理講解**\n\n本節的主題是「位元運算（bitwise）」。整數在電腦裡是用二進位存的，例如 `13` 就是 `1101`。很多看起來很難的題目，只要**把整數拆成一位一位來看**，就會發現每一位之間其實互不干擾，可以分開處理或逐位貪心。\n\n:::example 三個基本位元運算（對每一位獨立作用）\n先複習三個位元運算的本質（把兩個數對齊，逐位比較）：\n\n```cpp\n// 三個基本位元運算（對每一位獨立作用）\nint a = 0b1100, b = 0b1010;\nint x1 = a & b;  // AND：兩位都是 1 才是 1  -> 1000\nint x2 = a | b;  // OR ：有一位是 1 就是 1  -> 1110\nint x3 = a ^ b;  // XOR：兩位不同才是 1     -> 0110\n```\n:::\n\n\n\n關鍵觀察：**這些運算在每一位上是各自獨立計算的**——第 3 位的結果只跟兩個數的第 3 位有關。所以「整體的條件」常常可以拆成「每一位各自的條件」加總。例如要算一堆數兩兩 XOR 的某種總和，可以對每一位分別統計有幾個 0、幾個 1，再算這一位對答案的貢獻。\n\n:::example 從高位往低位列舉每一位（最高位最重要）\n第二個常用手法是「從高位到低位貪心」。因為高位的權值遠大於所有低位之和（`1000` 比 `0111` 大），要讓結果最大，就先盡力把最高位變成 1，再依序處理低位：\n\n```cpp\n// 從高位往低位列舉每一位（最高位最重要）\nfor (int b = 30; b >= 0; b--) {\n  int bit = (x >> b) & 1;  // 取出 x 的第 b 位\n                           // ... 依題意決定這一位要不要 / 能不能設成 1\n}\n```\n:::\n\n\n\n像「最大或值」（2680）就是用 OR 的「位只會多不會少」特性貪心；「最小化陣列最後一個元素」（3133）則是逐位安排哪些位該填。\n\n**實作方式**\n\n**1. 先決定「拆位的方向」。** 若是 XOR/AND/OR 的計數題，通常是「對每一位統計」；若是求最大 / 最小值，通常是「從高位往低位貪心試填」。\n\n:::example 取第 b 位 / 把第 b 位設為 1 / 清為 0\n**2. 取位、設位、清位的基本操作要熟。** 這是所有位元題的零件：\n\n```cpp\n// 取第 b 位 / 把第 b 位設為 1 / 清為 0\nint get = (x >> b) & 1;\nint setb = x | (1 << b);\nint clr = x & ~(1 << b);\n```\n:::\n\n\n\n:::example 統計每一位有幾個 1（之後可換算成貢獻）\n**3. 逐位統計型：對每一位累加貢獻。** 例如統計所有數在第 `b` 位是 1 的個數，再乘上該位的權值 `1 << b`：\n\n```cpp\n// 統計每一位有幾個 1（之後可換算成貢獻）\nint cnt[32] = {0};\nfor (int v : nums) {\n  for (int b = 0; b < 32; b++) {\n    if ((v >> b) & 1) {\n      cnt[b]++;\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 高位貪心型：能讓高位變好就先讓它變好。** 從第 30 位往下，嘗試把該位設成有利的值，若整體仍可行就保留。OR 題（2680）尤其直觀：把次數用在最高的位上，因為高位權值最大。\n\n**5. 邊界與複雜度。** 位移前注意型別寬度：可能用到第 31 位時 `1 << 31` 在 `int` 會溢位，要寫成 `1LL << 31` 或用 `unsigned`；處理 64 位數要用 `long long`。複雜度通常是 `O(n × 位數)`，位數約 30～60，可視為線性。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「位元運算」想：\n\n- **題目直接出現 XOR / AND / OR，或要在二進位下做決策**，例如「最大或值」（2680）、「按位或最大的最小子陣列」（2411）。\n- **發現「每一位互不影響、可以分開算」**：把問題從「對整個數字」降成「對每一位」，例如統計每位的 0/1 個數來求總貢獻。\n- **要最大化 / 最小化一個整數結果**，且高位明顯比低位重要——這是「從高位往低位貪心」的訊號，例如「最小化陣列最後一個元素」（3133）。\n- **題目和二進位的結構性質有關**，例如「查詢樹中環的長度」（2509）其實是把節點編號當成二進位路徑，用位元找最近公共祖先。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把「整體條件」拆成「每一位各自的條件」？\n2. 我要的是「逐位統計貢獻」還是「從高位往低位貪心」？\n3. 有沒有可能碰到第 31 / 63 位？我用對 `1LL` / `unsigned` / `long long` 了嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2680 | [最大或值](https://leetcode.cn/problems/maximum-or/) | 1912 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2654 | [使陣列所有元素變成 1 的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-all-array-elements-equal-to-1/) | 1929 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 3133 | [陣列最後一個元素的最小值](https://leetcode.cn/problems/minimum-array-end/) | 1935 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2411 | [按位或最大的最小子陣列長度](https://leetcode.cn/problems/smallest-subarrays-with-maximum-bitwise-or/) | 1938 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2509 | [查詢樹中環的長度](https://leetcode.cn/problems/cycle-length-queries-in-a-tree/) | 1948 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1310. 子陣列異或查詢 展示核心寫法。\n\n**完整題目**：給定陣列與多個 `[l, r]` 查詢，回傳每段子陣列的 XOR 值。\n\n:::example 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR…\n**線索**：題目出現 XOR/AND/OR、mask 或二進位；先判斷每一位是否能獨立統計或從高位貪心。\n\n```cpp\nclass Solution {\n public:\n  // 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR 值\n  vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {\n    vector<int> pre(arr.size() + 1);  // pre[i] 為 arr[0..i-1] 的 XOR 前綴\n    for (int i = 0; i < (int)arr.size(); ++i) {\n      pre[i + 1] = pre[i] ^ arr[i];\n    }\n    vector<int> ans;\n    for (auto& q : queries) {\n      ans.push_back(pre[q[1] + 1] ^ pre[q[0]]);  // 區間 [q[0], q[1]] 的 XOR\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- XOR 具有自反性 `x ^ x = 0`，因此區間 XOR 可用前綴 XOR 相消。\n- 程式把整數轉成位元狀態或前綴 XOR，利用位元運算的可逆性/獨立性更新答案。\n- 涉及高位貪心時，迴圈通常從最高位往低位試，因為高位決定主要大小關係。\n\n**常見變形**\n\n- 前綴 XOR 處理區間異或。\n- 試填最高位處理最大 XOR/最大值。\n- SOS DP 處理子集包含關係。\n\n**實作注意事項**\n\n- 位移用 `1LL << b` 避免溢位。\n- 負數右移與符號位要小心。\n- 按位貢獻要確認每位獨立。\n- `__builtin_popcountll` 對 long long 使用 ll 版本。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2680 | [最大或值](https://leetcode.cn/problems/maximum-or) | 1912 |\n| 2654 | [使陣列所有元素變成 1 的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-all-array-elements-equal-to-1) | 1929 |\n| 3133 | [陣列最後一個元素的最小值](https://leetcode.cn/problems/minimum-array-end) | 1935 |\n| 2411 | [按位或最大的最小子陣列長度](https://leetcode.cn/problems/smallest-subarrays-with-maximum-bitwise-or) | 1938 |\n| 2509 | [查詢樹中環的長度](https://leetcode.cn/problems/cycle-length-queries-in-a-tree) | 1948 |\n| 2564 | [子字串異或查詢](https://leetcode.cn/problems/substring-xor-queries) | 1959 |\n| 3690 | [拆分合並陣列](https://leetcode.cn/problems/split-and-merge-array-transformation) | 1982 |",
        },
        {
          id: 30,
          title: "3.3 資料結構",
          summary:
            "**原理講解**\n\n本節的主題是「前綴和搭配雜湊表」——一組專門對付「子陣列總和」類問題的資料結構技巧。看這節的例題（「表現良好的最長時間段」1124、「邊界與內部和相等的子陣列」3728）會發現，它們都在問「某段連續區間的和」滿足某個條件，而且暴力枚舉所有區間是 `O(n^2)`，太慢。\n\n:::example 前綴和：pre[i] = 前 i 個元素的總和\n第一個工具是「前綴和」。先算出「從頭到第 `i` 個元素的總和」`pre[i]`，那麼任一段 `[l, r]` 的和就能 `O(1)` 求出：\n\n```cpp\n// 前綴和：pre[i] = 前 i 個元素的總和\nvector<long long> pre(n + 1, 0);\nfor (int i = 0; i < n; i++) {\n  pre[i + 1] = pre[i] + a[i];\n}\n// 區間 [l, r] 的和 = pre[r + 1] - pre[l]\n```\n:::\n\n\n\n光有前綴和還是要枚舉 `l`、`r` 兩端，仍是 `O(n^2)`。第二個工具「雜湊表」才是關鍵：**很多區間條件可以改寫成「兩個前綴和之間的關係」，於是只要一邊掃一邊用 map 查「有沒有合適的前綴和出現過」。**\n\n:::example 子陣列和等於 k 的個數：邊掃邊查前綴和\n最經典的例子是「子陣列和等於 `k`」：`pre[r] - pre[l] = k`，等價於 `pre[l] = pre[r] - k`。所以掃到 `r` 時，只要問 map「`pre[r] - k` 之前出現過幾次」就好：\n\n```cpp\n// 子陣列和等於 k 的個數：邊掃邊查前綴和\nunordered_map<long long, int> cnt{{0, 1}};  // 空前綴 pre=0 算一次\nlong long pre = 0;\nint ans = 0;\nfor (int x : a) {\n  pre += x;\n  ans += cnt[pre - k];  // 想要的左端前綴出現過幾次\n  cnt[pre]++;           // 記錄當前前綴，供後面查\n}\n```\n:::\n\n\n\n這樣把 `O(n^2)` 降成 `O(n)`。「表現良好的最長時間段」（1124）也是同套路：把「勞累 / 不勞累」轉成 `+1 / -1`，問題變成「找最長的、和大於 0 的子陣列」，用前綴和加 map 記錄每個前綴第一次出現的位置即可。\n\n**實作方式**\n\n**1. 想清楚「要查的量會變、要快查的量是什麼」。** 這正是「資料結構題」的核心：哪些量隨掃描變化（前綴和），哪些量需要快速查詢（之前見過的前綴）。答案通常是：用前綴和當變化量，用雜湊表做快查。\n\n**2. 把區間條件改寫成「兩個前綴和的關係」。** 例如「和為 `k`」→ `pre[l] = pre[r] - k`；「和大於 0」→ 找更小的更早前綴。寫不出這個關係，就先回到暴力解，找出重複計算的部分。\n\n:::example 通用骨架：先查答案，再把當前前綴記進 map\n**3. 一邊掃一邊更新雜湊表。** 注意先查、後存，避免把當前自己算進去；並記得放入「空前綴」初始值：\n\n```cpp\n// 通用骨架：先查答案，再把當前前綴記進 map\nunordered_map<long long, int> seen{{0, 1}};\nlong long pre = 0;\nint ans = 0;\nfor (int x : a) {\n  pre += x;\n  // ans += 由 seen 查到的合適前綴數量\n  seen[pre]++;  // 後存：保證查的是「更早」的前綴\n}\n```\n:::\n\n\n\n:::example 求最長子陣列：只記前綴第一次出現的下標\n**4. 求「最長」時，map 存「第一次出現的位置」。** 計數題存次數；長度題要存下標，且只在第一次出現時記錄，這樣 `r - 位置` 才會最大：\n\n```cpp\n// 求最長子陣列：只記前綴第一次出現的下標\nunordered_map<long long, int> first;\nlong long pre = 0;\nint ans = 0;\nfor (int i = 0; i < n; i++) {\n  pre += a[i];\n  // 若需要的前綴在 first 裡，更新 ans = max(ans, i - first[...])\n  if (!first.count(pre)) {\n    first[pre] = i;  // 只記最早一次\n  }\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。** 前綴和可能很大，用 `long long` 防溢位；別忘了「空前綴 pre=0」這個初始項，否則「從頭開始的區間」會被漏掉。時間 `O(n)`，空間 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「前綴和 + 雜湊表」想：\n\n- **題目在問「連續子陣列」的總和滿足某條件**（和為 `k`、和大於 0、兩段和相等），例如 1124「最長表現良好時間段」、3728「邊界與內部和相等的子陣列」。\n- **暴力枚舉所有區間是 `O(n^2)`**，而你發現「條件能改寫成兩個前綴和的差或關係」——這就是用 map 把它降到 `O(n)` 的時機。\n- **要的是「個數」或「最長 / 最短長度」**：個數題 map 存次數，長度題 map 存「第一次出現的位置」。\n- **元素可正可負**（不能用滑動視窗），但和的差值有結構可查，例如把布林條件轉成 `+1 / -1` 後求子陣列和。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能把區間條件寫成「`pre[r]` 和某個 `pre[l]` 的關係」嗎？\n2. 我的雜湊表該存「次數」還是「第一次出現的位置」？\n3. 我有沒有放入空前綴的初始項、有沒有先查後存、有沒有用 `long long`？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2602 | [使陣列元素全部相等的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-all-array-elements-equal/) | 1903 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 3081 | [替換字串中的問號使分數最小](https://leetcode.cn/problems/replace-question-marks-in-string-to-minimize-its-value/) | 1905 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 1124 | [表現良好的最長時間段](https://leetcode.cn/problems/longest-well-performing-interval/) | 1908 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 3728 | [邊界與內部和相等的穩定子陣列](https://leetcode.cn/problems/stable-subarrays-with-equal-boundary-and-interior-sum/) | 1909 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 3067 | [在帶權樹網路中統計可連線伺服器對數目](https://leetcode.cn/problems/count-pairs-of-connectable-servers-in-a-weighted-tree-network/) | 1909 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n\n**C++ 範例講解**\n\n以下用 1124. 表現良好的最長時間段 展示資料結構（前綴和 + 雜湊表）的核心寫法。\n\n**完整題目**：給每日工作時數 hours。工時大於 8 的天記為「勞累日」。一段時間若勞累日數量嚴格多於非勞累日，稱為「表現良好時間段」。求最長表現良好時間段的長度。\n\n:::example 將每天轉化為 +1（勞累）或 -1（不勞累），問題轉為找最長正前綴和子陣列\n**線索**：把每天轉成 +1（勞累）或 −1，問題變成「最長且和大於 0 的子陣列」。用前綴和：若 `prefix > 0` 整段都好；否則找最早出現 `prefix - 1` 的位置，用雜湊表記每個前綴值首次出現的下標。\n\n```cpp\nclass Solution {\n public:\n  int longestWPI(vector<int>& hours) {\n    // 將每天轉化為 +1（勞累）或 -1（不勞累），問題轉為找最長正前綴和子陣列\n    // firstSeen 記錄各前綴和第一次出現的下標\n    unordered_map<int, int> firstSeen;\n    int prefix = 0, ans = 0;\n    for (int i = 0; i < (int)hours.size(); ++i) {\n      prefix += hours[i] > 8 ? 1 : -1;\n      if (prefix > 0) {\n        ans = i + 1;  // 從頭到 i 整段都是「表現良好」區間\n      } else {\n        // 若存在前綴和為 prefix-1 的位置 j，則 (j, i] 的和為 1 > 0\n        if (firstSeen.find(prefix - 1) != firstSeen.end()) {\n          ans = max(ans, i - firstSeen[prefix - 1]);\n        }\n        if (firstSeen.find(prefix) == firstSeen.end()) {\n          firstSeen[prefix] = i;  // 只記第一次出現，保證子陣列盡可能長\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 勞累日 +1、其餘 −1，前綴和大於 0 即代表該前綴整段表現良好。\n- 當 `prefix > 0`，從頭到 i 都是好時間段，長度為 `i + 1`。\n- 否則要找一個更早、前綴恰為 `prefix - 1` 的位置，兩者之間的和必為正。\n- 用 `firstSeen` 只記每個前綴值「首次」出現下標，才能取到最長區間。\n\n**常見變形**\n\n- 和大於 / 等於某值的最長子陣列：前綴和 + 單調結構。\n- 0 / 1 平衡子陣列（最長相等 0、1）：以 ±1 前綴配雜湊表。\n- 連續子陣列和達標問題的通用變形。\n\n**實作注意事項**\n\n- 只在 `prefix <= 0` 時才查與更新雜湊表，`prefix > 0` 直接結算更省事。\n- 雜湊表只存首次出現位置，後續同值不可覆蓋。\n- 查 `prefix - 1` 是因為和只要嚴格大於 0，差一即可形成正區間。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2602 | [使陣列元素全部相等的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-all-array-elements-equal) | 1903 |\n| 3081 | [替換字串中的問號使分數最小](https://leetcode.cn/problems/replace-question-marks-in-string-to-minimize-its-value) | 1905 |\n| 1124 | [表現良好的最長時間段](https://leetcode.cn/problems/longest-well-performing-interval) | 1908 |\n| 3728 | [邊界與內部和相等的穩定子陣列](https://leetcode.cn/problems/stable-subarrays-with-equal-boundary-and-interior-sum) | 1909 |\n| 3067 | [在帶權樹網路中統計可連線伺服器對數目](https://leetcode.cn/problems/count-pairs-of-connectable-servers-in-a-weighted-tree-network) | 1909 |\n| 2296 | [設計一個文字編輯器](https://leetcode.cn/problems/design-a-text-editor) | 1912 |\n| 3356 | [零陣列變換 II](https://leetcode.cn/problems/zero-array-transformation-ii) | 1913 |\n| 2398 | [預算內的最多機器人數目](https://leetcode.cn/problems/maximum-number-of-robots-within-budget) | 1917 |\n| 1562 | [查詢大小為 M 的最新分組](https://leetcode.cn/problems/find-latest-group-of-size-m) | 1928 |\n| 1705 | [喫蘋果的最大數目](https://leetcode.cn/problems/maximum-number-of-eaten-apples) | 1930 |\n| 2762 | [不間斷子陣列](https://leetcode.cn/problems/continuous-subarrays) | 1940 |\n| 3381 | [長度可被 K 整除的子陣列的最大元素和](https://leetcode.cn/problems/maximum-subarray-sum-with-length-divisible-by-k) | 1943 |\n| 2434 | [使用機器人列印字典序最小的字串](https://leetcode.cn/problems/using-a-robot-to-print-the-lexicographically-smallest-string) | 1953 |\n| 3756 | [連線非零數字並乘以其數字和 II](https://leetcode.cn/problems/concatenate-non-zero-digits-and-multiply-by-sum-ii) | 1968 |\n| 1943 | [描述繪畫結果](https://leetcode.cn/problems/describe-the-painting) | 1969 |\n| 1488 | [避免洪水氾濫](https://leetcode.cn/problems/avoid-flood-in-the-city) | 1974 |\n| 1882 | [使用伺服器處理任務](https://leetcode.cn/problems/process-tasks-using-servers) | 1979 |\n| 3695 | [交換元素後的最大交替和](https://leetcode.cn/problems/maximize-alternating-sum-using-swaps) | 1984 |\n| 3224 | [使差值相等的最少陣列改動次數](https://leetcode.cn/problems/minimum-array-changes-to-make-differences-equal) | 1996 |\n| 2250 | [統計包含每個點的矩形數目](https://leetcode.cn/problems/count-number-of-rectangles-containing-each-point) | 1998 |\n| 2488 | [統計中位數為 K 的子陣列](https://leetcode.cn/problems/count-subarrays-with-median-k) | 1999 |",
        },
        {
          id: 31,
          title: "3.4 動態規劃",
          summary:
            "**原理講解**\n\n本節的主題是「動態規劃（DP）」。它對付的是這種題目：要做一連串決策、求最大 / 最小 / 方法數，而且如果硬枚舉所有選法會指數爆炸。DP 的核心是：**很多子問題會被重複算到，我們把每個子問題只算一次、把答案存起來，後面要用直接查表。**\n\n關鍵的第一步永遠是「定義狀態」——也就是想清楚 `dp[i]`（或 `dp[i][j]`）到底代表什麼。狀態定義好，轉移式（怎麼從小狀態推大狀態）就會自然浮現。技巧是想「最後一步怎麼決策」：\n\n:::example dp[i]：以 a[i] 結尾的最長遞增子序列長度\n以「最長遞增子序列」為例（和 1671「山形陣列」、873「斐波那契子序列」同一家族），令 `dp[i]` = 「以第 `i` 個元素結尾」的最長遞增長度。最後一步是「`a[i]` 接在哪個更小的元素後面」：\n\n```cpp\n// dp[i]：以 a[i] 結尾的最長遞增子序列長度\nvector<int> dp(n, 1);\nfor (int i = 0; i < n; i++) {\n  for (int j = 0; j < i; j++) {\n    if (a[j] < a[i]) {\n      dp[i] = max(dp[i], dp[j] + 1);  // 接在 a[j] 後面\n    }\n  }\n}\nint ans = *max_element(dp.begin(), dp.end());\n```\n:::\n\n\n\n這裡 `dp[i]` 用到的 `dp[j]` 都是更早算好的，所以「按 `i` 從小到大」填表就不會重算。整體 `O(n^2)`。\n\n:::example dp[s]：湊出分數 s 的方法數（一種題目可選 0..cnt 道）\n再看一個「方法數」的例子（呼應 2585「獲得分數的方法數」）。背包式 DP：`dp[s]` = 「湊出分數 `s` 的方法數」，每種題型依次更新：\n\n```cpp\n// dp[s]：湊出分數 s 的方法數（一種題目可選 0..cnt 道）\nvector<long long> dp(target + 1, 0);\ndp[0] = 1;  // 什麼都不選湊出 0，一種\nfor (auto& [mark, cnt] : types) {\n  for (int s = target; s >= 0; s--) {\n    for (int k = 1; k <= cnt && k * mark <= s; k++) {\n      dp[s] += dp[s - k * mark];\n    }\n  }\n}\n```\n:::\n\n\n\n**實作方式**\n\n**1. 定義狀態語意，寫成一句話。** 例如「`dp[i]` = 以 `i` 結尾的最長…」「`dp[i][j]` = 前 `i` 個、用了 `j` 個的最小成本」。這句話越精確，後面越順。\n\n:::example 轉移的通用形狀：列舉最後一步的所有選擇\n**2. 從「最後一步決策」推轉移。** 問自己：要算 `dp[i]`，最後一個動作有哪些選擇？每個選擇對應一個更小的狀態，取最大 / 最小 / 求和：\n\n```cpp\n// 轉移的通用形狀：列舉最後一步的所有選擇\n// dp[i] = best over choices( dp[較小狀態] + 這步的成本/貢獻 )\n```\n:::\n\n\n\n**3. 處理初始化與填表順序。** 邊界（`dp[0]`、空集合）要先設對；填表順序要保證「用到的小狀態已經算好」：線性題從前往後、區間題按長度遞增、樹形題用後序、狀壓題按 mask 遞增。\n\n**4. 確定答案在哪。** 答案可能是某個終態 `dp[n]`，也可能是「所有結尾狀態的最大值」。一定要和狀態定義一致——若 `dp[i]` 是「以 `i` 結尾」，答案多半要對所有 `i` 取最值，而不是只看 `dp[n-1]`。\n\n**5. 邊界與複雜度。** 別急著背公式；最常出錯的是初始化、不可達狀態（設成 `INF` 或 `-INF`）、以及空間壓縮時的更新方向（像上面背包要從大到小，否則會重複選）。複雜度 = 狀態數 × 每個轉移的成本。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「動態規劃」想：\n\n- **要求最大 / 最小 / 方法數**，而且做的是「一連串選或不選 / 怎麼擺」的決策，例如 2585「獲得分數的方法數」、1671「得到山形陣列的最少刪除次數」。\n- **暴力會把同一個子問題重算很多次**：你發現「算大問題時不斷碰到相同的小問題」，這正是 DP 把它存起來的時機。\n- **答案可由「更小規模的答案」組合而來**，且選擇之間有明確的「最後一步」，例如 873「最長斐波那契子序列」把「以哪兩個數結尾」當狀態。\n- **題目限制看似要枚舉指數種組合，但狀態其實不多**（受限於下標、目標值、或一個 mask），例如 3122「使矩陣滿足條件的最少操作」把「每列填什麼」當狀態逐列轉移。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `dp` 下標代表什麼？能用一句話講清楚嗎？\n2. 算 `dp[i]` 時「最後一步」有哪些選擇？它們對應哪些更小的狀態？\n3. 我的填表順序保證了「用到的狀態都先算好」嗎？答案到底讀哪一格？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3122 | [使矩陣滿足條件的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-satisfy-conditions/) | 1905 | 把位置與限制一起放進狀態；練習檢查移動方向、邊界與滾動更新順序。 |\n| 991 | [壞了的計算器](https://leetcode.cn/problems/broken-calculator/) | 1909 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 2585 | [獲得分數的方法數](https://leetcode.cn/problems/number-of-ways-to-earn-points/) | 1910 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 873 | [最長的斐波那契子序列的長度](https://leetcode.cn/problems/length-of-longest-fibonacci-subsequence/) | 1911 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 1671 | [得到山形陣列的最少刪除次數](https://leetcode.cn/problems/minimum-number-of-removals-to-make-mountain-array/) | 1913 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1425. 帶限制的子序列和 展示核心寫法。\n\n**完整題目**：選擇一個非空子序列，使相鄰被選元素下標差不超過 k，並最大化元素和。\n\n:::example 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以…\n**線索**：題目能拆成前綴、位置或選擇序列，且不同路徑會到達相同子問題；需要把影響未來的資訊寫成狀態。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以 i 結尾的最大子集和\n  int constrainedSubsetSum(vector<int>& nums, int k) {\n    deque<int> dq;  // 單調遞減 deque，儲存下標，保證 dp[dq.front()] 為窗口最大值\n    vector<int> dp(nums.size());\n    int ans = INT_MIN;\n    for (int i = 0; i < (int)nums.size(); ++i) {\n      // 移除已超出 k 範圍的窗口頭部\n      while (!dq.empty() && dq.front() < i - k) {\n        dq.pop_front();\n      }\n      // dp[i]：選 nums[i] 加上前 k 個位置中最大的正 dp 值（若最大值為負則不取前段）\n      dp[i] = nums[i] + (dq.empty() ? 0 : max(0, dp[dq.front()]));\n      // 維護單調遞減性：彈出所有 dp 值不大於 dp[i] 的尾部\n      while (!dq.empty() && dp[dq.back()] <= dp[i]) {\n        dq.pop_back();\n      }\n      dq.push_back(i);\n      ans = max(ans, dp[i]);\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `dp[i]` 需要查前 k 個位置的最大 dp 值；單調佇列把這個區間最大值查詢降成均攤 O(1)。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 前綴和優化。\n- 單調佇列/堆疊優化。\n- 樹狀陣列、線段樹、斜率優化。\n\n**實作注意事項**\n\n- 先保留樸素式方便驗證。\n- 資料結構查的是哪個前驅集合要寫清楚。\n- 單調性不成立時不能硬套。\n- 可用小範圍對拍確認優化等價。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3122 | [使矩陣滿足條件的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-satisfy-conditions) | 1905 |\n| 991 | [壞了的計算器](https://leetcode.cn/problems/broken-calculator) | 1909 |\n| 2585 | [獲得分數的方法數](https://leetcode.cn/problems/number-of-ways-to-earn-points) | 1910 |\n| 873 | [最長的斐波那契子序列的長度](https://leetcode.cn/problems/length-of-longest-fibonacci-subsequence) | 1911 |\n| 1671 | [得到山形陣列的最少刪除次數](https://leetcode.cn/problems/minimum-number-of-removals-to-make-mountain-array) | 1913 |\n| 1043 | [分隔陣列以得到最大和](https://leetcode.cn/problems/partition-array-for-maximum-sum) | 1916 |\n| 3144 | [分割字元頻率相等的最少子字串](https://leetcode.cn/problems/minimum-substring-partition-of-equal-character-frequency) | 1917 |\n| 1130 | [葉值的最小代價生成樹](https://leetcode.cn/problems/minimum-cost-tree-from-leaf-values) | 1919 |\n| 1416 | [恢復陣列](https://leetcode.cn/problems/restore-the-array) | 1920 |\n| 1745 | [分割回文串 IV](https://leetcode.cn/problems/palindrome-partitioning-iv) | 1925 |\n| 3372 | [連線兩棵樹後最大目標節點數目 I](https://leetcode.cn/problems/maximize-the-number-of-target-nodes-after-connecting-trees-i) | 1927 |\n| 1449 | [數位成本和為目標值的最大數字](https://leetcode.cn/problems/form-largest-integer-with-digits-that-add-up-to-target) | 1927 |\n| 996 | [平方陣列的數目](https://leetcode.cn/problems/number-of-squareful-arrays) | 1932 |\n| 1964 | [找出到每個位置為止最長的有效障礙賽跑路線](https://leetcode.cn/problems/find-the-longest-valid-obstacle-course-at-each-position) | 1933 |\n| 2925 | [在樹上執行操作以後得到的最大分數](https://leetcode.cn/problems/maximum-score-after-applying-operations-on-a-tree) | 1940 |\n| 2111 | [使陣列 K 遞增的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-the-array-k-increasing) | 1941 |\n| 1690 | [石子游戲 VII](https://leetcode.cn/problems/stone-game-vii) | 1951 |\n| 2435 | [矩陣中和能被 K 整除的路徑](https://leetcode.cn/problems/paths-in-matrix-whose-sum-is-divisible-by-k) | 1952 |\n| 1696 | [跳躍遊戲 VI](https://leetcode.cn/problems/jump-game-vi) | 1954 |\n| 3599 | [劃分陣列得到最小 XOR](https://leetcode.cn/problems/partition-array-to-minimize-xor) | 1955 |\n| 1463 | [摘櫻桃 II](https://leetcode.cn/problems/cherry-pickup-ii) | 1957 |\n| 1537 | [最大得分](https://leetcode.cn/problems/get-the-maximum-score) | 1961 |\n| 3686 | [穩定子序列的數量](https://leetcode.cn/problems/number-of-stable-subsequences) | 1969 |\n| 3202 | [找出有效子序列的最大長度 II](https://leetcode.cn/problems/find-the-maximum-length-of-valid-subsequence-ii) | 1974 |\n| 1092 | [最短公共超序列](https://leetcode.cn/problems/shortest-common-supersequence) | 1977 |\n| 1278 | [分割回文串 III](https://leetcode.cn/problems/palindrome-partitioning-iii) | 1979 |\n| 3725 | [統計每一行選擇互質整數的方案數](https://leetcode.cn/problems/count-ways-to-choose-coprime-integers-from-rows) | 1982 |\n| 940 | [不同的子序列 II](https://leetcode.cn/problems/distinct-subsequences-ii) | 1985 |\n| 902 | [最大為 N 的數字組合](https://leetcode.cn/problems/numbers-at-most-n-given-digit-set) | 1990 |\n| 1986 | [完成任務的最少工作時間段](https://leetcode.cn/problems/minimum-number-of-work-sessions-to-finish-the-tasks) | 1995 |",
        },
        {
          id: 32,
          title: "3.5 圖論演算法",
          summary:
            "**原理講解**\n\n本節的主題是「圖論演算法」。圖就是「一堆點（節點）+ 連接它們的線（邊）」。很多題目雖然沒明寫「圖」，但只要有「東西之間互相連接 / 可以互相到達」的關係，就能建成圖來解。學這節最重要的兩步是：**先把題目建模成節點與邊，再依邊的性質選對遍歷 / 最短路演算法。**\n\n:::example BFS：在無權圖（每步成本相同）求最短步數\n最基礎的兩種遍歷是 DFS 和 BFS。它們都從某點出發、沿邊走遍所有可達的點，差別在於 BFS「一層一層」擴散，因此在「每條邊長度都相同」時，BFS 第一次抵達某點就是最短步數：\n\n```cpp\n// BFS：在無權圖（每步成本相同）求最短步數\nqueue<int> q;\nq.push(src);\nvector<int> dist(n, -1);\ndist[src] = 0;\nwhile (!q.empty()) {\n  int u = q.front();\n  q.pop();\n  for (int v : g[u]) {\n    if (dist[v] == -1) {      // 還沒走過\n      dist[v] = dist[u] + 1;  // 第一次抵達就是最短\n      q.push(v);\n    }\n  }\n}\n```\n:::\n\n\n\n:::example Dijkstra：非負權圖的單源最短路（鬆弛核心）\n當邊有「不同的非負權重」時，BFS 不夠用，要改用 Dijkstra：用一個小根堆每次取出「目前已知距離最短」的點，再去「鬆弛」它的鄰居（看看能不能讓鄰居的距離變更短）：\n\n```cpp\n// Dijkstra：非負權圖的單源最短路（鬆弛核心）\nif (d[u] + w < d[v]) {  // 經過 u 到 v 更近？\n  d[v] = d[u] + w;      // 更新更短距離\n  pq.push({d[v], v});   // 放回堆，之後再處理\n}\n```\n:::\n\n\n\n像「圖中的最短環」（2608）就是先判斷邊權是 1（用 BFS）還是一般非負（用 Dijkstra）。關鍵不是背演算法，而是**先看清楚「節點是什麼、邊是什麼、邊有沒有權」**。\n\n另一類常見題是「依賴關係」，要用拓撲排序：例如「給定條件下構造矩陣」（2392），把「A 必須在 B 前面」當成一條有向邊，再依入度為 0 的順序逐一排出。\n\n**實作方式**\n\n:::example 鄰接表：g[u] 存所有 (鄰居 v, 邊權 w)\n**1. 建模：先寫下節點與邊。** 節點可以是真實的點，也可以是「(位置, 額外狀態)」的組合（例如帶著「還剩幾次特權」的狀態）。邊要寫清楚「成本」與「什麼情況下可以走」。常用鄰接表存圖：\n\n```cpp\n// 鄰接表：g[u] 存所有 (鄰居 v, 邊權 w)\nvector<vector<pair<int, int>>> g(n);\ng[u].push_back({v, w});\ng[v].push_back({u, w});  // 無向圖才加這行\n```\n:::\n\n\n\n**2. 依邊權選演算法。** 無權 / 邊權全相同 → BFS；邊權只有 0/1 → 0-1 BFS（雙端佇列）；非負權 → Dijkstra；有負權 → Bellman-Ford / SPFA；只是要走遍 / 找連通塊 → DFS 或並查集。\n\n**3. 用 visited / dist 防重複。** 圖可能有環，必須記錄「走過沒」，否則會無限繞圈。把「額外狀態」也納進 visited 的鍵，例如 `(位置, 狀態)` 一起標記。\n\n:::example 拓撲排序：每次取出入度為 0 的節點\n**4. 拓撲排序處理依賴。** 算每個點的入度，把入度為 0 的先入佇列，每移除一個就把鄰居入度減一，減到 0 再入佇列：\n\n```cpp\n// 拓撲排序：每次取出入度為 0 的節點\nqueue<int> q;\nfor (int i = 0; i < n; i++) {\n  if (indeg[i] == 0) {\n    q.push(i);\n  }\n}\nwhile (!q.empty()) {\n  int u = q.front();\n  q.pop();\n  order.push_back(u);\n  for (int v : g[u]) {\n    if (--indeg[v] == 0) {\n      q.push(v);\n    }\n  }\n}\n// order 大小 < n 代表有環，無法排序\n```\n:::\n\n\n\n**5. 答案讀取與複雜度。** 答案常來自 `dist` 陣列、連通塊數、拓撲序、或某種可達狀態。複雜度：BFS/DFS 是 `O(V + E)`；Dijkstra 用堆是 `O(E log V)`；拓撲排序 `O(V + E)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「圖論」想：\n\n- **資料天然是「點 + 連接關係」**：城市與道路、課程與先修、人與好友——或抽象的「狀態與轉移」，例如「最短環」（2608）。\n- **問「最短步數 / 最短距離 / 能不能到達」**：先看邊權，再選 BFS（等權）或 Dijkstra（非負權）。\n- **問「先後 / 依賴順序」**：例如 2392「給定條件下構造矩陣」，把限制變成有向邊做拓撲排序；排不出來就代表有矛盾（環）。\n- **問「連通塊 / 移除某點影響多少」**：例如 928「儘量減少惡意軟體傳播 II」，先用 DFS 或並查集找連通結構，再分析每個關鍵點的影響。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點代表什麼？邊代表什麼？需不需要把「額外狀態」也塞進節點？\n2. 邊權是等權、0/1、非負，還是有負？對應該選哪個演算法？\n3. 我答案要從哪裡讀？是 `dist`、連通塊數、拓撲序，還是某個可達狀態？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2608 | [圖中的最短環](https://leetcode.cn/problems/shortest-cycle-in-a-graph/) | 1904 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n| 2392 | [給定條件下構造矩陣](https://leetcode.cn/problems/build-a-matrix-with-conditions/) | 1961 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 928 | [儘量減少惡意軟體的傳播 II](https://leetcode.cn/problems/minimize-malware-spread-ii/) | 1985 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 743. 網路延遲時間 展示核心寫法。\n\n**完整題目**：給定有向帶權邊、節點數與起點，求訊號從起點傳到所有節點所需的最長最短路時間；若有節點不可達則回傳 `-1`。\n\n:::example Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n**線索**：題目描述可達、路徑、依賴或關係傳遞；把物件當節點、操作當邊後，就能選遍歷或最短路。\n\n```cpp\nclass Solution {\n public:\n  // Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n  int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    vector<vector<pair<int, int>>> g(n + 1);  // 鄰接表，g[u] = {(v, w)}\n    for (auto& e : times) {\n      g[e[0]].push_back({e[1], e[2]});\n    }\n    const int INF = 1e9;\n    vector<int> dist(n + 1, INF);\n    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;  // 最小堆：{距離, 節點}\n    dist[k] = 0;\n    pq.push({0, k});\n    while (!pq.empty()) {\n      auto [d, u] = pq.top();\n      pq.pop();\n      if (d != dist[u]) {\n        continue;  // 已有更短路徑，跳過過期條目\n      }\n      for (auto [v, w] : g[u]) {\n        if (dist[v] > d + w) {\n          dist[v] = d + w;  // 鬆弛操作\n          pq.push({dist[v], v});\n        }\n      }\n    }\n    // 所有節點最短距離的最大值即為信號傳播時間\n    int ans = *max_element(dist.begin() + 1, dist.end());\n    return ans == INF ? -1 : ans;  // 若有節點不可達則返回 -1\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 非負權最短路使用 Dijkstra；堆中舊距離要跳過，避免重複鬆弛造成常數過大。\n- 先建出鄰接關係，再用 `visited`、`dist`、`indeg` 等狀態控制每個節點何時處理。\n- 如果同一節點在不同資源狀態下未來不同，就不能只用節點編號當 visited 鍵。\n\n**常見變形**\n\n- 無權最短路用 BFS。\n- 0/1 權用 0-1 BFS。\n- 非負權用 Dijkstra；有負權需 Bellman-Ford/SPFA 類思路。\n\n**實作注意事項**\n\n- 先估點數與邊數，決定鄰接表或矩陣。\n- Dijkstra 不適用負權邊。\n- 多源 BFS 要把所有源點同時入隊。\n- 狀態圖要包含資源、鑰匙、次數等額外維度。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2608 | [圖中的最短環](https://leetcode.cn/problems/shortest-cycle-in-a-graph) | 1904 |\n| 2392 | [給定條件下構造矩陣](https://leetcode.cn/problems/build-a-matrix-with-conditions) | 1961 |\n| 928 | [儘量減少惡意軟體的傳播 II](https://leetcode.cn/problems/minimize-malware-spread-ii) | 1985 |",
        },
        {
          id: 33,
          title: "3.6 貪心",
          summary:
            "**原理講解**\n\n本節要解決的問題是：在一堆選擇中，每一步都「先挑看起來最好的那一個」，最後就能拼出全域最優解。這種「每步只看眼前、卻能保證整體正確」的策略，就叫做貪心（greedy）。\n\n貪心和暴力枚舉最大的差別在於：暴力會把所有可能的組合都試一遍，而貪心只走一條路——它相信「依某個規則排序後，由前往後做當下最安全的選擇」就夠了。問題是：憑什麼相信？這就是貪心最難、也最關鍵的地方——你必須能「證明」這個規則不會害你錯過最優解。\n\n最常用的證明工具叫「交換論證（exchange argument）」。它的想法是：假設存在一個比你更好的最優解，那麼把它裡面的某兩個選擇對調，使它更像你的貪心解，並說明對調後答案「不會變差」。一直換下去，最優解就會被改造成你的貪心解，於是兩者一樣好——這就證明貪心是對的。\n\n:::example 活動選擇：按「結束時間」由早到晚排序，能選就選\n舉個最直覺的例子：選最多場不重疊的活動。\n\n```cpp\n// 活動選擇：按「結束時間」由早到晚排序，能選就選\nsort(jobs.begin(), jobs.end(), [](auto& a, auto& b) {\n  return a.end < b.end;  // 結束越早的排前面\n});\nint lastEnd = -1, count = 0;\nfor (auto& j : jobs) {\n  if (j.start >= lastEnd) {  // 和上一個不衝突 → 安全選\n    lastEnd = j.end;\n    count++;\n  }\n}\n```\n:::\n\n\n\n為什麼按「結束時間」而不是「開始時間」？因為越早結束的活動，留給後面的空間越多——這正是可以用交換論證證明的「安全選擇規則」。像「3394 判斷網格圖能否被切割成塊」也是類似：找出一條最早就能把區塊分開的切線。\n\n:::example 反悔貪心骨架：先全選進堆，超標時丟掉最差的\n有時候「當下最好」會在後面變成錯誤，這時要用「反悔（regret）」貪心：先樂觀地選，發現選錯了再用一個堆把最差的那個換掉。\n\n```cpp\n// 反悔貪心骨架：先全選進堆，超標時丟掉最差的\npriority_queue<int> worst;  // 大根堆，堆頂是最差（代價最大）的已選項\nfor (auto& x : items) {\n  worst.push(cost(x));      // 先樂觀納入\n  if (overBudget()) {       // 超出限制\n    budget += worst.top();  // 把最差的退回去（反悔）\n    worst.pop();\n  }\n}\n```\n:::\n\n\n\n**實作方式**\n\n任何貪心題都可拆成三件事：狀態表示、更新規則、答案讀取。逐一搭配程式碼。\n\n:::example 狀態表示：只保留判斷下一步所需的最小資訊\n**1. 狀態表示：保存「目前已選集合」的摘要。** 你通常不需要記住整個集合，只要記一個能支撐下一步判斷的摘要，例如「上一個選的結束時間」、「剩餘的能量／資源」、或一個放著可反悔候選的堆。\n\n```cpp\n// 狀態表示：只保留判斷下一步所需的最小資訊\nint lastEnd = -1;                // 上一個已選活動的結束時間\nlong long remain = budget;       // 剩餘資源\npriority_queue<int> candidates;  // 可反悔的已選候選（最差在頂端）\n```\n:::\n\n\n\n:::example 更新：排序後逐一決策（以 1665 為例的排序鍵）\n**2. 更新流程：按證明需要的鍵排序，逐一做安全選擇。** 排序鍵是貪心的靈魂——它必須是你能證明正確的那個鍵。掃描時對每個元素做「當下安全選擇」，需要時反悔。\n\n```cpp\n// 更新：排序後逐一決策（以 1665 為例的排序鍵）\nsort(tasks.begin(), tasks.end(), [](auto& a, auto& b) {\n  return a.minReq - a.actual > b.minReq - b.actual;  // 按「需求-消耗」差由大到小\n});\nfor (auto& t : tasks) { /* 對 t 做當下安全選擇 */\n}\n```\n:::\n\n\n\n「1665 完成所有任務的最少初始能量」的關鍵就是這個排序鍵：把「需求量與實際消耗差距大」的任務排前面先做，能讓初始能量需求最小。\n\n:::example 答案讀取：掃描結束後狀態即答案\n**3. 答案讀取：掃描完就直接讀出。** 走完一遍後，答案就藏在你維護的摘要裡——已選集合大小、累積代價、或構造出來的字串。\n\n```cpp\n// 答案讀取：掃描結束後狀態即答案\nreturn count;  // 例如：最多可選的活動數\n               // 或 return totalCost; / return builtString;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 千萬不要「只憑直覺選最大或最小」。沒有證明的貪心常常是錯的——若交換論證說不通，這題很可能需要 DP 或搜尋，而不是貪心。\n- 排序鍵選錯，整題就錯，而且常常「小測資會過、大測資掛掉」，很難 debug。\n- 複雜度通常由排序主導，為 `O(n log n)`；用到反悔堆時，每個元素最多進出堆一次，仍是 `O(n log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「貪心」想：\n\n- **題目要最大化或最小化某個量，而且感覺「一步一步往前做就好」，不需要回頭嘗試所有組合。** 例如「最多能選幾個」「最少需要多少初始值」。\n- **元素之間存在一個自然的「優先順序」**——某個欄位排序後，由前往後處理會變得很順。這個排序鍵往往就是貪心的關鍵。\n- **你能說出一句「為什麼先做這個一定不吃虧」的理由。** 如果說不出來，先別套貪心：\n\n:::example 套貪心前先自問：這個排序鍵有沒有反例？\n```cpp\n// 套貪心前先自問：這個排序鍵有沒有反例？\n// 若想得到一組「按此鍵排序卻得到較差答案」的輸入 → 規則錯了\n// 想不出反例、且能用交換論證說明 → 才放心用\n```\n:::\n\n\n\n- **「3002 移除後集合的最多元素數」「2673 使二叉樹所有路徑值相等的最小代價」這類題**，都是先想清楚一條安全規則，再一路套用。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的排序鍵是什麼？為什麼是它？\n2. 我能不能用交換論證說明「先選它不會讓答案變差」？\n3. 如果某步選錯了，我需不需要反悔機制（堆）來補救？\n\n三題都答得出來，貪心才算套對。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1665 | [完成所有任務的最少初始能量](https://leetcode.cn/problems/minimum-initial-energy-to-finish-tasks/) | 1901 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 1147 | [段式迴文](https://leetcode.cn/problems/longest-chunked-palindrome-decomposition/) | 1912 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3394 | [判斷網格圖能否被切割成塊](https://leetcode.cn/problems/check-if-grid-can-be-cut-into-sections/) | 1916 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2673 | [使二叉樹所有路徑值相等的最小代價](https://leetcode.cn/problems/make-costs-of-paths-equal-in-a-binary-tree/) | 1917 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 3002 | [移除後集合的最多元素數](https://leetcode.cn/problems/maximum-size-of-a-set-after-removals/) | 1917 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(), [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        kept++;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1665 | [完成所有任務的最少初始能量](https://leetcode.cn/problems/minimum-initial-energy-to-finish-tasks) | 1901 |\n| 1147 | [段式迴文](https://leetcode.cn/problems/longest-chunked-palindrome-decomposition) | 1912 |\n| 3394 | [判斷網格圖能否被切割成塊](https://leetcode.cn/problems/check-if-grid-can-be-cut-into-sections) | 1916 |\n| 2673 | [使二叉樹所有路徑值相等的最小代價](https://leetcode.cn/problems/make-costs-of-paths-equal-in-a-binary-tree) | 1917 |\n| 3002 | [移除後集合的最多元素數](https://leetcode.cn/problems/maximum-size-of-a-set-after-removals) | 1917 |\n| 2731 | [移動機器人](https://leetcode.cn/problems/movement-of-robots) | 1923 |\n| 1727 | [重新排列後的最大子矩陣](https://leetcode.cn/problems/largest-submatrix-with-rearrangements) | 1927 |\n| 1798 | [你能構造出連續值的最大數目](https://leetcode.cn/problems/maximum-number-of-consecutive-values-you-can-make) | 1931 |\n| 2227 | [加密解密字串](https://leetcode.cn/problems/encrypt-and-decrypt-strings) | 1945 |\n| 3720 | [大於目標字串的最小字典序排列](https://leetcode.cn/problems/lexicographically-smallest-permutation-greater-than-target) | 1958 |\n| 2350 | [不可能得到的最短骰子序列](https://leetcode.cn/problems/shortest-impossible-sequence-of-rolls) | 1961 |\n| 1969 | [陣列元素的最小非零乘積](https://leetcode.cn/problems/minimum-non-zero-product-of-the-array-elements) | 1967 |\n| 3771 | [探索地牢的得分](https://leetcode.cn/problems/total-score-of-dungeon-runs) | 1981 |\n| 1733 | [需要教語言的最少人數](https://leetcode.cn/problems/minimum-number-of-people-to-teach) | 1984 |\n| 2963 | [統計好分割方案的數目](https://leetcode.cn/problems/count-the-number-of-good-partitions) | 1985 |\n| 3440 | [重新安排會議得到最多空餘時間 II](https://leetcode.cn/problems/reschedule-meetings-for-maximum-free-time-ii) | 1998 |",
        },
        {
          id: 34,
          title: "3.7 網格圖",
          summary:
            "**原理講解**\n\n本節處理的是「網格圖」題目：給你一個二維方格（地圖、迷宮、棋盤），要你算「從某格走到某格的最短步數」「有幾塊連在一起的區域」之類的問題。\n\n核心觀念只有一句話：**把每一格當成圖上的一個「節點」，相鄰的格子之間連一條「邊」。** 一旦這樣看，網格題就變成了標準的圖論搜尋，可以直接套 BFS、Dijkstra 等現成演算法。\n\n:::example 方向陣列：上下左右四個方向的座標位移\n「相鄰」通常指上下左右四個方向（有些題加上斜對角共八方向）。實作上不用真的建圖，而是用一個「方向陣列」即時算出鄰居：\n\n```cpp\n// 方向陣列：上下左右四個方向的座標位移\nint dr[] = {-1, 1, 0, 0};\nint dc[] = {0, 0, -1, 1};\nfor (int k = 0; k < 4; k++) {\n  int nr = r + dr[k], nc = c + dc[k];  // (nr, nc) 是 (r, c) 的一個鄰居\n  // 之後再檢查 nr, nc 有沒有出界、是不是障礙\n}\n```\n:::\n\n\n\n:::example BFS 求最短步數：一層一層往外擴\n如果每走一步代價都一樣（求最少步數），用 BFS：它像水波一樣一圈一圈往外擴，第一次碰到目標時的圈數，就是最短距離。\n\n```cpp\n// BFS 求最短步數：一層一層往外擴\nqueue<pair<int, int>> q;\nq.push({sr, sc});\nvector<vector<bool>> vis(R, vector<bool>(C, false));\nvis[sr][sc] = true;\nint dist = 0;\nwhile (!q.empty()) {\n  int sz = q.size();\n  for (int i = 0; i < sz; i++) { /* 取出一格、擴展鄰居、標記 vis */\n  }\n  dist++;  // 擴完一整層，距離加一\n}\n```\n:::\n\n\n\n關鍵的進階點：當題目允許「帶鑰匙」「可消除有限次障礙」「有方向成本」時，光記「我在哪一格」不夠——同一格用不同條件到達，是不同的局面。這時要把這些條件也放進「狀態」裡。例如「1293 網格中的最短路徑（可消除 k 個障礙）」，狀態就是 `(row, col, 還能消除幾次)`。\n\n**實作方式**\n\n:::example 狀態表示：座標加上「剩餘可消除障礙次數」\n**1. 狀態表示：座標 + 必要的附加資訊。** 最單純時狀態就是 `(row, col)`；若有鑰匙、剩餘消除次數、朝向或時間，全都要併進狀態。\n\n```cpp\n// 狀態表示：座標加上「剩餘可消除障礙次數」\nstruct State {\n  int r, c, leftK;\n};  // 1293 的狀態\n// visited 要對 (r, c, leftK) 整組去重，不能只看 (r, c)\nvector<vector<vector<bool>>> vis(R, vector<vector<bool>>(C, vector<bool>(K + 1, false)));\n```\n:::\n\n\n\n:::example 更新：擴展前先擋掉出界與障礙\n**2. 更新流程：檢查邊界與障礙後，擴展鄰居。** 對每個方向算出鄰居，先擋掉出界與不可走的格子，再依代價選對的演算法：步數相同用 BFS，邊權 0 或 1 用雙端佇列 deque，非負權用 Dijkstra（優先佇列）。\n\n```cpp\n// 更新：擴展前先擋掉出界與障礙\nauto inGrid = [&](int r, int c) { return r >= 0 && r < R && c >= 0 && c < C; };\nfor (int k = 0; k < 4; k++) {\n  int nr = r + dr[k], nc = c + dc[k];\n  if (!inGrid(nr, nc)) {\n    continue;  // 出界，跳過\n  }\n  // 再依本題規則處理障礙 / 鑰匙 / 代價後，推入佇列\n}\n```\n:::\n\n\n\n:::example 答案讀取：BFS 中第一次碰到目標就回傳距離\n**3. 答案讀取：依題型直接取。** 最短路題取「第一次到達目標的距離」；連通塊題取「掃完所有格子數出來的區域數量或大小」；遍歷染色題取「最後的標記矩陣」。\n\n```cpp\n// 答案讀取：BFS 中第一次碰到目標就回傳距離\nif (nr == tr && nc == tc) {\n  return dist + 1;  // 抵達終點\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- **visited 不能只看座標。** 這是網格題最常見的錯誤：同一格以不同鑰匙集合 / 剩餘次數到達，是不同狀態，要對整組狀態去重，否則會誤判「來過了」而漏掉答案。\n- BFS / Dijkstra 別忘了入隊時就標記 visited，避免同一格重複入隊。\n- 複雜度：純 BFS 是 `O(R * C)`；帶 k 維狀態變 `O(R * C * k)`；用 Dijkstra 則是 `O(R * C * log(R * C))`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「網格圖」想：\n\n- **輸入是一個二維陣列（地圖、迷宮、棋盤），要在上面「走動」**，問最短路徑、能否到達、或區域數量。\n- **問「最少幾步」且每步代價相同** → 幾乎一定是 BFS；只要記得它的本質是「把格子當節點」：\n\n:::example 嗅到味道的瞬間：要在方格上找最短步數\n```cpp\n// 嗅到味道的瞬間：要在方格上找最短步數\n// → 別寫 DFS 暴搜，改用 BFS 一層層擴，第一次到目標就是答案\n```\n:::\n\n\n\n- **題目給了「鑰匙」「最多可打掉 k 道牆」「限定步數內」這類附加條件** → 提醒你把它塞進狀態，並讓 visited 跟著變多一個維度。\n- **問「有幾座島」「最大連通區域」** → 對每個沒走過的格子發起一次 BFS/DFS 染色，數區域。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的一個「狀態」到底由哪些東西組成？只有座標，還是要加鑰匙 / 剩餘次數？\n2. 每步代價一樣嗎？（決定用 BFS 還是 Dijkstra）\n3. 我的 visited 維度有沒有和狀態完全對齊？\n\n三題都答得出來，網格圖就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1293 | [網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-grid-with-obstacles-elimination/) | 1967 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n:::example DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\n public:\n  int numIslands(vector<vector<char>>& grid) {\n    int m = grid.size(), n = grid[0].size(), ans = 0;\n    int dirs[5] = {1, 0, -1, 0, 1};  // 方向陣列，相鄰兩元素構成上下左右四個方向向量\n    // DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n    function<void(int, int)> dfs = [&](int r, int c) {\n      grid[r][c] = '0';  // 標記已訪問，避免重複計算\n      for (int k = 0; k < 4; ++k) {\n        int nr = r + dirs[k], nc = c + dirs[k + 1];\n        if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n          dfs(nr, nc);\n        }\n      }\n    };\n    // 掃描整個網格，每遇到一個未訪問的 '1' 就是一座新島嶼\n    for (int r = 0; r < m; ++r) {\n      for (int c = 0; c < n; ++c) {\n        if (grid[r][c] == '1') {\n          ans++, dfs(r, c);\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1293 | [網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-grid-with-obstacles-elimination) | 1967 |",
        },
        {
          id: 35,
          title: "3.8 數學",
          summary:
            "**原理講解**\n\n本節要解決的是「數學」類題目：題目表面上講的是某個情境（排座位、分長廊、找質數），但解法的核心是先把文字翻成數學式子，再用數學性質把計算化簡到很快。\n\n這類題的關鍵心法是：**不要急著寫迴圈暴力試，先把問題公式化。** 常見的數學工具有幾種：\n\n- 整除與質數：判斷一個數能不能被整除、找出所有質數或因數。\n- 同餘與取模：答案很大時，題目會要你「對 1e9+7 取模」。\n- 組合計數：算「有幾種排法 / 選法」，常用組合數 C(n, k)。\n- 最大公因數（gcd）：兩數能湊出什麼，往往和它們的 gcd 有關。\n\n:::example 埃拉托斯特尼篩法：一次找出 [2, n] 內所有質數\n先看一個最常用的工具——找質數的「篩法」。與其對每個數逐一判斷是不是質數（慢），不如反過來：把每個質數的倍數全部劃掉。\n\n```cpp\n// 埃拉托斯特尼篩法：一次找出 [2, n] 內所有質數\nvector<bool> isPrime(n + 1, true);\nisPrime[0] = isPrime[1] = false;\nfor (int i = 2; (long long)i * i <= n; i++) {\n  if (isPrime[i]) {\n    for (int j = i * i; j <= n; j += i) {  // i 的倍數都不是質數\n      isPrime[j] = false;\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 整組數的 gcd：邊算邊更新，等於 1 就代表能湊出 1\n再看一個關鍵性質——最大公因數。「1250 檢查好陣列」其實就是裴蜀定理的應用：一組數能用整數係數湊出 1，當且僅當它們的 gcd 等於 1。\n\n```cpp\n// 整組數的 gcd：邊算邊更新，等於 1 就代表能湊出 1\nint g = 0;\nfor (int x : nums) {\n  g = gcd(g, x);  // <numeric> 內建 gcd\n}\nbool good = (g == 1);  // 1250 的答案\n```\n:::\n\n\n\n這就是數學題的威力：一個性質看穿，整題瞬間變成幾行。\n\n**實作方式**\n\n:::example 狀態表示：預處理組合數表（取模），之後 O(1) 查 C(n, k)\n**1. 狀態表示：先準備好要用的數學工具。** 依題目需要，預先算好質數表、各數的因子、組合數表，或把幾何問題的點存成向量。\n\n```cpp\n// 狀態表示：預處理組合數表（取模），之後 O(1) 查 C(n, k)\nconst int MOD = 1e9 + 7;\nvector<vector<long long>> C(N + 1, vector<long long>(N + 1, 0));\nfor (int i = 0; i <= N; i++) {\n  C[i][0] = 1;\n  for (int j = 1; j <= i; j++) {\n    C[i][j] = (C[i - 1][j - 1] + C[i - 1][j]) % MOD;  // 帕斯卡三角\n  }\n}\n```\n:::\n\n\n\n:::example 更新：枚舉到 sqrt(n) 找出所有因數（比逐一試到 n 快很多）\n**2. 更新流程：套用對應的數學運算。** 依公式做篩法、質因數分解、快速冪、容斥、遞推或座標計算。例如分解因數時，只要枚舉到根號 n 就夠——這是「3669 K 因數分解」「866 迴文質數」常用的加速技巧。\n\n```cpp\n// 更新：枚舉到 sqrt(n) 找出所有因數（比逐一試到 n 快很多）\nfor (int d = 1; (long long)d * d <= n; d++) {\n  if (n % d == 0) {\n    // d 是因數，n / d 也是因數\n  }\n}\n```\n:::\n\n\n\n:::example 答案讀取：把各段方案數相乘（取模），即為總方案數（2147 思路）\n**3. 答案讀取：由推導出的公式直接算。** 答案常常是一條閉式、一個計數公式、取模後的結果，或一個幾何判定（如「2147 分隔長廊」就是把各段的方案數相乘）。\n\n```cpp\n// 答案讀取：把各段方案數相乘（取模），即為總方案數（2147 思路）\nlong long ans = 1;\nfor (long long gap : gaps) {\n  ans = ans * (gap + 1) % MOD;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- **用小資料猜規律可以，但最後一定要補上證明。** 數學題最危險的就是「前幾筆對、規律其實是錯的」。\n- **取模運算要小心：** 加法、乘法都要邊算邊取模避免溢位；遇到「除法」不能直接除，要用「模逆元」（費馬小定理求 a 的逆元為 a^(MOD-2)）。乘法前記得轉 `long long`。\n- 複雜度：篩法 `O(n log log n)`，質因數分解 `O(sqrt(n))`，快速冪 `O(log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「數學」想：\n\n- **資料範圍特別大（如 n 到 1e9），卻又要求精確答案** → 不可能暴力枚舉，一定有公式或數學性質可用。\n- **題目出現「對 1e9+7 取模」「方案數」「有幾種」** → 多半是組合計數，要預處理組合數、注意取模。\n- **問題和「整除」「質數」「因數」「互質」有關** → 想篩法、質因數分解、gcd：\n\n:::example 嗅到味道的瞬間：題目問「能不能整除 / 互不互質 / 有幾個因數」\n```cpp\n// 嗅到味道的瞬間：題目問「能不能整除 / 互不互質 / 有幾個因數」\n// → 別逐一暴力試，先想 gcd、篩法、根號枚舉\n```\n:::\n\n\n\n- **「835 影象重疊」「866 迴文質數」這類題**，先用小例子手算找出規律，再公式化、處理溢位與精度。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把題目的文字條件寫成一條數學式子或性質？\n2. 資料範圍是不是大到「只能靠公式、不能靠迴圈」？\n3. 我有沒有處理好取模、溢位、逆元這些數值細節？\n\n三題都答得出來，數學題就抓對方向了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2147 | [分隔長廊的方案數](https://leetcode.cn/problems/number-of-ways-to-divide-a-long-corridor/) | 1915 | 先拆成計數模型；練習確認是否需要容斥、排列順序或模逆元。 |\n| 3669 | [K 因數分解](https://leetcode.cn/problems/balanced-k-factor-decomposition/) | 1917 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 866 | [迴文質數](https://leetcode.cn/problems/prime-palindrome/) | 1938 | 先處理整除與篩法邊界；練習用根號枚舉或預處理避免逐一暴力。 |\n| 835 | [影象重疊](https://leetcode.cn/problems/image-overlap/) | 1970 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1250 | [檢查「好陣列」](https://leetcode.cn/problems/check-if-it-is-a-good-array/) | 1983 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n public:\n  int countPrimes(int n) {\n    // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n    vector<char> isPrime(n, true);\n    int ans = 0;\n    for (int i = 2; i < n; ++i) {\n      if (!isPrime[i]) {\n        continue;\n      }\n      ans++;\n      // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n      if ((long long)i * i < n) {\n        for (long long j = 1LL * i * i; j < n; j += i) {\n          isPrime[j] = false;\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i*i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2147 | [分隔長廊的方案數](https://leetcode.cn/problems/number-of-ways-to-divide-a-long-corridor) | 1915 |\n| 3669 | [K 因數分解](https://leetcode.cn/problems/balanced-k-factor-decomposition) | 1917 |\n| 866 | [迴文質數](https://leetcode.cn/problems/prime-palindrome) | 1938 |\n| 835 | [影象重疊](https://leetcode.cn/problems/image-overlap) | 1970 |\n| 1250 | [檢查「好陣列」](https://leetcode.cn/problems/check-if-it-is-a-good-array) | 1983 |\n| 963 | [最小面積矩形 II](https://leetcode.cn/problems/minimum-area-rectangle-ii) | 1991 |\n| 1625 | [執行操作後字典序最小的字串](https://leetcode.cn/problems/lexicographically-smallest-string-after-applying-operations) | 1992 |",
        },
        {
          id: 36,
          title: "3.9 單調堆疊",
          summary:
            "**原理講解**\n\n本節要解決一類很常見的問題：對陣列中的每個元素，找出「左邊或右邊第一個比它大（或比它小）的元素」。例如「下一個更大的氣溫」「以某高度為最矮柱的最大矩形」。\n\n暴力做法是對每個元素都往左右掃一遍找邊界，那是 `O(n^2)`，資料一大就超時。這個技巧叫「單調堆疊（monotonic stack）」，能把它壓到 `O(n)`。\n\n核心想法：**用一個堆疊保存「還沒找到答案的元素」，並讓堆疊裡的元素保持遞增（或遞減）。當新元素破壞了這個單調性時，被「擠出去」的那些元素，當下就找到了它們要的邊界。**\n\n:::example 找每個元素「右邊第一個更大值」：維護遞減堆疊（存下標）\n打個比方：你排隊看遠方，前面如果站了個比你高的人，你就被擋住；一旦來了個更高的人站到隊伍裡，那些比他矮的、被他擋住的人就「定案」了——他們右邊第一個更高的就是這位新來者。\n\n```cpp\n// 找每個元素「右邊第一個更大值」：維護遞減堆疊（存下標）\nvector<int> ans(n, -1);  // 預設找不到為 -1\nstack<int> st;           // 存下標，對應的值由大到小\nfor (int i = 0; i < n; i++) {\n  while (!st.empty() && a[i] > a[st.top()]) {  // 新元素更大 → 結算棧頂\n    ans[st.top()] = a[i];                      // 棧頂的「右邊第一個更大值」就是 a[i]\n    st.pop();\n  }\n  st.push(i);  // 自己入棧，等待未來被結算\n}\n```\n:::\n\n\n\n注意兩個重點：**棧裡通常存「下標」而不是「值」**，因為很多題要算寬度、距離，需要知道位置；以及**每個元素最多進棧一次、出棧一次**，所以總共只做 `O(n)` 次操作——這就是它快的原因。\n\n**實作方式**\n\n:::example 狀態表示：存下標的堆疊，這裡維持「對應值遞減」\n**1. 狀態表示：一個存下標的堆疊，維持單調性。** 想清楚要遞增還是遞減：找「更大」用遞減棧（被更大的擠出），找「更小」用遞增棧。\n\n```cpp\n// 狀態表示：存下標的堆疊，這裡維持「對應值遞減」\nstack<int> st;  // st 中下標對應的 a[] 值由大到小\n```\n:::\n\n\n\n:::example 更新：彈出所有「比新元素小」的棧頂（每個就此確定右邊界）\n**2. 更新流程：掃到新元素時，把不再滿足單調性的棧頂一路彈出，彈出的瞬間就確定它的一側邊界。** 彈完後自己入棧。\n\n```cpp\n// 更新：彈出所有「比新元素小」的棧頂（每個就此確定右邊界）\nwhile (!st.empty() && a[i] >= a[st.top()]) {\n  int top = st.top();\n  st.pop();\n  // top 的右邊界 = i；若需左邊界，看彈完後新的 st.top()\n}\nst.push(i);\n```\n:::\n\n\n\n:::example 答案讀取：以 a[i] 為最小值的子陣列個數 = 左長 × 右長（907 思路）\n**3. 答案讀取：由左右邊界算出結果。** 「下一個更大 / 更小」直接是邊界元素；矩形寬度是「右邊界 - 左邊界 - 1」；貢獻法（如「907 子陣列最小值之和」）是「左可延伸長度 × 右可延伸長度 × 該值」。\n\n```cpp\n// 答案讀取：以 a[i] 為最小值的子陣列個數 = 左長 × 右長（907 思路）\nlong long contrib = (long long)(i - left) * (right - i) * a[i];\n// left：左邊第一個更小的下標；right：右邊第一個更小的下標\nans = (ans + contrib) % MOD;\n```\n:::\n\n\n\n「1793 好子陣列的最大分數」也是同型：以某根柱子為最矮，往左右擴到第一個更矮處，算 `高度 × 寬度`。\n\n**4. 邊界與複雜度。**\n\n- **相等元素用 `<` 還是 `<=` 會改變結果。** 在貢獻法裡，為了避免同一個子陣列被算兩次，要「一側嚴格、一側非嚴格」（例如左邊界用「嚴格更小」、右邊界用「小於等於」），這是最容易出錯的地方。\n- 掃完後棧裡可能還剩元素，代表它們那一側「沒有邊界」，要按題意給預設值（如 -1 或陣列端點）。\n- 複雜度：每個元素進出棧各一次，總計 `O(n)`，空間 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「單調堆疊」想：\n\n- **題目要對每個元素找「下一個 / 上一個更大或更小的元素」**，或「左右第一個破壞某條件的位置」。\n- **要算「以某元素為最大 / 最小值的子陣列」的數量或總和**（貢獻法），如「907 子陣列的最小值之和」。\n- **柱狀圖求最大矩形、接雨水**這類「需要左右最近邊界」的題：\n\n:::example 嗅到味道的瞬間：你想對每個 i 都往左右找最近的更大/更小\n```cpp\n// 嗅到味道的瞬間：你想對每個 i 都往左右找最近的更大/更小\n// 暴力是雙層迴圈 O(n^2)\n// → 改用單調堆疊，讓每個元素只進出一次，降到 O(n)\n```\n:::\n\n\n\n- **暴力解是「對每個元素往兩側掃」的雙層迴圈** → 強烈暗示可以用單調堆疊優化。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的是「更大」還是「更小」？所以棧該遞增還是遞減？\n2. 我存的是下標還是值？（要算寬度 / 距離就得存下標）\n3. 遇到相等元素時，我的 `<` / `<=` 有沒有避免重複計算？\n\n三題都答得出來，單調堆疊就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1793 | [好子陣列的最大分數](https://leetcode.cn/problems/maximum-score-of-a-good-subarray/) | 1946 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 907 | [子陣列的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums/) | 1976 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n    vector<int> st;\n    for (int x : nums2) {\n      // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1793 | [好子陣列的最大分數](https://leetcode.cn/problems/maximum-score-of-a-good-subarray) | 1946 |\n| 907 | [子陣列的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums) | 1976 |",
        },
        {
          id: 37,
          title: "3.10 滑動視窗",
          summary:
            "**原理講解**\n\n本節要解決的是「在一段連續的子陣列 / 子字串上找答案」的題目：例如「最長的、滿足某條件的子陣列」「最短的、總和達標的子陣列」。\n\n暴力做法是枚舉所有「起點 + 終點」的組合，逐一檢查，那是 `O(n^2)` 甚至 `O(n^3)`。這個技巧叫「滑動視窗（sliding window）」，能把它降到 `O(n)`。\n\n核心想法：**用左右兩個指標框出一段連續區間 `[left, right]`，當作一個「窗口」。窗口在陣列上由左往右滑動，每次只在端點加入或移除一個元素，並增量地更新窗口內的摘要（總和、各字元出現次數等）——不必每次重算整個窗口。**\n\n打個比方：你拿一個固定大小（或可伸縮）的取景框在一排照片上平移。每移動一格，只有一張照片進框、一張出框，框內的內容你「修一下」就好，不用重看全部。\n\n:::example 滑動視窗骨架：右端擴張，左端在不合法時收縮\n最關鍵的前提是「單調性」：當右端往右擴，窗口會變得「更難滿足」或「更容易滿足」某條件，而且這個趨勢是單向的。有了單調性，左端才只需要「往右走、不回頭」。\n\n```cpp\n// 滑動視窗骨架：右端擴張，左端在不合法時收縮\nint left = 0;\nlong long sum = 0;\nfor (int right = 0; right < n; right++) {\n  sum += a[right];        // 右端納入新元素（增量更新摘要）\n  while (sum > target) {  // 窗口不合法 → 收縮左端\n    sum -= a[left];       // 移除左端元素\n    left++;\n  }\n  // 此時 [left, right] 是合法窗口，可在這裡更新答案\n}\n```\n:::\n\n\n\n像「2875 無限陣列的最短子陣列」「2516 每種字元至少取 K 個」都是這個骨架的變形：右擴納入、左縮維持合法、在合法窗口處更新答案。\n\n**實作方式**\n\n:::example 狀態表示：用計數陣列記錄窗口內每個字元出現次數\n**1. 狀態表示：保存窗口內可增可減的摘要。** 視題目而定：總和、某數值的出現次數（用陣列或 map）、不同種類的數量、最大值等。重點是這個摘要必須能「加一個元素」「減一個元素」都很快。\n\n```cpp\n// 狀態表示：用計數陣列記錄窗口內每個字元出現次數\nint cnt[26] = {0};\nint kinds = 0;  // 窗口內不同字元的種類數\n```\n:::\n\n\n\n:::example 更新：右端加入一個字元，維護種類數\n**2. 更新流程：右端擴張納入，左端在需要時收縮移除。** 兩個動作都是「對摘要做增量修改」。\n\n```cpp\n// 更新：右端加入一個字元，維護種類數\nauto add = [&](char c) {\n  if (cnt[c - 'a']++ == 0) {\n    kinds++;  // 從 0 變 1，種類數加一\n  }\n};\nauto remove = [&](char c) {\n  if (--cnt[c - 'a'] == 0) {\n    kinds--;  // 變回 0，種類數減一\n  }\n};\n```\n:::\n\n\n\n:::example 答案讀取：在合法窗口處更新最長長度\n**3. 答案讀取：在每個合法窗口更新答案。** 求最長就取最大的窗口長度，求最短就取最小，計數題則累加合法窗口的數量。\n\n```cpp\n// 答案讀取：在合法窗口處更新最長長度\nans = max(ans, right - left + 1);\n```\n:::\n\n\n\n:::example 恰好 K = 至多 K - 至多 (K-1)，各跑一次滑動視窗\n**4. 一個重要變形：「恰好 K」轉成「至多」相減。** 很多計數題問「恰好 K 個」不好直接滑，但「至多 K 個」很好滑。於是用：恰好 K = 至多 K − 至多 (K−1)。「3298 統計重新排列後包含另一個字串的子字串數目」一類常用此招。\n\n```cpp\n// 恰好 K = 至多 K - 至多 (K-1)，各跑一次滑動視窗\nint exactly = atMost(K) - atMost(K - 1);\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n\n- **必須先確認「單調性」成立**：右端擴張時條件的變化要單向。若陣列含負數、又要對「總和」做大小判斷，往往不單調，就不能直接滑動（要改用前綴和 + 其他結構）。\n- 左右指標各自最多走 n 步，總計 `O(n)`，空間取決於摘要（計數陣列為 `O(1)` 或 `O(字元集)`）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「滑動視窗」想：\n\n- **題目要的是「連續」的子陣列 / 子字串**（不是任意子序列），並求最長、最短或數量。\n- **條件具有單調性**：窗口變長就越難（或越容易）滿足。例如「總和 ≥ target 的最短窗口」「不同字元 ≤ K 的最長窗口」：\n\n:::example 嗅到味道的瞬間：要找「最長/最短的連續區段，滿足某條件」\n```cpp\n// 嗅到味道的瞬間：要找「最長/最短的連續區段，滿足某條件」\n// 且「區段變長 → 條件單向變化」\n// → 別枚舉所有起終點 O(n^2)，改用左右指標滑動 O(n)\n```\n:::\n\n\n\n- **題目問「恰好 K 個 …」的連續子陣列數量** → 想「至多 K − 至多 (K−1)」的轉換。\n- **暴力是雙層迴圈枚舉起點與終點** → 強烈暗示可用滑動視窗。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是「連續區段」嗎？條件對窗口長度有沒有單調性？\n2. 窗口內我要維護什麼摘要？它能不能 `O(1)` 加一個、減一個元素？\n3. 我的左端是不是「只往右、不回頭」？\n\n三題都答得出來，滑動視窗就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3298 | [統計重新排列後包含另一個字串的子字串數目 II](https://leetcode.cn/problems/count-substrings-that-can-be-rearranged-to-contain-a-string-ii/) | 1909 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2875 | [無限陣列的最短子陣列](https://leetcode.cn/problems/minimum-size-subarray-in-infinite-array/) | 1914 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 1574 | [刪除最短的子陣列使剩餘陣列有序](https://leetcode.cn/problems/shortest-subarray-to-be-removed-to-make-array-sorted/) | 1932 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 1000000000 | LCP 68. 美觀的花束 | 1940 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2516 | [每種字元至少取 K 個](https://leetcode.cn/problems/take-k-of-each-character-from-left-and-right/) | 1948 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 3. 無重複字元的最長子串 展示核心寫法。\n\n**完整題目**：給定字串 `s`，回傳不含重複字元的最長子字串長度。\n\n:::example 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass Solution {\n public:\n  int lengthOfLongestSubstring(string s) {\n    // 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n    vector<int> cnt(256);\n    int ans = 0, left = 0;\n    for (int right = 0; right < (int)s.size(); ++right) {\n      cnt[s[right]]++;\n      // 若新字元造成重複，縮小左邊界直到視窗內無重複\n      while (cnt[s[right]] > 1) {\n        cnt[s[left++]]--;\n      }\n      ans = max(ans, right - left + 1);  // 更新無重複子字串最大長度\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 右端加入新元素後，只在違反條件時移動左端；因此每個位置最多進出視窗一次，總複雜度為 O(n)。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n**常見變形**\n\n- 越短越合法：違法時收縮，更新最長。\n- 越長越合法：合法時收縮，更新最短。\n- 恰好型可轉成 `atMost(k)-atMost(k-1)`。\n\n**實作注意事項**\n\n- 先判斷合法性方向，再寫 while 條件。\n- 答案計數時常用 `right-left+1` 或 `left`，兩者語意不同。\n- 頻次表字元集可用陣列，值域大時用 map。\n- 含負數時滑窗的單調性可能失效。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3298 | [統計重新排列後包含另一個字串的子字串數目 II](https://leetcode.cn/problems/count-substrings-that-can-be-rearranged-to-contain-a-string-ii) | 1909 |\n| 2875 | [無限陣列的最短子陣列](https://leetcode.cn/problems/minimum-size-subarray-in-infinite-array) | 1914 |\n| 1574 | [刪除最短的子陣列使剩餘陣列有序](https://leetcode.cn/problems/shortest-subarray-to-be-removed-to-make-array-sorted) | 1932 |\n| 1000000000 | [LCP 68. 美觀的花束](https://leetcode.cn/problems//1GxJYY/) | 1940 |\n| 2516 | [每種字元至少取 K 個](https://leetcode.cn/problems/take-k-of-each-character-from-left-and-right) | 1948 |\n| 2831 | [找出最長等值子陣列](https://leetcode.cn/problems/find-the-longest-equal-subarray) | 1976 |",
        },
        {
          id: 38,
          title: "3.11 字串",
          summary:
            '**原理講解**\n\n本節處理「字串」的進階題：字串匹配、找重複片段、判回文、即時查詢某段是否出現過。這些題的共同特色是——**如果每次比較都從頭一個字一個字比，會很慢；技巧的核心是「重用之前已經比對過的資訊」，避免重複勞動。**\n\n舉個直覺例子：在文章裡找一個樣式字串 "ABCABD"。比到第 6 個字失配時，笨方法是把樣式整個往右挪一格、從頭再比。但其實前面已經比過的 "ABCAB" 裡藏著資訊——它的開頭 "AB" 和結尾 "AB" 相同，所以可以直接跳，不用退回起點。這正是 KMP 演算法的精神。\n\n這套思路有好幾種具體工具，都建立在「預處理出一張表，之後查表 `O(1)`」之上：\n\n- **KMP 的 pi 陣列 / Z 函數**：記錄「每個前綴的最長相同前後綴」，用於快速字串匹配。\n- **字串雜湊（hash）**：把任一子字串壓成一個數字，`O(1)` 比較兩段是否相等。\n- **Trie（字典樹）/ 自動機**：把多個字串塞進一棵樹，沿字元走訪即可即時匹配。\n- **Manacher**：線性時間求每個位置的回文半徑。\n\n:::example 前綴雜湊預處理：h[i] 是前 i 個字元的雜湊，pw 是底數次方\n先看最常用的「前綴雜湊」：預處理後，任意子字串的雜湊值都能 `O(1)` 算出。\n\n```cpp\n// 前綴雜湊預處理：h[i] 是前 i 個字元的雜湊，pw 是底數次方\nconst unsigned long long B = 131;\nvector<unsigned long long> h(n + 1, 0), pw(n + 1, 1);\nfor (int i = 0; i < n; i++) {\n  h[i + 1] = h[i] * B + s[i];  // 把第 i 個字元接到雜湊尾端\n  pw[i + 1] = pw[i] * B;\n}\n```\n:::\n\n\n\n:::example Trie 節點：每個字元一個子指標，end 標記是否為某字串結尾\n「1032 字元流」則是 Trie + 自動機的代表：把多個查詢字串建成一棵樹，再順著輸入字流走，即時判斷有沒有命中某個字串。\n\n```cpp\n// Trie 節點：每個字元一個子指標，end 標記是否為某字串結尾\nstruct Node {\n  Node* nxt[26] = {};\n  bool end = false;\n};\n// 插入字串：沿字元逐層往下建節點，最後一層標 end\n```\n:::\n\n\n\n**實作方式**\n\n:::example 狀態表示：用前綴雜湊取任意子串 [l, r] 的雜湊值（O(1)）\n**1. 狀態表示：依方法選對要預處理的結構。** 匹配用 pi / Z 陣列；比較任意子串用前綴雜湊；多字串查詢用 Trie；回文用半徑陣列。\n\n```cpp\n// 狀態表示：用前綴雜湊取任意子串 [l, r] 的雜湊值（O(1)）\nauto sub = [&](int l, int r) {  // 閉區間 [l, r]\n  return h[r + 1] - h[l] * pw[r - l + 1];\n};\n```\n:::\n\n\n\n:::example 更新：字元流到來時，沿 Trie / 自動機走一步（1032 思路）\n**2. 更新流程：按字元掃描，邊掃邊維護當前狀態。** 維護的可能是「目前已匹配的長度」「最右匹配區間」「雜湊前綴」或「自動機目前停在哪個節點」。\n\n```cpp\n// 更新：字元流到來時，沿 Trie / 自動機走一步（1032 思路）\ncur = cur->nxt[c - \'a\'] ? cur->nxt[c - \'a\'] : root;  // 走得通就走，否則回退\nbool matched = (cur && cur->end);                    // 走到某字串結尾 → 命中\n```\n:::\n\n\n\n:::example 答案讀取：判斷兩段子串是否相等（前綴雜湊，O(1)）\n**3. 答案讀取：由預處理狀態直接取。** 匹配位置、最長公共前綴（LCP）、回文長度、兩子串是否相等，全都能 `O(1)` 或均攤 `O(1)` 得到。\n\n```cpp\n// 答案讀取：判斷兩段子串是否相等（前綴雜湊，O(1)）\nbool equalSeg(int l1, int l2, int len) { return sub(l1, l1 + len - 1) == sub(l2, l2 + len - 1); }\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- **雜湊有碰撞風險**：不同字串可能算出相同雜湊。怕碰撞時可用雙雜湊（兩組底數 / 模數）降低機率，或在關鍵處再逐字驗證一次。\n- KMP / Z 的「失配回退」邊界很容易寫錯，下標 +1 / −1 要想清楚；Manacher 算出的半徑要換算回原字串長度，建議統一一種寫法背熟。\n- 複雜度：預處理多為 `O(n)`，之後每次查詢 `O(1)` 或均攤 `O(1)`；Trie 的空間是 `O(總字元數 × 字元集)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「字串技巧」想：\n\n- **要在一個大字串裡反覆找樣式、或反覆比較兩段子串是否相等** → 前綴雜湊或 KMP。\n- **同時要匹配很多個字串、或處理「字元一個一個進來」的串流** → Trie / 自動機，如「1032 字元流」：\n\n:::example 嗅到味道的瞬間：要對「同一個字串」反覆做子串比較 / 匹配\n```cpp\n// 嗅到味道的瞬間：要對「同一個字串」反覆做子串比較 / 匹配\n// 暴力是每次都重新逐字比 O(n) 每次\n// → 預處理一次（hash / pi / trie），之後每次查詢降到 O(1) 均攤\n```\n:::\n\n\n\n- **題目和「回文」「最長公共前綴 / 後綴」「重複子串」有關** → Manacher、Z 函數、後綴結構。\n- **暴力是「對每對位置都從頭逐字比較」** → 強烈暗示要用預處理重用資訊。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我重複在做的「逐字比較」能不能預處理成一張表，之後查表？\n2. 這題是單一樣式匹配（KMP）、任意子串比較（hash）、還是多字串 / 串流（Trie）？\n3. 如果用雜湊，我有沒有防範碰撞（雙雜湊或事後驗證）？\n\n三題都答得出來，字串題就抓對工具了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1032 | [字元流](https://leetcode.cn/problems/stream-of-characters/) | 1970 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\n public:\n  // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n  int strStr(string haystack, string needle) {\n    int m = needle.size();\n    vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n    // 建構 pi 陣列\n    for (int i = 1; i < m; ++i) {\n      int j = pi[i - 1];\n      // 若失配，利用 pi 回退到上一個可能匹配的位置\n      while (j && needle[i] != needle[j]) {\n        j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n      }\n      if (needle[i] == needle[j]) {\n        j++;\n      }\n      pi[i] = j;\n    }\n    // 在 haystack 中用 pi 輔助匹配\n    for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n      while (j && haystack[i] != needle[j]) {\n        j = pi[j - 1];\n      }\n      if (haystack[i] == needle[j]) {\n        j++;\n      }\n      if (j == m) {  // 完整匹配，返回起始位置\n        return i - m + 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n**常見變形**\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n**實作注意事項**\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1032 | [字元流](https://leetcode.cn/problems//stream-of-characters/) | 1970 |',
        },
        {
          id: 39,
          title: "3.12 樹和二元樹",
          summary:
            "**原理講解**\n\n本節處理「樹和二元樹」的題目：資料長成一棵樹（有一個根、每個節點有若干子節點），要你算高度、路徑和、最大子樹、最近共同祖先（LCA）之類的東西。\n\n樹題最核心的工具是 DFS（深度優先搜尋）——順著一條路一直往下走到底，再回頭。配合 DFS，資訊在樹上有兩個流動方向：\n\n- **自底向上（彙總子樹）**：先算好每個子節點的答案，再把它們合併成「我這棵子樹」的答案，回傳給父親。例如算高度、子樹和。\n- **自頂向下（攜帶祖先狀態）**：從根往下走時，把「一路累積的資訊」當參數帶下去。例如帶著「從根到我的路徑和」。\n\n:::example 自底向上：dfs 回傳「以 node 為根的子樹」的摘要（這裡是高度）\n絕大多數樹題都是「設計每個節點要回傳什麼摘要」這件事。先看自底向上的骨架：\n\n```cpp\n// 自底向上：dfs 回傳「以 node 為根的子樹」的摘要（這裡是高度）\nint dfs(TreeNode* node) {\n  if (!node) {\n    return 0;  // 空樹高度 0\n  }\n  int lh = dfs(node->left);   // 先算左子樹\n  int rh = dfs(node->right);  // 再算右子樹\n  return max(lh, rh) + 1;     // 合併：我的高度 = 較高子樹 + 1\n}\n```\n:::\n\n\n\n:::example 邊算高度，邊更新「經過某節點的最長路徑」\n很多題的答案不是直接回傳值，而是「在合併過程中順手更新一個全域最佳值」。例如求樹的直徑（最長路徑）：\n\n```cpp\n// 邊算高度，邊更新「經過某節點的最長路徑」\nint best = 0;\nint height(TreeNode* node) {\n  if (!node) {\n    return 0;\n  }\n  int lh = height(node->left), rh = height(node->right);\n  best = max(best, lh + rh);  // 經過 node 的路徑長 = 左高 + 右高\n  return max(lh, rh) + 1;     // 回傳給父親的仍是「向上的高度」\n}\n```\n:::\n\n\n\n注意上面這個關鍵區分：**回傳給父親的值（向上延伸的單邊長度）** 和 **拿來更新答案的值（在此節點閉合的完整路徑）** 往往不一樣——這是樹形 DP 最常見的設計，務必分清楚。\n\n**實作方式**\n\n:::example 狀態表示：用 struct 一次回傳多個子樹摘要（如 1373 需要的資訊）\n**1. 狀態表示：想清楚每個節點要回傳什麼摘要。** 可能是高度、子樹和、是否平衡、子樹節點數、或一組 DP 值（常用一個小 struct 包起來）。\n\n```cpp\n// 狀態表示：用 struct 一次回傳多個子樹摘要（如 1373 需要的資訊）\nstruct Info {\n  bool isBST;      // 子樹是不是合法 BST\n  int minV, maxV;  // 子樹的最小 / 最大值\n  int sum;         // 子樹鍵值總和\n};\n```\n:::\n\n\n\n「1373 二叉搜尋子樹的最大鍵值和」就是靠每個節點回傳這組 Info，父節點才能判斷自己是不是 BST，並更新最大和。\n\n:::example 更新：先遞迴子節點，拿到結果後合併出自己的 Info\n**2. 更新流程：先遞迴處理子節點，再合併。** 需要「父側 / 祖先」資訊時，再做第二次 DFS（換根）或倍增預處理（LCA）。\n\n```cpp\n// 更新：先遞迴子節點，拿到結果後合併出自己的 Info\nInfo L = dfs(node->left), R = dfs(node->right);\nbool ok = L.isBST && R.isBST && node->val > L.maxV && node->val < R.minV;  // BST 條件\n```\n:::\n\n\n\n「2049 統計最高分的節點數目」也是同型：先算每棵子樹的大小，刪掉某節點後各塊大小都能由「子樹大小」與「總數 − 子樹大小」算出。\n\n:::example 答案讀取：通常用全域變數承接過程中的最佳值\n**3. 答案讀取：答案可能藏在不同地方。** 根的回傳值、一個全域變數、每個節點的 DP 陣列，或查詢時的祖先跳躍結果。\n\n```cpp\n// 答案讀取：通常用全域變數承接過程中的最佳值\nint ans = INT_MIN;\ndfs(root);  // dfs 內部沿途更新 ans\nreturn ans;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- **遞迴深度可能很大**：一條鏈狀的樹會讓遞迴疊很深而爆 stack；題目給的樹很深時要留意（必要時改成迭代或加深限制）。\n- **分清「向父延伸值」與「閉合答案」**：如直徑、最大路徑和、換根 DP，這兩者混用是經典 bug。\n- 複雜度：一次 DFS 走訪每個節點一次，為 `O(n)`；LCA 倍增預處理 `O(n log n)`、每次查詢 `O(log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往「樹 DFS / 樹形 DP」想：\n\n- **輸入是一棵二元樹或一般樹（給 root，或給邊建鄰接表）**，要算和子樹、路徑、祖先有關的量。\n- **「某節點的答案」可以由「它各個子節點的答案」拼出來** → 自底向上彙總，設計回傳摘要：\n\n:::example 嗅到味道的瞬間：問題在子樹上有遞迴結構\n```cpp\n// 嗅到味道的瞬間：問題在子樹上有遞迴結構\n// 「我的答案 = f(左子樹答案, 右子樹答案, 我自己)」\n// → 設計 dfs 回傳子樹摘要，自底向上合併\n```\n:::\n\n\n\n- **要找最近共同祖先、或反覆查詢兩節點關係** → LCA（倍增 / 歐拉序）。\n- **答案要對「每個節點當根」都算一次** → 換根 DP（兩次 DFS）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 dfs 對每個節點「回傳什麼摘要」？這個摘要夠父親合併用嗎？\n2. 我「回傳給父親的值」和「用來更新答案的值」是不是同一個？（常常不是）\n3. 我需要祖先 / 父側資訊嗎？需要的話要不要第二次 DFS 或倍增？\n\n三題都答得出來，樹題就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2049 | [統計最高分的節點數目](https://leetcode.cn/problems/count-nodes-with-the-highest-score/) | 1912 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1373 | [二叉搜尋子樹的最大鍵值和](https://leetcode.cn/problems/maximum-sum-bst-in-binary-tree/) | 1914 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems/minimum-increments-to-equalize-leaf-paths/) | 1959 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2872 | [可以被 K 整除連通塊的最大數目](https://leetcode.cn/problems/maximum-number-of-k-divisible-components/) | 1968 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2151 | [基於陳述統計最多好人數](https://leetcode.cn/problems/maximum-good-people-based-on-statements/) | 1980 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2049 | [統計最高分的節點數目](https://leetcode.cn/problems//count-nodes-with-the-highest-score/) | 1912 |\n| 1373 | [二叉搜尋子樹的最大鍵值和](https://leetcode.cn/problems//maximum-sum-bst-in-binary-tree/) | 1914 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems//minimum-increments-to-equalize-leaf-paths/) | 1959 |\n| 2872 | [可以被 K 整除連通塊的最大數目](https://leetcode.cn/problems//maximum-number-of-k-divisible-components/) | 1968 |\n| 2151 | [基於陳述統計最多好人數](https://leetcode.cn/problems//maximum-good-people-based-on-statements/) | 1980 |",
        },
      ],
    },
    {
      id: 40,
      title: "4. Phase 4：衝刺 2100 穩定輸出（2000-2100）",
      summary:
        "最後一段專注於高強度綜合題。建議採用虛擬競賽節奏：先在 20 分鐘內定方向，再用 40-60 分鐘完成實作；賽後整理錯題時，重點不是背答案，而是提煉可複用的決策規則。",
      children: [
        {
          id: 41,
          title: "4.1 二分搜尋",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目要你求「最大的最小值」「最小的最大值」，或是「滿足某條件的最大／最小值是多少」。這類問題直接去想「最佳值剛好是多少」往往很難，但反過來問「某個猜測值 `x` 行不行得通」卻很容易判斷。這就是「二分答案（binary search on the answer）」。\n\n:::example 答案的單調性：true 一段、false 一段，中間只有一個分界\n先看清楚一個關鍵性質：可行與不可行之間有一條「分界線」。以「禮盒的最大甜蜜度」(2517) 為例，甜蜜度定義為任選 `k` 顆糖中價格差距的最小值，我們想讓它越大越好。如果「甜蜜度能達到 5」成立，那「能達到 4、3、2」一定也成立（要求更鬆）；如果「能達到 10」不成立，那「11、12」也不成立。把每個候選值是否可行排成一列，會長這樣：\n\n```cpp\n// 答案的單調性：true 一段、false 一段，中間只有一個分界\n// 候選值 x:  2    3    4    5    6    7\n// 可行 ?  : true true true true false false   ← 我們要找最後一個 true\n```\n:::\n\n\n\n只要可行性具備這種「一段 true 接一段 false」（或反過來）的單調性，就能用二分法在 `O(log(值域))` 次猜測內找到分界，而不必把每個候選值都試一遍。\n\n:::example check(x)：假設答案至少是 x，能不能做到？（細節依題目而定）\n核心想法只有一句話：**不要直接求最佳值，而是寫一個 `check(x)` 判斷「`x` 這個目標達不達得到」，再用二分法去逼近那條 true/false 分界。**\n\n```cpp\n// check(x)：假設答案至少是 x，能不能做到？（細節依題目而定）\nbool check(int x) {\n  // 依題意貪心或計數，回傳「目標值 x 可行嗎」\n  return feasible_under(x);\n}\n```\n:::\n\n\n\n每次 `check` 通常是一次 `O(n)` 的掃描或計數，配上 `O(log)` 次二分，總複雜度多半是 `O(n log U)`（`U` 是值域大小），遠快過逐一枚舉。\n\n**實作方式**\n\n把任何二分答案題拆成三步，逐一搭配程式碼：\n\n**1. 定義候選值 `x` 的語意，並確認單調性。** 先講清楚「`x` 代表什麼、check 在判斷什麼」。例如「打家劫舍 IV」(2560)：`x` 是「容許的最大金額」，`check(x)` 問「只偷金額 ≤ `x` 的房子，能不能湊滿 `k` 間」。`x` 越大越容易湊滿，單調成立。\n\n:::example check：能否選出至少 k 間「金額 <= x」且不相鄰的房子\n**2. 寫出 `check(x)`。** 這是全題重心，通常是一次貪心或計數掃描。\n\n```cpp\n// check：能否選出至少 k 間「金額 <= x」且不相鄰的房子\nbool check(const vector<int>& nums, int x, int k) {\n  int cnt = 0;\n  for (int i = 0; i < (int)nums.size(); ++i) {\n    if (nums[i] <= x) {  // 這間可偷\n      ++cnt;\n      ++i;  // 跳過相鄰的下一間\n    }\n  }\n  return cnt >= k;\n}\n```\n:::\n\n\n\n:::example 在 [lo, hi] 上找最小的可行 x\n**3. 套二分模板找分界。** 用左閉右閉區間，邊界判斷決定要「第一個 true」還是「最後一個 true」。下面是「找最小可行 `x`」的寫法：\n\n```cpp\n// 在 [lo, hi] 上找最小的可行 x\nint lo = minVal, hi = maxVal, ans = hi;\nwhile (lo <= hi) {\n  int mid = lo + (hi - lo) / 2;  // 防溢位\n  if (check(mid)) {              // 可行 → 試著更小\n    ans = mid;\n    hi = mid - 1;\n  } else {  // 不可行 → 必須更大\n    lo = mid + 1;\n  }\n}\n// ans 即為最小可行值\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- `mid` 用 `lo + (hi - lo) / 2`，避免 `lo + hi` 溢位。\n- 想清楚要「第一個 true」還是「最後一個 true」：求「最大的最小值」是找最後一個 true，求「最小的最大值」是找第一個 true。寫反就會差一格。\n- 初始 `lo / hi` 要把整個合法值域涵蓋進去，漏掉端點會找不到答案。\n- 複雜度：`check` 是 `O(n)`，二分 `O(log U)`，合計 `O(n log U)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「二分答案」想：\n\n- **題目在求「最大的最小」或「最小的最大」。** 像「禮盒的最大甜蜜度」(2517) 要最大化「最小差距」，這種對偶字眼幾乎是二分答案的招牌。\n- **直接求最佳值很難，但「給定一個目標值，判斷可不可行」很容易。** 這正是這個技巧存在的理由：把「最佳化」換成一連串「是非題」。\n- **答案具有單調性：目標放寬就更容易成立，收緊就更難。** 例如「醜數 III」(1201) 中「不超過 `x` 的醜數有幾個」隨 `x` 遞增，可直接二分定位第 `k` 個。\n- **暴力做法是把每個候選值都試一遍（值域很大時 `O(U·n)` 太慢）；** 二分答案把它壓成 `O(n log U)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我二分的「候選值 `x`」具體代表什麼？\n2. 我的 `check(x)` 在判斷什麼？它為什麼隨 `x` 單調？\n3. 我要的是第一個 true 還是最後一個 true，邊界寫對了嗎？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 911 | [線上選舉](https://leetcode.cn/problems/online-election/) | 2001 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2517 | [禮盒的最大甜蜜度](https://leetcode.cn/problems/maximum-tastiness-of-candy-basket/) | 2021 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1201 | [醜數 III](https://leetcode.cn/problems/ugly-number-iii/) | 2039 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1648 | [銷售價值減少的顏色球](https://leetcode.cn/problems/sell-diminishing-valued-colored-balls/) | 2050 | 把逐顆賣球改成價格層門檻：先二分最後會賣到的價格層，再用等差級數一次加總收益。 |\n| 2560 | [打家劫舍 IV](https://leetcode.cn/problems/house-robber-iv/) | 2081 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n:::example 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  // 標準二分搜尋：在有序陣列中尋找 target，找到回傳下標，否則回傳 -1\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;  // 閉區間 [l, r]\n    while (l <= r) {\n      int mid = l + (r - l) / 2;  // 取中點，避免 l+r 溢位\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;  // 目標在右半段\n      } else {\n        r = mid - 1;  // 目標在左半段\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 911 | [線上選舉](https://leetcode.cn/problems/online-election) | 2001 |\n| 2517 | [禮盒的最大甜蜜度](https://leetcode.cn/problems/maximum-tastiness-of-candy-basket) | 2021 |\n| 1201 | [醜數 III](https://leetcode.cn/problems/ugly-number-iii) | 2039 |\n| 1648 | [銷售價值減少的顏色球](https://leetcode.cn/problems/sell-diminishing-valued-colored-balls) | 2050 |\n| 2560 | [打家劫舍 IV](https://leetcode.cn/problems/house-robber-iv) | 2081 |",
        },
        {
          id: 42,
          title: "4.2 位元運算",
          summary:
            "**原理講解**\n\n本節的核心觀念是：把整數不要當成一個「值」，而是當成一排「位元（bit）」。很多看似複雜的整數題，一旦拆到位元層級，每一位都可以「各自獨立處理」，難題就被切成許多簡單小題。\n\n:::example 三個最常用的位運算（對每一位獨立作用）\n先複習工具。一個整數在電腦裡是一串 0/1，位運算就是逐位操作：\n\n```cpp\n// 三個最常用的位運算（對每一位獨立作用）\nint a = 0b1100, b = 0b1010;\nint x = a ^ b;  // XOR：兩位不同才是 1 → 0b0110\nint y = a & b;  // AND：兩位都 1 才是 1 → 0b1000\nint z = a | b;  // OR ：任一位是 1 就是 1 → 0b1110\n```\n:::\n\n\n\n最關鍵的兩個性質要記牢：\n\n- **XOR 可逆**：`a ^ b ^ b == a`，一個數 XOR 自己等於 0。許多「還原序列」的題目就靠這點，例如「解碼異或後的排列」(1734) 用前綴 XOR 與全體 XOR 把原排列倒推回來。\n- **位元獨立**：第 `i` 位的結果只跟所有數字的第 `i` 位有關，跟其他位無關。於是可以「一位一位地統計或決策」。\n\n:::example 取出 x 的第 i 位（最低位是第 0 位）\n```cpp\n// 取出 x 的第 i 位（最低位是第 0 位）\nint bit = (x >> i) & 1;  // 結果是 0 或 1\n```\n:::\n\n\n\n核心想法只有一句話：**遇到整數的全域條件，先問「能不能拆成每一位各自處理」；能拆，就逐位統計貢獻或逐位貪心。**\n\n:::example 計算 x 的二進位裡有幾個 1\n另一個常見小工具是 `popcount`（數一個整數有幾個 1），像「優質數對的數目」(2354) 就需要它：\n\n```cpp\n// 計算 x 的二進位裡有幾個 1\nint ones = __builtin_popcount(x);  // GCC/Clang 內建，O(1)\n```\n:::\n\n\n\n逐位掃描通常是 `O(n · B)`（`B` 約 30 或 60 位），對 `int`/`long long` 來說 `B` 是常數，所以幾乎等同線性。\n\n**實作方式**\n\n把位元題拆成下面幾種固定套路，逐一搭配程式碼：\n\n:::example 逐位統計：算出每一位上「1 的個數」\n**1. 逐位統計貢獻。** 對每一位獨立計算它對答案貢獻多少，再乘上權值 `1<<i` 加總。\n\n```cpp\n// 逐位統計：算出每一位上「1 的個數」\nint cnt[32] = {0};\nfor (int x : nums) {\n  for (int i = 0; i < 32; ++i) {\n    cnt[i] += (x >> i) & 1;  // 第 i 位是 1 就累加\n  }\n}\n```\n:::\n\n\n\n:::example 前綴 XOR：prefix[i] = nums[0] ^ ... ^ nums[i-1]\n**2. 利用 XOR 可逆做還原 / 配對。** 前綴 XOR、全體 XOR 互相抵銷，是還原型題目的主力。\n\n```cpp\n// 前綴 XOR：prefix[i] = nums[0] ^ ... ^ nums[i-1]\nvector<int> prefix(n + 1, 0);\nfor (int i = 0; i < n; ++i) {\n  prefix[i + 1] = prefix[i] ^ nums[i];\n}\n// 區間 [l, r] 的 XOR = prefix[r+1] ^ prefix[l]\n```\n:::\n\n\n\n:::example 高位優先貪心的骨架：從高位往低位決定答案每一位\n**3. 從高位到低位貪心試填。** 求「最大 XOR」這類題，從最高位開始，能讓該位變 1 就讓它變 1（例如「子序列最大 XOR 值」(3681) 配合線性基）。\n\n```cpp\n// 高位優先貪心的骨架：從高位往低位決定答案每一位\nint ans = 0;\nfor (int i = B - 1; i >= 0; --i) {\n  if (canMakeBitOne(ans, i)) {  // 這一位能變 1 嗎？\n    ans |= (1 << i);            // 能就拿下這一位\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 位移前注意型別寬度：操作第 31 / 63 位，或處理負數時，改用 `unsigned` 或 `long long`，否則 `1 << 31` 會未定義行為。\n- 確認位元真的「獨立」再逐位處理；像子陣列 AND 等於 K (3209) 這種帶區間限制的，往往要結合別的技巧而非單純逐位。\n- 複雜度：逐位掃描 `O(n · B)`，`B` 為常數，實務上近似 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「位元運算」想：\n\n- **題目大量出現 XOR / AND / OR，或要你還原一個被 XOR 過的序列。** 「解碼異或後的排列」(1734) 就是典型的可逆還原題。\n- **答案或條件可以「一位一位分開看」。** 若第 `i` 位的結果只受各數的第 `i` 位影響，幾乎可以斷定要逐位處理。\n- **題目跟「二進位中 1 的個數（popcount）」有關。** 像「優質數對的數目」(2354) 直接以 popcount 入題。\n- **暴力做法是枚舉所有數對或子集逐一算（常是 `O(n^2)` 以上）；** 逐位統計或線性基能把它壓到 `O(n · B)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能把條件拆成「每一位各自的判斷」？\n2. 我用到的是 XOR 的可逆性，還是逐位統計、還是高位貪心？\n3. 我的型別寬度夠嗎？最高位與負數會不會出錯？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1734 | [解碼異或後的排列](https://leetcode.cn/problems/decode-xored-permutation/) | 2024 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 3681 | [子序列最大 XOR 值](https://leetcode.cn/problems/maximum-xor-of-subsequences/) | 2027 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 3209 | [子陣列按位與值為 K 的數目](https://leetcode.cn/problems/number-of-subarrays-with-and-value-of-k/) | 2050 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 3782 | [交替刪除操作後最後剩下的整數](https://leetcode.cn/problems/last-remaining-integer-after-alternating-deletion-operations/) | 2074 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n| 2354 | [優質數對的數目](https://leetcode.cn/problems/number-of-excellent-pairs/) | 2076 | 要拆成位元看；練習判斷 XOR/AND/OR 的每一位能否獨立統計或貪心決策。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 1310. 子陣列異或查詢 展示核心寫法。\n\n**完整題目**：給定陣列與多個 `[l, r]` 查詢，回傳每段子陣列的 XOR 值。\n\n:::example 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR…\n**線索**：題目出現 XOR/AND/OR、mask 或二進位；先判斷每一位是否能獨立統計或從高位貪心。\n\n```cpp\nclass Solution {\n public:\n  // 前綴 XOR：利用 pre[r+1] ^ pre[l] 求區間 [l,r] 的 XOR 值\n  vector<int> xorQueries(vector<int>& arr, vector<vector<int>>& queries) {\n    vector<int> pre(arr.size() + 1);  // pre[i] 為 arr[0..i-1] 的 XOR 前綴\n    for (int i = 0; i < (int)arr.size(); ++i) {\n      pre[i + 1] = pre[i] ^ arr[i];\n    }\n    vector<int> ans;\n    for (auto& q : queries) {\n      ans.push_back(pre[q[1] + 1] ^ pre[q[0]]);  // 區間 [q[0], q[1]] 的 XOR\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- XOR 具有自反性 `x ^ x = 0`，因此區間 XOR 可用前綴 XOR 相消。\n- 程式把整數轉成位元狀態或前綴 XOR，利用位元運算的可逆性/獨立性更新答案。\n- 涉及高位貪心時，迴圈通常從最高位往低位試，因為高位決定主要大小關係。\n\n**常見變形**\n\n- 前綴 XOR 處理區間異或。\n- 試填最高位處理最大 XOR/最大值。\n- SOS DP 處理子集包含關係。\n\n**實作注意事項**\n\n- 位移用 `1LL << b` 避免溢位。\n- 負數右移與符號位要小心。\n- 按位貢獻要確認每位獨立。\n- `__builtin_popcountll` 對 long long 使用 ll 版本。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1734 | [解碼異或後的排列](https://leetcode.cn/problems/decode-xored-permutation) | 2024 |\n| 3681 | [子序列最大 XOR 值](https://leetcode.cn/problems/maximum-xor-of-subsequences) | 2027 |\n| 3209 | [子陣列按位與值為 K 的數目](https://leetcode.cn/problems/number-of-subarrays-with-and-value-of-k) | 2050 |\n| 3782 | [交替刪除操作後最後剩下的整數](https://leetcode.cn/problems/last-remaining-integer-after-alternating-deletion-operations) | 2074 |\n| 2354 | [優質數對的數目](https://leetcode.cn/problems/number-of-excellent-pairs) | 2076 |\n| 2857 | [統計距離為 k 的點對](https://leetcode.cn/problems/count-pairs-of-points-with-distance-k) | 2082 |\n| 982 | [按位與為零的三元組](https://leetcode.cn/problems/triples-with-bitwise-and-equal-to-zero) | 2085 |",
        },
        {
          id: 43,
          title: "4.3 資料結構",
          summary:
            "**原理講解**\n\n本節談的不是某一個固定演算法，而是一種解題姿勢：**先看清楚「哪些量會一直變、哪些量需要被快速查詢」，再挑一個合適的資料結構把它們維護起來。** 選對結構，題目就從「每次都重算」變成「每次只做局部更新」。\n\n實務上最常用到的兩個工具是「堆（heap）」和「並查集（union-find）」，先各看一眼。\n\n:::example 堆：隨時取最小值（小根堆）\n堆能隨時 `O(log n)` 取出目前最大或最小值，適合「不斷有新東西進來，且要反覆取極值」的場景，例如「最多可以參加的會議數目」(1353) 就用小根堆維護「目前還能參加、最早結束的會議」：\n\n```cpp\n// 堆：隨時取最小值（小根堆）\npriority_queue<int, vector<int>, greater<int>> pq;\npq.push(5);\npq.push(2);\nint mn = pq.top();  // 2：目前最小，O(1)\npq.pop();           // 移除最小，O(log n)\n```\n:::\n\n\n\n:::example 並查集：找代表元（路徑壓縮）\n並查集能快速回答「這兩個元素現在屬不屬於同一群」，並把兩群合併，適合「動態維護連通關係」，像「找出知曉祕密的所有專家」(2092) 把同一時刻開會的人合併成一群、後面又可能拆開：\n\n```cpp\n// 並查集：找代表元（路徑壓縮）\nint find(vector<int>& fa, int x) {\n  while (fa[x] != x) {\n    fa[x] = fa[fa[x]];  // 路徑壓縮，攤平樹高\n    x = fa[x];\n  }\n  return x;\n}\n```\n:::\n\n\n\n核心想法只有一句話：**先回答「我需要對資料做哪種查詢與更新」，再用那個查詢／更新最快的結構去支撐它**——取極值找堆、查連通找並查集、查有序前驅後繼找有序集合。\n\n選對之後，原本一次 `O(n)` 的暴力查詢往往降到 `O(log n)`，整題從 `O(n^2)` 變 `O(n log n)`。\n\n**實作方式**\n\n把這類題拆成下面四步，逐一搭配程式碼：\n\n**1. 列出「會變的量」與「要查的量」。** 先寫下來：輸入裡哪些值會被新增 / 刪除 / 修改？我每一步需要快速問出什麼？這一步決定了該選哪個結構。\n\n:::example 對照表：需求 → 結構\n**2. 依查詢型態挑結構。**\n\n```cpp\n// 對照表：需求 → 結構\n// 反覆取最大/最小值        -> priority_queue（堆）\n// 動態合併、查同群         -> 並查集\n// 查有序鄰居 / 前驅後繼     -> set / map（紅黑樹）\n// 計數、查某鍵是否存在      -> unordered_map\n```\n:::\n\n\n\n:::example 會議按開始時間排序後，逐天處理\n**3. 按自然順序掃描，每步只做局部更新。** 多半先排序（按時間、座標或代價），再逐一處理，只更新與當前元素相關的狀態。以會議題為例：\n\n```cpp\n// 會議按開始時間排序後，逐天處理\n// 1) 把今天開放的會議丟進小根堆（依結束時間）\n// 2) 丟掉已過期（結束時間 < 今天）的堆頂\n// 3) 參加堆頂那場最早結束的會議，ans++，pop\n```\n:::\n\n\n\n:::example 並查集合併兩群（把 x 的根掛到 y 的根下）\n**4. 在不變式成立的位置讀答案，並注意邊界與複雜度。**\n\n```cpp\n// 並查集合併兩群（把 x 的根掛到 y 的根下）\nvoid unite(vector<int>& fa, int x, int y) { fa[find(fa, x)] = find(fa, y); }\n```\n:::\n\n\n\n- 用堆時，過期 / 失效的元素記得在讀取前清掉，否則 `top()` 可能是無效資料。\n- 用並查集前確認關係只「合併」不「真的拆開」；若需要反覆拆開，並查集通常不適用，要換思路。\n- 複雜度：堆每次操作 `O(log n)`，並查集近乎 `O(1)`（反阿克曼），整題多為 `O(n log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「挑一個資料結構來維護狀態」想：\n\n- **資料會動態變化（新增 / 刪除 / 合併），又要反覆查詢某種摘要值。** 只查一次用排序就好；反覆查才值得上資料結構。\n- **需求是「取極值」「查同群」「查有序鄰居」其中之一。** 這三種需求分別精準對應堆、並查集、有序集合。像「最多可以參加的會議數目」(1353) 的「目前最早結束」就是取極值。\n- **暴力做法是每次查詢都重掃全部資料（一次 `O(n)`），整體 `O(n^2)` 太慢；** 換成合適結構可把單次查詢降到 `O(log n)`。\n- **題目同時牽涉「時間 / 座標排序」與「即時查詢」，** 通常是「排序掃描 + 一個維護結構」的組合拳。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的資料裡，哪些量會變、哪些量要被反覆查詢？\n2. 我的查詢屬於「取極值 / 查連通 / 查有序鄰居」哪一種？\n3. 我選的結構，單次更新與查詢各是 `O(?)`？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2092 | [找出知曉祕密的所有專家](https://leetcode.cn/problems/find-all-people-with-secret/) | 2004 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 1354 | [多次求和構造目標陣列](https://leetcode.cn/problems/construct-target-array-with-multiple-sums/) | 2015 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 1353 | [最多可以參加的會議數目](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended/) | 2016 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 2547 | [拆分陣列的最小代價](https://leetcode.cn/problems/minimum-cost-to-split-an-array/) | 2020 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n| 3027 | [人員站位的方案數 II](https://leetcode.cn/problems/find-the-number-of-ways-to-place-people-ii/) | 2020 | 請套用「資料結構」的狀態、不變式與實作方式，先寫出資料表示、更新流程與答案讀取規則。 |\n\n**C++ 範例講解**\n\n以下用 1353. 最多可以參加的會議數目 展示資料結構（貪心 + 堆）的核心寫法。\n\n**完整題目**：給若干會議 `events[i] = [startDay, endDay]`，你在每一天最多參加一場會議，且只要在 `[startDay, endDay]` 範圍內任一天皆可參加。求最多能參加幾場會議。\n\n:::example 貪心：按起始日排序，每天優先選結束最早的可參加活動\n**線索**：逐天推進，貪心地「在每一天，參加目前可參加且結束最早的會議」。用最小堆按結束日維護當天所有開放會議，過期的清掉，這樣能把快截止的會議優先消化掉。\n\n```cpp\nclass Solution {\n public:\n  int maxEvents(vector<vector<int>>& events) {\n    // 貪心：按起始日排序，每天優先選結束最早的可參加活動\n    sort(events.begin(), events.end());\n    priority_queue<int, vector<int>, greater<>> pq;  // 最小堆，存尚可參加活動的結束日\n    int i = 0, n = events.size(), ans = 0, maxDay = 0;\n    for (auto& e : events) {\n      maxDay = max(maxDay, e[1]);  // 找到最晚的結束日，作為模擬上限\n    }\n    for (int day = 1; day <= maxDay; ++day) {\n      // 將今天開始的所有活動加入候選堆\n      while (i < n && events[i][0] == day) {\n        pq.push(events[i][1]);\n        ++i;\n      }\n      // 移除已過期（結束日 < 今天）的活動\n      while (!pq.empty() && pq.top() < day) {\n        pq.pop();\n      }\n      // 貪心選擇結束日最早的活動參加\n      if (!pq.empty()) {\n        pq.pop();\n        ++ans;\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 會議依開始日排序，方便按天加入當天開放的會議。\n- 最小堆以結束日為鍵，堆頂是最快截止、最該優先參加者。\n- 每天先把今天開放的會議入堆，再把已過期（結束日 < 今天）的清掉。\n- 若堆非空，就參加堆頂（結束最早）那場，答案加一。\n\n**常見變形**\n\n- 課程表 III（630）：用堆做反悔式的截止排程。\n- 任務截止排程：最早截止優先（EDF）的通用貪心。\n- 區間點覆蓋：對每個位置選最緊迫的區間。\n\n**實作注意事項**\n\n- 「結束最早優先」是貪心正確性的核心，不能改成開始最早。\n- 過期會議要在取用前清掉，否則會誤參加已結束的會議。\n- 以天為迴圈時要先算出 `maxDay`，避免漏掉尾段會議。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2092 | [找出知曉祕密的所有專家](https://leetcode.cn/problems/find-all-people-with-secret) | 2004 |\n| 1354 | [多次求和構造目標陣列](https://leetcode.cn/problems/construct-target-array-with-multiple-sums) | 2015 |\n| 1353 | [最多可以參加的會議數目](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended) | 2016 |\n| 2547 | [拆分陣列的最小代價](https://leetcode.cn/problems/minimum-cost-to-split-an-array) | 2020 |\n| 3027 | [人員站位的方案數 II](https://leetcode.cn/problems/find-the-number-of-ways-to-place-people-ii) | 2020 |\n| 2251 | [花期內花的數目](https://leetcode.cn/problems/number-of-flowers-in-full-bloom) | 2022 |\n| 1235 | [規劃兼職工作](https://leetcode.cn/problems/maximum-profit-in-job-scheduling) | 2023 |\n| 1626 | [無矛盾的最佳球隊](https://leetcode.cn/problems/best-team-with-no-conflicts) | 2027 |\n| 895 | [最大頻率堆疊](https://leetcode.cn/problems/maximum-frequency-stack) | 2028 |\n| 2772 | [使陣列中的所有元素都等於零](https://leetcode.cn/problems/apply-operations-to-make-all-array-elements-equal-to-zero) | 2029 |\n| 2426 | [滿足不等式的數對數目](https://leetcode.cn/problems/number-of-pairs-satisfying-inequality) | 2030 |\n| 947 | [移除最多的同行或同列石頭](https://leetcode.cn/problems/most-stones-removed-with-same-row-or-column) | 2035 |\n| 2245 | [轉角路徑的乘積中最多能有幾個尾隨零](https://leetcode.cn/problems/maximum-trailing-zeros-in-a-cornered-path) | 2037 |\n| 2116 | [判斷一個括號字串是否有效](https://leetcode.cn/problems/check-if-a-parentheses-string-can-be-valid) | 2038 |\n| 1590 | [使陣列和能被 P 整除](https://leetcode.cn/problems/make-sum-divisible-by-p) | 2039 |\n| 1371 | [每個母音包含偶數次的最長子字串](https://leetcode.cn/problems/find-the-longest-substring-containing-vowels-in-even-counts) | 2041 |\n| 3072 | [將元素分配到兩個陣列中 II](https://leetcode.cn/problems/distribute-elements-into-two-arrays-ii) | 2053 |\n| 839 | [相似字串組](https://leetcode.cn/problems/similar-string-groups) | 2054 |\n| 2542 | [最大子序列的分數](https://leetcode.cn/problems/maximum-subsequence-score) | 2056 |\n| 2197 | [替換陣列中的非互質數](https://leetcode.cn/problems/replace-non-coprime-numbers-in-array) | 2057 |\n| 3229 | [使陣列等於目標陣列所需的最少操作次數](https://leetcode.cn/problems/minimum-operations-to-make-array-equal-to-target) | 2067 |\n| 1850 | [鄰位交換的最小次數](https://leetcode.cn/problems/minimum-adjacent-swaps-to-reach-the-kth-smallest-number) | 2073 |\n| 2845 | [統計趣味子陣列的數目](https://leetcode.cn/problems/count-of-interesting-subarrays) | 2073 |\n| 871 | [最低加油次數](https://leetcode.cn/problems/minimum-number-of-refueling-stops) | 2074 |\n| 1712 | [將陣列分成三個子陣列的方案數](https://leetcode.cn/problems/ways-to-split-array-into-three-subarrays) | 2079 |\n| 2555 | [兩個線段獲得的最多獎品](https://leetcode.cn/problems/maximize-win-from-two-segments) | 2081 |\n| 1847 | [最近的房間](https://leetcode.cn/problems/closest-room) | 2082 |\n| 3624 | [位計數深度為 K 的整數數目 II](https://leetcode.cn/problems/number-of-integers-with-popcount-depth-equal-to-k-ii) | 2086 |\n| 3739 | [統計主要元素子陣列數目 II](https://leetcode.cn/problems/count-subarrays-with-majority-element-ii) | 2090 |\n| 2193 | [得到迴文串的最少操作次數](https://leetcode.cn/problems/minimum-number-of-moves-to-make-palindrome) | 2091 |\n| 1383 | [最大的團隊表現值](https://leetcode.cn/problems/maximum-performance-of-a-team) | 2091 |\n| 2751 | [機器人碰撞](https://leetcode.cn/problems/robot-collisions) | 2092 |\n| 2402 | [會議室 III](https://leetcode.cn/problems/meeting-rooms-iii) | 2093 |",
        },
        {
          id: 44,
          title: "4.4 動態規劃",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目可以一步一步做決策，而且**同一個「局面」會被重複算到很多次**。動態規劃（DP）的精神，就是把每個局面只算一次、把答案記下來，後面要用直接查表，避免指數級的重複計算。\n\n先理解「局面」這個詞。以「擲骰子模擬」(1223) 為例，每一次擲骰子後的局面，可以用「已經擲了幾次、上一次擲到哪個點數、那個點數連續出現幾次」來完整描述。只要兩條不同路徑走到「相同局面」，它們往後的算法就一模一樣——這就是可以共用、可以記憶化的訊號。\n\n:::example DP 三件事（以一維為例）\nDP 有三個動作要想清楚：\n\n```cpp\n// DP 三件事（以一維為例）\n// 1) 狀態定義：dp[i] 代表「到第 i 步為止的某個量」\n// 2) 轉移：dp[i] 由更早的 dp[...] 推出來\n// 3) 答案：從某個 dp[?] 讀出\nvector<long long> dp(n + 1, 0);\ndp[0] = 1;  // 邊界：起點\nfor (int i = 1; i <= n; ++i) {\n  dp[i] = dp[i - 1] + dp[i - 2];  // 轉移（此處為示意）\n}\n```\n:::\n\n\n\n:::example 從最後一步推轉移：列出最後一步的所有選擇，合併子問題\n最有效的推導順序是「從最後一步往回想」：問「答案的最後一步有哪些可能選擇？選了之後，剩下的子問題長什麼樣？」每個選擇都對應一個更小的子問題，把它們合併（求和、取最大或最小）就是轉移。\n\n```cpp\n// 從最後一步推轉移：列出最後一步的所有選擇，合併子問題\n// dp[局面] = 合併( dp[做選擇 a 後的局面], dp[做選擇 b 後的局面], ... )\n```\n:::\n\n\n\n核心想法只有一句話：**先用一句話把「狀態」定義清楚，再從最後一步列出選擇推出轉移；只要子問題會重複，記下來就能把指數變多項式。**\n\n複雜度的估法很固定：`狀態數 × 每個狀態的轉移成本`。例如狀態是二維 `dp[i][j]`、每步 `O(1)` 轉移，就是 `O(n·m)`。\n\n**實作方式**\n\n把任何 DP 題拆成下面四步，逐一搭配程式碼：\n\n:::example 狀態定義範例：dp[i] = 以第 i 個元素「結尾」的方案數\n**1. 定義狀態，把語意寫死。** `dp` 的每個下標代表什麼範圍 / 結尾條件 / 階段，要一字不差講清楚。計數型題目（如「交替按位異或分割的數目」(3811)）尤其要說清「`dp` 算的是方案數還是別的」。\n\n```cpp\n// 狀態定義範例：dp[i] = 以第 i 個元素「結尾」的方案數\n// （結尾條件是區分計數 DP 對不對的關鍵）\n```\n:::\n\n\n\n:::example 計數型轉移（記得取模）：把所有合法的「上一步」累加\n**2. 寫轉移，並選對填表順序。** 線性題從前往後、區間題按長度、樹形題用後序、網格題按行列。\n\n```cpp\n// 計數型轉移（記得取模）：把所有合法的「上一步」累加\nconst int MOD = 1e9 + 7;\nfor (int i = 1; i <= n; ++i) {\n  for (int j : prevChoices(i)) {\n    dp[i] = (dp[i] + dp[j]) % MOD;\n  }\n}\n```\n:::\n\n\n\n:::example 記憶化 DFS：算過就存，存過就直接回傳\n**3. 用記憶化搜尋當作 DP 的等價寫法。** 像「網格圖中遞增路徑的數目」(2328)，自然地用「記憶化 DFS」表達：\n\n```cpp\n// 記憶化 DFS：算過就存，存過就直接回傳\nint dfs(int x, int y) {\n  if (memo[x][y] != -1) {\n    return memo[x][y];  // 命中快取\n  }\n  int res = 1;  // 至少有自己這條\n  for (auto [nx, ny] : largerNeighbors(x, y)) {\n    res = (res + dfs(nx, ny)) % MOD;  // 累加更長的路徑\n  }\n  return memo[x][y] = res;  // 存起來再回傳\n}\n```\n:::\n\n\n\n**4. 邊界、初始化與複雜度。**\n\n- 初始化最容易出錯：起點 / 空集合的值要和狀態定義一致（計數常是 1，求最大常是 0 或 `-INF`）。\n- 不可達狀態要用一個明確的「無效值」標記，別讓它誤入轉移。\n- 做空間壓縮（滾動陣列）時，注意更新方向，避免用到「已被本輪覆蓋」的舊值。\n- 複雜度 = 狀態數 × 單步轉移成本。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「動態規劃」想：\n\n- **題目可以一步步做選擇，且問「方案數」「最大 / 最小值」「能不能達成」。** 這三類問法是 DP 的常客，像「擲骰子模擬」(1223) 問合法序列的方案數。\n- **同一個局面會被不同路徑重複走到。** 只要你能用幾個變數把「局面」描述完整，且它會重複出現，就是 DP 的訊號。\n- **暴力是窮舉所有選擇序列（指數級）；** DP 把重複子問題各算一次，壓成「狀態數 × 轉移」的多項式。\n- **「最後一步有哪些選法」這個問題答得出來，** 而且每個選法都導向一個更小的同型問題。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 `dp` 下標，每個維度精確代表什麼？\n2. 從最後一步看，有哪些選擇？每個選擇對應哪個子問題？\n3. 邊界 / 初始值是否和狀態定義一致？填表順序對嗎？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2328 | [網格圖中遞增路徑的數目](https://leetcode.cn/problems/number-of-increasing-paths-in-a-grid/) | 2001 | 這是計數型 DP；先說清 `dp` 計的是前綴、容量還是狀態集合，並處理模數與初始化。 |\n| 2850 | [將石頭分散到網格圖的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-spread-stones-over-grid/) | 2001 | 把位置與限制一起放進狀態；練習檢查移動方向、邊界與滾動更新順序。 |\n| 3811 | [交替按位異或分割的數目](https://leetcode.cn/problems/number-of-alternating-xor-partitions/) | 2006 | 這是計數型 DP；先說清 `dp` 計的是前綴、容量還是狀態集合，並處理模數與初始化。 |\n| 3524 | [求出陣列的 X 值 I](https://leetcode.cn/problems/find-x-value-of-array-i/) | 2008 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n| 1223 | [擲骰子模擬](https://leetcode.cn/problems/dice-roll-simulation/) | 2008 | 放在「動態規劃」下，重點是先定義狀態含義，再由最後一步推導轉移與答案位置。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 1425. 帶限制的子序列和 展示核心寫法。\n\n**完整題目**：選擇一個非空子序列，使相鄰被選元素下標差不超過 k，並最大化元素和。\n\n:::example 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以…\n**線索**：題目能拆成前綴、位置或選擇序列，且不同路徑會到達相同子問題；需要把影響未來的資訊寫成狀態。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減 deque + DP：在長度 k 的窗口內取最大 dp 值，dp[i] 為以 i 結尾的最大子集和\n  int constrainedSubsetSum(vector<int>& nums, int k) {\n    deque<int> dq;  // 單調遞減 deque，儲存下標，保證 dp[dq.front()] 為窗口最大值\n    vector<int> dp(nums.size());\n    int ans = INT_MIN;\n    for (int i = 0; i < (int)nums.size(); ++i) {\n      // 移除已超出 k 範圍的窗口頭部\n      while (!dq.empty() && dq.front() < i - k) {\n        dq.pop_front();\n      }\n      // dp[i]：選 nums[i] 加上前 k 個位置中最大的正 dp 值（若最大值為負則不取前段）\n      dp[i] = nums[i] + (dq.empty() ? 0 : max(0, dp[dq.front()]));\n      // 維護單調遞減性：彈出所有 dp 值不大於 dp[i] 的尾部\n      while (!dq.empty() && dp[dq.back()] <= dp[i]) {\n        dq.pop_back();\n      }\n      dq.push_back(i);\n      ans = max(ans, dp[i]);\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `dp[i]` 需要查前 k 個位置的最大 dp 值；單調佇列把這個區間最大值查詢降成均攤 O(1)。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 前綴和優化。\n- 單調佇列/堆疊優化。\n- 樹狀陣列、線段樹、斜率優化。\n\n**實作注意事項**\n\n- 先保留樸素式方便驗證。\n- 資料結構查的是哪個前驅集合要寫清楚。\n- 單調性不成立時不能硬套。\n- 可用小範圍對拍確認優化等價。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2328 | [網格圖中遞增路徑的數目](https://leetcode.cn/problems/number-of-increasing-paths-in-a-grid) | 2001 |\n| 2850 | [將石頭分散到網格圖的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-spread-stones-over-grid) | 2001 |\n| 3811 | [交替按位異或分割的數目](https://leetcode.cn/problems/number-of-alternating-xor-partitions) | 2006 |\n| 3524 | [求出陣列的 X 值 I](https://leetcode.cn/problems/find-x-value-of-array-i) | 2008 |\n| 1223 | [擲骰子模擬](https://leetcode.cn/problems/dice-roll-simulation) | 2008 |\n| 1981 | [最小化目標值與所選元素的差](https://leetcode.cn/problems/minimize-the-difference-between-target-and-chosen-elements) | 2010 |\n| 2472 | [不重疊迴文子字串的最大數目](https://leetcode.cn/problems/maximum-number-of-non-overlapping-palindrome-substrings) | 2013 |\n| 1105 | [填充書架](https://leetcode.cn/problems/filling-bookcase-shelves) | 2014 |\n| 2741 | [特別的排列](https://leetcode.cn/problems/special-permutations) | 2021 |\n| 2597 | [美麗子集的數目](https://leetcode.cn/problems/the-number-of-beautiful-subsets) | 2023 |\n| 1406 | [石子游戲 III](https://leetcode.cn/problems/stone-game-iii) | 2027 |\n| 1320 | [二指輸入的的最小距離](https://leetcode.cn/problems/minimum-distance-to-type-a-word-using-two-fingers) | 2028 |\n| 2919 | [使陣列變美的最小增量運算數](https://leetcode.cn/problems/minimum-increment-operations-to-make-array-beautiful) | 2031 |\n| 1425 | [帶限制的子序列和](https://leetcode.cn/problems/constrained-subsequence-sum) | 2032 |\n| 3578 | [統計極差最大為 K 的分割方式數](https://leetcode.cn/problems/count-partitions-with-max-min-difference-at-most-k) | 2033 |\n| 2262 | [字串的總引力](https://leetcode.cn/problems/total-appeal-of-a-string) | 2033 |\n| 828 | [統計子串中的唯一字元](https://leetcode.cn/problems/count-unique-characters-of-all-substrings-of-a-given-string) | 2034 |\n| 1335 | [工作計劃的最低難度](https://leetcode.cn/problems/minimum-difficulty-of-a-job-schedule) | 2035 |\n| 1140 | [石子游戲 II](https://leetcode.cn/problems/stone-game-ii) | 2035 |\n| 3654 | [刪除可整除和後的最小陣列和](https://leetcode.cn/problems/minimum-sum-after-divisible-sum-deletions) | 2039 |\n| 3366 | [最小陣列和](https://leetcode.cn/problems/minimum-array-sum) | 2040 |\n| 1751 | [最多可以參加的會議數目 II](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended-ii) | 2041 |\n| 3494 | [釀造藥水需要的最少總時間](https://leetcode.cn/problems/find-the-minimum-amount-of-time-to-brew-potions) | 2042 |\n| 1553 | [喫掉 N 個橘子的最少天數](https://leetcode.cn/problems/minimum-number-of-days-to-eat-n-oranges) | 2048 |\n| 1575 | [統計所有可行路徑](https://leetcode.cn/problems/count-all-possible-routes) | 2055 |\n| 1473 | [粉刷房子 III](https://leetcode.cn/problems/paint-house-iii) | 2056 |\n| 3316 | [從原字串裡進行刪除操作的最多次數](https://leetcode.cn/problems/find-maximum-removals-from-source-string) | 2062 |\n| 1770 | [執行乘法運算的最大分數](https://leetcode.cn/problems/maximum-score-from-performing-multiplication-operations) | 2068 |\n| 3489 | [零陣列變換 IV](https://leetcode.cn/problems/zero-array-transformation-iv) | 2068 |\n| 3154 | [到達第 K 級臺階的方案數](https://leetcode.cn/problems/find-number-of-ways-to-reach-the-k-th-stair) | 2071 |\n| 2866 | [美麗塔 II](https://leetcode.cn/problems/beautiful-towers-ii) | 2072 |\n| 1799 | [N 次操作後的最大分數和](https://leetcode.cn/problems/maximize-score-after-n-operations) | 2073 |\n| 3685 | [含上限元素的子序列和](https://leetcode.cn/problems/subsequence-sum-after-capping-elements) | 2073 |\n| 2906 | [構造乘積矩陣](https://leetcode.cn/problems/construct-product-matrix) | 2075 |\n| 1786 | [從第一個節點出發到最後一個節點的受限路徑數](https://leetcode.cn/problems/number-of-restricted-paths-from-first-to-last-node) | 2079 |\n| 975 | [奇偶跳](https://leetcode.cn/problems/odd-even-jump) | 2079 |\n| 1639 | [透過給定詞典構造目標字串的方案數](https://leetcode.cn/problems/number-of-ways-to-form-a-target-string-given-a-dictionary) | 2082 |\n| 1494 | [並行課程 II](https://leetcode.cn/problems/parallel-courses-ii) | 2082 |\n| 2050 | [並行課程 III](https://leetcode.cn/problems/parallel-courses-iii) | 2084 |\n| 2267 | [檢查是否有合法括號字串路徑](https://leetcode.cn/problems/check-if-there-is-a-valid-parentheses-string-path) | 2085 |\n| 3351 | [好子序列的元素之和](https://leetcode.cn/problems/sum-of-good-subsequences) | 2086 |\n| 1563 | [石子游戲 V](https://leetcode.cn/problems/stone-game-v) | 2087 |\n| 2318 | [不同骰子序列的數目](https://leetcode.cn/problems/number-of-distinct-roll-sequences) | 2090 |\n| 1049 | [最後一塊石頭的重量 II](https://leetcode.cn/problems/last-stone-weight-ii) | 2092 |\n| 3434 | [子陣列操作後的最大頻率](https://leetcode.cn/problems/maximum-frequency-after-subarray-operation) | 2094 |\n| 1976 | [到達目的地的方案數](https://leetcode.cn/problems/number-of-ways-to-arrive-at-destination) | 2095 |",
        },
        {
          id: 45,
          title: "4.5 圖論演算法",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目裡有「東西」和「東西之間的關係」，例如城市與道路、人與好友、狀態與轉移。把「東西」當成節點、把「關係」當成邊，問題就變成一張「圖」，可以套用成熟的圖論演算法。\n\n:::example 鄰接表建圖（無向圖：兩邊都加）\n第一步永遠是「建圖」。最常用的存法是「鄰接表」——每個節點記下它能到哪些鄰居：\n\n```cpp\n// 鄰接表建圖（無向圖：兩邊都加）\nvector<vector<int>> g(n);\nfor (auto& e : edges) {\n  g[e[0]].push_back(e[1]);\n  g[e[1]].push_back(e[0]);  // 有向圖就拿掉這行\n}\n```\n:::\n\n\n\n建好之後，「怎麼走這張圖」決定了用哪個演算法，最常見的幾種一次看清：\n\n- **DFS / BFS**：探索可達性、數連通塊、無權圖最短步數。\n- **Dijkstra**：邊權非負時求最短路（用堆每次取最近點）。\n- **拓撲排序**：有向無環圖上排出合法順序（反覆移除入度為 0 的點）。\n- **Tarjan 低鏈（low-link）**：找橋 / 割點，像「查詢叢集內的關鍵連線」(1192) 就是找橋。\n\n:::example BFS：從 src 出發，求到各點的「最少邊數」\n```cpp\n// BFS：從 src 出發，求到各點的「最少邊數」\nqueue<int> q;\nq.push(src);\nvector<int> dist(n, -1);\ndist[src] = 0;\nwhile (!q.empty()) {\n  int u = q.front();\n  q.pop();\n  for (int v : g[u]) {\n    if (dist[v] == -1) {  // 沒走過才更新\n      dist[v] = dist[u] + 1;\n      q.push(v);\n    }\n  }\n}\n```\n:::\n\n\n\n核心想法只有一句話：**先把題意翻譯成節點與邊（節點不一定是真實物件，也可以是 `(位置, 狀態)`），再依「邊有沒有權、權正不正、要不要排序、找不找橋」選對演算法。**\n\n複雜度通常是 `O(V + E)`（DFS/BFS）或 `O(E log V)`（Dijkstra），`V` 是節點數、`E` 是邊數。\n\n**實作方式**\n\n把圖論題拆成下面四步，逐一搭配程式碼：\n\n**1. 建模：定義節點與邊。** 寫清楚「一個節點代表什麼、一條邊代表什麼、邊有沒有成本」。若有額外狀態（剩餘油量、是否帶鑰匙），要把它併進節點，例如節點變成 `(城市, 狀態)`。像「新增邊使所有節點度數都為偶數」(2508) 就要先想清楚「度數」這個圖性質。\n\n:::example 對照表：需求 → 演算法\n**2. 選演算法並準備好 visited / dist。** 對照需求挑工具：\n\n```cpp\n// 對照表：需求 → 演算法\n// 連通塊 / 可達性          -> DFS 或 BFS\n// 無權最短路（每邊算一步） -> BFS\n// 非負權最短路            -> Dijkstra（堆）\n// 邊權只有 0/1            -> 0-1 BFS（雙端佇列）\n// 找橋 / 割點             -> Tarjan low-link\n```\n:::\n\n\n\n:::example Dijkstra：堆裡每次取出「目前最近」的點來鬆弛\n**3. 跑演算法、維護核心狀態。** 以 Dijkstra 為例（「最短路徑中的邊」(3123) 這類非負權題）：\n\n```cpp\n// Dijkstra：堆裡每次取出「目前最近」的點來鬆弛\npriority_queue<pair<long long, int>, vector<pair<long long, int>>, greater<>> pq;\ndist[src] = 0;\npq.push({0, src});\nwhile (!pq.empty()) {\n  auto [d, u] = pq.top();\n  pq.pop();\n  if (d > dist[u]) {\n    continue;  // 過期條目，跳過\n  }\n  for (auto [v, w] : g[u]) {\n    if (dist[u] + w < dist[v]) {  // 鬆弛成功才更新\n      dist[v] = dist[u] + w;\n      pq.push({dist[v], v});\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 從狀態讀答案，注意邊界與複雜度。** 答案常來自 `dist` 陣列、連通塊數、拓撲序，或橋 / 割點集合。\n\n- visited 不能只看「座標 / 編號」：若節點含額外狀態，同一個位置帶不同狀態算「不同節點」。\n- 邊權性質決定演算法：含負權不能用 Dijkstra；只有 0/1 權用 0-1 BFS 更快。\n- 複雜度：DFS/BFS `O(V+E)`，Dijkstra `O(E log V)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「圖論」想：\n\n- **題目出現「節點 / 邊 / 路徑 / 連通 / 度數 / 最短」這類字眼。** 像「一個圖中連通三元組的最小度數」(1761) 直接講連通與度數。\n- **問「能不能從 A 到 B」「最少幾步 / 最短距離」「有幾個連通群」。** 這些是 DFS/BFS/Dijkstra 的標準問法。\n- **要找「拿掉就會斷開」的關鍵邊或關鍵點。** 這是橋 / 割點，對應 Tarjan，例如「查詢叢集內的關鍵連線」(1192)。\n- **暴力做法是窮舉所有路徑（指數級）；** 圖論演算法靠 visited / dist 把每個節點或邊只處理常數次，壓到 `O(V+E)` 或 `O(E log V)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點代表什麼？邊代表什麼？需不需要把額外狀態併進節點？\n2. 邊有沒有權？權是 0/1、非負、還是可能為負？據此該選哪個演算法？\n3. 我的答案要從 `dist` / 連通塊 / 拓撲序 / 橋集合 哪裡讀出來？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1761 | [一個圖中連通三元組的最小度數](https://leetcode.cn/problems/minimum-degree-of-a-connected-trio-in-a-graph/) | 2005 | 要維護連通塊；練習比較 DFS/BFS 標記與並查集合併的邊界。 |\n| 2508 | [新增邊使所有節點度數都為偶數](https://leetcode.cn/problems/add-edges-to-make-degrees-of-all-nodes-even/) | 2060 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 2959 | [關閉分部的可行集合數目](https://leetcode.cn/problems/number-of-possible-sets-of-closing-branches/) | 2077 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 1192 | [查詢叢集內的關鍵連線](https://leetcode.cn/problems/critical-connections-in-a-network/) | 2085 | 先做圖建模：節點代表什麼、邊代表什麼、額外狀態是否要進入 visited/dist。 |\n| 3123 | [最短路徑中的邊](https://leetcode.cn/problems/find-edges-in-shortest-paths/) | 2093 | 核心是狀態圖最短路；先判斷邊權是 1、0/1 還是一般非負權，再選 BFS 或 Dijkstra。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 743. 網路延遲時間 展示核心寫法。\n\n**完整題目**：給定有向帶權邊、節點數與起點，求訊號從起點傳到所有節點所需的最長最短路時間；若有節點不可達則回傳 `-1`。\n\n:::example Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n**線索**：題目描述可達、路徑、依賴或關係傳遞；把物件當節點、操作當邊後，就能選遍歷或最短路。\n\n```cpp\nclass Solution {\n public:\n  // Dijkstra 最短路：從節點 k 出發，求到所有節點的最短距離，答案為最大者\n  int networkDelayTime(vector<vector<int>>& times, int n, int k) {\n    vector<vector<pair<int, int>>> g(n + 1);  // 鄰接表，g[u] = {(v, w)}\n    for (auto& e : times) {\n      g[e[0]].push_back({e[1], e[2]});\n    }\n    const int INF = 1e9;\n    vector<int> dist(n + 1, INF);\n    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;  // 最小堆：{距離, 節點}\n    dist[k] = 0;\n    pq.push({0, k});\n    while (!pq.empty()) {\n      auto [d, u] = pq.top();\n      pq.pop();\n      if (d != dist[u]) {\n        continue;  // 已有更短路徑，跳過過期條目\n      }\n      for (auto [v, w] : g[u]) {\n        if (dist[v] > d + w) {\n          dist[v] = d + w;  // 鬆弛操作\n          pq.push({dist[v], v});\n        }\n      }\n    }\n    // 所有節點最短距離的最大值即為信號傳播時間\n    int ans = *max_element(dist.begin() + 1, dist.end());\n    return ans == INF ? -1 : ans;  // 若有節點不可達則返回 -1\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 非負權最短路使用 Dijkstra；堆中舊距離要跳過，避免重複鬆弛造成常數過大。\n- 先建出鄰接關係，再用 `visited`、`dist`、`indeg` 等狀態控制每個節點何時處理。\n- 如果同一節點在不同資源狀態下未來不同，就不能只用節點編號當 visited 鍵。\n\n**常見變形**\n\n- 無權最短路用 BFS。\n- 0/1 權用 0-1 BFS。\n- 非負權用 Dijkstra；有負權需 Bellman-Ford/SPFA 類思路。\n\n**實作注意事項**\n\n- 先估點數與邊數，決定鄰接表或矩陣。\n- Dijkstra 不適用負權邊。\n- 多源 BFS 要把所有源點同時入隊。\n- 狀態圖要包含資源、鑰匙、次數等額外維度。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1761 | [一個圖中連通三元組的最小度數](https://leetcode.cn/problems/minimum-degree-of-a-connected-trio-in-a-graph) | 2005 |\n| 2508 | [新增邊使所有節點度數都為偶數](https://leetcode.cn/problems/add-edges-to-make-degrees-of-all-nodes-even) | 2060 |\n| 2959 | [關閉分部的可行集合數目](https://leetcode.cn/problems/number-of-possible-sets-of-closing-branches) | 2077 |\n| 1192 | [查詢叢集內的關鍵連線](https://leetcode.cn/problems/critical-connections-in-a-network) | 2085 |\n| 3123 | [最短路徑中的邊](https://leetcode.cn/problems/find-edges-in-shortest-paths) | 2093 |",
        },
        {
          id: 46,
          title: "4.6 貪心",
          summary:
            "**原理講解**\n\n本節要解決的場景是：每一步都有好幾種選擇，而我們希望「每一步都挑當下看起來最好的那個」，最後就能得到全域最佳解。這就是「貪心（greedy）」。它最快、最省，但有個前提——**你得能證明「當下的最佳選擇不會害到後面」**，否則貪心會給出錯誤答案。\n\n:::example 交換論證的精神：若交換相鄰兩項後答案不會更好，\n先看一個具體例子體會「安全的局部選擇」。「對 Bob 造成的最少傷害」(3273) 要決定先打哪些敵人。直覺上，每秒傷害高、又難殺的敵人應該先處理；嚴謹一點，可以證明：把任意相鄰兩個敵人交換順序，若按某個「比值」排序總代價不會變差。這種「交換不會更好」的論證，就是貪心的安全證明。\n\n```cpp\n// 交換論證的精神：若交換相鄰兩項後答案不會更好，\n// 那「依某個鍵排序」就是安全的選擇順序\nsort(a.begin(), a.end(), [](const Item& x, const Item& y) {\n  return x.damage * y.time > y.damage * x.time;  // 比值排序\n});\n```\n:::\n\n\n\n:::example 反悔/削峰貪心：每次取出當前最大者處理，再放回去\n另一種常見形態是「反悔貪心」：先大膽選，事後發現某個早先的選擇不划算，就把它「退回去」。「最小差值平方和」(2333) 就是反覆找出目前最大的差值、把它削小一點，這種「每次處理當前最極端者」的策略常配合堆：\n\n```cpp\n// 反悔/削峰貪心：每次取出當前最大者處理，再放回去\npriority_queue<int> pq;  // 大根堆\n// pq.top() 是目前最大差值；削小後再 push 回去\nint big = pq.top();\npq.pop();\npq.push(big - 1);  // 把最大的削一格（示意）\n```\n:::\n\n\n\n核心想法只有一句話：**找到一條「可證明安全」的局部選擇規則，照那個順序掃過去，每步做當下最佳決定；安全性靠交換論證或不變式來保證。**\n\n貪心多半是「排序 + 一次掃描」，複雜度常是 `O(n log n)`（排序主導）。\n\n**實作方式**\n\n把貪心題拆成下面四步，逐一搭配程式碼：\n\n**1. 猜出局部規則，並嘗試證明它安全。** 先寫下「我每一步打算怎麼選」，再用交換論證（交換相鄰兩項不會更好）或不變式（某個量始終維持最佳）說服自己。證不出來，就別貿然貪心，可能要改用 DP。\n\n:::example 例：區間類常按「結束時間」排序，先處理早結束的\n**2. 決定排序鍵，把選擇順序定下來。** 多數貪心的關鍵就是「按什麼排序」。\n\n```cpp\n// 例：區間類常按「結束時間」排序，先處理早結束的\nsort(jobs.begin(), jobs.end(), [](auto& a, auto& b) { return a.end < b.end; });\n```\n:::\n\n\n\n:::example 掃描骨架：逐項做安全選擇，必要時反悔\n**3. 掃描並維護「已選集合的摘要」。** 不用記下選了哪些，只記能支撐決策的摘要：最後結束時間、剩餘資源、堆裡的可反悔候選、或正在構造的前綴。\n\n```cpp\n// 掃描骨架：逐項做安全選擇，必要時反悔\nfor (auto& item : sorted) {\n  if (canTake(item)) {\n    take(item);  // 安全就選\n  } else if (betterToSwap(item)) {\n    undoWorst();\n    take(item);  // 反悔：退掉最差的，換成更好的\n  }\n}\n```\n:::\n\n\n\n**4. 讀答案，注意邊界與複雜度。** 掃完後，已選數量、累積代價或構造出的字串就是答案。\n\n- 別只憑「選最大 / 最小」的直覺：沒有證明的貪心很容易反例翻車。\n- 「使陣列相等的最小開銷」(2448) 這類「往哪個值靠攏最省」的題，最佳點常是（加權）中位數而非平均數——這正是需要證明、直覺易錯的地方。\n- 反悔貪心記得用堆維護「最差的已選項」，才能 `O(log n)` 退回。\n- 複雜度：排序 `O(n log n)` 通常是瓶頸；純掃描部分 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「貪心」想：\n\n- **題目求最大 / 最小，且每一步的選擇彼此關聯不太強。** 如果「這一步怎麼選」不太牽動「下一步能怎麼選」，貪心很可能成立。\n- **你能講出一條清楚的排序規則或選擇規則，並能用交換論證撐住它。** 像「對 Bob 造成的最少傷害」(3273) 的比值排序。\n- **題目帶有「可以反悔 / 替換」的結構，** 適合反悔貪心，例如「最小差值平方和」(2333) 反覆削最大差值。\n- **暴力是窮舉所有選擇組合（指數級）；** 貪心靠一條安全規則，一次掃描 `O(n log n)` 就定案。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的局部選擇規則是什麼？我能用交換論證或不變式證明它安全嗎？\n2. 我該按什麼鍵排序？掃描時要維護哪個「摘要」？\n3. 需不需要反悔？若要，我用什麼結構退回最差的已選項？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2448 | [使陣列相等的最小開銷](https://leetcode.cn/problems/minimum-cost-to-make-array-equal/) | 2005 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 3614 | [用特殊操作處理字串 II](https://leetcode.cn/problems/process-string-with-special-operations-ii/) | 2011 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 2333 | [最小差值平方和](https://leetcode.cn/problems/minimum-sum-of-squared-difference/) | 2011 | 通常從高位或關鍵位置決策；練習說明當前選擇如何保留後續最優空間。 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital/) | 2012 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n| 3273 | [對 Bob 造成的最少傷害](https://leetcode.cn/problems/minimum-amount-of-damage-dealt-to-bob/) | 2013 | 不能只憑局部最大；請寫出選擇規則，並用交換論證或歸納說明它保留最優解。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 435. 無重疊區間 展示核心寫法。\n\n**完整題目**：給定多個區間，移除最少區間，使剩下區間兩兩不重疊。\n\n:::example 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n**線索**：題目可拆成一連串決策，且存在排序鍵或局部選擇能保留後續最優空間；要同步準備交換論證。\n\n```cpp\nclass Solution {\n public:\n  // 貪心：按右端點排序，盡量保留更多不重疊區間（等價於移除最少區間）\n  int eraseOverlapIntervals(vector<vector<int>>& intervals) {\n    // 按右端點升序排序，讓每次選的區間盡早結束以留出更多空間\n    sort(intervals.begin(), intervals.end(), [](auto& a, auto& b) { return a[1] < b[1]; });\n    int kept = 0, end = INT_MIN;\n    for (auto& in : intervals) {\n      if (in[0] >= end) {  // 無重疊：可保留此區間\n        kept++;\n        end = in[1];  // 更新已選區間的右端點\n      }\n    }\n    return intervals.size() - kept;  // 總數減去保留數即為需移除數\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 按右端點最小優先，能留下最多後續空間；這是區間排程的典型交換論證。\n- 排序鍵必須直接服務於貪心證明；程式中的比較器就是解法的核心決策。\n- 掃描時維護的變數代表目前已選集合的邊界或成本，用來判斷下一個候選能否保留。\n\n**常見變形**\n\n- 區間排程按右端點。\n- 字典序題從高位決策。\n- 數學貪心常需不等式或構造證明。\n\n**實作注意事項**\n\n- 先寫出貪心準則，再補證明。\n- 排序 tie-breaker 要與證明一致。\n- 相等元素與空集合是常見反例來源。\n- 可用小範圍 brute force 對拍檢查。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2448 | [使陣列相等的最小開銷](https://leetcode.cn/problems/minimum-cost-to-make-array-equal) | 2005 |\n| 3614 | [用特殊操作處理字串 II](https://leetcode.cn/problems/process-string-with-special-operations-ii) | 2011 |\n| 2333 | [最小差值平方和](https://leetcode.cn/problems/minimum-sum-of-squared-difference) | 2011 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital) | 2012 |\n| 3273 | [對 Bob 造成的最少傷害](https://leetcode.cn/problems/minimum-amount-of-damage-dealt-to-bob) | 2013 |\n| 3645 | [最優啟用順序得到的最大總和](https://leetcode.cn/problems/maximum-total-from-optimal-activation-order) | 2019 |\n| 3428 | [最多 K 個元素的子序列的最值之和](https://leetcode.cn/problems/maximum-and-minimum-sums-of-at-most-size-k-subsequences) | 2028 |\n| 2136 | [全部開花的最早一天](https://leetcode.cn/problems/earliest-possible-day-of-full-bloom) | 2033 |\n| 2551 | [將珠子放入背包中](https://leetcode.cn/problems/put-marbles-in-bags) | 2042 |\n| 2948 | [交換得到字典序最小的陣列](https://leetcode.cn/problems/make-lexicographically-smallest-array-by-swapping-elements) | 2047 |\n| 3785 | [避免禁用值的最小交換次數](https://leetcode.cn/problems/minimum-swaps-to-avoid-forbidden-values) | 2052 |\n| 2681 | [英雄的力量](https://leetcode.cn/problems/power-of-heroes) | 2060 |\n| 2366 | [將陣列排序的最少替換次數](https://leetcode.cn/problems/minimum-replacements-to-sort-the-array) | 2060 |\n| 2607 | [使子陣列元素和相等](https://leetcode.cn/problems/make-k-subarray-sums-equal) | 2071 |\n| 2449 | [使陣列相似的最少操作次數](https://leetcode.cn/problems/minimum-number-of-operations-to-make-arrays-similar) | 2076 |\n| 843 | [猜猜這個單詞](https://leetcode.cn/problems/guess-the-word) | 2078 |\n| 3240 | [最少翻轉次數使二進位制矩陣迴文 II](https://leetcode.cn/problems/minimum-number-of-flips-to-make-binary-grid-palindromic-ii) | 2080 |\n| 2412 | [完成所有交易的初始最少錢數](https://leetcode.cn/problems/minimum-money-required-before-transactions) | 2092 |",
        },
        {
          id: 47,
          title: "4.7 網格圖",
          summary:
            "**原理講解**\n\n本節要解決的場景是：給你一個二維方格（迷宮、棋盤、地圖），問「從起點到終點最少要走幾步 / 最少花多少代價」「能走到哪些格子」「連通區域有多大」。關鍵領悟是：**把每個格子看成圖上的一個節點，相鄰可走的格子之間連一條邊**，網格題就變成圖的遍歷題，直接套 BFS / Dijkstra。\n\n:::example 方向陣列：上下左右（要八方向再補四個斜角）\n「往四個方向走」這件事，用一個方向陣列就能優雅表達：\n\n```cpp\n// 方向陣列：上下左右（要八方向再補四個斜角）\nint dx[4] = {-1, 1, 0, 0};\nint dy[4] = {0, 0, -1, 1};\n// 從 (x, y) 走到鄰居 (x+dx[k], y+dy[k])\n```\n:::\n\n\n\n:::example 合法鄰居的三道檢查\n每走一步前，都要檢查「沒出界、不是障礙、沒走過」：\n\n```cpp\n// 合法鄰居的三道檢查\nbool ok(int x, int y, vector<vector<int>>& vis, vector<vector<int>>& grid) {\n  return x >= 0 && x < (int)grid.size() && y >= 0 && y < (int)grid[0].size() && grid[x][y] != WALL &&\n         !vis[x][y];  // 在界內、非牆、未訪問\n}\n```\n:::\n\n\n\n選哪個演算法，取決於「走一步的代價」：\n\n- **每步代價都是 1**（普通迷宮）：用 **BFS**，第一次到達就是最短，例如「蛇梯棋」(909) 把每次擲骰子當一步。\n- **代價只有 0 或 1**（某些方向免費、某些要付 1）：用 **0-1 BFS**（雙端佇列），如「使網格圖至少有一條有效路徑的最小代價」(1368)。\n- **代價是任意非負數**：用 **Dijkstra**。\n\n核心想法只有一句話：**格子即節點、相鄰即邊；先判斷「一步的代價」再選 BFS / 0-1 BFS / Dijkstra，並把任何額外狀態（鑰匙、朝向、剩餘次數）一起放進節點。**\n\n複雜度通常是 `O(R·C)`（BFS）到 `O(R·C·log(R·C))`（Dijkstra），`R`、`C` 是行列數。\n\n**實作方式**\n\n把網格題拆成下面四步，逐一搭配程式碼：\n\n:::example 帶狀態的 visited：同一格、不同狀態算不同節點\n**1. 定義節點，想清楚要不要加狀態。** 最單純時節點就是 `(row, col)`；但若移動受額外條件影響（持有哪些鑰匙、目前朝向、還能消除幾個障礙），節點要擴成 `(row, col, 額外狀態)`。像「穿過迷宮的最少移動次數」(1210) 蛇有「橫 / 豎」兩種姿態，必須進節點。\n\n```cpp\n// 帶狀態的 visited：同一格、不同狀態算不同節點\n// vis[x][y][s]  其中 s 是鑰匙集合 / 朝向 / 剩餘次數等\nvector<vector<array<int, K>>> vis;  // 依狀態維度開大\n```\n:::\n\n\n\n**2. 選演算法、初始化容器。** 一步代價固定用佇列（BFS）；代價不均用雙端佇列（0-1 BFS）或堆（Dijkstra）。\n\n:::example BFS：逐層擴展，第一次到達即最短步數\n**3. 標準遍歷迴圈。** 以 BFS 求最短步數為例：\n\n```cpp\n// BFS：逐層擴展，第一次到達即最短步數\nqueue<pair<int, int>> q;\nq.push({sx, sy});\nvis[sx][sy] = 1;\nint steps = 0;\nwhile (!q.empty()) {\n  int sz = q.size();\n  while (sz--) {  // 一次處理一整層\n    auto [x, y] = q.front();\n    q.pop();\n    if (x == tx && y == ty) {\n      return steps;\n    }\n    for (int k = 0; k < 4; ++k) {\n      int nx = x + dx[k], ny = y + dy[k];\n      if (ok(nx, ny, vis, grid)) {\n        vis[nx][ny] = 1;\n        q.push({nx, ny});\n      }\n    }\n  }\n  ++steps;  // 走完一層步數 +1\n}\n```\n:::\n\n\n\n**4. 讀答案、注意邊界與複雜度。** 答案是「首次到達終點的步數 / 距離」、連通區域大小，或遍歷後的標記矩陣。\n\n- visited 必須涵蓋完整狀態：若節點含鑰匙 / 朝向，只記座標會把「不同狀態」誤判成已訪問而漏解。\n- 0-1 BFS 時，代價 0 的邊從佇列「前端」放入、代價 1 的邊從「後端」放入，才能維持單調。\n- 傳送門 / 蛇梯這類「瞬移邊」要當成額外的邊加進去（如「網格傳送門旅遊」(3552)）。\n- 複雜度：BFS `O(R·C·狀態數)`；Dijkstra 再乘上 `log`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「網格圖」想：\n\n- **輸入是二維矩陣，問「最少幾步 / 最短路 / 能否到達 / 連通區域」。** 「蛇梯棋」(909) 求最少擲骰次數就是典型。\n- **移動是「往相鄰格子走」，可能帶障礙、權重或瞬移。** 這正對應「格子即節點、相鄰即邊」的建模。\n- **走一步的代價不一致（有的免費、有的收費）。** 這是 0-1 BFS 或 Dijkstra 的訊號，如「使網格圖至少有一條有效路徑的最小代價」(1368)。\n- **暴力做法是窮舉所有路徑（指數級）；** BFS / Dijkstra 靠 visited 讓每個（狀態化的）格子只處理常數次，壓到多項式。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點只是 `(row, col)`，還是需要加上鑰匙 / 朝向 / 剩餘次數？\n2. 走一步的代價是固定 1、0/1、還是任意非負？該選 BFS / 0-1 BFS / Dijkstra？\n3. 我的 visited 有沒有把完整狀態都考慮進去？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 909 | [蛇梯棋](https://leetcode.cn/problems/snakes-and-ladders/) | 2020 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 1210 | [穿過迷宮的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-reach-target-with-rotations/) | 2022 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 3552 | [網格傳送門旅遊](https://leetcode.cn/problems/grid-teleportation-traversal/) | 2036 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 1368 | [使網格圖至少有一條有效路徑的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid/) | 2069 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n:::example DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\n public:\n  int numIslands(vector<vector<char>>& grid) {\n    int m = grid.size(), n = grid[0].size(), ans = 0;\n    int dirs[5] = {1, 0, -1, 0, 1};  // 方向陣列，相鄰兩元素構成上下左右四個方向向量\n    // DFS：將與 (r,c) 相連的所有 '1' 標記為 '0'（原地沉島）\n    function<void(int, int)> dfs = [&](int r, int c) {\n      grid[r][c] = '0';  // 標記已訪問，避免重複計算\n      for (int k = 0; k < 4; ++k) {\n        int nr = r + dirs[k], nc = c + dirs[k + 1];\n        if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n          dfs(nr, nc);\n        }\n      }\n    };\n    // 掃描整個網格，每遇到一個未訪問的 '1' 就是一座新島嶼\n    for (int r = 0; r < m; ++r) {\n      for (int c = 0; c < n; ++c) {\n        if (grid[r][c] == '1') {\n          ans++, dfs(r, c);\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 909 | [蛇梯棋](https://leetcode.cn/problems/snakes-and-ladders) | 2020 |\n| 1210 | [穿過迷宮的最少移動次數](https://leetcode.cn/problems/minimum-moves-to-reach-target-with-rotations) | 2022 |\n| 3552 | [網格傳送門旅遊](https://leetcode.cn/problems/grid-teleportation-traversal) | 2036 |\n| 1368 | [使網格圖至少有一條有效路徑的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid) | 2069 |",
        },
        {
          id: 48,
          title: "4.8 數學",
          summary:
            '**原理講解**\n\n本節要處理的題目，表面上是在問「誰會贏」「最大值是多少」「有幾種排法」，但真正的解法不是把所有情況跑一遍，而是先把題目的文字條件翻成一條數學式子或一個數學性質，再用這條性質直接算出答案。這個技巧的名字就叫「數學化」：把問題從「程式怎麼模擬」改寫成「式子怎麼推」。\n\n為什麼這樣比暴力快？因為暴力常常要枚舉很多組合（例如所有取法、所有排列），數量大到跑不完；但只要找到背後的規律或閉式（closed form，意思是「一條能直接代入算出答案的公式」），就能用常數或對數時間算完。\n\n:::example 石子游戲：按「兩人價值之和」由大到小排序，雙方輪流取\n舉個具體例子。[石子游戲 VI](https://leetcode.cn/problems/stone-game-vi/) 裡兩人輪流拿石子，每堆石子對兩人各有不同價值。看起來要模擬博弈，但其實有個漂亮的性質：雙方都該優先拿「兩人價值總和最大」的那一堆。知道這條性質後，整題就只剩排序：\n\n```cpp\n// 石子游戲：按「兩人價值之和」由大到小排序，雙方輪流取\nvector<int> idx(n);\niota(idx.begin(), idx.end(), 0);  // idx = 0,1,2,...\nsort(idx.begin(), idx.end(), [&](int i, int j) { return aliceValues[i] + bobValues[i] > aliceValues[j] + bobValues[j]; });\n```\n:::\n\n\n\n:::example 拆絕對值：把每種正負號組合 s1,s2,s3 ∈ {+1,-1} 攤開成可分離的式子\n另一類常見的數學化是「拆絕對值」。[絕對值表示式的最大值](https://leetcode.cn/problems/maximum-of-absolute-value-expression/) 要最大化 `|x1-x2|+|y1-y2|+|i-j|`，直接枚舉每兩個點是 `O(n^2)`。但 `|a|` 等於 `max(a, -a)`，把每個絕對值的正負號展開，式子就拆成幾組「各自獨立的加減組合」，每組只要掃一遍記最大最小即可：\n\n```cpp\n// 拆絕對值：把每種正負號組合 s1,s2,s3 ∈ {+1,-1} 攤開成可分離的式子\n// |x1-x2| = max over s1 of s1*(x1-x2)，三個絕對值各自獨立\nfor (int s1 : {1, -1}) {\n  for (int s2 : {1, -1}) {\n    for (int s3 : {1, -1}) {\n      // 對固定符號，val = s1*x + s2*y + s3*i 可逐點累計最大/最小\n    }\n  }\n}\n```\n:::\n\n\n\n**實作方式**\n\n數學化題目沒有單一模板，但有固定的思考步驟，逐一搭配做法：\n\n**1. 先把文字翻成式子。** 把「贏」「最多」「整除」「同餘」這些詞，寫成明確的等式或不等式。例如「a 能被 b 整除」寫成 `a % b == 0`；「兩數同餘」寫成 `a % m == b % m`。這一步是純紙筆工作，但決定了後面一切。\n\n:::example 猜規律的輔助：暴力算小 n 的答案印出來，肉眼找模式\n**2. 用小資料猜規律，再補證明。** 數學題常常先觀察出規律。允許先用暴力跑小範圍印出答案找規律，但提交前一定要說服自己這規律為什麼對，否則容易在大測資翻車。\n\n```cpp\n// 猜規律的輔助：暴力算小 n 的答案印出來，肉眼找模式\nfor (int n = 1; n <= 10; ++n) {\n  cout << n << " -> " << brute(n) << "\\n";  // 觀察輸出有無公式\n}\n```\n:::\n\n\n\n:::example 組合計數：階乘預處理 + 快速冪求模逆元（取代取模下的除法）\n**3. 套用對應的數學工具做局部計算。** 依題型選工具：質數篩、因數分解、快速冪（次方取模）、組合數預處理、容斥、遞推。例如 [統計同位異構字串數目](https://leetcode.cn/problems/count-anagrams/) 要算排列數 `n! / (c1! c2! ...)`，會用到階乘與「模逆元」（在取模世界裡做除法的方式）：\n\n```cpp\n// 組合計數：階乘預處理 + 快速冪求模逆元（取代取模下的除法）\nlong long power(long long a, long long b, long long M) {\n  long long r = 1;\n  a %= M;\n  while (b) {\n    if (b & 1) {\n      r = r * a % M;\n    }\n    a = a * a % M;\n    b >>= 1;\n  }\n  return r;  // a^b mod M\n}\n// 模逆元 = power(x, M-2, M)，前提 M 是質數\n```\n:::\n\n\n\n**4. 注意溢位與取模除法。** 兩個 `int` 相乘可能爆掉，要先轉 `long long`；在取模世界裡不能直接除，必須乘上「模逆元」。這兩點是數學題最常見的坑。\n\n複雜度依工具而定：排序類 `O(n log n)`，篩法 `O(n log log n)`，快速冪單次 `O(log b)`，組合數預處理 `O(n)` 後每次查詢 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到以下訊號，就往「先數學化」想：\n\n- **答案是個數量、機率、必勝必敗，或一條能整理成式子的判定。** 例如博弈題問誰贏（[求和遊戲](https://leetcode.cn/problems/sum-game/)）、計數題問有幾種（[統計同位異構字串數目](https://leetcode.cn/problems/count-anagrams/)）。\n- **資料範圍大到不可能枚舉，暗示存在閉式或規律。** 例如要找「第 N 小」的數（[二進位制中恰好K個1的第N小整數](https://leetcode.cn/problems/find-nth-smallest-integer-with-k-one-bits/)），N 可能極大，只能用數學構造而非逐一列舉。\n- **式子裡有絕對值、整除、同餘、階乘或組合，提示要拆式或用數論工具。**\n- **博弈題通常有「雙方都最優」的對稱性，往往化簡成排序或貪心。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能把題目的條件寫成一條明確的式子或性質嗎？\n2. 我猜到的規律，有沒有（哪怕是粗略的）理由說明它為什麼成立？\n3. 計算過程中哪裡會溢位、哪裡需要模逆元？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1686 | [石子游戲 VI](https://leetcode.cn/problems/stone-game-vi/) | 2001 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 1927 | [求和遊戲](https://leetcode.cn/problems/sum-game/) | 2005 | 要先定義必勝/必敗態；練習從終止局面反推轉移。 |\n| 1131 | [絕對值表示式的最大值](https://leetcode.cn/problems/maximum-of-absolute-value-expression/) | 2059 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 3821 | [二進位制中恰好K個1的第N小整數](https://leetcode.cn/problems/find-nth-smallest-integer-with-k-one-bits/) | 2069 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n| 2514 | [統計同位異構字串數目](https://leetcode.cn/problems/count-anagrams/) | 2070 | 需要先公式化；練習用小資料驗證規律，再處理溢位、取模或精度。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 204. 計數質數 展示核心寫法。\n\n**完整題目**：給定整數 `n`，計算所有小於 `n` 的質數個數。\n\n:::example 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n**線索**：題目核心限制可翻成整除、同餘、組合數、幾何量或博弈狀態；公式化比資料結構更重要。\n\n```cpp\nclass Solution {\n public:\n  int countPrimes(int n) {\n    // 線性篩（埃氏篩）：isPrime[i] 表示 i 是否為質數\n    vector<char> isPrime(n, true);\n    int ans = 0;\n    for (int i = 2; i < n; ++i) {\n      if (!isPrime[i]) {\n        continue;\n      }\n      ans++;\n      // 從 i*i 開始篩，因為更小的倍數已由更小的質數篩過\n      if ((long long)i * i < n) {\n        for (long long j = 1LL * i * i; j < n; j += i) {\n          isPrime[j] = false;\n        }\n      }\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 篩法從 `i*i` 開始標記合數，因為更小倍數已被更小質因數處理。\n- 程式直接落實公式或必勝/必敗規律；重點是邊界、溢位與取模。\n- 若規律來自小樣本，實作前要先確認公式對最小值與極值都成立。\n\n**常見變形**\n\n- 數論：gcd、篩法、質因數分解、模逆元。\n- 組合：乘法原理、容斥、生成函式。\n- 幾何：叉積、凸包、距離轉換。\n\n**實作注意事項**\n\n- 先確認數值範圍與是否溢位。\n- 取模除法要用逆元，且模數通常需為質數。\n- 浮點比較用 eps。\n- 公式推完後用小範圍暴力驗證。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1686 | [石子游戲 VI](https://leetcode.cn/problems/stone-game-vi) | 2001 |\n| 1927 | [求和遊戲](https://leetcode.cn/problems/sum-game) | 2005 |\n| 1131 | [絕對值表示式的最大值](https://leetcode.cn/problems/maximum-of-absolute-value-expression) | 2059 |\n| 3821 | [二進位制中恰好K個1的第N小整數](https://leetcode.cn/problems/find-nth-smallest-integer-with-k-one-bits) | 2069 |\n| 2514 | [統計同位異構字串數目](https://leetcode.cn/problems/count-anagrams) | 2070 |\n| 1808 | [好因子的最大數目](https://leetcode.cn/problems/maximize-number-of-nice-divisors) | 2070 |\n| 1643 | [第 K 條最小指令](https://leetcode.cn/problems/kth-smallest-instructions) | 2080 |\n| 2842 | [統計一個字串的 k 子序列美麗值最大的數目](https://leetcode.cn/problems/count-k-subsequences-of-a-string-with-maximum-beauty) | 2092 |',
        },
        {
          id: 49,
          title: "4.9 單調堆疊",
          summary:
            "**原理講解**\n\n本節要解決的場景是：對陣列中的每個元素，想知道「往左或往右，第一個比它大（或比它小）的元素在哪」。這類問題叫「下一個更大／更小元素」，而解它的工具就是「單調堆疊（monotonic stack）」。\n\n先想暴力法：對每個元素，往一邊一路掃到遇見更大的為止。最壞要 `O(n^2)`，太慢。單調堆疊的核心想法是：**用一個堆疊保存「還沒找到答案的元素」，並讓堆疊裡的值始終保持遞增（或遞減）。當新元素破壞這個順序時，就一次結算所有被它「頂掉」的元素。**\n\n:::example 找每個元素「右邊第一個更大元素」的下標，沒有就記 -1\n為什麼這樣對？因為一個元素被新元素彈出，正代表新元素就是它「右邊第一個更大（或更小）」的答案；而它還留在堆疊裡時，代表右邊還沒有人能贏過它。堆疊裡通常存「下標」而不是「值」，因為我們常需要算距離、寬度。\n\n```cpp\n// 找每個元素「右邊第一個更大元素」的下標，沒有就記 -1\nvector<int> nextGreater(vector<int>& a) {\n  int n = a.size();\n  vector<int> ans(n, -1);\n  stack<int> st;  // 存下標，對應的值由大到小（堆疊底大、頂小）\n  for (int i = 0; i < n; ++i) {\n    while (!st.empty() && a[i] > a[st.top()]) {\n      ans[st.top()] = i;  // a[i] 就是 st.top() 右邊第一個更大者\n      st.pop();\n    }\n    st.push(i);\n  }\n  return ans;\n}\n```\n:::\n\n\n\n關鍵在於：**每個元素最多被 push 一次、pop 一次**，所以整個流程是 `O(n)`，比暴力快一個量級。\n\n**實作方式**\n\n單調堆疊題大多可拆成以下步驟：\n\n:::example 狀態：堆疊存下標，搭配原陣列判斷單調性\n**1. 決定堆疊存什麼、維持哪種單調性。** 通常存下標。要找「更大元素」就維持堆疊內對應值「遞減」（新來更大的就彈出）；要找「更小元素」就維持「遞增」。\n\n```cpp\n// 狀態：堆疊存下標，搭配原陣列判斷單調性\nstack<int> st;  // 由堆疊底到頂，a[...] 遞減 → 用來找「下一個更大」\n```\n:::\n\n\n\n:::example 更新：先把被當前元素頂掉的舊元素結算，再 push 自己\n**2. 掃描時，破壞單調性就先結算再入堆。** 每讀到一個新元素，先把堆疊頂端「打不過它」的元素全部彈出並記下答案，再把自己推進去。彈出的瞬間，那個元素的一側邊界就確定了。\n\n```cpp\n// 更新：先把被當前元素頂掉的舊元素結算，再 push 自己\nwhile (!st.empty() && a[i] > a[st.top()]) {\n  int j = st.top();\n  st.pop();\n  // 此刻 i 是 j 右邊第一個更大者；若要算寬度可用 i - j\n}\nst.push(i);\n```\n:::\n\n\n\n:::example 例：以每個元素為「最小值」能覆蓋的區間 [L+1, R-1]\n**3. 答案讀取：用左右邊界算寬度或貢獻。** 很多題目（如最小值乘積、矩形面積）要的是「某元素能往左右延伸多遠」。一次正向掃 + 一次反向掃，就能得到每個元素左右兩側第一個破壞單調性的邊界。\n\n```cpp\n// 例：以每個元素為「最小值」能覆蓋的區間 [L+1, R-1]\n// left[i] = 左邊第一個更小元素的下標，right[i] = 右邊第一個更小元素的下標\n// 寬度 = right[i] - left[i] - 1，再乘上區間和即為該元素的貢獻\n```\n:::\n\n\n\n[子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product/) 就是這個套路：枚舉每個元素當「最小值」，用單調堆疊求出它能延伸的左右邊界，乘上該區間和。\n\n**4. 注意嚴格與非嚴格的選擇。** 遇到相等元素，用 `>` 還是 `>=` 會影響去重和邊界歸屬。在「貢獻法」中常需要一側用嚴格、一側用非嚴格，才能保證每個子陣列恰好被計算一次，不重不漏。\n\n複雜度：每元素進出堆疊各一次，總計 `O(n)`，空間 `O(n)`。\n\n**題目線索（Pattern）**\n\n出現以下特徵，就可以往單調堆疊想：\n\n- **題目反覆問「下一個更大／更小」「前一個更大／更小」。** [邊界元素是最大值的子陣列數目](https://leetcode.cn/problems/find-the-number-of-subarrays-where-boundary-elements-are-maximum/) 要找以最大值為邊界的子陣列，本質就是定位每個值的最近更大邊界。\n- **要枚舉「以某元素為最大／最小值」的所有子陣列，並算它的貢獻或寬度。** 這正是 [子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product/) 的做法。\n- **暴力是雙重迴圈往一邊掃找邊界，`O(n^2)` 超時。** 單調堆疊能壓到 `O(n)`。\n- **元素只關心「最近一個破壞單調性的鄰居」，而不是全域排序。** 這是單調堆疊和「整個排序」的分界。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的堆疊存的是下標還是值？要維持遞增還是遞減？\n2. 元素被彈出的那一刻，確定了它哪一側的什麼邊界？\n3. 遇到相等元素，我該用嚴格（`>`）還是非嚴格（`>=`）才不會重複計數？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3113 | [邊界元素是最大值的子陣列數目](https://leetcode.cn/problems/find-the-number-of-subarrays-where-boundary-elements-are-maximum/) | 2046 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1856 | [子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product/) | 2051 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n:::example 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  // 單調遞減棧：掃描 nums2，維護「尚未找到下一個更大元素」的元素\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;  // nxt[x] = x 在 nums2 中的下一個更大元素\n    vector<int> st;\n    for (int x : nums2) {\n      // 棧頂元素小於 x，表示 x 就是它們的下一個更大元素\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);  // 找不到則回傳 -1\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3113 | [邊界元素是最大值的子陣列數目](https://leetcode.cn/problems/find-the-number-of-subarrays-where-boundary-elements-are-maximum) | 2046 |\n| 1856 | [子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product) | 2051 |",
        },
        {
          id: 50,
          title: "4.10 滑動視窗",
          summary:
            "**原理講解**\n\n本節要處理的場景是：在一個陣列或字串上，找「一段連續區間」滿足某條件，並對它求最長、最短或計數。這個技巧叫「滑動視窗（sliding window）」。\n\n先想暴力：枚舉所有區間的左右端點，再檢查每段是否合法，這是 `O(n^2)` 甚至 `O(n^3)`。滑動視窗的核心想法是：**用左右兩個指標 `left`、`right` 框住一段窗口，當右端往前擴張、左端往前收縮時，窗口內的摘要資訊（總和、出現次數、種類數…）只要「加上新進來的、減去離開的」就能更新，不必每次重算。**\n\n:::example 滑動視窗骨架：right 擴張、left 收縮，窗口摘要增量維護\n但這招有個前提：**條件要對端點移動具有「單調性」。** 意思是——如果一個窗口太大不合法，繼續放大只會更不合法（所以該縮左端）；如果合法，那它的子窗口也合法。有了這個性質，左指標就永遠只往前、不回頭，整體 `O(n)`。\n\n```cpp\n// 滑動視窗骨架：right 擴張、left 收縮，窗口摘要增量維護\nint left = 0;\nfor (int right = 0; right < n; ++right) {\n  add(a[right]);      // 新元素進窗口，更新摘要\n  while (!valid()) {  // 窗口不合法就縮左端\n    remove(a[left]);  // 舊元素離開窗口\n    ++left;\n  }\n  // 此刻 [left, right] 是合法窗口，可在這裡更新答案\n}\n```\n:::\n\n\n\n要特別注意：**含負數的「總和」條件通常不能直接滑動**，因為加一個元素不一定讓和變大，單調性就壞了，這時要改用前綴和加其他結構。\n\n**實作方式**\n\n滑動視窗題大致照下面步驟組裝：\n\n:::example 狀態：用頻次表維護窗口內各值出現次數，distinct 記種類數\n**1. 決定窗口要維護什麼摘要。** 依題目選擇：總和、各元素出現頻次（用 `unordered_map` 或陣列）、不同種類數、最大值（用單調佇列）等。摘要必須能「加一個元素」「減一個元素」都 `O(1)` 或近似 `O(1)`。\n\n```cpp\n// 狀態：用頻次表維護窗口內各值出現次數，distinct 記種類數\nunordered_map<int, int> cnt;\nint distinct = 0;\n```\n:::\n\n\n\n:::example 擴張：a[right] 進窗口\n**2. 右端擴張，把新元素併入摘要。**\n\n```cpp\n// 擴張：a[right] 進窗口\nif (cnt[a[right]]++ == 0) {\n  ++distinct;  // 第一次出現，種類數 +1\n}\n```\n:::\n\n\n\n:::example 收縮：把 a[left] 移出窗口，維護摘要\n**3. 當窗口不合法或可縮短時，左端前進並移除舊元素。**\n\n```cpp\n// 收縮：把 a[left] 移出窗口，維護摘要\nif (--cnt[a[left]] == 0) {\n  --distinct;  // 降為 0，種類數 -1\n}\n++left;\n```\n:::\n\n\n\n:::example 恰好 K 轉換：寫一個「至多 K 個」的滑動視窗計數函式 atMost\n**4. 在合法窗口處更新答案。** 求最長就在窗口合法時取 `right - left + 1` 的最大值；求最短就在剛滿足條件時取最小。**「恰好 K 個」這類題目常用一個技巧：恰好 K = 至多 K − 至多 (K−1)**，把不好處理的「恰好」轉成兩次「至多」。\n\n```cpp\n// 恰好 K 轉換：寫一個「至多 K 個」的滑動視窗計數函式 atMost\n// 答案 = atMost(K) - atMost(K - 1)\n```\n:::\n\n\n\n[摘水果](https://leetcode.cn/problems/maximum-fruits-harvested-after-at-most-k-steps/) 與 [使陣列連續的最少運算元](https://leetcode.cn/problems/minimum-number-of-operations-to-make-array-continuous/) 都是先排序或定位後，用窗口找「能涵蓋的最多元素」；[查詢給定雜湊值的子串](https://leetcode.cn/problems/find-substring-with-given-hash-value/) 則是固定長度窗口配合滾動雜湊增量更新。\n\n複雜度：左右指標各最多走過陣列一遍，總計 `O(n)`（若摘要含排序或 `map` 則可能 `O(n log n)`）。\n\n**題目線索（Pattern）**\n\n看到以下特徵，往滑動視窗想：\n\n- **問的是「連續」子陣列／子字串，求最長、最短或數量。** 注意「連續」是關鍵詞；不連續的就不是這招。\n- **條件對端點移動有單調性。** 例如 [毯子覆蓋的最多白色磚塊數](https://leetcode.cn/problems/maximum-white-tiles-covered-by-a-carpet/)：毯子（固定長度窗口）右移時能覆蓋的範圍可增量計算。\n- **暴力是枚舉所有區間再檢查，`O(n^2)` 超時。**\n- **「恰好 K」型，可拆成兩次「至多」。** 像 [使二進位制字串字元交替的最少反轉次數](https://leetcode.cn/problems/minimum-number-of-flips-to-make-the-binary-string-alternating/) 這種需要在固定或可變長度上累積差異的題目。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的條件在窗口放大／縮小時，是不是單調變化的？\n2. 窗口的摘要能不能在「加一個」「減一個」時 `O(1)` 更新？\n3. 題目要的是最長、最短，還是計數？若是「恰好 K」，我要不要拆成兩次「至多」？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1888 | [使二進位制字串字元交替的最少反轉次數](https://leetcode.cn/problems/minimum-number-of-flips-to-make-the-binary-string-alternating/) | 2006 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2271 | [毯子覆蓋的最多白色磚塊數](https://leetcode.cn/problems/maximum-white-tiles-covered-by-a-carpet/) | 2022 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2106 | [摘水果](https://leetcode.cn/problems/maximum-fruits-harvested-after-at-most-k-steps/) | 2062 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2156 | [查詢給定雜湊值的子串](https://leetcode.cn/problems/find-substring-with-given-hash-value/) | 2063 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n| 2009 | [使陣列連續的最少運算元](https://leetcode.cn/problems/minimum-number-of-operations-to-make-array-continuous/) | 2084 | 先判斷合法性是否隨左右端單調變化；再決定何時移動左端與何時計數。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 3. 無重複字元的最長子串 展示核心寫法。\n\n**完整題目**：給定字串 `s`，回傳不含重複字元的最長子字串長度。\n\n:::example 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n**線索**：題目限制連續子陣列/子字串，且合法性可透過移動左端恢復，這是滑動視窗的線索。\n\n```cpp\nclass Solution {\n public:\n  int lengthOfLongestSubstring(string s) {\n    // 滑動視窗：cnt 記錄視窗內每個字元的出現次數\n    vector<int> cnt(256);\n    int ans = 0, left = 0;\n    for (int right = 0; right < (int)s.size(); ++right) {\n      cnt[s[right]]++;\n      // 若新字元造成重複，縮小左邊界直到視窗內無重複\n      while (cnt[s[right]] > 1) {\n        cnt[s[left++]]--;\n      }\n      ans = max(ans, right - left + 1);  // 更新無重複子字串最大長度\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 右端加入新元素後，只在違反條件時移動左端；因此每個位置最多進出視窗一次，總複雜度為 O(n)。\n- 右端加入新元素後更新窗口狀態；若條件被破壞，就移動左端直到恢復合法。\n- 每個下標最多被加入與移出一次，因此整體是線性掃描。\n\n**常見變形**\n\n- 越短越合法：違法時收縮，更新最長。\n- 越長越合法：合法時收縮，更新最短。\n- 恰好型可轉成 `atMost(k)-atMost(k-1)`。\n\n**實作注意事項**\n\n- 先判斷合法性方向，再寫 while 條件。\n- 答案計數時常用 `right-left+1` 或 `left`，兩者語意不同。\n- 頻次表字元集可用陣列，值域大時用 map。\n- 含負數時滑窗的單調性可能失效。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1888 | [使二進位制字串字元交替的最少反轉次數](https://leetcode.cn/problems/minimum-number-of-flips-to-make-the-binary-string-alternating) | 2006 |\n| 2271 | [毯子覆蓋的最多白色磚塊數](https://leetcode.cn/problems/maximum-white-tiles-covered-by-a-carpet) | 2022 |\n| 2106 | [摘水果](https://leetcode.cn/problems/maximum-fruits-harvested-after-at-most-k-steps) | 2062 |\n| 2156 | [查詢給定雜湊值的子串](https://leetcode.cn/problems/find-substring-with-given-hash-value) | 2063 |\n| 2009 | [使陣列連續的最少運算元](https://leetcode.cn/problems/minimum-number-of-operations-to-make-array-continuous) | 2084 |\n| 2444 | [統計定界子陣列的數目](https://leetcode.cn/problems/count-subarrays-with-fixed-bounds) | 2093 |",
        },
        {
          id: 51,
          title: "4.11 字串",
          summary:
            "**原理講解**\n\n本節聚焦字串題的共同核心：**重用已經比對過的資訊，避免每次比較、匹配或查詢都從頭來過。** 字串題的暴力解常常是「兩兩比對」或「每個位置都重新掃一遍」，動輒 `O(n^2)` 甚至更糟；字串技巧的價值就在於先做一次預處理，之後每次查詢只要 `O(1)` 或均攤 `O(1)`。\n\n:::example 前綴雜湊：把字串看成 base 進位的數，預處理後 O(1) 取任意子串雜湊\n以「子字串是否相等」為例。如果每問一次就逐字元比，最壞 `O(n)` 一次。改用「前綴雜湊（prefix hash）」：先把字串每個前綴算成一個數字（像把字串看成某個進位的整數），任意子字串的雜湊就能由前綴相減在 `O(1)` 得到，比較兩段是否相等只需比兩個數字。\n\n```cpp\n// 前綴雜湊：把字串看成 base 進位的數，預處理後 O(1) 取任意子串雜湊\nconst long long B = 131, M = 1e9 + 7;\nvector<long long> h(n + 1, 0), p(n + 1, 1);\nfor (int i = 0; i < n; ++i) {\n  h[i + 1] = (h[i] * B + s[i]) % M;  // 前綴雜湊\n  p[i + 1] = p[i] * B % M;           // base 的次方，供對齊用\n}\n// 子串 s[l..r] 的雜湊：\n// (h[r + 1] - h[l] * p[r + 1 - l]) % M  （記得處理負數取模）\n```\n:::\n\n\n\n:::example KMP 的 pi 陣列：pi[i] = s[0..i] 的最長 border 長度\n另一條主線是「字串匹配 / 找重複前綴」用的 border 概念。一個字串的 border 指「既是前綴又是後綴」的片段；KMP 的 `pi` 陣列（也叫 next 陣列）就是記每個位置的最長 border 長度。掃描比對時若失配，可以利用 border 直接跳到下一個可能的對齊位置，而不是退回開頭重比。\n\n```cpp\n// KMP 的 pi 陣列：pi[i] = s[0..i] 的最長 border 長度\nvector<int> pi(n, 0);\nfor (int i = 1, j = 0; i < n; ++i) {\n  while (j > 0 && s[i] != s[j]) {\n    j = pi[j - 1];  // 失配就沿 border 回退\n  }\n  if (s[i] == s[j]) {\n    ++j;\n  }\n  pi[i] = j;\n}\n```\n:::\n\n\n\n[有序佇列](https://leetcode.cn/problems/orderly-queue/) 在 `k = 1` 時要在所有「旋轉」中找字典序最小者，正是「用字串的週期 / border 性質避免逐一暴力比較」的典型場景。\n\n**實作方式**\n\n字串題依用到的工具，狀態與步驟不同，但流程相近：\n\n:::example 狀態選擇備忘\n**1. 選對狀態結構。** 想 `O(1)` 比較子串相等 → 前綴雜湊；想做字串匹配 / 找最長 border → KMP 的 `pi`；想處理回文 → Manacher 的回文半徑；想處理大量字串前綴查詢 → Trie（字典樹）。\n\n```cpp\n// 狀態選擇備忘：\n// pi / Z 陣列  -> 匹配、週期、最長 border\n// 前綴 hash   -> O(1) 比較任意子串是否相等\n// 回文半徑    -> Manacher，求最長回文 / 回文計數\n```\n:::\n\n\n\n**2. 按字元掃描，邊掃邊維護狀態。** 不論哪種方法，主體都是一次線性掃描，維護「目前匹配長度」「最右匹配區間」「雜湊前綴」之類的量。\n\n:::example 例：用 pi 陣列做匹配，在文字 t 中找模式 s\n**3. 答案從預處理狀態直接讀。** 匹配位置、最長公共前綴（LCP）、回文長度、子串相等性，都從上一步建好的陣列查表得到，不再重新比對。\n\n```cpp\n// 例：用 pi 陣列做匹配，在文字 t 中找模式 s\n// 先對 s 建 pi，再掃 t；j 走到 s.size() 即找到一次完整匹配\n```\n:::\n\n\n\n**4. 注意邊界與正確性。**\n\n- **雜湊有碰撞風險**：兩個不同子串可能算出同一個雜湊。卡得緊的題目要用雙雜湊（兩組 base/模數）降低誤判。\n- **取模相減可能變負**：要 `(x % M + M) % M` 修正。\n- **KMP / Z 的回退邏輯**容易差一格，務必用小例子驗證。\n\n複雜度：KMP、Z、Manacher 都是 `O(n)` 預處理；前綴雜湊 `O(n)` 預處理後每次查詢 `O(1)`；Trie 的查詢與字串長度成正比。\n\n**題目線索（Pattern）**\n\n出現以下訊號，往字串技巧想：\n\n- **要反覆比較子串是否相等、找出現位置、求最長公共前綴。** 暴力逐字元比會 `O(n^2)`，提示用雜湊或 KMP。\n- **題目和「旋轉」「週期」「border」「重複前綴」有關。** 像 [有序佇列](https://leetcode.cn/problems/orderly-queue/) 比較各旋轉的字典序。\n- **要處理回文（最長回文子串、回文計數）** → Manacher。\n- **大量字串共享前綴、要做前綴匹配或統計** → Trie。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我重複在做的「比較」或「掃描」，能不能用一次預處理換成 `O(1)` 查詢？\n2. 我選的結構（hash / pi / 回文半徑 / Trie）對應的不變式是什麼？\n3. 如果用雜湊，碰撞會不會影響正確性？需不需要雙雜湊？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 899 | [有序佇列](https://leetcode.cn/problems/orderly-queue/) | 2097 | 重點是重用已比對資訊；練習把 border、LCP、hash 或自動機狀態寫成不變式。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 28. 找出字串中第一個匹配項的下標 展示核心寫法。\n\n**完整題目**：在字串 `haystack` 中尋找字串 `needle` 第一次出現的位置，若不存在則回傳 `-1`。\n\n:::example KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線…\n**線索**：題目需要大量子串匹配、前後綴或相等比較；重用已比較資訊就是 KMP/Z/雜湊/自動機的線索。\n\n```cpp\nclass Solution {\n public:\n  // KMP 字串匹配：先建立 needle 的前綴函數 pi，再在 haystack 上做線性搜尋\n  int strStr(string haystack, string needle) {\n    int m = needle.size();\n    vector<int> pi(m);  // pi[i]：needle[0..i] 的最長相等真前後綴長度\n    // 建構 pi 陣列\n    for (int i = 1; i < m; ++i) {\n      int j = pi[i - 1];\n      // 若失配，利用 pi 回退到上一個可能匹配的位置\n      while (j && needle[i] != needle[j]) {\n        j = pi[j - 1];  // 失配時回退，不從頭重新匹配\n      }\n      if (needle[i] == needle[j]) {\n        j++;\n      }\n      pi[i] = j;\n    }\n    // 在 haystack 中用 pi 輔助匹配\n    for (int i = 0, j = 0; i < (int)haystack.size(); ++i) {\n      while (j && haystack[i] != needle[j]) {\n        j = pi[j - 1];\n      }\n      if (haystack[i] == needle[j]) {\n        j++;\n      }\n      if (j == m) {  // 完整匹配，返回起始位置\n        return i - m + 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `pi[i]` 表示前綴的最長 border；失配時跳到下一個可能 border，而不是回頭重比。\n- 預處理陣列或狀態機保存已比對資訊，主迴圈失配時不用回到原點重比。\n- 下標與長度的換算要固定，尤其是 border、LCP、半徑與回傳起點。\n\n**常見變形**\n\n- KMP 處理模式串 border。\n- Z 函式處理每個後綴和整串的 LCP。\n- 雜湊適合快速判等，AC 自動機適合多模式。\n\n**實作注意事項**\n\n- 索引從 0 開始時 border 長度與下標要分清。\n- 雜湊建議雙模或 unsigned long long，並理解碰撞風險。\n- 字元集大時不要硬開 26。\n- 空模式或長度 1 要特判。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 899 | [有序佇列](https://leetcode.cn/problems//orderly-queue/) | 2097 |",
        },
        {
          id: 52,
          title: "4.12 樹和二元樹",
          summary:
            "**原理講解**\n\n本節處理的是「在樹上傳遞資訊」的題目。樹是一種沒有環的連通結構：有一個根，每個節點往下接幾個子節點。核心技巧是用 DFS（深度優先搜尋，沿著一條路一直往下走到底再回頭）或 BFS（廣度優先搜尋，一層一層往外擴）在樹上走訪，並在走訪時讓資訊在節點間流動。\n\n資訊流動有兩個方向，這是理解全部樹題的關鍵：\n\n- **自底向上彙總**：先算完所有子節點，再把它們的結果合併成「這棵子樹的摘要」回傳給父節點。例如子樹的節點數、子樹的高度、子樹的路徑和。\n- **自頂向下攜帶**：從根往下走時，把「祖先那邊的資訊」一路帶下來給子節點。例如目前的深度、從根到這裡累積的權重。\n\n:::example 自底向上：回傳「以 node 為根的子樹大小」，順手把每棵子樹大小記下\n最常見的形態是一個遞迴函式：它先遞迴處理每個子節點（往下），拿到回傳值後合併（往上）。\n\n```cpp\n// 自底向上：回傳「以 node 為根的子樹大小」，順手把每棵子樹大小記下\nint dfs(int node, int parent, vector<vector<int>>& g, vector<int>& size) {\n  int s = 1;  // 自己算一個\n  for (int child : g[node]) {\n    if (child == parent) {\n      continue;  // 不要走回父節點\n    }\n    s += dfs(child, node, g, size);  // 加上每棵子樹的大小\n  }\n  size[node] = s;  // 子樹摘要：大小\n  return s;\n}\n```\n:::\n\n\n\n[修改後子樹的大小](https://leetcode.cn/problems/find-subtree-sizes-after-changes/) 就是這種「回傳子樹摘要」的直接應用。\n\n有些題目同時需要「子樹資訊」和「子樹以外的資訊」，這時往往要 **兩次 DFS**：第一次自底向上算出每棵子樹的答案，第二次自頂向下，把「父側 / 其他分支」的資訊補進來。\n\n**實作方式**\n\n樹題的組裝步驟如下：\n\n:::example 狀態設計範例：回傳 pair = {子樹高度, 是否平衡}\n**1. 想清楚每個節點要回傳什麼摘要。** 這是最重要的一步。常見摘要：高度、子樹大小、子樹路徑和、是否平衡、子樹內最佳路徑等。摘要要能「由子節點的摘要合併出來」。\n\n```cpp\n// 狀態設計範例：回傳 pair = {子樹高度, 是否平衡}\npair<int, bool> dfs(TreeNode* node);\n```\n:::\n\n\n\n:::example 合併：先收集所有子節點結果，再決定自己的狀態\n**2. 遞迴處理子節點後再合併。** 標準形態是先對每個子節點呼叫 DFS，拿到結果，再算自己的摘要。樹形 DP（在樹上做動態規劃）也是這個骨架，只是回傳值換成 DP 狀態。\n\n```cpp\n// 合併：先收集所有子節點結果，再決定自己的狀態\nint best = 0;\nfor (int child : children) {\n  int sub = dfs(child, node);\n  best = max(best, sub);  // 依題意合併（這裡取最大）\n}\n```\n:::\n\n\n\n:::example 換根/第二次 DFS：把父側資訊 fromParent 帶下去\n**3. 需要父側資訊時做第二次 DFS（換根）。** 第一次算子樹，第二次把父方向的資訊往下傳。[樹上最大得分和路徑](https://leetcode.cn/problems/most-profitable-path-in-a-tree/) 需要同時考慮兩個角色在樹上的行進，常用一次 DFS 標記時間 / 路徑，再做一次計算得分。\n\n```cpp\n// 換根/第二次 DFS：把父側資訊 fromParent 帶下去\nvoid dfs2(int node, int parent, int fromParent) {\n  // 用 fromParent 結合子樹摘要算出該節點的全域答案\n  for (int child : g[node]) {\n    if (child != parent) {\n      dfs2(child, node, /* 由 node 算給 child 的父側值 */ 0);\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 答案讀取與邊界。** 答案可能在「根的回傳值」「一個全域變數」「每個節點各自的 DP」或「查詢時沿祖先跳躍的結果」。注意：\n\n- **遞迴太深可能爆堆疊（stack overflow）**，極深的樹要改成迭代或加大堆疊。\n- 路徑、直徑類題目要分清「能往父節點延伸的值」與「在此節點閉合的答案」，兩者常常不同。\n\n複雜度：每個節點與每條邊各處理常數次，DFS/BFS 為 `O(n)`；若加倍增（求 LCA）則預處理 `O(n log n)`、每次查詢 `O(log n)`。\n\n**題目線索（Pattern）**\n\n看到以下特徵，往樹上 DFS/BFS 想：\n\n- **輸入是一棵樹（給父子關係、邊清單，或二元樹節點），要對「每棵子樹」或「每條路徑」算東西。** 像 [修改後子樹的大小](https://leetcode.cn/problems/find-subtree-sizes-after-changes/)、[樹上最大得分和路徑](https://leetcode.cn/problems/most-profitable-path-in-a-tree/)。\n- **答案能由「子節點的答案」合併得到** → 自底向上。\n- **每個節點的答案需要「來自父方向 / 其他分支」的資訊** → 兩次 DFS（換根）。\n- **題目雖然不長相像樹，但隱含遞迴的層次結構**，例如 [索引處的解碼字串](https://leetcode.cn/problems/decoded-string-at-index/) 的展開過程可看成自底向上回推。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS 每個節點要回傳什麼摘要？它能由子節點的摘要合併出來嗎？\n2. 答案只靠子樹資訊就夠，還是需要父方向資訊（要不要第二次 DFS）？\n3. 樹可能多深？需不需要擔心遞迴爆堆疊？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 880 | [索引處的解碼字串](https://leetcode.cn/problems/decoded-string-at-index/) | 2011 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3331 | [修改後子樹的大小](https://leetcode.cn/problems/find-subtree-sizes-after-changes/) | 2046 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2467 | [樹上最大得分和路徑](https://leetcode.cn/problems/most-profitable-path-in-a-tree/) | 2053 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1718 | [構建字典序最大的可行序列](https://leetcode.cn/problems/construct-the-lexicographically-largest-valid-sequence/) | 2080 | 放在「樹和二元樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 880 | [索引處的解碼字串](https://leetcode.cn/problems//decoded-string-at-index/) | 2011 |\n| 3331 | [修改後子樹的大小](https://leetcode.cn/problems//find-subtree-sizes-after-changes/) | 2046 |\n| 2467 | [樹上最大得分和路徑](https://leetcode.cn/problems//most-profitable-path-in-a-tree/) | 2053 |\n| 1718 | [構建字典序最大的可行序列](https://leetcode.cn/problems//construct-the-lexicographically-largest-valid-sequence/) | 2080 |",
        },
      ],
    },
    {
      id: 53,
      title: "5. 小專題：固定一個維度",
      summary:
        "**原理講解**\n\n本節的技巧叫「固定一個維度」。它專治那種「要從所有『配對』或『兩兩組合』裡找答案」的題目——這些題目暴力做就是兩層迴圈，`O(n^2)`，資料一大就超時。\n\n核心想法非常單純：**既然答案牽涉「一對」元素（記作 i 和 j），那就只用一層迴圈枚舉其中一個（例如 j，當作「右邊那個」），另一個（i，所有「左邊已經看過的」）不要重新掃，而是邊走邊維護成一份摘要，讓我們對每個 j 能用 `O(1)` 或 `O(log n)` 直接查到需要的東西。** 一個維度用迴圈「固定」住，另一個維度壓進可維護的狀態裡。\n\n:::example 固定 j，維護左側最佳的 (values[i] + i)\n看 [最佳觀光組合](https://leetcode.cn/problems/best-sightseeing-pair/)：要最大化 `values[i] + values[j] + i - j`（其中 `i < j`）。式子可以拆成「只跟 i 有關的 `values[i] + i`」加「只跟 j 有關的 `values[j] - j`」。固定 j 往右掃時，前面那塊只要記住「目前看過的最大 `values[i] + i`」即可：\n\n```cpp\n// 固定 j，維護左側最佳的 (values[i] + i)\nint best = values[0] + 0, ans = INT_MIN;\nfor (int j = 1; j < n; ++j) {\n  ans = max(ans,\n            best + values[j] - j);  // 用左側已知最佳，O(1) 算出含 j 的最好答案\n  best = max(best, values[j] + j);  // 把 j 併入「左側摘要」，給後面的人用\n}\n```\n:::\n\n\n\n關鍵在於：原本要對每個 j 回頭看所有 i，現在因為「左側摘要」一路維護著，每個 j 只花常數時間，總共 `O(n)`。\n\n維護的「摘要」不一定是一個最大值，也可能是一張計數表、一個排序結構，甚至樹狀陣列，視題目要查什麼而定。\n\n**實作方式**\n\n固定一維題大致照以下步驟：\n\n**1. 找出題目裡的「兩個維度」，決定固定哪一個。** 通常固定「右端 j」往右掃，把「左端 i」累積成摘要。先問自己：對每個 j，我需要知道左側什麼資訊？\n\n:::example 摘要結構選擇\n**2. 設計左側摘要的資料結構。** 依「要查什麼」選結構：\n\n```cpp\n// 摘要結構選擇：\n// 要左側某個量的最大/最小   -> 一個變數\n// 要左側某值出現過幾次       -> unordered_map / 陣列\n// 要左側有幾個元素 < 某門檻 -> 排序後二分，或樹狀陣列\n```\n:::\n\n\n\n:::example 好對子：把條件化成 f(i)==f(j)，固定 j 查左側 f 出現次數\n[統計一個陣列中好對子的數目](https://leetcode.cn/problems/count-nice-pairs-in-an-array/) 把條件變形成「某個函數值相等」，於是摘要就是一張「該函數值出現次數」的計數表：\n\n```cpp\n// 好對子：把條件化成 f(i)==f(j)，固定 j 查左側 f 出現次數\nunordered_map<long long, int> cnt;\nlong long ans = 0;\nfor (int j = 0; j < n; ++j) {\n  long long key = f(nums[j]);\n  ans += cnt[key];  // 左側有幾個同 key，就配出幾對\n  cnt[key]++;       // 把自己加入摘要\n}\n```\n:::\n\n\n\n**3. 更新流程：對每個 j，先查再加。** 順序很重要——先用「目前的左側摘要」算出含 j 的答案，**再**把 j 自己加入摘要。這樣能保證只配對 `i < j`，不會把自己跟自己算進去。\n\n**4. 答案讀取與邊界。** 答案通常在掃描過程中累加或取極值。若條件是不等式（如 [滿足不等式的數對數目](https://leetcode.cn/problems/number-of-pairs-satisfying-inequality/)）或需要排序、區間計數（如 [統計定界子陣列的數目](https://leetcode.cn/problems/count-subarrays-with-fixed-bounds/)、[奇偶跳](https://leetcode.cn/problems/odd-even-jump/)），摘要就升級成二分 / 樹狀陣列 / 單調結構，但「固定一維、維護另一維」的骨架不變。\n\n複雜度：摘要查詢 `O(1)` 時總計 `O(n)`；需要二分或樹狀陣列時 `O(n log n)`——仍遠勝暴力的 `O(n^2)`。\n\n**題目線索（Pattern）**\n\n看到以下特徵，往「固定一個維度」想：\n\n- **答案定義在「一對」元素上（i 和 j），且暴力是雙重迴圈。** 例如數對計數、最佳組合。\n- **配對條件能拆成「只跟 i 有關」與「只跟 j 有關」兩塊，或能化成「某函數值相等 / 某值小於門檻」。** 一旦能拆，左側那塊就能預先維護成摘要。\n- **你發現第二層迴圈每次都在「重新掃左半邊找最大 / 計數 / 比較」**——這正是該被摘要取代的重複工作。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我打算固定哪一維（通常是右端 j）？對每個 j，我需要左側的什麼資訊？\n2. 這份「左側摘要」該用什麼結構？是一個變數、計數表，還是要支援二分 / 區間查詢？\n3. 我有沒有遵守「先查摘要、再把自己加入」的順序，確保只配對 `i < j`？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1014 | [最佳觀光組合](https://leetcode.cn/problems/best-sightseeing-pair/) | 1730 | 要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。 |\n| 1814 | [統計一個陣列中好對子的數目](https://leetcode.cn/problems/count-nice-pairs-in-an-array/) | 1738 | 要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。 |\n| 2426 | [滿足不等式的數對數目](https://leetcode.cn/problems/number-of-pairs-satisfying-inequality/) | 2030 | 要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。 |\n| 2444 | [統計定界子陣列的數目](https://leetcode.cn/problems/count-subarrays-with-fixed-bounds/) | 2093 | 要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。 |\n| 975 | [奇偶跳](https://leetcode.cn/problems/odd-even-jump/) | 2079 | 要固定一個枚舉維度，再維護另一側已看過的資訊；重點是避免雙重暴力重複掃描。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 498. 對角線遍歷 展示核心寫法。\n\n**完整題目**：給定矩陣，依題目指定的鋸齒狀對角線順序回傳所有元素。\n\n:::example 對角線順序遍歷：按反對角線編號 s = r + c 分組，偶數對角線反轉方向\n**線索**：題目看似多重枚舉，但其中一維可以固定，另一側用已掃描資訊或分組鍵維護。\n\n```cpp\nclass Solution {\n public:\n  // 對角線順序遍歷：按反對角線編號 s = r + c 分組，偶數對角線反轉方向\n  vector<int> findDiagonalOrder(vector<vector<int>>& mat) {\n    int m = mat.size(), n = mat[0].size();\n    vector<int> ans;\n    for (int s = 0; s <= m + n - 2; ++s) {  // s 為反對角線編號，共 m+n-1 條\n      vector<int> cur;\n      // 枚舉該對角線上所有合法的列索引\n      for (int r = max(0, s - n + 1); r <= min(m - 1, s); ++r) {\n        int c = s - r;\n        cur.push_back(mat[r][c]);\n      }\n      if (s % 2 == 0) {\n        reverse(cur.begin(), cur.end());  // 偶數對角線由下往上，需反轉\n      }\n      ans.insert(ans.end(), cur.begin(), cur.end());\n    }\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 同一條副對角線滿足 `r+c` 相同；先按對角線分組，再依題目要求決定是否反轉。\n- 外層迴圈固定一個維度，內層只處理與該維度相關的必要候選。\n- 分組鍵、左右端點或對角線編號要先算清合法範圍，避免漏算邊界。\n\n**常見變形**\n\n- 枚舉右端點並維護左側資訊。\n- 枚舉中心點處理左右對稱。\n- 依 `r+c` 或 `r-c` 分組遍歷矩陣對角線。\n\n**實作注意事項**\n\n- 先寫出枚舉變數的合法範圍。\n- 二維轉一維時檢查邊界是否越界。\n- 分組鍵可能為負，使用 map 或加偏移。\n- 若每組需要排序，總複雜度要算所有組的總和。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1014 | [最佳觀光組合](https://leetcode.cn/problems/best-sightseeing-pair) | 1730 |\n| 1814 | [統計一個陣列中好對子的數目](https://leetcode.cn/problems/count-nice-pairs-in-an-array) | 1738 |\n| 2426 | [滿足不等式的數對數目](https://leetcode.cn/problems/number-of-pairs-satisfying-inequality) | 2030 |\n| 2444 | [統計定界子陣列的數目](https://leetcode.cn/problems/count-subarrays-with-fixed-bounds) | 2093 |\n| 975 | [奇偶跳](https://leetcode.cn/problems/odd-even-jump) | 2079 |\n| 1011 | [在 D 天內送達包裹的能力](https://leetcode.cn/problems/capacity-to-ship-packages-within-d-days) | 1725 |\n| 875 | [愛喫香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) | 1766 |\n| 3281 | [範圍內整數的最大得分](https://leetcode.cn/problems/maximize-score-of-numbers-in-ranges) | 1768 |\n| 2359 | [找到離給定兩個節點最近的節點](https://leetcode.cn/problems/find-closest-node-to-given-two-nodes) | 1715 |\n| 2385 | [感染二叉樹需要的總時間](https://leetcode.cn/problems/amount-of-time-for-binary-tree-to-be-infected) | 1711 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems/minimum-fuel-cost-to-report-to-the-capital) | 2012 |\n| 2106 | [摘水果](https://leetcode.cn/problems/maximum-fruits-harvested-after-at-most-k-steps) | 2062 |",
    },
    {
      id: 9002,
      title: "6. 常見面試模式",
      summary:
        "衝刺 2100 分的關鍵不在背題，而在於從題目訊號快速對應到「解法模式」。下表整理高頻區段最常考的通用模式，幫你在限時內收斂方向。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 滑動視窗 | 連續子陣列計數或最值、約束可單調維護 | 右擴左縮，維護視窗統計量，`O(n)` | [乘積小於 K 的子數組](https://leetcode.cn/problems/subarray-product-less-than-k) |\n| 單調堆疊 | 找下一個更大/更小元素、區間貢獻 | 維護單調性，彈出時結算貢獻 | [子數組的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums) |\n| 前綴和 + 雜湊 | 子陣列和等於定值、可整除性質 | 記錄前綴和出現次數或餘數 | [和為 K 的子數組](https://leetcode.cn/problems/subarray-sum-equals-k) |\n| 二分答案 | 最小化最大值、可行性單調 | 對答案二分，貪心判定可行性 | [愛吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) |\n| 區間 DP | 對區間做合併、$n \\le 500$ | 枚舉分割點，狀態 $dp[i][j]$ | [戳氣球](https://leetcode.cn/problems/burst-balloons) |\n| 並查集 | 連通性、動態合併、離線查詢 | 路徑壓縮 + 按秩合併，近 `O(1)` | [省份數量](https://leetcode.cn/problems/number-of-provinces) |\n| 位元壓縮 DP | 子集狀態、$n \\le 20$ | 以位元遮罩表示集合，子集轉移 | [我能贏嗎](https://leetcode.cn/problems/can-i-win) |\n\n小提醒：\n\n- 先讀約束估複雜度上限：$n \\le 20$ 想狀態壓縮，$n \\le 500$ 想 $O(n^3)$ 區間 DP。\n- 「最小化最大值/最大化最小值」幾乎都是二分答案的訊號。\n- 卡住時退一步問：能不能轉成已知模式？多數 2100 題是基本模式的組合。",
    },
  ],
} as TutorialData.Root;
