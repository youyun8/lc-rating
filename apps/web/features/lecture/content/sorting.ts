import type { TutorialData } from "@/types";

export const sorting = {
  id: 1,
  title: "排序（計數排序／基數排序／桶排序）",
  src: null,
  last_update: "2026-06-04T00:00:00+00:00",
  summary:
    "> *當你不需要「比較」，就能突破 O(n log n) 的排序下界。*\n\n**前置知識**：熟悉 `std::sort`、穩定排序（stable sort）的概念，以及前綴和。\n\n任何只靠「兩兩比較」取得資訊的排序，理論下界都是 `O(n log n)`。但當待排序的鍵是「範圍有限的整數」時，我們可以改用「以值本身當下標、直接定位」的策略，不做比較，把排序壓到線性。\n\n**本講義涵蓋**\n- **計數排序（Counting Sort）**：小值域整數，`O(n + k)`。\n- **基數排序（Radix Sort）**：大值域整數逐位排序，`O(d·(n + b))`。\n- **桶排序（Bucket Sort）**：依區間／頻率分桶，平均 `O(n)`（含頻率桶、鴿巢桶）。\n\n**如何選擇**\n- 值域與 n 同級（例如 `0 <= x <= 1000`）→ 計數排序。\n- 值域很大但位數固定（例如 32-bit 整數）→ 基數排序。\n- 求 Top-K 頻率、相鄰最大差、或資料近似均勻分佈 → 桶排序。\n\n**三種非比較排序一覽**\n\n| 技術 | 適用情境 | 時間複雜度 | 額外空間 | 穩定性 |\n| --- | --- | --- | --- | --- |\n| 計數排序 | 值域小且與 n 同級（0..k） | `O(n + k)` | `O(n + k)` | 前綴和版可穩定 |\n| 基數排序 | 值域大但位數固定的整數／等長字串 | `O(d·(n + b))` | `O(n + b)` | 是（每輪須穩定） |\n| 桶排序 | 資料近似均勻分佈，或求 Top-K 頻率 | 平均 `O(n)`、最壞 `O(n^2)` | `O(n)` | 視桶內排序而定 |\n\n:::example 計數排序：對值域 [0, k] 的整數排序，O(n + k)\n**計數排序模板（值域 [0, k]）**\n\n```cpp\n// 計數排序：對值域 [0, k] 的整數排序，O(n + k)\nvector<int> countingSort(const vector<int>& a, int k) {\n  vector<int> cnt(k + 1, 0);\n  for (int x : a) cnt[x]++;  // 1) 統計每個值出現的次數\n  vector<int> res;\n  for (int v = 0; v <= k; ++v)  // 2) 依值由小到大倒出\n    while (cnt[v]-- > 0) res.push_back(v);\n  return res;\n}\n```\n:::\n\n\n\n**參考資料**：[靈茶山艾府（0x3F）的 Bilibili 主頁](https://space.bilibili.com/206214/) —— 基礎演算法精講系列，可搭配本講義一起學習。\n",
  children: [
    {
      id: 2,
      title: "1. 計數排序",
      summary:
        "**原理講解**\n\n比較排序（quicksort、mergesort、`std::sort`）的時間下界是 `O(n log n)`，因為它們只透過「兩兩比較」取得資訊。但如果待排序的是「範圍有限的整數」（例如 0..k），我們可以**完全不比較**，改用「數值本身當作下標」直接定位，達到 `O(n + k)`。\n\n核心想法只有三步：**先數每個值出現幾次，再決定每個值該放哪，最後放回去。**\n\n:::example 計數排序：對值域 [0, k] 的整數排序，O(n + k)\n最單純的版本（只需要排序結果、值域是 0..k）：\n\n```cpp\n// 計數排序：對值域 [0, k] 的整數排序，O(n + k)\nvector<int> countingSort(const vector<int>& a, int k) {\n  vector<int> cnt(k + 1, 0);\n  for (int x : a) cnt[x]++;  // 1) 統計每個值出現的次數\n  vector<int> res;\n  for (int v = 0; v <= k; ++v)  // 2) 依值由小到大倒出\n    while (cnt[v]-- > 0) res.push_back(v);\n  return res;\n}\n```\n:::\n\n\n\n:::example 穩定計數排序：用前綴和決定每個元素的最終位置（也是基數排序的基礎）\n如果不只是排數字，而是要「帶著整個物件一起搬移」（例如依分數排整列資料）並保持等值元素的原順序（**穩定**），就要用「前綴和」版本：前綴和讓我們知道「值 <= v 的元素有幾個」，也就是值 v 的元素該結束在哪個位置。\n\n```cpp\n// 穩定計數排序：用前綴和決定每個元素的最終位置（也是基數排序的基礎）\nvector<int> stableCountingSort(const vector<int>& a, int k) {\n  int n = a.size();\n  vector<int> cnt(k + 1, 0), res(n);\n  for (int x : a) cnt[x]++;\n  for (int v = 1; v <= k; ++v)\n    cnt[v] += cnt[v - 1];           // 前綴和：cnt[v] = 值 <= v 的個數\n  for (int i = n - 1; i >= 0; --i)  // 由後往前掃才能保持穩定\n    res[--cnt[a[i]]] = a[i];\n  return res;\n}\n```\n:::\n\n\n\n何時值得用：當 `k` 與 `n` 同數量級（`k = O(n)`）時，計數排序是 `O(n)`，遠勝 `O(n log n)`；但若 `k` 遠大於 `n`（例如值到 `1e9`），桶陣列的空間與時間都會爆炸，這時要改用**基數排序**或**桶排序**。\n\n**實作方式**\n\n任何計數排序題都可拆成四個固定步驟：\n\n:::example 平移處理負數：把值域 [mn, mx] 映射到下標 [0, mx - mn]\n**1. 找值域、平移成非負下標。** 值可能有負數，用 `x - mn` 把值域 `[mn, mx]` 映射到下標 `[0, mx-mn]`。\n\n```cpp\n// 平移處理負數：把值域 [mn, mx] 映射到下標 [0, mx - mn]\nint mn = *min_element(a.begin(), a.end());\nint mx = *max_element(a.begin(), a.end());\nvector<int> cnt(mx - mn + 1, 0);\nfor (int x : a) cnt[x - mn]++;\n```\n:::\n\n\n\n**2. 計數。** 掃一遍把每個值的出現次數記進 `cnt`。\n\n**3. 輸出 / 前綴和放回。** 只要排序結果就直接依值倒出；要穩定或要搬移整個結構，就做前綴和後「由後往前」放回。\n\n**4. 邊界與複雜度。**\n\n- 時間 `O(n + k)`，空間 `O(n + k)`，其中 `k = mx - mn`。\n- 動手前先確認 `k` 不會過大（否則改基數／桶排序）。\n- 直接倒值的版本不穩定；需要穩定就用前綴和版。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「計數排序」想：\n\n- **待排序的鍵是「小範圍整數」**：分數 0..100、字母 a..z、年齡、像素值 0..255 等。\n- **題目明確給了小上界**，例如 `1 <= nums[i] <= 1000`，而且值域與 n 同級。\n- **要依「自訂順序」重排**（先按另一個陣列指定的順序），這正是計數排序「先建庫存、再依序倒出」的強項。\n\n:::example 嗅到味道的瞬間：constraints 寫著 0 <= arr[i] <= 1000（小…\n```cpp\n// 嗅到味道的瞬間：constraints 寫著 0 <= arr[i] <= 1000（小值域）\n// 又要排序 / 統計頻率 → 開一個大小 1001 的桶，O(n) 解決，不必 O(n log n)\n```\n:::\n\n\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 75 | [顏色分類](https://leetcode.cn/problems/sort-colors/) | N/A | 值域只有 {0,1,2}，計數排序一掃統計、再倒出即可（亦可三路指標原地）。 |\n| 1051 | [高度檢查器](https://leetcode.cn/problems/height-checker/) | 1303 | 計數排序得到「期望高度序列」，與原序列逐位比較，數出不相符的位置數。 |\n| 1122 | [陣列的相對排序](https://leetcode.cn/problems/relative-sort-array/) | 1189 | 用計數陣列記 arr1 各值次數，先按 arr2 指定順序倒出，再把其餘值由小到大補上。 |\n| 1366 | [通過投票對團隊排名](https://leetcode.cn/problems/rank-teams-by-votes/) | 1626 | 對每個團隊統計各名次的得票（計數），再用名次向量比較排序。 |\n| 2418 | [按身高排序](https://leetcode.cn/problems/sort-the-people/) | 1193 | 依身高排序並帶著名字一起搬移；身高互異時也可直接以身高為桶。 |\n\n**C++ 範例講解**\n\n以下用 1122. 陣列的相對排序 展示計數排序的核心寫法。\n\n**完整題目**：給 `arr1` 和 `arr2`，`arr2` 的元素互異且都出現在 `arr1` 中。將 `arr1` 重排，使其相對順序與 `arr2` 相同；不在 `arr2` 中的元素則按升冪排在後面。\n\n:::example 陣列的相對排序：計數排序 + 自訂輸出順序\n**線索**：`arr1[i]` 的範圍是 `0..1000`（小值域），又要按「`arr2` 指定的順序」重排——計數排序最自然：先數每個值有幾個，再按指定順序把庫存倒出來。\n\n```cpp\n// 1122. 陣列的相對排序：計數排序 + 自訂輸出順序\nclass Solution {\n public:\n  vector<int> relativeSortArray(vector<int>& arr1, vector<int>& arr2) {\n    vector<int> cnt(1001, 0);\n    for (int x : arr1) cnt[x]++;  // 統計 arr1 中每個值出現的次數\n\n    vector<int> res;\n    res.reserve(arr1.size());\n    for (int x : arr2) {  // 1) 先依 arr2 指定的順序倒出\n      while (cnt[x] > 0) {\n        res.push_back(x);\n        cnt[x]--;\n      }\n    }\n    for (int v = 0; v <= 1000; ++v) {  // 2) 不在 arr2 的值，依數值由小到大補上\n      while (cnt[v] > 0) {\n        res.push_back(v);\n        cnt[v]--;\n      }\n    }\n    return res;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `cnt[v]` 是值 `v` 的出現次數，下標即數值本身——這就是計數排序「不比較、用值定位」的精神。\n- 第一個迴圈把 `cnt` 當作「還剩幾個沒倒出」的庫存表，依 `arr2` 的順序倒貨；該值倒完庫存自然歸 0。\n- 第二個迴圈掃過整個值域，把剩下（不在 `arr2`）的值依序補上，因為是由小到大掃，輸出自然升冪。\n- 全程沒有任何兩兩比較，時間 `O(n + k)`，這裡 `k = 1000`。\n\n**常見變形**\n\n- 值域含負數 → 用 `x - mn` 平移成非負下標。\n- 要保持等值元素的原順序（穩定）→ 改用前綴和版本，由後往前放回。\n- 不只排數字，要搬移整個物件（帶名字、帶整列分數）→ 把 `cnt` 換成 `vector<vector<T>>` 桶，或記錄原索引。\n\n**實作注意事項**\n\n- 開桶大小要用「值域上界 + 1」，不要誤用 `n`。\n- 值域很大（例如 `1e9`）時計數排序不可行，改用基數或桶排序。\n- 直接倒值的計數排序不穩定；需要穩定就用前綴和版本。\n- 別忘了處理負數與 0。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 75 | [顏色分類](https://leetcode.cn/problems/sort-colors) |  |\n| 1051 | [高度檢查器](https://leetcode.cn/problems/height-checker) | 1303 |\n| 1122 | [陣列的相對排序](https://leetcode.cn/problems/relative-sort-array) | 1189 |\n| 1366 | [通過投票對團隊排名](https://leetcode.cn/problems/rank-teams-by-votes) | 1626 |\n| 2418 | [按身高排序](https://leetcode.cn/problems/sort-the-people) | 1193 |\n| 2545 | [根據第 K 場考試的分數排序](https://leetcode.cn/problems/sort-the-students-by-their-kth-score) | 1294 |\n| 2966 | [劃分陣列並滿足最大差限制](https://leetcode.cn/problems/divide-array-into-arrays-with-max-difference) | 1396 |\n| 3011 | [判斷一個陣列是否可以變為有序](https://leetcode.cn/problems/find-if-array-can-be-sorted) | 1497 |",
    },
    {
      id: 3,
      title: "2. 基數排序",
      summary:
        "**原理講解**\n\n計數排序在值域 `k` 很大時會爆炸。基數排序（Radix Sort）的想法是：把每個數字拆成「好幾位」（十進位的個位、十位、百位，或每 8 個 bit 一組），對**每一位**分別做一次「穩定計數排序」，從最低位排到最高位（LSD，least significant digit）。\n\n關鍵在於每一輪都必須**穩定**：當我們按高位排序時，低位已經排好的相對順序會被完整保留，於是逐位處理完，整體就有序了。\n\n:::example 基數排序的一輪：依第 exp 位（十進位）做穩定計數排序\n一輪（依某一位做穩定計數排序）長這樣：\n\n```cpp\n// 基數排序的一輪：依第 exp 位（十進位）做穩定計數排序\nvoid countingByDigit(vector<int>& a, int exp) {\n  int n = a.size();\n  vector<int> out(n), cnt(10, 0);\n  for (int x : a) cnt[(x / exp) % 10]++;  // 統計這一位上 0..9 的個數\n  for (int d = 1; d < 10; ++d) cnt[d] += cnt[d - 1];  // 前綴和\n  for (int i = n - 1; i >= 0; --i) {  // 由後往前 → 保持穩定\n    int d = (a[i] / exp) % 10;\n    out[--cnt[d]] = a[i];\n  }\n  a = out;\n}\n```\n:::\n\n\n\n為何選基數排序：對 n 個「最多 d 位、每位基數 b」的數，複雜度是 `O(d·(n + b))`。當數值上界固定（例如 32-bit 整數，位數 d 為常數），這就是線性 `O(n)`。\n\n**實作方式**\n\n:::example 以 base = 256、每輪 8 bits 處理 32-bit 非負整數，固定 4 輪\n**1. 決定基數（base）與位數。** 十進位用 `base = 10`；追求效率常用 `base = 256`（每 8 個 bit 一輪，32-bit 整數固定 4 輪）。\n\n```cpp\n// 以 base = 256、每輪 8 bits 處理 32-bit 非負整數，固定 4 輪\nconst int BITS = 8, BASE = 1 << BITS, MASK = BASE - 1;\nfor (int shift = 0; shift < 32; shift += BITS) {\n  // 對 (x >> shift) & MASK 做一輪穩定計數排序\n}\n```\n:::\n\n\n\n**2. 每輪做穩定計數排序**（上面的 `countingByDigit`）。\n\n**3. 由最低位到最高位重複**，直到處理完最高位。\n\n:::example 處理負數的簡單做法：整體加上 |min| 變成非負，排完再減回\n**4. 處理負數。** 基數排序預設只吃非負整數，常見做法是整體平移成非負，排完再平移回來。\n\n```cpp\n// 處理負數的簡單做法：整體加上 |min| 變成非負，排完再減回\nint mn = *min_element(a.begin(), a.end());\nfor (int& x : a) x -= mn;  // 平移成非負；排序完成後再對每個元素 += mn\n```\n:::\n\n\n\n複雜度：時間 `O(d·(n + b))`，空間 `O(n + b)`。\n\n**題目線索（Pattern）**\n\n- **要排的整數值域很大**（超過可開的桶大小），計數排序不可行，但仍想要近線性。\n- **需要依「數字的某幾位 / 某段字元」排序或比較**，例如裁剪後的數字、等長字串的字典序。\n- **對固定長度的字串／等長數字做字典序排序**（可視為 `base = 26` 或 `base = 10` 的 MSD/LSD radix）。\n\n:::example 線索：值到 1e9 卻要 O(n) 排序，或「依末 k 位數字排序」→ 想基數排序\n```cpp\n// 線索：值到 1e9 卻要 O(n) 排序，或「依末 k 位數字排序」→ 想基數排序\n```\n:::\n\n\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 164 | [最大間距](https://leetcode.cn/problems/maximum-gap/) | N/A | 用基數排序在 `O(n)` 排好後，線性掃描相鄰差取最大（亦可桶排序／鴿巢）。 |\n| 912 | [排序陣列](https://leetcode.cn/problems/sort-an-array/) | N/A | 練習場：可直接實作含負數處理的 LSD 基數排序通過。 |\n| 1985 | [找出陣列中的第 K 大整數](https://leetcode.cn/problems/find-the-kth-largest-integer-in-the-array/) | 1414 | 數字以字串給定且可能很長，先比長度再比字典序，等長部分即逐位（基數）比較。 |\n| 2191 | [將雜亂無章的數字排序](https://leetcode.cn/problems/sort-the-jumbled-numbers/) | 1496 | 依「數位映射後的值」排序並穩定保留原索引，本質是依鍵的計數／基數思路。 |\n| 2343 | [裁剪數字後查詢第 K 小的數字](https://leetcode.cn/problems/query-kth-smallest-trimmed-number/) | 1652 | 每次查詢取每個數字的末 trim 位，對這些等長字串逐位做穩定排序找第 k 小。 |\n\n**C++ 範例講解**\n\n以下用 164. 最大間距 展示基數排序的核心寫法。\n\n**完整題目**：給未排序陣列 `nums`，回傳排序後相鄰元素的最大差值；要求時間與空間皆為 `O(n)`。\n\n:::example 最大間距：LSD 基數排序（base = 10）後線性掃描相鄰差\n**線索**：要 `O(n)` 排序，比較排序的 `O(n log n)` 不達標；元素是非負整數，用 LSD 基數排序 `O(d·n) ≈ O(n)` 排好，再線性掃一遍相鄰差即可。\n\n```cpp\n// 164. 最大間距：LSD 基數排序（base = 10）後線性掃描相鄰差\nclass Solution {\n  void radixSort(vector<int>& a) {\n    int n = a.size();\n    int mx = *max_element(a.begin(), a.end());\n    vector<int> out(n);\n    for (long exp = 1; mx / exp > 0; exp *= 10) {  // 逐位：個位、十位、百位…\n      vector<int> cnt(10, 0);\n      for (int x : a) cnt[(x / exp) % 10]++;  // 統計這一位的數字分佈\n      for (int d = 1; d < 10; ++d) cnt[d] += cnt[d - 1];  // 前綴和定位\n      for (int i = n - 1; i >= 0; --i) {  // 由後往前 → 穩定\n        int d = (a[i] / exp) % 10;\n        out[--cnt[d]] = a[i];\n      }\n      a = out;  // 這一位排好，進入下一位\n    }\n  }\n\n public:\n  int maximumGap(vector<int>& nums) {\n    if (nums.size() < 2) return 0;\n    radixSort(nums);\n    int gap = 0;\n    for (size_t i = 1; i < nums.size(); ++i)\n      gap = max(gap, nums[i] - nums[i - 1]);  // 排序後相鄰差的最大值\n    return gap;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 外層 `for` 以 `exp = 1, 10, 100…` 推進，每輪只看十進位的某一位。\n- 內部就是一次穩定計數排序：統計 0..9、前綴和、由後往前放回 `out`，保證低位已排好的順序不被破壞。\n- `mx / exp > 0` 控制輪數，等於最大值的位數 `d`；最大值固定時 `d` 是常數，總時間 `O(d·n) ≈ O(n)`。\n- 排好後相鄰差一遍掃完即可，全程線性。\n\n**常見變形**\n\n- `base` 改用 256（位元分組），對 32-bit 整數固定 4 輪，常數更小。\n- 含負數：先平移成非負，或把負數與非負數分桶處理。\n- MSD 基數排序：從最高位開始遞迴分組，適合變長字串的字典序排序。\n- 對 pair／物件排序：把要比較的鍵組合成數字，或對多個鍵分多輪逐一排序。\n\n**實作注意事項**\n\n- `exp` 用 `long`，避免 `exp *= 10` 在大值時溢位。\n- 每一輪都必須穩定，否則前面排好的位會被打亂——務必由後往前放回。\n- 基數排序預設只處理非負整數，負數要額外處理。\n- 雖然漸進線性，但常數比 `std::sort` 大；資料量小或值域不利時未必更快。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 164 | [最大間距](https://leetcode.cn/problems/maximum-gap) |  |\n| 912 | [排序陣列](https://leetcode.cn/problems/sort-an-array) |  |\n| 1985 | [找出陣列中的第 K 大整數](https://leetcode.cn/problems/find-the-kth-largest-integer-in-the-array) | 1414 |\n| 2191 | [將雜亂無章的數字排序](https://leetcode.cn/problems/sort-the-jumbled-numbers) | 1496 |\n| 2343 | [裁剪數字後查詢第 K 小的數字](https://leetcode.cn/problems/query-kth-smallest-trimmed-number) | 1652 |",
    },
    {
      id: 4,
      title: "3. 桶排序",
      summary:
        "**原理講解**\n\n桶排序（Bucket Sort）把資料依「值的區間」分配到若干個桶，桶內各自排序（或只維護統計量），最後依桶的順序串起來。當資料在值域上分佈均勻時，平均可達 `O(n)`。\n\n它在面試裡有兩種常見面貌：\n\n1. **真的要排序**：把 `[mn, mx]` 切成 n 個等寬桶，每個元素丟進對應桶，桶內排序後串接。\n2. **更常考的「頻率桶」**：以「出現次數」當下標建桶，藉此 `O(n)` 找出 Top-K 高頻元素，完全不需要對頻率做 `O(n log n)` 排序。\n\n:::example 頻率桶：以「出現次數」為下標，O(n) 取得高頻元素\n頻率桶骨架：\n\n```cpp\n// 頻率桶：以「出現次數」為下標，O(n) 取得高頻元素\nvector<vector<int>> bucket(n + 1);  // bucket[f] = 出現 f 次的所有值\nfor (auto& [val, f] : freq) bucket[f].push_back(val);\nfor (int f = n; f >= 1; --f)  // 從高頻往低頻收集\n  for (int val : bucket[f]) {\n    // 依序取出 val，即是由高頻到低頻\n  }\n```\n:::\n\n\n\n何時值得用：值或頻率有自然的「桶下標」，或資料近似均勻分佈，就能避開比較排序的下界。\n\n**實作方式**\n\n**真排序版**四步：\n\n:::example 等寬分桶：把值線性映射到 [0, n - 1] 的桶下標\n**1. 決定桶數與映射。** 開 n 個桶，元素 x 落在 `idx = (x - mn) * (n - 1) / (mx - mn)`（以 `n-1` 為上界避免越界）。\n\n```cpp\n// 等寬分桶：把值線性映射到 [0, n - 1] 的桶下標\nint idx = (long long)(x - mn) * (n - 1) / (mx - mn);\nbuckets[idx].push_back(x);\n```\n:::\n\n\n\n**2. 入桶。** 把每個元素丟進對應桶。\n\n**3. 桶內排序。** 元素少時用插入排序或 `std::sort`。\n\n**4. 依桶序串接輸出。** 複雜度：均勻分佈平均 `O(n)`，最壞（全擠在一桶）退化成 `O(n log n)` 或 `O(n^2)`。\n\n**頻率桶版**更簡單：統計頻率 → 以頻率為下標入桶 → 從高頻（或低頻）方向掃出答案。\n\n**題目線索（Pattern）**\n\n- **求「出現頻率最高的前 K 個」或「依頻率排序」** → 頻率桶，`O(n)` 勝過排序。\n- **求排序後相鄰最大差距，且要求 `O(n)`** → 鴿巢桶（每桶只記 min/max）。\n- **資料宣告為實數且均勻分佈於某區間。**\n- **值域大但只關心是否落在同一小區間**（例如是否存在差 `<= t` 的一對）→ 以 `t+1` 為桶寬分桶。\n\n:::example 看到「Top K 頻率」「相鄰最大差 O(n)」「均勻分佈」→ 想桶排序\n```cpp\n// 看到「Top K 頻率」「相鄰最大差 O(n)」「均勻分佈」→ 想桶排序\n```\n:::\n\n\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 164 | [最大間距](https://leetcode.cn/problems/maximum-gap/) | N/A | 鴿巢原理：n-1 個桶只記每桶 min/max，最大差必跨桶（前桶 max 到後桶 min），`O(n)`。 |\n| 220 | [存在重複元素 III](https://leetcode.cn/problems/contains-duplicate-iii/) | N/A | 以 `t+1` 為桶寬分桶：同桶必符合，相鄰桶再檢查，配合滑動視窗達 `O(n)`。 |\n| 347 | [前 K 個高頻元素](https://leetcode.cn/problems/top-k-frequent-elements/) | N/A | 統計頻率後以頻率為下標建桶，從高頻往低頻收集前 K 個，`O(n)`。 |\n| 451 | [根據字元出現頻率排序](https://leetcode.cn/problems/sort-characters-by-frequency/) | N/A | 同樣以頻率為桶下標，由高到低把字元 × 頻率拼成字串。 |\n| 2070 | [每一個查詢的最大美麗值](https://leetcode.cn/problems/most-beautiful-item-for-each-query/) | 1724 | 將物品依價格排序並維護前綴最大美麗值，查詢時找 `<=` 上限者；分桶／排序皆可。 |\n\n**C++ 範例講解**\n\n以下用 347. 前 K 個高頻元素 展示頻率桶的核心寫法。\n\n**完整題目**：給整數陣列 `nums` 與整數 `k`，回傳出現頻率前 `k` 高的元素（任意順序）。要求優於 `O(n log n)`。\n\n:::example 前 K 個高頻元素：頻率桶，O(n)\n**線索**：任一元素的頻率最多就是 `n`，可以把「頻率」當桶下標：`bucket[f]` 放所有出現 `f` 次的數。從高頻桶往低頻掃，收滿 `k` 個就停——完全不必對頻率做比較排序，`O(n)`。\n\n```cpp\n// 347. 前 K 個高頻元素：頻率桶，O(n)\nclass Solution {\n public:\n  vector<int> topKFrequent(vector<int>& nums, int k) {\n    unordered_map<int, int> freq;\n    for (int x : nums) freq[x]++;  // 1) 統計每個值的出現次數\n\n    int n = nums.size();\n    vector<vector<int>> bucket(n + 1);  // bucket[f]：出現 f 次的所有值\n    for (auto& [val, f] : freq)\n      bucket[f].push_back(val);  // 2) 以頻率為下標入桶\n\n    vector<int> res;\n    for (int f = n; f >= 1 && (int)res.size() < k;\n         --f) {  // 3) 從高頻往低頻收集\n      for (int val : bucket[f]) {\n        res.push_back(val);\n        if ((int)res.size() == k) break;\n      }\n    }\n    return res;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- `freq` 統計每個值的出現次數，`O(n)`。\n- `bucket` 大小開 `n + 1`，因為任何頻率都落在 `1..n`；以頻率當下標，就把「排序」變成了「定位」。\n- 從 `f = n` 往下掃，最先收集到的就是最高頻，輸出天然由高到低，無需排序頻率。\n- 收滿 `k` 個立即停止，總時間 `O(n)`。\n\n**常見變形**\n\n- 要前 K 高頻「並依字典序 tie-break」（如 692）→ 桶內再排序，或改用堆。\n- 依頻率重建字串（如 451）→ 從高頻桶把字元重複輸出。\n- 等寬值桶求相鄰最大差（如 164）→ 每桶只存 min/max，答案在跨桶相減。\n- 滑動視窗中維護頻率桶，以近 `O(1)` 取極值。\n\n**實作注意事項**\n\n- 桶大小要用「最大可能頻率 + 1」= `n + 1`，否則越界。\n- 頻率桶天然不保證同頻元素的相對順序；題目要求 tie-break 時要在桶內再處理。\n- 真值桶排序在資料極不均勻時會退化，最壞 `O(n^2)`；必要時桶內用 `std::sort` 保 `O(n log n)`。\n- 映射桶下標時注意整數溢位，並特判 `mx == mn`（避免除以零）。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 164 | [最大間距](https://leetcode.cn/problems/maximum-gap) |  |\n| 220 | [存在重複元素 III](https://leetcode.cn/problems/contains-duplicate-iii) |  |\n| 347 | [前 K 個高頻元素](https://leetcode.cn/problems/top-k-frequent-elements) |  |\n| 451 | [根據字元出現頻率排序](https://leetcode.cn/problems/sort-characters-by-frequency) |  |\n| 692 | [前 K 個高頻單詞](https://leetcode.cn/problems/top-k-frequent-words) |  |\n| 2070 | [每一個查詢的最大美麗值](https://leetcode.cn/problems/most-beautiful-item-for-each-query) | 1724 |",
    },
    {
      id: 9004,
      title: "4. 常見面試模式",
      summary:
        "排序類題目在面試中很常見，除了直接呼叫內建排序外，更重要的是辨識何時該用線性排序、自訂比較或只取前 K 名而非完整排序。以下整理常見的面試模式與訊號。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 計數排序 | 值域小且為整數，要求線性時間 | 用桶記錄每個值出現次數後依序輸出，`O(n + k)` | [數組的相對排序](https://leetcode.cn/problems/relative-sort-array) |\n| 桶排序 | 求「相鄰元素最大間隔」或資料近似均勻分佈 | 依值域切桶，只比較相鄰桶的最小/最大值，`O(n)` | [最大間距](https://leetcode.cn/problems/maximum-gap) |\n| 基數排序 | 多關鍵字或大整數、固定位數 | 由低位到高位逐位做穩定計數排序，`O(d(n + k))` | [最大間距](https://leetcode.cn/problems/maximum-gap) |\n| 自訂比較 | 要按某種規則重排而非數值大小 | 自訂 comparator，注意比較需滿足嚴格弱序與傳遞性 | [最大數](https://leetcode.cn/problems/largest-number) |\n| 前 K 大（堆） | 串流資料或只要前 K 名 | 維護大小為 K 的堆，`O(n log k)` | [數組中的第K個最大元素](https://leetcode.cn/problems/kth-largest-element-in-an-array) |\n| 前 K 大（quickselect） | 一次性求第 K 大且可破壞順序 | 借用快排 partition，平均 `O(n)` | [前 K 個高頻元素](https://leetcode.cn/problems/top-k-frequent-elements) |\n\n**小提醒**\n\n- 自訂比較時，回傳值要明確表達「a 是否應排在 b 前」，避免寫出不滿足傳遞性的比較導致未定義行為。\n- quickselect 最壞為 `O(n^2)`，可隨機選 pivot 降低退化機率；若需穩定或多次查詢改用堆。\n- 計數/桶/基數排序前先確認值域與資料分佈，值域過大時這些方法反而更慢。",
    },
  ],
} as TutorialData.Root;
