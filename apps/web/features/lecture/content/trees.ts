import type { TutorialData } from "@/types";

export const trees = {
  id: 1,
  title: "鏈結串列、二元樹與回溯（前後指標/快慢指標/DFS/BFS/直徑/LCA/一般樹）",
  src: "https://leetcode.cn/circle/discuss/K0n2gO/",
  last_update: "2025-11-07T23:47:26.098808+00:00",
  summary:
    "本題單涵蓋連結串列、二叉樹、一般樹、回溯與分治五大主題，系統性地整理各類經典演算法與資料結構操作。\n\n**本題單涵蓋**：\n- 前後指標\n- 快慢指標\n- DFS\n- BFS\n- 直徑\n- LCA\n- 一般樹\n\n:::example 反轉連結串列（迭代）\n**C++ 模板（反轉連結串列）**：\n\n```cpp\n// 反轉連結串列（迭代）\nListNode* reverseList(ListNode* head) {\n  ListNode* prev = nullptr;\n  ListNode* curr = head;\n  while (curr) {\n    ListNode* next = curr->next;\n    curr->next = prev;  // 反轉指向\n    prev = curr;\n    curr = next;\n  }\n  return prev;\n}\n```\n:::\n",
  children: [
    {
      id: 2,
      title: "1. 連結串列",
      summary:
        "涵蓋連結串列的遍歷、刪除、插入、反轉、前後指標、快慢指標、雙指標、合併、分治及綜合應用，包含會員題與經典題型。\n\n:::example 遍歷連結串列\n**C++ 模板（遍歷與反轉）**：\n\n```cpp\n// 遍歷連結串列\nvoid traverse(ListNode* head) {\n  for (ListNode* cur = head; cur; cur = cur->next) {\n    // 處理 cur->val\n  }\n}\n\n// 反轉連結串列（迭代）\nListNode* reverseList(ListNode* head) {\n  ListNode* prev = nullptr;\n  ListNode* curr = head;\n  while (curr) {\n    ListNode* next = curr->next;\n    curr->next = prev;\n    prev = curr;\n    curr = next;\n  }\n  return prev;\n}\n```\n:::",
      children: [
        {
          id: 3,
          title: "1.1 遍歷連結串列",
          summary:
            "**原理講解**\n\n本節要解決的場景是：資料不像陣列那樣排成一整排存在記憶體裡，而是被切成一個個「節點（node）」，每個節點除了存自己的值，還存一個指向「下一個節點」的箭頭（指標）。這種結構叫做「連結串列（linked list）」。\n\n:::example 連結串列節點：值 val + 指向下一個節點的指標 next\n先看清楚一個節點長什麼樣，以及它們怎麼串起來：\n\n```cpp\n// 連結串列節點：值 val + 指向下一個節點的指標 next\nstruct ListNode {\n  int val;\n  ListNode* next;  // 指向下一個節點；最後一個節點的 next 是 nullptr（空）\n};\n```\n:::\n\n\n\n:::example 遍歷：從頭走到尾，把每個節點都看過一遍\n因為節點之間只靠 `next` 箭頭連著，你沒辦法像陣列那樣用 `a[i]` 直接跳到第 5 個。想看到所有資料，唯一的辦法就是**從頭節點出發，沿著 `next` 一個一個往下走，直到走到 `nullptr`（串列結尾）為止**。這個「沿著箭頭走完整條串列」的動作就是「遍歷（traverse）」。\n\n```cpp\n// 遍歷：從頭走到尾，把每個節點都看過一遍\nfor (ListNode* cur = head; cur != nullptr; cur = cur->next) {\n  // 這裡可以讀 cur->val，或一邊走一邊累積某個摘要\n}\n```\n:::\n\n\n\n為什麼這樣就夠用？因為很多題目要的答案，其實只要「走一趟、邊走邊記東西」就能算出來：例如數有幾個節點、把每個位元拼成一個整數、或記住上一個節點的值來和現在比較。關鍵心法是：**選一個你從頭掃到尾就能維護的「摘要」**（一個計數器、一個累積值、上一個節點等），讓答案在走到結尾時自然成形，而不必來回重掃。整趟遍歷只走一遍，時間是 `O(n)`。\n\n**實作方式**\n\n把遍歷類題目拆成三件固定的事，逐一搭配程式碼。\n\n:::example 狀態表示：以「把二進位拼成整數」為例（LeetCode 1290）\n**1. 狀態表示：想清楚「邊走邊要記什麼」。** 這就是你的摘要，常見的有計數器、累積值、或「上一個節點」。\n\n```cpp\n// 狀態表示：以「把二進位拼成整數」為例（LeetCode 1290）\nint result = 0;  // 邊走邊把目前累積的數字記在這裡\n```\n:::\n\n\n\n:::example 更新：每走到一個節點，就把它的位元接到結果的最低位（1290）\n**2. 更新流程：固定一個游標 `cur`，每步只更新摘要再前進。** 游標每次往 `cur->next` 移動一格，移動前先用目前節點更新摘要。\n\n```cpp\n// 更新：每走到一個節點，就把它的位元接到結果的最低位（1290）\nfor (ListNode* cur = head; cur; cur = cur->next) {\n  result = result * 2 + cur->val;  // 左移一位再加上目前位元\n}\n```\n:::\n\n\n\n:::example 答案讀取：迴圈結束時，摘要就是要回傳的東西\n**3. 答案讀取：走到 `nullptr` 後，摘要本身就是答案。** 例如 LeetCode 2058「臨界點」是邊走邊比較「前一個、現在、後一個」三者，記下臨界點位置；2181「合併零之間的節點」則是邊走邊把零之間的值加起來。\n\n```cpp\n// 答案讀取：迴圈結束時，摘要就是要回傳的東西\nreturn result;  // 整條串列已遍歷完畢，result 即為答案\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 空串列（`head == nullptr`）要先處理，否則一開始就解參考 `nullptr` 會當掉。\n- 需要比較相鄰節點時（如 2058），要小心 `cur->next` 是否存在，避免讀到空指標。\n- 複雜度：只走一趟，時間 `O(n)`、額外空間 `O(1)`（n 為節點數）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「遍歷連結串列」想：\n\n- **輸入是一條連結串列，而你要的答案只要「看過每個節點一次」就能算出。** 例如數節點、求總和、把節點值拼成一個數（1290）、找出符合某條件的節點位置（2058）。\n- **答案可以表示成一個邊走邊維護的摘要**，不需要回頭、也不需要隨機跳到第 k 個節點。\n- **你發現自己想寫 `list[i]` 卻發現連結串列沒有這種隨機存取**——這正提醒你只能老老實實沿著 `next` 走一遍。\n\n:::example 嗅到味道的瞬間：你只需要一個游標，從頭順順走到尾\n```cpp\n// 嗅到味道的瞬間：你只需要一個游標，從頭順順走到尾\n// for (ListNode* cur = head; cur; cur = cur->next) { ... }\n// 沒有回頭、沒有跳格，就是純遍歷\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我邊走邊要維護的「摘要」是什麼？（計數器？累積值？上一個節點？）\n2. 我在走到每個節點時，要怎麼用它更新摘要？\n3. 走到 `nullptr` 時，摘要是不是就等於我要的答案？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 725 | 分隔連結串列 | N/A | 放在「遍歷連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 817 | 連結串列元件 | N/A | 放在「遍歷連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1290 | [二進位制連結串列轉整數](https://leetcode.cn/problems/convert-binary-number-in-a-linked-list-to-integer/) | 1151 | 放在「遍歷連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2058 | [找出臨界點之間的最小和最大距離](https://leetcode.cn/problems/find-the-minimum-and-maximum-number-of-nodes-between-critical-points/) | 1311 | 放在「遍歷連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2181 | [合併零之間的節點](https://leetcode.cn/problems/merge-nodes-in-between-zeros/) | 1333 | 放在「遍歷連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 725 | [分隔連結串列](https://leetcode.cn/problems//split-linked-list-in-parts/) |  |\n| 817 | [連結串列元件](https://leetcode.cn/problems//linked-list-components/) |  |\n| 1290 | [二進位制連結串列轉整數](https://leetcode.cn/problems//convert-binary-number-in-a-linked-list-to-integer/) | 1151 |\n| 2058 | [找出臨界點之間的最小和最大距離](https://leetcode.cn/problems//find-the-minimum-and-maximum-number-of-nodes-between-critical-points/) | 1311 |\n| 2181 | [合併零之間的節點](https://leetcode.cn/problems//merge-nodes-in-between-zeros/) | 1333 |\n| 3062 | [連結串列遊戲的獲勝者](https://leetcode.cn/problems//winner-of-the-linked-list-game/) |  |\n| 3063 | [連結串列頻率](https://leetcode.cn/problems//linked-list-frequency/) |  |\n| 3263 | [將雙鏈錶轉換為陣列 I](https://leetcode.cn/problems//convert-doubly-linked-list-to-array-i/) |  |\n| 3294 | [將雙鏈錶轉換為陣列 II](https://leetcode.cn/problems//convert-doubly-linked-list-to-array-ii/) |  |",
        },
        {
          id: 4,
          title: "1.2 刪除節點",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在一條連結串列裡「刪掉某個節點」。和陣列不同，連結串列刪節點不用搬動一大堆元素，只要**把箭頭改個方向**——讓被刪節點的前一個節點，直接指向被刪節點的後一個節點，那個被「跳過」的節點就等於從串列裡消失了。\n\n:::example 刪除核心：讓 prev 跳過 cur，直接指向 cur 的下一個\n先看刪除的核心動作：\n\n```cpp\n// 刪除核心：讓 prev 跳過 cur，直接指向 cur 的下一個\nprev->next = cur->next;  // cur 被跳過，從串列裡消失\n                         // delete cur;            // 若需要釋放記憶體可在此進行\n```\n:::\n\n\n\n這個技巧的精髓只有一句話：**改箭頭之前，一定要先抓住「下一個節點」，否則一改就把後面整條鏈弄丟了。** 因為連結串列只能單向往前，一旦 `prev->next` 被改掉、又沒人記得原本的下一個是誰，後面的節點就再也找不到了。\n\n:::example 虛擬頭節點：讓頭節點也有「前一個」，統一處理\n第二個關鍵是**虛擬頭節點（dummy head）**。如果要刪的剛好是第一個節點，那它沒有「前一個」可以改箭頭，會變成特例。解法是在真正的頭前面接一個假節點 `dummy`，讓每個節點（包含原本的頭）都保證有「前一個」，程式就不必為頭節點寫特殊分支。\n\n```cpp\n// 虛擬頭節點：讓頭節點也有「前一個」，統一處理\nListNode dummy;\ndummy.next = head;  // dummy 之後才是真正的串列\n                    // 最後回傳 dummy.next（可能新的頭已經不是原本的 head 了）\n```\n:::\n\n\n\n像 LeetCode 203「移除連結串列元素」就是邊走邊把值等於目標的節點用上面的方式跳過；83 / 82「刪除排序串列中的重複元素」則是利用「已排序、重複的會相鄰」，比較相鄰節點來決定是否跳過。整趟只走一遍，時間 `O(n)`。\n\n**實作方式**\n\n把刪除類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：dummy + prev/cur 兩個游標（以 203 為例）\n**1. 狀態表示：準備 dummy 與兩個游標 `prev`、`cur`。** `prev` 永遠停在「上一個保留下來的節點」，`cur` 是正在檢查的節點。\n\n```cpp\n// 狀態表示：dummy + prev/cur 兩個游標（以 203 為例）\nListNode dummy;\ndummy.next = head;\nListNode* prev = &dummy;  // 已保留的最後一個節點\nListNode* cur = head;     // 正在檢查的節點\n```\n:::\n\n\n\n:::example 更新：值等於 val 就跳過（刪除），否則 prev 前進\n**2. 更新流程：要刪就改 prev 的箭頭，要留就讓 prev 跟上。** 這是刪除的主迴圈，注意改箭頭前已先用 `cur->next` 決定下一步往哪走。\n\n```cpp\n// 更新：值等於 val 就跳過（刪除），否則 prev 前進\nwhile (cur) {\n  if (cur->val == val) {\n    prev->next = cur->next;  // 刪：跳過 cur\n  } else {\n    prev = cur;  // 留：prev 跟上\n  }\n  cur = cur->next;  // cur 永遠前進\n}\n```\n:::\n\n\n\n:::example 答案讀取：dummy.next 才是更新後的新頭\n**3. 答案讀取：回傳 `dummy.next`。** 因為頭節點也可能被刪掉，所以不要回傳原本的 `head`，要回傳 dummy 之後的節點——那才是更新後真正的頭。\n\n```cpp\n// 答案讀取：dummy.next 才是更新後的新頭\nreturn dummy.next;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 連續多個都要刪時（如 82 要刪掉「所有出現過重複的值」），`prev` 在確定刪除前不能往前移，否則會漏。\n- 特例 237「刪除連結串列中的節點」拿不到 `prev`：作法是把「下一個節點的值複製過來、再刪掉下一個」，等效於刪掉自己。\n- 複雜度：單趟掃描，時間 `O(n)`、額外空間 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「刪除節點」想：\n\n- **題目要你從連結串列中拿掉符合某條件的節點**：等於某值（203）、重複（83、82）、或指定位置。\n- **你發現「改箭頭」就能達成，不需要搬資料。** 這正是連結串列刪除的特徵——`O(1)` 改一條邊即可。\n- **頭節點可能也要被刪，讓你開始想寫特例**——這就是該拿出 dummy head 的訊號：\n\n:::example 嗅到味道的瞬間：頭可能被刪，於是先架一個 dummy\n```cpp\n// 嗅到味道的瞬間：頭可能被刪，於是先架一個 dummy\n// ListNode dummy; dummy.next = head;  prev = &dummy;\n// 之後所有節點都有「前一個」，不必為頭寫特例\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 改箭頭之前，我有沒有先抓住「下一個節點」、避免弄丟後面的鏈？\n2. 我需不需要 dummy head 來統一處理「頭也被刪」的情況？\n3. 該刪的時候 `prev` 是不是「不前進」，該留的時候才前進？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 82 | 刪除排序連結串列中的重複元素 II | N/A | 放在「刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 83 | 刪除排序連結串列中的重複元素 | N/A | 放在「刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 203 | 移除連結串列元素 | N/A | 放在「刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 237 | 刪除連結串列中的節點 | N/A | 放在「刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1669 | [合併兩個連結串列](https://leetcode.cn/problems/merge-in-between-linked-lists/) | 1428 | 放在「刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 82 | [刪除排序連結串列中的重複元素 II](https://leetcode.cn/problems//remove-duplicates-from-sorted-list-ii/) |  |\n| 83 | [刪除排序連結串列中的重複元素](https://leetcode.cn/problems//remove-duplicates-from-sorted-list/) |  |\n| 203 | [移除連結串列元素](https://leetcode.cn/problems//remove-linked-list-elements/) |  |\n| 237 | [刪除連結串列中的節點](https://leetcode.cn/problems//delete-node-in-a-linked-list/) |  |\n| 1669 | [合併兩個連結串列](https://leetcode.cn/problems//merge-in-between-linked-lists/) | 1428 |\n| 1836 | [從未排序的連結串列中移除重複元素](https://leetcode.cn/problems//remove-duplicates-from-an-unsorted-linked-list/) |  |\n| 2487 | [從連結串列中移除節點](https://leetcode.cn/problems//remove-nodes-from-linked-list/) | 1455 |\n| 3217 | [從連結串列中移除在陣列中存在的節點](https://leetcode.cn/problems//delete-nodes-from-linked-list-present-in-array/) | 1342 |",
        },
        {
          id: 5,
          title: "1.3 插入節點",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在一條連結串列裡「插入一個新節點」。和刪除一樣，插入也只是**改箭頭**——找到要插入的位置，讓前一個節點指向新節點、新節點再指向原本的後一個節點。整個動作只動兩條邊，不必搬移任何資料。\n\n:::example 插入核心：把 node 接在 prev 和 prev->next 之間\n先看插入的核心動作。注意箭頭的設定**順序很重要**：\n\n```cpp\n// 插入核心：把 node 接在 prev 和 prev->next 之間\nnode->next = prev->next;  // 1) 先讓新節點指向「後面那段」\nprev->next = node;        // 2) 再讓 prev 指向新節點\n```\n:::\n\n\n\n順序不能反。如果先做 `prev->next = node`，那原本 `prev->next` 指的「後面那段」就被覆蓋、再也找不到了。所以心法是：**新節點要先接住後面，前一個節點才放手交棒給新節點。** 這跟刪除「改箭頭前先抓住下一個」是同一個道理——永遠別把還用得到的鏈弄丟。\n\n:::example 虛擬頭節點：讓「插在最前面」也能用同一套邏輯\n同樣地，如果新節點要插在最前面（變成新的頭），它就沒有「前一個」可以改箭頭，會變成特例。用**虛擬頭節點（dummy head）**可以讓頭也有「前一個」，免去特殊分支：\n\n```cpp\n// 虛擬頭節點：讓「插在最前面」也能用同一套邏輯\nListNode dummy;\ndummy.next = head;\n// 找到插入點的前一個節點 prev（可能就是 &dummy），再接上新節點\n// 最後回傳 dummy.next\n```\n:::\n\n\n\n像 LeetCode 147「對連結串列進行插入排序」就是每次取一個節點，從頭找到它該插入的位置再接進去；2807「在連結串列中插入最大公約數」則是邊走邊在每對相鄰節點之間，插入一個值為兩者最大公約數的新節點。\n\n**實作方式**\n\n把插入類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：dummy + prev 游標\n**1. 狀態表示：準備 dummy 與游標 `prev`。** `prev` 用來停在「新節點要插在它後面」的那個節點。\n\n```cpp\n// 狀態表示：dummy + prev 游標\nListNode dummy;\ndummy.next = head;\nListNode* prev = &dummy;  // 新節點將被插在 prev 之後\n```\n:::\n\n\n\n:::example 找插入點：prev 停在「下一個就比 val 大」的地方\n**2. 找插入點：沿著串列走到正確位置。** 以插入排序（147）為例，要往後走到第一個「比待插入值大」的節點之前停下。\n\n```cpp\n// 找插入點：prev 停在「下一個就比 val 大」的地方\nwhile (prev->next && prev->next->val < val) {\n  prev = prev->next;\n}\n```\n:::\n\n\n\n:::example 接上：node 先指向後段，prev 再指向 node\n**3. 接上新節點：先接後面，再交棒。** 這是插入的關鍵兩步，順序不可顛倒。\n\n```cpp\n// 接上：node 先指向後段，prev 再指向 node\nnode->next = prev->next;\nprev->next = node;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 在「每對相鄰節點之間」插入時（如 2807），插完要記得讓游標**跳過剛插入的新節點**，否則可能無限插入或位置錯亂。\n- 迴圈型串列的插入（708）要小心找不到邊界，需處理「整圈值都相同」等特例。\n- 複雜度：每次找位置最壞 `O(n)`，插入本身 `O(1)`；插入排序整體最壞 `O(n^2)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「插入節點」想：\n\n- **題目要你把新節點放進串列的某個位置**：依大小排序插入（147）、在每對相鄰節點間插入（2807）、或維持迴圈串列的有序性（708）。\n- **你發現只要改兩條箭頭就能完成**，不像陣列插入要搬動後面一整排。\n- **新節點可能要插在最前面**，讓你想寫特例——這就是該用 dummy head 的訊號：\n\n:::example 嗅到味道的瞬間：插在最前面沒有「前一個」，於是架 dummy\n```cpp\n// 嗅到味道的瞬間：插在最前面沒有「前一個」，於是架 dummy\n// ListNode dummy; dummy.next = head; prev = &dummy;\n// 之後不論插哪裡都有 prev，邏輯統一\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我接箭頭的順序對嗎？（先讓新節點接住後面，前一個才交棒）\n2. 我的 `prev` 怎麼停到「正確的插入位置之前」？\n3. 插完之後游標要不要跳過新節點，避免重複處理？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 147 | 對連結串列進行插入排序 | N/A | 放在「插入節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 708 | 迴圈有序列表的插入（Premium） | N/A | 放在「插入節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2046 | 給按照絕對值排序的連結串列排序（Premium） | N/A | 放在「插入節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2807 | [在連結串列中插入最大公約數](https://leetcode.cn/problems/insert-greatest-common-divisors-in-linked-list/) | 1279 | 放在「插入節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 4046 | LCR 029. 迴圈有序列表的插入 | N/A | 放在「插入節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 147 | [對連結串列進行插入排序](https://leetcode.cn/problems//insertion-sort-list/) |  |\n| 708 | [迴圈有序列表的插入](https://leetcode.cn/problems//insert-into-a-sorted-circular-linked-list/) |  |\n| 2046 | [給按照絕對值排序的連結串列排序](https://leetcode.cn/problems//sort-linked-list-already-sorted-using-absolute-values/) |  |\n| 2807 | [在連結串列中插入最大公約數](https://leetcode.cn/problems//insert-greatest-common-divisors-in-linked-list/) | 1279 |\n| 4046 | [LCR 029. 迴圈有序列表的插入](https://leetcode.cn/problems//4ueAj6/) |  |",
        },
        {
          id: 6,
          title: "1.4 反轉連結串列",
          summary:
            "**原理講解**\n\n本節要解決的場景是：把一條連結串列「反轉」——原本 `1 -> 2 -> 3` 變成 `3 -> 2 -> 1`。重點是，我們不搬移節點本身，而是**把每個節點的 `next` 箭頭反過來指**：原本指向後一個，改成指向前一個。\n\n:::example 反轉一步：先存下一個，再把 cur 的箭頭掉頭，最後兩個游標一起前進\n反轉的核心是同時握住三個游標：`prev`（已反轉好的那段的頭）、`cur`（正在處理的節點）、`nxt`（先存起來的下一個節點）。每一步做四件事：\n\n```cpp\n// 反轉一步：先存下一個，再把 cur 的箭頭掉頭，最後兩個游標一起前進\nListNode* nxt = cur->next;  // 1) 先記住下一個，否則改箭頭後就丟了\ncur->next = prev;           // 2) 把 cur 的箭頭掉頭，指向前面\nprev = cur;                 // 3) prev 前進到 cur\ncur = nxt;                  // 4) cur 前進到原本的下一個\n```\n:::\n\n\n\n第 1 步是整個技巧的命門：**改箭頭之前一定要先把「下一個」存起來。** 因為第 2 步 `cur->next = prev` 會覆蓋掉原本的下一個，如果沒先存，後面整條鏈就斷了、找不回來。\n\n:::example 完整反轉：cur 掃完整條串列後，prev 就是新的頭（LeetCode 206）\n把這四步放進迴圈，從頭走到尾，`prev` 就會一路收集反轉好的節點，最後它指向的就是新的頭：\n\n```cpp\n// 完整反轉：cur 掃完整條串列後，prev 就是新的頭（LeetCode 206）\nListNode *prev = nullptr, *cur = head;\nwhile (cur) {\n  ListNode* nxt = cur->next;\n  cur->next = prev;\n  prev = cur;\n  cur = nxt;\n}\nreturn prev;  // 新的頭\n```\n:::\n\n\n\n像 LeetCode 206「反轉連結串列」就是上面這段；92「反轉連結串列 II」是只反轉中間某一段；25「K 個一組翻轉」是每湊滿 k 個就反轉一次；24「兩兩交換」是 k=2 的特例。整趟只走一遍，時間 `O(n)`、額外空間 `O(1)`。\n\n**實作方式**\n\n把反轉類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：prev 從空開始，cur 從頭開始\n**1. 狀態表示：三個游標 `prev`、`cur`、`nxt`。** `prev` 一開始是 `nullptr`（反轉後它會變成新尾巴的 next，剛好是空）。\n\n```cpp\n// 狀態表示：prev 從空開始，cur 從頭開始\nListNode* prev = nullptr;\nListNode* cur = head;\nListNode* nxt = nullptr;  // 每步暫存「下一個」\n```\n:::\n\n\n\n:::example 更新：存下一個 -> 掉頭 -> 兩游標前進\n**2. 更新流程：四步一循環，掉頭再前進。** 這就是反轉的主迴圈，務必照順序。\n\n```cpp\n// 更新：存下一個 -> 掉頭 -> 兩游標前進\nwhile (cur) {\n  nxt = cur->next;   // 存\n  cur->next = prev;  // 掉頭\n  prev = cur;        // prev 前進\n  cur = nxt;         // cur 前進\n}\n```\n:::\n\n\n\n:::example 局部反轉的接線：以反轉 [a..b] 為例\n**3. 局部反轉要記住「邊界節點」。** 若只反轉中間一段（92、25），反轉前要先記住「這段的前一個節點」和「這段的第一個節點」——反轉後，前者要接到新段頭，後者會變成新段尾，要接回剩下的鏈。\n\n```cpp\n// 局部反轉的接線：以反轉 [a..b] 為例\n// before -> a -> ... -> b -> after  反轉成  before -> b -> ... -> a -> after\nbefore->next = newHead;  // newHead 是反轉後的段頭（原本的 b）\na->next = after;         // 原本的段頭 a 變成段尾，接回 after\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 空串列或只有一個節點時，直接回傳 `head` 即可，反轉迴圈也能自然處理。\n- 分組反轉（25）若最後不滿 k 個，依題意通常「保持原樣不反轉」，要先數夠 k 個再動手。\n- 複雜度：每個節點被處理一次，時間 `O(n)`、額外空間 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「反轉連結串列」想：\n\n- **題目要你把整條或某一段串列的順序倒過來**（206、92），或**成組地翻轉**（24 兩兩交換、25 每 k 個一組）。\n- **你發現只要把 `next` 箭頭逐一掉頭就能完成**，不需要額外開陣列存值再重建。\n- **看到「掉頭」就想到要先存下一個**——這是反轉最容易踩的雷：\n\n:::example 嗅到味道的瞬間：你正要寫 cur->next = prev\n```cpp\n// 嗅到味道的瞬間：你正要寫 cur->next = prev\n// 但這會蓋掉原本的下一個，所以前一行一定先 nxt = cur->next;\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有在掉頭前先把 `nxt = cur->next` 存起來？\n2. 反轉結束後，新的頭是不是 `prev`？\n3. 若是局部反轉，我有沒有記住段的「前一個」和「原段頭」來接回剩下的鏈？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 24 | 兩兩交換連結串列中的節點 | N/A | 放在「反轉連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 25 | K 個一組翻轉連結串列 | N/A | 放在「反轉連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 92 | 反轉連結串列 II | N/A | 放在「反轉連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 206 | 反轉連結串列 | N/A | 放在「反轉連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2074 | [反轉偶數長度組的節點](https://leetcode.cn/problems/reverse-nodes-in-even-length-groups/) | 1685 | 放在「反轉連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 24 | [兩兩交換連結串列中的節點](https://leetcode.cn/problems//swap-nodes-in-pairs/) |  |\n| 25 | [K 個一組翻轉連結串列](https://leetcode.cn/problems//reverse-nodes-in-k-group/) |  |\n| 92 | [反轉連結串列 II](https://leetcode.cn/problems//reverse-linked-list-ii/) |  |\n| 206 | [反轉連結串列](https://leetcode.cn/problems//reverse-linked-list/) |  |\n| 2074 | [反轉偶數長度組的節點](https://leetcode.cn/problems//reverse-nodes-in-even-length-groups/) | 1685 |",
        },
        {
          id: 7,
          title: "1.5 前後指標",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在連結串列上做「倒數第 N 個」這類事情，例如刪除倒數第 N 個節點。難點在於連結串列只能從頭往前走、不能像陣列那樣回頭，也不能直接知道總長度。「前後指標（也叫間隔雙指標）」就是專門解這個的技巧。\n\n:::example 第一步：讓 fast 先走 N 步，拉開固定間隔 N\n核心想法只有一句話：**讓兩個游標保持固定的間隔一起往前走。** 先讓「前指標 `fast`」單獨往前走 N 步，於是 `fast` 和「後指標 `slow`」之間相差恰好 N 個節點。接著兩個一起走，當 `fast` 走到結尾時，`slow` 自然就停在「倒數第 N 個」的位置——因為它們的間隔一直是 N。\n\n```cpp\n// 第一步：讓 fast 先走 N 步，拉開固定間隔 N\nListNode* fast = head;\nfor (int i = 0; i < n; ++i) {\n  fast = fast->next;\n}\n// 現在 fast 領先 slow（在 head）剛好 N 個節點\n```\n:::\n\n\n\n:::example 第二步：兩指標同速前進，fast 到尾時 slow 正好在倒數第 N 個\n```cpp\n// 第二步：兩指標同速前進，fast 到尾時 slow 正好在倒數第 N 個\nListNode* slow = head;\nwhile (fast) {\n  fast = fast->next;\n  slow = slow->next;\n}\n// slow 此刻就是「倒數第 N 個」節點\n```\n:::\n\n\n\n為什麼這樣就對？因為兩指標永遠相差 N 步，這個間隔在整個過程中不變（這就是「不變式」）。`fast` 走到 `nullptr`（總共走了「長度」步），`slow` 因為落後 N 步，所以停在「長度 − N」的位置，正是倒數第 N 個。整個過程**只走一遍串列**就找到位置，不必先數一次長度再走第二次，時間 `O(n)`、空間 `O(1)`。\n\n如果要刪除倒數第 N 個，技巧是讓 `slow` 停在它的**前一個**節點（一樣靠 dummy head 統一處理頭被刪的情況），這樣才好改箭頭。\n\n**實作方式**\n\n把前後指標類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：dummy 統一邊界，fast/slow 都從 dummy 出發（LeetCo…\n**1. 狀態表示：dummy + 兩個游標 `fast`、`slow`。** 用 dummy 讓「刪到頭節點」也不必寫特例。\n\n```cpp\n// 狀態表示：dummy 統一邊界，fast/slow 都從 dummy 出發（LeetCode 19）\nListNode dummy;\ndummy.next = head;\nListNode* fast = &dummy;\nListNode* slow = &dummy;\n```\n:::\n\n\n\n:::example 拉開間隔：fast 先行 n 步\n**2. 拉開間隔：fast 先走 N 步。** 走完後 `fast` 與 `slow` 相差 N。\n\n```cpp\n// 拉開間隔：fast 先行 n 步\nfor (int i = 0; i < n; ++i) {\n  fast = fast->next;\n}\n```\n:::\n\n\n\n:::example 同速前進：fast 到尾時，slow 停在待刪節點的前一個\n**3. 同速前進：直到 fast 走到最後一個節點。** 因為要刪除，所以條件用 `fast->next`，讓 `slow` 停在「待刪節點的前一個」。\n\n```cpp\n// 同速前進：fast 到尾時，slow 停在待刪節點的前一個\nwhile (fast->next) {\n  fast = fast->next;\n  slow = slow->next;\n}\nslow->next = slow->next->next;  // 跳過倒數第 N 個，完成刪除\n```\n:::\n\n\n\n**4. 答案讀取與複雜度。** 回傳 `dummy.next`（頭可能已被刪）。\n\n- 旋轉串列（61）也用間隔思想：先數長度、取 `k % len`，找到斷點再接成新頭。\n- N 可能等於整條長度（要刪的就是頭），dummy 讓這種情況自然成立。\n- 複雜度：單趟掃描，時間 `O(n)`、額外空間 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「前後指標」想：\n\n- **題目出現「倒數第 N 個」「距離結尾 K」「相隔固定間隔」這類描述**（19 刪除倒數第 N 個、1721 交換串列中第 k 個與倒數第 k 個）。\n- **你只想掃一遍串列就定位，不想先數長度再走第二次。** 固定間隔雙指標正是把「兩趟」壓成「一趟」。\n- **你需要在某個位置「停在它的前一個」來改箭頭**，這時讓 `slow` 落後一格即可：\n\n:::example 嗅到味道的瞬間：要找「倒數第 N 個」卻不想數兩遍\n```cpp\n// 嗅到味道的瞬間：要找「倒數第 N 個」卻不想數兩遍\n// 讓 fast 先走 N 步，再一起走到底，slow 就停在那裡\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我讓前指標先走幾步？這個間隔對應題目要的「倒數第幾個」嗎？\n2. 結束條件用 `fast` 還是 `fast->next`？（決定 slow 停在目標還是目標前一個）\n3. 我需不需要 dummy head 來處理「目標就是頭節點」的情況？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 19 | 刪除連結串列的倒數第 N 個結點 | N/A | 放在「前後指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 61 | 旋轉連結串列 | N/A | 放在「前後指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1474 | 刪除連結串列 M 個節點之後的 N 個節點（Premium） | N/A | 放在「前後指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1721 | [交換連結串列中的節點](https://leetcode.cn/problems/swapping-nodes-in-a-linked-list/) | 1387 | 放在「前後指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 19 | [刪除連結串列的倒數第 N 個結點](https://leetcode.cn/problems//remove-nth-node-from-end-of-list/) |  |\n| 61 | [旋轉連結串列](https://leetcode.cn/problems//rotate-list/) |  |\n| 1474 | [刪除連結串列 M 個節點之後的 N 個節點](https://leetcode.cn/problems//delete-n-nodes-after-m-nodes-of-a-linked-list/) |  |\n| 1721 | [交換連結串列中的節點](https://leetcode.cn/problems//swapping-nodes-in-a-linked-list/) | 1387 |",
        },
        {
          id: 8,
          title: "1.6 快慢指標",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在連結串列上判斷「有沒有環」、找「正中間的節點」、或偵測「重複」這類問題。共同工具叫「快慢指標（也叫 Floyd 龜兔算法）」——讓兩個游標**用不同的速度**在串列上跑，一步一個（慢，slow）和一步兩個（快，fast）。\n\n:::example 找中點：fast 走兩步、slow 走一步，fast 到尾時 slow 在中間\n先看最常見的兩個用法。其一是**找中點**：快指標一次走兩格、慢指標一次走一格，快指標到尾時，慢指標剛好在正中間（因為它只走了一半的步數）。\n\n```cpp\n// 找中點：fast 走兩步、slow 走一步，fast 到尾時 slow 在中間\nListNode *slow = head, *fast = head;\nwhile (fast && fast->next) {\n  slow = slow->next;        // 慢：一次一格\n  fast = fast->next->next;  // 快：一次兩格\n}\n// slow 現在是中間節點\n```\n:::\n\n\n\n:::example 判環：若 fast 追上 slow（相遇），就有環（LeetCode 141）\n其二是**判斷有沒有環**。如果串列是一條直線，快指標遲早走到 `nullptr`；但如果有環，快指標會在環裡繞圈，最終一定會「從後面追上」慢指標，兩者相遇。這就像操場上跑得快的人遲早會套圈追上跑得慢的人。\n\n```cpp\n// 判環：若 fast 追上 slow（相遇），就有環（LeetCode 141）\nwhile (fast && fast->next) {\n  slow = slow->next;\n  fast = fast->next->next;\n  if (slow == fast) {\n    return true;  // 相遇 = 有環\n  }\n}\nreturn false;  // fast 走到底 = 無環\n```\n:::\n\n\n\n為什麼有環就一定會相遇？因為進入環之後，快指標每步比慢指標多走一格，兩者的「距離」每步縮短 1，環長有限，所以距離一定會減到 0（相遇）。這個「不同速度造成距離單調變化」的不變式，就是快慢指標能成立的原因。整個過程只用兩個游標、走線性步數，時間 `O(n)`、空間 `O(1)`。\n\n**實作方式**\n\n把快慢指標類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：slow 慢、fast 快，都從頭開始\n**1. 狀態表示：兩個游標 `slow`、`fast`，通常都從 `head` 出發。**\n\n```cpp\n// 狀態表示：slow 慢、fast 快，都從頭開始\nListNode* slow = head;\nListNode* fast = head;\n```\n:::\n\n\n\n:::example 更新：注意條件，否則 fast->next->next 會解參考 nullptr\n**2. 更新流程：每步 slow 走一格、fast 走兩格。** 迴圈條件要同時檢查 `fast` 和 `fast->next`，避免走兩格時踩到空指標。\n\n```cpp\n// 更新：注意條件，否則 fast->next->next 會解參考 nullptr\nwhile (fast && fast->next) {\n  slow = slow->next;\n  fast = fast->next->next;\n}\n```\n:::\n\n\n\n:::example 找環入口：相遇後，ptr 從頭、slow 從相遇點，同速走到再次相遇\n**3. 找環的入口（142）：相遇後，用第二個游標從頭重走。** 數學上可證：相遇點與頭節點到「環入口」的距離相等，所以一個從頭、一個從相遇點，同速前進，再次相遇處就是入口。\n\n```cpp\n// 找環入口：相遇後，ptr 從頭、slow 從相遇點，同速走到再次相遇\nListNode* ptr = head;\nwhile (ptr != slow) {\n  ptr = ptr->next;\n  slow = slow->next;\n}\nreturn ptr;  // 環的入口節點\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 287「尋找重複數」可把陣列看成「值即下一個位置」的隱式串列，重複數對應到環入口，正好套用 142 的解法。\n- 234「迴文串列」常用快慢指標找中點，再反轉後半段比對。\n- 複雜度：時間 `O(n)`、額外空間 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「快慢指標」想：\n\n- **題目要判斷有沒有環、找環的入口**（141、142），或把問題轉化成「找隱式串列裡的環」（287 尋找重複數）。\n- **題目要找「正中間」的節點**，當作後續操作的分界（234 迴文、143 重排）。\n- **你想用 `O(1)` 空間達成上面的事**，不想開一個 `set` 記錄走過的節點：\n\n:::example 嗅到味道的瞬間：要判環或找中點，又想省空間\n```cpp\n// 嗅到味道的瞬間：要判環或找中點，又想省空間\n// 兩個游標一快一慢；相遇=有環，fast 到尾=slow 在中間\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的快、慢指標各走幾格？（找中點與判環都是 1 比 2）\n2. 迴圈條件有沒有同時檢查 `fast` 和 `fast->next`，避免空指標？\n3. 我要的是「相遇與否」（判環）還是「相遇位置」（環入口、中點）？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 141 | 環形連結串列 | N/A | 放在「快慢指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 142 | 環形連結串列 II | N/A | 放在「快慢指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 143 | 重排連結串列 | N/A | 放在「快慢指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 234 | 迴文連結串列 | N/A | 放在「快慢指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 287 | 尋找重複數 | N/A | 放在「快慢指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 141 | [環形連結串列](https://leetcode.cn/problems//linked-list-cycle/) |  |\n| 142 | [環形連結串列 II](https://leetcode.cn/problems//linked-list-cycle-ii/) |  |\n| 143 | [重排連結串列](https://leetcode.cn/problems//reorder-list/) |  |\n| 234 | [迴文連結串列](https://leetcode.cn/problems//palindrome-linked-list/) |  |\n| 287 | [尋找重複數](https://leetcode.cn/problems//find-the-duplicate-number/) |  |\n| 457 | [環形陣列是否存在迴圈](https://leetcode.cn/problems//circular-array-loop/) |  |\n| 2674 | [拆分迴圈連結串列](https://leetcode.cn/problems//split-a-circular-linked-list/) |  |\n| 876 | [連結串列的中間結點](https://leetcode.cn/problems//middle-of-the-linked-list/) | 1232 |\n| 2130 | [連結串列最大孿生和](https://leetcode.cn/problems//maximum-twin-sum-of-a-linked-list/) | 1318 |\n| 2095 | [刪除連結串列的中間節點](https://leetcode.cn/problems//delete-the-middle-node-of-a-linked-list/) | 1324 |\n| 3790 | [最小全 1 倍數](https://leetcode.cn/problems//smallest-all-ones-multiple/) | 1593 |\n| 1015 | [可被 K 整除的最小整數](https://leetcode.cn/problems//smallest-integer-divisible-by-k/) | 1875 |",
        },
        {
          id: 9,
          title: "1.7 雙指標",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在連結串列上同時維護**兩個（或多個）獨立前進的游標**，各自負責收集一部分節點，最後再把它們接起來。和「快慢指標」不同速、「前後指標」固定間隔不一樣，這裡的兩個指標通常是**各自獨立地往前走**，分頭做事。\n\n:::example 分流：兩個 dummy 各帶一個游標，分頭收節點（LeetCode 86）\n最典型的用法是「分流」：走訪原串列時，依條件把節點分到兩條新的子串列，最後再首尾相接。例如 LeetCode 86「分隔串列」要把小於 x 的節點排前面、其餘排後面——就開兩條子串列，一條收小的、一條收大的：\n\n```cpp\n// 分流：兩個 dummy 各帶一個游標，分頭收節點（LeetCode 86）\nListNode small, large;                // 兩條子串列的虛擬頭\nListNode *sp = &small, *lp = &large;  // 兩個游標分別收尾\n```\n:::\n\n\n\n:::example 收尾：每個節點依條件掛到對應子串列，最後兩條相接\n走訪時，每個節點依條件被接到對應游標後面，游標再前進；最後把「小的那條」尾巴接上「大的那條」的頭：\n\n```cpp\n// 收尾：每個節點依條件掛到對應子串列，最後兩條相接\nfor (ListNode* cur = head; cur; cur = cur->next) {\n  if (cur->val < x) {\n    sp->next = cur;\n    sp = sp->next;\n  } else {\n    lp->next = cur;\n    lp = lp->next;\n  }\n}\nlp->next = nullptr;     // 大的那條結尾封住\nsp->next = large.next;  // 小的尾巴接上大的頭\n```\n:::\n\n\n\n:::example 對齊兩條串列：走完自己就接到對方起點，最終相遇於交點（160）\n另一種用法是「對齊」兩條不同長度的串列。LeetCode 160「相交串列」讓兩個游標分別走 A、B，走到底就跳到對方的起點繼續走；因為 `a + b == b + a`，兩個游標走過的總長度相同，會在交點相遇（沒有交點則同時變 `nullptr`）。心法是：**用兩個指標把「不對齊」的兩段資料拉到同一個節奏上比較。**\n\n```cpp\n// 對齊兩條串列：走完自己就接到對方起點，最終相遇於交點（160）\nListNode *a = headA, *b = headB;\nwhile (a != b) {\n  a = a ? a->next : headB;\n  b = b ? b->next : headA;\n}\nreturn a;  // 交點，或同時為 nullptr\n```\n:::\n\n\n\n整類做法都只走常數趟，時間 `O(n + m)`、空間 `O(1)`。\n\n**實作方式**\n\n把雙指標分流／對齊類題目拆成幾個固定步驟，逐一搭配程式碼。\n\n:::example 狀態表示：奇偶分流為例（LeetCode 328）\n**1. 狀態表示：為每條子串列各備一個 dummy 與游標。** dummy 讓「每條子串列的頭」不必特判。\n\n```cpp\n// 狀態表示：奇偶分流為例（LeetCode 328）\nListNode oddDummy, evenDummy;\nListNode *odd = &oddDummy, *even = &evenDummy;\n```\n:::\n\n\n\n:::example 更新：奇數位掛 odd、偶數位掛 even（328）\n**2. 更新流程：走訪原串列，依條件把節點掛到對應游標後。** 每掛一個就讓對應游標前進。\n\n```cpp\n// 更新：奇數位掛 odd、偶數位掛 even（328）\nfor (ListNode* cur = head; cur;) {\n  odd->next = cur;\n  odd = cur;\n  cur = cur->next;  // 奇數位\n  if (cur) {\n    even->next = cur;\n    even = cur;\n    cur = cur->next;\n  }  // 偶數位\n}\n```\n:::\n\n\n\n:::example 收尾：偶串列結尾封住，奇串列尾接偶串列頭（328）\n**3. 收尾接線：封住結尾，再把子串列接成最終答案。** 漏接或漏封 `nullptr` 會造成斷鏈或成環。\n\n```cpp\n// 收尾：偶串列結尾封住，奇串列尾接偶串列頭（328）\neven->next = nullptr;\nodd->next = evenDummy.next;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 收尾時務必把最後一條子串列的 `next` 設為 `nullptr`，否則可能殘留舊箭頭而成環。\n- 對齊型（160）要確認「沒有交點」時兩游標會同時變 `nullptr` 而正常結束，不會死迴圈。\n- 複雜度：常數趟掃描，時間 `O(n + m)`、額外空間 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「雙指標（分流／對齊）」想：\n\n- **題目要按某條件把串列「拆成兩組」再重新拼接**（86 分隔串列、328 奇偶串列）。\n- **題目要比較或找兩條串列的關係**，例如交點（160），而兩條長度可能不同需要「對齊節奏」。\n- **你發現一個游標不夠用**，需要同時維護「目前在收哪一條」的兩個尾巴：\n\n:::example 嗅到味道的瞬間：要分兩堆或比兩條\n```cpp\n// 嗅到味道的瞬間：要分兩堆或比兩條\n// 開兩個 dummy + 兩個游標，分頭收，最後接起來\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要幾條子串列？每條的 dummy 和游標都備好了嗎？\n2. 走訪時，每個節點依什麼條件被分到哪一條？\n3. 收尾接線時，我有沒有封住最後一條的 `nullptr`，避免斷鏈或成環？\n\n三題都答得出來，這個技巧就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 86 | 分隔連結串列 | N/A | 放在「雙指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 160 | 相交連結串列 | N/A | 放在「雙指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 328 | 奇偶連結串列 | N/A | 放在「雙指標」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 86 | [分隔連結串列](https://leetcode.cn/problems//partition-list/) |  |\n| 160 | [相交連結串列](https://leetcode.cn/problems//intersection-of-two-linked-lists/) |  |\n| 328 | [奇偶連結串列](https://leetcode.cn/problems//odd-even-linked-list/) |  |",
        },
        {
          id: 10,
          title: "1.8 合併連結串列",
          summary:
            "**原理講解**\n\n本節要解決的場景是：把一條或多條「連結串列（linked list）」接在一起，或在運算過程中重新組合節點。連結串列是由一個個節點串成的鏈，每個節點記住自己的值，再用一個指標 `next` 指向下一個節點；最後一個節點的 `next` 是空（`nullptr`）。\n\n合併連結串列的核心想法是：**不要搬資料，只要改指標。** 想像兩條繩子各打了一排結，要把它們交錯接成一條，你只需要解開幾個接點重新綁，而不必把每個結重新做一遍。在程式裡「重新綁」就是改 `node->next`。\n\n:::example 連結串列節點長這樣\n這裡有一個新手最容易踩的陷阱：一旦你把某個節點的 `next` 改指到別處，原本它後面那一整串就「找不到了」。所以鐵律是：**改指標之前，先用一個變數把下一個節點存起來。**\n\n```cpp\n// 連結串列節點長這樣\nstruct ListNode {\n  int val;\n  ListNode* next;\n};\n```\n:::\n\n\n\n:::example 用 dummy 統一處理「新頭節點」，最後回傳 dummy.next\n第二個關鍵工具是「啞節點（dummy head）」。合併時新串列的「頭」是誰，往往要比較兩條串列才知道，寫起來很囉嗦。我們先做一個假的、不存資料的節點放在最前面，所有節點都接在它後面，最後回傳 `dummy.next` 即可，省去一堆「第一個怎麼辦」的特判。\n\n```cpp\n// 用 dummy 統一處理「新頭節點」，最後回傳 dummy.next\nListNode dummy;           // 假節點，不存實際資料\nListNode* tail = &dummy;  // tail 永遠指向已接好部分的最後一個\n// ... 把節點一個個接到 tail 後面 ...\nreturn dummy.next;  // 真正的頭就是 dummy 後面那個\n```\n:::\n\n\n\n像第 21 題「合併兩個有序連結串列」就是：兩條已排序的串列，每次比較兩個頭，把較小的接到 `tail` 後面。第 2、445 題「兩數相加」則是逐位相加、把結果一位位串成新串列。整個過程每個節點只走訪一次，是 `O(n)`。\n\n**實作方式**\n\n合併類題目可拆成三個固定步驟，逐一配程式碼：\n\n:::example 步驟一：準備 dummy 與 tail\n**1. 建立 dummy 與 tail 指標。** `tail` 代表「目前已接好串列的尾巴」，新節點一律接在它後面，再把 `tail` 往後移。\n\n```cpp\n// 步驟一：準備 dummy 與 tail\nListNode dummy;\nListNode* tail = &dummy;\n```\n:::\n\n\n\n:::example 步驟二：每次挑較小的接上，再前進\n**2. 逐一比較 / 計算，接到 tail 後面。** 以合併兩條有序串列為例，每輪挑出較小的節點接上去，並把那條串列往前推進。\n\n```cpp\n// 步驟二：每次挑較小的接上，再前進\nwhile (l1 && l2) {\n  if (l1->val <= l2->val) {\n    tail->next = l1;\n    l1 = l1->next;\n  } else {\n    tail->next = l2;\n    l2 = l2->next;\n  }\n  tail = tail->next;  // tail 永遠跟到最尾端\n}\n```\n:::\n\n\n\n:::example 步驟三：接上剩餘的一條，回傳真正的頭\n**3. 收尾並讀答案。** 把還沒接完的那條整串直接接上（剩下的本來就有序），最後回傳 `dummy.next`。\n\n```cpp\n// 步驟三：接上剩餘的一條，回傳真正的頭\ntail->next = l1 ? l1 : l2;  // 其中一條可能還有剩\nreturn dummy.next;\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 改 `next` 前若還會用到原本的後續節點，務必先用 `ListNode* next = cur->next;` 存起來。\n- 頭節點可能被換掉或刪除，用 dummy 就不必特判「第一個」。\n- 時間 `O(n + m)`（兩條長度相加），只用了幾個指標，額外空間 `O(1)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「合併連結串列」想：\n\n- **輸入是一條或多條連結串列，要把它們接成一條新串列。** 例如第 21 題合併兩條有序串列、第 2／445 題把兩個數字逐位相加成新串列。\n- **答案的頭節點不固定、或第一個節點可能被刪改**，需要 dummy 來統一處理：\n\n:::example 嗅到味道的瞬間：新頭是誰要比較後才知道 → 用 dummy\n```cpp\n// 嗅到味道的瞬間：新頭是誰要比較後才知道 → 用 dummy\nListNode dummy;  // 先放假頭，最後 return dummy.next\nListNode* tail = &dummy;\n```\n:::\n\n\n\n- **你發現只要「改幾條指標」就能完成，不必真的搬移或複製資料。** 這正是連結串列題的價值：插入、刪除都是 `O(1)` 的局部改接。\n- **暴力做法是把節點值倒進陣列再排序重建（一次 `O(n log n)` 或多用一份空間）；** 直接改指標可做到 `O(n)` 且原地完成。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有用 dummy 來避開「第一個節點」的特判？\n2. 改某個節點的 `next` 之前，後面那串我有先存起來嗎？\n3. 迴圈結束後，還沒接完的那一段我有接上去嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2 | 兩數相加 | N/A | 放在「合併連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 21 | 合併兩個有序連結串列 | N/A | 放在「合併連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 369 | 給單連結串列加一（Premium） | N/A | 放在「合併連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 445 | 兩數相加 II | N/A | 放在「合併連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1634 | 求兩個多項式連結串列的和（Premium） | N/A | 放在「合併連結串列」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2 | [兩數相加](https://leetcode.cn/problems//add-two-numbers/) |  |\n| 21 | [合併兩個有序連結串列](https://leetcode.cn/problems//merge-two-sorted-lists/) |  |\n| 369 | [給單連結串列加一](https://leetcode.cn/problems//plus-one-linked-list/) |  |\n| 445 | [兩數相加 II](https://leetcode.cn/problems//add-two-numbers-ii/) |  |\n| 1634 | [求兩個多項式連結串列的和](https://leetcode.cn/problems//add-two-polynomials-represented-as-linked-lists/) |  |\n| 2816 | [翻倍以連結串列形式表示的數字](https://leetcode.cn/problems//double-a-number-represented-as-a-linked-list/) | 1394 |",
        },
        {
          id: 11,
          title: "1.9 分治",
          summary:
            "**原理講解**\n\n本節談的是「分治（divide and conquer）」在連結串列上的應用。分治的精神是一句老話：**「大問題拆成幾個一樣的小問題，分別解完再合起來。」** 當小問題小到不能再拆（例如只剩一個節點），答案就直接看得出來，這叫「遞迴的終點（base case）」。\n\n:::example 分治三步驟：拆 -> 遞迴解 -> 合併\n為什麼分治會比較快？關鍵在「合併」這一步很便宜，而「拆」每次把規模砍半，於是只需要拆 `log n` 層。以第 148 題「排序連結串列」為例，做法是歸併排序（merge sort）：把串列從中間切兩半，各自排好序，再用前一節「合併兩條有序串列」的技巧把它們併回去。\n\n```cpp\n// 分治三步驟：拆 -> 遞迴解 -> 合併\nListNode* sortList(ListNode* head) {\n  if (!head || !head->next) {\n    return head;  // 0 或 1 個節點，已排序\n  }\n  ListNode* mid = split(head);   // 1) 從中間切成兩半\n  ListNode* l = sortList(head);  // 2) 各自遞迴排序\n  ListNode* r = sortList(mid);\n  return merge(l, r);  // 3) 合併兩條有序串列\n}\n```\n:::\n\n\n\n:::example 快慢指標找中點，並把串列斷成兩半\n「從中間切開」用的是經典的快慢指標：快指標一次走兩步、慢指標一次走一步，快指標到底時，慢指標剛好在中間。\n\n```cpp\n// 快慢指標找中點，並把串列斷成兩半\nListNode* split(ListNode* head) {\n  ListNode* slow = head;\n  ListNode* fast = head->next;\n  while (fast && fast->next) {\n    slow = slow->next;\n    fast = fast->next->next;\n  }\n  ListNode* mid = slow->next;\n  slow->next = nullptr;  // 從中間切斷，前半結束\n  return mid;            // 回傳後半的頭\n}\n```\n:::\n\n\n\n每一層要把所有節點掃過一遍合併（`O(n)`），總共 `log n` 層，所以整體是 `O(n log n)`，比兩兩比較的 `O(n^2)` 快得多。\n\n**實作方式**\n\n分治題目可拆成三個固定步驟，逐一配程式碼：\n\n:::example 步驟一：base case，0 或 1 個元素已是答案\n**1. 寫終點（base case）。** 規模小到不能再拆時直接回傳，否則遞迴會無止境拆下去。\n\n```cpp\n// 步驟一：base case，0 或 1 個元素已是答案\nif (!head || !head->next) {\n  return head;\n}\n```\n:::\n\n\n\n:::example 步驟二：拆成兩半各自遞迴（以 K 條串列為例，分一半）\n**2. 拆成子問題並遞迴。** 連結串列從中點切兩半；若是第 23 題「合併 K 個串列」，則是把 K 條兩兩配對遞迴合併。\n\n```cpp\n// 步驟二：拆成兩半各自遞迴（以 K 條串列為例，分一半）\nListNode* mergeKLists(vector<ListNode*>& lists, int lo, int hi) {\n  if (lo == hi) {\n    return lists[lo];  // 只剩一條\n  }\n  int mid = (lo + hi) / 2;\n  ListNode* l = mergeKLists(lists, lo, mid);\n  ListNode* r = mergeKLists(lists, mid + 1, hi);\n  return merge(l, r);  // 合併兩條有序串列\n}\n```\n:::\n\n\n\n:::example 步驟三：合併兩條有序串列（分治的「合」）\n**3. 合併子答案。** 連結串列題的合併就是前一節「合併兩條有序串列」：比較兩個頭，較小的接上去。\n\n```cpp\n// 步驟三：合併兩條有序串列（分治的「合」）\nListNode* merge(ListNode* a, ListNode* b) {\n  ListNode dummy;\n  ListNode* t = &dummy;\n  while (a && b) {\n    if (a->val <= b->val) {\n      t->next = a;\n      a = a->next;\n    } else {\n      t->next = b;\n      b = b->next;\n    }\n    t = t->next;\n  }\n  t->next = a ? a : b;\n  return dummy.next;\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 一定要寫 base case，否則無限遞迴會堆爆呼叫堆疊（stack overflow）。\n- 切串列後務必把前半的尾端 `next` 設成 `nullptr`，否則兩半會還黏在一起。\n- 第 148 題 `O(n log n)`；第 23 題 K 條共 N 個節點，分治版是 `O(N log K)`，比一條條串接的 `O(N·K)` 快。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「分治」想：\n\n- **問題可以「對半切」，兩半的解法和原問題一模一樣。** 例如第 148 題排序串列、第 23 題合併 K 條串列，切一半之後子問題仍是「排序 / 合併」。\n- **合併兩個子答案很容易做。** 分治划不划算，往往取決於「合」這一步便不便宜：\n\n:::example 嗅到味道的瞬間：子問題同型、且「合」很便宜\n```cpp\n// 嗅到味道的瞬間：子問題同型、且「合」很便宜\n// 子答案是兩條有序串列 -> 合併只要 O(長度) -> 適合分治\n```\n:::\n\n\n\n- **暴力做法層層相依、規模沒被有效縮小（如逐條串接 K 個串列是 `O(N·K)`）；** 改成對半遞迴可把因子降成 `log`。\n- **資料天然有「中點 / 中心」可切**，例如用快慢指標就能切連結串列。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 base case 是什麼？遞迴一定會停嗎？\n2. 我把問題拆成的子問題，是不是和原問題同型？\n3. 兩個子答案怎麼「合」回去？這步是不是夠便宜？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 23 | 合併 K 個升序連結串列 | N/A | 放在「分治」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 148 | 排序連結串列 | N/A | 放在「分治」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 23 | [合併 K 個升序連結串列](https://leetcode.cn/problems//merge-k-sorted-lists/) |  |\n| 148 | [排序連結串列](https://leetcode.cn/problems//sort-list/) |  |",
        },
        {
          id: 12,
          title: "1.10 綜合應用",
          summary:
            "**原理講解**\n\n本節是連結串列的「綜合應用」：把連結串列當成一塊積木，和雜湊表（hash map）等結構搭在一起，設計出有特定行為的資料結構。代表題目是第 146 題「LRU 快取」、第 460 題「LFU 快取」、第 707 題「設計連結串列」、第 432 題「全 `O(1)` 的資料結構」。\n\n先說為什麼要用連結串列。陣列的麻煩在於：要在中間插入或刪除元素，後面全部得搬位子，是 `O(n)`。連結串列剛好相反：**只要手上握有某個節點，把它從鏈中拔掉或插進去，都只是改幾條指標，是 `O(1)`。** 這正是設計「需要頻繁搬動元素」的快取時最需要的能力。\n\n:::example 招牌組合：哈希表(鍵 -> 節點) + 雙向連結串列\n但連結串列也有弱點：**它不能像陣列那樣用索引一步跳到某個節點，要找東西得從頭一個個走。** 解法是搭配一個雜湊表，把「鍵」直接對應到「節點指標」，這樣就能 `O(1)` 找到節點，再 `O(1)` 把它搬走。這個「雜湊表 + 雙向連結串列」的組合是本節的招牌。\n\n```cpp\n// 招牌組合：哈希表(鍵 -> 節點) + 雙向連結串列\nstruct Node {\n  int key, val;\n  Node *prev, *next;\n};\nunordered_map<int, Node *>\n    mp;  // O(1) 由鍵找到節點\n         // 雙向：每個節點同時有 prev 與 next，方便從中間拔除\n```\n:::\n\n\n\n:::example O(1) 從雙向串列中拔除一個節點\n為什麼要「雙向」連結串列？因為要把中間某個節點拔掉，必須知道它「前一個」是誰才能接合。單向串列只有 `next`，找前驅得從頭掃；雙向串列多存一個 `prev`，拔除就真的是 `O(1)`。\n\n```cpp\n// O(1) 從雙向串列中拔除一個節點\nvoid remove(Node* x) {\n  x->prev->next = x->next;  // 前一個直接指向後一個\n  x->next->prev = x->prev;  // 後一個的 prev 也接回去\n}\n```\n:::\n\n\n\n以 LRU 為例：把「最近用過」的放鏈頭、「最久沒用」的留在鏈尾。每次存取就把該節點搬到頭；容量滿了就刪鏈尾。每個操作都是 `O(1)`。\n\n**實作方式**\n\n設計這類結構可拆成四個步驟，逐一配程式碼：\n\n:::example 步驟一：頭尾哨兵，串列永遠非空，省去邊界特判\n**1. 選資料表示：雙向串列 + 雜湊表，並加頭尾哨兵。** 頭尾各放一個不存資料的哨兵節點（sentinel），這樣任何節點都保證有 `prev` 和 `next`，插刪不必特判邊界。\n\n```cpp\n// 步驟一：頭尾哨兵，串列永遠非空，省去邊界特判\nNode* head = new Node();  // 哨兵頭\nNode* tail = new Node();\nhead->next = tail;\ntail->prev = head;\n```\n:::\n\n\n\n:::example 步驟二：插到頭部（代表「最新使用」）\n**2. 寫好兩個 O(1) 基本動作：插到頭、從中間拔除。** 所有高階操作都由這兩個拼出來。\n\n```cpp\n// 步驟二：插到頭部（代表「最新使用」）\nvoid pushFront(Node* x) {\n  x->prev = head;\n  x->next = head->next;\n  head->next->prev = x;\n  head->next = x;\n}\n```\n:::\n\n\n\n:::example 步驟三：get = 找到 + 搬到頭 + 回傳值\n**3. 用雜湊表把鍵接到節點，組出 get / put。** `get` 找到節點、搬到頭並回傳值；`put` 若已存在就更新並搬到頭，不存在就新建、必要時淘汰鏈尾。\n\n```cpp\n// 步驟三：get = 找到 + 搬到頭 + 回傳值\nint get(int key) {\n  if (!mp.count(key)) {\n    return -1;\n  }\n  Node* x = mp[key];\n  remove(x);\n  pushFront(x);  // 標記為最近使用\n  return x->val;\n}\n```\n:::\n\n\n\n:::example 步驟四：容量滿，淘汰最久未用（尾端）\n**4. 處理容量上限與淘汰。** 超過容量時，刪掉哨兵尾巴前一個節點，並從雜湊表移除對應的鍵。\n\n```cpp\n// 步驟四：容量滿，淘汰最久未用（尾端）\nif (mp.size() > capacity) {\n  Node* lru = tail->prev;\n  remove(lru);\n  mp.erase(lru->key);\n  delete lru;\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 哨兵頭尾能避免「鏈為空 / 只有一個節點」的特判。\n- 拔除節點後別忘了同步從雜湊表 `erase` 鍵，否則會殘留懸空指標。\n- 每個操作 `O(1)`；空間 `O(容量)`。LFU（第 460 題）多一層「按使用次數分組」，但每組內仍是同一套雙向串列搬移。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「連結串列綜合應用」想：\n\n- **題目要你「設計一個資料結構」，並要求多數操作做到 `O(1)`。** 例如第 146 題 LRU、第 460 題 LFU、第 432 題全 `O(1)` 結構。\n- **需要頻繁「依某種順序」搬動元素**（最近用的提到前面、最久沒用的淘汰），同時又要能用鍵快速找到元素：\n\n:::example 嗅到味道的瞬間：既要 O(1) 查找、又要 O(1) 從中間搬移\n```cpp\n// 嗅到味道的瞬間：既要 O(1) 查找、又要 O(1) 從中間搬移\n// 查找 -> unordered_map；中間插刪 -> 雙向連結串列\n```\n:::\n\n\n\n- **單用陣列會在插刪時 `O(n)` 搬移、單用雜湊表又記不住順序。** 兩者合體才能同時滿足「查得快」與「搬得快」。\n- **第 707 題直接要你實作連結串列本身**，是練習這些指標改接基本功的入門。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用什麼結構做到 `O(1)` 查找？又用什麼做到 `O(1)` 從中間插刪？\n2. 我有沒有加頭尾哨兵來省掉邊界特判？\n3. 淘汰一個節點時，雜湊表裡對應的鍵我有一起刪掉嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 146 | LRU 快取 | N/A | 放在「綜合應用」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 432 | 全 $O(1)$ 的資料結構 | N/A | 放在「綜合應用」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 460 | LFU 快取 | N/A | 放在「綜合應用」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 707 | 設計連結串列 | N/A | 放在「綜合應用」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1019 | [連結串列中的下一個更大節點](https://leetcode.cn/problems/next-greater-node-in-linked-list/) | 1571 | 放在「綜合應用」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 146 | [LRU 快取](https://leetcode.cn/problems//lru-cache/) |  |\n| 432 | [全 $O(1)$ 的資料結構](https://leetcode.cn/problems//all-oone-data-structure/) |  |\n| 460 | [LFU 快取](https://leetcode.cn/problems//lfu-cache/) |  |\n| 707 | [設計連結串列](https://leetcode.cn/problems//design-linked-list/) |  |\n| 1019 | [連結串列中的下一個更大節點](https://leetcode.cn/problems//next-greater-node-in-linked-list/) | 1571 |\n| 1171 | [從連結串列中刪去總和值為零的連續節點](https://leetcode.cn/problems//remove-zero-sum-consecutive-nodes-from-linked-list/) | 1782 |\n| 1206 | [設計跳錶](https://leetcode.cn/problems//design-skiplist/) |  |",
        },
        {
          id: 13,
          title: "1.11 其他",
          summary:
            "**原理講解**\n\n本節收錄連結串列的「其他」技巧，這些題目不靠單一招式，而是各有巧思。代表題目有第 138 題「隨機連結串列的複製」、第 382 題「連結串列隨機節點」、第 430 題「扁平化多級雙向連結串列」、第 1265 題「逆序列印不可變連結串列」。它們共同的精神是：**面對連結串列，先想清楚「我需要什麼額外資訊」，再決定要不要多走幾趟、或借一份輔助結構。**\n\n:::example 第 138 題：用哈希表記住 原節點 -> 新節點 的對應\n以第 138 題為例。每個節點除了 `next`，還有一個 `random` 指標可指向任意節點。複製時的難點是：當你正在做某節點的副本，它的 `random` 指向的那個節點可能還沒被複製出來，你不知道副本是誰。解法是借一個雜湊表，先把「原節點 -> 新節點」的對應全部記下來，再回頭接指標。\n\n```cpp\n// 第 138 題：用哈希表記住 原節點 -> 新節點 的對應\nunordered_map<Node*, Node*> mp;\nfor (Node* p = head; p; p = p->next) {\n  mp[p] = new Node(p->val);  // 第一趟：先把所有節點複製出來\n}\n```\n:::\n\n\n\n:::example 第二趟：副本的 next / random 都靠查表接上\n```cpp\n// 第二趟：副本的 next / random 都靠查表接上\nfor (Node* p = head; p; p = p->next) {\n  mp[p]->next = mp[p->next];  // p->next 可能是 nullptr，查表得 nullptr\n  mp[p]->random = mp[p->random];  // 此時對應的副本一定已存在\n}\n```\n:::\n\n\n\n第 430 題「扁平化多級串列」則是另一種思路：每個節點除了 `next` 還有一個 `child` 子串列，要把它攤平成一條。遇到有 `child` 的節點，就先把子串列整段插進「目前節點和它原本的 next 之間」，再繼續往下，本質是深度優先（DFS）。第 382 題用到「水塘抽樣（reservoir sampling）」，可在不知道長度、只走一趟的情況下等機率隨機選一個節點。\n\n**實作方式**\n\n這類題沒有單一模板，但有幾個反覆出現的招式，逐一配程式碼：\n\n:::example 招式一：兩趟法 + 哈希表（第 138 題核心）\n**1. 需要「之後才知道的對應關係」時，先建雜湊表。** 像複製帶 `random` 指標的串列，分兩趟：第一趟造節點建表，第二趟接指標。\n\n```cpp\n// 招式一：兩趟法 + 哈希表（第 138 題核心）\nfor (Node* p = head; p; p = p->next) {\n  mp[p] = new Node(p->val);\n}\nfor (Node* p = head; p; p = p->next) {\n  mp[p]->next = mp[p->next];\n  mp[p]->random = mp[p->random];\n}\n```\n:::\n\n\n\n:::example 招式二：扁平化，把 child 串列插到 cur 與 cur->next 之間（第 43…\n**2. 遇到「多層 / 巢狀」結構，用 DFS 把子串列就地插入。** 把 `child` 那段攤開接到目前節點後面，再把原本的 `next` 接到子串列尾端。\n\n```cpp\n// 招式二：扁平化，把 child 串列插到 cur 與 cur->next 之間（第 430 題）\nif (cur->child) {\n  Node* nxt = cur->next;\n  cur->next = cur->child;\n  cur->child->prev = cur;\n  cur->child = nullptr;  // 攤平後 child 要清空\n  Node* tail = cur->next;\n  while (tail->next) {\n    tail = tail->next;  // 找到子串列尾\n  }\n  tail->next = nxt;\n  if (nxt) {\n    nxt->prev = tail;\n  }\n}\n```\n:::\n\n\n\n:::example 招式三：水塘抽樣，O(1) 空間、一趟取等機率隨機節點（第 382 題）\n**3. 只能走一趟又要隨機，用水塘抽樣。** 走到第 `i` 個節點時，以 `1/i` 的機率用它替換目前答案，最終每個節點被選中的機率都相等。\n\n```cpp\n// 招式三：水塘抽樣，O(1) 空間、一趟取等機率隨機節點（第 382 題）\nint res = 0, i = 0;\nfor (Node* p = head; p; p = p->next) {\n  if (rand() % (++i) == 0) {\n    res = p->val;  // 第 i 個以 1/i 機率被選中\n  }\n}\nreturn res;\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 查表接指標時，`nullptr` 也要能查到 `nullptr`；用 `unordered_map` 時 `mp[nullptr]` 預設就是 `nullptr`，剛好正確。\n- 扁平化插入子串列後，記得把 `child` 清成 `nullptr`，並維護好雙向的 `prev`。\n- 第 138／430 題時間 `O(n)`；雜湊表法空間 `O(n)`，扁平化與水塘抽樣可做到 `O(1)` 額外空間。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往本節的「其他技巧」想：\n\n- **節點上有「非線性」的額外指標**，例如 `random`（第 138 題）或 `child`（第 430 題），單純沿 `next` 走無法解決。\n- **要建立「節點到副本／到某資訊」的對應，而對應對象當下還沒生出來** —— 先建雜湊表，分趟處理：\n\n:::example 嗅到味道的瞬間：現在要接的指標，指向的東西還沒準備好\n```cpp\n// 嗅到味道的瞬間：現在要接的指標，指向的東西還沒準備好\n// 解法：先把所有對應記進 unordered_map，第二趟再接\n```\n:::\n\n\n\n- **不知道串列長度、又限制只走一趟或 `O(1)` 空間做隨機** —— 這是水塘抽樣的招牌場景（第 382 題）。\n- **巢狀 / 多層結構要攤平** —— 想到 DFS，把子層就地插入主鏈（第 430 題）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這題卡住，是不是因為「現在要用的資訊還沒準備好」？要不要先建表分趟做？\n2. 節點上除了 `next`，還有沒有別的指標（`random`、`child`）需要一起處理？\n3. 若要隨機或限制空間，我能不能用水塘抽樣只走一趟？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 138 | 隨機連結串列的複製 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 382 | 連結串列隨機節點 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 430 | 扁平化多級雙向連結串列 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1265 | 逆序列印不可變連結串列（Premium） | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 138 | [隨機連結串列的複製](https://leetcode.cn/problems//copy-list-with-random-pointer/) |  |\n| 382 | [連結串列隨機節點](https://leetcode.cn/problems//linked-list-random-node/) |  |\n| 430 | [扁平化多級雙向連結串列](https://leetcode.cn/problems//flatten-a-multilevel-doubly-linked-list/) |  |\n| 1265 | [逆序列印不可變連結串列](https://leetcode.cn/problems//print-immutable-linked-list-in-reverse/) |  |",
        },
      ],
    },
    {
      id: 14,
      title: "2. 二叉樹",
      summary:
        "涵蓋二叉樹的遍歷、DFS/BFS、LCA、直徑、BST、N叉樹、樹形DP、連結串列+二叉樹等，適合系統學習與刷題。\n\n:::example 二叉樹 DFS（前序遍歷）\n**C++ 模板（二叉樹 DFS）**：\n\n```cpp\n// 二叉樹 DFS（前序遍歷）\nvoid dfs(TreeNode* root) {\n  if (!root) {\n    return;\n  }\n  // 處理 root->val（前序）\n  dfs(root->left);\n  dfs(root->right);\n}\n\n// 求二叉樹最大深度（後序遍歷）\nint maxDepth(TreeNode* root) {\n  if (!root) {\n    return 0;\n  }\n  return max(maxDepth(root->left), maxDepth(root->right)) + 1;\n}\n```\n:::",
      children: [
        {
          id: 15,
          title: "2.1 遍歷二叉樹",
          summary:
            "**原理講解**\n\n本節要學的是「遍歷二叉樹（traverse a binary tree）」：把一棵樹上每個節點都不重不漏地走訪一次。先講清楚名詞。二叉樹是一種分叉的結構，每個節點最多有左、右兩個子節點；最上面那個叫「根（root）」，沒有子節點的叫「葉（leaf）」。\n\n樹和陣列最大的不同是：**陣列是一條直線，從頭走到尾就好；樹有分叉，走到一個節點時你有兩條路（左、右）要走。** 所以遍歷的關鍵是定義「先走哪邊、什麼時候處理自己」。最自然的寫法是遞迴：對每個節點，就「處理自己、遞迴左、遞迴右」，三者的先後順序不同，就得到三種遍歷：\n\n- 前序（preorder）：自己 -> 左 -> 右（第 144 題）\n- 中序（inorder）：左 -> 自己 -> 右（第 94 題）\n- 後序（postorder）：左 -> 右 -> 自己（第 145 題）\n\n:::example 二叉樹節點與「中序遍歷」：左 -> 自己 -> 右\n```cpp\n// 二叉樹節點與「中序遍歷」：左 -> 自己 -> 右\nstruct TreeNode {\n  int val;\n  TreeNode *left, *right;\n};\nvoid inorder(TreeNode* root, vector<int>& out) {\n  if (!root) {\n    return;  // 走到空節點就回頭（base case）\n  }\n  inorder(root->left, out);   // 先把左子樹全部走完\n  out.push_back(root->val);   // 再處理自己\n  inorder(root->right, out);  // 最後走右子樹\n}\n```\n:::\n\n\n\n為什麼這樣就能不重不漏？因為每個節點都恰好被它的父節點呼叫一次，而 `if (!root) return;` 保證走到底就回頭，不會無限繞。整棵樹有 n 個節點，每個處理一次，所以是 `O(n)`。\n\n:::example 「前序遍歷」：把「處理自己」提到最前 -> 自己 -> 左 -> 右\n換個順序就換一種遍歷。例如前序只要把「處理自己」這行移到最前面：\n\n```cpp\n// 「前序遍歷」：把「處理自己」提到最前 -> 自己 -> 左 -> 右\nvoid preorder(TreeNode* root, vector<int>& out) {\n  if (!root) {\n    return;\n  }\n  out.push_back(root->val);  // 先處理自己\n  preorder(root->left, out);\n  preorder(root->right, out);\n}\n```\n:::\n\n\n\n順序的選擇要看題目要的資訊何時可用。例如第 671 題「二叉樹中第二小的節點」、第 404 題「左葉子之和」，都是在遍歷過程中順手累積一個答案。\n\n**實作方式**\n\n遍歷題可拆成三個步驟，逐一配程式碼：\n\n:::example 步驟一：空節點直接返回\n**1. 想清楚 base case。** 走到空節點（`nullptr`）就回頭，這是遞迴一定會停下來的保證。\n\n```cpp\n// 步驟一：空節點直接返回\nif (!root) {\n  return;\n}\n```\n:::\n\n\n\n:::example 步驟二：以「左葉子之和」為例，遍歷時順手累加（第 404 題）\n**2. 決定處理自己的時機（前 / 中 / 後序）。** 依題目需要選順序：要照大小順序輸出二叉搜尋樹用中序；要先看父再看子用前序；要先彙整子樹資訊再算自己用後序。\n\n```cpp\n// 步驟二：以「左葉子之和」為例，遍歷時順手累加（第 404 題）\nvoid dfs(TreeNode* root, int& sum) {\n  if (!root) {\n    return;\n  }\n  if (root->left && !root->left->left && !root->left->right) {\n    sum += root->left->val;  // 左子節點且它是葉 -> 累加\n  }\n  dfs(root->left, sum);\n  dfs(root->right, sum);\n}\n```\n:::\n\n\n\n:::example 步驟三：呼叫端拿到結果\n**3. 從累積的狀態讀出答案。** 用一個外部變數（或回傳值）邊走邊更新，遍歷結束後它就是答案。\n\n```cpp\n// 步驟三：呼叫端拿到結果\nint sumOfLeftLeaves(TreeNode* root) {\n  int sum = 0;\n  dfs(root, sum);\n  return sum;\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 一定要先寫 `if (!root) return;`，否則對空指標取 `->left` 會崩潰。\n- 累積資訊用「傳參考」或「回傳值」都行，新手用 `int&` 傳參考最直觀。\n- 時間 `O(n)`（每節點一次）；遞迴會用到呼叫堆疊，空間 `O(樹高)`，最壞（退化成鏈）是 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「遍歷二叉樹」想：\n\n- **輸入是一棵二叉樹，要求「對每個節點做點事」或收集所有節點的某種資訊。** 例如第 94／144／145 題就是按特定順序輸出所有節點值。\n- **答案可以邊走邊累積。** 走訪時順手更新一個變數即可：\n\n:::example 嗅到味道的瞬間：每個節點都要看一眼、順手記點東西\n```cpp\n// 嗅到味道的瞬間：每個節點都要看一眼、順手記點東西\n// -> 遞迴 dfs，挑前 / 中 / 後序，邊走邊更新答案\n```\n:::\n\n\n\n- **題目暗示了順序需求。** 「按由小到大」往往是中序（二叉搜尋樹中序即有序）；「先處理父再處理子」是前序。\n- **暴力地對每個節點都重新從根找一遍是 `O(n^2)`；** 一次遍歷把每個節點只看一次就是 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 base case 是不是「節點為空就 return」？\n2. 我該在「走左 / 走右」之前還是之後處理自己（前 / 中 / 後序）？\n3. 答案是用哪個變數邊走邊累積、遍歷結束後讀出來的？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 94 | 二叉樹的中序遍歷 | N/A | 放在「遍歷二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 144 | 二叉樹的前序遍歷 | N/A | 放在「遍歷二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 145 | 二叉樹的後序遍歷 | N/A | 放在「遍歷二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 404 | 左葉子之和 | N/A | 放在「遍歷二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 671 | 二叉樹中第二小的節點 | N/A | 放在「遍歷二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 94 | [二叉樹的中序遍歷](https://leetcode.cn/problems//binary-tree-inorder-traversal/) |  |\n| 144 | [二叉樹的前序遍歷](https://leetcode.cn/problems//binary-tree-preorder-traversal/) |  |\n| 145 | [二叉樹的後序遍歷](https://leetcode.cn/problems//binary-tree-postorder-traversal/) |  |\n| 404 | [左葉子之和](https://leetcode.cn/problems//sum-of-left-leaves/) |  |\n| 671 | [二叉樹中第二小的節點](https://leetcode.cn/problems//second-minimum-node-in-a-binary-tree/) |  |\n| 872 | [葉子相似的樹](https://leetcode.cn/problems//leaf-similar-trees/) | 1288 |\n| 1214 | [查詢兩棵二叉搜索樹之和](https://leetcode.cn/problems//two-sum-bsts/) | 1389 |\n| 1469 | [尋找所有的獨生節點](https://leetcode.cn/problems//find-all-the-lonely-nodes/) |  |\n| 2764 | [陣列是否表示某二叉樹的前序遍歷](https://leetcode.cn/problems//is-array-a-preorder-of-some-binary-tree/) |  |\n| 4046 | [LCP 44. 開幕式焰火](https://leetcode.cn/problems//sZ59z6/) |  |",
        },
        {
          id: 16,
          title: "2.2 自頂向下 DFS（先序遍歷）",
          summary:
            "**原理講解**\n\n本節是「自頂向下 DFS（先序遍歷）」。DFS 是「深度優先搜尋（depth-first search）」，意思是沿著一條路一直往深處走到底，再回頭走旁邊的路。「自頂向下」指的是資訊的流向：**從根節點往下傳，父節點先把它沿途累積的資訊（例如目前深度、從根到這裡的路徑和）算好，當作參數交給子節點。**\n\n這正好對應「先序」：先處理自己（更新要往下傳的資訊），再遞迴左右子樹。和上一節單純遍歷的差別在於：這裡我們會多帶一個「沿路帶下去的參數」。\n\n:::example 第 104 題：自頂向下，把「目前深度」當參數往下傳\n舉第 104 題「二叉樹的最大深度」為例。每往下走一層，深度就 +1，把這個深度當參數傳給子節點；走到葉子時，目前的深度就是一條從根到底的長度，拿來更新答案。\n\n```cpp\n// 第 104 題：自頂向下，把「目前深度」當參數往下傳\nvoid dfs(TreeNode* root, int depth, int& ans) {\n  if (!root) {\n    return;\n  }\n  ++depth;                      // 自己這層，深度 +1（先處理自己）\n  ans = max(ans, depth);        // 用目前累積資訊更新答案\n  dfs(root->left, depth, ans);  // 把累積好的 depth 交給子節點\n  dfs(root->right, depth, ans);\n}\n```\n:::\n\n\n\n:::example 第 129 題：把根到目前節點組成的數字往下傳\n再看第 129 題「求根到葉節點數字之和」：從根往下走，把一路上的數字串成一個數（每下一層就 `cur = cur * 10 + 節點值`），到葉子時這個數就完整了，加進答案。這個「沿路累積、傳給子節點」就是自頂向下的精髓。\n\n```cpp\n// 第 129 題：把根到目前節點組成的數字往下傳\nvoid dfs(TreeNode* root, int cur, int& sum) {\n  if (!root) {\n    return;\n  }\n  cur = cur * 10 + root->val;         // 先把自己接到數字尾端\n  if (!root->left && !root->right) {  // 到葉子，一條完整數字\n    sum += cur;\n  }\n  dfs(root->left, cur, sum);\n  dfs(root->right, cur, sum);\n}\n```\n:::\n\n\n\n每個節點仍只走訪一次，是 `O(n)`。\n\n**實作方式**\n\n自頂向下 DFS 可拆成三個步驟，逐一配程式碼：\n\n:::example 步驟一：以「路徑總和」第 112 題為例，往下傳「還差多少」\n**1. 設計「要往下傳的參數」。** 想清楚子節點需要知道父輩累積了什麼：深度、路徑和、目前組成的數字、剩餘目標值等。\n\n```cpp\n// 步驟一：以「路徑總和」第 112 題為例，往下傳「還差多少」\nbool dfs(TreeNode* root, int remain) {  // remain = 目標 - 已走過的和\n  if (!root) {\n    return false;\n  }\n  remain -= root->val;  // 先扣掉自己（先序：先處理自己）\n                        // ...\n}\n```\n:::\n\n\n\n:::example 步驟二：到葉子時檢查是否剛好湊到目標\n**2. 在「處理自己」時更新參數並判斷答案。** 這一步要放在遞迴左右之前（這就是先序）。到葉子節點時通常是檢查或結算的時機。\n\n```cpp\n// 步驟二：到葉子時檢查是否剛好湊到目標\nif (!root->left && !root->right) {\n  return remain == 0;  // 葉子且剛好扣到 0 -> 找到一條路徑\n}\n```\n:::\n\n\n\n:::example 步驟三：把新的 remain 交給左右子樹\n**3. 把更新後的參數遞迴給左右子節點。** 左右只要有一邊成立（或依題意彙整）即可。\n\n```cpp\n// 步驟三：把新的 remain 交給左右子樹\nreturn dfs(root->left, remain) || dfs(root->right, remain);\n```\n:::\n\n\n\n邊界與複雜度：\n\n- 「先處理自己再往下傳」是先序的關鍵；順序顛倒會把尚未更新的值傳下去而出錯。\n- 參數用「傳值」傳下去（如 `int depth`），這樣每條路徑各有自己的副本，回溯時不互相污染。\n- 像第 199 題「右檢視」可在進入每層時，把「該層第一個被訪問到的節點」記下，配合「先右後左」的順序即得每層最右節點。\n- 時間 `O(n)`，空間 `O(樹高)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「自頂向下 DFS」想：\n\n- **答案取決於「從根到目前節點」這一路的累積資訊。** 例如深度（第 104／111 題）、路徑數字（第 129 題）、路徑和是否達標（第 112 題）。\n- **你會想「在進入子節點前，先把某個值算好交給它」。** 這就是自頂向下、用參數往下帶：\n\n:::example 嗅到味道的瞬間：子節點需要知道「父輩一路帶下來的東西」\n```cpp\n// 嗅到味道的瞬間：子節點需要知道「父輩一路帶下來的東西」\n// -> 把那個東西當參數，先序時更新後再遞迴下去\n```\n:::\n\n\n\n- **常在葉子節點結算。** 「到葉子時……」這種敘述，多半是自頂向下累積到底再判斷。\n- **暴力對每個節點都從根重算一次路徑是 `O(n^2)`；** 自頂向下把累積值順手帶下去，一趟 `O(n)` 解決。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要往子節點「帶下去」的資訊是什麼？（深度？路徑和？）\n2. 我有沒有在「遞迴左右之前」就先更新好這個資訊（先序）？\n3. 我在哪裡結算答案——是不是在葉子節點？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 104 | 二叉樹的最大深度 | N/A | 放在「自頂向下 DFS（先序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 111 | 二叉樹的最小深度 | N/A | 放在「自頂向下 DFS（先序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 112 | 路徑總和 | N/A | 放在「自頂向下 DFS（先序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 129 | 求根節點到葉節點數字之和 | N/A | 放在「自頂向下 DFS（先序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 199 | 二叉樹的右檢視 | N/A | 放在「自頂向下 DFS（先序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 104 | [二叉樹的最大深度](https://leetcode.cn/problems//maximum-depth-of-binary-tree/) |  |\n| 111 | [二叉樹的最小深度](https://leetcode.cn/problems//minimum-depth-of-binary-tree/) |  |\n| 112 | [路徑總和](https://leetcode.cn/problems//path-sum/) |  |\n| 129 | [求根節點到葉節點數字之和](https://leetcode.cn/problems//sum-root-to-leaf-numbers/) |  |\n| 199 | [二叉樹的右檢視](https://leetcode.cn/problems//binary-tree-right-side-view/) |  |\n| 298 | [二叉樹最長連續序列](https://leetcode.cn/problems//binary-tree-longest-consecutive-sequence/) |  |\n| 545 | [二叉樹的邊界](https://leetcode.cn/problems//boundary-of-binary-tree/) |  |\n| 623 | [在二叉樹中增加一行](https://leetcode.cn/problems//add-one-row-to-tree/) |  |\n| 971 | [翻轉二叉樹以匹配先序遍歷](https://leetcode.cn/problems//flip-binary-tree-to-match-preorder-traversal/) | 1787 |\n| 988 | [從葉結點開始的最小字串](https://leetcode.cn/problems//smallest-string-starting-from-leaf/) | 1429 |\n| 1022 | [從根到葉的二進位制數之和](https://leetcode.cn/problems//sum-of-root-to-leaf-binary-numbers/) | 1462 |\n| 1026 | [節點與其祖先之間的最大差值](https://leetcode.cn/problems//maximum-difference-between-node-and-ancestor/) | 1446 |\n| 1315 | [祖父節點值為偶數的節點和](https://leetcode.cn/problems//sum-of-nodes-with-even-valued-grandparent/) | 1427 |\n| 1372 | [二叉樹中的最長交錯路徑](https://leetcode.cn/problems//longest-zigzag-path-in-a-binary-tree/) | 1713 |\n| 1430 | [判斷給定的序列是否是二叉樹從根到葉的路徑](https://leetcode.cn/problems//check-if-a-string-is-a-valid-sequence-from-root-to-leaves-path-in-a-binary-tree/) |  |\n| 1448 | [統計二叉樹中好節點的數目](https://leetcode.cn/problems//count-good-nodes-in-binary-tree/) | 1360 |\n| 2689 | [從 Rope 樹中提取第 K 個字元](https://leetcode.cn/problems//extract-kth-character-from-the-rope-tree/) |  |",
        },
        {
          id: 17,
          title: "2.3 自底向上 DFS（後序遍歷）",
          summary:
            "**原理講解**\n\n本節是「自底向上 DFS（後序遍歷）」。和上一節相反，這裡資訊的流向是**從下往上彙整：先把左、右子樹的答案各自算出來，再用這兩個結果合成「自己這棵子樹」的答案，往上交給父節點。** 這正對應「後序」：先遞迴左、遞迴右，最後才處理自己。\n\n關鍵差別在於這次我們善用「回傳值」。每次遞迴回傳「以這個節點為根的子樹」的某個資訊（高度、是否平衡、是否相同……），父節點拿到左右兩份回傳值後做合併。\n\n:::example 第 104 題（自底向上）：先拿左右深度，再合成自己的\n以第 104 題「最大深度」的自底向上寫法為例：一棵子樹的深度 = 左右子樹深度的較大者 + 1。葉子以下是空樹，深度 0。\n\n```cpp\n// 第 104 題（自底向上）：先拿左右深度，再合成自己的\nint maxDepth(TreeNode* root) {\n  if (!root) {\n    return 0;  // 空樹深度 0（base case）\n  }\n  int l = maxDepth(root->left);   // 先算左子樹答案\n  int r = maxDepth(root->right);  // 再算右子樹答案\n  return max(l, r) + 1;           // 合成：較深的一邊 +1\n}\n```\n:::\n\n\n\n:::example 第 110 題：回傳高度；用 -1 代表「已偵測到不平衡」\n很多「整棵樹的某性質」都能這樣由下而上判斷。第 110 題「平衡二叉樹」要求每個節點的左右高度差 ≤ 1：我們讓遞迴回傳高度，順便檢查；一旦發現不平衡就回傳一個哨兵值（例如 -1）一路往上傳，代表「整棵已經不平衡」。\n\n```cpp\n// 第 110 題：回傳高度；用 -1 代表「已偵測到不平衡」\nint height(TreeNode* root) {\n  if (!root) {\n    return 0;\n  }\n  int l = height(root->left);\n  if (l == -1) {\n    return -1;\n  }\n  int r = height(root->right);\n  if (r == -1) {\n    return -1;\n  }\n  if (abs(l - r) > 1) {\n    return -1;  // 自己這裡就不平衡\n  }\n  return max(l, r) + 1;  // 否則回傳正常高度\n}\n```\n:::\n\n\n\n這種「一次走訪就回傳兩種意義（高度 + 是否合格）」的技巧，比每個節點都重算高度的暴力 `O(n^2)` 快，整體是 `O(n)`。\n\n**實作方式**\n\n自底向上 DFS 可拆成三個步驟，逐一配程式碼：\n\n:::example 步驟一：base case 通常是空樹回傳「中性值」\n**1. 想清楚「每個節點要回傳什麼」。** 這是整個方法的核心：回傳的資訊必須足以讓父節點合成自己的答案（高度、子樹和、節點數、是否相同……）。\n\n```cpp\n// 步驟一：base case 通常是空樹回傳「中性值」\nif (!root) {\n  return 0;  // 高度 0 / 和 0 / 計數 0 等\n}\n```\n:::\n\n\n\n:::example 步驟二：先取得左右子樹的回傳值\n**2. 先遞迴左右，拿到兩份子答案。** 一定先把子問題解完（後序：左、右在前）。\n\n```cpp\n// 步驟二：先取得左右子樹的回傳值\nint l = dfs(root->left);\nint r = dfs(root->right);\n```\n:::\n\n\n\n:::example 步驟三：以「相同的樹」第 100 題為例，兩棵同時往下比\n**3. 用左右結果合成自己的答案並回傳。** 合併規則由題意決定；若要同時維護一個全域答案（如最大路徑和），就在這一步用 `l + r + 自己` 更新它。\n\n```cpp\n// 步驟三：以「相同的樹」第 100 題為例，兩棵同時往下比\nbool isSame(TreeNode* a, TreeNode* b) {\n  if (!a && !b) {\n    return true;  // 同時為空 -> 相同\n  }\n  if (!a || !b || a->val != b->val) {\n    return false;\n  }\n  return isSame(a->left, b->left)  // 左右子樹都要相同\n         && isSame(a->right, b->right);\n}\n```\n:::\n\n\n\n邊界與複雜度：\n\n- base case 要回傳「中性值」（高度 0、和 0），讓合併公式對葉子也成立。\n- 若要邊算邊更新一個全域答案（如最長路徑），通常回傳「單邊鏈的長度」，但更新答案時用「左 + 右」。第 101 題「對稱二叉樹」則是把左右子樹鏡像比對。\n- 時間 `O(n)`，每節點處理一次；空間 `O(樹高)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「自底向上 DFS」想：\n\n- **要判斷或計算「整棵子樹」的某種性質**：高度、是否平衡（第 110 題）、兩樹是否相同（第 100 題）/ 對稱（第 101 題）、子樹和等。\n- **自己的答案需要先知道左右子樹的答案才能算。** 這就是由下往上、靠回傳值合成：\n\n:::example 嗅到味道的瞬間：算自己之前，得先有左右子樹的結果\n```cpp\n// 嗅到味道的瞬間：算自己之前，得先有左右子樹的結果\n// -> 後序：先遞迴左右，拿回傳值再合成自己\n```\n:::\n\n\n\n- **可以一邊遞迴一邊「短路」傳壞消息**（如第 110 題用 -1 表示已不平衡），避免重複計算。\n- **暴力對每個節點都重新往下掃一遍子樹是 `O(n^2)`；** 自底向上讓每節點只回傳一次資訊，一趟 `O(n)` 完成。\n\n確認自己有沒有套對，問三個問題：\n\n1. 每個節點該「回傳」什麼，才夠父節點算出自己的答案？\n2. 空樹（base case）應該回傳什麼中性值，才讓合併公式對葉子也成立？\n3. 我是不是先遞迴完左右，最後才合成自己（後序）？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 67 | LCP 67. 裝飾樹 | N/A | 放在「自底向上 DFS（後序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 100 | 相同的樹 | N/A | 放在「自底向上 DFS（後序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 101 | 對稱二叉樹 | N/A | 放在「自底向上 DFS（後序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 104 | 二叉樹的最大深度 | N/A | 放在「自底向上 DFS（後序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 110 | 平衡二叉樹 | N/A | 放在「自底向上 DFS（後序遍歷）」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 67 | [LCP 67. 裝飾樹](https://leetcode.cn/problems//KnLfVT/) |  |\n| 100 | [相同的樹](https://leetcode.cn/problems//same-tree/) |  |\n| 101 | [對稱二叉樹](https://leetcode.cn/problems//symmetric-tree/) |  |\n| 104 | [二叉樹的最大深度](https://leetcode.cn/problems//maximum-depth-of-binary-tree/) |  |\n| 110 | [平衡二叉樹](https://leetcode.cn/problems//balanced-binary-tree/) |  |\n| 111 | [二叉樹的最小深度](https://leetcode.cn/problems//minimum-depth-of-binary-tree/) |  |\n| 156 | [上下翻轉二叉樹](https://leetcode.cn/problems//binary-tree-upside-down/) |  |\n| 226 | [翻轉二叉樹](https://leetcode.cn/problems//invert-binary-tree/) |  |\n| 250 | [統計同值子樹](https://leetcode.cn/problems//count-univalue-subtrees/) |  |\n| 298 | [二叉樹最長連續序列](https://leetcode.cn/problems//binary-tree-longest-consecutive-sequence/) |  |\n| 333 | [最大二叉搜尋子樹](https://leetcode.cn/problems//largest-bst-subtree/) |  |\n| 366 | [尋找二叉樹的葉子節點](https://leetcode.cn/problems//find-leaves-of-binary-tree/) |  |\n| 508 | [出現次數最多的子樹元素和](https://leetcode.cn/problems//most-frequent-subtree-sum/) |  |\n| 563 | [二叉樹的坡度](https://leetcode.cn/problems//binary-tree-tilt/) |  |\n| 572 | [另一棵樹的子樹](https://leetcode.cn/problems//subtree-of-another-tree/) |  |\n| 606 | [根據二叉樹建立字串](https://leetcode.cn/problems//construct-string-from-binary-tree/) |  |\n| 617 | [合併二叉樹](https://leetcode.cn/problems//merge-two-binary-trees/) |  |\n| 663 | [均勻樹劃分](https://leetcode.cn/problems//equal-tree-partition/) |  |\n| 951 | [翻轉等價二叉樹](https://leetcode.cn/problems//flip-equivalent-binary-trees/) | 1477 |\n| 965 | [單值二叉樹](https://leetcode.cn/problems//univalued-binary-tree/) | 1178 |\n| 1026 | [節點與其祖先之間的最大差值](https://leetcode.cn/problems//maximum-difference-between-node-and-ancestor/) | 1446 |\n| 1120 | [子樹的最大平均值](https://leetcode.cn/problems//maximum-average-subtree/) | 1362 |\n| 1145 | [二叉樹著色遊戲](https://leetcode.cn/problems//binary-tree-coloring-game/) | 1741 |\n| 1339 | [分裂二叉樹的最大乘積](https://leetcode.cn/problems//maximum-product-of-splitted-binary-tree/) | 1675 |\n| 1372 | [二叉樹中的最長交錯路徑](https://leetcode.cn/problems//longest-zigzag-path-in-a-binary-tree/) | 1713 |\n| 1379 | [找出克隆二叉樹中的相同節點](https://leetcode.cn/problems//find-a-corresponding-node-of-a-binary-tree-in-a-clone-of-that-tree/) |  |\n| 1530 | [好葉子節點對的數量](https://leetcode.cn/problems//number-of-good-leaf-nodes-pairs/) | 1746 |\n| 1612 | [檢查兩棵二叉表示式樹是否等價](https://leetcode.cn/problems//check-if-two-expression-trees-are-equivalent/) |  |\n| 1973 | [值等於子節點值之和的節點數量](https://leetcode.cn/problems//count-nodes-equal-to-sum-of-descendants/) |  |\n| 2265 | [統計值等於子樹平均值的節點數](https://leetcode.cn/problems//count-nodes-equal-to-average-of-subtree/) | 1473 |\n| 2331 | [計算布林二叉樹的值](https://leetcode.cn/problems//evaluate-boolean-binary-tree/) | 1304 |\n| 2792 | [計算足夠大的節點數](https://leetcode.cn/problems//count-nodes-that-are-great-enough/) |  |\n| 3319 | [第 K 大的完美二叉子樹的大小](https://leetcode.cn/problems//k-th-largest-perfect-subtree-size-in-binary-tree/) | 1603 |",
        },
        {
          id: 18,
          title: "2.4 自底向上 DFS：刪點",
          summary:
            "**原理講解**\n\n本節要解決的場景是：給你一棵二元樹，你想根據「整棵子樹的情況」來決定要不要刪掉某個節點。例如題目 814「二叉樹剪枝」，要把所有「裡面完全沒有 1」的子樹整片砍掉；或 1325「刪除給定值的葉子節點」，刪掉一個葉子之後它的父親可能也變成葉子、也要被刪。\n\n關鍵難點是：一個節點該不該刪，往往**取決於它底下的小孩處理完之後的結果**，而不是只看它自己。葉子是否被刪 → 影響父親是否變葉子 → 再影響祖父……這是一條「由下往上傳遞」的連鎖。\n\n:::example 自底向上的骨架：先處理小孩，再決定自己\n這正是「自底向上 DFS（深度優先搜尋）」的拿手好戲。所謂自底向上，就是**先把左右子樹遞迴處理完、拿到它們回傳的結果，再回頭處理自己**。每個節點對外回傳一個值，代表「我這棵子樹整理後變成什麼樣」（通常是整理後的子樹根，沒了就回 `nullptr`）。\n\n```cpp\n// 自底向上的骨架：先處理小孩，再決定自己\nTreeNode* dfs(TreeNode* node) {\n  if (!node) {\n    return nullptr;  // 空樹：沒東西可整理\n  }\n  node->left = dfs(node->left);    // 先把左子樹整理好，接回來\n  node->right = dfs(node->right);  // 先把右子樹整理好，接回來\n  // 到這裡，左右小孩都「已經是最終結果」了，才輪到判斷自己\n  return node;\n}\n```\n:::\n\n\n\n:::example 刪一個節點 = 父親把指向它的指標改掉\n「刪點」用這個骨架特別自然：刪掉一個節點，只要讓它的父親把對應指標接成 `nullptr`（或接成「孩子整理後的新根」）即可。因為小孩先處理完，父親拿到的永遠是最新狀態，連鎖刪除就自動完成了。\n\n```cpp\n// 刪一個節點 = 父親把指向它的指標改掉\nnode->left = dfs(node->left);  // 若 dfs 回傳 nullptr，等於把左孩子刪了\n```\n:::\n\n\n\n整棵樹每個節點只被走訪一次，時間複雜度 `O(n)`，`n` 是節點數。\n\n**實作方式**\n\n把刪點題拆成三步，逐一搭配程式碼：\n\n:::example 回傳值的語意：整理後這棵子樹的新根（nullptr 代表整片沒了）\n**1. 想清楚每個節點要「回傳什麼」。** 通常回傳「整理後的子樹根」：保留就回自己，刪掉就回 `nullptr`（或回某個替代孩子）。這個回傳值就是父親接回去的東西。\n\n```cpp\n// 回傳值的語意：整理後這棵子樹的新根（nullptr 代表整片沒了）\nTreeNode* dfs(TreeNode* node);\n```\n:::\n\n\n\n:::example 814 二叉樹剪枝：砍掉不含 1 的子樹\n**2. 先遞迴左右、把結果接回來，再判斷自己。** 順序千萬不能反——一定要等小孩變成最終狀態，自己的判斷才正確。\n\n```cpp\n// 814 二叉樹剪枝：砍掉不含 1 的子樹\nTreeNode* prune(TreeNode* node) {\n  if (!node) {\n    return nullptr;\n  }\n  node->left = prune(node->left);    // 左子樹剪完\n  node->right = prune(node->right);  // 右子樹剪完\n  // 自己是 0、且左右都被剪光了 → 整片砍掉\n  if (node->val == 0 && !node->left && !node->right) {\n    return nullptr;\n  }\n  return node;  // 否則保留\n}\n```\n:::\n\n\n\n:::example 1325 刪除值為 target 的葉子（可能連鎖）\n**3. 處理「刪除引發連鎖」的題目。** 像 1325，刪葉子後父親可能變新葉子。因為是自底向上，孩子先被刪、父親再判斷，連鎖自然發生，不必額外迴圈。\n\n```cpp\n// 1325 刪除值為 target 的葉子（可能連鎖）\nTreeNode* removeLeaf(TreeNode* node, int target) {\n  if (!node) {\n    return nullptr;\n  }\n  node->left = removeLeaf(node->left, target);\n  node->right = removeLeaf(node->right, target);\n  if (!node->left && !node->right && node->val == target) {\n    return nullptr;  // 現在是葉子且值符合 → 刪\n  }\n  return node;\n}\n```\n:::\n\n\n\n**邊界與複雜度：**\n\n- 別忘了根節點本身也可能被刪，所以最外層要用 `root = dfs(root)` 接回傳值。\n- 遞迴深度等於樹高，極端歪斜的樹可能逼近 `O(n)`、有爆 stack 風險。\n- 整體時間 `O(n)`、額外空間 `O(h)`（`h` 為樹高，遞迴堆疊用掉的）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「自底向上 DFS 刪點」想：\n\n- **某個節點刪不刪，取決於它整棵子樹的狀態**（子樹有沒有 1、是不是全空、葉子值是多少），而不是只看節點自己。\n- **刪除會往上連鎖**：刪了葉子，父親可能變新葉子也要刪——例如 1325。普通「掃一遍刪葉子」做不到連鎖，得反覆掃很多次，很慢。\n- **題目要回傳「整理後的樹」**：返回值是樹根或森林（如 1110 刪點成林，刪掉某些點後把斷開的子樹收集成多棵樹）。\n- 暴力做法常是「每刪一個就重掃整棵樹看誰又該刪」，`O(n^2)` 起跳；自底向上一次 DFS 就 `O(n)` 解決。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS「回傳值」代表什麼？（通常是整理後的子樹根，`nullptr` 表示刪光）\n2. 我有沒有「先遞迴左右、接回結果」，再判斷自己？順序對嗎？\n3. 「刪一個節點」這件事，是不是就等於「讓父親把指標接成 `nullptr` 或新根」？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 814 | 二叉樹剪枝 | N/A | 放在「自底向上 DFS：刪點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1110 | [刪點成林](https://leetcode.cn/problems/delete-nodes-and-return-forest/) | 1511 | 放在「自底向上 DFS：刪點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1325 | [刪除給定值的葉子節點](https://leetcode.cn/problems/delete-leaves-with-a-given-value/) | 1407 | 放在「自底向上 DFS：刪點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 814 | [二叉樹剪枝](https://leetcode.cn/problems//binary-tree-pruning/) |  |\n| 1110 | [刪點成林](https://leetcode.cn/problems//delete-nodes-and-return-forest/) | 1511 |\n| 1325 | [刪除給定值的葉子節點](https://leetcode.cn/problems//delete-leaves-with-a-given-value/) | 1407 |",
        },
        {
          id: 19,
          title: "2.5 有遞有歸",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在樹上走訪時，光靠「自底向上彙總子樹」還不夠，有些資訊必須**從上往下帶**，或是要在同一次 DFS 裡「去的時候做一件事、回來的時候再做另一件事」。這就是「有遞有歸」——「遞」是往下深入（進入子節點前），「歸」是往上返回（離開子節點後）。\n\n:::example 反中序：右 → 自己 → 左，會由大到小造訪 BST\n先看一個最能說明問題的例子：538 / 1038「把二元搜尋樹轉成累加樹」。要把每個節點的值改成「所有 ≥ 它的值之和」。二元搜尋樹的中序走訪是由小到大；若我們**反過來走（右 → 自己 → 左）**，就會由大到小遇到每個節點，於是維護一個「目前累加和」變數，邊走邊加即可。\n\n```cpp\n// 反中序：右 → 自己 → 左，會由大到小造訪 BST\nint sum = 0;  // 一路累加「比目前大的值之和」\nvoid dfs(TreeNode* node) {\n  if (!node) {\n    return;\n  }\n  dfs(node->right);  // 先走比自己大的那邊\n  sum += node->val;  // 「歸」的時機：此刻 sum 已含所有更大的值\n  node->val = sum;   // 改寫成累加值\n  dfs(node->left);   // 再走比自己小的那邊\n}\n```\n:::\n\n\n\n:::example 一個 DFS 同時有「遞」與「歸」的動作\n重點在於：`sum` 是一個**沿途攜帶、隨走訪推進的狀態**。「遞」與「歸」之間插入動作的時機決定了答案。把動作寫在遞迴呼叫「之前」是下行時做（自頂向下攜帶資訊）；寫在「之後」是上行時做（自底向上彙總）。同一個函式裡兩者都可以有。\n\n```cpp\n// 一個 DFS 同時有「遞」與「歸」的動作\nvoid dfs(TreeNode* node, int depth) {\n  if (!node) {\n    return;\n  }\n  enter(node, depth);  // 遞：下行時帶著 depth 等祖先資訊\n  dfs(node->left, depth + 1);\n  dfs(node->right, depth + 1);\n  leave(node);  // 歸：上行時彙總子樹結果\n}\n```\n:::\n\n\n\n每個節點進出各一次，時間 `O(n)`。\n\n**實作方式**\n\n把「有遞有歸」的題目拆成三步：\n\n:::example 往下帶用參數，往上回用回傳值\n**1. 狀態表示：分清楚哪些資訊要「往下帶」、哪些要「往上回」。** 往下帶的（如深度、路徑前綴、限制範圍）當成 DFS 參數；往上回的（如子樹高度、是否合法）當成回傳值或全域變數。\n\n```cpp\n// 往下帶用參數，往上回用回傳值\nReturnInfo dfs(TreeNode* node, DownInfo carry);\n```\n:::\n\n\n\n:::example 1080 根到葉路徑上的不足節點：往下帶「剩餘 limit」，往上回「是否被刪光」\n**2. 更新流程：選對「插入動作的時機」。** 下行才知道的事，寫在遞迴呼叫前；要等小孩算完才知道的事，寫在呼叫後。\n\n```cpp\n// 1080 根到葉路徑上的不足節點：往下帶「剩餘 limit」，往上回「是否被刪光」\nTreeNode* dfs(TreeNode* node, int limit) {\n  if (!node) {\n    return nullptr;\n  }\n  if (!node->left && !node->right) {  // 葉子：路徑和不足就刪\n    return node->val < limit ? nullptr : node;\n  }\n  node->left = dfs(node->left, limit - node->val);  // 遞：扣掉自己的值往下帶\n  node->right = dfs(node->right, limit - node->val);\n  return (!node->left && !node->right) ? nullptr\n                                       : node;  // 歸：兩邊都沒了就刪自己\n}\n```\n:::\n\n\n\n:::example 865：歸的時候同時回傳「深度」與「答案候選」\n**3. 答案讀取：依題目落點不同。** 可能在根的回傳值、全域變數，或在「歸」的時刻順手記錄。865「含所有最深節點的最小子樹」就是在「歸」時回傳 `{子樹最大深度, 該答案候選}` 一路往上合併。\n\n```cpp\n// 865：歸的時候同時回傳「深度」與「答案候選」\npair<int, TreeNode*> dfs(TreeNode* node) {\n  if (!node) {\n    return {0, nullptr};\n  }\n  auto [ld, ln] = dfs(node->left);\n  auto [rd, rn] = dfs(node->right);\n  if (ld == rd) {\n    return {ld + 1, node};  // 左右一樣深 → 自己是 LCA\n  }\n  return ld > rd ? pair{ld + 1, ln} : pair{rd + 1, rn};\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 想清楚每個變數是「下行有效」還是「全程有效」；全域變數記得在多筆測資間重置。時間 `O(n)`、空間 `O(h)`（樹高）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「有遞有歸」想：\n\n- **需要沿路攜帶祖先資訊**（深度、路徑前綴和、目前到根的限制值）——這要靠 DFS 參數一路往下帶。\n- **答案要同時用到「上面帶下來的」和「下面回上來的」兩種資訊**：例如 1080 既要往下帶 limit，又要往上回報是否被刪空。\n- **走訪順序本身就是解法**：像 538 / 1038 改成反中序，由大到小造訪，邊走邊累加。\n- 暴力做法常是「對每個節點都重新往上爬一次祖先、或重新算一次子樹」，造成 `O(n^2)`；一次有遞有歸的 DFS 把它壓成 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 哪些資訊要「往下帶」（放參數）、哪些要「往上回」（放回傳值或全域）？\n2. 我的動作該寫在遞迴呼叫「之前（遞）」還是「之後（歸）」？\n3. 換個走訪順序（例如反中序）會不會讓問題瞬間變簡單？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 538 | 把二叉搜索樹轉換為累加樹 | N/A | 放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 865 | [具有所有最深節點的最小子樹](https://leetcode.cn/problems/smallest-subtree-with-all-the-deepest-nodes/) | 1534 | 放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1038 | [從二叉搜索樹到更大和樹](https://leetcode.cn/problems/binary-search-tree-to-greater-sum-tree/) | 1375 | 放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1080 | [根到葉路徑上的不足節點](https://leetcode.cn/problems/insufficient-nodes-in-root-to-leaf-paths/) | 1805 | 放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 538 | [把二叉搜索樹轉換為累加樹](https://leetcode.cn/problems//convert-bst-to-greater-tree/) |  |\n| 865 | [具有所有最深節點的最小子樹](https://leetcode.cn/problems//smallest-subtree-with-all-the-deepest-nodes/) | 1534 |\n| 1038 | [從二叉搜索樹到更大和樹](https://leetcode.cn/problems//binary-search-tree-to-greater-sum-tree/) | 1375 |\n| 1080 | [根到葉路徑上的不足節點](https://leetcode.cn/problems//insufficient-nodes-in-root-to-leaf-paths/) | 1805 |",
        },
        {
          id: 20,
          title: "2.6 二叉樹的直徑",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在一棵樹上找「一條穿過某個節點、左右各往下延伸的最長（或最大）路徑」。最經典的就是 543「二元樹的直徑」——直徑是任意兩節點間最長路徑的邊數；以及 124「二元樹中的最大路徑和」——找一條路徑使節點值總和最大。\n\n:::example 以 node 為轉折點的路徑長 = 左臂 + 右臂\n關鍵觀察：任何一條這樣的路徑，一定有一個**最高點（轉折點）**。以這個最高點來看，路徑就是「從它往左子樹一路向下」接上「從它往右子樹一路向下」。所以只要對**每個節點**都算一次「以它為轉折點時，左臂 + 右臂能有多長」，取全部的最大值，就是答案。\n\n```cpp\n// 以 node 為轉折點的路徑長 = 左臂 + 右臂\n// 左臂 = 左子樹往下能延伸的最長長度；右臂同理\nint through = leftArm + rightArm;\n```\n:::\n\n\n\n:::example 核心二分：回傳給父親的是「單邊臂長」，但更新答案時用「左+右」\n難點在於：每個節點需要知道「我往下單邊能延伸多長」，而這要靠子樹算出來。於是用自底向上 DFS：DFS 回傳的是「**單邊**往下的最長延伸」（只能選左或右一條，因為回傳值是要給父親接著往上用的，不能左右都要），但在 DFS 內部、計算答案時，可以「左右都要」更新全域最大值。\n\n```cpp\n// 核心二分：回傳給父親的是「單邊臂長」，但更新答案時用「左+右」\nint dfs(TreeNode* node) {\n  if (!node) {\n    return 0;\n  }\n  int L = dfs(node->left);   // 左子樹單邊最長\n  int R = dfs(node->right);  // 右子樹單邊最長\n  ans = max(ans, L + R);     // 答案：以 node 轉折，左右都用上\n  return max(L, R) + 1;      // 回傳：給父親只能選一條臂 + 自己\n}\n```\n:::\n\n\n\n這個「回傳單邊、更新雙邊」的不對稱，是整個技巧的靈魂，務必分清楚。\n\n**實作方式**\n\n把直徑類問題拆成三步：\n\n:::example 兩個量分開：一個回傳、一個全域累計\n**1. 想清楚 DFS「回傳值」與「答案」的差別。** 回傳值＝單邊向下延伸（給父親用）；答案＝在某節點左右臂合起來的最大值（用全域變數記錄，不回傳）。\n\n```cpp\n// 兩個量分開：一個回傳、一個全域累計\nint ans = 0;              // 全域答案：最佳的「左臂 + 右臂」\nint dfs(TreeNode* node);  // 回傳：單邊臂長\n```\n:::\n\n\n\n:::example 687 最長同值路徑：值相同才能把臂接上來\n**2. 在「歸」的時候同時做兩件事：更新答案、回傳單邊。** 543 求邊數，臂長就是子樹高度；687「最長同值路徑」則多一個條件：只有子值等於自己時臂才能接。\n\n```cpp\n// 687 最長同值路徑：值相同才能把臂接上來\nint dfs(TreeNode* node) {\n  if (!node) {\n    return 0;\n  }\n  int L = dfs(node->left), R = dfs(node->right);\n  int la = (node->left && node->left->val == node->val) ? L + 1 : 0;\n  int ra = (node->right && node->right->val == node->val) ? R + 1 : 0;\n  ans = max(ans, la + ra);  // 左右同值臂合併\n  return max(la, ra);       // 回傳單邊\n}\n```\n:::\n\n\n\n:::example 124 最大路徑和：負臂寧可捨棄（與 0 取大）\n**3. 處理「值可能為負」的變形。** 124 的路徑和裡，若某條臂是負的，不如不要，所以延伸時和 0 取大。\n\n```cpp\n// 124 最大路徑和：負臂寧可捨棄（與 0 取大）\nint dfs(TreeNode* node) {\n  if (!node) {\n    return 0;\n  }\n  int L = max(0, dfs(node->left));  // 負的就當 0，不接\n  int R = max(0, dfs(node->right));\n  ans = max(ans, node->val + L + R);  // 以 node 為頂的整條路徑和\n  return node->val + max(L, R);       // 回傳：自己 + 較大的單邊\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 答案初值要設安全值（求和題用很小的負數，避免全負樹算錯）；分清楚「邊數」與「節點數」（直徑常以邊數計，臂長相加即是）。時間 `O(n)`、空間 `O(h)`（樹高）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「樹的直徑 / 轉折點路徑」想：\n\n- **求一條路徑的最長 / 最大，且路徑可以「拐彎」**：從某節點往左下又往右下，不必經過根——這就是要找每個節點當轉折點。\n- **答案是「左邊貢獻 + 右邊貢獻」，但往上傳遞只能給一邊**：543、124、687 全是這個結構。\n- **題目雖然字面不叫直徑，本質卻一樣**：例如 2385「感染二元樹需要的總時間」，把樹從某點當作起點，所求其實是「離起點最遠的距離」，也用同樣的「左右臂 + 上傳單邊」框架（需先建父指標或換根）。\n- 暴力做法是「對每個節點都重新往下量左右最長」，重複計算成 `O(n^2)`；一次 DFS 把臂長記下來就 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS「回傳值」是不是「單邊臂長」（給父親接）？\n2. 我更新答案時，是不是用「左臂 + 右臂」（在轉折點合併）？\n3. 臂能不能接上來，有沒有額外條件（同值？非負？）需要判斷？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 124 | 二叉樹中的最大路徑和 | N/A | 放在「二叉樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 543 | 二叉樹的直徑 | N/A | 放在「二叉樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 549 | 二叉樹最長連續序列 II（Premium） | N/A | 放在「二叉樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 687 | 最長同值路徑 | N/A | 放在「二叉樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2385 | [感染二叉樹需要的總時間](https://leetcode.cn/problems/amount-of-time-for-binary-tree-to-be-infected/) | 1711 | 放在「二叉樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 124 | [二叉樹中的最大路徑和](https://leetcode.cn/problems//binary-tree-maximum-path-sum/) |  |\n| 543 | [二叉樹的直徑](https://leetcode.cn/problems//diameter-of-binary-tree/) |  |\n| 549 | [二叉樹最長連續序列 II](https://leetcode.cn/problems//binary-tree-longest-consecutive-sequence-ii/) |  |\n| 687 | [最長同值路徑](https://leetcode.cn/problems//longest-univalue-path/) |  |\n| 2385 | [感染二叉樹需要的總時間](https://leetcode.cn/problems//amount-of-time-for-binary-tree-to-be-infected/) | 1711 |",
        },
        {
          id: 21,
          title: "2.7 回溯",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在樹上「枚舉所有從根往下的路徑」，並在過程中收集或統計符合條件的路徑。例如 257「二元樹的所有路徑」要列出每條根到葉的路徑；113「路徑總和 II」要找出所有和等於目標的根到葉路徑。\n\n這類問題的共通點：**答案是一條條路徑**，而路徑會隨著走訪「邊走邊長、回頭邊縮」。我們需要一個容器（通常叫 `path`）記錄「目前從根走到這裡經過了哪些節點」。這正是「回溯（backtracking）」：\n\n> 進入一個節點前「做選擇」（把它加進 path），遞迴探索它的子樹，**離開時「撤銷選擇」（把它從 path 移除）**，讓 path 回到進來之前的樣子。\n\n:::example 回溯三部曲：選擇 → 遞迴 → 撤銷\n```cpp\n// 回溯三部曲：選擇 → 遞迴 → 撤銷\npath.push_back(node->val);  // 1) 做選擇：把自己加入路徑\ndfs(node->left);            // 2) 遞迴探索\ndfs(node->right);\npath.pop_back();  // 3) 撤銷選擇：離開時還原，不污染兄弟分支\n```\n:::\n\n\n\n:::example 不變式：任何時刻 path 就是「根 → 當前節點」這條路徑\n為什麼一定要撤銷？因為左右子樹、以及更上層的其他分支共用同一個 `path`。若不撤銷，走完左子樹後 path 還殘留左邊的節點，再去走右子樹就會把不相干的節點混進來。撤銷的動作保證「每進入一個分支時，path 都恰好是從根到當前位置的正確狀態」——這就是回溯要維持的**不變式**。\n\n```cpp\n// 不變式：任何時刻 path 就是「根 → 當前節點」這條路徑\n// push 後成立，pop 後也回到呼叫前成立\n```\n:::\n\n\n\n樹上回溯每個節點進出各一次，框架本身是 `O(n)`；若每到葉子要複製整條路徑收進答案，總成本和「路徑數 × 路徑長」有關。\n\n**實作方式**\n\n把樹上回溯題拆成三步：\n\n:::example 狀態：path 記路徑；remain 記離目標還差多少\n**1. 狀態表示：用一個 `path` 記錄目前路徑，需要時再加輔助量。** 求和題可額外帶「剩餘目標」`remain`，省得每次重算。\n\n```cpp\n// 狀態：path 記路徑；remain 記離目標還差多少\nvector<int> path;\n// dfs(node, remain)：remain 是 target 扣掉沿途值後剩餘\n```\n:::\n\n\n\n:::example 113 路徑總和 II：收集所有「根到葉、和為 target」的路徑\n**2. 更新流程：標準三部曲，撤銷一定要對稱。** push 幾個就 pop 幾個，順序相反。\n\n```cpp\n// 113 路徑總和 II：收集所有「根到葉、和為 target」的路徑\nvoid dfs(TreeNode* node, int remain) {\n  if (!node) {\n    return;\n  }\n  path.push_back(node->val);\n  remain -= node->val;\n  if (!node->left && !node->right && remain == 0) {\n    ans.push_back(path);  // 抵達葉子且剛好用完 → 收一條\n  }\n  dfs(node->left, remain);\n  dfs(node->right, remain);\n  path.pop_back();  // 撤銷，還原 path\n}\n```\n:::\n\n\n\n:::example 257：收集字串形式的路徑（同樣在葉子處收）\n**3. 答案讀取：到合法位置就「複製」一份收進答案。** 注意收的是 `path` 的**拷貝**（`ans.push_back(path)`），不能存指標／參考，否則後續 pop 會把它改掉。\n\n```cpp\n// 257：收集字串形式的路徑（同樣在葉子處收）\nif (!node->left && !node->right) {\n  ans.push_back(cur);  // cur 為當前拼好的字串\n}\n```\n:::\n\n\n\n**邊界與複雜度：**\n\n- 437「路徑總和 III」允許路徑「不必從根開始」，純回溯會 `O(n^2)`；常改用「前綴和 + 雜湊表」邊走邊回溯計數，降到 `O(n)`。\n- 1457「偽迴文路徑」收集的是「各數字出現次數的奇偶」，回溯時用一個位元遮罩翻轉，葉子處檢查最多一個奇數。\n- 空樹、單邊樹、目標為負都要能正確處理；時間視路徑數而定，空間 `O(h)`（path 與遞迴深度）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「樹上回溯」想：\n\n- **要枚舉 / 收集「一條條路徑」**（根到葉、或任意起訖），而不是只回傳單一數值。\n- **沿途需要記錄「目前走過的節點序列」**，且走完一個分支要回到原狀去走另一個分支——這就是 push / pop 的撤銷。\n- **答案出現在「葉子」或某個合法終點**：抵達時把當前 path 收下來。\n- 暴力做法常是「對每個節點都重新從根找一遍路徑」或反覆重算路徑和，造成大量重複；回溯靠「邊走邊維護 path」一次走訪就枚舉完。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我維護的 `path` 不變式是什麼？（任何時刻它就是「根 → 當前節點」）\n2. 我每個 `push_back` 是否都有對稱的 `pop_back`？順序對嗎？\n3. 收答案時我存的是 path 的「拷貝」還是「參考」？（必須是拷貝）\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 113 | 路徑總和 II | N/A | 放在「回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 257 | 二叉樹的所有路徑 | N/A | 放在「回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 437 | 路徑總和 III | N/A | 放在「回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1457 | [二叉樹中的偽迴文路徑](https://leetcode.cn/problems/pseudo-palindromic-paths-in-a-binary-tree/) | 1405 | 放在「回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 113 | [路徑總和 II](https://leetcode.cn/problems//path-sum-ii/) |  |\n| 257 | [二叉樹的所有路徑](https://leetcode.cn/problems//binary-tree-paths/) |  |\n| 437 | [路徑總和 III](https://leetcode.cn/problems//path-sum-iii/) |  |\n| 1457 | [二叉樹中的偽迴文路徑](https://leetcode.cn/problems//pseudo-palindromic-paths-in-a-binary-tree/) | 1405 |",
        },
        {
          id: 22,
          title: "2.8 最近公共祖先",
          summary:
            "**原理講解**\n\n本節要解決的場景是：給樹上兩個節點 `p`、`q`，找它們的「最近公共祖先（Lowest Common Ancestor, LCA）」——也就是同時是 `p` 和 `q` 祖先、且**深度最深**的那個節點。代表題是 236「二元樹的最近公共祖先」、235「BST 的最近公共祖先」。\n\n先建立直覺：兩個節點的所有公共祖先排成一條從根往下的鏈，最近公共祖先就是這條鏈最底下那個。換句話說，LCA 是「`p` 與 `q` 第一次分道揚鑣的那個節點」——在它這裡，一個往左子樹、另一個往右子樹（或其中一個就是它本身）。\n\n:::example 236：回傳「這棵子樹裡找到的 LCA 候選」，沒找到回 nullptr\n對一般二元樹（236），用自底向上 DFS：每個節點回報「我這棵子樹裡有沒有見到 `p` 或 `q`」。\n\n```cpp\n// 236：回傳「這棵子樹裡找到的 LCA 候選」，沒找到回 nullptr\nTreeNode* dfs(TreeNode* node, TreeNode* p, TreeNode* q) {\n  if (!node || node == p || node == q) {\n    return node;  // 撞到目標或空就回報\n  }\n  TreeNode* L = dfs(node->left, p, q);\n  TreeNode* R = dfs(node->right, p, q);\n  if (L && R) {\n    return node;  // 左右各找到一個 → node 就是分岔點 = LCA\n  }\n  return L ? L : R;  // 否則把找到的那邊往上傳\n}\n```\n:::\n\n\n\n精妙之處：若左子樹回傳非空、右子樹也非空，代表 `p`、`q` 分別在兩側，當前節點就是它們的分岔點，也就是 LCA；若只有一側非空，把那一側往上傳（可能是 LCA，也可能只是其中一個目標，交給更上層判斷）。\n\n:::example 235 BST：靠值的大小決定往哪走，第一個「夾在中間」的就是 LCA\n對 BST（235）更簡單：利用「左小右大」的性質，從根往下，只要 `p`、`q` 都比目前小就往左、都比目前大就往右，**一旦目前節點落在兩者之間（或等於某一個），它就是 LCA**。\n\n```cpp\n// 235 BST：靠值的大小決定往哪走，第一個「夾在中間」的就是 LCA\nwhile (node) {\n  if (p->val < node->val && q->val < node->val) {\n    node = node->left;\n  } else if (p->val > node->val && q->val > node->val) {\n    node = node->right;\n  } else {\n    return node;  // p、q 分屬兩側或等於 node → LCA\n  }\n}\n```\n:::\n\n\n\n兩種做法都是 `O(n)`（235 更精確是 `O(h)`，`h` 為樹高）。\n\n**實作方式**\n\n把 LCA 題拆成三步：\n\n:::example 一般樹：回傳值的語意就是「這棵子樹貢獻的 LCA 候選」\n**1. 狀態表示：DFS 回傳「子樹裡找到的目標 / 候選」。** 一般樹用「回傳找到的節點，沒找到回 `nullptr`」；BST 則靠值比較直接定位，連回傳都省了。\n\n```cpp\n// 一般樹：回傳值的語意就是「這棵子樹貢獻的 LCA 候選」\nTreeNode* dfs(TreeNode* node, TreeNode* p, TreeNode* q);\n```\n:::\n\n\n\n:::example 合併規則（236 的核心兩行）\n**2. 更新流程：自底向上合併左右回報。** 左右都非空 → 自己是 LCA；否則把非空那邊上傳。這套邏輯對 236 直接適用。\n\n```cpp\n// 合併規則（236 的核心兩行）\nif (L && R) {\n  return node;  // 分岔點\n}\nreturn L ? L : R;  // 單側上傳\n```\n:::\n\n\n\n:::example 1123：歸時回傳 {子樹最大深度, 該深度的 LCA}\n**3. 處理變形題。** 1123「最深葉節點的最近公共祖先」需要同時回傳「子樹深度」與「該深度下的 LCA」，在「歸」時依左右深度合併（這其實和直徑類的雙回傳一脈相承）。1644 / 1650 是 Premium 變形：1644 要求 `p`、`q` 不一定都存在（需先確認兩者都在），1650 給了父指標（可從兩節點各自往上爬、用「兩指針相遇」找交點）。\n\n```cpp\n// 1123：歸時回傳 {子樹最大深度, 該深度的 LCA}\npair<int, TreeNode*> dfs(TreeNode* node) {\n  if (!node) {\n    return {0, nullptr};\n  }\n  auto [ld, ln] = dfs(node->left);\n  auto [rd, rn] = dfs(node->right);\n  if (ld == rd) {\n    return {ld + 1, node};  // 左右等深 → 自己是 LCA\n  }\n  return ld > rd ? pair{ld + 1, ln} : pair{rd + 1, rn};\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 注意「一個節點是否可為另一個的祖先」（題目通常允許 LCA 等於 `p` 或 `q` 本身）；1644 要先確認兩節點都存在才回傳。時間 `O(n)`（235 為 `O(h)`），空間 `O(h)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「最近公共祖先」想：\n\n- **問兩個（或多個）節點「最近的共同祖先」**，或等價的「兩條根到節點路徑第一次分岔的地方」。\n- **要找「兩個目標分屬左右子樹」的那個轉折節點**：236 的左右都非空即是。\n- **若是 BST**，立刻想到用「值落在 `p`、`q` 之間」一路往下定位（235），比一般樹更快。\n- 暴力做法是「分別求出 `p`、`q` 到根的兩條路徑，再比對最後一個共同節點」，需要額外存路徑；自底向上 DFS 一趟就能合併判斷，省下儲存。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 DFS 回傳值代表什麼？（「子樹裡找到的目標 / LCA 候選」）\n2. 「左右子樹都回報找到」時，是不是就代表當前節點是 LCA？\n3. 這是不是 BST？若是，能不能改用「值比較往下走」省成 `O(h)`？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 235 | 二叉搜索樹的最近公共祖先 | N/A | 放在「最近公共祖先」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 236 | 二叉樹的最近公共祖先 | N/A | 放在「最近公共祖先」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1123 | [最深葉節點的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-deepest-leaves/) | 1607 | 放在「最近公共祖先」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1644 | 二叉樹的最近公共祖先 II（Premium） | N/A | 放在「最近公共祖先」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1650 | 二叉樹的最近公共祖先 III（Premium） | N/A | 放在「最近公共祖先」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 235 | [二叉搜索樹的最近公共祖先](https://leetcode.cn/problems//lowest-common-ancestor-of-a-binary-search-tree/) |  |\n| 236 | [二叉樹的最近公共祖先](https://leetcode.cn/problems//lowest-common-ancestor-of-a-binary-tree/) |  |\n| 1123 | [最深葉節點的最近公共祖先](https://leetcode.cn/problems//lowest-common-ancestor-of-deepest-leaves/) | 1607 |\n| 1644 | [二叉樹的最近公共祖先 II](https://leetcode.cn/problems//lowest-common-ancestor-of-a-binary-tree-ii/) |  |\n| 1650 | [二叉樹的最近公共祖先 III](https://leetcode.cn/problems//lowest-common-ancestor-of-a-binary-tree-iii/) |  |\n| 1676 | [二叉樹的最近公共祖先 IV](https://leetcode.cn/problems//lowest-common-ancestor-of-a-binary-tree-iv/) |  |\n| 1740 | [找到二叉樹中的距離](https://leetcode.cn/problems//find-distance-in-a-binary-tree/) |  |\n| 2096 | [從二叉樹一個節點到另一個節點每一步的方向](https://leetcode.cn/problems//step-by-step-directions-from-a-binary-tree-node-to-another/) | 1805 |",
        },
        {
          id: 23,
          title: "2.9 二叉搜索樹",
          summary:
            "**原理講解**\n\n本節要解決的場景是：處理「二元搜尋樹（Binary Search Tree, BST）」。BST 是一種有特殊規則的二元樹——**每個節點，左子樹所有值都比它小，右子樹所有值都比它大**。這條規則看似簡單，卻帶來一個威力強大的性質：\n\n> 對 BST 做「中序走訪（左 → 自己 → 右）」，得到的值序列一定是**由小到大嚴格遞增**的。\n\n:::example BST 的中序走訪 → 值由小到大冒出來\n幾乎所有 BST 題目，都是在反覆利用「左小右大」或「中序＝升序」這兩件事。例如 98「驗證二元搜尋樹」要檢查一棵樹是不是合法 BST；230「BST 中第 K 小的元素」要找第 k 小；270「最接近的 BST 值」要找離目標最近的值。\n\n```cpp\n// BST 的中序走訪 → 值由小到大冒出來\nvoid inorder(TreeNode* node) {\n  if (!node) {\n    return;\n  }\n  inorder(node->left);  // 先把更小的都走完\n  visit(node->val);     // 此刻拿到的就是「下一個最小值」\n  inorder(node->right);\n}\n```\n:::\n\n\n\n:::example 98 驗證 BST：中序時若沒有嚴格遞增，就不是 BST\n有了「中序＝升序」，很多題目立刻變簡單：第 k 小，就是中序走訪數到第 k 個；驗證 BST，就是檢查中序序列是否嚴格遞增（只要比對「上一個訪問到的值」即可，不必存整個陣列）。\n\n```cpp\n// 98 驗證 BST：中序時若沒有嚴格遞增，就不是 BST\nlong prev = LONG_MIN;  // 上一個中序訪問到的值\nbool ok = true;\nvoid check(TreeNode* node) {\n  if (!node || !ok) {\n    return;\n  }\n  check(node->left);\n  if (node->val <= prev) {\n    ok = false;  // 沒比前一個大 → 違規\n  }\n  prev = node->val;\n  check(node->right);\n}\n```\n:::\n\n\n\n另一個常用招式是「往下定位」：要找某個值或最接近的值，從根出發，比目前小就往左、比目前大就往右，每一步排除半棵樹，`O(h)` 到位（`h` 為樹高）。\n\n**實作方式**\n\n把 BST 題拆成三步：\n\n:::example 兩條主路線\n**1. 先問：這題該用「中序升序」還是「往下定位」？** 要「按大小順序處理全部節點」（驗證、第 k 小、累加）→ 中序；要「找單一目標 / 最接近值」→ 往下定位。\n\n```cpp\n// 兩條主路線\n// (A) 中序走訪：按升序逐一處理節點\n// (B) 從根往下：比大小決定 left / right，O(h) 定位\n```\n:::\n\n\n\n:::example 230 第 K 小：中序計數，數到 k 就是答案\n**2. 中序路線：用「上一個值 prev」省去存陣列。** 230 數到第 k 個就停；98 檢查嚴格遞增；99「恢復 BST」找出中序裡兩個「逆序對」對應的被交換節點。\n\n```cpp\n// 230 第 K 小：中序計數，數到 k 就是答案\nint cnt = 0, ans = -1;\nvoid kth(TreeNode* node, int k) {\n  if (!node || ans != -1) {\n    return;\n  }\n  kth(node->left, k);\n  if (++cnt == k) {\n    ans = node->val;\n    return;\n  }  // 第 k 個冒出來的\n  kth(node->right, k);\n}\n```\n:::\n\n\n\n:::example 270 最接近的 BST 值：往下走，沿途更新最接近者\n**3. 定位路線：每步排除半棵樹。** 270 沿途記錄「目前最接近 target 的值」。\n\n```cpp\n// 270 最接近的 BST 值：往下走，沿途更新最接近者\nint closest(TreeNode* node, double target) {\n  int best = node->val;\n  while (node) {\n    if (abs(node->val - target) < abs(best - target)) {\n      best = node->val;\n    }\n    node = target < node->val ? node->left : node->right;  // 排除半棵\n  }\n  return best;\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 驗證 BST 要小心「只比父子不夠」，必須用「整體上下界」或「中序前一個值」來判斷（左子樹的每個值都要小於祖先，不只小於直接父親）；數值可能到 `INT_MIN/MAX`，prev 用更寬的型別。中序為 `O(n)`，定位為 `O(h)`，空間 `O(h)`。255「驗證前序序列」則用單調堆疊維護「下界」。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「二元搜尋樹」想：\n\n- **題目明說是 BST，或保證「左小右大」**——立刻聯想「中序＝升序」與「往下定位」兩大武器。\n- **要按值的大小順序處理**（第 k 小、累加和、驗證遞增、找逆序對）→ 中序走訪 + 維護「上一個值」。\n- **要找某個值、前驅 / 後繼、或最接近的值** → 從根往下、每步排除半棵樹，`O(h)` 解決。\n- 暴力做法常是「把所有節點值倒進陣列再排序」`O(n log n)`，或「對每個查詢都全樹掃描」；善用 BST 性質可降到 `O(n)`（中序）或 `O(h)`（定位），且常常不必額外開陣列。\n\n確認自己有沒有套對，問三個問題：\n\n1. 這棵樹保證是 BST 嗎？我能不能用上「中序＝升序」？\n2. 我該「按順序走全部節點（中序）」還是「往下定位單一目標（比大小）」？\n3. 若用中序，我是不是只要記「上一個值」就夠，不必存整個陣列？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 98 | 驗證二叉搜索樹 | N/A | 放在「二叉搜索樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 99 | 恢復二叉搜索樹 | N/A | 放在「二叉搜索樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 230 | 二叉搜索樹中第 K 小的元素 | N/A | 放在「二叉搜索樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 255 | 驗證二叉搜索樹的前序遍歷序列（Premium） | N/A | 放在「二叉搜索樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 270 | 最接近的二叉搜索樹值（Premium） | N/A | 放在「二叉搜索樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 98 | [驗證二叉搜索樹](https://leetcode.cn/problems//validate-binary-search-tree/) |  |\n| 99 | [恢復二叉搜索樹](https://leetcode.cn/problems//recover-binary-search-tree/) |  |\n| 230 | [二叉搜索樹中第 K 小的元素](https://leetcode.cn/problems//kth-smallest-element-in-a-bst/) |  |\n| 255 | [驗證二叉搜索樹的前序遍歷序列](https://leetcode.cn/problems//verify-preorder-sequence-in-binary-search-tree/) |  |\n| 270 | [最接近的二叉搜索樹值](https://leetcode.cn/problems//closest-binary-search-tree-value/) |  |\n| 272 | [最接近的二叉搜索樹值 II](https://leetcode.cn/problems//closest-binary-search-tree-value-ii/) |  |\n| 285 | [二叉搜索樹中的中序後繼](https://leetcode.cn/problems//inorder-successor-in-bst/) |  |\n| 501 | [二叉搜索樹中的眾數](https://leetcode.cn/problems//find-mode-in-binary-search-tree/) |  |\n| 510 | [二叉搜索樹中的中序後繼 II](https://leetcode.cn/problems//inorder-successor-in-bst-ii/) |  |\n| 530 | [二叉搜索樹的最小絕對差](https://leetcode.cn/problems//minimum-absolute-difference-in-bst/) |  |\n| 653 | [兩數之和 IV - 輸入二叉搜索樹](https://leetcode.cn/problems//two-sum-iv-input-is-a-bst/) |  |\n| 700 | [二叉搜索樹中的搜尋](https://leetcode.cn/problems//search-in-a-binary-search-tree/) |  |\n| 783 | [二叉搜索樹節點最小距離](https://leetcode.cn/problems//minimum-distance-between-bst-nodes/) |  |\n| 897 | [遞增順序搜尋樹](https://leetcode.cn/problems//increasing-order-search-tree/) | 1473 |\n| 938 | [二叉搜索樹的範圍和](https://leetcode.cn/problems//range-sum-of-bst/) | 1335 |\n| 1305 | [兩棵二叉搜索樹中的所有元素](https://leetcode.cn/problems//all-elements-in-two-binary-search-trees/) | 1260 |\n| 1373 | [二叉搜尋子樹的最大鍵值和](https://leetcode.cn/problems//maximum-sum-bst-in-binary-tree/) | 1914 |\n| 1902 | [給定二叉搜索樹的插入順序求深度](https://leetcode.cn/problems//depth-of-bst-given-insertion-order/) |  |\n| 1932 | [合併多棵二叉搜索樹](https://leetcode.cn/problems//merge-bsts-to-create-single-bst/) | 2484 |\n| 2476 | [二叉搜索樹最近節點查詢](https://leetcode.cn/problems//closest-nodes-queries-in-a-binary-search-tree/) | 1597 |",
        },
        {
          id: 24,
          title: "2.10 建立二叉樹",
          summary:
            "**原理講解**\n\n本節要解決的場景是：給你某種「描述樹的資料」，要你**把樹真正建出來**（產生一堆節點、接好左右指標）。代表題有 105「從前序與中序建二元樹」、106「從中序與後序建」、108「把有序陣列轉成 BST」、654「最大二元樹」。\n\n:::example 建樹通用骨架：定根 → 切左右 → 遞迴 → 接上\n所有建樹題的共同骨架是「**遞迴分治**」：先在資料裡找到「誰是這一段的根」，再把剩下的資料切成「左子樹的料」和「右子樹的料」，分別遞迴建好後，掛到根的左右指標上。難點只有一個——**怎麼從給定資料判斷出根、以及如何切分左右**。\n\n```cpp\n// 建樹通用骨架：定根 → 切左右 → 遞迴 → 接上\nTreeNode* build(/* 某段資料 */) {\n  if (/* 這段空了 */) {\n    return nullptr;  // 沒料 → 空子樹\n  }\n  TreeNode* root = new TreeNode(/* 找出的根值 */);\n  root->left = build(/* 左半段料 */);   // 遞迴建左子樹\n  root->right = build(/* 右半段料 */);  // 遞迴建右子樹\n  return root;\n}\n```\n:::\n\n\n\n不同題目的差別，只在「根怎麼找」「左右怎麼切」：\n\n- 105：**前序的第一個**就是根；拿這個根值去**中序**裡找位置，位置左邊是左子樹、右邊是右子樹（中序的特性：根把序列切成左右兩段）。\n- 106：**後序的最後一個**是根，其餘同理用中序切分。\n- 108：有序陣列**正中間**當根（這樣左右一樣多，樹才平衡），左半段、右半段遞迴。\n- 654：**區間裡的最大值**當根，最大值左邊、右邊各自遞迴。\n\n:::example 108：取中點當根，自然建出平衡 BST\n```cpp\n// 108：取中點當根，自然建出平衡 BST\nTreeNode* build(vector<int>& a, int lo, int hi) {\n  if (lo > hi) {\n    return nullptr;\n  }\n  int mid = (lo + hi) / 2;  // 中點當根 → 左右節點數最接近\n  TreeNode* root = new TreeNode(a[mid]);\n  root->left = build(a, lo, mid - 1);\n  root->right = build(a, mid + 1, hi);\n  return root;\n}\n```\n:::\n\n\n\n**實作方式**\n\n把建樹題拆成三步：\n\n:::example 105：前序首元素即為根值\n**1. 想清楚「根」藏在資料的哪裡。** 前序看最前、後序看最後、有序陣列看中間、最大樹看最大值。\n\n```cpp\n// 105：前序首元素即為根值\nint rootVal = preorder[preStart];\n```\n:::\n\n\n\n:::example 105：用 inorder 值→索引的表，O(1) 找到根在中序的位置\n**2. 用「邊界索引」遞迴，避免狂複製陣列。** 不要每層都切出新 vector（很慢又耗記憶體），改用 `[lo, hi]` 區間下標。105 / 106 還要先用一張雜湊表記「中序每個值在哪個位置」，才能 `O(1)` 切分。\n\n```cpp\n// 105：用 inorder 值→索引的表，O(1) 找到根在中序的位置\nunordered_map<int, int> pos;  // 預先：inorder[i] -> i\nTreeNode* build(int preL, int preR, int inL, int inR) {\n  if (preL > preR) {\n    return nullptr;\n  }\n  int rootVal = preorder[preL];\n  int k = pos[rootVal];    // 根在中序的位置\n  int leftSize = k - inL;  // 左子樹有幾個節點\n  TreeNode* root = new TreeNode(rootVal);\n  root->left = build(preL + 1, preL + leftSize, inL, k - 1);\n  root->right = build(preL + leftSize + 1, preR, k + 1, inR);\n  return root;\n}\n```\n:::\n\n\n\n:::example 654：區間最大值當根，左右遞迴\n**3. 算對左右兩段的邊界。** 最容易錯的就是 105 / 106 裡前序 / 後序那一段的切點：要用「左子樹節點數 `leftSize`」去推前序的分界（如上 `preL + leftSize`）。畫個小例子手動對一遍最保險。\n\n```cpp\n// 654：區間最大值當根，左右遞迴\nTreeNode* build(vector<int>& a, int lo, int hi) {\n  if (lo > hi) {\n    return nullptr;\n  }\n  int m = lo;\n  for (int i = lo + 1; i <= hi; ++i) {\n    if (a[i] > a[m]) {\n      m = i;  // 找最大值位置\n    }\n  }\n  TreeNode* root = new TreeNode(a[m]);\n  root->left = build(a, lo, m - 1);\n  root->right = build(a, m + 1, hi);\n  return root;\n}\n```\n:::\n\n\n\n**邊界與複雜度：** 空區間（`lo > hi`）一定回 `nullptr`；題目通常保證值不重複，否則「用值去中序找位置」會出錯。105 / 106 加了雜湊表後是 `O(n)`；108 是 `O(n)`；654 樸素找最大是 `O(n^2)`（可用單調堆疊優化）。空間 `O(h)` 遞迴 + 必要的索引表。536（從字串生成二元樹，Premium）則是先解析括號結構再遞迴建。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「建立二元樹」想：\n\n- **題目給的是「描述」而非現成的樹**：兩種走訪序列、有序陣列、含括號的字串、或一段數列要你「按某規則造樹」。\n- **能在資料裡定出「誰是根」並把剩餘切成左右兩塊**——這就是分治建樹的核心。\n- **想建出「平衡」或「有特定結構」的樹**：如 108 取中點保平衡、654 取最大值定根。\n- 暴力做法常是「每層都 substring / 複製子陣列」造成 `O(n^2)` 甚至更糟；改用邊界索引 + 雜湊表能壓到 `O(n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 在這段資料裡，「根」要怎麼一眼定出來（最前？最後？中間？最大？）？\n2. 定了根之後，左子樹的料和右子樹的料各是哪一段（邊界索引算對了嗎）？\n3. 我有沒有避免「每層複製陣列」，改用區間下標（必要時配雜湊表）？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 105 | 從前序與中序遍歷序列構造二叉樹 | N/A | 放在「建立二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 106 | 從中序與後序遍歷序列構造二叉樹 | N/A | 放在「建立二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 108 | 將有序陣列轉換為二叉搜索樹 | N/A | 放在「建立二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 536 | 從字串生成二叉樹（Premium） | N/A | 放在「建立二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 654 | 最大二叉樹 | N/A | 放在「建立二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 105 | [從前序與中序遍歷序列構造二叉樹](https://leetcode.cn/problems//construct-binary-tree-from-preorder-and-inorder-traversal/) |  |\n| 106 | [從中序與後序遍歷序列構造二叉樹](https://leetcode.cn/problems//construct-binary-tree-from-inorder-and-postorder-traversal/) |  |\n| 108 | [將有序陣列轉換為二叉搜索樹](https://leetcode.cn/problems//convert-sorted-array-to-binary-search-tree/) |  |\n| 536 | [從字串生成二叉樹](https://leetcode.cn/problems//construct-binary-tree-from-string/) |  |\n| 654 | [最大二叉樹](https://leetcode.cn/problems//maximum-binary-tree/) |  |\n| 889 | [根據前序和後序遍歷構造二叉樹](https://leetcode.cn/problems//construct-binary-tree-from-preorder-and-postorder-traversal/) | 1732 |\n| 998 | [最大二叉樹 II](https://leetcode.cn/problems//maximum-binary-tree-ii/) | 1498 |\n| 1008 | [前序遍歷構造二叉搜索樹](https://leetcode.cn/problems//construct-binary-search-tree-from-preorder-traversal/) | 1563 |\n| 1028 | [從先序遍歷還原二叉樹](https://leetcode.cn/problems//recover-a-tree-from-preorder-traversal/) | 1797 |\n| 1382 | [將二叉搜索樹變平衡](https://leetcode.cn/problems//balance-a-binary-search-tree/) | 1541 |\n| 1597 | [根據中綴表示式構造二叉表示式樹](https://leetcode.cn/problems//build-binary-expression-tree-from-infix-expression/) |  |\n| 1628 | [設計帶解析函式的表示式樹](https://leetcode.cn/problems//design-an-expression-tree-with-evaluate-function/) |  |\n| 2196 | [根據描述建立二叉樹](https://leetcode.cn/problems//create-binary-tree-from-descriptions/) | 1644 |",
        },
        {
          id: 25,
          title: "2.11 插入/刪除節點",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在「二叉搜尋樹（BST）」上插入或刪除一個節點，而且改完之後它還要繼續是一棵合法的 BST。\n\n:::example BST 的查找：比目標小就往右，比目標大就往左\n先複習 BST 是什麼。BST 是一棵二叉樹，它有一條鐵律：**任何節點的左子樹全部比它小，右子樹全部比它大**。這條規律有個好處——找一個值時，只要比較大小就知道往左走還是往右走，平均只要 `O(log n)` 步。\n\n```cpp\n// BST 的查找：比目標小就往右，比目標大就往左\nTreeNode* find(TreeNode* root, int target) {\n  while (root && root->val != target) {\n    root = target < root->val ? root->left : root->right;\n  }\n  return root;  // 找到就回傳該節點，找不到回傳 nullptr\n}\n```\n:::\n\n\n\n:::example 插入：照大小往下走，空位就掛上新節點\n插入很單純：照上面的規則一路往下走，走到底（遇到空位）就把新節點掛在那裡，這個位置一定不會破壞 BST 規律。\n\n```cpp\n// 插入：照大小往下走，空位就掛上新節點\nTreeNode* insert(TreeNode* root, int val) {\n  if (!root) {\n    return new TreeNode(val);  // 找到空位\n  }\n  if (val < root->val) {\n    root->left = insert(root->left, val);\n  } else {\n    root->right = insert(root->right, val);\n  }\n  return root;  // 回傳本層的根，串回上層\n}\n```\n:::\n\n\n\n刪除比較麻煩。如果被刪節點有兩個小孩，不能直接拔掉，否則樹會斷成兩半。解法是找一個「替身」來頂替它：右子樹裡最小的那個節點（它剛好比左子樹全大、又比右子樹其餘全小），把替身的值搬上來，再去右子樹刪掉那個替身即可。Leetcode 450（刪除 BST 中的節點）就是這個。\n\n**實作方式**\n\n這類題目可拆成四個固定步驟，逐一搭配程式碼：\n\n:::example 統一介面：每個遞迴回傳「修改後子樹的根」\n**1. 狀態表示：用「回傳新的子樹根」串接修改。** 不要直接去改父節點的指標，而是讓每個遞迴函式回傳「處理後這棵子樹的新根」，父層用 `root->left = f(root->left)` 接回去。這樣插入、刪除、修剪都能統一寫法。\n\n```cpp\n// 統一介面：每個遞迴回傳「修改後子樹的根」\n// root->left  = modify(root->left);\n// root->right = modify(root->right);\n```\n:::\n\n\n\n**2. 插入流程：往下找空位。** 如上面的 `insert`，遞迴到 `nullptr` 就 new 一個節點接上（Leetcode 701）。\n\n:::example 刪除：核心是「兩個小孩」用右子樹最小值頂替\n**3. 刪除流程：分三種情況。** 沒有小孩 → 直接刪；只有一個小孩 → 用那個小孩頂替；有兩個小孩 → 用右子樹最小值當替身。\n\n```cpp\n// 刪除：核心是「兩個小孩」用右子樹最小值頂替\nTreeNode* del(TreeNode* root, int key) {\n  if (!root) {\n    return nullptr;\n  }\n  if (key < root->val) {\n    root->left = del(root->left, key);\n  } else if (key > root->val) {\n    root->right = del(root->right, key);\n  } else {\n    if (!root->left) {\n      return root->right;  // 0 或 1 個小孩\n    }\n    if (!root->right) {\n      return root->left;\n    }\n    TreeNode* s = root->right;  // 找右子樹最小值\n    while (s->left) {\n      s = s->left;\n    }\n    root->val = s->val;                      // 搬替身的值上來\n    root->right = del(root->right, s->val);  // 再刪掉替身\n  }\n  return root;\n}\n```\n:::\n\n\n\n**4. 修剪與邊界。** 修剪 BST（Leetcode 669）是把超出 `[low, high]` 的節點整段砍掉：若節點太小，整個左子樹都太小，直接回傳處理後的右子樹；太大則對稱處理。複雜度都是樹高級別，平均 `O(log n)`、最壞 `O(n)`（退化成鏈）。注意遞迴深度過大可能爆 stack。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「BST 插入/刪除」想：\n\n- **題目明說是二叉搜尋樹，要求加入、移除、修剪某個值或某個範圍**，而且改完還要維持 BST 性質。Leetcode 450、669、701 都是。\n- **你發現「比大小決定左右走向」能省下大量比較**——這正是 BST 存在的理由：\n\n:::example 嗅到味道的瞬間：能用「大小」直接決定方向，不必整棵掃\n```cpp\n// 嗅到味道的瞬間：能用「大小」直接決定方向，不必整棵掃\n// if (val < root->val) 往左; else 往右;   // 每層砍掉一半\n```\n:::\n\n\n\n- **刪除有兩個小孩讓你卡住**，這就是要用「右子樹最小值頂替」的訊號。\n- **把 BST 拆開或合併**（如 Leetcode 776 拆分、1666 改變根節點），本質還是局部改幾條指標、其餘子樹整段搬移。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每個遞迴函式回傳的是什麼？（應該是「修改後子樹的根」）\n2. 刪除有兩個小孩時，我的替身是誰？\n3. 我有沒有靠「比大小」決定往左還往右，而不是整棵樹掃一遍？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 450 | 刪除二叉搜索樹中的節點 | N/A | 放在「插入/刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 669 | 修剪二叉搜索樹 | N/A | 放在「插入/刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 701 | 二叉搜索樹中的插入操作 | N/A | 放在「插入/刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 776 | 拆分二叉搜索樹（Premium） | N/A | 放在「插入/刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1666 | 改變二叉樹的根節點（Premium） | N/A | 放在「插入/刪除節點」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 450 | [刪除二叉搜索樹中的節點](https://leetcode.cn/problems//delete-node-in-a-bst/) |  |\n| 669 | [修剪二叉搜索樹](https://leetcode.cn/problems//trim-a-binary-search-tree/) |  |\n| 701 | [二叉搜索樹中的插入操作](https://leetcode.cn/problems//insert-into-a-binary-search-tree/) |  |\n| 776 | [拆分二叉搜索樹](https://leetcode.cn/problems//split-bst/) |  |\n| 1666 | [改變二叉樹的根節點](https://leetcode.cn/problems//change-the-root-of-a-binary-tree/) |  |",
        },
        {
          id: 26,
          title: "2.12 樹形 DP",
          summary:
            "**原理講解**\n\n本節要解決的場景是：答案跟「整棵樹」有關，而每個節點要做的決策（選或不選、放或不放）又會影響它的小孩。這類問題用「樹形 DP（樹上動態規劃）」來解。\n\n先說 DP 是什麼。DP 就是「把一個大問題拆成小問題，算過的小答案先記起來，之後直接拿來用，不重算」。在樹上，小問題天然就是「子樹」：一棵子樹的答案，可以由它兩個小孩子樹的答案拼出來。\n\n:::example 每個節點回報兩種狀態：{偷自己, 不偷自己} 的子樹最大值\n關鍵想法：**每個節點向上回報「以我為根的子樹」算出來的結果，而且通常要回報好幾種狀態**。例如「打家劫舍 III」（Leetcode 337）裡，房子排成樹，相鄰不能同時偷。對每個節點，我們回報兩個數字：偷這個節點時子樹能拿的最大金額、不偷時的最大金額。\n\n```cpp\n// 每個節點回報兩種狀態：{偷自己, 不偷自己} 的子樹最大值\nstruct R {\n  int rob;\n  int skip;\n};  // rob：偷此節點；skip：不偷此節點\n```\n:::\n\n\n\n:::example 合併小孩：偷自己=自己的錢+兩個小孩都不偷；不偷自己=小孩各取較大者\n為什麼要回報兩種？因為「父節點要不要偷」取決於「小孩有沒有被偷」。只回報一個總和不夠，必須把「自己被選 / 沒被選」分開記，父層才能正確接續。\n\n```cpp\n// 合併小孩：偷自己=自己的錢+兩個小孩都不偷；不偷自己=小孩各取較大者\nR merge(int val, R l, R r) {\n  int rob = val + l.skip + r.skip;\n  int skip = max(l.rob, l.skip) + max(r.rob, r.skip);\n  return {rob, skip};\n}\n```\n:::\n\n\n\n走訪順序一定是「後序」——先把兩個小孩算完，才能算自己，因為自己依賴小孩的結果。最後在根節點取 `max(rob, skip)` 就是答案。整體每個節點只算一次，複雜度 `O(n)`。\n\n**實作方式**\n\n樹形 DP 題目可拆成四個固定步驟：\n\n:::example 狀態定義範例（監控二叉樹 Leetcode 968）：每節點三態\n**1. 狀態定義：想清楚每個節點要回報哪幾個值。** 這是最難也最關鍵的一步。問自己：「父節點要做決策時，需要知道我的哪些情況？」需要幾種情況，就回報幾個值。\n\n```cpp\n// 狀態定義範例（監控二叉樹 Leetcode 968）：每節點三態\n// 0：自己有攝影機  1：自己被別人看著  2：自己沒被看著（待父層處理）\n```\n:::\n\n\n\n:::example 後序：先算小孩，再算自己\n**2. 設計遞迴：後序回傳狀態。** 先遞迴左右小孩拿到它們的狀態，再用它們組出自己的狀態回傳。\n\n```cpp\n// 後序：先算小孩，再算自己\nR dfs(TreeNode* node) {\n  if (!node) {\n    return baseCase();  // 空節點的初始狀態\n  }\n  R l = dfs(node->left);\n  R r = dfs(node->right);\n  return combine(node, l, r);  // 用小孩的狀態組出自己的\n}\n```\n:::\n\n\n\n:::example 監控二叉樹的轉移骨架（示意）\n**3. 寫轉移：把「決策」翻成幾條 max/min 公式。** 列舉自己每一種狀態，分別算出在該狀態下子樹的最佳值。轉移寫對的前提是狀態定義清楚。\n\n```cpp\n// 監控二叉樹的轉移骨架（示意）\n// 放攝影機  = 1 + min(各小孩三態的最小)\n// 被看著    = min(左放+右任意, 右放+左任意)\n// 沒被看著  = 左被看著 + 右被看著\n```\n:::\n\n\n\n**4. 讀答案與邊界。** 答案通常在根節點的回傳值裡取極值（如 968 還要處理「根自己沒被看著就得補一台攝影機」）。空節點的初始值要設得不破壞 min/max（例如不可能的狀態設成極大值）。複雜度 `O(n)`，但遞迴深度大時小心爆 stack。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「樹形 DP」想：\n\n- **資料是一棵樹，要求最大/最小/方案數，而每個節點都要做「選或不選」之類的決策**，且這決策會牽動相鄰節點。337 打家劫舍 III、968 監控二叉樹、3400 二叉樹染色都是。\n- **你發現只回報一個總數不夠**，父節點還想知道「你這個節點到底有沒有被選」——這就是要多回報幾個狀態的訊號：\n\n:::example 嗅到味道的瞬間：父層的決策依賴「我被選了沒」\n```cpp\n// 嗅到味道的瞬間：父層的決策依賴「我被選了沒」\n// 於是回傳就不能只給一個總和，要拆成 {選我, 不選我}\n```\n:::\n\n\n\n- **暴力做法會把同一棵子樹反覆重算**（指數級），樹形 DP 讓每節點只算一次，壓成 `O(n)`。\n- **題目能自然描述成「子樹的答案由小孩子樹拼出來」**，這就是後序遞迴的標誌。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我每個節點回報哪幾種狀態？為什麼是這幾種？\n2. 我的轉移公式怎麼從小孩的狀態組出自己的？\n3. 最終答案在根節點的哪個狀態（或哪幾個的極值）？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 337 | 打家劫舍 III | N/A | 放在「樹形 DP」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 968 | [監控二叉樹](https://leetcode.cn/problems/binary-tree-cameras/) | 2124 | 放在「樹形 DP」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1000 | [LCP 10. 二叉樹任務排程](https://leetcode.cn/problems/minimum-cost-to-merge-stones/) | 2423 | 放在「樹形 DP」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2313 | 二叉樹中得到結果所需的最少翻轉次數（Premium） | N/A | 放在「樹形 DP」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3400 | LCP 34. 二叉樹染色 | N/A | 放在「樹形 DP」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 337 | [打家劫舍 III](https://leetcode.cn/problems//house-robber-iii/) |  |\n| 968 | [監控二叉樹](https://leetcode.cn/problems//binary-tree-cameras/) | 2124 |\n| 1000 | [LCP 10. 二叉樹任務排程](https://leetcode.cn/problems//er-cha-shu-ren-wu-diao-du/) | 2423 |\n| 2313 | [二叉樹中得到結果所需的最少翻轉次數](https://leetcode.cn/problems//minimum-flips-in-binary-tree-to-get-result/) |  |\n| 3400 | [LCP 34. 二叉樹染色](https://leetcode.cn/problems//er-cha-shu-ran-se-UGC/) |  |\n| 6400 | [LCP 64. 二叉樹燈飾](https://leetcode.cn/problems//U7WvvU/) |  |",
        },
        {
          id: 27,
          title: "2.13 二叉樹 BFS",
          summary:
            "**原理講解**\n\n本節要解決的場景是：要按「一層一層」的順序走訪二叉樹——先處理離根最近的所有節點，再處理下一層。這叫「BFS（廣度優先搜尋）」或「層序遍歷」。\n\n:::example 佇列：先進先出，是 BFS 的核心工具\n對比一下：平常遞迴走樹是 DFS，會「一條路走到底」再回頭。BFS 不一樣，它像水波擴散，先把同一層全部看完，再往下一層。要做到這件事，靠的是一個「佇列（queue）」——先進先出的排隊結構：先排進去的節點先被處理，剛好讓上層先於下層。\n\n```cpp\n// 佇列：先進先出，是 BFS 的核心工具\nqueue<TreeNode*> q;\nq.push(root);               // 根先排隊\nTreeNode* cur = q.front();  // 看最前面的\nq.pop();                    // 取出最前面的，O(1)\n```\n:::\n\n\n\n:::example 一層的處理：先記住當層大小 n，只處理這 n 個\n關鍵技巧：**怎麼知道「這一層」到哪裡結束？** 答案是——在開始處理某層前，先記下此刻佇列裡有幾個節點（`int n = q.size()`），這個 `n` 就是當層節點數。接著只處理這 `n` 個，並把它們的小孩排進佇列（成為下一層）。\n\n```cpp\n// 一層的處理：先記住當層大小 n，只處理這 n 個\nint n = q.size();  // 當層有幾個節點\nfor (int i = 0; i < n; ++i) {\n  TreeNode* cur = q.front();\n  q.pop();\n  // ... 處理 cur ...\n  if (cur->left) {\n    q.push(cur->left);  // 小孩進下一層\n  }\n  if (cur->right) {\n    q.push(cur->right);\n  }\n}\n```\n:::\n\n\n\n有了「能切出每一層」這個能力，很多題目就迎刃而解：把每層收集成一個陣列（Leetcode 102 層序遍歷）、把奇數層反向（103 鋸齒形）、取每層最後一個（199 右檢視）、取最底層最左邊（513）。它們的骨架完全一樣，只是「對每層做什麼」不同。每個節點進出佇列各一次，複雜度 `O(n)`。\n\n**實作方式**\n\n層序遍歷題目可拆成四個固定步驟：\n\n:::example 初始化：根入隊，並準備收集結果\n**1. 狀態表示：用佇列存「待處理的節點」。** 初始只放根節點。佇列裡隨時都是「接下來要展開的前線」。\n\n```cpp\n// 初始化：根入隊，並準備收集結果\nqueue<TreeNode*> q;\nif (root) {\n  q.push(root);\n}\nvector<vector<int>> ans;  // 每個元素是一層\n```\n:::\n\n\n\n:::example 外層：佇列非空就還有層要處理\n**2. 外層迴圈：每轉一圈處理一整層。** 用 `while (!q.empty())` 包住，每圈一開始先抓 `n = q.size()` 鎖定當層。\n\n```cpp\n// 外層：佇列非空就還有層要處理\nwhile (!q.empty()) {\n  int n = q.size();\n  vector<int> level;  // 收集這一層\n  // ... 內層處理 n 個 ...\n  ans.push_back(level);\n}\n```\n:::\n\n\n\n:::example 內層：取 n 個、記錄值、推入小孩\n**3. 內層迴圈：處理當層 n 個，並把小孩送進下一層。**\n\n```cpp\n// 內層：取 n 個、記錄值、推入小孩\nfor (int i = 0; i < n; ++i) {\n  TreeNode* cur = q.front();\n  q.pop();\n  level.push_back(cur->val);\n  if (cur->left) {\n    q.push(cur->left);\n  }\n  if (cur->right) {\n    q.push(cur->right);\n  }\n}\n```\n:::\n\n\n\n**4. 變化與邊界。** 「鋸齒形」只要對偶數層把 `level` 反轉；「右檢視」取每層 `i == n - 1` 的那個；「層序 II」把結果整個反轉。空樹要先判 `root == nullptr`。複雜度時間 `O(n)`、空間最壞 `O(n)`（最寬那層的節點數）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「二叉樹 BFS」想：\n\n- **題目跟「層」「深度」「每一層」「最底層」「某層最左/最右」有關。** 102、103、107、199、513 全都是按層在問問題。\n- **你發現需要「同一層的節點一起看」**，而 DFS 一條路走到底不好控制層界線：\n\n:::example 嗅到味道的瞬間：你想「一次看完一整層」\n```cpp\n// 嗅到味道的瞬間：你想「一次看完一整層」\n// DFS 會跨層亂跑；改用佇列 + 每圈先抓 q.size()，層界線就清楚了\n```\n:::\n\n\n\n- **要找「離根最近且滿足某條件」的節點**（最短層數），BFS 一層層擴散天然就是由近到遠。\n- **暴力對每層重新從根掃一遍太慢**，BFS 讓每個節點只進出佇列一次，`O(n)` 解決。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用什麼結構存「待處理節點」？（應該是 queue）\n2. 我怎麼切出每一層的邊界？（每圈先記 `q.size()`）\n3. 我對「每一層」要做的事是什麼（收集？取最後一個？反轉？）\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 102 | 二叉樹的層序遍歷 | N/A | 放在「二叉樹 BFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 103 | 二叉樹的鋸齒形層序遍歷 | N/A | 放在「二叉樹 BFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 107 | 二叉樹的層序遍歷 II | N/A | 放在「二叉樹 BFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 199 | 二叉樹的右檢視 | N/A | 放在「二叉樹 BFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 513 | 找樹左下角的值 | N/A | 放在「二叉樹 BFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 102 | [二叉樹的層序遍歷](https://leetcode.cn/problems//binary-tree-level-order-traversal/) |  |\n| 103 | [二叉樹的鋸齒形層序遍歷](https://leetcode.cn/problems//binary-tree-zigzag-level-order-traversal/) |  |\n| 107 | [二叉樹的層序遍歷 II](https://leetcode.cn/problems//binary-tree-level-order-traversal-ii/) |  |\n| 199 | [二叉樹的右檢視](https://leetcode.cn/problems//binary-tree-right-side-view/) |  |\n| 513 | [找樹左下角的值](https://leetcode.cn/problems//find-bottom-left-tree-value/) |  |\n| 515 | [在每個樹行中找最大值](https://leetcode.cn/problems//find-largest-value-in-each-tree-row/) |  |\n| 623 | [在二叉樹中增加一行](https://leetcode.cn/problems//add-one-row-to-tree/) |  |\n| 637 | [二叉樹的層平均值](https://leetcode.cn/problems//average-of-levels-in-binary-tree/) |  |\n| 662 | [二叉樹最大寬度](https://leetcode.cn/problems//maximum-width-of-binary-tree/) |  |\n| 742 | [二叉樹最近的葉節點](https://leetcode.cn/problems//closest-leaf-in-a-binary-tree/) |  |\n| 1602 | [找到二叉樹中最近的右側節點](https://leetcode.cn/problems//find-nearest-right-node-in-binary-tree/) |  |\n| 1660 | [糾正二叉樹](https://leetcode.cn/problems//correct-a-binary-tree/) |  |\n| 3157 | [找到具有最小和的樹的層數](https://leetcode.cn/problems//find-the-level-of-tree-with-minimum-sum/) |  |\n| 3831 | [二叉搜尋樹某一層的中位數](https://leetcode.cn/problems//median-of-a-binary-search-tree-level/) |  |\n| 1161 | [最大層內元素和](https://leetcode.cn/problems//maximum-level-sum-of-a-binary-tree/) | 1250 |\n| 993 | [二叉樹的堂兄弟節點](https://leetcode.cn/problems//cousins-in-binary-tree/) | 1288 |\n| 2583 | [二叉樹中的第 K 大層和](https://leetcode.cn/problems//kth-largest-sum-in-a-binary-tree/) | 1374 |\n| 1302 | [層數最深葉子節點的和](https://leetcode.cn/problems//deepest-leaves-sum/) | 1388 |\n| 2415 | [反轉二叉樹的奇數層](https://leetcode.cn/problems//reverse-odd-levels-of-binary-tree/) | 1431 |\n| 1609 | [奇偶樹](https://leetcode.cn/problems//even-odd-tree/) | 1438 |\n| 2471 | [逐層排序二叉樹所需的最少運算元目](https://leetcode.cn/problems//minimum-number-of-operations-to-sort-a-binary-tree-by-level/) | 1635 |\n| 863 | [二叉樹中所有距離為 K 的結點](https://leetcode.cn/problems//all-nodes-distance-k-in-binary-tree/) | 1663 |\n| 2641 | [二叉樹的堂兄弟節點 II](https://leetcode.cn/problems//cousins-in-binary-tree-ii/) | 1677 |\n| 919 | [完全二叉樹插入器](https://leetcode.cn/problems//complete-binary-tree-inserter/) | 1691 |\n| 958 | [二叉樹的完全性檢驗](https://leetcode.cn/problems//check-completeness-of-a-binary-tree/) | 1703 |",
        },
        {
          id: 28,
          title: "2.14 連結串列+二叉樹",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目同時牽涉「連結串列（linked list）」和「二叉樹」，常見任務是把樹「攤平」成一條鏈、或反過來把有序鏈「建」成一棵樹、又或者在樹的節點間「拉線」連成鏈。\n\n:::example 連結串列的鐵則：改 next 前先存下一個，免得鏈斷掉\n先複習連結串列：它是一串節點，每個節點存一個值和一個指向「下一個」的指標。它沒有索引，只能順著指標走。操作它的精髓是**改指標前先把下一個存起來，否則一改就找不到後面**。\n\n```cpp\n// 連結串列的鐵則：改 next 前先存下一個，免得鏈斷掉\nListNode* nxt = cur->next;  // 1) 先存後繼\ncur->next = somethingNew;   // 2) 再改指標，原本的後繼不會遺失\n```\n:::\n\n\n\n:::example 114 的核心一步：把左子樹接到右邊，左指標清空\n樹和鏈的共同點是「都靠指標連節點」，所以很多操作其實就是「小心地改幾條指標」。例如「二叉樹展開為連結串列」（Leetcode 114）：要把整棵樹照前序順序壓成一條只用右指標的鏈。做法是對每個節點，把它的左子樹整段插到右子樹前面。\n\n```cpp\n// 114 的核心一步：把左子樹接到右邊，左指標清空\nTreeNode *l = node->left, *r = node->right;\nnode->left = nullptr;  // 攤平後不再用左指標\nnode->right = l;       // 左子樹接到右側\n                  // 再把原本的右子樹 r 接到「新右鏈的尾端」\n```\n:::\n\n\n\n反方向的「有序鏈轉 BST」（Leetcode 109）：鏈是排好序的，鏈的「中點」就是樹根（左半變左子樹、右半變右子樹），遞迴下去就得到一棵高度平衡的 BST。核心觀念是**有序序列的中點 = 平衡樹的根**。\n\n還有「填充右側 next 指標」（116、117）：本質是在樹的同一層節點之間「拉一條鏈」，把同層相鄰節點用 `next` 串起來。\n\n**實作方式**\n\n這類題目可拆成四個固定步驟：\n\n:::example dummy 頭：讓「接第一個節點」和「接後面節點」寫法一致\n**1. 狀態表示：盯緊幾個關鍵指標。** 通常需要 `prev`、`cur`、`next`，以及一個「dummy 頭節點」當作鏈的固定起點，省去處理「第一個節點」的特例。\n\n```cpp\n// dummy 頭：讓「接第一個節點」和「接後面節點」寫法一致\nListNode dummy(0);\nListNode* tail = &dummy;  // tail 永遠指向已建好鏈的最後一個\n```\n:::\n\n\n\n:::example 通用：先存，再改，最後前進\n**2. 改邊流程：每動一條指標前先存後繼。** 不論是攤平、插入還是反轉，都只改局部幾條指標，但順序錯了鏈就斷。\n\n```cpp\n// 通用：先存，再改，最後前進\nListNode* nxt = cur->next;\ncur->next = tail;  // 接到目標位置\ntail = cur;        // 推進已建鏈的尾端\ncur = nxt;         // 前進到原本的後繼\n```\n:::\n\n\n\n:::example 快慢指標找鏈中點：slow 每次一步、fast 每次兩步\n**3. 樹鏈互轉：抓住「中點」或「同層相鄰」。** 鏈轉樹用快慢指標找中點當根（109）；同層拉線（116/117）則用上一層已連好的 `next` 鏈，橫向走訪這一層、把它們的小孩串起來。\n\n```cpp\n// 快慢指標找鏈中點：slow 每次一步、fast 每次兩步\nListNode *slow = head, *fast = head, *prev = nullptr;\nwhile (fast && fast->next) {\n  prev = slow;\n  slow = slow->next;\n  fast = fast->next->next;\n}\n// 迴圈結束 slow 在中點，prev 用來把左半截斷\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 頭節點可能被改動 → 用 dummy 統一；空鏈、單節點要單獨確認。攤平/拉線多為 `O(n)`；鏈轉 BST 若每次重走找中點是 `O(n log n)`，改用「中序模擬」可優化到 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「連結串列 + 二叉樹」想：\n\n- **題目要在「樹」和「鏈」之間轉換**，或要在樹節點間「拉一條橫向的鏈」。109、114、116、117、426 都是。\n- **你發現解法就是「小心地改幾條指標」**，而一不小心就會把後面整段弄丟：\n\n:::example 嗅到味道的瞬間：你正要改某個 next/left/right，但後面還有用\n```cpp\n// 嗅到味道的瞬間：你正要改某個 next/left/right，但後面還有用\n// 反射動作：先 ListNode* nxt = cur->next; 存起來再改\n```\n:::\n\n\n\n- **看到「有序」+「要建平衡樹」**，馬上想到「中點當根」。\n- **要把同一層連起來**，想到沿著上一層已建好的 `next` 橫向走。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我需要哪幾個指標（prev / cur / next / dummy）？\n2. 我每次改指標前，有沒有先把後繼存起來？\n3. 如果是鏈轉樹，我的「樹根」對應鏈的哪個位置（中點？）\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 109 | 有序連結串列轉換二叉搜索樹 | N/A | 放在「連結串列+二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 114 | 二叉樹展開為連結串列 | N/A | 放在「連結串列+二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 116 | 填充每個節點的下一個右側節點指標 | N/A | 放在「連結串列+二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 117 | 填充每個節點的下一個右側節點指標 II | N/A | 放在「連結串列+二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 426 | 將二叉搜索樹轉化為排序的雙向連結串列（Premium） | N/A | 放在「連結串列+二叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 206. 反轉連結串列 展示核心寫法。\n\n**完整題目**：給定單向連結串列的頭節點，反轉整條串列並回傳新的頭節點。\n\n:::example 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n**線索**：題目中的「題目要求刪除、插入、反轉、合併或找第 k 個節點。」是主要線索；先把它翻成狀態、候選集合或判斷函式，再套用本節 pattern。\n\n```cpp\nclass Solution {\n public:\n  ListNode* reverseList(ListNode* head) {\n    // 迭代反轉：prev 指向已反轉部分的頭節點，head 為待處理節點\n    ListNode* prev = nullptr;\n    while (head) {\n      ListNode* nxt = head->next;  // 暫存下一節點，避免斷鏈後遺失\n      head->next = prev;           // 將當前節點指向已反轉部分\n      prev = head;                 // prev 前移\n      head = nxt;                  // head 移至下一節點\n    }\n    return prev;  // prev 即新鏈結串列的頭節點\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 每步先保存下一個節點，再反轉當前指標；連結串列題的核心是不要丟失後續鏈。\n- `dp` 變數代表已處理前綴或子問題的答案，不是模糊的目前最佳值。\n- 迴圈順序必須保證轉移來源已經算好；壓縮空間時更要確認讀到的是舊值還是新值。\n\n**常見變形**\n\n- 快慢指標找中點或倒數第 k。\n- dummy head 處理刪除頭節點。\n- 分治合併多條有序鏈。\n\n**實作注意事項**\n\n- 改 `next` 前先保存後繼。\n- dummy 節點可避免特判 head。\n- 循環條件用 `node && node->next` 時先確認訪問順序。\n- 返回值可能是新 head，不一定是原 head。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 109 | [有序連結串列轉換二叉搜索樹](https://leetcode.cn/problems//convert-sorted-list-to-binary-search-tree/) |  |\n| 114 | [二叉樹展開為連結串列](https://leetcode.cn/problems//flatten-binary-tree-to-linked-list/) |  |\n| 116 | [填充每個節點的下一個右側節點指標](https://leetcode.cn/problems//populating-next-right-pointers-in-each-node/) |  |\n| 117 | [填充每個節點的下一個右側節點指標 II](https://leetcode.cn/problems//populating-next-right-pointers-in-each-node-ii/) |  |\n| 426 | [將二叉搜索樹轉化為排序的雙向連結串列](https://leetcode.cn/problems//convert-binary-search-tree-to-sorted-doubly-linked-list/) |  |\n| 1367 | [二叉樹中的連結串列](https://leetcode.cn/problems//linked-list-in-binary-tree/) | 1650 |",
        },
        {
          id: 29,
          title: "2.15 N 叉樹",
          summary:
            "**原理講解**\n\n本節要解決的場景是：樹的每個節點不再固定只有「左、右」兩個小孩，而是可以有任意多個小孩——這叫「N 叉樹（N-ary tree）」。四叉樹（每節點 0 或 4 個小孩）是它的特例。\n\n:::example N 叉樹節點：用一個 vector 裝所有小孩\n二叉樹的小孩寫成 `left`、`right` 兩個欄位；N 叉樹改用一個「小孩陣列」來裝不定數量的小孩：\n\n```cpp\n// N 叉樹節點：用一個 vector 裝所有小孩\nstruct Node {\n  int val;\n  vector<Node*> children;  // 想有幾個小孩就放幾個\n};\n```\n:::\n\n\n\n好消息是：**二叉樹的那套走訪思路完全沿用，只是把「處理 left、處理 right」換成「用一個 for 迴圈跑過所有小孩」**。自底向上彙總子樹、自頂向下傳遞資訊，全都一樣。\n\n:::example N 叉樹最大深度：1 + 所有小孩深度的最大值\n例如「N 叉樹的最大深度」（Leetcode 559）：一個節點的深度 = 1 + 所有小孩深度的最大值。二叉樹是 `1 + max(左, 右)`，N 叉樹只是把 max 攤開到所有小孩：\n\n```cpp\n// N 叉樹最大深度：1 + 所有小孩深度的最大值\nint maxDepth(Node* root) {\n  if (!root) {\n    return 0;\n  }\n  int best = 0;\n  for (Node* c : root->children) {  // 遍歷每個小孩\n    best = max(best, maxDepth(c));\n  }\n  return best + 1;  // 加上自己這一層\n}\n```\n:::\n\n\n\n層序遍歷（Leetcode 429）也一樣，只是把小孩入隊時從「push 左、push 右」改成「for 迴圈把所有小孩 push 進去」。四叉樹題（427 建立四叉樹、558 四叉樹交集）則是「能合併成一塊就合併、否則切四塊遞迴」，本質仍是遞迴處理每個小孩再彙總。\n\n**實作方式**\n\nN 叉樹題目可拆成四個固定步驟：\n\n:::example 摘要由「自己」加上「所有小孩的摘要」合併而成\n**1. 狀態表示：節點回傳子樹摘要。** 跟二叉樹一樣，每個節點向上回報「以我為根的子樹」算出的東西（深度、節點數、序列化字串等）。\n\n```cpp\n// 摘要由「自己」加上「所有小孩的摘要」合併而成\n// summary(node) = combine(node->val, summary(c1), summary(c2), ...)\n```\n:::\n\n\n\n:::example 通用 DFS 骨架：遞迴每個小孩，再合併\n**2. 走訪流程：用 for 迴圈跑過所有小孩。** 這是 N 叉樹和二叉樹唯一的寫法差別。\n\n```cpp\n// 通用 DFS 骨架：遞迴每個小孩，再合併\nvoid dfs(Node* node) {\n  if (!node) {\n    return;\n  }\n  // ... 進入時的處理（前序）...\n  for (Node* c : node->children) {\n    dfs(c);\n  }\n  // ... 離開時的處理（後序，彙總小孩結果）...\n}\n```\n:::\n\n\n\n:::example N 叉層序：取出節點後，把它所有小孩排進佇列\n**3. BFS 版本：入隊時把整個 children 推進去。**\n\n```cpp\n// N 叉層序：取出節點後，把它所有小孩排進佇列\nint n = q.size();\nfor (int i = 0; i < n; ++i) {\n  Node* cur = q.front();\n  q.pop();\n  for (Node* c : cur->children) {\n    q.push(c);  // 一次推所有小孩\n  }\n}\n```\n:::\n\n\n\n**4. 序列化與邊界。** 序列化 N 叉樹（428）因小孩數不定，要額外記「每個節點有幾個小孩」或用特殊分隔符，反序列化才知道讀幾個。四叉樹合併（427/558）的邊界是「整塊同值就停止細分」。複雜度多為 `O(n)`（每節點訪一次）；遞迴深時注意 stack。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「N 叉樹」想：\n\n- **節點的小孩數量不固定**，題目直接給你 `children` 陣列，或描述成四叉樹。427、428、429、558、559 都是。\n- **你發現「二叉樹會做的事我也想做，只是小孩變多了」**——這就是把 `left/right` 換成 for 迴圈的訊號：\n\n:::example 嗅到味道的瞬間：本來想寫 dfs(left); dfs(right);\n```cpp\n// 嗅到味道的瞬間：本來想寫 dfs(left); dfs(right);\n// 但小孩不止兩個，於是改成 for (auto c : children) dfs(c);\n```\n:::\n\n\n\n- **要彙總整棵子樹的資訊**（深度、總數、序列化），本質仍是後序遞迴 + 合併小孩結果。\n- **四叉樹的「同值整塊」可合併**，遇到不一致才切四塊往下遞迴。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有把「處理 left、處理 right」換成「for 迴圈跑所有小孩」？\n2. 每個節點向上回報的摘要是什麼？怎麼由小孩合併出來？\n3. 如果要序列化，我怎麼記錄「每個節點有幾個小孩」？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 427 | 建立四叉樹 | N/A | 放在「N 叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 428 | 序列化和反序列化 N 叉樹（Premium） | N/A | 放在「N 叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 429 | N 叉樹的層序遍歷 | N/A | 放在「N 叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 558 | 四叉樹交集 | N/A | 放在「N 叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 559 | N 叉樹的最大深度 | N/A | 放在「N 叉樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 427 | [建立四叉樹](https://leetcode.cn/problems//construct-quad-tree/) |  |\n| 428 | [序列化和反序列化 N 叉樹](https://leetcode.cn/problems//serialize-and-deserialize-n-ary-tree/) |  |\n| 429 | [N 叉樹的層序遍歷](https://leetcode.cn/problems//n-ary-tree-level-order-traversal/) |  |\n| 558 | [四叉樹交集](https://leetcode.cn/problems//logical-or-of-two-binary-grids-represented-as-quad-trees/) |  |\n| 559 | [N 叉樹的最大深度](https://leetcode.cn/problems//maximum-depth-of-n-ary-tree/) |  |\n| 589 | [N 叉樹的前序遍歷](https://leetcode.cn/problems//n-ary-tree-preorder-traversal/) |  |\n| 590 | [N 叉樹的後序遍歷](https://leetcode.cn/problems//n-ary-tree-postorder-traversal/) |  |\n| 1490 | [克隆 N 叉樹](https://leetcode.cn/problems//clone-n-ary-tree/) |  |\n| 1506 | [找到 N 叉樹的根節點](https://leetcode.cn/problems//find-root-of-n-ary-tree/) |  |\n| 1516 | [移動 N 叉樹的子樹](https://leetcode.cn/problems//move-sub-tree-of-n-ary-tree/) |  |\n| 1522 | [N 叉樹的直徑](https://leetcode.cn/problems//diameter-of-n-ary-tree/) |  |",
        },
        {
          id: 30,
          title: "2.16 其他",
          summary:
            "**原理講解**\n\n本節是二叉樹的「其他」綜合題，共同精神只有一句：**把資訊用 DFS/BFS 在樹上傳遞——自底向上彙總子樹，或自頂向下攜帶狀態**。同一套走訪框架，套不同的「在節點上做什麼」，就能解一票看似不相干的題目。\n\n:::example 二叉樹遞迴萬用骨架\n先抓住二叉樹遞迴的萬用骨架。每個節點做三件事：進來時可以先做點事（前序）、遞迴左右小孩、回來後彙總小孩的結果（後序）。\n\n```cpp\n// 二叉樹遞迴萬用骨架\nvoid dfs(TreeNode* node) {\n  if (!node) {\n    return;  // 邊界：空節點\n  }\n  // 前序：進入時的處理\n  dfs(node->left);\n  dfs(node->right);\n  // 後序：彙總左右小孩的結果\n}\n```\n:::\n\n\n\n:::example 297 序列化：前序輸出，空節點寫成 '#'\n舉本節幾個例子看它如何變形。「序列化與反序列化」（Leetcode 297）：序列化就是前序把每個節點（含空節點用特殊符號）寫成字串；反序列化就是照同樣順序讀回、遞迴建樹。\n\n```cpp\n// 297 序列化：前序輸出，空節點寫成 '#'\nvoid ser(TreeNode* n, string& s) {\n  if (!n) {\n    s += \"#,\";\n        return;\n  }  // 空節點也要記，才能還原結構\n  s += to_string(n->val) + \",\";\n       ser(n->left, s);\n  ser(n->right, s);\n}\n```\n:::\n\n\n\n「完全二叉樹的節點個數」（Leetcode 222）則利用完全二叉樹的特性：若左右子樹高度相同，左子樹是滿的，可用公式 `2^h - 1` 直接算，不必一個個數，把 `O(n)` 壓到 `O(log² n)`。「BST 迭代器」（173）則用一個堆疊模擬中序走訪，做到「每次呼叫回傳下一個最小值」。\n\n共同點：**先確定「節點要回報什麼 / 攜帶什麼」，再套 DFS 或 BFS 骨架。**\n\n**實作方式**\n\n這類題目可拆成四個固定步驟：\n\n:::example 兩種方向：選一個\n**1. 狀態表示：決定節點回報或攜帶什麼。** 是回報子樹摘要（高度、節點數），還是攜帶從根帶下來的資訊（路徑、深度、座標）？\n\n```cpp\n// 兩種方向：選一個\n// 自底向上：int dfs(node) 回傳子樹摘要\n// 自頂向下：void dfs(node, 累積狀態) 把狀態帶給小孩\n```\n:::\n\n\n\n:::example 中序：對 BST 會由小到大輸出（173 迭代器的基礎）\n**2. 套走訪骨架：DFS 或 BFS。** 需要層 / 距離概念用 BFS；需要彙總子樹用 DFS（後序）；需要按值有序輸出用中序。\n\n```cpp\n// 中序：對 BST 會由小到大輸出（173 迭代器的基礎）\nvoid inorder(TreeNode* n) {\n  if (!n) {\n    return;\n  }\n  inorder(n->left);\n  // 處理 n（此時 n 是「目前最小的未輸出值」）\n  inorder(n->right);\n}\n```\n:::\n\n\n\n:::example 222：左右子樹等高 → 左子樹是滿的，直接用公式\n**3. 用結構特性加速。** 像 222 利用「完全二叉樹左滿則公式直算」、垂直遍歷（314）用一個 map 依「列座標」分桶收集。\n\n```cpp\n// 222：左右子樹等高 → 左子樹是滿的，直接用公式\nint lh = leftHeight(node), rh = rightHeight(node);\nif (lh == rh) {\n  return (1 << lh) - 1;  // 2^lh - 1，不必逐一數\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 序列化要妥善處理空節點（漏了就無法還原）；迭代器要用堆疊保存「待回」路徑；遞迴深易爆 stack。複雜度視題而定，多為 `O(n)`，能用結構特性的可更快（如 222 的 `O(log² n)`）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「二叉樹綜合走訪」想：\n\n- **題目是二叉樹上的某種操作**：序列化、計數、做成迭代器、特殊順序輸出。297、222、173、314 都是。\n- **你發現答案能由「子樹結果彙總」或「從根帶下狀態」得到**——這就是套 DFS/BFS 骨架的訊號：\n\n:::example 嗅到味道的瞬間：每個節點都在做同一件事，只是位置不同\n```cpp\n// 嗅到味道的瞬間：每個節點都在做同一件事，只是位置不同\n// 反射動作：套上 dfs(node) 骨架，想清楚「回報什麼/攜帶什麼」\n```\n:::\n\n\n\n- **看到「中序 = 由小到大」「完全二叉樹」「按層」這些結構特性**，往往能用它加速，別硬掃整棵樹。\n- **暴力反覆重算子樹資訊**，改成一次後序遍歷就把重複工作省掉。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我用哪種走訪（前序 / 中序 / 後序 / BFS）？為什麼？\n2. 節點向上回報什麼、或向下攜帶什麼？\n3. 有沒有可利用的結構特性（BST 有序、完全二叉樹）能加速？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 60 | LCP 60. 力扣泡泡龍 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 173 | 二叉搜索樹迭代器 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 222 | 完全二叉樹的節點個數 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 297 | 二叉樹的序列化與反序列化 | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 314 | 二叉樹的垂直遍歷（Premium） | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 60 | [LCP 60. 力扣泡泡龍](https://leetcode.cn/problems//WInSav/) |  |\n| 173 | [二叉搜索樹迭代器](https://leetcode.cn/problems//binary-search-tree-iterator/) |  |\n| 222 | [完全二叉樹的節點個數](https://leetcode.cn/problems//count-complete-tree-nodes/) |  |\n| 297 | [二叉樹的序列化與反序列化](https://leetcode.cn/problems//serialize-and-deserialize-binary-tree/) |  |\n| 314 | [二叉樹的垂直遍歷](https://leetcode.cn/problems//binary-tree-vertical-order-traversal/) |  |\n| 331 | [驗證二叉樹的前序序列化](https://leetcode.cn/problems//verify-preorder-serialization-of-a-binary-tree/) |  |\n| 431 | [將 N 叉樹編碼為二叉樹](https://leetcode.cn/problems//encode-n-ary-tree-to-binary-tree/) |  |\n| 449 | [序列化和反序列化二叉搜索樹](https://leetcode.cn/problems//serialize-and-deserialize-bst/) |  |\n| 652 | [尋找重複的子樹](https://leetcode.cn/problems//find-duplicate-subtrees/) |  |\n| 655 | [輸出二叉樹](https://leetcode.cn/problems//print-binary-tree/) |  |\n| 666 | [路徑總和 IV](https://leetcode.cn/problems//path-sum-iv/) |  |\n| 979 | [在二叉樹中分配硬幣](https://leetcode.cn/problems//distribute-coins-in-binary-tree/) | 1709 |\n| 987 | [二叉樹的垂序遍歷](https://leetcode.cn/problems//vertical-order-traversal-of-a-binary-tree/) | 1676 |\n| 1104 | [二叉樹尋路](https://leetcode.cn/problems//path-in-zigzag-labelled-binary-tree/) | 1545 |\n| 1261 | [在受汙染的二叉樹中查詢元素](https://leetcode.cn/problems//find-elements-in-a-contaminated-binary-tree/) | 1440 |\n| 1485 | [克隆含隨機指標的二叉樹](https://leetcode.cn/problems//clone-binary-tree-with-random-pointer/) |  |\n| 1586 | [二叉搜索樹迭代器 II](https://leetcode.cn/problems//binary-search-tree-iterator-ii/) |  |\n| 2005 | [斐波那契樹的移除子樹遊戲](https://leetcode.cn/problems//subtree-removal-game-with-fibonacci-tree/) |  |\n| 2049 | [統計最高分的節點數目](https://leetcode.cn/problems//count-nodes-with-the-highest-score/) | 1912 |\n| 2445 | [值為 1 的節點數](https://leetcode.cn/problems//number-of-nodes-with-value-one/) |  |\n| 2458 | [移除子樹後的二叉樹高度](https://leetcode.cn/problems//height-of-binary-tree-after-subtree-removal-queries/) | 2299 |\n| 2509 | [查詢樹中環的長度](https://leetcode.cn/problems//cycle-length-queries-in-a-tree/) | 1948 |\n| LCP 26 | [LCP 26. 導航裝置](https://leetcode.cn/problems/hSRGyL) |  |\n| 2673 | [使二叉樹所有路徑值相等的最小代價](https://leetcode.cn/problems//make-costs-of-paths-equal-in-a-binary-tree/) | 1917 |\n| 2773 | [特殊二叉樹的高度](https://leetcode.cn/problems//height-of-special-binary-tree/) |  |",
        },
      ],
    },
    {
      id: 31,
      title: "3. 一般樹",
      summary:
        "涵蓋一般樹的遍歷、DFS、LCA、直徑、拓撲排序、時間戳、點分治等，適合進階學習。\n\n:::example 一般樹 DFS（鄰接表）\n**C++ 模板（一般樹 DFS）**：\n\n```cpp\n// 一般樹 DFS（鄰接表）\nvoid dfs(int u, int fa, vector<vector<int>>& adj) {\n  // 處理節點 u\n  for (int v : adj[u]) {\n    if (v != fa) {\n      dfs(v, u, adj);\n    }\n  }\n}\n```\n:::",
      children: [
        {
          id: 32,
          title: "3.1 遍歷",
          summary:
            "**原理講解**\n\n本節進入「圖（graph）」的世界，主題是最基本也最重要的能力：**走遍整張圖**。圖就是「一堆點，加上點與點之間的連線（邊）」。樹其實是圖的特例（無環、連通），所以樹會的 DFS/BFS，在圖上一樣能用，只是多了一個必須注意的點。\n\n:::example 圖走訪的鐵則：每個點只處理一次，靠 visited 防重複/防繞圈\n那個關鍵差別是：**圖可能有環，同一個點可能從好幾條路走到**。如果不做記號，就會無限繞圈或重複處理。所以圖走訪一定要有一個「visited（去過了沒）」標記。\n\n```cpp\n// 圖走訪的鐵則：每個點只處理一次，靠 visited 防重複/防繞圈\nvector<bool> visited(n, false);\n// 進入一個點時：先檢查、再標記\nif (visited[u]) {\n  return;  // 去過就別再進\n}\nvisited[u] = true;\n```\n:::\n\n\n\n:::example 鄰接表 + DFS：標記自己，再遞迴每個沒去過的鄰居\n圖通常用「鄰接表（adjacency list）」存：對每個點，記下它能直接到的鄰居清單。走訪就是「從某點出發，沿著鄰居一個個展開，直到全圖看完」。\n\n```cpp\n// 鄰接表 + DFS：標記自己，再遞迴每個沒去過的鄰居\nvector<vector<int>> g(n);  // g[u] 是 u 的所有鄰居\nvoid dfs(int u) {\n  visited[u] = true;\n  for (int v : g[u]) {\n    if (!visited[v]) {\n      dfs(v);  // 只走沒去過的鄰居\n    }\n  }\n}\n```\n:::\n\n\n\n有了「走遍全圖」這個基本功，很多題目就成立了。例如「受限條件下可到達節點的數目」（Leetcode 2368）：把被限制的點先標成 visited（等於封路），再從 0 號點 DFS，能走到幾個點就是答案。又如「重新規劃路線」（Leetcode 1466）：把每條邊同時記下「原方向」，走訪時若方向是「背離 0 號城市」就需要反轉，邊走邊累加答案。每個點、每條邊各處理一次，複雜度 `O(點 + 邊)`。\n\n**實作方式**\n\n圖遍歷題目可拆成四個固定步驟：\n\n:::example 建圖：把邊清單轉成鄰接表（這裡是無向圖，兩邊都加）\n**1. 狀態表示：建鄰接表 + visited 陣列。** 先把題目給的邊轉成「每個點的鄰居清單」，再準備一個 visited 防重複。\n\n```cpp\n// 建圖：把邊清單轉成鄰接表（這裡是無向圖，兩邊都加）\nvector<vector<int>> g(n);\nfor (auto& e : edges) {\n  g[e[0]].push_back(e[1]);\n  g[e[1]].push_back(e[0]);\n}\n```\n:::\n\n\n\n:::example BFS 版走遍：從 src 出發一層層擴散\n**2. 選 DFS 或 BFS 走訪。** 兩者都能走遍全圖：DFS 用遞迴/堆疊一條路深入；BFS 用佇列一圈圈擴散（要算「最短步數」時選 BFS）。\n\n```cpp\n// BFS 版走遍：從 src 出發一層層擴散\nqueue<int> q;\nq.push(src);\nvisited[src] = true;\nwhile (!q.empty()) {\n  int u = q.front();\n  q.pop();\n  for (int v : g[u]) {\n    if (!visited[v]) {\n      visited[v] = true;\n      q.push(v);\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 2368 思路：先把受限點當成「已訪」封死，再從 0 出發數可達點\n**3. 在走訪過程中順手算答案。** 走到一個點就 `count++`（數可達點數）、或檢查這條邊的方向是否要反轉（累加反轉次數）。把要算的東西嵌進走訪迴圈即可。\n\n```cpp\n// 2368 思路：先把受限點當成「已訪」封死，再從 0 出發數可達點\nfor (int x : restricted) {\n  visited[x] = true;\n}\n// 之後 dfs(0)，過程中每進一個新點就 ++count\n```\n:::\n\n\n\n**4. 邊界與複雜度。** 圖不一定連通，要遍歷「所有點」找答案時，需對每個沒去過的點都起一次走訪（數連通塊就是這樣）；有向圖建圖只加單向。複雜度時間 `O(V + E)`（點數加邊數），空間 `O(V + E)`；DFS 遞迴深時小心爆 stack，可改 BFS。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「圖遍歷」想：\n\n- **資料是「點 + 邊」**（城市與道路、節點與連線），問「能到哪些點」「有幾個連通塊」「最少步數」。1466、2368、582 都是這型。\n- **你發現可能繞圈或重複走到同一點**——這就是要加 visited 的訊號：\n\n:::example 嗅到味道的瞬間：同一個點會被多條路走到\n```cpp\n// 嗅到味道的瞬間：同一個點會被多條路走到\n// 反射動作：準備 visited，進點先檢查再標記，保證每點只處理一次\n```\n:::\n\n\n\n- **要「從某點擴散到所有相連的點」**，DFS / BFS 走一遍就是答案的骨架。\n- **暴力對每個查詢都重走整張圖太慢**，一次遍歷把每點每邊只看一次，壓成 `O(V + E)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我怎麼把題目的邊存成鄰接表？是有向還是無向？\n2. 我的 visited 在哪裡檢查、哪裡標記？能保證每點只處理一次嗎？\n3. 答案是在走訪過程中累加（可達數 / 反轉數），還是走完才統計？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 582 | 殺掉程式（Premium） | N/A | 放在「遍歷」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1466 | [重新規劃路線](https://leetcode.cn/problems/reorder-routes-to-make-all-paths-lead-to-the-city-zero/) | 1634 | 放在「遍歷」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2368 | [受限條件下可到達節點的數目](https://leetcode.cn/problems/reachable-nodes-with-restrictions/) | 1477 | 放在「遍歷」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 582 | [殺掉程式](https://leetcode.cn/problems//kill-process/) |  |\n| 1466 | [重新規劃路線](https://leetcode.cn/problems//reorder-routes-to-make-all-paths-lead-to-the-city-zero/) | 1634 |\n| 2368 | [受限條件下可到達節點的數目](https://leetcode.cn/problems//reachable-nodes-with-restrictions/) | 1477 |",
        },
        {
          id: 33,
          title: "3.2 自頂向下 DFS",
          summary:
            "**原理講解**\n\n本節要解決的場景是：資料長成一棵「樹」（每個節點有一個父節點、若干子節點，沒有環），我們要算的答案和「從根往下、一路累積的資訊」有關。例如「某個節點到根的距離」「從根走到這裡經過的累加值」。這類問題的共同點是：**子節點需要的資訊，父節點都已經算好了，只要往下傳即可。**\n\n這就是「自頂向下 DFS」。DFS（深度優先搜尋）就是「從根出發，先一路鑽到底，再回頭走另一條分支」的走法。所謂「自頂向下」，是指我們在「往下走、進入子節點之前」就把一份**從祖先累積下來的狀態**算好，當作參數傳給子節點。\n\n:::example 樹 DFS 的最基本骨架：fa 記住父節點，避免走回頭\n先看最基本的樹 DFS 骨架。樹用「鄰接表」表示：`g[u]` 是 `u` 的所有鄰居。因為樹沒有環，只要記得「從哪來」就不會走回頭路：\n\n```cpp\n// 樹 DFS 的最基本骨架：fa 記住父節點，避免走回頭\nvoid dfs(int u, int fa) {\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;  // v 是父節點就跳過\n    }\n    dfs(v, u);  // 往下遞迴，u 變成 v 的父\n  }\n}\n```\n:::\n\n\n\n:::example 自頂向下：depth 由父節點 +1 傳給子節點\n「自頂向下」的關鍵，是多帶一個參數，把「從根累積到目前」的狀態一路往下傳。例如算每個節點到根的深度：\n\n```cpp\n// 自頂向下：depth 由父節點 +1 傳給子節點\nvoid dfs(int u, int fa, int depth) {\n  dep[u] = depth;  // 進入 u 時就確定它的深度\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    dfs(v, u, depth + 1);  // 把「父的深度+1」帶給子\n  }\n}\n```\n:::\n\n\n\n重點在於：答案在「下行（遞）」的途中就決定了，不必等子樹回傳。像 1376「通知所有員工所需的時間」，每個員工的「收到通知時刻」＝主管的時刻＋主管的通知耗時，正是把時間從上司一路往下加；1443 把訊息（或開銷）從根帶往各節點，也是同一個模式。整體只走每個節點一次，複雜度 `O(n)`。\n\n**實作方式**\n\n把任何「自頂向下」題目拆成三件事：狀態怎麼帶、怎麼往下傳、答案在哪讀。\n\n:::example 狀態就是 DFS 的參數：這裡帶「到根的路徑和」\n**1. 決定要往下帶的狀態。** 問自己：「子節點要算它的答案，需要知道哪些『從祖先來的』資訊？」常見是深度、到根的路徑和、或一個累積的集合。把它做成 DFS 的參數。\n\n```cpp\n// 狀態就是 DFS 的參數：這裡帶「到根的路徑和」\nvoid dfs(int u, int fa, long long sum) {\n  sum += val[u];     // 加上目前節點的值\n  rootSum[u] = sum;  // 記下 u 到根的累積和\n                     // ... 往下遞迴\n}\n```\n:::\n\n\n\n:::example 更新後再下傳：把「父的狀態 + 這條邊的貢獻」交給子\n**2. 進入子節點前更新狀態，再遞迴。** 「自頂向下」的精神就是：先把狀態更新成「子節點看到的版本」，再傳下去。\n\n```cpp\n// 更新後再下傳：把「父的狀態 + 這條邊的貢獻」交給子\nfor (int v : g[u]) {\n  if (v == fa) {\n    continue;\n  }\n  dfs(v, u, sum + w[u][v]);  // w 是邊權；沒有邊權時就傳 sum\n}\n```\n:::\n\n\n\n:::example 答案：每個節點進來時就寫好，最後直接讀陣列\n**3. 讀答案。** 自頂向下的答案通常「進入節點當下就確定」，所以可以邊走邊寫進一個全域陣列，或在進入葉節點時收集。\n\n```cpp\n// 答案：每個節點進來時就寫好，最後直接讀陣列\n// dfs(root, -1, 起始狀態);\n// for (int i = 0; i < n; ++i) cout << rootSum[i];\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要傳 `fa`（或用 visited 標記）擋住回頭路，否則父子互相遞迴會無限迴圈。\n- 樹很深（接近一條鏈）時遞迴層數可能上萬，有爆 stack 風險；必要時改成用自己的堆疊模擬，或確認題目 n 的上限。\n- 每個節點、每條邊各處理一次，時間 `O(n)`，遞迴額外空間 `O(樹高)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「自頂向下 DFS」想：\n\n- **答案和「從根到該節點的路徑」有關**：到根距離、路徑累加值、沿途收集到的東西。資訊天然是「由上往下流」。\n- **子節點的答案可以由父節點的答案直接推出**：像 1376 的「下屬收到通知時刻 = 上司時刻 + 上司耗時」，1443 把開銷從根往各子節點傳。一旦發現「父算完，子就能算」，就是這個模式。\n- **暴力做法是對每個節點都重新從根爬一次**（一次 `O(深度)`，全部 `O(n²)`）；自頂向下一次 DFS 把累積值順手帶下去，攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「要算 u 的答案，只需要父親 fa 已算好的值 + 一條邊」\n```cpp\n// 你發現「要算 u 的答案，只需要父親 fa 已算好的值 + 一條邊」\n// ans[u] = ans[fa] + something;   // 就把 something 當參數往下傳\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要往下帶的「狀態」是什麼？（深度？路徑和？）\n2. 從父進到子時，這個狀態怎麼更新？\n3. 答案是在「進入節點當下」就能寫好，還是其實得等子樹算完（那就該用自底向上）？\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3535 | 單位轉換 II（Premium） | N/A | 放在「自頂向下 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1376 | [通知所有員工所需的時間](https://leetcode.cn/problems/time-needed-to-inform-all-employees/) | 1561 | 放在「自頂向下 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3528 | [單位轉換 I](https://leetcode.cn/problems/unit-conversion-i/) | 1580 | 放在「自頂向下 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1443 | [收集樹上所有蘋果的最少時間](https://leetcode.cn/problems/minimum-time-to-collect-all-apples-in-a-tree/) | 1683 | 放在「自頂向下 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3820 | [樹上的勾股距離節點](https://leetcode.cn/problems/pythagorean-distance-nodes-in-a-tree/) | 1725 | 放在「自頂向下 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3535 | [單位轉換 II](https://leetcode.cn/problems//unit-conversion-ii/) |  |\n| 1376 | [通知所有員工所需的時間](https://leetcode.cn/problems//time-needed-to-inform-all-employees/) | 1561 |\n| 3528 | [單位轉換 I](https://leetcode.cn/problems//unit-conversion-i/) | 1580 |\n| 1443 | [收集樹上所有蘋果的最少時間](https://leetcode.cn/problems//minimum-time-to-collect-all-apples-in-a-tree/) | 1683 |\n| 3820 | [樹上的勾股距離節點](https://leetcode.cn/problems//pythagorean-distance-nodes-in-a-tree/) | 1725 |\n| 1377 | [T 秒後青蛙的位置](https://leetcode.cn/problems//frog-position-after-t-seconds/) | 1824 |\n| 3067 | [在帶權樹網路中統計可連線伺服器對數目](https://leetcode.cn/problems//count-pairs-of-connectable-servers-in-a-weighted-tree-network/) | 1909 |\n| 3372 | [連線兩棵樹後最大目標節點數目 I](https://leetcode.cn/problems//maximize-the-number-of-target-nodes-after-connecting-trees-i/) | 1927 |\n| 2467 | [樹上最大得分和路徑](https://leetcode.cn/problems//most-profitable-path-in-a-tree/) | 2053 |\n| 3373 | [連線兩棵樹後最大目標節點數目 II](https://leetcode.cn/problems//maximize-the-number-of-target-nodes-after-connecting-trees-ii/) | 2162 |\n| 1766 | [互質樹](https://leetcode.cn/problems//tree-of-coprimes/) | 2232 |\n| 2791 | [樹中可以形成迴文的路徑數](https://leetcode.cn/problems//count-paths-that-can-form-a-palindrome-in-a-tree/) | 2677 |",
        },
        {
          id: 34,
          title: "3.3 自底向上 DFS",
          summary:
            "**原理講解**\n\n本節要解決的場景是：答案和「整棵子樹的彙總資訊」有關。也就是說，要算某個節點的結果，你得先知道它**所有子節點各自的子樹算出了什麼**，再把這些結果合併起來。例如「以某節點為根的子樹有多少節點」「子樹節點值的總和」「子樹高度」。\n\n這就是「自底向上 DFS」。它和自頂向下相反：自頂向下是「進入子節點前」就把祖先資訊算好傳下去；自底向上則是「子節點全部遞迴完、回來之後」才用子樹的結果算自己。換句話說，答案是在「回程（歸）」產生的——遞迴先一路鑽到葉子，再從葉子一層層往上彙總。\n\n:::example 自底向上：dfs 回傳「以 u 為根的子樹大小」\n先看骨架。和一般樹 DFS 一樣記住父節點防回頭，但這次讓 `dfs` **回傳一個值**——代表「這棵子樹的摘要」：\n\n```cpp\n// 自底向上：dfs 回傳「以 u 為根的子樹大小」\nint dfs(int u, int fa) {\n  int size = 1;  // 自己算 1 個\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    size += dfs(v, u);  // 先算完子樹，回來再加上去\n  }\n  return size;  // 把彙總結果交給父節點\n}\n```\n:::\n\n\n\n關鍵在最後一行：節點 `u` 必須等迴圈裡每個 `dfs(v, u)` 都回來，拿到所有子樹的摘要，才能算出自己的答案再回傳。這就是「自底向上」——資訊由葉往根流。\n\n:::example 合併不只是加：這裡把子樹的「字母計數表」累加進來\n合併時不一定只是相加，也可能取最大、計數、或更新一個全域答案。例如 3249「統計好節點的數目」要看「子樹中每個子節點的子樹大小是否都相同」，1519「子樹中標籤相同的節點數」要回傳一張「各字母出現次數」的表，由子往上累加：\n\n```cpp\n// 合併不只是加：這裡把子樹的「字母計數表」累加進來\narray<int, 26> dfs(int u, int fa) {\n  array<int, 26> cnt{};\n  cnt[label[u]]++;  // 算上自己這個字母\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    auto sub = dfs(v, u);  // 拿到子樹的計數表\n    for (int i = 0; i < 26; ++i) {\n      cnt[i] += sub[i];\n    }\n  }\n  ans[u] = cnt[label[u]];  // 答案：和自己同字母的數量\n  return cnt;\n}\n```\n:::\n\n\n\n每個節點只被遞迴一次，合併成本通常是常數或固定大小（如 26），整體 `O(n)`。\n\n**實作方式**\n\n把任何「自底向上」題目拆成三件事：子樹摘要是什麼、怎麼合併、答案在哪裡。\n\n:::example 摘要 = 子樹節點值總和（回傳一個 long long）\n**1. 決定「子樹摘要」——也就是 dfs 的回傳型別。** 問自己：「父節點要算它的答案，需要從每棵子樹拿到什麼？」是一個數字（大小、和、高度），還是一張表（計數）？這決定回傳型別。\n\n```cpp\n// 摘要 = 子樹節點值總和（回傳一個 long long）\nlong long dfs(int u, int fa) {\n  long long s = val[u];\n  // ... 遞迴累加子樹的和\n  return s;\n}\n```\n:::\n\n\n\n:::example 先收齊子樹結果，再合併\n**2. 遞迴完所有子節點後再合併。** 順序很重要：必須先把 `dfs(v, u)` 全部叫完、拿到回傳值，才能算自己。\n\n```cpp\n// 先收齊子樹結果，再合併\nfor (int v : g[u]) {\n  if (v == fa) {\n    continue;\n  }\n  long long sub = dfs(v, u);  // 子樹算好了\n  s += sub;                   // 合併進自己的摘要\n}\n```\n:::\n\n\n\n:::example 答案寫在 return 前；最終答案常在根：ans = dfs(root, -1);\n**3. 在「回來」的時機讀／寫答案。** 答案可能就是根的回傳值（如整棵樹的總和），也可能是「每個節點各自一個答案」——那就在合併完、return 之前寫進全域陣列。\n\n```cpp\n// 答案寫在 return 前；最終答案常在根：ans = dfs(root, -1);\nans[u] = computeFrom(s);  // 用彙總好的 s 算出 u 的答案\nreturn s;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 葉節點是遞迴終點：它沒有子節點，迴圈不執行，直接回傳「只含自己」的摘要——這就是 base case，不需特別寫 if。\n- 別忘了傳 `fa` 防回頭。\n- 回傳型別若是大物件（如 vector），每次複製會變慢；可改成傳參考、或回傳固定大小的 `array`。\n- 時間 `O(n × 單次合併成本)`，遞迴空間 `O(樹高)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「自底向上 DFS」想：\n\n- **要算某節點的答案，必須先知道它整棵子樹的資訊**：子樹大小、子樹和、子樹裡某種東西的數量。資訊天然「由下往上」彙總，像 690「員工的重要性」要把下屬的重要度全部加上來。\n- **答案是「每棵子樹各算一份，再合併」**：3249 要比較各子樹大小是否一致，3004「相同顏色的最大子樹」要回傳「子樹是否同色＋大小」，1519 由子往上累加字母計數。一看到「合併子結果」就是它。\n- **暴力做法是對每個節點都重新遍歷它整棵子樹**（一次最壞 `O(n)`，全部 `O(n²)`）；自底向上一次 DFS 讓每棵子樹只算一次，攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「u 的答案 = 合併(各個子節點 v 的子樹結果)」\n```cpp\n// 你發現「u 的答案 = 合併(各個子節點 v 的子樹結果)」\n// 就讓 dfs 回傳那個結果，在迴圈裡收齊後再算 u\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的 dfs 要回傳什麼「子樹摘要」？型別是數字還是表？\n2. 拿到每棵子樹的摘要後，我用什麼規則合併成自己的？\n3. 最終答案在根的回傳值，還是每個節點各一份？\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 690 | 員工的重要性 | N/A | 放在「自底向上 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3004 | 相同顏色的最大子樹（Premium） | N/A | 放在「自底向上 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3249 | [統計好節點的數目](https://leetcode.cn/problems/count-the-number-of-good-nodes/) | 1566 | 放在「自底向上 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1273 | [刪除樹節點（Premium）](https://leetcode.cn/problems/delete-tree-nodes/) | 1733 | 放在「自底向上 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1519 | [子樹中標籤相同的節點數](https://leetcode.cn/problems/number-of-nodes-in-the-sub-tree-with-the-same-label/) | 1809 | 放在「自底向上 DFS」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 690 | [員工的重要性](https://leetcode.cn/problems//employee-importance/) |  |\n| 3004 | [相同顏色的最大子樹](https://leetcode.cn/problems//maximum-subtree-of-the-same-color/) |  |\n| 3249 | [統計好節點的數目](https://leetcode.cn/problems//count-the-number-of-good-nodes/) | 1566 |\n| 1273 | [刪除樹節點](https://leetcode.cn/problems//delete-tree-nodes/) | 1733 |\n| 1519 | [子樹中標籤相同的節點數](https://leetcode.cn/problems//number-of-nodes-in-the-sub-tree-with-the-same-label/) | 1809 |\n| 3558 | [給邊賦權值的方案數 I](https://leetcode.cn/problems//number-of-ways-to-assign-edge-weights-i/) | 1845 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems//minimum-increments-to-equalize-leaf-paths/) | 1959 |\n| 2872 | [可以被 K 整除連通塊的最大數目](https://leetcode.cn/problems//maximum-number-of-k-divisible-components/) | 1968 |\n| 2477 | [到達首都的最少油耗](https://leetcode.cn/problems//minimum-fuel-cost-to-report-to-the-capital/) | 2012 |\n| 3812 | [翻轉樹上最少邊](https://leetcode.cn/problems//minimum-edge-toggles-on-a-tree/) | 2179 |\n| 2973 | [樹中每個節點放置的金幣數目](https://leetcode.cn/problems//find-number-of-coins-to-place-in-tree-nodes/) | 2277 |\n| 2440 | [建立價值相同的連通塊](https://leetcode.cn/problems//create-components-with-same-value/) | 2460 |",
        },
        {
          id: 35,
          title: "3.4 有遞有歸",
          summary:
            "**原理講解**\n\n本節要解決的場景是：一次 DFS 同時需要「往下帶的祖先資訊」**和**「往上彙總的子樹資訊」。前一節（自頂向下）只用下行的「遞」，再前一節（自底向上）只用回程的「歸」。但有些題目兩邊都要用到——進節點時要看祖先的狀態，出節點時又要把子樹結果交還給父。這種「下行帶一份、回程還一份」的寫法，就叫「有遞有歸」。\n\n:::example 一次呼叫的兩個時機：遞（往下前）與 歸（往下回來後）\n理解的關鍵，是看清一次遞迴呼叫其實有「兩個時間點」：**進入節點（遞）**和**離開節點（歸）**。同一個 `dfs(u)` 函式裡，遞迴呼叫子節點「之前」的程式碼，是在「遞」的時候跑；遞迴呼叫「之後」的程式碼，是在「歸」的時候跑：\n\n```cpp\n// 一次呼叫的兩個時機：遞（往下前）與 歸（往下回來後）\nvoid dfs(int u, int fa) {\n  // —— 這裡是「遞」：可用祖先資訊、做進入時的處理 ——\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    dfs(v, u);\n  }\n  // —— 這裡是「歸」：子樹都算完了，可彙總子樹、回填答案 ——\n}\n```\n:::\n\n\n\n:::example 有遞有歸：down 往下傳，回傳值往上彙總\n把這兩個時機都用上，就能做到「自頂向下傳一個值，自底向上回一個值」。例如 3331「修改後子樹的大小」：先在「遞」的時候沿著鏈往上找到「最近的非葉祖先」當作新父親（祖先資訊），再在「歸」的時候統計每棵子樹的大小（子樹彙總）。下面示意同一函式裡兩件事各自發生在哪：\n\n```cpp\n// 有遞有歸：down 往下傳，回傳值往上彙總\nint dfs(int u, int fa, int down) {  // down：從祖先帶來的狀態\n  int size = 1;                     // 「歸」要用的子樹大小\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    size += dfs(v, u, nextDown(down));  // 遞：更新後傳下；歸：累加回來\n  }\n  use(u, down);  // 用祖先資訊處理 u\n  return size;   // 把子樹摘要交還父節點\n}\n```\n:::\n\n\n\n每個節點仍只進出一次，時間 `O(n)`。\n\n**實作方式**\n\n把「有遞有歸」題目拆成：往下帶什麼、往上回什麼、各自在哪個時機處理。\n\n:::example 參數 down = 祖先資訊；回傳值 = 子樹摘要（例如路徑成本）\n**1. 同時定義「下傳參數」和「上傳回傳值」。** 下傳的放參數（祖先資訊），上傳的放回傳值（子樹摘要）。\n\n```cpp\n// 參數 down = 祖先資訊；回傳值 = 子樹摘要（例如路徑成本）\nlong long dfs(int u, int fa, long long down) {\n  // ...\n}\n```\n:::\n\n\n\n:::example 遞：進入子節點前更新下傳狀態\n**2.「遞」的時機：先用祖先資訊更新，再下傳。** 在 for 迴圈呼叫子節點之前，把 `down` 更新成子節點該看到的版本。\n\n```cpp\n// 遞：進入子節點前更新下傳狀態\nfor (int v : g[u]) {\n  if (v == fa) {\n    continue;\n  }\n  long long childAns = dfs(v, u, down + cost[u]);  // 更新後下傳\n  // childAns 是「歸」回來的結果，下一步合併\n}\n```\n:::\n\n\n\n:::example 歸：彙總子樹，算自己的答案後回傳\n**3.「歸」的時機：合併子樹結果、回填答案、再回傳。** 子節點都回來後，把它們的回傳值合併，算出自己的答案，最後 return 給父節點。\n\n```cpp\n// 歸：彙總子樹，算自己的答案後回傳\nlong long best = 0;\nfor (...) {\n  best = max(best, childAns);  // 合併各子樹\n}\nans[u] = best;          // 回填這個節點的答案\nreturn best + cost[u];  // 把摘要交還父節點\n```\n:::\n\n\n\n像 3593「使葉子路徑成本相等的最小增量」：遞時把「到目前的路徑成本」往下帶，歸時讓每個節點對齊各子分支的最大成本、累加需要的增量——正是兩個時機各做一件事。\n\n**4. 邊界與複雜度。**\n\n- 想清楚「這段程式該放在遞迴呼叫之前還之後」：放錯時機，要嘛拿不到祖先資訊，要嘛拿不到子樹結果。\n- 葉節點同時是「遞」的終點和「歸」的起點，記得它的回傳值要有正確的初始意義。\n- 一樣傳 `fa` 防回頭；時間 `O(n)`，空間 `O(樹高)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「有遞有歸」想：\n\n- **算一個節點同時要用到「上面（祖先）」和「下面（子樹）」的資訊**：只缺一邊用前兩節就夠，兩邊都缺才需要把遞、歸都用上。\n- **題目同時出現「沿路徑往下累積」和「對齊／彙總各子分支」兩種操作**：像 3593 要先有「往下的路徑成本」，又要在每點「讓各子分支成本相等」；3331 要先往上認新父親，又要往下統計子樹大小。\n- **暴力做法把「找祖先」和「掃子樹」拆成多次獨立遍歷**（容易變成 `O(n²)`）；有遞有歸用一次 DFS、在進出兩個時機各做一件事，攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現有些事「進節點時就要做」（要祖先），\n```cpp\n// 你發現有些事「進節點時就要做」（要祖先），\n// 有些事「子樹算完才能做」（要子結果）——一次 DFS 兩個時機分別處理\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我往下帶（遞）的祖先資訊是什麼？在哪裡更新它？\n2. 我往上回（歸）的子樹摘要是什麼？怎麼合併？\n3. 每段程式碼，到底該放在「遞迴呼叫之前」還「之後」？\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3331 | [修改後子樹的大小](https://leetcode.cn/problems/find-subtree-sizes-after-changes/) | 2046 | 放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems/minimum-increments-to-equalize-leaf-paths/) | 1959 | 放在「有遞有歸」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3331 | [修改後子樹的大小](https://leetcode.cn/problems//find-subtree-sizes-after-changes/) | 2046 |\n| 3593 | [使葉子路徑成本相等的最小增量](https://leetcode.cn/problems//minimum-increments-to-equalize-leaf-paths/) | 1959 |",
        },
        {
          id: 36,
          title: "3.5 樹的直徑",
          summary:
            "**原理講解**\n\n本節要解決的場景是：求一棵樹的「直徑」——也就是**樹上任意兩個節點之間最長的那條路徑**（長度可以算邊數，或邊權總和）。例如一個公司組織圖、一張道路樹狀網，問「相隔最遠的兩點距離是多少」，就是直徑問題。\n\n直觀但慢的做法是枚舉所有點對算距離，太貴。聰明的做法只要**一次自底向上 DFS**。關鍵觀察是：任何一條最長路徑，一定有一個「最高點」——路徑上離根最近的那個節點。在那個最高點，這條路徑是「往左子分支下去最長 + 往右（另一）子分支下去最長」拼起來的。所以我們只要在每個節點，算出「從它往下能延伸的最長兩條鏈」，把它們相加，就是「以該節點為最高點的最長路徑」；對所有節點取最大，就是直徑。\n\n:::example 往下最長鏈：u 朝子樹方向能延伸的最大長度（以邊數計）\n先定義「往下最長鏈」：節點 `u` 往子樹方向，最遠能走多長。它等於「子節點裡最大的往下鏈 + 1」：\n\n```cpp\n// 往下最長鏈：u 朝子樹方向能延伸的最大長度（以邊數計）\nint down(int u, int fa) {\n  int best = 0;  // 沒有子節點就是 0\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    best = max(best, down(v, u) + 1);\n  }\n  return best;\n}\n```\n:::\n\n\n\n:::example 在每個節點，把最長 + 次長兩條往下鏈拼成一條穿過 u 的路徑\n但只算「最長一條」不夠，直徑要「最長兩條接起來」。所以遍歷子節點時，順手記住「最長」和「次長」兩條往下鏈，相加更新全域答案：\n\n```cpp\n// 在每個節點，把最長 + 次長兩條往下鏈拼成一條穿過 u 的路徑\nint ans = 0;  // 全域：目前最大直徑（邊數）\nint dfs(int u, int fa) {\n  int max1 = 0, max2 = 0;  // 最長、次長往下鏈\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    int d = dfs(v, u) + 1;\n    if (d > max1) {\n      max2 = max1, max1 = d;\n    } else if (d > max2) {\n      max2 = d;\n    }\n  }\n  ans = max(ans, max1 + max2);  // 穿過 u 的最長路徑\n  return max1;                  // 往上只回傳「一條」最長鏈\n}\n```\n:::\n\n\n\n注意回傳值只回「一條」（`max1`），因為對父節點而言，只能接續一條往下鏈；但更新答案時用「兩條相加」。整體一次 DFS，`O(n)`。\n\n**實作方式**\n\n把任何直徑題拆成：往下鏈是什麼、怎麼在每點拼兩條、答案在哪。\n\n:::example 帶權版：往下鏈加的是邊權 w，而非固定 +1\n**1.「往下鏈」的定義與回傳。** dfs 回傳「以 u 為頂、往子樹方向的最長鏈」。無權樹就是「子節點最長鏈 +1」；帶權樹則加上邊權。\n\n```cpp\n// 帶權版：往下鏈加的是邊權 w，而非固定 +1\nlong long d = dfs(v, u) + w;  // w 是邊 (u,v) 的權重\n```\n:::\n\n\n\n:::example 維護最長 max1、次長 max2\n**2. 每個節點記「最長＋次長」並更新答案。** 直徑＝穿過某節點的最長路徑＝該節點兩條最長往下鏈之和。逐一比較維護 max1、max2。\n\n```cpp\n// 維護最長 max1、次長 max2\nif (d > max1) {\n  max2 = max1;\n  max1 = d;\n} else if (d > max2) {\n  max2 = d;\n}\nans = max(ans, max1 + max2);  // 拼起來嘗試刷新直徑\n```\n:::\n\n\n\n:::example 給父節點的，是單條最長往下鏈\n**3. 往上只回傳一條鏈。** 對父節點，u 只能貢獻「一條」往下鏈（不能同時用兩條，否則路徑會分岔），所以 `return max1`。\n\n```cpp\n// 給父節點的，是單條最長往下鏈\nreturn max1;\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 直徑用「邊數」還是「節點數」要對齊題意：邊數版葉子回傳 0，節點數版葉子回傳 1，差一要小心。\n- 把全域 `ans` 初始化好（0），單節點樹答案就是 0。\n- 像 2246「相鄰字元不同的最長路徑」是直徑的變形：只有「子節點字元 ≠ 自己」時那條往下鏈才能接續，否則視為 0；3203「合併兩棵樹後的最小直徑」要先各自求直徑再分析合併點。\n- 時間 `O(n)`，空間 `O(樹高)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「樹的直徑」想：\n\n- **問「樹上最遠兩點的距離」或「最長的一條路徑」**：1245「樹的直徑」是裸題，2246 是「最長路徑」加上字元限制的變形。\n- **路徑可以是任意兩點、不一定經過根**：這正是要用「每個節點都當一次最高點、拼最長＋次長兩條鏈」的理由。\n- **暴力做法是枚舉所有點對、各自 BFS 求距離**（`O(n²)` 甚至更糟）；直徑技巧用一次 DFS 攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「最長路徑 = 某節點往下最長的兩條鏈接起來」\n```cpp\n// 你發現「最長路徑 = 某節點往下最長的兩條鏈接起來」\n// 就在每個節點維護 max1、max2，ans = max(ans, max1 + max2)\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「往下鏈」怎麼定義？葉子回傳 0 還是 1？帶不帶權？\n2. 在每個節點，我有沒有同時維護「最長」與「次長」兩條鏈？\n3. 為什麼往上只能回傳「一條」鏈，而更新答案卻用「兩條相加」？\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3313 | 查詢樹中最後標記的節點（Premium） | N/A | 放在「樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3787 | 查詢樹的直徑端點（Premium） | N/A | 放在「樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1245 | [樹的直徑（Premium）](https://leetcode.cn/problems/tree-diameter/) | 1792 | 放在「樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2246 | [相鄰字元不同的最長路徑](https://leetcode.cn/problems/longest-path-with-different-adjacent-characters/) | 2126 | 放在「樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3203 | [合併兩棵樹後的最小直徑](https://leetcode.cn/problems/find-minimum-diameter-after-merging-two-trees/) | 2266 | 放在「樹的直徑」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3313 | [查詢樹中最後標記的節點](https://leetcode.cn/problems//find-the-last-marked-nodes-in-tree/) |  |\n| 3787 | [查詢樹的直徑端點](https://leetcode.cn/problems//find-diameter-endpoints-of-a-tree/) |  |\n| 1245 | [樹的直徑](https://leetcode.cn/problems//tree-diameter/) | 1792 |\n| 2246 | [相鄰字元不同的最長路徑](https://leetcode.cn/problems//longest-path-with-different-adjacent-characters/) | 2126 |\n| 3203 | [合併兩棵樹後的最小直徑](https://leetcode.cn/problems//find-minimum-diameter-after-merging-two-trees/) | 2266 |\n| 1617 | [統計子樹中城市之間最大距離](https://leetcode.cn/problems//count-subtrees-with-max-distance-between-cities/) | 2309 |\n| 2538 | [最大價值和與最小价值和的差值](https://leetcode.cn/problems//difference-between-maximum-and-minimum-price-sum/) | 2398 |",
        },
        {
          id: 37,
          title: "3.6 樹的拓撲排序",
          summary:
            "**原理講解**\n\n本節要解決的場景是：題目給的是「無根樹」（節點和邊，沒指定誰是根），而我們想從「最外圈」一層層往內處理。最典型的例子是 310「最小高度樹」：要找哪些節點當根時整棵樹最矮——答案就是把葉子一圈圈剝掉後，最後剩下的 1～2 個中心點。\n\n這裡借用了圖論裡「拓撲排序」的想法。一般拓撲排序處理「有向圖、按入度為 0 的順序逐個移除」。在無根樹上，我們關注的是**度數為 1 的節點，也就是葉子**：葉子只連一條邊，是最容易先處理掉的；剝掉一個葉子，它的鄰居度數會減 1，可能變成新的葉子。如此一層層往內收，就像剝洋蔥。\n\n:::example 算度數，把所有葉子（度為 1）放進佇列當起點\n先看怎麼算「度數」並找出初始葉子。度數 = 一個節點連了幾條邊；度數為 1 的就是葉子：\n\n```cpp\n// 算度數，把所有葉子（度為 1）放進佇列當起點\nvector<int> deg(n, 0);\nfor (auto& e : edges) {\n  deg[e[0]]++;\n  deg[e[1]]++;\n}\nqueue<int> q;\nfor (int i = 0; i < n; ++i) {\n  if (deg[i] == 1) {\n    q.push(i);  // 一開始的最外圈葉子\n  }\n}\n```\n:::\n\n\n\n:::example 一層層剝葉子：把當前所有葉子同時移除，產生下一圈葉子\n接著像 BFS 一樣，一整層一整層地剝。每剝掉一個葉子，就把它鄰居的度數減 1；鄰居度數掉到 1，代表它變成新葉子，加入下一層：\n\n```cpp\n// 一層層剝葉子：把當前所有葉子同時移除，產生下一圈葉子\nwhile (!q.empty()) {\n  int sz = q.size();  // 這一層的葉子數\n  for (int i = 0; i < sz; ++i) {\n    int u = q.front();\n    q.pop();\n    for (int v : g[u]) {\n      if (--deg[v] == 1) {  // 鄰居度數降到 1 → 新葉子\n        q.push(v);\n      }\n    }\n  }\n}\n```\n:::\n\n\n\n每個節點、每條邊各被處理一次，複雜度 `O(n)`。\n\n**實作方式**\n\n把「樹的拓撲排序（剝葉子）」拆成：建圖算度、找初始葉、逐層剝除、讀答案。\n\n:::example 建圖 + 度數\n**1. 建鄰接表並統計度數。** 用 `g[u]` 存鄰居，用 `deg[u]` 存度數，兩者同步建立。\n\n```cpp\n// 建圖 + 度數\nvector<vector<int>> g(n);\nfor (auto& e : edges) {\n  g[e[0]].push_back(e[1]);\n  g[e[1]].push_back(e[0]);\n  deg[e[0]]++;\n  deg[e[1]]++;\n}\n```\n:::\n\n\n\n:::example 初始最外圈\n**2. 把所有初始葉子（度為 1）入列。** 這是「最外圈」，剝洋蔥的第一層。\n\n```cpp\n// 初始最外圈\nfor (int i = 0; i < n; ++i) {\n  if (deg[i] == 1) {\n    q.push(i);\n  }\n}\n```\n:::\n\n\n\n:::example 逐層剝除，remaining 追蹤還沒被剝掉的節點數\n**3. 一層一層剝，並記住「還剩幾個節點」。** 每剝一層就更新剩餘節點數；當剩餘 ≤ 2 時，剩下的就是中心。\n\n```cpp\n// 逐層剝除，remaining 追蹤還沒被剝掉的節點數\nint remaining = n;\nwhile (remaining > 2) {\n  int sz = q.size();\n  remaining -= sz;  // 這一層整層剝掉\n  for (int i = 0; i < sz; ++i) {\n    int u = q.front();\n    q.pop();\n    for (int v : g[u]) {\n      if (--deg[v] == 1) {\n        q.push(v);\n      }\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 剩在佇列裡的就是中心（1 或 2 個）\n**4. 讀答案、處理邊界。** 佇列裡最後剩下的就是答案（最小高度樹的根）。\n\n```cpp\n// 剩在佇列裡的就是中心（1 或 2 個）\nvector<int> ans;\nwhile (!q.empty()) {\n  ans.push_back(q.front());\n  q.pop();\n}\n```\n:::\n\n\n\n- 邊界：`n == 1` 時沒有邊，唯一節點自己就是答案；`n == 2` 時兩個都是答案——記得先處理，否則迴圈條件可能漏掉。\n- 一定要「整層一起剝」（記 `sz`），不能一個一個剝到底，否則分不出洋蔥的層次。\n- 像 2603「收集樹中金幣」也用同樣手法先剝掉「沒有金幣的葉子」化簡樹，再處理核心。時間 `O(n)`，空間 `O(n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「樹的拓撲排序（剝葉子）」想：\n\n- **要從「最外層葉子」往內逐層處理**，或要找「樹的中心 / 最小高度根」：310 是裸題，剝到最後剩的就是中心。\n- **題目允許『先把對答案沒影響的葉子刪掉』來化簡**：像 2603 先移除無金幣的葉子，3812「翻轉樹上最少邊」也常從葉子端開始定序處理。\n- **暴力做法是『以每個節點為根各跑一次 DFS 求高度』**（`O(n²)`）；剝葉子用一次層序剝除攤平成 `O(n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「度數為 1 的節點最好先處理，處理完鄰居度數會變」\n```cpp\n// 你發現「度數為 1 的節點最好先處理，處理完鄰居度數會變」\n// 就用佇列裝葉子，--deg[v]==1 時把新葉子加進來\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「葉子」條件是什麼？（度數 == 1）初始葉子怎麼找？\n2. 剝掉一個葉子後，要怎麼更新鄰居、判斷誰變成新葉子？\n3. 停止條件是什麼？（剩 ≤ 2 個？剝完所有可剝的？）邊界 n=1、n=2 處理了嗎？\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 310 | 最小高度樹 | N/A | 放在「樹的拓撲排序」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3812 | [翻轉樹上最少邊](https://leetcode.cn/problems/minimum-edge-toggles-on-a-tree/) | 2179 | 放在「樹的拓撲排序」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2603 | [收集樹中金幣](https://leetcode.cn/problems/collect-coins-in-a-tree/) | 2712 | 放在「樹的拓撲排序」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 310 | [最小高度樹](https://leetcode.cn/problems//minimum-height-trees/) |  |\n| 3812 | [翻轉樹上最少邊](https://leetcode.cn/problems//minimum-edge-toggles-on-a-tree/) | 2179 |\n| 2603 | [收集樹中金幣](https://leetcode.cn/problems//collect-coins-in-a-tree/) | 2712 |",
        },
        {
          id: 38,
          title: "3.7 DFS 時間戳",
          summary:
            "**原理講解**\n\n本節要解決的場景是：常常要問「節點 `x` 是不是在以 `y` 為根的子樹裡？」或「把整棵子樹當成一個整體去做區間操作（求和、加值）」。如果每次都重新 DFS 一遍子樹去判斷，太慢。DFS 時間戳（也叫歐拉序、DFS 序）能把「子樹」這個概念，變成陣列上的一段**連續區間**，於是子樹問題就轉化成大家熟悉的區間問題。\n\n核心想法：用一個全域計數器 `clock`，在 DFS **進入**每個節點時記下「進入時間」`in[u]`，在**離開**（子樹全部走完）時記下「離開時間」`out[u]`。因為 DFS 是「進去後一定把整棵子樹走完才出來」，所以一棵子樹裡所有節點的進入時間，必定落在 `[in[u], out[u]]` 這個區間內——子樹和區間於是一一對應。\n\n:::example DFS 時間戳：進入記 in，離開記 out\n先看怎麼打時間戳：\n\n```cpp\n// DFS 時間戳：進入記 in，離開記 out\nint clk = 0;\nvoid dfs(int u, int fa) {\n  in[u] = ++clk;  // 進入 u 的時刻\n  for (int v : g[u]) {\n    if (v == fa) {\n      continue;\n    }\n    dfs(v, u);\n  }\n  out[u] = clk;  // 子樹走完，離開時刻\n}\n```\n:::\n\n\n\n:::example x 是否在 y 的子樹中：看 x 的進入時間是否落在 y 的區間內\n打完戳後，「x 在 y 的子樹裡」就只是一個區間包含判斷，`O(1)`：\n\n```cpp\n// x 是否在 y 的子樹中：看 x 的進入時間是否落在 y 的區間內\nbool inSubtree(int x, int y) { return in[y] <= in[x] && in[x] <= out[y]; }\n```\n:::\n\n\n\n更強的是：如果每個節點還有一個值，按 `in` 順序把值排成一個陣列，那「子樹 y 的所有值」就是這個陣列上的連續區間 `[in[y], out[y]]`。要「子樹求和」「子樹整體加值」，就接上樹狀陣列或線段樹，把樹問題完全變成區間問題。一次 DFS 打戳 `O(n)`，之後每次查詢 `O(1)` 或 `O(log n)`。\n\n**實作方式**\n\n把任何「DFS 時間戳」題拆成：打戳、子樹轉區間、接資料結構、讀答案。\n\n:::example in[u] 是子樹區間左端，out[u] 是右端\n**1. 一次 DFS 打進入／離開時間戳。** 用 `in[u]`、`out[u]` 把每個子樹對應到一段連續區間。\n\n```cpp\n// in[u] 是子樹區間左端，out[u] 是右端\nin[u] = ++clk;\nfor (int v : g[u]) {\n  if (v != fa) {\n    dfs(v, u);\n  }\n}\nout[u] = clk;\n```\n:::\n\n\n\n:::example 攤平：base[in[u]] 對應節點 u 的值\n**2. 把節點值按 in 順序攤平成陣列。** 第 `in[u]` 個位置放節點 u 的值，於是子樹 = 陣列上的 `[in[u], out[u]]`。\n\n```cpp\n// 攤平：base[in[u]] 對應節點 u 的值\nvector<long long> base(n + 1);\nfor (int u = 0; u < n; ++u) {\n  base[in[u]] = val[u];\n}\n```\n:::\n\n\n\n:::example 子樹求和 = 區間求和（fen 為樹狀陣列）\n**3. 接上區間資料結構處理子樹操作。** 子樹求和 / 子樹加值，就用樹狀陣列或線段樹在區間 `[in[u], out[u]]` 上做。\n\n```cpp\n// 子樹求和 = 區間求和（fen 為樹狀陣列）\nlong long subtreeSum(int u) {\n  return fen.query(in[u], out[u]);  // 整棵子樹一次搞定\n}\n```\n:::\n\n\n\n**4. 子樹包含判斷與邊界。** 「x 在 y 子樹中」用區間包含 `O(1)`；判斷祖先關係、做某些 LCA 也可借時間戳。\n\n- `in`/`out` 用同一個 `clk`：本寫法 `out[u]` 取「子樹最後一個進入時間」，故用 `<=` 判包含；若改成「離開時 ++clk」則邊界用法不同，要前後一致。\n- 像 2322「從樹中刪除邊的最小分數」用時間戳快速判「兩刪邊形成的子樹是否互相包含」；3515「帶權樹中的最短路徑」把路徑更新轉成子樹區間更新。\n- 打戳 `O(n)`；查詢視所接結構為 `O(1)`～`O(log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「DFS 時間戳」想：\n\n- **反覆問「某節點是否在某子樹內」或「某兩節點誰是誰的祖先」**：用 in/out 區間包含一次判斷，2322 判子樹互含就是典型。\n- **要對「整棵子樹」做求和、加值、計數等操作**：把子樹轉成連續區間後，直接套樹狀陣列／線段樹，如 3515 的路徑（子樹）更新。\n- **暴力做法是每次查詢都重新 DFS 掃一遍子樹**（每次 `O(n)`）；時間戳把子樹變區間，查詢攤平成 `O(1)` 或 `O(log n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「子樹」一直被當作整體反覆操作／查詢\n```cpp\n// 你發現「子樹」一直被當作整體反覆操作／查詢\n// 就打 in/out 時間戳，把子樹變成陣列上的 [in[u], out[u]]\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有用同一個計數器正確記下每個節點的 in 與 out？\n2. 子樹 u 對應到陣列上的哪一段區間？端點怎麼取（`<=` 還是 `<`）？\n3. 子樹操作要接什麼結構？（純包含判斷？還是要區間求和／加值？）\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 5 | LCP 05. 發 LeetCoin | N/A | 放在「DFS 時間戳」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2322 | [從樹中刪除邊的最小分數](https://leetcode.cn/problems/minimum-score-after-removals-on-a-tree/) | 2392 | 放在「DFS 時間戳」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3327 | [判斷 DFS 字串是否是迴文串](https://leetcode.cn/problems/check-if-dfs-strings-are-palindromes/) | 2454 | 放在「DFS 時間戳」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3515 | [帶權樹中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-weighted-tree/) | 2312 | 放在「DFS 時間戳」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum/) | 2646 | 放在「DFS 時間戳」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 5 | [LCP 05. 發 LeetCoin](https://leetcode.cn/problems//coin-bonus/) |  |\n| 2322 | [從樹中刪除邊的最小分數](https://leetcode.cn/problems//minimum-score-after-removals-on-a-tree/) | 2392 |\n| 3327 | [判斷 DFS 字串是否是迴文串](https://leetcode.cn/problems//check-if-dfs-strings-are-palindromes/) | 2454 |\n| 3515 | [帶權樹中的最短路徑](https://leetcode.cn/problems//shortest-path-in-a-weighted-tree/) | 2312 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems//kth-smallest-path-xor-sum/) | 2646 |",
        },
        {
          id: 39,
          title: "3.8 最近公共祖先（LCA）、倍增演算法",
          summary:
            "**原理講解**\n\n本節要解決的場景是：在一棵有根樹上反覆問「節點 `x` 和節點 `y` 的最近公共祖先（LCA）是誰？」或「x 往上走第 `k` 步到哪個祖先？」。LCA 就是「同時是 x 和 y 祖先、而且離它們最近的那個節點」——直觀講，是兩個節點往上走、最先碰頭的地方。\n\n最樸素的做法是讓較深的點先往上爬，直到兩點同深度，再一起一步步往上走到相遇。但「一步步走」最壞要 `O(n)`，查詢多了就太慢。「倍增（binary lifting）」的核心想法是：**預先記下每個節點往上 1、2、4、8…步（2 的次方步）分別到哪**，這樣任何往上 k 步都能拆成幾個 2 的次方相加，用跳的，`O(log n)` 完成。\n\n:::example 倍增預處理：up[u][j] = u 往上 2^j 步的祖先\n先看怎麼預處理這張「祖先跳表」。`up[u][j]` 表示「u 往上跳 2^j 步到達的節點」。關鍵遞推：跳 2^j 步 = 先跳 2^(j-1) 步，再從那裡跳 2^(j-1) 步：\n\n```cpp\n// 倍增預處理：up[u][j] = u 往上 2^j 步的祖先\n// up[u][0] = parent[u]（先由一次 DFS 求出 parent 與 depth）\nfor (int j = 1; j < LOG; ++j) {\n  for (int u = 0; u < n; ++u) {\n    int mid = up[u][j - 1];                      // 先跳 2^(j-1)\n    up[u][j] = (mid < 0) ? -1 : up[mid][j - 1];  // 再跳 2^(j-1)\n  }\n}\n```\n:::\n\n\n\n:::example 往上 k 步：按 k 的二進位逐位跳\n有了跳表，「往上 k 步」就把 k 寫成二進位，哪一位是 1 就跳對應的 2^j 步。1483「樹節點的第 K 個祖先」正是直接用這招：\n\n```cpp\n// 往上 k 步：按 k 的二進位逐位跳\nint kthAncestor(int u, int k) {\n  for (int j = 0; j < LOG && u >= 0; ++j) {\n    if (k >> j & 1) {\n      u = up[u][j];  // 第 j 位是 1 就跳 2^j\n    }\n  }\n  return u;\n}\n```\n:::\n\n\n\n求 LCA 就分兩步：先把深的點跳到和淺的點同深度，再讓兩點一起「能跳就跳、但不跳過頭」，最後它們的父節點就是 LCA。預處理 `O(n log n)`，每次查詢 `O(log n)`。\n\n**實作方式**\n\n把任何 LCA / 倍增題拆成：求 parent 與 depth、建跳表、跳同深度、一起跳到 LCA。\n\n:::example DFS 求 parent（即 up[u][0]）與 depth\n**1. 一次 DFS 求每點的父節點與深度。** 這是倍增的基礎資料。\n\n```cpp\n// DFS 求 parent（即 up[u][0]）與 depth\nvoid dfs(int u, int fa, int d) {\n  up[u][0] = fa;\n  depth[u] = d;\n  for (int v : g[u]) {\n    if (v != fa) {\n      dfs(v, u, d + 1);\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 由小到大填每個 2^j 級別\n**2. 用遞推填滿整張跳表。** 如前述：先跳一半，再跳一半。\n\n```cpp\n// 由小到大填每個 2^j 級別\nfor (int j = 1; j < LOG; ++j) {\n  for (int u = 0; u < n; ++u) {\n    up[u][j] = up[u][j - 1] < 0 ? -1 : up[up[u][j - 1]][j - 1];\n  }\n}\n```\n:::\n\n\n\n:::example 把 u 往上抬到與目標同深度\n**3. 查詢時先把較深的點提到同深度。** 用「往上 k 步」把深度差補平。\n\n```cpp\n// 把 u 往上抬到與目標同深度\nint lift(int u, int k) {  // k = depth 差\n  for (int j = 0; j < LOG; ++j) {\n    if (k >> j & 1) {\n      u = up[u][j];\n    }\n  }\n  return u;\n}\n```\n:::\n\n\n\n:::example 同深度後一起跳：能不相遇就跳，最後父節點是 LCA\n**4. 兩點一起往上跳，求 LCA。** 從大步到小步，若兩點跳完仍不同才跳（避免跳過頭）；結束時父節點即 LCA。\n\n```cpp\n// 同深度後一起跳：能不相遇就跳，最後父節點是 LCA\nint lca(int a, int b) {\n  if (depth[a] < depth[b]) {\n    swap(a, b);\n  }\n  a = lift(a, depth[a] - depth[b]);\n  if (a == b) {\n    return a;\n  }\n  for (int j = LOG - 1; j >= 0; j--) {\n    if (up[a][j] != up[b][j]) {\n      a = up[a][j];\n      b = up[b][j];\n    }\n  }\n  return up[a][0];\n}\n```\n:::\n\n\n\n- `LOG` 要夠大：滿足 `2^LOG >= n`（取 `ceil(log2(n)) + 1`），否則跳不夠高。\n- 跳到根時 parent 用 `-1` 表示，跳轉前要判，避免越界。\n- 像 2646「最小化旅行的價格總和」用 LCA 拆解每條路徑、統計節點被經過次數；3553「包含給定路徑的最小帶權子樹 II」也靠 LCA 定位路徑交點。預處理 `O(n log n)`，每查 `O(log n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「LCA / 倍增」想：\n\n- **反覆查「兩點的最近公共祖先」或「某點的第 k 個祖先」**：1483 是「第 K 祖先」裸題，許多路徑題要先用 LCA 找交會點。\n- **要把「樹上 x 到 y 的路徑」拆解**：x→y 的路徑 = x→LCA 加 LCA→y，2646、3553、3559 都靠這個拆法統計或求權。\n- **暴力做法是每次查詢都一步步往上爬**（每查 `O(n)`，查詢多就爆）；倍增預處理一次後每查 `O(log n)`。\n- 嗅到味道的瞬間：\n\n:::example 你發現「要往上跳很多步、而且要跳很多次」\n```cpp\n// 你發現「要往上跳很多步、而且要跳很多次」\n// 就預存 2^j 步跳表，把任意步數拆成二進位來跳\n```\n:::\n\n\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有先用一次 DFS 求出每點的 parent 與 depth？\n2. 我的跳表 `up[u][j]` 遞推對不對？LOG 夠大嗎？跳到根的 -1 有處理嗎？\n3. 求 LCA 時，我有沒有「先補平深度，再一起跳到不相遇為止」？\n\n三題都答得清楚，這個模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2277 | 樹中最接近路徑的節點（Premium） | N/A | 放在「最近公共祖先（LCA）、倍增演算法」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1483 | [樹節點的第 K 個祖先](https://leetcode.cn/problems/kth-ancestor-of-a-tree-node/) | 2115 | 放在「最近公共祖先（LCA）、倍增演算法」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3559 | [給邊賦權值的方案數 II](https://leetcode.cn/problems/number-of-ways-to-assign-edge-weights-ii/) | 2146 | 放在「最近公共祖先（LCA）、倍增演算法」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2646 | [最小化旅行的價格總和](https://leetcode.cn/problems/minimize-the-total-price-of-the-trips/) | 2238 | 放在「最近公共祖先（LCA）、倍增演算法」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3553 | [包含給定路徑的最小帶權子樹 II](https://leetcode.cn/problems/minimum-weighted-subgraph-with-the-required-paths-ii/) | 2411 | 放在「最近公共祖先（LCA）、倍增演算法」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2277 | [樹中最接近路徑的節點](https://leetcode.cn/problems//closest-node-to-path-in-tree/) |  |\n| 1483 | [樹節點的第 K 個祖先](https://leetcode.cn/problems//kth-ancestor-of-a-tree-node/) | 2115 |\n| 3559 | [給邊賦權值的方案數 II](https://leetcode.cn/problems//number-of-ways-to-assign-edge-weights-ii/) | 2146 |\n| 2646 | [最小化旅行的價格總和](https://leetcode.cn/problems//minimize-the-total-price-of-the-trips/) | 2238 |\n| 3553 | [包含給定路徑的最小帶權子樹 II](https://leetcode.cn/problems//minimum-weighted-subgraph-with-the-required-paths-ii/) | 2411 |\n| 3585 | [樹中找到帶權中位節點](https://leetcode.cn/problems//find-weighted-median-node-in-tree/) | 2429 |\n| 3534 | [針對圖的路徑存在性查詢 II](https://leetcode.cn/problems//path-existence-queries-in-a-graph-ii/) | 2507 |\n| 2846 | [邊權重均等查詢](https://leetcode.cn/problems//minimum-edge-weight-equilibrium-queries-in-a-tree/) | 2508 |\n| 2836 | [在傳球遊戲中最大化函式值](https://leetcode.cn/problems//maximize-value-of-function-in-a-ball-passing-game/) | 2769 |\n| 3464 | [正方形上的點之間的最大距離](https://leetcode.cn/problems//maximize-the-distance-between-points-on-a-square/) | 2806 |",
        },
        {
          id: 55,
          title: "3.9 虛樹",
          summary:
            "**原理講解**\n\n本節要解決的場景是：一棵很大的樹（節點上萬、上百萬），但每次題目其實只「關心」其中少數幾個特殊節點（稱為關鍵點）。如果每次查詢都把整棵樹掃一遍，會慢到無法接受。「虛樹（virtual tree）」這個技巧的目的，就是只用這些關鍵點和它們之間的「轉折點」，臨時搭出一棵很小的樹，在小樹上算答案。\n\n先想清楚：兩個關鍵點之間的路徑，會在某個「最近公共祖先（LCA，兩個節點往上走第一次相遇的點）」匯合。所有關鍵點之間的 LCA，就是路徑上真正重要的「岔路口」。虛樹的點集合 = 所有關鍵點 + 它們兩兩之間的 LCA。\n\n:::example 虛樹的點集合 = 關鍵點 + 它們在 DFS 序下相鄰兩個的 LCA\n關鍵的事實是：**就算原樹有上百萬個點，虛樹的點數最多只有關鍵點數量的兩倍。** 所以原本 `O(n)` 的處理，可以縮到 `O(k)`（k 是關鍵點數），快非常多。\n\n```cpp\n// 虛樹的點集合 = 關鍵點 + 它們在 DFS 序下相鄰兩個的 LCA\nsort(key.begin(), key.end(), [&](int a, int b) {\n  return dfn[a] < dfn[b];  // dfn：DFS 進入順序（時間戳）\n});\nfor (int i = 0; i + 1 < key.size(); ++i) {\n  key.push_back(lca(key[i], key[i + 1]));  // 補進轉折點\n}\n```\n:::\n\n\n\n:::example 排序去重後，剩下的就是虛樹所有節點\n為什麼要照 DFS 序排？因為在 DFS 序裡相鄰的兩個關鍵點，它們的 LCA 已經涵蓋所有需要的岔路口，不必兩兩都算，省下大量計算。\n\n```cpp\n// 排序去重後，剩下的就是虛樹所有節點\nsort(key.begin(), key.end(), [&](int a, int b) { return dfn[a] < dfn[b]; });\nkey.erase(unique(key.begin(), key.end()), key.end());\n// 之後用單調棧把這些點按祖先關係連成一棵小樹\n```\n:::\n\n\n\n像 3786「樹組的互動代價總和」這種題，分組後每組只在乎自己那幾個節點，虛樹讓我們對每組獨立、快速地計算。\n\n**實作方式**\n\n虛樹的建構是固定套路，拆成幾步：\n\n:::example 預處理：記錄 dfn、深度、倍增祖先表\n**1. 預處理：DFS 序 + LCA。** 先對原樹做一次 DFS，記下每個點的進入時間戳 `dfn` 與深度，並建好可以 `O(log n)` 查 LCA 的倍增表（倍增：預存「往上跳 1, 2, 4, 8… 步」的祖先，加快往上找）。\n\n```cpp\n// 預處理：記錄 dfn、深度、倍增祖先表\nvoid dfs(int u, int fa) {\n  dfn[u] = ++clk;\n  dep[u] = dep[fa] + 1;\n  up[u][0] = fa;\n  for (int k = 1; k < LOG; ++k) {\n    up[u][k] = up[up[u][k - 1]][k - 1];\n  }\n  for (int v : g[u]) {\n    if (v != fa) {\n      dfs(v, u);\n    }\n  }\n}\n```\n:::\n\n\n\n**2. 收集點集合。** 把關鍵點按 `dfn` 排序，相鄰兩個求 LCA 加進來，再排序去重（如上方原理的程式碼）。\n\n:::example 單調棧建虛樹：棧裡是當前的一條祖先鏈\n**3. 用單調棧連邊。** 依 `dfn` 順序處理每個點，維護一個「目前這條根到當前點的鏈」的棧。新點若不在棧頂的子樹內，就把棧頂彈出並連邊，直到能掛上去。\n\n```cpp\n// 單調棧建虛樹：棧裡是當前的一條祖先鏈\nstk[top = 0] = key[0];\nfor (int i = 1; i < key.size(); ++i) {\n  int u = key[i], l = lca(stk[top], u);\n  while (top && dep[stk[top - 1]] >= dep[l]) {  // 彈出比 l 還深的\n    vt[stk[top - 1]].push_back(stk[top]);\n    top--;  // 連虛樹邊\n  }\n  if (stk[top] != l) {\n    vt[l].push_back(stk[top]);\n    stk[top] = l;\n  }\n  stk[++top] = u;\n}\nwhile (top) {\n  vt[stk[top - 1]].push_back(stk[top]);\n  top--;\n}\n```\n:::\n\n\n\n**4. 在虛樹上跑你的演算法、再清空。** 虛樹建好後，就在它上面做樹形 DP 或彙總。多次查詢時務必只清空用到的點（不要 memset 整棵原樹），否則複雜度又退回 `O(n)`。\n\n複雜度：每次查詢 `O(k log k)`（排序 + LCA），總和遠小於對整棵樹硬做。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，可以往虛樹想：\n\n- **樹很大，但每次詢問只牽涉一小撮特殊節點**，而且所有詢問的節點數加起來不算太多。這是虛樹最典型的觸發點。\n- **答案只跟「關鍵點之間的路徑、它們的 LCA」有關**，路徑上其他普通點只是「過路」，不影響結果。\n- **暴力是每次詢問都掃整棵樹**（一次 `O(n)`），詢問一多就爆；虛樹把每次壓到關鍵點規模。\n- 題目把節點「分組」，要對每組各算一次（如 3786），各組合起來節點總數可控——非常適合對每組建虛樹。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「關鍵點」是誰？所有詢問的關鍵點總數是否遠小於 n？\n2. 我的答案是不是只依賴關鍵點與它們的 LCA，普通點可以被壓縮掉？\n3. 多次查詢時，我有沒有只清空虛樹用到的點，而不是整棵樹？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3786 | [樹組的互動代價總和](https://leetcode.cn/problems/total-sum-of-interaction-cost-in-tree-groups/) | 2139 | 放在「虛樹」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3786 | [樹組的互動代價總和](https://leetcode.cn/problems//total-sum-of-interaction-cost-in-tree-groups/) | 2139 |",
        },
        {
          id: 40,
          title: "3.10 樹上滑動視窗",
          summary:
            "**原理講解**\n\n本節把熟悉的「滑動視窗」搬到樹上。先回顧陣列版的滑動視窗：用左右兩個指標框住一段連續區間 `[left, right]`，右指標往右擴張納入新元素，當區間違反條件時左指標往右縮，整段移動過程中答案可以「增量更新」而不必重算。它快的關鍵是條件對端點移動有單調性。\n\n「樹上滑動視窗」要處理的是：視窗不再是陣列上的連續區間，而是**樹上一條由上往下的路徑**（從某個祖先到某個後代）。我們在 DFS 往下走時，這條路徑就是「當前根到當前節點」的那一段；節點進入路徑 = 右指標擴張，節點離開（回溯）= 左指標的概念。\n\n:::example 樹上滑動視窗：窗口 = 根到當前節點的那條鏈，沿 DFS 移動\n以 3425「最長特殊路徑」為例：要找一條由上往下、路徑上節點顏色都不重複的最長路徑。這正是「視窗內元素不可重複」這個經典滑動窗口條件，只是窗口長在樹上。\n\n```cpp\n// 樹上滑動視窗：窗口 = 根到當前節點的那條鏈，沿 DFS 移動\n// top 代表窗口左端（祖先方向）的深度界線，往下走時動態抬高\nvoid dfs(int u, int fa, int topDep) {\n  // topDep：窗口上界；u 加入後若有衝突，就把上界往下壓\n}\n```\n:::\n\n\n\n:::example 用 lastDepth[color] 記某顏色「上次出現的深度」當作左端依據\n核心想法：DFS 沿著一條鏈往下，相當於右指標一直擴張；當某個顏色重複，就把「左端」（窗口頂端的深度）往下壓到讓衝突消失為止——這跟陣列版「左指標前進、移除舊元素」一模一樣。回溯時要把這次加入造成的改動還原，這樣兄弟子樹才不會互相污染。\n\n```cpp\n// 用 lastDepth[color] 記某顏色「上次出現的深度」當作左端依據\nint prev = lastDepth[color[u]];  // 同色上次的深度\nint newTop = max(topDep, prev + 1);  // 衝突 → 把窗口上界壓到它下面一格\nans = max(ans, dis[u] - dis[newTop祖先]);  // 用合法窗口更新答案\n```\n:::\n\n\n\n**實作方式**\n\n把樹上滑動視窗拆成幾步，每步對照陣列版的影子：\n\n:::example 狀態：dis 是根到各點的距離前綴；lastDepth 記每色最後出現深度\n**1. 狀態表示：窗口 = 當前 DFS 路徑。** 維護「根到當前點」這條鏈的摘要：路徑長度前綴和、每種顏色最近出現的深度等可增刪的資訊。\n\n```cpp\n// 狀態：dis 是根到各點的距離前綴；lastDepth 記每色最後出現深度\nvector<int> dis;                    // dis[u] = 根到 u 的路徑長\nunordered_map<int, int> lastDepth;  // color -> 該色最近出現的深度\n```\n:::\n\n\n\n:::example 進入 u：納入窗口，必要時把窗口上界往下壓\n**2. 右端擴張：DFS 進入節點。** 進入 `u` 時把它納入窗口，並依條件（如顏色衝突）更新左端界線。\n\n```cpp\n// 進入 u：納入窗口，必要時把窗口上界往下壓\nint saved = lastDepth.count(color[u]) ? lastDepth[color[u]] : -1;\nint top = max(parentTop, saved + 1);  // 新的窗口上界（深度）\nlastDepth[color[u]] = dep[u];         // 記錄本色最新深度\n```\n:::\n\n\n\n:::example 答案：窗口 [top, u] 的長度\n**3. 答案讀取：每個合法窗口都更新。** 走到每個節點時，用「當前深度的前綴和 − 窗口上界的前綴和」得到合法路徑長，更新最長答案。\n\n```cpp\n// 答案：窗口 [top, u] 的長度\nans = max(ans, dis[u] - dis_at_depth(top));\n```\n:::\n\n\n\n:::example 回溯：還原，避免污染兄弟子樹\n**4. 回溯：撤銷本次修改（最關鍵）。** 離開 `u` 前，把 `lastDepth[color[u]]` 還原成進入前的值，否則會影響別的分支。\n\n```cpp\n// 回溯：還原，避免污染兄弟子樹\nlastDepth[color[u]] = saved;  // 還原成進入前的狀態\n```\n:::\n\n\n\n複雜度：每個節點進出窗口各一次，整體 `O(n)`（用雜湊或陣列維護顏色深度）；3486「最長特殊路徑 II」放寬到「最多一個顏色可重複」，本質仍是同一個窗口框架加一點條件。\n\n**題目線索（Pattern）**\n\n看到這些特徵，往樹上滑動視窗想：\n\n- **答案是「一條由上往下的路徑」**，且路徑要滿足某個對端點單調的條件（元素不重複、和不超過上限、種類數受限）。\n- **這個條件在陣列上就是經典滑動視窗**，只是把「連續子陣列」換成「樹上的祖先-後代鏈」。\n- **暴力是枚舉每對祖先-後代再驗證**（`O(n^2)` 起跳）；本節用一次 DFS + 窗口更新壓到接近 `O(n)`。\n- 題目常見「特殊路徑」「不重複顏色路徑」「最長/最短向下路徑」等字眼（如 3425 / 3486）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「窗口」是不是就是當前 DFS 的那條根到節點的鏈？\n2. 條件是否對端點移動具單調性，能像陣列那樣縮左端？\n3. 我有沒有在回溯時還原本次的修改，避免污染兄弟子樹？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3425 | [最長特殊路徑](https://leetcode.cn/problems/longest-special-path/) | 2435 | 放在「樹上滑動視窗」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3486 | [最長特殊路徑 II](https://leetcode.cn/problems/longest-special-path-ii/) | 2925 | 放在「樹上滑動視窗」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3425 | [最長特殊路徑](https://leetcode.cn/problems//longest-special-path/) | 2435 |\n| 3486 | [最長特殊路徑 II](https://leetcode.cn/problems//longest-special-path-ii/) | 2925 |",
        },
        {
          id: 41,
          title: "3.11 樹上啟發式合併",
          summary:
            "**原理講解**\n\n本節要解決一類「對每棵子樹各問一個問題」的題目，例如：每棵子樹裡出現了哪些顏色？子樹內某個統計量是多少？最直覺的做法是對每個節點都重新掃一遍它的整棵子樹，但這樣會 `O(n^2)`，n 一大就爆。\n\n「樹上啟發式合併（DSU on tree，又叫 small-to-large）」是一個聰明的省力技巧。核心觀察是：一個節點的答案來自它所有孩子的子樹資訊合併。合併時，與其把所有孩子的資訊都拷貝一份，不如**保留最大的那個孩子的資訊（重兒子），只把其他較小孩子的資訊逐一搬進來**。\n\n:::example 重兒子：子樹最大的那個孩子，它的資訊保留不搬\n為什麼這樣會快？因為「啟發式合併」有個著名結論：每個元素被搬動的次數最多 `O(log n)` 次。直覺是：一個元素只有在「它所在的集合被併進一個更大的集合」時才被搬，而集合每被搬一次大小至少翻倍，翻倍最多 `log n` 次。所以總搬動量是 `O(n log n)`，遠優於暴力。\n\n```cpp\n// 重兒子：子樹最大的那個孩子，它的資訊保留不搬\nvoid dfsSize(int u, int fa) {\n  sz[u] = 1;\n  int mx = 0;\n  for (int v : g[u]) {\n    if (v != fa) {\n      dfsSize(v, u);\n      sz[u] += sz[v];\n      if (sz[v] > mx) {\n        mx = sz[v];\n        big[u] = v;\n      }  // big[u] = 重兒子\n    }\n  }\n}\n```\n:::\n\n\n\n:::example small-to-large 的精神：保留重兒子，輕兒子重新加入\n整個流程像這樣：先遞迴算完所有「輕兒子」（較小的孩子）並清掉它們留下的資訊，最後算重兒子並**保留**它的資訊，再把輕兒子的子樹一個個重新加回來，湊出當前節點的完整答案。\n\n```cpp\n// small-to-large 的精神：保留重兒子，輕兒子重新加入\nadd(u);  // 把當前節點自己算進來\nfor (輕兒子 v) {\n  addSubtree(v);  // 把每個輕兒子整棵加回 cnt\n}\nans[u] = 查詢當前統計();  // 此刻 cnt 就是 u 子樹的完整資訊\n```\n:::\n\n\n\n像 2003「每棵子樹內缺失的最小基因值」、3575「最大好子樹分數」，都是「每棵子樹要一個答案、且子樹資訊可增量維護」的典型。\n\n**實作方式**\n\n固定模板，分幾步：\n\n**1. 第一次 DFS：算子樹大小、標記重兒子。**（見上方 `dfsSize`。）重兒子是子樹最大的孩子，它的貢獻將被保留。\n\n:::example add / remove：把單一節點的貢獻加入或移出全域統計\n**2. 設計 add / remove：單點加入、移除對統計的影響。** 用一個全域計數結構（如 `cnt[]`）記錄當前「已加入集合」的狀態。\n\n```cpp\n// add / remove：把單一節點的貢獻加入或移出全域統計\nint cnt[MAXV];\nvoid add(int u) { cnt[val[u]]++; /* 視題目更新答案輔助量 */ }\nvoid remove(int u) { cnt[val[u]]--; }\n```\n:::\n\n\n\n:::example 主流程：輕兒子算完即清空，重兒子保留，再補回輕兒子\n**3. 主 DFS：先輕後重，重兒子資訊保留。** 先遞迴輕兒子並用 `remove` 把它們清乾淨（`keep = false`）；再遞迴重兒子且保留（`keep = true`）；接著把自己和所有輕兒子子樹重新 `add` 回來。\n\n```cpp\n// 主流程：輕兒子算完即清空，重兒子保留，再補回輕兒子\nvoid dfs(int u, int fa, bool keep) {\n  for (int v : g[u]) {\n    if (v != fa && v != big[u]) {\n      dfs(v, u, false);  // 輕：不留\n    }\n  }\n  if (big[u]) {\n    dfs(big[u], u, true);  // 重：保留\n  }\n  add(u);\n  for (int v : g[u]) {\n    if (v != fa && v != big[u]) {\n      addSubtree(v);  // 補回輕兒子\n    }\n  }\n  ans[u] = query();  // u 的答案\n  if (!keep) {\n    for (int v : g[u]) {\n      if (v != fa) {\n        removeSubtree(v), remove(u);\n      }\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n- `addSubtree` 要遍歷整棵子樹逐點 `add`，不能用快取（那是重兒子才有的待遇）。\n- 清空只清「不保留」的子樹，且務必清乾淨，否則兄弟之間會互相污染。\n- 複雜度 `O(n log n)`：每個點被 add/remove 最多 `O(log n)` 次。\n\n**題目線索（Pattern）**\n\n看到這些特徵，往樹上啟發式合併想：\n\n- **要對「每棵子樹」各回答一個問題**（每個節點一個答案），而且答案可由子樹內元素的統計（顏色數、眾數、是否出現某值…）得到。\n- **這個統計可以用 add / remove 單點增量維護**，加入一個點、移除一個點都好寫。\n- **暴力是對每個節點重掃整棵子樹**（`O(n^2)`）；本節用「保留重兒子」把它降到 `O(n log n)`。\n- 題目沒有「修改/刪除歷史」需求，只是純粹的子樹彙總（如 2003 / 3575 / 3590）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的答案是不是「每棵子樹各一個」，且能用單點 add/remove 維護？\n2. 我有沒有正確標出重兒子，並只保留它、清空輕兒子？\n3. 清空時我有沒有把不保留的子樹清得乾乾淨淨，避免污染兄弟？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2003 | [每棵子樹內缺失的最小基因值](https://leetcode.cn/problems/smallest-missing-genetic-value-in-each-subtree/) | 2415 | 放在「樹上啟發式合併」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3575 | [最大好子樹分數](https://leetcode.cn/problems/maximum-good-subtree-score/) | 2360 | 放在「樹上啟發式合併」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems/kth-smallest-path-xor-sum/) | 2646 | 放在「樹上啟發式合併」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2003 | [每棵子樹內缺失的最小基因值](https://leetcode.cn/problems//smallest-missing-genetic-value-in-each-subtree/) | 2415 |\n| 3575 | [最大好子樹分數](https://leetcode.cn/problems//maximum-good-subtree-score/) | 2360 |\n| 3590 | [第 K 小的路徑異或和](https://leetcode.cn/problems//kth-smallest-path-xor-sum/) | 2646 |",
        },
        {
          id: 42,
          title: "3.12 點分治",
          summary:
            "**原理講解**\n\n本節要解決的是「跟樹上路徑有關的統計」題，例如：有多少條路徑長度等於 K？有多少條路徑滿足某個條件？路徑可以從任何點到任何點，所以總共有 `O(n^2)` 條，硬枚舉太慢。「點分治（centroid decomposition，重心分治）」用分治的思想把它壓下來。\n\n關鍵觀察：任何一條路徑，要嘛**經過某個我們選定的中心點**，要嘛**完全落在它移除後分裂出的某棵子樹裡**。於是我們可以：先把「經過中心點的路徑」全部算掉，再對每個分裂出的子樹遞迴做同樣的事。這樣不重不漏地涵蓋所有路徑。\n\n:::example 找重心：移除某點後，最大塊也不超過總數一半的那個點\n但分治要快，每次必須把樹砍得夠平均，否則退化成一條鏈會變 `O(n^2)`。解法是每次選「重心（centroid）」當中心：重心是「移除它後，剩下最大子樹的大小最小」的那個點。數學上保證最大子樹不超過原樹的一半，所以分治層數只有 `O(log n)`。\n\n```cpp\n// 找重心：移除某點後，最大塊也不超過總數一半的那個點\nvoid findCentroid(int u, int fa, int total) {\n  int mx = total - sz[u];  // 往父方向那一塊的大小\n  for (int v : g[u]) {\n    if (v != fa && !done[v]) {\n      mx = max(mx, sz[v]);  // 各子樹塊\n    }\n  }\n  if (mx <= total / 2) {\n    centroid = u;  // 最大塊 ≤ 一半 → 它就是重心\n  }\n}\n```\n:::\n\n\n\n:::example 經過重心的路徑 = 兩條從重心出發的半路徑拼起來\n每一層分治，我們從重心出發 DFS，量出每個點到重心的距離（或其他要統計的量），再用這些「半路徑」湊出「經過重心的整條路徑」的數量。\n\n```cpp\n// 經過重心的路徑 = 兩條從重心出發的半路徑拼起來\n// 例：統計長度為 K 的路徑，就找 dist[a] + dist[b] == K 的配對\n```\n:::\n\n\n\n像 3372「連線兩棵樹後最大目標節點數目」這類，需要對「每個點考慮它到其他點的距離分佈」的題目，點分治能把它組織成 `O(n log n)` 的分治流程。\n\n**實作方式**\n\n點分治的骨架很固定，分幾步：\n\n:::example 算大小（已被標記 done 的重心不再進入）\n**1. 算子樹大小、找重心。** 每進入一塊（連通分量），先 DFS 算大小，再找重心當這層的中心。\n\n```cpp\n// 算大小（已被標記 done 的重心不再進入）\nvoid getSize(int u, int fa) {\n  sz[u] = 1;\n  for (int v : g[u]) {\n    if (v != fa && !done[v]) {\n      getSize(v, u);\n      sz[u] += sz[v];\n    }\n  }\n}\n```\n:::\n\n\n\n:::example 容斥：全部配對 − 同子樹內部配對 = 真正經過重心的路徑\n**2. 統計「經過重心」的路徑。** 從重心出發收集每個點的距離，常用「容斥」：先算所有半路徑兩兩配對，再扣掉「同一子樹內部的配對」（那些其實沒真的經過重心，會繞回去）。\n\n```cpp\n// 容斥：全部配對 − 同子樹內部配對 = 真正經過重心的路徑\nlong long ans = calc(centroid, 0);  // 以重心為起點，全部\nfor (int v : g[centroid]) {\n  if (!done[v]) {\n    ans -= calc(v, edgeLen(centroid, v));  // 扣掉同一子樹內部的\n  }\n}\n```\n:::\n\n\n\n:::example 分治下去：重心用過就封住，遞迴各子樹\n**3. 標記重心、遞迴每個子樹。** 把重心標成 `done`（之後不再跨過它），對它分裂出的每塊遞迴重複步驟 1–3。\n\n```cpp\n// 分治下去：重心用過就封住，遞迴各子樹\nvoid solve(int u) {\n  getSize(u, 0);\n  centroid = -1;\n  findCentroid(u, 0, sz[u]);\n  int c = centroid;\n  done[c] = true;\n  countThrough(c);  // 統計經過 c 的路徑\n  for (int v : g[c]) {\n    if (!done[v]) {\n      solve(v);  // 對每塊遞迴\n    }\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n- 收集距離時務必用 `done` 擋住已封住的重心，否則會跨塊重複計算。\n- 容斥那步若漏扣「同子樹內部」，會把不經過重心的路徑算進去。\n- 複雜度：分治 `O(log n)` 層，每層所有塊合計 `O(n)`（或加上排序的 `O(n log n)`），總計 `O(n log n)` 到 `O(n log^2 n)`。\n\n**題目線索（Pattern）**\n\n看到這些特徵，往點分治想：\n\n- **要統計「樹上所有點對之間的路徑」是否滿足某條件**（路徑長 = K、和、異或、距離分佈…），且路徑可任意起終點。\n- **路徑可以自然拆成「經過某中心點的兩段半路徑」來配對計算**。\n- **暴力是枚舉所有點對**（`O(n^2)`），n 上萬就爆；點分治用重心把它降到 `O(n log n)` 級。\n- 題目強調「距離」「路徑數量」且和具體子樹結構無關，只關乎點到點關係（如 3372 的目標節點數隨距離奇偶變化）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要統計的東西是不是「跨任意兩點的路徑」，能拆成經過中心的兩段？\n2. 我每層是不是都選重心當中心，確保分治只有 `O(log n)` 層？\n3. 統計時我有沒有用容斥扣掉「同子樹內部、其實沒經過重心」的配對？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3372 | [連線兩棵樹後最大目標節點數目 I](https://leetcode.cn/problems/maximize-the-number-of-target-nodes-after-connecting-trees-i/) | 1927 | 放在「點分治」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3372 | [連線兩棵樹後最大目標節點數目 I](https://leetcode.cn/problems//maximize-the-number-of-target-nodes-after-connecting-trees-i/) | 1927 |",
        },
        {
          id: 43,
          title: "3.13 其他",
          summary:
            "**原理講解**\n\n本節是「一般樹」的綜合題集，收的是不容易歸到單一名詞、但都圍繞同一個核心心法的題目：**把樹上的資訊用一兩次 DFS/BFS 傳遞，自底向上彙總子樹、自頂向下攜帶祖先資訊，再搭配排序、並查集（DSU）或位元運算把答案湊出來。** 這類題的價值在於訓練你「先想清楚狀態，再決定怎麼遍歷」。\n\n:::example 樹形 DFS：節點回傳子樹摘要（這裡示範回傳子樹大小）\n第一個常用工具是「樹上 DFS 回傳子樹摘要」。每個節點向上回報一個總結值（高度、節點數、最大鏈長、某種 DP 值），父節點合併孩子的回報得到自己的。\n\n```cpp\n// 樹形 DFS：節點回傳子樹摘要（這裡示範回傳子樹大小）\nint dfs(int u, int fa) {\n  int size = 1;\n  for (int v : g[u]) {\n    if (v != fa) {\n      size += dfs(v, u);  // 合併孩子\n    }\n  }\n  return size;  // 向上回報\n}\n```\n:::\n\n\n\n:::example 並查集核心：找代表 + 合併兩團\n第二個常用工具是「並查集 + 按某種順序加邊」。像 2421「好路徑的數目」、2867「統計樹中的合法路徑數目」，技巧是**先排序（按點權或質數性質），再依序把邊加入並查集**，在合併的瞬間統計新產生的合法路徑。並查集是一種「快速判斷兩點是否已連通、並把兩團合併」的結構。\n\n```cpp\n// 並查集核心：找代表 + 合併兩團\nint find(int x) { return fa[x] == x ? x : fa[x] = find(fa[x]); }  // 路徑壓縮\nvoid merge(int a, int b) { fa[find(a)] = find(b); }\n```\n:::\n\n\n\n心法是：很多「其他」樹題看似花俏，骨架都是「定義一個能向上合併、或能在合併時統計的不變量」。例如 2867 把「合法路徑」拆成「路徑上只有端點是質數，或全不是質數」，用篩法 + 並查集分塊統計；1719「重構一棵樹的方案數」則是分析祖先包含關係。先抓住「狀態是什麼、怎麼合併」，再選遍歷或資料結構。\n\n**實作方式**\n\n雖然題目雜，但解題流程可固定成幾步：\n\n:::example 例：回傳「以 u 為頂、向下的最長鏈」，順便更新全域直徑類答案\n**1. 定狀態：節點要向上回報什麼？** 高度、子樹大小、最長鏈、某顏色計數、DP 值——這決定 DFS 的回傳型別。\n\n```cpp\n// 例：回傳「以 u 為頂、向下的最長鏈」，順便更新全域直徑類答案\nint dfs(int u, int fa) {\n  int best = 0;\n  for (int v : g[u]) {\n    if (v != fa) {\n      best = max(best, dfs(v, u) + 1);\n    }\n  }\n  return best;\n}\n```\n:::\n\n\n\n**2. 選傳遞方向。** 只需子樹資訊就一次 DFS 自底向上；需要祖先/父側資訊就再做一次自頂向下，或用倍增預處理 LCA。\n\n:::example 合併時統計：兩團相連，新增「一端在 A、一端在 B」的路徑數\n**3. 配合排序或並查集做合併統計。** 路徑計數型常「排序後逐邊合併」，在 `merge` 前用兩團的大小相乘統計跨團新路徑。\n\n```cpp\n// 合併時統計：兩團相連，新增「一端在 A、一端在 B」的路徑數\nlong long cntA = size[find(a)], cntB = size[find(b)];\nans += cntA * cntB;  // 跨團配對數\nmerge(a, b);         // 再真正合併\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n- 遞迴可能很深，必要時改成顯式堆疊或 BFS 反向序，避免爆 stack。\n- 並查集記得路徑壓縮 + 按秩/大小合併，單次近似 `O(1)`。\n- 多數此類題整體 `O(n log n)`（含排序），純 DFS 部分 `O(n)`。\n\n**題目線索（Pattern）**\n\n這些題沒有統一名字，但有共同氣味：\n\n- **資料是一棵樹，答案跟「子樹彙總」或「路徑/連通性」有關**，但又不完全是某個標準模板。\n- **可以定義一個向上合併的不變量**（子樹摘要），或**可以「排序後逐邊加入並查集、在合併時統計」**（如 2421 / 2867）。\n- **暴力會反覆重算子樹或重掃所有路徑**；本節把重複工作壓進「一次遍歷的回報值」或「並查集的合併瞬間」。\n- 題目要你「數路徑」「判斷能否重構」「在子樹間找極值」，且 n 很大不允許 `O(n^2)`（如 1719 / 2479）。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能不能用一句話說出「每個節點向上回報什麼」？\n2. 我需要的只是子樹資訊，還是也要祖先資訊（決定要遍歷幾次）？\n3. 若是路徑計數，我能不能「排序後逐邊合併、在合併瞬間統計」？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 2479 | 兩個不重疊子樹的最大異或值（Premium） | N/A | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2867 | [統計樹中的合法路徑數目](https://leetcode.cn/problems/count-valid-paths-in-a-tree/) | 2428 | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 2421 | [好路徑的數目](https://leetcode.cn/problems/number-of-good-paths/) | 2445 | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1719 | [重構一棵樹的方案數](https://leetcode.cn/problems/number-of-ways-to-reconstruct-a-tree/) | 3018 | 放在「其他」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2479 | [兩個不重疊子樹的最大異或值](https://leetcode.cn/problems//maximum-xor-of-two-non-overlapping-subtrees/) |  |\n| 2867 | [統計樹中的合法路徑數目](https://leetcode.cn/problems//count-valid-paths-in-a-tree/) | 2428 |\n| 2421 | [好路徑的數目](https://leetcode.cn/problems//number-of-good-paths/) | 2445 |\n| 1719 | [重構一棵樹的方案數](https://leetcode.cn/problems//number-of-ways-to-reconstruct-a-tree/) | 3018 |",
        },
      ],
    },
    {
      id: 44,
      title: "4. 回溯",
      summary:
        "涵蓋回溯的入門、子集、劃分、組合、排列、有重複元素、搜尋與折半列舉，適合系統學習。\n\n:::example 回溯框架（子集型：選或不選）\n**C++ 模板（回溯框架）**：\n\n```cpp\n// 回溯框架（子集型：選或不選）\nvector<vector<int>> result;\nvector<int> path;\nvoid backtrack(vector<int>& nums, int i) {\n  if (i == (int)nums.size()) {\n    result.push_back(path);\n    return;\n  }\n  // 不選 nums[i]\n  backtrack(nums, i + 1);\n  // 選 nums[i]\n  path.push_back(nums[i]);\n  backtrack(nums, i + 1);\n  path.pop_back();  // 回溯\n}\n```\n:::",
      children: [
        {
          id: 45,
          title: "4.1 入門回溯",
          summary:
            '**原理講解**\n\n本節介紹「回溯（backtracking）」的入門。很多題目要你「列出所有可能的組合 / 排列 / 選法」，例如 17「電話號碼的字母組合」：輸入 "23"，2 對應 abc、3 對應 def，要列出 ad, ae, af, bd, …, cf 全部 9 種。這種「窮舉所有可能」最自然的寫法就是回溯。\n\n:::example 回溯三件事：做選擇 → 遞迴 → 撤銷選擇\n回溯的本質是「在一棵想像中的決策樹上走遍每條路」。每一步你做一個選擇（這位數字選哪個字母），往下遞迴；走到底（所有位數都選完）就收集一個答案；然後**退回上一步、撤銷剛才的選擇，換下一個分支**。這個「撤銷、換下一個」就是「回溯」這個名字的由來。\n\n```cpp\n// 回溯三件事：做選擇 → 遞迴 → 撤銷選擇\npath.push_back(choice);  // 做選擇\nbacktrack(next);         // 往下走\npath.pop_back();         // 撤銷，換下一個分支試\n```\n:::\n\n\n\n為什麼要「撤銷」？因為 `path` 是一條被所有分支共用的路徑紀錄。如果選了不還原，下一個分支會帶著上一個分支的殘留，答案就錯了。撤銷保證「每條路徑都是乾淨地從當前狀態出發」。\n\n:::example 電話字母組合：第 i 位選一個字母，遞迴到下一位\n以 17 為例，決策樹的第 i 層代表第 i 個數字，分支數 = 該數字對應的字母數；走到第 n 層就湊滿一個字串：\n\n```cpp\n// 電話字母組合：第 i 位選一個字母，遞迴到下一位\nvoid dfs(int i) {\n  if (i == digits.size()) {\n    ans.push_back(path);\n    return;\n  }                                    // 收集\n  for (char c : mapping[digits[i]]) {  // 枚舉本位所有候選\n    path.push_back(c);\n    dfs(i + 1);\n    path.pop_back();  // 撤銷\n  }\n}\n```\n:::\n\n\n\n回溯的時間複雜度通常是「分支數的連乘」，例如全是 3 個字母的 n 位數就是 `O(3^n * n)`（最後乘 n 是複製字串的成本）。它本來就是窮舉，慢是正常的；能用是因為題目規模小或有剪枝。\n\n**實作方式**\n\n入門回溯有固定骨架，分幾步：\n\n**1. 想清楚「決策樹」：每層選什麼、有幾個分支。** 第幾層代表什麼、候選有哪些，直接決定迴圈與遞迴參數。\n\n:::example 狀態：path 是當前部分解，i 是進度\n**2. 設計狀態：path + 進度索引。** `path` 記目前選到的東西，一個索引（如 `i`）記「現在輪到第幾步」。\n\n```cpp\n// 狀態：path 是當前部分解，i 是進度\nstring path;  // 已經選好的前幾位\n              // dfs(int i)：表示正在決定第 i 步\n```\n:::\n\n\n\n:::example 終止：選滿了就收集一個完整解\n**3. 寫終止條件與收集。** 走到底（索引到頭）就把 `path` 存進答案，然後 `return`。\n\n```cpp\n// 終止：選滿了就收集一個完整解\nif (i == n) {\n  ans.push_back(path);\n  return;\n}\n```\n:::\n\n\n\n:::example 心臟：選 → 遞迴 → 還原，缺一不可\n**4. 枚舉候選：做選擇 → 遞迴 → 撤銷。** 這是回溯的心臟，三步缺一不可（見原理的程式碼）。撤銷一定要和「做選擇」嚴格成對。\n\n```cpp\n// 心臟：選 → 遞迴 → 還原，缺一不可\nfor (auto& c : candidates(i)) {\n  path.push_back(c);  // 做選擇\n  dfs(i + 1);         // 往下\n  path.pop_back();    // 撤銷\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n- 空輸入要特判（如 digits 為空時答案是空陣列而非含一個空字串）。\n- 撤銷與選擇必須對稱：push 配 pop、加旗標配清旗標。\n- 複雜度多為指數級（如 `O(3^n)`），只適用小規模或有強剪枝。\n\n**題目線索（Pattern）**\n\n看到這些特徵，往入門回溯想：\n\n- **要「列出所有」組合 / 排列 / 路徑 / 選法**，或要在所有可能中找最值，且資料規模很小。\n- **每一步是一個獨立的選擇**，做完一個選擇問題就縮小成「同樣形式的子問題」。\n- **暴力多層巢狀迴圈寫不出來**（層數不固定、由輸入決定）——這正是遞迴回溯的舞台。\n- 題目像 17 那樣，答案的長度/結構由輸入動態決定，需要逐步構造。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的決策樹「每一層」代表什麼？「每個分支」是哪些候選？\n2. 我的終止條件是什麼？什麼時候算湊出一個完整解？\n3. 我有沒有把「做選擇」和「撤銷」嚴格成對寫出來？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 17 | 電話號碼的字母組合 | N/A | 放在「入門回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 17 | [電話號碼的字母組合](https://leetcode.cn/problems//letter-combinations-of-a-phone-number/) |  |',
        },
        {
          id: 46,
          title: "4.2 子集型回溯",
          summary:
            "**原理講解**\n\n本節聚焦「子集型回溯」。它處理的題目共通點是：**對每個元素獨立做「選 / 不選」的決定**，最後把所有可能的選法列出來，或在其中找符合條件的。例如 78「子集」：給 [1,2,3]，要列出全部 8 個子集（含空集）。每個數字都有「要 / 不要」兩種選擇，這就是「子集型」這個名字的由來。\n\n回顧回溯的精神：在決策樹上做選擇、遞迴、再撤銷。子集型的決策樹特別規律——它有兩種等價的長法：\n\n:::example 子集：對第 i 個元素「不選」和「選」各遞迴一次\n**長法一：對每個元素二選一（選 / 不選）。** 第 i 層決定「第 i 個元素要不要」，走到底就是一個子集。\n\n```cpp\n// 子集：對第 i 個元素「不選」和「選」各遞迴一次\nvoid dfs(int i) {\n  if (i == nums.size()) {\n    ans.push_back(path);\n    return;\n  }\n  dfs(i + 1);               // 不選 nums[i]\n  path.push_back(nums[i]);  // 選 nums[i]\n  dfs(i + 1);\n  path.pop_back();  // 撤銷\n}\n```\n:::\n\n\n\n:::example 子集（起始下標版）：每到一個節點就收集，再往後延伸\n**長法二：用「起始下標」枚舉。** 每進一層就從 `start` 往後挑一個元素加入，挑誰就從誰的下一位繼續，天然避免重複組合（避免 {1,2} 和 {2,1} 都出現）。\n\n```cpp\n// 子集（起始下標版）：每到一個節點就收集，再往後延伸\nvoid dfs(int start) {\n  ans.push_back(path);  // 每個節點都是一個合法子集\n  for (int i = start; i < nums.size(); ++i) {\n    path.push_back(nums[i]);\n    dfs(i + 1);  // 從 i+1 繼續，不回頭\n    path.pop_back();\n  }\n}\n```\n:::\n\n\n\n:::example 組合總和：可重複選同一元素，故遞迴傳 i（不是 i+1）\n像 39「組合總和」（可重複選、湊出目標和）、254「因子的組合」、320「單詞縮寫」，本質都是「每個候選選或不選 / 從某處往後挑」的變形。組合總和因為同一個數可重複用，遞迴時傳 `i` 而不是 `i + 1`：\n\n```cpp\n// 組合總和：可重複選同一元素，故遞迴傳 i（不是 i+1）\npath.push_back(candidates[i]);\ndfs(i, target - candidates[i]);  // 還能再選一次 i\npath.pop_back();\n```\n:::\n\n\n\n複雜度：子集共有 `2^n` 個，列出全部就是 `O(2^n * n)`（乘 n 是複製每個子集的成本）。\n\n**實作方式**\n\n子集型回溯的固定步驟：\n\n**1. 選一種決策樹寫法。** 二選一版直觀；起始下標版更容易處理「組合不計順序」與去重，建議優先掌握。\n\n:::example 狀態：path 為當前子集，start 為可挑選的起點\n**2. 狀態：path + start（或進度 i）。** `path` 是當前部分解，`start` 控制「只能往後挑」以避免重複。\n\n```cpp\n// 狀態：path 為當前子集，start 為可挑選的起點\nvector<int> path;\n// dfs(int start)：只從 nums[start..] 裡挑\n```\n:::\n\n\n\n:::example 收集：子集每節點都收；組合總和則在和達標時收\n**3. 收集時機。** 子集題「每個節點都收集」（含空集）；組合/目標和題則在「達成條件時」才收集。\n\n```cpp\n// 收集：子集每節點都收；組合總和則在和達標時收\nif (target == 0) {\n  ans.push_back(path);\n  return;\n}\n```\n:::\n\n\n\n:::example 同層去重：排序後，跳過同一層重複的值\n**4. 含重複元素要「同層去重」。** 先排序，遞迴迴圈裡若當前元素和前一個相同、且前一個沒被選，就跳過，避免產生重複子集。\n\n```cpp\n// 同層去重：排序後，跳過同一層重複的值\nfor (int i = start; i < nums.size(); ++i) {\n  if (i > start && nums[i] == nums[i - 1]) {\n    continue;  // 同層相同 → 跳\n  }\n  path.push_back(nums[i]);\n  dfs(i + 1);\n  path.pop_back();\n}\n```\n:::\n\n\n\n**5. 邊界與複雜度。**\n- 「選或不選」「可否重複選」「計不計順序」三件事決定傳 `i` 還是 `i + 1`、要不要去重。\n- 去重前一定要先排序，否則相同元素不相鄰，去重失效。\n- 複雜度子集 `O(2^n)`、組合視剪枝而定，皆為指數級，適用小規模。\n\n**題目線索（Pattern）**\n\n看到這些特徵，往子集型回溯想：\n\n- **要列舉「所有子集 / 所有組合 / 所有選法」**，或在其中找滿足條件者（如和等於目標）。\n- **每個元素的核心決定是「選或不選」**，元素之間相對獨立、不講順序。\n- **暴力無法用固定層數的巢狀迴圈枚舉**（子集數隨 n 指數成長），需要遞迴。\n- 題目像 78 / 39 / 254：輸出一堆「子集合 / 組合」，常伴隨「不可重複」「可重複選」「湊目標」等條件。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的每個元素是不是在做「選 / 不選」或「從 start 往後挑」的決定？\n2. 我該在「每個節點」收集（子集），還是「達標時」收集（組合）？\n3. 有重複元素時，我有沒有先排序、再做同層去重？\n\n三題都答得出來，就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 39 | 組合總和 | N/A | 放在「子集型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 51 | LCP 51. 烹飪料理 | N/A | 放在「子集型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 78 | 子集 | N/A | 放在「子集型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 254 | 因子的組合（Premium） | N/A | 放在「子集型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 320 | 列舉單詞的全部縮寫（Premium） | N/A | 放在「子集型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 39 | [組合總和](https://leetcode.cn/problems//combination-sum/) |  |\n| 51 | [LCP 51. 烹飪料理](https://leetcode.cn/problems//UEcfPD/) |  |\n| 78 | [子集](https://leetcode.cn/problems//subsets/) |  |\n| 254 | [因子的組合](https://leetcode.cn/problems//factor-combinations/) |  |\n| 320 | [列舉單詞的全部縮寫](https://leetcode.cn/problems//generalized-abbreviation/) |  |\n| 784 | [字母大小寫全排列](https://leetcode.cn/problems//letter-case-permutation/) |  |\n| 2174 | [透過翻轉行或列來去除所有的 1 II](https://leetcode.cn/problems//remove-all-ones-with-row-and-column-flips-ii/) |  |\n| 1863 | [找出所有子集的異或總和再求和](https://leetcode.cn/problems//sum-of-all-subset-xor-totals/) | 1372 |\n| 3566 | [等積子集的劃分方案](https://leetcode.cn/problems//partition-array-into-two-equal-product-subsets/) | 1459 |\n| 2044 | [統計按位或能得到最大值的子集數目](https://leetcode.cn/problems//count-number-of-maximum-bitwise-or-subsets/) | 1568 |\n| 2397 | [被列覆蓋的最多行數](https://leetcode.cn/problems//maximum-rows-covered-by-columns/) | 1719 |\n| 1239 | [串聯字串的最大長度](https://leetcode.cn/problems//maximum-length-of-a-concatenated-string-with-unique-characters/) | 1720 |\n| 2212 | [射箭比賽中的最大得分](https://leetcode.cn/problems//maximum-points-in-an-archery-competition/) | 1869 |\n| 1255 | [得分最高的單詞集合](https://leetcode.cn/problems//maximum-score-words-formed-by-letters/) | 1882 |\n| 2151 | [基於陳述統計最多好人數](https://leetcode.cn/problems//maximum-good-people-based-on-statements/) | 1980 |\n| 2597 | [美麗子集的數目](https://leetcode.cn/problems//the-number-of-beautiful-subsets/) | 2023 |\n| 2959 | [關閉分部的可行集合數目](https://leetcode.cn/problems//number-of-possible-sets-of-closing-branches/) | 2077 |\n| 1601 | [最多可達成的換樓請求數目](https://leetcode.cn/problems//maximum-number-of-achievable-transfer-requests/) | 2119 |\n| 1617 | [統計子樹中城市之間最大距離](https://leetcode.cn/problems//count-subtrees-with-max-distance-between-cities/) | 2309 |",
        },
        {
          id: 47,
          title: "4.3 劃分型回溯",
          summary:
            '**原理講解**\n\n本節要解決的場景是：一個字串（或一段陣列）要被「切成連續的好幾段」，而且每一段都要滿足某種規則，我們想枚舉所有合法的切法。例如把 `"25525511135"` 切成四段合法 IP，或把 `"aab"` 切成每段都是回文的片段。\n\n這類問題的共通點是：**邊界只能切在元素「之間」，而且每一段都連著、不重疊、不漏掉。** 我們稱它為「劃分型回溯」——回溯（backtracking）就是「先試一種選擇，做下去；走不通或試完了，就退回來把剛才那步收回，再試下一種」。\n\n:::example 劃分的骨架：start 是還沒切的起點，往後枚舉每一刀的位置\n核心想法是：用一個下標 `start` 代表「目前還沒被切的部分從哪裡開始」。我們從 `start` 往後枚舉「下一刀切在哪」，每切一刀就得到一段 `[start, i]`，檢查它合不合法，合法就遞迴去切剩下的 `[i + 1, ...]`。\n\n```cpp\n// 劃分的骨架：start 是還沒切的起點，往後枚舉每一刀的位置\nvoid dfs(int start) {\n  if (start == s.size()) {\n    record();\n    return;\n  }  // 整個字串切完了\n  for (int i = start; i < s.size(); ++i) {\n    // 這一段是 s[start..i]\n  }\n}\n```\n:::\n\n\n\n:::example 做選擇 → 遞迴 → 撤銷選擇（恢復現場）\n為什麼「退回來」很重要？因為一段路試完之後，我們必須讓現場回到試之前的樣子，下一個選擇才不會被汙染。這就是「遞迴前做選擇、遞迴後撤銷」：\n\n```cpp\n// 做選擇 → 遞迴 → 撤銷選擇（恢復現場）\npath.push_back(seg);  // 選：把這一段加進路徑\ndfs(i + 1);           // 遞迴：去切剩下的部分\npath.pop_back();  // 撤銷：拿掉這一段，回到原狀，準備試下一刀\n```\n:::\n\n\n\n只要每段都連續、起點接著上一段的終點，就保證切出來的片段拼回去剛好是原字串，不重不漏。整體會走遍所有切法，是指數級，所以要靠「不合法就提前停」來剪枝。\n\n**實作方式**\n\n把劃分型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：start 是當前未處理起點，path 是已切好的片段\n**1. 狀態表示：一個起點下標 + 一條已選路徑。** `start` 記錄還沒切的部分從哪開始，`path` 存目前已經切好的段。\n\n```cpp\n// 狀態：start 是當前未處理起點，path 是已切好的片段\nstring s;\nvector<string> path;  // 已經切出來的段\nvector<vector<string>> ans;\n```\n:::\n\n\n\n:::example 枚舉這一段的結尾 i，合法就深入，回來撤銷\n**2. 更新流程：枚舉這一刀、檢查、遞迴、撤銷。** 從 `start` 到結尾枚舉切點 `i`，取出子段 `s[start..i]`，合法才往下走，回來一定要 `pop_back`。\n\n```cpp\n// 枚舉這一段的結尾 i，合法就深入，回來撤銷\nfor (int i = start; i < s.size(); ++i) {\n  string seg = s.substr(start, i - start + 1);\n  if (!valid(seg)) {\n    continue;  // 剪枝：這段不合法就跳過\n  }\n  path.push_back(seg);\n  dfs(i + 1);       // 從 i+1 繼續切剩下的\n  path.pop_back();  // 恢復現場\n}\n```\n:::\n\n\n\n:::example 切到字串尾端 → path 是一組完整劃分\n**3. 答案讀取：切到底就收集。** 當 `start` 走到字串結尾，代表整個字串剛好被切完，這時 `path` 就是一組合法劃分（IP 題還要額外檢查「剛好切成四段」）。\n\n```cpp\n// 切到字串尾端 → path 是一組完整劃分\nif (start == s.size()) {\n  ans.push_back(path);\n  return;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 每段的合法性檢查要早做（例如 IP 段不能有前導零、數值 ≤ 255），越早剪枝越快。\n- 段數有限制時（如 IP 固定四段），用一個 `count` 一起傳，超過就回頭。\n- 複雜度是指數級，但被合法性剪枝大幅壓縮；像 IP 這種長度固定的題目實際分支極少。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「劃分型回溯」想：\n\n- **要把一個字串或陣列切成「連續的好幾段」，並列出所有切法**，例如 93「復原 IP 地址」、131「分割回文串」、140「單詞拆分 II」、291「單詞規律 II」。\n- **段與段之間不重疊、剛好拼回原本**——這正是用單一 `start` 下標就能描述的訊號：\n\n:::example 嗅到味道的瞬間：答案是「把整體切成連續段」的所有方案\n```cpp\n// 嗅到味道的瞬間：答案是「把整體切成連續段」的所有方案\n// 不是選哪些元素，而是「刀切在哪些縫隙」\n```\n:::\n\n\n\n- **每段有獨立的合法條件**（是回文嗎？是合法 IP 段嗎？在字典裡嗎？）——這天生適合「切一段、驗一段、再遞迴」。\n- **3566「拆分字串使唯一子字串的數目最大」** 這類求「最多能切幾段」的，也是同一個骨架，只是把「收集所有方案」換成「更新全局最大段數」。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「未處理起點」`start` 代表什麼？切完的判斷條件是什麼？\n2. 每枚舉一刀，我取出的那一段如何檢查合法？\n3. 遞迴回來後，我有沒有把剛加進 `path` 的段撤銷掉？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 93 | 復原 IP 地址 | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 131 | 分割回文串 | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 140 | 單詞拆分 II | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 291 | 單詞規律 II（Premium） | N/A | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1593 | [拆分字串使唯一子字串的數目最大](https://leetcode.cn/problems/split-a-string-into-the-max-number-of-unique-substrings/) | 1740 | 放在「劃分型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 93 | [復原 IP 地址](https://leetcode.cn/problems//restore-ip-addresses/) |  |\n| 131 | [分割回文串](https://leetcode.cn/problems//palindrome-partitioning/) |  |\n| 140 | [單詞拆分 II](https://leetcode.cn/problems//word-break-ii/) |  |\n| 291 | [單詞規律 II](https://leetcode.cn/problems//word-pattern-ii/) |  |\n| 1593 | [拆分字串使唯一子字串的數目最大](https://leetcode.cn/problems//split-a-string-into-the-max-number-of-unique-substrings/) | 1740 |\n| 1849 | [將字串拆分為遞減的連續值](https://leetcode.cn/problems//splitting-a-string-into-descending-consecutive-values/) | 1747 |\n| 2698 | [求一個整數的懲罰數](https://leetcode.cn/problems//find-the-punishment-number-of-an-integer/) | 1679 |',
        },
        {
          id: 48,
          title: "4.4 組合型回溯",
          summary:
            "**原理講解**\n\n本節要解決的場景是：從一堆候選裡「挑出一組」，只在乎「挑了哪些」、不在乎「挑的先後順序」。例如從 1~n 裡挑 k 個數（77 組合）、挑出加總等於某值的數（216 組合總和 III）。這種「只問選哪些、不問順序」的問題，就是「組合型回溯」。\n\n:::example 組合的關鍵：用 start 限制「只往後挑」，自動避免 {1,2}/{2,1} 重複\n回溯（backtracking）的意思是「先試一種選擇做下去，試完或走不通就退回來、收回剛才那步、再試別的」。組合和排列最大的差別在這裡：**組合裡 {1,2} 和 {2,1} 算同一組，所以我們必須避免重複。** 做法是規定「只能往後挑，不能回頭」——用一個 `start` 下標記住「下一個可以挑的最小位置」。\n\n```cpp\n// 組合的關鍵：用 start 限制「只往後挑」，自動避免 {1,2}/{2,1} 重複\nvoid dfs(int start) {\n  // 從 start 開始往後選，永遠不回頭去選比 start 小的\n  for (int i = start; i <= n; ++i) { /* 選 i，然後遞迴到 i+1 */\n  }\n}\n```\n:::\n\n\n\n:::example 做選擇 → 遞迴（從 i+1 繼續）→ 撤銷\n核心動作同樣是「遞迴前做選擇、遞迴後撤銷」：把候選加進路徑、往下遞迴、回來時把它拿掉，讓現場恢復原狀，下一個選擇才不會被汙染。\n\n```cpp\n// 做選擇 → 遞迴（從 i+1 繼續）→ 撤銷\npath.push_back(i);\ndfs(i + 1);  // 注意是 i+1：下一個從更後面挑，避免重複也避免重選自己\npath.pop_back();  // 恢復現場\n```\n:::\n\n\n\n像 22「括號生成」雖然不是從陣列挑數，但本質一樣：每一步「放左括號或放右括號」也是一種組合式的選擇，只要在合法範圍內枚舉即可。整體是指數級枚舉，靠剪枝（提早停掉不可能的分支）加速。\n\n**實作方式**\n\n把組合型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：path 是已挑出的元素，start 控制只往後選\n**1. 狀態表示：一條已選路徑 + 一個起點下標。** `path` 存目前挑了哪些，`start` 限制接下來只能往後挑。\n\n```cpp\n// 狀態：path 是已挑出的元素，start 控制只往後選\nint n, k;\nvector<int> path;\nvector<vector<int>> ans;\n```\n:::\n\n\n\n:::example 從 start 往後枚舉，選 i 後遞迴到 i+1，回來撤銷\n**2. 更新流程：枚舉、選、遞迴、撤銷。** 從 `start` 往後枚舉候選 `i`，選進 `path`，遞迴時傳 `i + 1`（下一個更後面），回來 `pop_back`。\n\n```cpp\n// 從 start 往後枚舉，選 i 後遞迴到 i+1，回來撤銷\nfor (int i = start; i <= n; ++i) {\n  path.push_back(i);\n  dfs(i + 1);\n  path.pop_back();\n}\n```\n:::\n\n\n\n:::example 路徑長度達到 k → 找到一組合法組合\n**3. 答案讀取：路徑湊滿條件就收集。** 組合通常有「選滿 k 個」或「加總達標」的終止條件，達成就把 `path` 收進答案。\n\n```cpp\n// 路徑長度達到 k → 找到一組合法組合\nif (path.size() == k) {\n  ans.push_back(path);\n  return;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 強力剪枝：剩下的候選數量不夠湊滿 k 個時，直接停。`for` 上界可寫成 `i <= n - (k - path.size()) + 1`。\n- 求和題（216）可在加總已超標時提前 `break`（若候選有序）。\n- 複雜度為組合數量級 `O(C(n, k) * k)`，剪枝能把實際分支大幅砍掉。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「組合型回溯」想：\n\n- **要「挑出一組」而且 {1,2} 與 {2,1} 視為相同**，例如 77「組合」、216「組合總和 III」、1286「字母組合迭代器」。順序不重要，就是組合的訊號。\n- **你發現需要避免「同一組被算兩次」**——解法就是用 `start` 只往後挑：\n\n:::example 嗅到味道的瞬間：你想「不重複地列出所有子集合 / 固定大小的組合」\n```cpp\n// 嗅到味道的瞬間：你想「不重複地列出所有子集合 / 固定大小的組合」\n// dfs(i + 1) 而非 dfs(start)，就是「不回頭」的具體寫法\n```\n:::\n\n\n\n- **每一步是「二選一或多選一」的合法構造**，像 22「括號生成」「放左還是放右」、301「刪除無效的括號」「這個字元留還是刪」，本質都是組合式枚舉加合法性剪枝。\n- **暴力列出所有子集再篩選太慢**；組合回溯邊構造邊剪枝，省掉大量不可能分支。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我這題在意順序嗎？（不在意 → 組合，要用 `start` 防重複）\n2. 遞迴時我傳的是 `i + 1` 還是 `start`？（組合不重選通常傳 `i + 1`）\n3. 我的終止與收集條件是什麼？有沒有可以提早 `break` 的剪枝？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 22 | 括號生成 | N/A | 放在「組合型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 77 | 組合 | N/A | 放在「組合型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 216 | 組合總和 III | N/A | 放在「組合型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 301 | 刪除無效的括號 | N/A | 放在「組合型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1286 | [字母組合迭代器](https://leetcode.cn/problems/iterator-for-combination/) | 1591 | 放在「組合型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 22 | [括號生成](https://leetcode.cn/problems//generate-parentheses/) |  |\n| 77 | [組合](https://leetcode.cn/problems//combinations/) |  |\n| 216 | [組合總和 III](https://leetcode.cn/problems//combination-sum-iii/) |  |\n| 301 | [刪除無效的括號](https://leetcode.cn/problems//remove-invalid-parentheses/) |  |\n| 1286 | [字母組合迭代器](https://leetcode.cn/problems//iterator-for-combination/) | 1591 |",
        },
        {
          id: 49,
          title: "4.5 排列型回溯",
          summary:
            "**原理講解**\n\n本節要解決的場景是：把一堆元素「排成一個順序」，列出所有可能的排列。例如把 `[1, 2, 3]` 排成 `[1, 2, 3]、[1, 3, 2]、[2, 1, 3]…` 全部六種（46 全排列）；或在棋盤上放皇后讓彼此不攻擊（51 N 皇后），其實也是「每一列選一個欄位、組成一個放法」。這種「順序有意義、要枚舉所有排法」的問題，就是「排列型回溯」。\n\n:::example 排列的關鍵：每層都從頭掃，靠 used 跳過已用過的元素\n回溯（backtracking）就是「先試一種選擇做下去，試完或走不通就退回來、收回剛才那步、再試別的」。排列和組合最大的差別：**排列在意順序，{1,2} 和 {2,1} 是兩個不同答案，所以每一步都要「能回頭挑前面還沒用過的元素」。** 既然能回頭，就不能再用 `start` 限制方向，改用一個 `used` 旗標陣列記住「哪些元素已經在路徑裡了」。\n\n```cpp\n// 排列的關鍵：每層都從頭掃，靠 used 跳過已用過的元素\nfor (int i = 0; i < n; ++i) {\n  if (used[i]) {\n    continue;  // 已經放進 path 的不能再選\n               // 選 nums[i] ...\n  }\n}\n```\n:::\n\n\n\n:::example 做選擇（標記+入路徑）→ 遞迴 → 撤銷（兩者都還原）\n核心動作仍是「遞迴前做選擇、遞迴後撤銷」，但這次選擇包含「標記 used、加入 path」，撤銷就要把兩者都還原：\n\n```cpp\n// 做選擇（標記+入路徑）→ 遞迴 → 撤銷（兩者都還原）\nused[i] = true;\npath.push_back(nums[i]);\ndfs();\npath.pop_back();\nused[i] = false;  // 現場完全恢復，才能正確試下一個\n```\n:::\n\n\n\n當 `path` 的長度等於元素總數，就湊成了一個完整排列。整體是 `n!` 級別的枚舉，靠剪枝（例如 N 皇后用「同欄、同對角線是否被佔」提前停）大幅加速。\n\n**實作方式**\n\n把排列型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：path 是當前排列前綴，used 記錄哪些已用\n**1. 狀態表示：一條路徑 + 一組 used 旗標。** `path` 是目前排好的前綴，`used[i]` 表示第 i 個元素是否已被放入。\n\n```cpp\n// 狀態：path 是當前排列前綴，used 記錄哪些已用\nvector<int> nums, path;\nvector<bool> used;\nvector<vector<int>> ans;\n```\n:::\n\n\n\n:::example 每層從 0 掃起，未用過才選；回來把 used 與 path 都還原\n**2. 更新流程：每層從頭掃、跳過已用、選、遞迴、撤銷。** 注意排列每一層都從 `0` 開始掃（不像組合用 `start`），靠 `used` 過濾。\n\n```cpp\n// 每層從 0 掃起，未用過才選；回來把 used 與 path 都還原\nfor (int i = 0; i < n; ++i) {\n  if (used[i]) {\n    continue;\n  }\n  used[i] = true;\n  path.push_back(nums[i]);\n  dfs();\n  path.pop_back();\n  used[i] = false;\n}\n```\n:::\n\n\n\n:::example path 排滿全部元素 → 一個完整排列\n**3. 答案讀取：路徑排滿就收集。** 長度達到 `n` 代表一個完整排列產生。\n\n```cpp\n// path 排滿全部元素 → 一個完整排列\nif (path.size() == n) {\n  ans.push_back(path);\n  return;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- N 皇后類問題用「每列放一個」天然避免列衝突，再用三個布林陣列記欄、兩條對角線是否被佔，衝突就剪枝。\n- 解數獨（37）是同型骨架：每個空格枚舉 1~9，檢查行/列/宮是否合法，不合法就回頭。\n- 複雜度最壞 `O(n! * n)`，但好的剪枝（如 N 皇后）能把實際搜尋空間砍得很小。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「排列型回溯」想：\n\n- **順序有意義、要列出所有「排法」或「放法」**，例如 46「全排列」、51/52「N 皇后」、267「迴文排列 II」。\n- **每一步要「從還沒用過的元素裡挑一個」，且能回頭挑前面的**——這就是改用 `used` 而非 `start` 的訊號：\n\n:::example 嗅到味道的瞬間：{1,2} 和 {2,1} 是不同答案，且每個元素恰好用一次\n```cpp\n// 嗅到味道的瞬間：{1,2} 和 {2,1} 是不同答案，且每個元素恰好用一次\n// 於是每層從 0 掃起 + used 過濾，而不是 dfs(i+1)\n```\n:::\n\n\n\n- **「把東西一一就位」型問題**，如 N 皇后「每列放一個皇后」、數獨「每格填一個數」，本質都是排列式的逐位賦值 + 合法性剪枝。\n- **暴力生成所有序列再驗證太慢**；排列回溯邊放邊檢查衝突，提早砍掉死路。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我這題在意順序嗎？（在意 → 排列，用 `used` 而不是 `start`）\n2. 撤銷時我有沒有把 `used[i]` 也設回 `false`？（只還原 path 會出錯）\n3. 我有沒有可剪枝的合法性條件，能在放下去之前就擋掉死路？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 37 | 解數獨 | N/A | 放在「排列型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 46 | 全排列 | N/A | 放在「排列型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 51 | N 皇后 | N/A | 放在「排列型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 52 | N 皇后 II | N/A | 放在「排列型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 267 | 迴文排列 II（Premium） | N/A | 放在「排列型回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 37 | [解數獨](https://leetcode.cn/problems//sudoku-solver/) |  |\n| 46 | [全排列](https://leetcode.cn/problems//permutations/) |  |\n| 51 | [N 皇后](https://leetcode.cn/problems//n-queens/) |  |\n| 52 | [N 皇后 II](https://leetcode.cn/problems//n-queens-ii/) |  |\n| 267 | [迴文排列 II](https://leetcode.cn/problems//palindrome-permutation-ii/) |  |\n| 3437 | [全排列 III](https://leetcode.cn/problems//permutations-iii/) |  |\n| 3799 | [單詞方塊 II](https://leetcode.cn/problems//word-squares-ii/) | 1606 |\n| 3376 | [破解鎖的最少時間 I](https://leetcode.cn/problems//minimum-time-to-break-locks-i/) | 1793 |\n| 2850 | [將石頭分散到網格圖的最少移動次數](https://leetcode.cn/problems//minimum-moves-to-spread-stones-over-grid/) | 2001 |\n| 1718 | [構建字典序最大的可行序列](https://leetcode.cn/problems//construct-the-lexicographically-largest-valid-sequence/) | 2080 |\n| 1307 | [口算難題](https://leetcode.cn/problems//verbal-arithmetic-puzzle/) | 2250 |",
        },
        {
          id: 50,
          title: "4.6 有重複元素的回溯",
          summary:
            "**原理講解**\n\n本節要解決的場景是：做組合 / 排列 / 子集的回溯，但**輸入裡有重複的元素**，例如 `[1, 2, 2]`。如果照普通寫法枚舉，你會得到兩個一模一樣的 `[1, 2]`，因為兩個 `2` 被當成不同的東西各選了一次。本節「有重複元素的回溯」就是專門處理「如何不產生重複答案」。\n\n先理解問題在哪。重複答案來自「在同一個位置（同一層遞迴），用兩個值相同的元素各試了一次」。例如第一層既選了第一個 `2`、又選了第二個 `2`，兩條分支長得完全一樣。關鍵洞察：**重複只發生在「同一層」選了相同的值；不同層選到相同值是合法的（例如答案真的需要兩個 2）。**\n\n:::example 第一步：排序，讓相同的值相鄰，後面才好判斷「同層重複」\n解法分兩步。第一步先「排序」，把相同的值聚在一起，方便偵測。\n\n```cpp\n// 第一步：排序，讓相同的值相鄰，後面才好判斷「同層重複」\nsort(nums.begin(), nums.end());  // [2,1,2] -> [1,2,2]\n```\n:::\n\n\n\n:::example 同層去重：i>start 且和左鄰相同 → 這個值這一層已試過，跳過\n第二步是「同層去重」：在 `for` 迴圈裡，如果目前這個值和「同一層剛剛試過的前一個值」相同，就跳過它。\n\n```cpp\n// 同層去重：i>start 且和左鄰相同 → 這個值這一層已試過，跳過\nif (i > start && nums[i] == nums[i - 1]) {\n  continue;\n}\n```\n:::\n\n\n\n`i > start` 很重要：它代表「這不是本層第一個候選」。本層第一次遇到某個值要正常使用；只有「同一層第二次以後遇到相同值」才跳過。這樣每個不同的值在同一層只會被當作分支一次，重複答案就消失了。其餘骨架（做選擇、遞迴、撤銷）和一般回溯完全相同。\n\n**實作方式**\n\n把有重複元素的回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：先排序的 nums、當前 path；組合用 start，排列額外用 used\n**1. 狀態表示：排好序的輸入 + 路徑（組合題加 start，排列題加 used）。** 排序是這類題的前置動作。\n\n```cpp\n// 狀態：先排序的 nums、當前 path；組合用 start，排列額外用 used\nvector<int> nums, path;  // sort 過\nvector<vector<int>> ans;\n```\n:::\n\n\n\n:::example 組合/子集：i>start 且與左鄰相同 → 同層重複，跳過\n**2. 更新流程（組合 / 子集版）：同層相同值跳過。** 用 `start` 控制只往後挑，再加一行同層去重。\n\n```cpp\n// 組合/子集：i>start 且與左鄰相同 → 同層重複，跳過\nfor (int i = start; i < nums.size(); ++i) {\n  if (i > start && nums[i] == nums[i - 1]) {\n    continue;\n  }\n  path.push_back(nums[i]);\n  dfs(i + 1);\n  path.pop_back();\n}\n```\n:::\n\n\n\n:::example 排列：左鄰相同且左鄰本層未用(used[i-1]==false) → 跳過，避免重複排列\n**3. 更新流程（排列版）：用 used 判斷同層重複。** 排列每層從 0 掃，去重條件要看「左鄰那個相同值是否本層尚未使用」。\n\n```cpp\n// 排列：左鄰相同且左鄰本層未用(used[i-1]==false) → 跳過，避免重複排列\nif (i > 0 && nums[i] == nums[i - 1] && !used[i - 1]) {\n  continue;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要先 `sort`，去重條件才成立（它依賴「相同值相鄰」）。\n- 491「非遞減子序列」不能排序（會破壞原順序），這時改用「本層一個 `unordered_set` 記錄這層用過哪些值」來去重。\n- 複雜度仍是指數級，但去重把重複分支砍掉，實際輸出與搜尋量都明顯變小。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「有重複元素的回溯」想：\n\n- **是組合 / 排列 / 子集題，但輸入「含有重複元素」，且要求結果「不能有重複答案」**，例如 40「組合總和 II」、47「全排列 II」、90「子集 II」、1079「活字印刷」。\n- **你發現普通回溯產生了兩個一模一樣的答案**——這就是要加同層去重的訊號：\n\n:::example 嗅到味道的瞬間：輸出裡冒出重複的 [1,2,2] / [2,2]\n```cpp\n// 嗅到味道的瞬間：輸出裡冒出重複的 [1,2,2] / [2,2]\n// 對策：先 sort，再在 for 裡加「i>start && nums[i]==nums[i-1] → continue」\n```\n:::\n\n\n\n- **題目強調「結果集中不含重複」或「子集 / 排列 II」這種帶 II 的進階版**，幾乎都在考這個去重技巧。\n- **491「非遞減子序列」這種「不能排序」的變形**：仍是同層去重，只是改用「本層 set」記錄用過的值。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我有沒有先 `sort`（或用本層 set）讓相同值可被偵測？\n2. 我的去重條件針對的是「同一層」嗎？（不是把所有相同值都砍掉）\n3. 排列題我用的是 `!used[i - 1]` 的條件，組合題用的是 `i > start` 的條件嗎？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 40 | 組合總和 II | N/A | 放在「有重複元素的回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 47 | 全排列 II | N/A | 放在「有重複元素的回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 90 | 子集 II | N/A | 放在「有重複元素的回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 491 | 非遞減子序列 | N/A | 放在「有重複元素的回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1079 | [活字印刷](https://leetcode.cn/problems/letter-tile-possibilities/) | 1741 | 放在「有重複元素的回溯」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 40 | [組合總和 II](https://leetcode.cn/problems//combination-sum-ii/) |  |\n| 47 | [全排列 II](https://leetcode.cn/problems//permutations-ii/) |  |\n| 90 | [子集 II](https://leetcode.cn/problems//subsets-ii/) |  |\n| 491 | [非遞減子序列](https://leetcode.cn/problems//non-decreasing-subsequences/) |  |\n| 1079 | [活字印刷](https://leetcode.cn/problems//letter-tile-possibilities/) | 1741 |\n| 2014 | [重複 K 次的最長子序列](https://leetcode.cn/problems//longest-subsequence-repeated-k-times/) | 2558 |\n| 3646 | [下一個特殊迴文數](https://leetcode.cn/problems//next-special-palindrome-number/) | 2445 |",
        },
        {
          id: 51,
          title: "4.7 搜尋",
          summary:
            "**原理講解**\n\n本節要解決的場景是：答案藏在一個「會分岔的局面」裡，你必須一步步往下試、走到底看看通不通，不通就退回來換條路。這種「在格子上找一條路徑」或「沿著狀態圖深入探索」的問題，就是本節的「搜尋」——通常指 DFS（深度優先搜尋）型的回溯。\n\n:::example 四方向探索的骨架：對每個鄰格遞迴\n典型例子是 79「單詞搜尋」：在字母網格裡找一條相鄰路徑拼出指定單字。從某格出發，往上下左右四個方向各試一次，每走一格就比對單字的下一個字母，對不上就退回。回溯（backtracking）的精髓在這：**走進一格時把它標記成「已訪問」避免重複踩；退出時把標記清掉，讓別條路徑還能用這格。**\n\n```cpp\n// 四方向探索的骨架：對每個鄰格遞迴\nint dx[4] = {0, 0, 1, -1}, dy[4] = {1, -1, 0, 0};\nfor (int d = 0; d < 4; ++d) {\n  int nx = x + dx[d], ny = y + dy[d];\n  // 若 (nx,ny) 在界內、未訪問、且符合條件 → 往那走\n}\n```\n:::\n\n\n\n:::example 進格子前標記，遞迴探索，回來時還原（這格能被其他路徑再用）\n最關鍵的就是「進來標記、出去還原」這一對動作，否則一條路會把格子佔死，別條路就走不了：\n\n```cpp\n// 進格子前標記，遞迴探索，回來時還原（這格能被其他路徑再用）\nvisited[x][y] = true;\nbool found = dfs(x, y, k + 1);  // 嘗試比對單字第 k+1 個字母\nvisited[x][y] = false;          // 恢復現場\n```\n:::\n\n\n\n走到「拼完整個單字」就是成功；四個方向都失敗就回頭。這是指數級的探索，靠「字母不符就立刻停」這種剪枝大幅縮短。126「單詞接龍 II」、247「中心對稱數 II」也都是同一套「深入—碰壁—回退」的搜尋骨架，只是局面從網格換成了字典圖或數字構造。\n\n**實作方式**\n\n把搜尋型回溯拆成四步，逐一對應程式碼：\n\n:::example 狀態：座標 (x,y)、比對進度 k、訪問標記 visited\n**1. 狀態表示：目前位置 + 進度 + 訪問標記。** 在網格題裡是 `(x, y)` 座標、已比對到單字第幾個字母、以及 `visited` 陣列。\n\n```cpp\n// 狀態：座標 (x,y)、比對進度 k、訪問標記 visited\nvector<vector<char>> board;\nstring word;\nvector<vector<bool>> visited;\n```\n:::\n\n\n\n:::example 標記當前格 → 探索四鄰 → 還原標記\n**2. 更新流程：標記、向鄰居遞迴、還原。** 從當前格往四方向試，符合條件才深入；無論成功與否，回來都要清掉標記。\n\n```cpp\n// 標記當前格 → 探索四鄰 → 還原標記\nvisited[x][y] = true;\nfor (int d = 0; d < 4; ++d) {\n  int nx = x + dx[d], ny = y + dy[d];\n  if (ok(nx, ny) && board[nx][ny] == word[k + 1] && dfs(nx, ny, k + 1)) {\n    return true;\n  }\n}\nvisited[x][y] = false;  // 恢復現場，讓其他路徑可用此格\n```\n:::\n\n\n\n:::example 比對到單字最後一個字母 → 找到一條合法路徑\n**3. 答案讀取：抵達終止狀態即成功。** 比對到單字最後一個字母（或到達目標局面），就回傳成功 / 收集這條路徑。\n\n```cpp\n// 比對到單字最後一個字母 → 找到一條合法路徑\nif (k == (int)word.size() - 1) {\n  return true;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 越界、已訪問、字母不符都要在「踏進去之前」擋掉，這是主要剪枝。\n- 212「單詞搜尋 II」要同時找很多單字，會把字典建成 Trie（字典樹）一邊走網格一邊匹配，避免每個單字各掃一遍。\n- 複雜度最壞是指數級（每格約四個分支），但字母 / 邊界剪枝讓實際分支遠少於此。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「搜尋」想：\n\n- **要在網格 / 圖 / 構造空間裡「找一條路徑或一個解」，可能要試很多分岔**，例如 79「單詞搜尋」、212「單詞搜尋 II」、126「單詞接龍 II」、247「中心對稱數 II」、LCP 58「積木拼接」。\n- **走某一步會「佔用資源」，走完要釋放**——這就是「進來標記、出去還原」的訊號：\n\n:::example 嗅到味道的瞬間：你需要「這格暫時不能再走」，但回頭後又要放開\n```cpp\n// 嗅到味道的瞬間：你需要「這格暫時不能再走」，但回頭後又要放開\n// visited[x][y]=true ... 遞迴 ... visited[x][y]=false\n```\n:::\n\n\n\n- **每一步只能在「相鄰 / 合法的下一狀態」中選擇**（上下左右、字典裡差一個字母、下一塊積木的接法），是典型的圖上 DFS。\n- **暴力列出所有路徑再篩選會爆炸**；搜尋型回溯邊走邊用條件剪枝，提早砍掉死路。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的「目前局面」由哪些變數描述？（座標？進度？已訪問集合？）\n2. 從一個局面能走到哪些「下一個局面」？合法條件是什麼？\n3. 我有沒有「進來標記、回去還原」，避免一條路把資源佔死？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 58 | LCP 58. 積木拼接 | N/A | 放在「搜尋」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 79 | 單詞搜尋 | N/A | 放在「搜尋」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 126 | 單詞接龍 II | N/A | 放在「搜尋」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 212 | 單詞搜尋 II | N/A | 放在「搜尋」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 247 | 中心對稱數 II（Premium） | N/A | 放在「搜尋」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 58 | [LCP 58. 積木拼接](https://leetcode.cn/problems//De4qBB/) |  |\n| 79 | [單詞搜尋](https://leetcode.cn/problems//word-search/) |  |\n| 126 | [單詞接龍 II](https://leetcode.cn/problems//word-ladder-ii/) |  |\n| 212 | [單詞搜尋 II](https://leetcode.cn/problems//word-search-ii/) |  |\n| 247 | [中心對稱數 II](https://leetcode.cn/problems//strobogrammatic-number-ii/) |  |\n| 248 | [中心對稱數 III](https://leetcode.cn/problems//strobogrammatic-number-iii/) |  |\n| 282 | [給表示式新增運算子](https://leetcode.cn/problems//expression-add-operators/) |  |\n| 411 | [最短獨佔單詞縮寫](https://leetcode.cn/problems//minimum-unique-word-abbreviation/) |  |\n| 473 | [火柴拼正方形](https://leetcode.cn/problems//matchsticks-to-square/) |  |\n| 488 | [祖瑪遊戲](https://leetcode.cn/problems//zuma-game/) |  |\n| 638 | [大禮包](https://leetcode.cn/problems//shopping-offers/) |  |\n| 679 | [點遊戲](https://leetcode.cn/problems//24-game/) |  |\n| 691 | [貼紙拼詞](https://leetcode.cn/problems//stickers-to-spell-word/) |  |\n| 756 | [金字塔轉換矩陣](https://leetcode.cn/problems//pyramid-transition-matrix/) |  |\n| 2664 | [巡邏的騎士](https://leetcode.cn/problems//the-knights-tour/) |  |\n| 3565 | [順序網格路徑覆蓋](https://leetcode.cn/problems//sequential-grid-path-cover/) |  |\n| 1725 | [面試題 17.25. 單詞矩陣](https://leetcode.cn/problems//word-rectangle-lcci/) | 1229 |\n| 3211 | [生成不含相鄰零的二進位制字串](https://leetcode.cn/problems//generate-binary-strings-without-adjacent-zeros/) | 1353 |\n| 967 | [連續差相同的數字](https://leetcode.cn/problems//numbers-with-same-consecutive-differences/) | 1433 |\n| 2094 | [找出 3 位偶數](https://leetcode.cn/problems//finding-3-digit-even-numbers/) | 1455 |\n| 1415 | [長度為 n 的開心字串中字典序第 k 小的字串](https://leetcode.cn/problems//the-k-th-lexicographical-string-of-all-happy-strings-of-length-n/) | 1576 |\n| 1219 | [黃金礦工](https://leetcode.cn/problems//path-with-maximum-gold/) | 1663 |\n| 980 | [不同路徑 III](https://leetcode.cn/problems//unique-paths-iii/) | 1830 |\n| 2002 | [兩個迴文子序列長度的最大乘積](https://leetcode.cn/problems//maximum-product-of-the-length-of-two-palindromic-subsequences/) | 1869 |\n| 1255 | [得分最高的單詞集合](https://leetcode.cn/problems//maximum-score-words-formed-by-letters/) | 1882 |\n| 3669 | [K 因數分解](https://leetcode.cn/problems//balanced-k-factor-decomposition/) | 1917 |\n| 1088 | [易混淆數 II](https://leetcode.cn/problems//confusing-number-ii/) | 2077 |\n| 1240 | [鋪瓷磚](https://leetcode.cn/problems//tiling-a-rectangle-with-the-fewest-squares/) | 2242 |\n| 2056 | [棋盤上有效移動組合的數目](https://leetcode.cn/problems//number-of-valid-move-combinations-on-chessboard/) | 2611 |\n| 2386 | [找出陣列的第 K 大和](https://leetcode.cn/problems//find-the-k-sum-of-an-array/) | 2648 |\n| 3509 | [最大化交錯和為 K 的子序列乘積](https://leetcode.cn/problems//maximum-product-of-subsequences-with-an-alternating-sum-equal-to-k/) | 2703 |",
        },
        {
          id: 52,
          title: "4.8 折半列舉",
          summary:
            "**原理講解**\n\n本節要解決的場景是：每個元素都要在「選 / 不選」之間二選一（或從幾種值裡挑一個），要枚舉所有組合。直接枚舉是 `2^n` 種，n 一到 40 就是一兆級別，跑不動。但如果 n 大約是 40，`2^40` 太大、`2^20` 卻只有一百萬——本節的「折半列舉（meet in the middle，又叫雙向搜尋）」就是把指數的「指數部分砍一半」的技巧。\n\n:::example 步驟一：左半枚舉所有子集和，存進一個容器\n核心想法只有一句：**把元素切成左右兩半，各自暴力枚舉所有子集（各 `2^(n/2)` 種），再想辦法把兩半「拼起來」湊出完整答案。** 例如 494「目標和」要選正負號讓總和等於 target：左半枚舉出所有可能的部分和、右半也枚舉出所有部分和，那麼「左半和 + 右半和 = target」就是答案，等價於「對每個左半和 L，去右半找有沒有值等於 `target - L`」。\n\n```cpp\n// 步驟一：左半枚舉所有子集和，存進一個容器\nvector<long long> leftSums;  // 大小約 2^(n/2)\nfor (int mask = 0; mask < (1 << half); ++mask) {\n  leftSums.push_back(subsetSum(left, mask));\n}\n```\n:::\n\n\n\n:::example 步驟二：右半排序後，對每個左半和用二分查 target - L 是否存在\n關鍵在「拼接」要快。如果對每個左半值都線性掃右半，又退回 `2^n`。所以右半要先排序或放進雜湊表，讓查詢變成 `O(log m)` 或 `O(1)`：\n\n```cpp\n// 步驟二：右半排序後，對每個左半和用二分查 target - L 是否存在\nsort(rightSums.begin(), rightSums.end());\nfor (long long L : leftSums) {\n  cnt += countEqual(rightSums, target - L);  // 二分找區間\n}\n```\n:::\n\n\n\n這樣總時間是 `O(2^(n/2) * (n/2))` 加上排序與查詢，足以把 n≈40 的題目壓進可接受範圍。它的本質是「用空間換時間」：多存了兩半的枚舉結果，換掉了一半的指數。\n\n**實作方式**\n\n把折半列舉拆成四步，逐一對應程式碼：\n\n:::example 狀態：左右兩半的所有子集和\n**1. 狀態表示：左右兩半各自的枚舉結果。** 把 n 個元素對半分，分別準備兩個容器存它們所有子集的摘要（通常是子集和）。\n\n```cpp\n// 狀態：左右兩半的所有子集和\nint n;\nvector<int> a;\nint half = n / 2;  // 左半 [0,half)，右半 [half,n)\nvector<long long> leftSums, rightSums;\n```\n:::\n\n\n\n:::example 用 mask 的每個 bit 決定該元素選不選，累出這個子集的和\n**2. 枚舉一半：用位元遮罩列出所有子集。** `mask` 的每一個 bit 代表對應元素「選或不選」，跑遍 `0..2^k - 1` 就枚舉了所有子集。\n\n```cpp\n// 用 mask 的每個 bit 決定該元素選不選，累出這個子集的和\nlong long subsetSum(int lo, int hi, int mask) {\n  long long s = 0;\n  for (int i = lo; i < hi; ++i) {\n    if (mask >> (i - lo) & 1) {\n      s += a[i];\n    }\n  }\n  return s;\n}\n```\n:::\n\n\n\n:::example 右半排序後二分查 target - L，累計符合的數量\n**3. 拼接兩半：排序 + 二分（或雜湊）快速配對。** 把右半排好序，對每個左半值用二分查「需要的另一半」是否存在、有幾個。\n\n```cpp\n// 右半排序後二分查 target - L，累計符合的數量\nsort(rightSums.begin(), rightSums.end());\nfor (long long L : leftSums) {\n  auto lo = lower_bound(rightSums.begin(), rightSums.end(), target - L);\n  auto hi = upper_bound(rightSums.begin(), rightSums.end(), target - L);\n  ans += hi - lo;\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 拼接條件依題而變：求「剛好等於」用二分定位；求「最接近 target」（1755）對每個 L 找右半中最接近 `target - L` 的值。\n- 注意溢位：部分和可能很大，用 `long long`。\n- 複雜度 `O(2^(n/2) * (n + log))`；空間 `O(2^(n/2))` 存兩半結果。只有在 `2^n` 太大、`2^(n/2)` 可接受時才划算。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「折半列舉」想：\n\n- **每個元素「選 / 不選」或「正號 / 負號」，要枚舉所有組合，而 n 大約落在 30~45**，例如 494「目標和」、805「陣列的均值分割」、1755「最接近目標值的子序列和」、3566「等積子集的劃分方案」、LCP 82「萬靈之樹」。\n- **`2^n` 會超時，但 `2^(n/2)` 算得動**——這個數字感覺就是折半的訊號：\n\n:::example 嗅到味道的瞬間：n≈40，2^40 太大但 2^20≈1e6 可接受\n```cpp\n// 嗅到味道的瞬間：n≈40，2^40 太大但 2^20≈1e6 可接受\n// → 切兩半各枚舉 2^20，再拼起來\n```\n:::\n\n\n\n- **答案能拆成「左半貢獻 + 右半貢獻」並用簡單條件配對**（和相等、和最接近、乘積相等），就能左右分開算再合併。\n- **暴力 `2^n` 枚舉超時、又不是 DP / 貪心能解的子集問題**，折半列舉常是突破口。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我能把元素切成兩半、各自獨立枚舉嗎？每半的「摘要」是什麼（和？乘積？）\n2. 兩半要用什麼條件拼接？（相等？最接近？）配對能不能靠排序 + 二分加速？\n3. n 的大小是否落在「`2^n` 太大但 `2^(n/2)` 可行」的甜蜜區？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 82 | LCP 82. 萬靈之樹 | N/A | 放在「折半列舉」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 494 | 目標和 | N/A | 放在「折半列舉」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 805 | 陣列的均值分割 | N/A | 放在「折半列舉」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3566 | [等積子集的劃分方案](https://leetcode.cn/problems/partition-array-into-two-equal-product-subsets/) | 1459 | 放在「折半列舉」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1755 | [最接近目標值的子序列和](https://leetcode.cn/problems/closest-subsequence-sum/) | 2364 | 放在「折半列舉」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 46. 全排列 展示核心寫法。\n\n**完整題目**：給定不重複整數陣列，列出所有可能排列。\n\n:::example 回溯 DFS：每層從未使用的元素中選一個加入 path\n**線索**：題目要求列出所有方案、排列、組合或路徑；需要逐步選擇並在返回時撤銷。\n\n```cpp\nclass Solution {\n public:\n  vector<vector<int>> permute(vector<int>& nums) {\n    vector<vector<int>> ans;\n    vector<int> path;                // 當前排列路徑\n    vector<char> used(nums.size());  // 標記各元素是否已加入路徑\n    // 回溯 DFS：每層從未使用的元素中選一個加入 path\n    function<void()> dfs = [&]() {\n      if (path.size() == nums.size()) {\n        ans.push_back(path);  // 路徑長度等於 nums 時得到完整排列\n        return;\n      }\n      for (int i = 0; i < (int)nums.size(); ++i) {\n        if (used[i]) {\n          continue;  // 已使用的元素跳過\n        }\n        used[i] = true;\n        path.push_back(nums[i]);\n        dfs();\n        // 回溯：撤銷選擇，讓後續元素有機會被選入\n        path.pop_back();\n        used[i] = false;\n      }\n    };\n    dfs();\n    return ans;\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 回溯維護路徑與可選集合；遞迴返回前恢復現場，確保下一個分支看到同樣的狀態。\n- `path` 保存目前已做的選擇，`used` 或 `start` 控制下一步可選集合。\n- 每次遞迴後都要撤銷選擇，讓下一個分支看到相同的現場。\n\n**常見變形**\n\n- 排序後用 `i > start && nums[i] == nums[i - 1]` 去重。\n- 用 bitmask 表示已選集合。\n- 可與 DFS、DP 記憶化結合剪枝。\n\n**實作注意事項**\n\n- push 後必須 pop，標記後必須復原。\n- 排列與組合的 start/used 語意不同。\n- 剪枝條件要保證不漏解。\n- 答案要拷貝 path，而不是存引用。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 82 | [LCP 82. 萬靈之樹](https://leetcode.cn/problems//cnHoX6/) |  |\n| 494 | [目標和](https://leetcode.cn/problems//target-sum/) |  |\n| 805 | [陣列的均值分割](https://leetcode.cn/problems//split-array-with-same-average/) |  |\n| 3566 | [等積子集的劃分方案](https://leetcode.cn/problems//partition-array-into-two-equal-product-subsets/) | 1459 |\n| 1755 | [最接近目標值的子序列和](https://leetcode.cn/problems//closest-subsequence-sum/) | 2364 |\n| 956 | [最高的廣告牌](https://leetcode.cn/problems//tallest-billboard/) | 2381 |\n| 3801 | [合併有序列表的最小成本](https://leetcode.cn/problems//minimum-cost-to-merge-sorted-lists/) | 2399 |\n| 2035 | [將陣列分成兩個陣列並最小化陣列和的差](https://leetcode.cn/problems//partition-array-into-two-arrays-to-minimize-sum-difference/) | 2490 |\n| 3267 | [統計近似相等數對 II](https://leetcode.cn/problems//count-almost-equal-pairs-ii/) | 2545 |",
        },
      ],
    },
    {
      id: 53,
      title: "5. 其他遞迴/分治",
      summary:
        "分治與遞迴在鏈表上的應用：merge sort 鏈表、分治反轉；注意斷鏈與歸併後頭節點返回。\n\n**複雜度**：找中點 $O(n)$，總 $O(n\\log n)$。",
      children: [
        {
          id: 54,
          title: "5.1 應用題",
          summary:
            "**原理講解**\n\n本節是「其他遞迴 / 分治」的應用題集合。分治（divide and conquer）的核心想法是：**把一個大問題切成幾個同型的小問題，分別解決，再把小答案合併成大答案。** 遞迴（recursion）則是它的實現手段——一個函式「呼叫自己」去處理更小的子問題，小到不能再小（基底情形）就直接回答。\n\n:::example 分治三部曲：切一半 → 遞迴各排各的 → 合併兩段有序結果\n最經典的例子是排序。912「排序陣列」用合併排序：把陣列切兩半、各自排好、再合併兩條有序序列。\n\n```cpp\n// 分治三部曲：切一半 → 遞迴各排各的 → 合併兩段有序結果\nvoid mergeSort(int l, int r) {\n  if (l >= r) {\n    return;  // 基底情形：只剩 0 或 1 個元素，本就有序\n  }\n  int m = (l + r) / 2;\n  mergeSort(l, m);\n  mergeSort(m + 1, r);\n  merge(l, m, r);  // 合併兩段\n}\n```\n:::\n\n\n\n:::example 減治：分區後只往「答案所在的那半」遞迴，另一半丟掉\n另一種「分治」不需要兩邊都解，只要選一邊深入——這叫「減治」。215「陣列中的第 K 個最大元素」用快速選擇（quickselect）：隨機選一個基準把陣列分成「大的一堆 / 小的一堆」，依照 K 落在哪一堆，只遞迴那一邊，平均 `O(n)`。\n\n```cpp\n// 減治：分區後只往「答案所在的那半」遞迴，另一半丟掉\nint p = partition(l, r);  // 基準歸位，左小右大\nif (k == p) {\n  return a[p];\n}\nreturn (k < p) ? quickSelect(l, p - 1, k) : quickSelect(p + 1, r, k);\n```\n:::\n\n\n\n還有一類完全不碰原始資料，而是用遞迴「公式」直接算出第幾個位置的答案，例如 1545「找第 N 個二進位字串的第 K 位」、880「索引處的解碼字串」——它們利用「第 n 層由第 n-1 層按規則生成」的結構，把 K 不斷往前一層折算，避免真的把整個巨大字串造出來。共通精神都是：**找到「大問題 = 小問題 + 合併規則」的遞迴關係。**\n\n**實作方式**\n\n把這類分治 / 遞迴題拆成四步，逐一對應程式碼：\n\n:::example 狀態：子問題的範圍或層次參數\n**1. 狀態表示：用「範圍」或「層數 / 位置」描述子問題。** 排序類用區間 `[l, r]`；公式類用「第幾層、第幾位」。\n\n```cpp\n// 狀態：子問題的範圍或層次參數\nvector<int> a;\n// mergeSort(l, r) / quickSelect(l, r, k) / solve(n, k) ...\n```\n:::\n\n\n\n:::example 公式類：第 n 層長度 = 上一層的 2 倍 +1，據此把 k 折回上一層\n**2. 切分：把問題拆成更小的同型子問題。** 排序對半切；快速選擇用基準分區；公式類往「上一層」折算。\n\n```cpp\n// 公式類：第 n 層長度 = 上一層的 2 倍 +1，據此把 k 折回上一層\nlong long len = (1LL << n) - 1;  // 第 n 個字串長度\nlong long mid = len / 2 + 1;     // 正中間那位\nif (k == mid) {\n  return '1';  // 基底\n               // 否則把 k 對映回第 n-1 層的相對位置，再遞迴\n}\n```\n:::\n\n\n\n:::example 合併兩段有序區間（合併排序的核心）\n**3. 合併 / 選擇：把子答案組回大答案。** 合併排序要 `merge` 兩段；快速選擇只取一邊；公式類直接由折算結果回答。\n\n```cpp\n// 合併兩段有序區間（合併排序的核心）\nvoid merge(int l, int m, int r) {\n  vector<int> tmp;\n  int i = l, j = m + 1;\n  while (i <= m && j <= r) {\n    tmp.push_back(a[i] <= a[j] ? a[i++] : a[j++]);\n  }\n  while (i <= m) {\n    tmp.push_back(a[i++]);\n  }\n  while (j <= r) {\n    tmp.push_back(a[j++]);\n  }\n  for (int k = 0; k < (int)tmp.size(); ++k) {\n    a[l + k] = tmp[k];\n  }\n}\n```\n:::\n\n\n\n**4. 邊界與複雜度。**\n\n- 一定要有清楚的基底情形（區間長度 ≤ 1、或折到第 0/1 層），否則遞迴不收斂。\n- 合併排序穩定 `O(n log n)`；快速選擇平均 `O(n)`、最壞 `O(n^2)`（基準隨機化可避免）。\n- 公式類（1545、880、3537）關鍵是「不要真的建出整個結構」，只把查詢位置往前折算，通常 `O(log)` 或 `O(層數)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「分治 / 遞迴應用」想：\n\n- **大問題能自然切成「同型的小問題」再合併**，例如 912「排序陣列」（合併排序）、215「第 K 大」（快速選擇）。\n- **第 n 層的結構由第 n-1 層按固定規則生成**，要查某個位置卻不可能把整個結構造出來——往「公式遞迴 / 折算」想：\n\n:::example 嗅到味道的瞬間：字串 / 網格大到無法真的生成，但有「上一層 → 這一層」的規律\n```cpp\n// 嗅到味道的瞬間：字串 / 網格大到無法真的生成，但有「上一層 → 這一層」的規律\n// 880「解碼字串」、1545「第 N 個二進位字串」、3537「填充特殊網格」都是這型\n```\n:::\n\n\n\n- **只需要答案的「一部分」（第 K 個、某一位）而非全部**——常可只遞迴一邊（減治），省掉另一半。\n- **暴力把整個結構建出來會爆記憶體 / 超時**；分治用遞迴關係跳過大部分計算。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的大問題能拆成哪幾個「同型小問題」？合併規則是什麼？\n2. 基底情形（最小、可直接回答的情況）是什麼？遞迴會收斂到它嗎？\n3. 我是否只需要答案的一部分，能不能只遞迴一邊、或用公式折算而不真的建出結構？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 215 | 陣列中的第K個最大元素 | N/A | 放在「應用題」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 912 | 排序陣列 | N/A | 放在「應用題」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 1545 | [找出第 N 個二進位制字串中的第 K 位](https://leetcode.cn/problems/find-kth-bit-in-nth-binary-string/) | 1479 | 放在「應用題」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 3537 | [填充特殊網格](https://leetcode.cn/problems/fill-a-special-grid/) | 1542 | 放在「應用題」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n| 880 | [索引處的解碼字串](https://leetcode.cn/problems/decoded-string-at-index/) | 2011 | 放在「應用題」下，是為了練習該小節的核心狀態與不變式；請先寫出資料如何表示，再寫答案如何更新。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 104. 二叉樹的最大深度 展示核心寫法。\n\n**完整題目**：給定二叉樹根節點，回傳從根到最深葉節點的節點數。\n\n:::example 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n**線索**：題目資料本身是樹，或答案可由子樹合併/祖先下傳；DFS 的回傳值與參數就是核心線索。\n\n```cpp\nclass Solution {\n public:\n  // 遞迴求二元樹最大深度：左右子樹深度取大值再加 1\n  int maxDepth(TreeNode* root) {\n    if (!root) {\n      return 0;\n    }\n    return 1 + max(maxDepth(root->left), maxDepth(root->right));\n  }\n};\n```\n:::\n\n\n\n**程式碼拆解**\n\n- 後序 DFS 從子樹回傳資訊，父節點只負責合併左右答案；樹形 DP 也遵循這個資料流。\n- 遞迴函式回傳可被父節點合併的資訊，空節點返回值要和合併式相容。\n- 若需要全域答案，通常在回溯合併左右子樹時更新，而不是只看根節點。\n\n**常見變形**\n\n- 直徑題回傳向下最大鏈，順手更新全域答案。\n- LCA 可用遞迴、倍增或 Tarjan 離線。\n- 換根 DP 需要先下後上兩次遍歷。\n\n**實作注意事項**\n\n- 空節點的返回值要與合併式相容。\n- 全域答案在遞迴中更新時注意初始化。\n- 一般樹 DFS 要跳過 parent。\n- 深樹可能遞迴溢位，必要時改迭代。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 215 | [陣列中的第K個最大元素](https://leetcode.cn/problems//kth-largest-element-in-an-array/) |  |\n| 912 | [排序陣列](https://leetcode.cn/problems//sort-an-array/) |  |\n| 1545 | [找出第 N 個二進位制字串中的第 K 位](https://leetcode.cn/problems//find-kth-bit-in-nth-binary-string/) | 1479 |\n| 3537 | [填充特殊網格](https://leetcode.cn/problems//fill-a-special-grid/) | 1542 |\n| 880 | [索引處的解碼字串](https://leetcode.cn/problems//decoded-string-at-index/) | 2011 |\n| 3614 | [用特殊操作處理字串 II](https://leetcode.cn/problems//process-string-with-special-operations-ii/) | 2011 |\n| 3307 | [找出第 K 個字元 II](https://leetcode.cn/problems//find-the-k-th-character-in-string-game-ii/) | 2232 |\n| 932 | [漂亮陣列](https://leetcode.cn/problems//beautiful-array/) | 2294 |",
        },
      ],
    },
    {
      id: 9015,
      title: "6. 常見面試模式",
      summary:
        "樹題的核心是選對走訪順序與在遞迴中傳遞/回傳的資訊。先辨認下表訊號，再決定後序聚合、附帶狀態、利用 BST 性質或層序展開。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 後序樹形 DP | 答案由左右子樹結果合併 | 後序遞迴，子樹回傳聚合值，於每個節點更新全域答案 | [二叉樹中的最大路徑和](https://leetcode.cn/problems/binary-tree-maximum-path-sum) |\n| 帶狀態樹 DP | 相鄰節點互斥/選或不選 | 每節點回傳「選/不選」兩種狀態，父層據此取最優 | [打家劫舍 III](https://leetcode.cn/problems/house-robber-iii) |\n| BST 中序 | 與排序、第 `k` 小、區間有關 | 利用 BST 中序走訪即遞增序列的性質 | [驗證二叉搜尋樹](https://leetcode.cn/problems/validate-binary-search-tree) |\n| 層序 BFS | 按層處理、求最右/每層極值 | 佇列逐層展開，記錄當前層大小一次處理一層 | [二叉樹的層序遍歷](https://leetcode.cn/problems/binary-tree-level-order-traversal) |\n| 最近公共祖先 | 求兩節點的 LCA | 後序遞迴，左右子樹各找到一個目標時當前節點即為 LCA | [二叉樹的最近公共祖先](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree) |\n| 由走訪重建 | 給定前/中/後序還原樹 | 用前(後)序定根、中序分左右子樹，遞迴建樹 | [從前序與中序遍歷序列構造二叉樹](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal) |\n| 換根 DP | 求以每個節點為根的某指標 | 先一次 DFS 算出某根答案，再一次 DFS 將答案由父推子，`O(n)` | [樹中距離之和](https://leetcode.cn/problems/sum-of-distances-in-tree) |\n\n**小提醒**\n- 想清楚遞迴函式「回傳什麼」與「順帶更新什麼全域答案」，兩者常常不同。\n- 後序適合自底向上聚合；前序適合自頂向下傳遞路徑資訊。\n- 換根的關鍵是寫出由父節點答案推導子節點答案的轉移式，避免每個根都重算。",
    },
  ],
} as TutorialData.Root;
