import type { TutorialData } from "@/types";

export const waymoInterview = {
  id: 1,
  title: "Waymo 面試準備",
  src: "",
  last_update: "2026-06-06T11:20:16.645Z",
  summary:
    "這份 Waymo 面試準備題單把自動駕駛常見的演算法面試題整理成可複習的筆記：每一章先講何時用這類技巧、面試中要先問自己的判斷問題、常見複雜度，再附上可改寫到題目中的 C++ 模板。\n\n章節涵蓋路徑規劃與最短路、佔據網格、計算幾何與碰撞偵測、道路網建模、感測器資料流、車載即時系統與車隊平台設計。建議先讀觀念說明，再限時做題，最後用模板反推自己的程式碼。",
  children: [
    {
      id: 2,
      title: "1. 路徑規劃：圖搜尋與最短路",
      summary:
        "**觀念起點**\n\n路徑規劃是自駕系統最核心的搜尋問題：把路口、車道段、可行駛的網格格子當成節點，把「開過去的代價」當成邊權，問題就變成「從目前位置到目標的最短路」。先決定每條邊的成本長相，再選對演算法。\n\n每一步代價相同（例如等距網格、每段車道都算一步）時，BFS 的層序就保證最短步數；代價只有 0 與 1（順向免費、變換車道罰 1）時，用 deque 做 0-1 BFS；代價是任意非負值（不同路段耗時不同）時，才需要 priority queue 的 Dijkstra；若還有可採納的啟發式（到目標的直線距離），就升級成 A*。\n\nvisited 的維度必須等於完整狀態。同一格在「剩餘繞行預算不同」「已收集的通行證不同」時是不同狀態，不能只用座標去重，否則會漏掉更省的路線。\n\n**系統例題：含路障消除預算的最短路**\n\n把場景看成 0/1 佔據網格，0 可走、1 是路障。車輛最多能強行穿越 k 個路障（例如臨時推開錐桶），求左上到右下的最少步數。狀態是 `(row, col, left)`，`left` 是剩餘穿越次數；每步代價皆為 1，所以用 BFS，並用「到達該格時看過的最大剩餘預算」剪枝。\n\n```cpp\n// shortest grid path with at most k obstacle crossings\nint planPath(vector<vector<int>>& grid, int k) {\n  int rows = grid.size(), cols = grid[0].size();\n  vector<vector<int>> best(rows, vector<int>(cols, -1));\n  queue<array<int, 4>> q;  // row, col, remaining, dist\n  q.push({0, 0, k, 0});\n  best[0][0] = k;\n  const int step[5] = {1, 0, -1, 0, 1};\n  while (!q.empty()) {\n    auto [r, c, left, dist] = q.front();\n    q.pop();\n    if (r == rows - 1 && c == cols - 1) {\n      return dist;\n    }\n    for (int d = 0; d < 4; ++d) {\n      int nr = r + step[d], nc = c + step[d + 1];\n      if (nr < 0 || nr >= rows || nc < 0 || nc >= cols) {\n        continue;\n      }\n      int nleft = left - grid[nr][nc];\n      if (nleft < 0 || nleft <= best[nr][nc]) {\n        continue;  // out of budget, or we reached here with more slack before\n      }\n      best[nr][nc] = nleft;\n      q.push({nr, nc, nleft, dist + 1});\n    }\n  }\n  return -1;\n}\n```\n\n**A* 的關鍵：g + h，且 h 不可高估**\n\nDijkstra 盲目地向四周擴張；A* 用一個「到目標的樂觀估計」h（網格上常用曼哈頓或歐式距離）把搜尋拉向目標。只要 h 從不高估真實剩餘代價（admissible），A* 找到的就是最短路，而且通常展開的節點遠少於 Dijkstra。\n\n```cpp\n// A* on a weighted graph with an admissible heuristic h(node)\nlong long astar(int n, vector<vector<pair<int, int>>>& g, int s, int target,\n                function<long long(int)> h) {\n  const long long kInf = 4e18;\n  vector<long long> dist(n, kInf);\n  priority_queue<pair<long long, int>, vector<pair<long long, int>>,\n                 greater<>> pq;  // f = g + h, node\n  dist[s] = 0;\n  pq.push({h(s), s});\n  while (!pq.empty()) {\n    auto [f, u] = pq.top();\n    pq.pop();\n    if (u == target) {\n      return dist[u];\n    }\n    if (f - h(u) > dist[u]) {\n      continue;  // stale entry\n    }\n    for (auto [v, w] : g[u]) {\n      if (dist[u] + w < dist[v]) {\n        dist[v] = dist[u] + w;\n        pq.push({dist[v] + h(v), v});\n      }\n    }\n  }\n  return -1;\n}\n```\n\n面試時先講清楚邊權型態與狀態維度，再說為什麼選 BFS / 0-1 BFS / Dijkstra / A*；若把「剩餘預算」誤當成可加成本，模型就要從 BFS 改成 Dijkstra。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3112 | [訪問消失節點的最少時間](https://leetcode.cn/problems/minimum-time-to-visit-disappearing-nodes) | 1757 |\n| 1129 | [顏色交替的最短路徑](https://leetcode.cn/problems/shortest-path-with-alternating-colors) | 1780 |\n| 1514 | [機率最大的路徑](https://leetcode.cn/problems/path-with-maximum-probability) | 1846 |\n| 1334 | [閾值距離內鄰居最少的城市](https://leetcode.cn/problems/find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance) | 1855 |\n| 1786 | [從第一個節點出發到最後一個節點的受限路徑數](https://leetcode.cn/problems/number-of-restricted-paths-from-first-to-last-node) | 2079 |\n| 1976 | [到達目的地的方案數](https://leetcode.cn/problems/number-of-ways-to-arrive-at-destination) | 2095 |\n| 2812 | [找出最安全路徑](https://leetcode.cn/problems/find-the-safest-path-in-a-grid) | 2154 |\n| 3244 | [新增道路查詢後的最短距離 II](https://leetcode.cn/problems/shortest-distance-after-road-addition-queries-ii) | 2270 |",
    },
    {
      id: 3,
      title: "2. 佔據網格與多源 BFS",
      summary:
        "**觀念起點**\n\n感知模組輸出的常是佔據網格（occupancy grid）：每格標記可行駛、被占用或未知。很多問題其實是「在網格上做一次線性掃描的 BFS」——距離場、可達區域、被包圍的死角、最近的某類目標。\n\n辨識訊號：要「到所有格子的最短距離」用單源 BFS；要「到最近的某一類來源的距離」用多源 BFS，把所有來源同時入隊、距離設 0；要「能不能走到邊界 / 逃出去」用從邊界反向灌水（flood fill）。\n\nvisited 不要只看座標。若同一格在不同「方向、已轉彎次數、剩餘油量」下行為不同，這些都要進狀態，否則答案會偏小。\n\n**系統例題：到最近障礙的距離場（多源 BFS）**\n\n給佔據網格，要為每個可行駛格算出「到最近障礙的步數」，規劃時就能對障礙保持安全邊界。把所有障礙當作多源同時入隊，一次 BFS 就得到整張距離場。\n\n```cpp\n// multi-source BFS distance field: steps from each cell to the nearest obstacle\nvector<vector<int>> distanceField(vector<vector<int>>& occ) {\n  int rows = occ.size(), cols = occ[0].size();\n  vector<vector<int>> dist(rows, vector<int>(cols, -1));\n  queue<pair<int, int>> q;\n  for (int r = 0; r < rows; ++r) {\n    for (int c = 0; c < cols; ++c) {\n      if (occ[r][c] == 1) {  // obstacle is a source\n        dist[r][c] = 0;\n        q.push({r, c});\n      }\n    }\n  }\n  const int dir[5] = {1, 0, -1, 0, 1};\n  while (!q.empty()) {\n    auto [r, c] = q.front();\n    q.pop();\n    for (int d = 0; d < 4; ++d) {\n      int nr = r + dir[d], nc = c + dir[d + 1];\n      if (nr < 0 || nr >= rows || nc < 0 || nc >= cols || dist[nr][nc] != -1) {\n        continue;\n      }\n      dist[nr][nc] = dist[r][c] + 1;\n      q.push({nr, nc});\n    }\n  }\n  return dist;\n}\n```\n\n**逃出邊界：從邊界反灌**\n\n「哪些可行駛區域被障礙完全包圍、無法駛離地圖」這類問題，正著走每個格子很慢；反過來從邊界格往內 flood fill，凡是被標記到的都能通到外界，剩下沒標記的就是封閉死角。\n\n```cpp\n// mark every drivable cell that can reach the map border (reverse flood fill)\nvoid markEscapable(vector<vector<int>>& occ, vector<vector<char>>& safe) {\n  int rows = occ.size(), cols = occ[0].size();\n  queue<pair<int, int>> q;\n  auto add = [&](int r, int c) {\n    if (r < 0 || r >= rows || c < 0 || c >= cols) return;\n    if (occ[r][c] == 1 || safe[r][c]) return;\n    safe[r][c] = 1;\n    q.push({r, c});\n  };\n  for (int r = 0; r < rows; ++r) {\n    add(r, 0);\n    add(r, cols - 1);\n  }\n  for (int c = 0; c < cols; ++c) {\n    add(0, c);\n    add(rows - 1, c);\n  }\n  const int dir[5] = {1, 0, -1, 0, 1};\n  while (!q.empty()) {\n    auto [r, c] = q.front();\n    q.pop();\n    for (int d = 0; d < 4; ++d) {\n      add(r + dir[d], c + dir[d + 1]);\n    }\n  }\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1162 | [地圖分析](https://leetcode.cn/problems/as-far-from-land-as-possible) | 1666 |\n| 1391 | [檢查網格中是否存在有效路徑](https://leetcode.cn/problems/check-if-there-is-a-valid-path-in-a-grid) | 1746 |\n| 1765 | [地圖中的最高點](https://leetcode.cn/problems/map-of-highest-peak) | 1783 |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort) | 1948 |\n| 1293 | [網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-grid-with-obstacles-elimination) | 1967 |\n| 2503 | [矩陣查詢可獲得的最大分數](https://leetcode.cn/problems/maximum-number-of-points-from-grid-queries) | 2196 |\n| 2258 | [逃離火災](https://leetcode.cn/problems/escape-the-spreading-fire) | 2347 |",
    },
    {
      id: 4,
      title: "3. 計算幾何與碰撞偵測",
      summary:
        "**觀念起點**\n\n計算幾何是自駕面試的高頻區：碰撞偵測、邊界框是否相交、點是否在多邊形內、視野中可見的物體、最近點對。先掌握兩個原語，多數題目就拆得開：向量外積（叉積）判方向，內積判投影。\n\n叉積 `cross(o, a, b) = (a-o) x (b-o)` 的正負決定 b 在 a 的左側或右側，也等於三角形有號面積的兩倍。線段相交、凸包、點在多邊形內都靠它。注意用整數或夠寬的型別避免溢位，浮點比較要留 eps。\n\n**系統例題：兩個邊界框是否重疊**\n\n感知輸出常是軸對齊邊界框（AABB）。兩個閉區間框 `[ax1,ay1,ax2,ay2]`、`[bx1,by1,bx2,by2]` 重疊，等價於兩軸投影各自重疊：每一軸上「各自的起點都不超過對方的終點」。\n\n```cpp\n// do two axis-aligned bounding boxes overlap?\nbool boxesOverlap(const array<int, 4>& a, const array<int, 4>& b) {\n  bool x = a[0] <= b[2] && b[0] <= a[2];  // x-projections overlap\n  bool y = a[1] <= b[3] && b[1] <= a[3];  // y-projections overlap\n  return x && y;  // use < on both axes to require a true crossing\n}\n```\n\n**線段相交：用叉積判斷兩段是否跨越彼此**\n\n規劃出的軌跡與其他物體的軌跡是否相交，可化為線段相交。標準做法：A、B 兩端點看 CD 線段的兩側是否相反，且 C、D 兩端點看 AB 的兩側是否相反。\n\n```cpp\n// segment intersection test via orientation (cross product) signs\nlong long cross(array<long long, 2> o, array<long long, 2> a,\n                array<long long, 2> b) {\n  return (a[0] - o[0]) * (b[1] - o[1]) - (a[1] - o[1]) * (b[0] - o[0]);\n}\n\nint sgn(long long x) { return (x > 0) - (x < 0); }\n\nbool segmentsIntersect(array<long long, 2> a, array<long long, 2> b,\n                       array<long long, 2> c, array<long long, 2> d) {\n  int d1 = sgn(cross(a, b, c)), d2 = sgn(cross(a, b, d));\n  int d3 = sgn(cross(c, d, a)), d4 = sgn(cross(c, d, b));\n  return d1 != d2 && d3 != d4;  // proper crossing (collinear endpoints excluded)\n}\n```\n\n**凸包：用旋轉感測點雲的外輪廓**\n\n把一團偵測點包起來、估出占用範圍，就是凸包。Andrew 單調鏈先排序，再用叉積維護下凸殼與上凸殼，整體 `O(n log n)`。\n\n```cpp\n// Andrew's monotone chain convex hull\nvector<array<long long, 2>> convexHull(vector<array<long long, 2>> p) {\n  sort(p.begin(), p.end());\n  int n = p.size(), k = 0;\n  if (n < 3) return p;\n  vector<array<long long, 2>> h(2 * n);\n  for (int i = 0; i < n; ++i) {  // lower hull\n    while (k >= 2 && cross(h[k - 2], h[k - 1], p[i]) <= 0) k--;\n    h[k++] = p[i];\n  }\n  for (int i = n - 2, lo = k + 1; i >= 0; --i) {  // upper hull\n    while (k >= lo && cross(h[k - 2], h[k - 1], p[i]) <= 0) k--;\n    h[k++] = p[i];\n  }\n  h.resize(k - 1);\n  return h;\n}\n```\n\n面試重點：先寫出叉積/內積原語，明確 eps 與整數溢位策略，再把題目化成「方向、相交、包含、距離」其中一種。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 3047 | [求交集區域內的最大正方形面積](https://leetcode.cn/problems/find-the-largest-area-of-square-inside-two-rectangles) | 1602 |\n| 2249 | [統計圓內格點數目](https://leetcode.cn/problems/count-lattice-points-inside-a-circle) | 1603 |\n| 1401 | [圓和矩形是否有重疊](https://leetcode.cn/problems/circle-and-rectangle-overlapping) | 1709 |\n| 939 | [最小面積矩形](https://leetcode.cn/problems/minimum-area-rectangle) | 1752 |\n| 2101 | [引爆最多的炸彈](https://leetcode.cn/problems/detonate-the-maximum-bombs) | 1880 |\n| 858 | [鏡面反射](https://leetcode.cn/problems/mirror-reflection) | 1881 |\n| 963 | [最小面積矩形 II](https://leetcode.cn/problems/minimum-area-rectangle-ii) | 1991 |\n| 1610 | [可見點的最大數目](https://leetcode.cn/problems/maximum-number-of-visible-points) | 2147 |\n| 1515 | [服務中心的最佳位置](https://leetcode.cn/problems/best-position-for-a-service-centre) | 2157 |\n| 1453 | [圓形靶內的最大飛鏢數量](https://leetcode.cn/problems/maximum-number-of-darts-inside-of-a-circular-dartboard) | 2202 |\n| 3102 | [最小化曼哈頓距離](https://leetcode.cn/problems/minimize-manhattan-distances) | 2216 |",
    },
    {
      id: 5,
      title: "4. 道路網與場景圖建模",
      summary:
        "**觀念起點**\n\n道路網與場景圖的建模，第一步是判斷關係型態。無向邊代表雙向可達（路段、相鄰車道），適合連通塊、DFS/BFS 或並查集；有向邊代表單向或相依（單行道、任務先後、標定流程），常要檢查環、拓撲排序或強連通分量（SCC）。\n\n樹是沒有環的連通圖，每條邊都能把節點切成父側與子樹側；路網的層級（區域 → 路口 → 車道）常可用樹處理。問「移除哪條路段會切斷連通」是橋的問題；問「一群互相可達的路口」是 SCC。\n\n**Pattern 1：相依任務 + 拓撲排序**\n\n校準、感測器啟動、建圖管線常有「先 A 才能 B」的相依。建有向圖，入度為 0 先處理，拓撲順序上做最早完成時間 DP；若處理數小於節點數，代表有環、無法全部完成。\n\n```cpp\n// earliest finish time over a DAG of dependent tasks (topological DP)\nint minimumTime(int n, vector<vector<int>>& deps, vector<int>& cost) {\n  vector<vector<int>> g(n);\n  vector<int> indeg(n), finish(cost);\n  for (auto& e : deps) {\n    g[e[0]].push_back(e[1]);\n    indeg[e[1]]++;\n  }\n  queue<int> q;\n  for (int i = 0; i < n; ++i) {\n    if (indeg[i] == 0) q.push(i);\n  }\n  int seen = 0;\n  while (!q.empty()) {\n    int u = q.front();\n    q.pop();\n    seen++;\n    for (int v : g[u]) {\n      finish[v] = max(finish[v], finish[u] + cost[v]);\n      if (--indeg[v] == 0) q.push(v);\n    }\n  }\n  return seen < n ? -1 : *max_element(finish.begin(), finish.end());\n}\n```\n\n**Pattern 2：逐步合併連通性 + 並查集**\n\n路段一條條接上、只問「兩個路口是否連通」「目前還有幾個連通區域」時用並查集，路徑壓縮加按秩合併接近 `O(1)`。\n\n```cpp\n// disjoint set union with path compression + union by size\nstruct DSU {\n  vector<int> parent, sz;\n  explicit DSU(int n) : parent(n), sz(n, 1) {\n    iota(parent.begin(), parent.end(), 0);\n  }\n  int find(int x) { return parent[x] == x ? x : parent[x] = find(parent[x]); }\n  bool unite(int a, int b) {\n    a = find(a);\n    b = find(b);\n    if (a == b) return false;\n    if (sz[a] < sz[b]) swap(a, b);\n    parent[b] = a;\n    sz[a] += sz[b];\n    return true;\n  }\n};\n```\n\n**Pattern 3：關鍵路段 + 橋（Tarjan low-link）**\n\n「哪條路段一斷網路就分裂」是橋。DFS 樹中，若子節點 v 的子樹無法經由反向邊回到 u 或其祖先（`low[v] > dfn[u]`），則 (u, v) 是橋。無向圖要用邊 id 避免把父邊誤判為反向邊。\n\n```cpp\n// Tarjan bridges in an undirected road graph\nvector<vector<int>> findBridges(int n, vector<vector<int>>& edges) {\n  vector<vector<pair<int, int>>> g(n);\n  for (int i = 0; i < (int)edges.size(); ++i) {\n    g[edges[i][0]].push_back({edges[i][1], i});\n    g[edges[i][1]].push_back({edges[i][0], i});\n  }\n  vector<int> dfn(n, 0), low(n, 0);\n  vector<vector<int>> bridges;\n  int timer = 0;\n  function<void(int, int)> dfs = [&](int u, int pe) {\n    dfn[u] = low[u] = ++timer;\n    for (auto [v, id] : g[u]) {\n      if (id == pe) continue;\n      if (!dfn[v]) {\n        dfs(v, id);\n        low[u] = min(low[u], low[v]);\n        if (low[v] > dfn[u]) bridges.push_back({u, v});\n      } else {\n        low[u] = min(low[u], dfn[v]);\n      }\n    }\n  };\n  for (int i = 0; i < n; ++i) {\n    if (!dfn[i]) dfs(i, -1);\n  }\n  return bridges;\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2316 | [統計無向圖中無法互相到達點對數](https://leetcode.cn/problems/count-unreachable-pairs-of-nodes-in-an-undirected-graph) | 1604 |\n| 1466 | [重新規劃路線](https://leetcode.cn/problems/reorder-routes-to-make-all-paths-lead-to-the-city-zero) | 1634 |\n| 2492 | [兩個城市間路徑的最小分數](https://leetcode.cn/problems/minimum-score-of-a-path-between-two-cities) | 1680 |\n| 2685 | [統計完全連通分量的數量](https://leetcode.cn/problems/count-the-number-of-complete-components) | 1769 |\n| 2192 | [有向無環圖中一個節點的所有祖先](https://leetcode.cn/problems/all-ancestors-of-a-node-in-a-directed-acyclic-graph) | 1788 |\n| 1245 | [樹的直徑](https://leetcode.cn/problems/tree-diameter) | 1792 |\n| 1559 | [二維網格圖中探測環](https://leetcode.cn/problems/detect-cycles-in-2d-grid) | 1838 |\n| 2360 | [圖中的最長環](https://leetcode.cn/problems/longest-cycle-in-a-graph) | 1897 |\n| 1192 | [查詢叢集內的關鍵連線](https://leetcode.cn/problems/critical-connections-in-a-network) | 2085 |\n| 2876 | [有向圖訪問計數](https://leetcode.cn/problems/count-visited-nodes-in-a-directed-graph) | 2210 |\n| 2581 | [統計可能的樹根數目](https://leetcode.cn/problems/count-number-of-possible-root-nodes) | 2228 |",
    },
    {
      id: 6,
      title: "5. 決策與動態規劃",
      summary:
        "**觀念起點**\n\n很多自駕決策（要不要變換車道、何時減速、選哪條備援路線）可寫成動態規劃：若兩條不同的決策序列到達同一個「局面」，且之後可做的選擇完全相同，就能把局面存成狀態、重用答案。狀態不是陣列名稱，而是一句可驗證的語意。\n\n設計時先寫最後一步：最後一段從哪裡切、最後一個動作接在哪個前驅後。轉移式會自然浮現。轉移太慢時，再用排序、二分、前綴最佳值或單調資料結構加速。\n\n**系統例題：帶權區間排程（不重疊任務最大收益）**\n\n每個任務有開始、結束時間與收益（例如接駁訂單），不能選時間重疊的，求最大總收益。先按結束時間排序，`dp[i]` 為「前 i 個任務的最佳收益」，選第 i 個時用二分找最後一個相容任務。\n\n```cpp\n// weighted interval scheduling: max profit from non-overlapping jobs\nint jobScheduling(vector<int>& start, vector<int>& end, vector<int>& profit) {\n  int n = start.size();\n  vector<array<int, 3>> jobs(n);\n  for (int i = 0; i < n; ++i) jobs[i] = {end[i], start[i], profit[i]};\n  sort(jobs.begin(), jobs.end());\n  vector<int> ends(n);\n  for (int i = 0; i < n; ++i) ends[i] = jobs[i][0];\n  vector<int> dp(n + 1, 0);\n  for (int i = 1; i <= n; ++i) {\n    auto [e, s, p] = jobs[i - 1];\n    int j = upper_bound(ends.begin(), ends.end(), s) - ends.begin();\n    dp[i] = max(dp[i - 1], dp[j] + p);\n  }\n  return dp[n];\n}\n```\n\n**Pattern：記憶化搜尋（先暴力遞迴，再加 memo）**\n\n當遞迴會重複碰到同一個 `(index, state)`，先寫對的暴力遞迴，再用 memo 去重，是把搜尋變 DP 最穩的途徑。\n\n```cpp\n// memoized recursion skeleton over (index, capacity)\nint solve(int i, int cap, vector<vector<int>>& memo, vector<int>& w,\n          vector<int>& val) {\n  if (i < 0 || cap <= 0) return 0;\n  int& m = memo[i][cap];\n  if (m != -1) return m;\n  int best = solve(i - 1, cap, memo, w, val);  // skip\n  if (w[i] <= cap) {\n    best = max(best, val[i] + solve(i - 1, cap - w[i], memo, w, val));  // take\n  }\n  return m = best;\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1671 | [得到山形陣列的最少刪除次數](https://leetcode.cn/problems/minimum-number-of-removals-to-make-mountain-array) | 1913 |\n| 1235 | [規劃兼職工作](https://leetcode.cn/problems/maximum-profit-in-job-scheduling) | 2023 |\n| 1335 | [工作計劃的最低難度](https://leetcode.cn/problems/minimum-difficulty-of-a-job-schedule) | 2035 |\n| 1770 | [執行乘法運算的最大分數](https://leetcode.cn/problems/maximum-score-from-performing-multiplication-operations) | 2068 |\n| 1799 | [N 次操作後的最大分數和](https://leetcode.cn/problems/maximize-score-after-n-operations) | 2073 |\n| 1494 | [並行課程 II](https://leetcode.cn/problems/parallel-courses-ii) | 2082 |\n| 1937 | [扣分後的最大得分](https://leetcode.cn/problems/maximum-number-of-points-with-cost) | 2106 |\n| 1547 | [切棍子的最小成本](https://leetcode.cn/problems/minimum-cost-to-cut-a-stick) | 2116 |\n| 2218 | [從堆疊中取出 K 個硬幣的最大面值和](https://leetcode.cn/problems/maximum-value-of-k-coins-from-piles) | 2158 |\n| 1240 | [鋪瓷磚](https://leetcode.cn/problems/tiling-a-rectangle-with-the-fewest-squares) | 2242 |\n| 1125 | [最小的必要團隊](https://leetcode.cn/problems/smallest-sufficient-team) | 2251 |\n| 1723 | [完成所有工作的最短時間](https://leetcode.cn/problems/find-minimum-time-to-finish-all-jobs) | 2284 |\n| 1595 | [連通兩組點的最小成本](https://leetcode.cn/problems/minimum-cost-to-connect-two-groups-of-points) | 2538 |",
    },
    {
      id: 7,
      title: "6. 二分標定、貪心與滑動視窗",
      summary:
        "**觀念起點**\n\n二分、貪心、滑動視窗都依賴某種單調性。二分需要「可行性對答案單調」；滑動視窗需要「右擴、左縮後窗口合法性可維護」；貪心需要能用交換論證證明局部選擇安全。\n\n二分標定（calibration）是常見場景：找「能讓所有感測幀都通過檢查的最小參數」，把求最佳改寫成 `check(x)` 的可行性判定。實數值域用固定迭代次數收斂，避免浮點死迴圈。\n\n**系統例題：二分答案 + check（最小可行速率）**\n\n把任務在限定時間內完成的「最小速率」二分：速率越大越容易完成，`canFinish(speed)` 單調，對速率二分即可。\n\n```cpp\n// binary search on the answer: minimum speed to finish piles within h hours\nint minSpeed(vector<int>& piles, int h) {\n  auto canFinish = [&](long long speed) {\n    long long hours = 0;\n    for (int x : piles) hours += (x + speed - 1) / speed;\n    return hours <= h;\n  };\n  int lo = 1, hi = *max_element(piles.begin(), piles.end());\n  while (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (canFinish(mid)) {\n      hi = mid;\n    } else {\n      lo = mid + 1;\n    }\n  }\n  return lo;\n}\n```\n\n**Pattern：滑動視窗維護合法性（感測幀串流）**\n\n對連續幀做「最長/最短滿足條件的區段」時，右指標擴張、違規就縮左、合法時更新答案；只要合法性能隨左右增減而局部維護，攤還就是 `O(n)`。\n\n```cpp\n// longest window with at most k zeros (e.g. at most k dropped frames)\nint longestOnes(vector<int>& a, int k) {\n  int left = 0, zeros = 0, best = 0;\n  for (int right = 0; right < (int)a.size(); ++right) {\n    zeros += a[right] == 0;\n    while (zeros > k) {\n      zeros -= a[left++] == 0;\n    }\n    best = max(best, right - left + 1);\n  }\n  return best;\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2226 | [每個小孩最多能分到多少糖果](https://leetcode.cn/problems/maximum-candies-allocated-to-k-children) | 1646 |\n| 1658 | [將 x 減到 0 的最小運算元](https://leetcode.cn/problems/minimum-operations-to-reduce-x-to-zero) | 1817 |\n| 995 | [K 連續位的最小翻轉次數](https://leetcode.cn/problems/minimum-number-of-k-consecutive-bit-flips) | 1835 |\n| 1871 | [跳躍遊戲 VII](https://leetcode.cn/problems/jump-game-vii) | 1896 |\n| 2398 | [預算內的最多機器人數目](https://leetcode.cn/problems/maximum-number-of-robots-within-budget) | 1917 |\n| 2106 | [摘水果](https://leetcode.cn/problems/maximum-fruits-harvested-after-at-most-k-steps) | 2062 |\n| 843 | [猜猜這個單詞](https://leetcode.cn/problems/guess-the-word) | 2078 |\n| 2444 | [統計定界子陣列的數目](https://leetcode.cn/problems/count-subarrays-with-fixed-bounds) | 2093 |\n| 2141 | [同時執行 N 臺電腦的最長時間](https://leetcode.cn/problems/maximum-running-time-of-n-computers) | 2265 |",
    },
    {
      id: 8,
      title: "7. 軌跡與區間排程",
      summary:
        "**觀念起點**\n\n軌跡、預約、車道占用、充電時段都是區間。幾乎所有區間問題都歸結為一招：依端點排序，再做一次線性掃描，維護一點點狀態（目前合併塊的尾、最後保留的區間、目前同時活躍的數量）。排序之後，兩個區間重疊等價於「一個在另一個結束前開始」。\n\n辨識訊號：合併重疊用「依起點排序、能接就延長」；保留最多不重疊用「依結束排序、貪心接受」；最少資源/車道數等於最大同時並發，用掃描線或差分；對許多區段做加值更新，用差分陣列。\n\n**系統例題：最少車道數 = 最大同時並發**\n\n一批占用時段，要算「任一時刻最多同時佔用幾條車道」。把每段拆成 +1 起點事件與 -1 結束事件，分別排序，雙指標掃描；結束早於下一個起點就釋放一條。\n\n```cpp\n// minimum lanes = peak concurrency, via two sorted endpoint arrays\nint minLanes(vector<vector<int>>& iv) {\n  int n = iv.size();\n  vector<int> s(n), e(n);\n  for (int i = 0; i < n; ++i) {\n    s[i] = iv[i][0];\n    e[i] = iv[i][1];\n  }\n  sort(s.begin(), s.end());\n  sort(e.begin(), e.end());\n  int lanes = 0, best = 0, j = 0;\n  for (int i = 0; i < n; ++i) {\n    while (j < n && e[j] <= s[i]) j++;  // a lane freed before this start\n    lanes = i - j + 1;\n    best = max(best, lanes);\n  }\n  return best;\n}\n```\n\n**Pattern：差分陣列做區段加值（沿路徑加載重）**\n\n對 `[s, e)` 重複加值、最後查最大或最終值時，別逐格加：在起點加 delta、終點減 delta，再取前綴和。每筆 `O(1)`，一次掃描驗證整條時間軸。\n\n```cpp\n// range add via difference array; check load never exceeds capacity\nbool withinCapacity(vector<vector<int>>& trips, int capacity, int horizon) {\n  vector<long long> diff(horizon + 1, 0);\n  for (auto& tr : trips) {  // tr = {load, from, to}\n    diff[tr[1]] += tr[0];\n    diff[tr[2]] -= tr[0];  // half-open end\n  }\n  long long load = 0;\n  for (int i = 0; i < horizon; ++i) {\n    load += diff[i];\n    if (load > capacity) return false;\n  }\n  return true;\n}\n```\n\n注意端點包含性：要讓 `[1,2]` 與 `[2,3]` 視為相接就用 `<=`，要視為不重疊就用 `<`，並在比較器與合併判斷中保持一致。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1288 | [刪除被覆蓋區間](https://leetcode.cn/problems/remove-covered-intervals) | 1375 |\n| 986 | [區間列表的交集](https://leetcode.cn/problems/interval-list-intersections) | 1542 |\n| 2580 | [統計將重疊區間合併成組的方案數](https://leetcode.cn/problems/count-ways-to-group-overlapping-ranges) | 1632 |\n| 2406 | [將區間分為最少組數](https://leetcode.cn/problems/divide-intervals-into-minimum-number-of-groups) | 1713 |\n| 1024 | [影片拼接](https://leetcode.cn/problems/video-stitching) | 1746 |\n| 1326 | [灌溉花園的最少水龍頭數目](https://leetcode.cn/problems/minimum-number-of-taps-to-open-to-water-a-garden) | 1885 |\n| 1851 | [包含每個查詢的最小區間](https://leetcode.cn/problems/minimum-interval-to-include-each-query) | 2286 |",
    },
    {
      id: 9,
      title: "8. 感測器資料流與平行處理",
      summary:
        "**觀念起點**\n\n感測器資料流（LiDAR、相機、雷達）量大且持續到來，常不能整批存下。先把處理拆成 map / filter / scan / reduce：map 每筆獨立轉換（可平行）；reduce 多筆合併成摘要（需結合律）；scan 每個位置需前綴累積；streaming 只能看一遍，只能維護足夠小的摘要。\n\n能否平行取決於相依性。每元素獨立可平行；需要前綴狀態時做 scan；要全域 top-k，可先分片（shard）各留候選，再合併一次。資料流限制下，用大小為 k 的 heap、頻率表、滑動窗口統計或 sketch 取代完整歷史。\n\n**系統例題：資料流中的前 k 大（受限記憶體）**\n\n數值源源而來，不能全存，隨時要知道目前最大的 k 個（例如最近最強的 k 個回波）。用大小為 k 的最小堆，堆頂是目前第 k 大；新值大於堆頂才替換。\n\n```cpp\n// streaming top-k with a bounded min-heap (O(log k) per item, O(k) space)\nclass TopKStream {\n  int k;\n  priority_queue<int, vector<int>, greater<int>> heap;\n\n public:\n  explicit TopKStream(int k) : k(k) {}\n  void add(int x) {\n    if ((int)heap.size() < k) {\n      heap.push(x);\n    } else if (x > heap.top()) {\n      heap.pop();\n      heap.push(x);\n    }\n  }\n  int kthLargest() const { return heap.empty() ? -1 : heap.top(); }\n};\n```\n\n**Pattern：前綴和 / scan（沿時間軸的累積量）**\n\n每個位置需要前面累積結果（里程累積、能耗前綴、壓縮後位置）時，用一次 exclusive scan 取代重複求和。\n\n```cpp\n// exclusive prefix scan: scan[i] = sum of a[0..i)\nvector<long long> exclusiveScan(const vector<int>& a) {\n  vector<long long> scan(a.size() + 1, 0);\n  for (int i = 0; i < (int)a.size(); ++i) {\n    scan[i + 1] = scan[i] + a[i];\n  }\n  return scan;\n}\n```\n\n平行化時，讓每個 shard 維護自己的 top-k 或部分和，最後做一次合併 reduction，就是 map-reduce 的思路；務必說清楚每個摘要的空間上限。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 974 | [和可被 K 整除的子陣列](https://leetcode.cn/problems/subarray-sums-divisible-by-k) | 1676 |\n| 1942 | [最小未被佔據椅子的編號](https://leetcode.cn/problems/the-number-of-the-smallest-unoccupied-chair) | 1695 |\n| 2588 | [統計美麗子陣列數目](https://leetcode.cn/problems/count-the-number-of-beautiful-subarrays) | 1697 |\n| 2962 | [統計最大元素出現至少 K 次的子陣列](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times) | 1701 |\n| 1368 | [使網格圖至少有一條有效路徑的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid) | 2069 |\n| 3821 | [二進位制中恰好K個1的第N小整數](https://leetcode.cn/problems/find-nth-smallest-integer-with-k-one-bits) | 2069 |\n| 3123 | [最短路徑中的邊](https://leetcode.cn/problems/find-edges-in-shortest-paths) | 2093 |\n| 2223 | [構造字串的總得分和](https://leetcode.cn/problems/sum-of-scores-of-built-strings) | 2220 |\n| 882 | [細分圖中的可到達節點](https://leetcode.cn/problems/reachable-nodes-in-subdivided-graph) | 2328 |\n| 3797 | [統計在矩形格子裡移動的路徑數目](https://leetcode.cn/problems/count-routes-to-climb-a-rectangular-grid) | 2376 |\n| 2577 | [在網格圖中訪問一個格子的最少時間](https://leetcode.cn/problems/minimum-time-to-visit-a-cell-in-a-grid) | 2382 |\n| 3404 | [統計特殊子序列的數目](https://leetcode.cn/problems/count-special-subsequences) | 2445 |\n| 3410 | [刪除所有值為某個元素後的最大子陣列和](https://leetcode.cn/problems/maximize-subarray-sum-after-removing-all-occurrences-of-one-element) | 2844 |",
    },
    {
      id: 10,
      title: "9. 車載即時系統與位元狀態",
      summary:
        "**觀念起點**\n\n車載軟體跑在資源受限、需即時反應的環境：記憶體固定、不能任意配置、整數可能溢位、最壞延遲有上限。設計時要把空間上限寫進資料結構，並明確處理 full/empty 與 wrap-around。\n\n若狀態是「一組布林」且元素數小（感測器健康旗標、已校準的模組集合），用 bitmask 把集合壓成整數，加入/刪除/查詢都是 `O(1)` 位運算——但每一位都要有明確語意。\n\n**系統例題：固定容量環形緩衝（感測幀佇列）**\n\n固定容量的佇列，支援 push/pop、不擴容，適合穩定吞吐的幀緩衝。用 head/tail 與 count 區分空與滿，指標移動取模 wrap-around。\n\n```cpp\n// fixed-capacity ring buffer for a frame queue\nclass RingBuffer {\n  vector<int> buf;\n  int head = 0, tail = 0, count = 0;\n\n public:\n  explicit RingBuffer(int capacity) : buf(capacity) {}\n  bool push(int x) {\n    if (buf.empty() || count == (int)buf.size()) return false;\n    buf[tail] = x;\n    tail = (tail + 1) % buf.size();\n    count++;\n    return true;\n  }\n  bool pop(int& out) {\n    if (count == 0) return false;\n    out = buf[head];\n    head = (head + 1) % buf.size();\n    count--;\n    return true;\n  }\n  bool empty() const { return count == 0; }\n};\n```\n\n**Pattern：bitmask 狀態集合 + 溢位保護**\n\n集合操作用位運算；加總、乘法、距離先轉 `long long`，無限大常數設成加法後不溢位的值。\n\n```cpp\n// bitmask set ops + capped arithmetic for safe accumulation\nstruct Bits {\n  static bool has(int m, int i) { return m & (1 << i); }\n  static int add(int m, int i) { return m | (1 << i); }\n  static int del(int m, int i) { return m & ~(1 << i); }\n};\n\nconstexpr long long kInf = 4e18;\nlong long addCapped(long long a, long long b) {\n  return (a > kInf - b) ? kInf : a + b;  // never wrap past kInf\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1947 | [最大相容性評分和](https://leetcode.cn/problems/maximum-compatibility-score-sum) | 1704 |\n| 1011 | [在 D 天內送達包裹的能力](https://leetcode.cn/problems/capacity-to-ship-packages-within-d-days) | 1725 |\n| 2502 | [設計記憶體分配器](https://leetcode.cn/problems/design-memory-allocator) | 1746 |\n| 875 | [愛吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) | 1766 |\n| 3281 | [範圍內整數的最大得分](https://leetcode.cn/problems/maximize-score-of-numbers-in-ranges) | 1768 |\n| 3800 | [使二進位制字串相等的最小成本](https://leetcode.cn/problems/minimum-cost-to-make-two-binary-strings-equal) | 1841 |\n| 2002 | [兩個迴文子序列長度的最大乘積](https://leetcode.cn/problems/maximum-product-of-the-length-of-two-palindromic-subsequences) | 1869 |\n| 1255 | [得分最高的單詞集合](https://leetcode.cn/problems/maximum-score-words-formed-by-letters) | 1882 |\n| 2439 | [最小化陣列中的最大值](https://leetcode.cn/problems/minimize-maximum-of-array) | 1965 |\n| 3811 | [交替按位異或分割的數目](https://leetcode.cn/problems/number-of-alternating-xor-partitions) | 2006 |\n| 855 | [考場就座](https://leetcode.cn/problems/exam-room) | 2067 |\n| 2616 | [最小化數對的最大差值](https://leetcode.cn/problems/minimize-the-maximum-difference-of-pairs) | 2155 |\n| 2968 | [執行操作使頻率分數最大](https://leetcode.cn/problems/apply-operations-to-maximize-frequency-score) | 2444 |",
    },
    {
      id: 1101,
      title: "10. 車隊平台 API 與系統資料結構",
      summary:
        "**觀念起點**\n\n車隊平台題多半是 API 設計：不是算一次答案，而是設計一個物件長期支援多種操作（上報、查詢、更新狀態）。先列每個方法的輸入/輸出/目標複雜度，再決定哪份資料是事實來源（canonical state），哪些是為了查詢而建的衍生索引。\n\n更新時必須同步所有索引：先刪舊索引、改真實資料、再插新索引，否則 heap 會留 stale entry、set comparator 不完整、map 與索引不同步。tie-breaker（同分時比 id / 時間 / 名稱）要先定義清楚。\n\n**系統例題：時間版本鍵值（遙測歷史查詢）**\n\n每個感測器 key 的讀數隨時間到來，要查「某時間點的值」。每個 key 存遞增時間序列，查詢時對 timestamp 二分取最後一個 `<= t`。\n\n```cpp\n// time-based key-value store (telemetry history with binary search)\nclass TimeMap {\n  unordered_map<int, vector<pair<int, int>>> hist;  // key -> sorted (time, value)\n\n public:\n  void set(int key, int time, int value) { hist[key].push_back({time, value}); }\n  int get(int key, int time) const {\n    auto it = hist.find(key);\n    if (it == hist.end()) return -1;\n    const auto& v = it->second;\n    auto pos = upper_bound(v.begin(), v.end(), make_pair(time, INT_MAX));\n    return pos == v.begin() ? -1 : prev(pos)->second;\n  }\n};\n```\n\n**Pattern：LRU 快取（O(1) get/put）**\n\n最近最少使用淘汰：list 維護時間順序、unordered_map 指到 list 節點，get/put 皆 `O(1)`。\n\n```cpp\n// LRU cache: hash map of list iterators + splice for O(1) recency update\nclass LRUCache {\n  int cap;\n  list<pair<int, int>> order;  // front = most recent: (key, value)\n  unordered_map<int, list<pair<int, int>>::iterator> pos;\n\n public:\n  explicit LRUCache(int cap) : cap(cap) {}\n  int get(int key) {\n    auto it = pos.find(key);\n    if (it == pos.end()) return -1;\n    order.splice(order.begin(), order, it->second);\n    return it->second->second;\n  }\n  void put(int key, int value) {\n    if (cap == 0) return;\n    auto it = pos.find(key);\n    if (it != pos.end()) {\n      it->second->second = value;\n      order.splice(order.begin(), order, it->second);\n      return;\n    }\n    if ((int)order.size() == cap) {\n      pos.erase(order.back().first);\n      order.pop_back();\n    }\n    order.push_front({key, value});\n    pos[key] = order.begin();\n  }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2353 | [設計食物評分系統](https://leetcode.cn/problems/design-a-food-rating-system) | 1782 |\n| 3408 | [設計工作管理員](https://leetcode.cn/problems/design-task-manager) | 1807 |\n| 2034 | [股票價格波動](https://leetcode.cn/problems/stock-price-fluctuation) | 1832 |\n| 2013 | [檢測正方形](https://leetcode.cn/problems/detect-squares) | 1841 |\n| 3508 | [設計路由器](https://leetcode.cn/problems/implement-router) | 1851 |\n| 2296 | [設計一個文本編輯器](https://leetcode.cn/problems/design-a-text-editor) | 1912 |\n| 1032 | [字元流](https://leetcode.cn/problems/stream-of-characters) | 1970 |\n| 911 | [線上選舉](https://leetcode.cn/problems/online-election) | 2001 |\n| 1348 | [推文計數](https://leetcode.cn/problems/tweet-counts-per-frequency) | 2037 |\n| 2102 | [序列順序查詢](https://leetcode.cn/problems/sequentially-ordinal-rank-tracker) | 2159 |\n| 1912 | [設計電影租借系統](https://leetcode.cn/problems/design-movie-rental-system) | 2182 |\n| 1825 | [求出 MK 平均值](https://leetcode.cn/problems/finding-mk-average) | 2396 |\n| 3534 | [針對圖的路徑存在性查詢 II](https://leetcode.cn/problems/path-existence-queries-in-a-graph-ii) | 2507 |",
    },
    {
      id: 1102,
      title: "11. 狀態空間搜尋與壓縮 BFS",
      summary:
        "**觀念起點**\n\n當「目前位置」不足以決定未來能做什麼時，就要把其他資訊放進狀態，在狀態圖上搜尋。要造訪的航點集合、已開啟的閘門、剩餘操作次數，都可能是狀態的一部分。只要兩條路徑到達同一個完整狀態、之後選擇相同，就能去重。\n\n多源 BFS：起點不固定時，把所有合法起點距離設 0 同時入隊。狀態壓縮前先估狀態數：`n * 2^n` 在 n 小時可行，太大就要換貪心或不同建模。\n\n**系統例題：造訪所有航點的最短路（多源 bitmask BFS）**\n\n無權無向圖，可從任意節點出發，求造訪所有節點的最少邊數（巡檢全部站點）。狀態是 `(node, mask)`，邊權皆 1 用 BFS，所有 `(i, 1<<i)` 為多源起點，第一次達到 mask 全滿即答案。\n\n```cpp\n// shortest route visiting all waypoints (state = node + visited bitmask)\nint visitAll(vector<vector<int>>& graph) {\n  int n = graph.size(), full = (1 << n) - 1;\n  vector<vector<int>> dist(n, vector<int>(1 << n, -1));\n  queue<pair<int, int>> q;\n  for (int i = 0; i < n; ++i) {\n    dist[i][1 << i] = 0;\n    q.push({i, 1 << i});\n  }\n  while (!q.empty()) {\n    auto [u, mask] = q.front();\n    q.pop();\n    if (mask == full) return dist[u][mask];\n    for (int v : graph[u]) {\n      int nm = mask | (1 << v);\n      if (dist[v][nm] != -1) continue;\n      dist[v][nm] = dist[u][mask] + 1;\n      q.push({v, nm});\n    }\n  }\n  return -1;\n}\n```\n\n**Pattern：閘門與通行證（位置 + key mask）**\n\n同一格在不同已持通行證集合下不是同一狀態；visited 必須含 `(r, c, mask)`。把狀態編碼成整數方便去重。\n\n```cpp\n// encode (row, col, keyMask) into one integer for visited dedup\nint encode(int r, int c, int mask, int cols, int keyCount) {\n  return ((r * cols + c) << keyCount) | mask;\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 943 | [最短超級串](https://leetcode.cn/problems/find-the-shortest-superstring) | 2186 |\n| 847 | [訪問所有節點的最短路徑](https://leetcode.cn/problems/shortest-path-visiting-all-nodes) | 2201 |\n| 1568 | [使陸地分離的最少天數](https://leetcode.cn/problems/minimum-number-of-days-to-disconnect-island) | 2209 |\n| 864 | [獲取所有鑰匙的最短路徑](https://leetcode.cn/problems/shortest-path-to-get-all-keys) | 2259 |\n| 1434 | [每個人戴不同帽子的方案數](https://leetcode.cn/problems/number-of-ways-to-wear-different-hats-to-each-other) | 2273 |\n| 1263 | [推箱子](https://leetcode.cn/problems/minimum-moves-to-move-a-box-to-their-target-location) | 2297 |\n| 2612 | [最少翻轉運算元](https://leetcode.cn/problems/minimum-reverse-operations) | 2824 |",
    },
    {
      id: 1103,
      title: "12. 多階段路徑規劃與約束",
      summary:
        "**觀念起點**\n\n多階段路徑規劃常不是一次 Dijkstra 就結束，而要把條件拆成幾份可重用的距離表。兩個起點匯合再到終點、第二短路、含等待規則或通行折扣，都靠擴充狀態或多跑幾次距離表。\n\n關鍵技巧：從終點在反圖跑一次 Dijkstra，就得到所有點到終點的距離，再枚舉中繼點合併；次短路時每點保留最短與嚴格次短；折扣/通行證時狀態加一維變分層圖；交通號誌等待時，dist 表示實際抵達時間，relax 前先把出發時刻調到下一個可通行時間。\n\n**系統例題：兩來源匯合到終點的最小總代價**\n\n有向加權圖，要讓 src1、src2 都到 dest，最小化用到的邊權總和——等價於選匯合點 x，兩來源各走到 x，再從 x 到 dest。三次 Dijkstra（src1、src2 在原圖，dest 在反圖）後枚舉 x。\n\n```cpp\nvector<long long> dijkstra(int n, vector<vector<pair<int, int>>>& g, int s) {\n  const long long kInf = 4e18;\n  vector<long long> dist(n, kInf);\n  priority_queue<pair<long long, int>, vector<pair<long long, int>>,\n                 greater<>> pq;\n  dist[s] = 0;\n  pq.push({0, s});\n  while (!pq.empty()) {\n    auto [du, u] = pq.top();\n    pq.pop();\n    if (du != dist[u]) continue;\n    for (auto [v, w] : g[u]) {\n      if (du + w < dist[v]) {\n        dist[v] = du + w;\n        pq.push({dist[v], v});\n      }\n    }\n  }\n  return dist;\n}\n\nlong long minRendezvous(int n, vector<vector<int>>& edges, int s1, int s2,\n                        int dest) {\n  vector<vector<pair<int, int>>> g(n), rg(n);\n  for (auto& e : edges) {\n    g[e[0]].push_back({e[1], e[2]});\n    rg[e[1]].push_back({e[0], e[2]});\n  }\n  auto d1 = dijkstra(n, g, s1), d2 = dijkstra(n, g, s2),\n       dr = dijkstra(n, rg, dest);\n  const long long kInf = 4e18;\n  long long ans = kInf;\n  for (int x = 0; x < n; ++x) {\n    if (d1[x] < kInf && d2[x] < kInf && dr[x] < kInf) {\n      ans = min(ans, d1[x] + d2[x] + dr[x]);\n    }\n  }\n  return ans == kInf ? -1 : ans;\n}\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2290 | [到達角落需要移除障礙物的最小數目](https://leetcode.cn/problems/minimum-obstacle-removal-to-reach-corner) | 2138 |\n| 2662 | [前往目標的最小代價](https://leetcode.cn/problems/minimum-cost-of-a-path-with-special-roads) | 2154 |\n| 3377 | [使兩個整數相等的數位操作](https://leetcode.cn/problems/digit-operations-to-make-two-integers-equal) | 2186 |\n| 2045 | [到達目的地的第二短時間](https://leetcode.cn/problems/second-minimum-time-to-reach-destination) | 2202 |\n| 3419 | [圖的最大邊權的最小值](https://leetcode.cn/problems/minimize-the-maximum-edge-weight-of-graph) | 2243 |\n| 2203 | [得到要求路徑的最小帶權子圖](https://leetcode.cn/problems/minimum-weighted-subgraph-with-the-required-paths) | 2364 |\n| 1928 | [規定時間內到達終點的最小花費](https://leetcode.cn/problems/minimum-cost-to-reach-destination-in-time) | 2413 |",
    },
    {
      id: 1104,
      title: "13. 路網樹分解與子樹查詢",
      summary:
        "**觀念起點**\n\n路網常有層級結構（區域、路口、車道），可用樹處理。基礎是：樹上任一條邊把節點分成兩塊。後序 DFS 取得子樹資訊，Euler tour 把子樹映成連續區間，換根 DP 把父側資訊推給孩子。\n\n多次祖先或路徑查詢才需要 binary lifting、Euler + Fenwick 或樹鏈剖分；單次查詢通常一般 DFS 就夠。換根時不要背公式，先想根跨過一條邊後哪些節點變近、哪些變遠。\n\n**系統例題：每個節點到所有節點的距離和（換根 DP）**\n\n對每個路口 i 求「到所有路口的距離總和」（選集散中心）。第一次 DFS 算 size 與根 0 的答案；第二次把答案沿邊下推：根從 u 移到子 v 時，v 子樹的 size[v] 個點各減 1，其餘 n - size[v] 個點各加 1。\n\n```cpp\n// sum of distances to all nodes, for every node (reroot DP)\nvector<int> sumOfDistances(int n, vector<vector<int>>& edges) {\n  vector<vector<int>> g(n);\n  for (auto& e : edges) {\n    g[e[0]].push_back(e[1]);\n    g[e[1]].push_back(e[0]);\n  }\n  vector<int> sz(n, 1), ans(n, 0);\n  function<void(int, int, int)> down = [&](int u, int p, int depth) {\n    ans[0] += depth;\n    for (int v : g[u]) {\n      if (v == p) continue;\n      down(v, u, depth + 1);\n      sz[u] += sz[v];\n    }\n  };\n  function<void(int, int)> reroot = [&](int u, int p) {\n    for (int v : g[u]) {\n      if (v == p) continue;\n      ans[v] = ans[u] - sz[v] + (n - sz[v]);\n      reroot(v, u);\n    }\n  };\n  down(0, -1, 0);\n  reroot(0, -1);\n  return ans;\n}\n```\n\n**Pattern：binary lifting 求 LCA / k 級祖先**\n\n多次查兩路口的最近共同上層、或往上跳 k 級時，預處理 `up[j][v]` 為 v 的 `2^j` 祖先。\n\n```cpp\n// LCA via binary lifting (preprocess up[j][v] = 2^j-th ancestor)\nstruct LCA {\n  vector<vector<int>> up;\n  vector<int> depth;\n  LCA(const vector<vector<int>>& tree, int root) {\n    int n = tree.size(), LOG = 1;\n    while ((1 << LOG) <= n) LOG++;\n    up.assign(LOG, vector<int>(n));\n    depth.assign(n, 0);\n    function<void(int, int)> dfs = [&](int u, int p) {\n      up[0][u] = p;\n      for (int j = 1; j < LOG; ++j) up[j][u] = up[j - 1][up[j - 1][u]];\n      for (int v : tree[u]) {\n        if (v != p) {\n          depth[v] = depth[u] + 1;\n          dfs(v, u);\n        }\n      }\n    };\n    dfs(root, root);\n  }\n  int lca(int a, int b) {\n    if (depth[a] < depth[b]) swap(a, b);\n    int diff = depth[a] - depth[b];\n    for (int j = 0; j < (int)up.size(); ++j) {\n      if (diff & (1 << j)) a = up[j][a];\n    }\n    if (a == b) return a;\n    for (int j = (int)up.size() - 1; j >= 0; --j) {\n      if (up[j][a] != up[j][b]) {\n        a = up[j][a];\n        b = up[j][b];\n      }\n    }\n    return up[0][a];\n  }\n};\n```\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 1483 | [樹節點的第 K 個祖先](https://leetcode.cn/problems/kth-ancestor-of-a-tree-node) | 2115 |\n| 2246 | [相鄰字元不同的最長路徑](https://leetcode.cn/problems/longest-path-with-different-adjacent-characters) | 2126 |\n| 834 | [樹中距離之和](https://leetcode.cn/problems/sum-of-distances-in-tree) | 2197 |\n| 1766 | [互質樹](https://leetcode.cn/problems/tree-of-coprimes) | 2232 |\n| 2646 | [最小化旅行的價格總和](https://leetcode.cn/problems/minimize-the-total-price-of-the-trips) | 2238 |\n| 2458 | [移除子樹後的二叉樹高度](https://leetcode.cn/problems/height-of-binary-tree-after-subtree-removal-queries) | 2299 |\n| 2322 | [從樹中刪除邊的最小分數](https://leetcode.cn/problems/minimum-score-after-removals-on-a-tree) | 2392 |\n| 2440 | [建立價值相同的連通塊](https://leetcode.cn/problems/create-components-with-same-value) | 2460 |",
    },
    {
      id: 1105,
      title: "14. 高階 DP 與單調性優化",
      summary:
        "**觀念起點**\n\n當 DP 狀態好寫但轉移太慢時，用單調性或資料結構消掉枚舉維度。先寫出 `dp[i]` 從哪些 j 轉移，再看找最佳 j 的過程能否被取代：合法 j 是滑動窗口用單調隊列；j 是排序後相容前驅用二分；轉移是 `m*x+b` 的最小值用凸包優化或李超線段樹。\n\n優化不能改變狀態語意：消掉枚舉前，要確認資料結構保留的候選與暴力枚舉的候選集合一致。\n\n**系統例題：窗口受限的最大子序列和（單調隊列優化）**\n\n`dp[i] = nums[i] + max(dp[j])`，其中 j 落在 `[i-k, i-1]`。用單調遞減隊列維護窗口最大值，整體 `O(n)`。\n\n```cpp\n// dp[i] = nums[i] + max(dp[j]) for i-k <= j < i, via a monotonic deque\nint constrainedSubsetSum(vector<int>& nums, int k) {\n  deque<int> dq;  // indices, dp decreasing front->back\n  int n = nums.size();\n  vector<long long> dp(n);\n  long long best = LLONG_MIN;\n  for (int i = 0; i < n; ++i) {\n    while (!dq.empty() && dq.front() < i - k) dq.pop_front();\n    long long take = dq.empty() ? 0 : max(0LL, dp[dq.front()]);\n    dp[i] = nums[i] + take;\n    best = max(best, dp[i]);\n    while (!dq.empty() && dp[dq.back()] <= dp[i]) dq.pop_back();\n    dq.push_back(i);\n  }\n  return (int)best;\n}\n```\n\n**Pattern：排序 + 二分前驅**\n\n活動選擇、工作排程、帶 k 次選擇時，先排序，把相容條件變成前綴 index，再用二分接上前一個相容狀態，避免線性回掃。先建可證明正確的樸素 DP，再談優化。\n\n**搭配練習**\n\n| ID | Problem | Rating |\n| --- | --- | --- |\n| 2830 | [銷售利潤最大化](https://leetcode.cn/problems/maximize-the-profit-as-the-salesman) | 1851 |\n| 1996 | [遊戲中弱角色的數量](https://leetcode.cn/problems/the-number-of-weak-characters-in-the-game) | 1861 |\n| 1340 | [跳躍遊戲 V](https://leetcode.cn/problems/jump-game-v) | 1866 |\n| 2008 | [計程車的最大盈利](https://leetcode.cn/problems/maximum-earnings-from-taxi) | 1872 |\n| 1751 | [最多可以參加的會議數目 II](https://leetcode.cn/problems/maximum-number-of-events-that-can-be-attended-ii) | 2041 |\n| 1473 | [粉刷房子 III](https://leetcode.cn/problems/paint-house-iii) | 2056 |\n| 1883 | [準時抵達會議現場的最小跳過休息次數](https://leetcode.cn/problems/minimum-skips-to-arrive-at-meeting-on-time) | 2588 |\n| 1687 | [從倉庫到碼頭運輸箱子](https://leetcode.cn/problems/delivering-boxes-from-storage-to-ports) | 2610 |",
    },
    {
      id: 9001,
      title: "15. 常見 Waymo 面試模式",
      summary:
        "面試官常用「題目訊號」反推考點。下表把 Waymo 風格（自駕情境包裝的演算法題）最常見的模式整理成「訊號 → 核心做法 → 代表題」，幫你在讀題的前幾秒鎖定方向。\n\n| 模式 | 訊號 | 核心做法 | 代表題 |\n| --- | --- | --- | --- |\n| 最短路 / A* | 路徑規劃、最少時間或代價、非負權 | 對答案 Dijkstra；有啟發式升級 A* | [網格中的最短路徑](https://leetcode.cn/problems/shortest-path-in-a-grid-with-obstacles-elimination) |\n| 多源 BFS | 到最近某類目標的距離、距離場 | 所有來源同時入隊、距離設 0 | [地圖中的最高點](https://leetcode.cn/problems/map-of-highest-peak) |\n| 計算幾何 | 碰撞、邊界框相交、視野、點在多邊形內 | 叉積判方向、投影判重疊 | [圓和矩形是否有重疊](https://leetcode.cn/problems/circle-and-rectangle-overlapping) |\n| 區間 / 掃描線 | 車道數、最大同時並發、合併占用 | 端點排序後線性掃描或差分 | [會議室 II](https://leetcode.cn/problems/meeting-rooms-ii) |\n| 滑動視窗 | 連續幀、最長/最短滿足條件 | 右擴左縮維護合法性，攤還 `O(n)` | [無重複字元的最長子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters) |\n| 二分答案 | 「最小可行參數 / 速率」、可行性單調 | 對答案二分，寫 `check` 判定 | [愛吃香蕉的珂珂](https://leetcode.cn/problems/koko-eating-bananas) |\n| 動態規劃 | 最優子結構、計數或最值 | 定義狀態與轉移，自底向上 | [規劃兼職工作](https://leetcode.cn/problems/maximum-profit-in-job-scheduling) |\n| 拓撲排序 | 相依任務、校準先後、build pipeline | 入度為 0 先處理，順序上做 DP | [並行課程 III](https://leetcode.cn/problems/parallel-courses-iii) |\n| 狀態壓縮 BFS | 造訪所有航點、閘門與通行證 | 狀態含 bitmask，邊權 1 用 BFS | [訪問所有節點的最短路徑](https://leetcode.cn/problems/shortest-path-visiting-all-nodes) |\n| 系統設計 | 上報 / 查詢 / 更新長期維護 | canonical state + 衍生索引同步 | [設計食物評分系統](https://leetcode.cn/problems/design-a-food-rating-system) |\n\n小提醒：\n\n- 看到「地圖、網格、路徑」先想 BFS / Dijkstra / A*；看到「碰撞、相交、視野」先想計算幾何原語。\n- 看到「最少車道 / 平台 / 同時數量」先想掃描線或差分；看到「最小可行參數」先想二分答案。\n- 系統設計題先講清楚 canonical state 與索引同步，再寫程式碼。",
    },
  ],
} as TutorialData.Root;
