import { defaultOptions, useOptions } from "./hook";
import type {
  OptionKey,
  Options,
  OptionsStoreState,
  OptionValue,
} from "./types_v2";

export { defaultOptions, useOptions };
export type { OptionKey, Options, OptionsStoreState, OptionValue };
