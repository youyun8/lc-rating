{
  "id": 1,
  "title": "二分搜尋（二分答案/最小化最大值/最大化最小值/第K小）",
  "src": "https://leetcode.cn/circle/discuss/SqopEo/",
  "last_update": "2026-02-24T14:23:02.465505+00:00",
  "children": [
    {
      "id": 2,
      "title": "1. 基礎",
      "summary": "**原理講解**\n\n本節要解決的場景是：在一個**已經排序好**的陣列裡，快速找到某個值的位置，或找到「滿足某條件的分界點」。最直覺的暴力做法是從頭一個一個比對，`O(n)`；但只要資料有序，我們就能每次砍掉一半，做到 `O(log n)`。\n\n核心想法是「猜中點，再決定往左還是往右」。看陣列正中間那個值：如果它已經太大，答案一定在左半邊，右半邊整個丟掉；反之丟掉左半邊。每比一次就排除一半，所以很快。\n\n```cpp\n// 最基本的二分：在有序陣列中找 target，找到回傳下標，否則回傳 -1\nint l = 0, r = (int)a.size() - 1;  // 閉區間 [l, r]，答案可能落在這裡面\nwhile (l <= r) {\n  int mid = l + (r - l) / 2;  // 取中點（這樣寫可避免相加溢位）\n  if (a[mid] == target) {\n    return mid;\n  } else if (a[mid] < target) {\n    l = mid + 1;  // 太小，答案在右半\n  } else {\n    r = mid - 1;  // 太大，答案在左半\n  }\n}\nreturn -1;\n```\n\n這裡的關鍵叫「不變式」：迴圈裡永遠保證「若答案存在，它一定還在 `[l, r]` 之內」。每一步我們只丟掉「確定不可能」的那一半，所以不變式不會被破壞，迴圈結束時就能下結論。\n\n更進階一點，二分常用來找「第一個滿足條件的位置」，例如第一個 `>= target` 的元素（這正是 LeetCode 34、35 的核心）。這時不是找「相等」，而是找「真假分界」。\n\n```cpp\n// 找第一個 >= target 的下標（lower_bound）\nint l = 0, r = (int)a.size();  // 注意：r 取到 size()，半開區間 [l, r)\nwhile (l < r) {\n  int mid = l + (r - l) / 2;\n  if (a[mid] >= target) {\n    r = mid;  // mid 可能就是答案，保留它\n  } else {\n    l = mid + 1;  // mid 太小，答案在更右邊\n  }\n}\nreturn l;  // l 即第一個 >= target 的位置\n```\n\n**實作方式**\n\n**1. 確認資料有序、目標明確。** 二分的前提是「有序」或「有單調的真假分界」。先問自己：我要找的是「某個值」，還是「第一個／最後一個滿足某條件的位置」？\n\n**2. 決定區間寫法並固定到底。** 兩種常見寫法：閉區間 `[l, r]`（用 `l <= r`、`r = mid - 1`）或半開區間 `[l, r)`（用 `l < r`、`r = mid`）。挑一種、寫熟，不要混用。\n\n```cpp\n// 把判斷條件抽成一句話：mid 是否「達標」\nbool ok(int mid) { return a[mid] >= target; }  // 達標=值夠大\n```\n\n**3. 中點與收斂。** 中點一律寫 `mid = l + (r - l) / 2` 防溢位；每次依比較結果，把「不可能含答案」的半邊移除，確保區間嚴格變小，迴圈才會結束。\n\n```cpp\n// 半開區間找分界的骨架\nwhile (l < r) {\n  int mid = l + (r - l) / 2;\n  if (ok(mid)) {\n    r = mid;  // 達標 → 答案可能是 mid 或更左\n  } else {\n    l = mid + 1;  // 不達標 → 答案在右邊\n  }\n}\n// 結束時 l == r，即第一個達標的位置\n```\n\n**4. 邊界與複雜度。**\n\n- 「找不到」要處理：lower_bound 可能回傳 `size()`，代表沒有任何元素 `>= target`（LeetCode 35 插入位置就靠這個）。\n- 「第一個 `> target`」減「第一個 `>= target`」就是 target 出現的區間（LeetCode 34 找左右邊界的技巧）。\n- 複雜度固定 `O(log n)`，因為每次區間長度減半。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往二分搜尋想：\n\n- **輸入是「已排序陣列」，或排序後仍保留意義。** LeetCode 704、34、744 都明說陣列有序。\n- **要找「位置」「邊界」「插入點」「第一個／最後一個滿足某條件的元素」。** 例如 744 找「比目標字母大的最小字母」、2529 在排好序的數列裡數正負數的個數，本質都是找分界下標。\n- **暴力是線性掃描 `O(n)`，但題目資料量大、或要呼叫很多次查詢，需要更快。** 二分把每次查詢壓到 `O(log n)`。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的搜尋區間是閉的 `[l, r]` 還是半開的 `[l, r)`？對應的迴圈條件和更新寫對了嗎？\n2. 我要找的是「等於某值」，還是「第一個滿足條件的位置」？判斷條件 `ok(mid)` 是什麼？\n3. 找不到時會回傳什麼？這個回傳值對題目（例如插入位置）有意義嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 34 | 在排序陣列中查詢元素的第一個和最後一個位置 | N/A | 練習在重複元素中找左右邊界；核心不是找到任一 target，而是分別找第一個 `>= target` 與第一個 `> target`。 |\n| 35 | 搜尋插入位置 | N/A | 把插入位置視為第一個 `>= target` 的下標，統一處理存在與不存在兩種情況。 |\n| 704 | 二分查詢 | N/A | 確認閉區間二分的不變式：`[l, r]` 內仍可能有答案，每次比較後刪掉不可能的一半。 |\n| 744 | 尋找比目標字母大的最小字母 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2529 | [正整數和負整數的最大計數](https://leetcode.cn/problems/maximum-count-of-positive-integer-and-negative-integer/) | 1196 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;\n    while (l <= r) {\n      int mid = l + (r - l) / 2;\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;\n      } else {\n        r = mid - 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。",
      "children": []
    },
    {
      "id": 3,
      "title": "2. 進階",
      "summary": "**原理講解**\n\n本節延續基礎二分，處理稍微「拐個彎」的情境：題目沒有直接叫你找某個值，而是要你**先想清楚「真假分界」在哪裡**，或把二分當成定位工具去框出一段範圍。基礎二分像查字典；這個技巧像偵探，得先把問題翻譯成一條「越往右越成立（或越不成立）」的單調性質，才能套二分。\n\n關鍵心法：**把答案想成一個候選值 `x`，並確認「`x` 可不可行」具有單調性**——也就是只要某個 `x` 可行，比它更寬鬆的都可行；反之更嚴格的都不可行。一旦有這條真假分界，就能二分找分界點。\n\n```cpp\n// 進階二分的通用骨架：在 [lo, hi) 找第一個「可行」的 x\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (feasible(mid)) {\n    hi = mid;  // mid 可行 → 答案在左半（含 mid）\n  } else {\n    lo = mid + 1;  // mid 不可行 → 答案在右半\n  }\n}\nreturn lo;  // 第一個可行的 x\n```\n\n以 LeetCode 658「找 K 個最接近的元素」為例：要選一段長度 `k` 的連續視窗，使視窗整體最接近 `x`。可以二分「視窗的左端點」——比較 `a[mid]` 與 `a[mid+k]` 哪邊離 `x` 更遠，藉此決定左端點往哪移。這就是用二分去「定位最佳起點」，而不是找某個固定值。\n\n```cpp\n// LeetCode 658：二分視窗左端點 i，使 [i, i+k) 最接近 x\nint lo = 0, hi = (int)a.size() - k;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (x - a[mid] > a[mid + k] - x) {\n    lo = mid + 1;  // 左邊那個更遠，視窗右移\n  } else {\n    hi = mid;  // 否則往左\n  }\n}\nreturn lo;  // 最佳左端點\n```\n\n**實作方式**\n\n**1. 把問題翻成「候選值 + 真假判斷」。** 先問：我能不能設一個值 `x`，然後寫一個 `feasible(x)` 判斷它行不行？例題（如 1385、2819）多半要先想出 `x` 的語意。\n\n```cpp\n// 寫出判斷函式：給定候選 x，回傳是否可行\nbool feasible(int x) { /* 用題意計算，回傳 true/false */ }\n```\n\n**2. 確認單調性，決定找「第一個」還是「最後一個」可行。** 「最小化某量」通常找**第一個** `feasible` 為真的 `x`；「最大化某量」通常找**最後一個**為真的（見下方求最大章節）。先在紙上畫一條 F...F T...T 的分界線。\n\n**3. 設好上下界並二分。** 上下界要涵蓋所有可能答案，但不要過寬以免溢位或變慢。\n\n```cpp\n// 找第一個可行 x（最小化方向）\nint lo = MIN_X, hi = MAX_X;  // 答案的合理範圍\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (feasible(mid)) {\n    hi = mid;\n  } else {\n    lo = mid + 1;\n  }\n}\nreturn lo;\n```\n\n**4. 邊界與複雜度。**\n\n- `feasible` 一次的成本若是 `O(n)`，整體就是 `O(n log W)`（`W` 是值域寬度），通常足夠快。\n- 若答案是「定位下標」而非「值」（如 658），上界要設成 `size() - k`，避免視窗越界（LeetCode 1287、1385 也要注意索引範圍）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往進階二分想：\n\n- **答案有明顯的「越大越難／越小越難」單調性**，例如 2819「最小相對損失」、LCP 08「劇情觸發時間」——都是設一個門檻再判斷可不可達。\n- **題目要你在有序資料裡「定位一段視窗或一個鄰近區間」**，像 658 找最接近的 k 個、1385 數「距離值」。\n- **暴力是枚舉所有候選 `x`（一次 `O(W)` 或 `O(n)`）太慢；** 換成二分後變 `O(log W)` 次判斷。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我設的候選值 `x` 語意是什麼？`feasible(x)` 怎麼判斷？\n2. 這個可行性真的單調嗎（F...F 之後一路 T...T）？分界在哪？\n3. 我要找的是第一個可行還是最後一個可行？對應的迴圈更新寫對了嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 658 | 找到 K 個最接近的元素 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2819 | 購買巧克力後的最小相對損失（Premium） | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| LCP 08 | 劇情觸發時間 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1287 | [有序陣列中出現次數超過 25% 的元素](https://leetcode.cn/problems/element-appearing-more-than-25-in-sorted-array/) | 1179 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1385 | [兩個陣列間的距離值](https://leetcode.cn/problems/find-the-distance-value-between-two-arrays/) | 1235 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;\n    while (l <= r) {\n      int mid = l + (r - l) / 2;\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;\n      } else {\n        r = mid - 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。",
      "children": []
    },
    {
      "id": 5,
      "title": "3. 求最小",
      "summary": "**原理講解**\n\n本節談「二分答案」中最常見的一種：**求最小**。題目通常長這樣——「在滿足某條件的前提下，把某個量壓到最小」。例如 LeetCode 475 供暖器：要選一個暖爐半徑，讓所有房屋都被覆蓋，問**最小**可行半徑。\n\n直接枚舉每個可能的答案太慢，但這類問題有個漂亮的性質：**答案越大越容易滿足條件，答案越小越難**。把半徑放大，本來覆蓋不到的房屋也被涵蓋了；放到夠大一定可行，放太小一定不可行。於是「可行性」沿著答案軸呈現 F...F T...T 的單調分界，我們要找的就是**第一個 T**——最小的可行值。\n\n```cpp\n// 求最小的骨架：在 [lo, hi] 找第一個「可行」的答案\nint lo = MIN, hi = MAX;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;  // mid 可行 → 試試更小，但保留 mid\n  } else {\n    lo = mid + 1;  // mid 不可行 → 必須更大\n  }\n}\nreturn lo;  // 第一個可行 = 最小可行答案\n```\n\n這裡的 `check(mid)` 是整個解法的靈魂：給定一個候選答案 `mid`，判斷它「行不行得通」。以 475 為例，`check(r)` 就是「半徑 r 能不能讓每間房屋都被某個暖爐覆蓋」。\n\n```cpp\n// LeetCode 475：半徑 r 能否覆蓋所有房屋（heaters 已排序）\nbool check(int r) {\n  for (int h : houses) {  // 每間房屋\n    auto it = lower_bound(heaters.begin(), heaters.end(), h);\n    bool ok = false;  // 找最近的暖爐，看距離是否 <= r\n    if (it != heaters.end() && *it - h <= r) {\n      ok = true;\n    }\n    if (it != heaters.begin() && h - *prev(it) <= r) {\n      ok = true;\n    }\n    if (!ok) {\n      return false;  // 有房屋蓋不到 → r 太小\n    }\n  }\n  return true;\n}\n```\n\n**實作方式**\n\n**1. 設答案變數，確認「越大越可行」。** 先講清楚答案是什麼量（半徑、時間、操作次數…），並驗證放大答案只會讓條件更容易成立。\n\n**2. 寫 `check(x)`：給定答案 x，判斷是否可行。** 這是最需要動腦的一步，通常用貪心或一次掃描完成。\n\n```cpp\n// check 模板：給定上限/門檻 x，判斷題目條件能否滿足\nbool check(long long x) { /* 貪心或掃描，回傳 true/false */ }\n```\n\n**3. 設上下界，二分找第一個可行。** 下界取「絕對不可行或剛好臨界」的值，上界取「一定可行」的值。\n\n```cpp\n// 求最小：找第一個 check 為真的 x\nlong long lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n  long long mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;  // 可行就往左壓\n  } else {\n    lo = mid + 1;\n  }\n}\nreturn lo;\n```\n\n**4. 邊界與複雜度。**\n\n- 上界務必設到「保證可行」，否則迴圈可能回傳一個其實不可行的值。\n- `check` 成本 `O(n)` 或 `O(n log n)` 的話，整體是 `O(n log W)`（`W` 為值域）。\n- 答案可能很大，用 `long long` 避免溢位（如 2604 喫穀子的時間、3824 的 K 值）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「二分求最小」想：\n\n- **題目出現「最小的…使得…成立」**，例如 475「最小半徑」、2702「使數字變非正的最小操作次數」、3824「滿足條件的最小 K 值」。\n- **把答案放大，條件只會更容易達成（單調）。** 這保證 F...F T...T 的分界存在。\n- **直接求答案很難，但「給定一個答案、驗證可不可行」很容易。** 這正是二分答案的甜蜜點。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的答案是哪個量？它「越大越可行」嗎？\n2. `check(x)` 怎麼寫？給定 x 我如何判斷可不可行？\n3. 上界設到一定可行了嗎？下界會不會漏掉最小答案？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 475 | 供暖器 | N/A | 把暖爐半徑視為答案門檻；半徑越大越容易覆蓋所有房屋，因此可二分最小可行半徑。 |\n| 2604 | 喫掉所有穀子的最短時間（Premium） | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 2702 | 使數字變為非正數的最小操作次數（Premium） | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 3807 | 修復邊以遍歷圖的最小成本（Premium） | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 3824 | [減小陣列使其滿足條件的最小 K 值](https://leetcode.cn/problems/minimum-k-to-reduce-array-within-limit/) | 1531 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 410. 分割陣列的最大值 展示核心寫法。\n\n**完整題目**：將非負整數陣列 `nums` 分割成 `k` 個非空連續子陣列，最小化這 `k` 段中的最大段和。\n\n**線索**：題目要求最小化上限或最大化下限，且給定候選值後可以快速判斷可不可行，這是二分答案的線索。\n\n```cpp\nclass Solution {\n public:\n  int splitArray(vector<int>& nums, int k) {\n    long long l = *max_element(nums.begin(), nums.end());\n    long long r = accumulate(nums.begin(), nums.end(), 0LL);\n    auto ok = [&](long long limit) {\n      int groups = 1;\n      long long sum = 0;\n      for (int x : nums) {\n        if (sum + x > limit) {\n          groups++;\n          sum = 0;\n        }\n        sum += x;\n      }\n      return groups <= k;\n    };\n    while (l < r) {\n      long long mid = l + (r - l) / 2;\n      if (ok(mid)) {\n        r = mid;\n      } else {\n        l = mid + 1;\n      }\n    }\n    return (int)l;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 答案越大越容易分割成功，因此可二分最小可行值；`ok` 只做貪心掃描，維持每段和不超過 `limit`。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 求最小可行值：找第一個 `check(x)=true`。\n- 求最大可行值：找最後一個 `check(x)=true`。\n- 間接值題先把收益/成本轉成可比較的門檻。\n\n**實作注意事項**\n\n- 先寫出 `x` 的單位與上下界，避免把下標和答案值混在一起。\n- `check` 必須足夠快，否則外層二分會放大成本。\n- 明確單調方向後，再決定使用偏左或偏右中點。\n- 用最小值、最大值、剛好不可行三種測資檢查邊界。",
      "children": []
    },
    {
      "id": 7,
      "title": "4. 求最大",
      "summary": "**原理講解**\n\n本節是「二分答案」的另一面：**求最大**。題目通常是「在滿足某條件的前提下，把某個量盡量放大」。例如 LeetCode 275 H 指數 II：找**最大**的 `h`，使得至少有 `h` 篇論文的引用數不小於 `h`。\n\n和求最小相反，這類問題的單調性是：**答案越小越容易滿足，越大越難**。`h` 訂得低，門檻寬鬆，容易達成；`h` 訂得太高就達不到。於是可行性沿答案軸呈現 T...T F...F，我們要找的是**最後一個 T**——最大的可行值。\n\n```cpp\n// 求最大的骨架：在 [lo, hi] 找最後一個「可行」的答案\nint lo = MIN, hi = MAX;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo + 1) / 2;  // 注意 +1：偏右取中點，避免死迴圈\n  if (check(mid)) {\n    lo = mid;  // mid 可行 → 試更大，但保留 mid\n  } else {\n    hi = mid - 1;  // mid 不可行 → 必須更小\n  }\n}\nreturn lo;  // 最後一個可行 = 最大可行答案\n```\n\n特別注意中點要寫成 `lo + (hi - lo + 1) / 2`（**偏右取整**）。因為更新是 `lo = mid`，若中點偏左，當 `lo` 和 `hi` 只差 1 時 `mid` 會一直等於 `lo`，造成無窮迴圈。求最大用偏右、求最小用偏左，這是固定搭配。\n\n```cpp\n// LeetCode 275：引用數 citations 已升序，h 篇是否都 >= h ？\nbool check(int h) {\n  int n = citations.size();\n  // 後 h 篇（最大的 h 個）中，最小的那個是否 >= h\n  return h == 0 || citations[n - h] >= h;\n}\n```\n\n**實作方式**\n\n**1. 設答案變數，確認「越大越難」。** 講清楚要最大化的量（H 指數、字號、平均數、能到的最遠距離…），並驗證放大答案只會讓條件更難成立。\n\n**2. 寫 `check(x)`：給定答案 x，判斷是否仍可行。** 與求最小相同，多半用貪心或一次掃描。\n\n```cpp\n// check 模板：給定下限/規模 x，判斷是否仍能達成\nbool check(long long x) { /* 貪心或掃描，回傳 true/false */ }\n```\n\n**3. 設上下界，用「偏右中點」二分找最後一個可行。**\n\n```cpp\n// 求最大：找最後一個 check 為真的 x\nlong long lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n  long long mid = lo + (hi - lo + 1) / 2;  // 偏右，配合 lo = mid\n  if (check(mid)) {\n    lo = mid;\n  } else {\n    hi = mid - 1;\n  }\n}\nreturn lo;\n```\n\n**4. 邊界與複雜度。**\n\n- 中點偏右是求最大的關鍵，寫成偏左會死迴圈。\n- 下界要設到「保證可行」（例如 0 或最小可能），否則最後可能回傳不可行值。\n- 若答案是浮點（如 644 子陣列最大平均數），改用固定次數的浮點二分或設精度 `eps`。\n- 複雜度 `O(n log W)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「二分求最大」想：\n\n- **題目出現「最大的…使得…成立」**，例如 275「最大 H 指數」、1618「適應螢幕的最大字號」、1642「能到達的最遠建築」、LCP 78「城牆防線」。\n- **把答案放大，條件只會更難達成（單調）。** 保證 T...T F...F 的分界存在。\n- **直接求最大很難，但「給定一個值、驗證能否達成」很容易。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要最大化的量是什麼？它「越大越難」嗎？\n2. `check(x)` 怎麼判斷給定 x 仍可行？\n3. 我用的是「偏右中點 + `lo = mid`」嗎？會不會死迴圈？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 78 | 城牆防線 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 275 | H 指數 II | N/A | 在已排序引用數中二分 H 指數 `h`：若至少有 `h` 篇論文引用數不小於 `h`，就嘗試更大的 `h`。 |\n| 644 | 子陣列最大平均數 II（Premium） | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1618 | 找出適應螢幕的最大字號（Premium） | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 1642 | [可以到達的最遠建築](https://leetcode.cn/problems/furthest-building-you-can-reach/) | 1962 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 410. 分割陣列的最大值 展示核心寫法。\n\n**完整題目**：將非負整數陣列 `nums` 分割成 `k` 個非空連續子陣列，最小化這 `k` 段中的最大段和。\n\n**線索**：題目要求最小化上限或最大化下限，且給定候選值後可以快速判斷可不可行，這是二分答案的線索。\n\n```cpp\nclass Solution {\n public:\n  int splitArray(vector<int>& nums, int k) {\n    long long l = *max_element(nums.begin(), nums.end());\n    long long r = accumulate(nums.begin(), nums.end(), 0LL);\n    auto ok = [&](long long limit) {\n      int groups = 1;\n      long long sum = 0;\n      for (int x : nums) {\n        if (sum + x > limit) {\n          groups++;\n          sum = 0;\n        }\n        sum += x;\n      }\n      return groups <= k;\n    };\n    while (l < r) {\n      long long mid = l + (r - l) / 2;\n      if (ok(mid)) {\n        r = mid;\n      } else {\n        l = mid + 1;\n      }\n    }\n    return (int)l;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 答案越大越容易分割成功，因此可二分最小可行值；`ok` 只做貪心掃描，維持每段和不超過 `limit`。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 求最小可行值：找第一個 `check(x)=true`。\n- 求最大可行值：找最後一個 `check(x)=true`。\n- 間接值題先把收益/成本轉成可比較的門檻。\n\n**實作注意事項**\n\n- 先寫出 `x` 的單位與上下界，避免把下標和答案值混在一起。\n- `check` 必須足夠快，否則外層二分會放大成本。\n- 明確單調方向後，再決定使用偏左或偏右中點。\n- 用最小值、最大值、剛好不可行三種測資檢查邊界。",
      "children": []
    },
    {
      "id": 8,
      "title": "5. 分間接值",
      "summary": "**原理講解**\n\n本節處理一種比較隱晦的二分：**分間接值**。意思是——我們真正想要的答案（例如總收益、某個座標、某個計數），不能直接二分；但它和「某個有單調性的中間量」掛勾，於是我們先二分那個**間接量**，再由它一次算出真正的答案。\n\n以 LeetCode 1648「銷售價值減少的顏色球」為例：你要賣 `orders` 顆球，每顆球的售價等於該顏色當前剩餘數量，賣完價格就降一格，目標是**最大化總收益**。一顆一顆賣是 `O(orders)`，數量上看可能上億，太慢。觀察到一個單調量：「最後所有顏色都會被賣到某個**價格層 `p`**」——把所有數量 `>= p` 的球削到 `p`。`p` 越低，賣掉的球越多。於是我們二分這個「最終價格層 `p`」，再用等差級數一次加總收益。\n\n```cpp\n// 核心轉念：不二分「收益」，而二分「最終價格層 p」\n// 給定 p：把每堆 cnt 削到 p，賣掉 (cnt - p) 顆，共賣 sold(p) 顆\nlong long sold(int p, vector<int>& a) {\n  long long s = 0;\n  for (int c : a) {\n    if (c > p) {\n      s += c - p;  // 高於 p 的部分都會被賣掉\n    }\n  }\n  return s;  // sold(p) 隨 p 下降而單調遞增\n}\n```\n\n`sold(p)` 對 `p` 是單調的：`p` 越小賣得越多。我們二分找「剛好能賣到至少 `orders` 顆的最高價格層 `p`」，這是一個標準的單調分界。找到 `p` 後，每堆從 `c` 賣到 `p+1`（或 `p`）這段的收益是連續整數相加，用等差級數公式 `(首項+末項)*項數/2` 一次算完，避免逐顆累加。\n\n```cpp\n// 等差級數一次加總：賣價從 hi 降到 lo+1 的總收益\nlong long sumRange(long long lo, long long hi) {  // 賣價 lo+1, ..., hi\n  return (lo + 1 + hi) * (hi - lo) / 2;\n}\n```\n\n**實作方式**\n\n**1. 找出可二分的「間接量」。** 真答案（收益、計數）若不單調，就找一個與它相關、且**隨著它單調變化**的中間量（價格層、門檻、半徑…）來二分。\n\n**2. 寫間接量的判斷函式。** 給定間接量 `p`，能否快速算出「對應的數量／可行性」？這一步要 `O(n)` 或更快。\n\n```cpp\n// 二分最終價格層 p：找最高的 p 使 sold(p) >= orders\nint lo = 0, hi = *max_element(a.begin(), a.end());\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (sold(mid, a) >= orders) {\n    lo = mid + 1;  // 還能多賣 → 提高價格層門檻\n  } else {\n    hi = mid;\n  }\n}\n// 結束後 lo = 最低能滿足的下一層；p = lo - 1 之類，依語意微調\n```\n\n**3. 由間接量一次回推真答案。** 拿到分界後，**不要再逐項模擬**，用公式（等差級數、前綴和、計數）一次合成最終答案——這正是「分間接值」省時間的地方。\n\n**4. 邊界與複雜度。**\n\n- 二分邊界附近常有「剩餘零頭」要單獨處理（例如 1648 賣到 `p` 那層時可能只賣部分球），務必小心 off-by-one。\n- 收益可能極大，用 `long long`，並記得對大質數取模（1648 要求 mod 1e9+7）。\n- 複雜度 `O(n log W)`，遠優於逐顆 `O(orders)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「分間接值」想：\n\n- **真正要求的量（收益、座標、計數）本身不單調，無法直接二分**，但存在一個與它相關的單調中間量，例如 1648 的「價格層」、3143「正方形中最多點數」的「正方形邊長」。\n- **逐項模擬會超時**（如一顆一顆賣、一格一格擴張），需要用二分定位門檻後一次合成。\n- **算出間接量後，能用公式／前綴和把答案一次湊出來。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我真正要的答案是什麼？它本身單調嗎？\n2. 有沒有一個「間接量」隨答案單調變化、可以拿來二分？\n3. 二分定位到間接量後，我怎麼**一次**算出真答案（哪個公式）？邊界零頭處理了嗎？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1648 | [銷售價值減少的顏色球](https://leetcode.cn/problems/sell-diminishing-valued-colored-balls/) | 2050 | 把逐顆賣球改成價格層門檻：先二分最後會賣到的價格層，再用等差級數一次加總收益。 |\n| 3143 | [正方形中的最多點數](https://leetcode.cn/problems/maximum-points-inside-the-square/) | 1697 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 410. 分割陣列的最大值 展示核心寫法。\n\n**完整題目**：將非負整數陣列 `nums` 分割成 `k` 個非空連續子陣列，最小化這 `k` 段中的最大段和。\n\n**線索**：題目要求最小化上限或最大化下限，且給定候選值後可以快速判斷可不可行，這是二分答案的線索。\n\n```cpp\nclass Solution {\n public:\n  int splitArray(vector<int>& nums, int k) {\n    long long l = *max_element(nums.begin(), nums.end());\n    long long r = accumulate(nums.begin(), nums.end(), 0LL);\n    auto ok = [&](long long limit) {\n      int groups = 1;\n      long long sum = 0;\n      for (int x : nums) {\n        if (sum + x > limit) {\n          groups++;\n          sum = 0;\n        }\n        sum += x;\n      }\n      return groups <= k;\n    };\n    while (l < r) {\n      long long mid = l + (r - l) / 2;\n      if (ok(mid)) {\n        r = mid;\n      } else {\n        l = mid + 1;\n      }\n    }\n    return (int)l;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 答案越大越容易分割成功，因此可二分最小可行值；`ok` 只做貪心掃描，維持每段和不超過 `limit`。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 求最小可行值：找第一個 `check(x)=true`。\n- 求最大可行值：找最後一個 `check(x)=true`。\n- 間接值題先把收益/成本轉成可比較的門檻。\n\n**實作注意事項**\n\n- 先寫出 `x` 的單位與上下界，避免把下標和答案值混在一起。\n- `check` 必須足夠快，否則外層二分會放大成本。\n- 明確單調方向後，再決定使用偏左或偏右中點。\n- 用最小值、最大值、剛好不可行三種測資檢查邊界。",
      "children": []
    },
    {
      "id": 9,
      "title": "6. 最小化最大值",
      "summary": "**原理講解**\n\n本節是二分答案中非常經典的一型：**最小化最大值**。題目要你把東西分組、安排或調度，使得「其中**最大**的那一份盡量小」。例如 LeetCode 410 分割陣列：把陣列切成 `k` 段，使**最大段和**最小；或 1631 最小體力消耗路徑：找一條路，使路上**最大**的高度差最小。\n\n直接求「最小的最大值」很繞，但有個關鍵轉念：**把答案當成一個「允許上限 `limit`」**。問題就變成「如果規定每一份都不准超過 `limit`，做不做得到？」這個 `check(limit)` 具有單調性——`limit` 放得越寬鬆（越大）越容易做到，越緊（越小）越難。於是可行性沿 `limit` 軸呈 F...F T...T，我們二分找**第一個可行的 `limit`**，那就是答案。\n\n```cpp\n// 最小化最大值的骨架：找第一個「上限 limit 可行」的值\nint lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;  // 上限 mid 夠用 → 試更小\n  } else {\n    lo = mid + 1;  // mid 太緊 → 放寬\n  }\n}\nreturn lo;  // 最小可行上限 = 最小化後的最大值\n```\n\n`check(limit)` 怎麼寫，以 410 為例：規定每段和不超過 `limit`，用貪心一段一段塞，數一數至少需要幾段；若需要的段數 `<= k`，就代表這個 `limit` 可行。\n\n```cpp\n// LeetCode 410：每段和不超過 limit，最少需要幾段 <= k ?\nbool check(long long limit, vector<int>& a, int k) {\n  int seg = 1;\n  long long cur = 0;\n  for (int x : a) {\n    if (x > limit) {\n      return false;  // 單一元素就超上限，不可能\n    }\n    if (cur + x > limit) {\n      seg++;\n      cur = 0;\n    }  // 開新的一段\n    cur += x;\n  }\n  return seg <= k;  // 段數夠少 → 可行\n}\n```\n\n**實作方式**\n\n**1. 把答案設成「允許上限 limit」。** 確認「上限越大越容易達成」這個單調性，這是套這個模型的前提。\n\n**2. 寫 `check(limit)`：在上限下能否完成。** 多半是貪心（盡量塞滿一份再開新份）或 BFS/DFS（路徑類題目，如 778、1631 判斷在水位/體力 limit 下能否連通起終點）。\n\n```cpp\n// check 模板：每一份/每一步都不超過 limit，能否達成題目要求\nbool check(long long limit) { /* 貪心分組 或 圖上可達性，回傳 true/false */ }\n```\n\n**3. 設上下界，二分找第一個可行上限。** 下界常取「單一元素的最大值」（再小一定有元素超標），上界取「全部加起來」或值域最大。\n\n```cpp\n// 二分最小可行上限\nlong long lo = *max_element(a.begin(), a.end());     // 下界：最大單元素\nlong long hi = accumulate(a.begin(), a.end(), 0LL);  // 上界：全部一段\nwhile (lo < hi) {\n  long long mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;\n  } else {\n    lo = mid + 1;\n  }\n}\nreturn lo;\n```\n\n**4. 邊界與複雜度。**\n\n- 下界別設太低：若有單一元素就超過 `limit`，那段永遠塞不下，要在 `check` 裡擋掉。\n- 路徑類題目的 `check` 是一次 BFS/DFS `O(n)`，整體 `O(n log W)`。\n- 用 `long long` 防段和溢位。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「最小化最大值」想：\n\n- **題目字面就是「使最大的…最小」**，例如 410「分割陣列的最大值」、774「最小化加油站最大距離」、1631「最小體力消耗路徑」、LCP 12「刷題計劃」。\n- **把允許上限放寬只會更容易（單調）。** 保證 F...F T...T 分界存在。\n- **直接求最小的最大值很難，但「給定上限、判斷可不可行」很容易（貪心或一次搜尋）。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把答案設成「上限 limit」了嗎？上限越大越容易達成嗎？\n2. `check(limit)` 怎麼判斷？是貪心分組還是圖上可達？\n3. 我找的是第一個可行上限（偏左二分）嗎？下界會不會讓單一元素就超標？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| LCP 12 | 小張刷題計劃 | N/A | 把答案當成允許上限；練習設計 `check(limit)`，判斷所有局部成本能否被壓在上限內。 |\n| 410 | 分割陣列的最大值 | N/A | 把「分成 `k` 段後最大段和最小」改成判斷上限 `limit` 能否在不超過 `k` 段內完成。 |\n| 774 | 最小化去加油站的最大距離（Premium） | N/A | 把答案當成允許上限；練習設計 `check(limit)`，判斷所有局部成本能否被壓在上限內。 |\n| 778 | 水位上升的泳池中游泳 | N/A | 把答案當成允許上限；練習設計 `check(limit)`，判斷所有局部成本能否被壓在上限內。 |\n| 1631 | [最小體力消耗路徑](https://leetcode.cn/problems/path-with-minimum-effort/) | 1948 | 把答案當成允許上限；練習設計 `check(limit)`，判斷所有局部成本能否被壓在上限內。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 410. 分割陣列的最大值 展示核心寫法。\n\n**完整題目**：將非負整數陣列 `nums` 分割成 `k` 個非空連續子陣列，最小化這 `k` 段中的最大段和。\n\n**線索**：目標是壓低最大代價；看到「最小化最大值」時，通常把答案當成允許上限 `limit`，再檢查是否可行。\n\n```cpp\nclass Solution {\n public:\n  int splitArray(vector<int>& nums, int k) {\n    long long l = *max_element(nums.begin(), nums.end());\n    long long r = accumulate(nums.begin(), nums.end(), 0LL);\n    auto ok = [&](long long limit) {\n      int groups = 1;\n      long long sum = 0;\n      for (int x : nums) {\n        if (sum + x > limit) {\n          groups++;\n          sum = 0;\n        }\n        sum += x;\n      }\n      return groups <= k;\n    };\n    while (l < r) {\n      long long mid = l + (r - l) / 2;\n      if (ok(mid)) {\n        r = mid;\n      } else {\n        l = mid + 1;\n      }\n    }\n    return (int)l;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 答案越大越容易分割成功，因此可二分最小可行值；`ok` 只做貪心掃描，維持每段和不超過 `limit`。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 分割陣列最大段和：貪心檢查段數。\n- 路徑最大邊權最小：只保留不超過門檻的邊再判斷可達。\n- 工作量分配：判斷容量上限是否足夠。\n\n**實作注意事項**\n\n- 下界常是單個元素/單條邊的最大值，上界常是總和或最大可能代價。\n- `check` 裡若用貪心，要說明為何局部塞滿不會增加段數。\n- 找第一個 true 時使用偏左中點。\n- 注意題目是否要求恰好 k 組；很多 minimax check 只需不超過 k 組。",
      "children": []
    },
    {
      "id": 10,
      "title": "7. 最大化最小值",
      "summary": "**原理講解**\n\n本節是上一節的鏡像：**最大化最小值**。題目要你安排或挑選，使得「其中**最小**的那一份盡量大」。例如 LeetCode 1552 兩球之間的磁力：把 `m` 顆球放進不同位置，使**相鄰球最小間距**最大；或 2517 禮盒最大甜蜜度：選若干糖果，使任兩種的**最小差值**最大。\n\n轉念和上一節對稱：**把答案當成一個「保底下限 `d`」**。問題變成「如果要求每一份／每個間距都**至少** `d`，做不做得到？」這個 `check(d)` 的單調性是——`d` 訂得越小越寬鬆、越容易達成；`d` 越大越難。於是可行性沿 `d` 軸呈 T...T F...F，我們二分找**最後一個可行的 `d`**，那就是答案。\n\n```cpp\n// 最大化最小值的骨架：找最後一個「下限 d 可行」的值\nint lo = LOW, hi = HIGH;\nwhile (lo < hi) {\n  int mid = lo + (hi - lo + 1) / 2;  // 偏右取中點（配合 lo = mid，防死迴圈）\n  if (check(mid)) {\n    lo = mid;  // 下限 mid 達得到 → 試更大\n  } else {\n    hi = mid - 1;  // mid 太大 → 退一步\n  }\n}\nreturn lo;  // 最大可行下限 = 最大化後的最小值\n```\n\n注意中點要**偏右** `lo + (hi - lo + 1) / 2`，因為更新是 `lo = mid`，偏左會在剩兩個數時死迴圈——這和求最大章節一致。`check(d)` 以 1552 為例：座標排序後貪心放球，每放一顆就跳到下一個距離至少 `d` 的位置，數能放幾顆。\n\n```cpp\n// LeetCode 1552：座標已排序，相鄰間距至少 d，能否放下 m 顆球？\nbool check(int d, vector<int>& pos, int m) {\n  int cnt = 1, last = pos[0];  // 第一顆放最左\n  for (int i = 1; i < (int)pos.size(); i++) {\n    if (pos[i] - last >= d) {  // 距離夠遠才放下一顆\n      cnt++;\n      last = pos[i];\n    }\n  }\n  return cnt >= m;  // 放得下 m 顆 → 下限 d 可行\n}\n```\n\n**實作方式**\n\n**1. 把答案設成「保底下限 d」。** 確認「下限越小越容易達成」這個單調性，這是套用前提。\n\n**2. 寫 `check(d)`：在「每份至少 d」的要求下能否完成。** 多為貪心（能放/能分就盡量放，數夠不夠），如 1231 分巧克力、2517 選糖果都是這個套路。\n\n```cpp\n// check 模板：要求每一份至少 d，能否湊出題目要的數量\nbool check(long long d) { /* 貪心累積，回傳 是否達標 */ }\n```\n\n**3. 設上下界，用「偏右中點」二分找最後一個可行下限。**\n\n```cpp\n// 二分最大可行下限\nlong long lo = 0, hi = HIGH;  // 下界 0 一定可行，上界取值域最大\nwhile (lo < hi) {\n  long long mid = lo + (hi - lo + 1) / 2;  // 偏右\n  if (check(mid)) {\n    lo = mid;\n  } else {\n    hi = mid - 1;\n  }\n}\nreturn lo;\n```\n\n**4. 邊界與複雜度。**\n\n- 中點一定要偏右、配 `lo = mid`，否則死迴圈（這是最大化方向的通病）。\n- 上界別設太小，要能涵蓋最大可能下限（例如座標跨度、值域差）。\n- `check` 通常 `O(n)`（排序後一次掃描），整體 `O(n log W)`；圖上題（如 1102 得分最高路徑、2528 最小電量）的 `check` 可能是一次 BFS。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往「最大化最小值」想：\n\n- **題目字面是「使最小的…最大」**，例如 1552「兩球磁力（最大化最小間距）」、2517「最大甜蜜度」、1231「分享巧克力」、1102「路徑最小值最大」、2528「最大化最小電量」。\n- **把保底下限調低只會更容易（單調）。** 保證 T...T F...F 分界存在。\n- **直接求最大的最小值很難，但「給定下限、判斷達不達得到」很容易（貪心或一次搜尋）。**\n\n確認自己有沒有套對，問三個問題：\n\n1. 我把答案設成「下限 d」了嗎？下限越小越容易達成嗎？\n2. `check(d)` 怎麼判斷？是貪心放置還是圖上可達？\n3. 我用的是「偏右中點 + `lo = mid`」找最後一個可行嗎？會不會死迴圈？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1102 | [得分最高的路徑（Premium）](https://leetcode.cn/problems/path-with-maximum-minimum-value/) | 2011 | 把答案當成保底下限；練習證明下限提高後只會更難，並用偏右二分找最後可行值。 |\n| 1231 | [分享巧克力（Premium）](https://leetcode.cn/problems/divide-chocolate/) | 2029 | 把答案當成保底下限；練習證明下限提高後只會更難，並用偏右二分找最後可行值。 |\n| 1552 | [兩球之間的磁力](https://leetcode.cn/problems/magnetic-force-between-two-balls/) | 1920 | 排序座標後，判斷能否放下 `m` 顆球且相鄰距離至少為 `d`；`d` 越小越容易，答案要找最後一個可行值。 |\n| 2517 | [禮盒的最大甜蜜度](https://leetcode.cn/problems/maximum-tastiness-of-candy-basket/) | 2021 | 把答案當成保底下限；練習證明下限提高後只會更難，並用偏右二分找最後可行值。 |\n| 2528 | [最大化城市的最小電量](https://leetcode.cn/problems/maximize-the-minimum-powered-city/) | 2236 | 把答案當成保底下限；練習證明下限提高後只會更難，並用偏右二分找最後可行值。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 1552. 兩球之間的磁力 展示核心寫法。\n\n**完整題目**：給定籃子座標 `position` 與球數 `m`，選出 `m` 個位置放球，使任兩球距離的最小值最大化。\n\n**線索**：目標是拉高最低保障；看到「最大化最小值」時，通常把答案當成下限，判斷是否還能完成安排。\n\n```cpp\nclass Solution {\n public:\n  int maxDistance(vector<int>& position, int m) {\n    sort(position.begin(), position.end());\n    int l = 1, r = position.back() - position.front();\n    auto ok = [&](int dist) {\n      int cnt = 1, last = position[0];\n      for (int x : position) {\n        if (x - last >= dist) {\n          cnt++;\n          last = x;\n        }\n      }\n      return cnt >= m;\n    };\n    while (l < r) {\n      int mid = l + (r - l + 1) / 2;\n      if (ok(mid)) {\n        l = mid;\n      } else {\n        r = mid - 1;\n      }\n    }\n    return l;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 距離下限越小越容易放球，因此 `ok(dist)` 是前半段可行、後半段不可行；用偏右中點尋找最後一個可行距離。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 放置問題：排序後盡量早放，保留後續空間。\n- 切分收益：累積到至少 `x` 就切一段。\n- 網格安全值：只走安全值不低於門檻的格子。\n\n**實作注意事項**\n\n- 上界常是最大座標差、最大格值或總收益。\n- 找最後一個 true 時使用偏右中點，避免死循環。\n- 貪心檢查要維持「越早滿足越不吃虧」的不變式。\n- 若資料未排序，通常先排序或預處理門檻。",
      "children": []
    },
    {
      "id": 11,
      "title": "8. 第 K 小/大",
      "summary": "**原理講解**\n\n本節要解決的場景是：一堆數字裡，「排名第 K 小（或第 K 大）的那一個值是多少？」最直覺的做法是全部排序再數到第 K 個，但當資料是「一行行、一列列都已經有序」或「規模大到不該攤平」時，重排序很浪費。這個技巧有兩條主線，常常一起用。\n\n第一條是「二分答案 + 計數」。重點觀念：與其去找「第 K 小的元素在哪」，不如直接去猜「第 K 小的值是多少」。對一個候選值 `x`，我們問一個容易回答的問題：「有多少個元素 `<= x`？」這個計數記為 `count(x)`，它隨 `x` 變大而單調不減，於是可以二分。\n\n```cpp\n// 二分答案的骨架：找最小的 x，使得 <= x 的個數達到 k\nwhile (lo < hi) {\n  long long mid = lo + (hi - lo) / 2;\n  if (countLE(mid) >= k) {\n    hi = mid;  // 達標，答案可能更小\n  } else {\n    lo = mid + 1;  // 不夠，往大找\n  }\n}\n// lo 就是第 k 小的值\n```\n\n為什麼答案一定是某個真實存在的元素？因為「使 `count(x) >= k` 的最小 `x`」這個位置，剛好就是第 K 小那個值本身——再小一點計數就掉到 `k` 以下了。像 378（有序矩陣第 K 小）、668（乘法表第 K 小）、719（第 K 小數對距離）、786（第 K 小質數分數）都是這個套路，差別只在 `countLE` 怎麼快速算。\n\n第二條是「小根堆逐步展開」。當題目要「依序產生前 K 個最小組合」（如 373 和最小的 K 對數字），我們用堆只保留「目前已知的最小候選」，每取出一個就把它的「下一個鄰居」推進堆，避免一次生出全部組合。\n\n```cpp\n// 小根堆：每次取最小，再推入它的後繼候選\npriority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;\npq.push({nums1[0] + nums2[0], 0});  // {和, 在 nums2 的下標}，i 固定那條鏈\n```\n\n**實作方式**\n\n**1. 先判斷要走哪條路。** 問「第 K 個值是多少」→ 二分答案；問「前 K 個東西分別是什麼」→ 小根堆展開。\n\n**2.（二分答案）寫出 `countLE(x)`。** 利用題目結構讓計數變快。例如乘法表第 `i` 列不超過 `x` 的個數是 `min(n, x / i)`，一列 `O(1)`，全表 `O(n)`。\n\n```cpp\n// 乘法表：計算 <= x 的元素個數\nlong long countLE(int x, int n) {\n  long long cnt = 0;\n  for (int i = 1; i <= n; ++i) {\n    cnt += min(n, x / i);\n  }\n  return cnt;\n}\n```\n\n**3.（二分答案）設好二分上下界。** 下界是最小可能值、上界是最大可能值（如 `n*n`），再套上面的二分骨架。\n\n**4.（小根堆）每彈一個就補它的後繼。** 用「已彈出第 K 次時的值」當答案；推入前可加 `visited` 防重複。\n\n```cpp\n// 取出第 k 小：彈出 k 次，每次推入後繼\nwhile (k-- > 1) {\n  auto [sum, j] = pq.top();\n  pq.pop();\n  if (j + 1 < n2) {\n    pq.push({/*新和*/, j + 1});  // 同一條鏈往後一格\n  }\n}\n```\n\n**5. 邊界與複雜度。** 二分答案是 `O(計數成本 × log值域)`，如乘法表為 `O(n log(n*n))`；堆展開取前 K 個是 `O(k log k)`。注意值域可能很大，二分變數用 `long long` 避免溢位。\n\n**題目線索（Pattern）**\n\n看到下列特徵，往「第 K 小/大」想：\n\n- **問的是「排名第 K 的值」而不是位置**，且資料量大、不適合整個排序。這通常是二分答案的訊號。\n- **有一個比「直接找第 K 個」更好算的計數問題**：給定 `x`，能快速算出「不超過 `x` 的個數」。能算 `countLE`，就能二分。\n- **集合本身就有序或半有序**（有序矩陣、乘法表、排序後的數對距離），讓計數可以用公式或雙指標 `O(n)` 完成，而非 `O(n^2)`。\n- **要「依序吐出前 K 個最小組合」**（多條有序鏈合併），這是小根堆逐步展開的典型場景，如 373。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我是要「第 K 個的值」（二分答案）還是「前 K 個東西」（堆展開）？\n2. 若走二分，我的 `countLE(x)` 怎麼利用題目結構快速算？單調性對嗎？\n3. 若走堆，每彈出一個元素後，要推入哪個「後繼候選」才不漏不重？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 373 | 查詢和最小的 K 對數字 | N/A | 把每個 `nums1[i]` 搭配 `nums2` 的和視為一條遞增鏈，用小根堆只展開當前最小的下一對；適合對照「產生前 K 個」與「只求第 K 個值」的差別。 |\n| 378 | 有序矩陣中第 K 小的元素 | N/A | 矩陣行列有序時，不必攤平成 `n^2` 個數；對值 `x` 從左下或右上計算 `<= x` 的數量，二分第一個計數達到 `k` 的值。 |\n| 668 | 乘法表中第 K 小的數 | N/A | 乘法表不用真的建表；固定 `x` 後第 `i` 列有 `min(n, x / i)` 個數不超過 `x`，這就是排名判斷。 |\n| 719 | 找出第 K 小的數對距離 | N/A | 先排序陣列，固定距離 `x` 後用雙指標統計距離 `<= x` 的數對數，再二分最小可達第 `k` 名的距離。 |\n| 786 | 第 K 個最小的質數分數 | N/A | 分數有序但候選是有理數；可二分值並用雙指標計數，同時記錄不超過門檻的最大分數作答案候選。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 378. 有序矩陣中第 K 小的元素 展示核心寫法。\n\n**完整題目**：給定每列、每欄皆升序的 `n x n` 矩陣，回傳其中第 `k` 小的元素；重複值要分別計入排名。\n\n**線索**：題目問第 K 小/大，但候選集合太大不能全部列出；能計算 `<= x` 的候選數量，就是二分排名的線索。\n\n```cpp\nclass Solution {\n public:\n  int kthSmallest(vector<vector<int>>& matrix, int k) {\n    int n = matrix.size();\n    int l = matrix[0][0], r = matrix[n - 1][n - 1];\n    auto countLE = [&](int x) {\n      int cnt = 0, row = n - 1, col = 0;\n      while (row >= 0 && col < n) {\n        if (matrix[row][col] <= x) {\n          cnt += row + 1;\n          col++;\n        } else {\n          row--;\n        }\n      }\n      return cnt;\n    };\n    while (l < r) {\n      int mid = l + (r - l) / 2;\n      if (countLE(mid) >= k) {\n        r = mid;\n      } else {\n        l = mid + 1;\n      }\n    }\n    return l;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 二分的是元素值而不是矩陣下標；`countLE(x)` 利用行列有序在線性時間計算排名，最後找第一個排名至少為 `k` 的值。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 矩陣第 K 小：用行列有序計數。\n- 乘法表第 K 小：逐列加總 `<= x` 的個數。\n- 數對距離第 K 小：排序後雙指標計數距離不超過 `x` 的數對。\n\n**實作注意事項**\n\n- 二分值域時回傳值，不是回傳下標。\n- `count(<= mid) >= k` 表示答案在左半邊含 mid。\n- 若要回傳實際分數/配對，計數時還要同步維護最接近門檻的候選。\n- 大值域與乘法計數要用 long long 避免溢位。",
      "children": []
    },
    {
      "id": 12,
      "title": "9. 三分法",
      "summary": "**原理講解**\n\n本節要解決的場景是：有一個函數 `f(x)`，它先「一路下降到最低點、再一路上升」（這種形狀叫**單峰／凸函數**，這裡是「谷形」），我們想找出讓 `f(x)` 最小的那個 `x`。普通二分搜尋在這裡用不了——二分需要「左邊全錯、右邊全對」這種單調分界，而谷形函數兩邊都會升高，沒有這種分界。\n\n三分法（ternary search）的核心想法：在區間裡取兩個點 `m1 < m2`，比較 `f(m1)` 和 `f(m2)`，就能砍掉三分之一的區間。\n\n```cpp\n// 比較兩個內點，判斷最低點落在哪一側\ndouble m1 = lo + (hi - lo) / 3;\ndouble m2 = hi - (hi - lo) / 3;\nif (f(m1) < f(m2)) {\n  hi = m2;  // 最低點不在 m2 右邊，丟掉右段\n} else {\n  lo = m1;  // 最低點不在 m1 左邊，丟掉左段\n}\n```\n\n為什麼能這樣砍？因為函數是谷形：如果 `f(m1) < f(m2)`，那最低點絕不可能在 `m2` 的右側（右側只會更高），所以 `[m2, hi]` 整段安全捨棄。反之亦然。每一輪區間縮成原來的 `2/3`，重複夠多次就逼近谷底。\n\n像 1515（服務中心最佳位置）就是典型：要找一個點使「到所有客戶的距離總和」最小，這個總和隨座標移動是凸函數，沿 x 與 y 各做一次三分即可。\n\n**實作方式**\n\n**1. 先確認函數是單峰的。** 三分法只對「凸／凹」函數成立。距離總和、拋物線型代價、先減後增的時間成本，通常是凸的；若函數忽上忽下，三分會找錯。\n\n**2. 設定區間與停止條件。** 連續值用「區間夠窄就停」（看精度需求），整數則用「區間長度小到可暴力掃」收尾。\n\n```cpp\n// 連續版：固定迭代次數，足夠逼近精度\nfor (int iter = 0; iter < 100; ++iter) {\n  double m1 = lo + (hi - lo) / 3;\n  double m2 = hi - (hi - lo) / 3;\n  if (f(m1) < f(m2)) {\n    hi = m2;\n  } else {\n    lo = m1;\n  }\n}\n```\n\n**3. 寫出評估函數 `f(x)`。** 這是題目的本體，例如 1515 的 `f(x, y)` 是到所有點的歐式距離和。\n\n```cpp\n// 1515：固定一點，算到所有客戶的距離總和\ndouble cost(double x, double y, vector<vector<int>>& pos) {\n  double s = 0;\n  for (auto& p : pos) {\n    s += hypot(x - p[0], y - p[1]);\n  }\n  return s;\n}\n```\n\n**4. 多維就「嵌套三分」。** 二維問題（如平面選點）對 x 做三分，內層再對 y 做三分；外層每次評估都呼叫一次內層三分。\n\n```cpp\n// 外層對 x 三分，f(x) 內部再對 y 三分求該行最佳值\ndouble bestAtX(double x) { /* 對 y 做一次三分，回傳最小 cost */ }\n```\n\n**5. 邊界與複雜度。** 每輪砍掉 `1/3`，迭代 `t` 次精度約 `(2/3)^t`，複雜度 `O(t × 單次評估)`；二維嵌套是 `O(t^2 × 單次評估)`。整數三分要小心邊界，最後幾格直接暴力比較，避免兩內點重合卡死。\n\n**題目線索（Pattern）**\n\n看到下列特徵，往「三分法」想：\n\n- **要最小化／最大化一個值，而這個值對某個連續參數是「先降後升」或「先升後降」。** 關鍵字像「最佳位置」「最小總代價」「使……最小的座標」。\n- **答案是一個連續座標或實數，不是離散選項**，而且代價函數平滑、單峰。\n- **二分搜尋用不上**：沒有「是/否」的單調分界，只有一個極值點。這正是三分法補位的地方。\n- **可以在常數時間或線性時間內評估 `f(x)`**（給定 `x` 算出代價），否則每輪評估太貴就不划算。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的代價函數對目標參數真的是單峰（凸或凹）嗎？能不能說明為什麼？\n2. 我要找的是最小值還是最大值？比較方向（`<` 還是 `>`）對應對了嗎？\n3. 是一維還是多維？多維時內層要對哪個變數再三分？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 1515 | [服務中心的最佳位置](https://leetcode.cn/problems/best-position-for-a-service-centre/) | 2157 | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;\n    while (l <= r) {\n      int mid = l + (r - l) / 2;\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;\n      } else {\n        r = mid - 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。",
      "children": []
    },
    {
      "id": 13,
      "title": "10. 其他",
      "children": [],
      "summary": "**原理講解**\n\n本節是二分搜尋的「綜合場」，收錄各種不完全套在「最小化最大值／第 K 小」模板裡的二分題：旋轉排序陣列（33、81）、x 的平方根（69）、二維矩陣搜尋（74）、兩個有序陣列的中位數（4）。它們表面差很多，但骨子裡都是同一句話：**在一個有「單調分界」的搜尋空間上，用對折的方式快速逼近答案。**\n\n二分能用的唯一前提是：把問題改寫成一個「是/否」判斷後，答案空間長成「前面一段全是 false、後面一段全是 true」（或反過來）。我們要找的，就是這條分界線。\n\n```cpp\n// 找第一個讓 check 為 true 的位置（lower_bound 思路）\nwhile (lo < hi) {\n  int mid = lo + (hi - lo) / 2;\n  if (check(mid)) {\n    hi = mid;  // 達標，分界在 mid 或更左\n  } else {\n    lo = mid + 1;  // 不達標，分界在右\n  }\n}\n// lo 即第一個 check 為 true 的位置\n```\n\n最直接的應用是 69（x 的平方根）：`check(m) = (m*m <= x)`，前面全 true、後面全 false，找「最後一個平方仍 `<= x` 的數」。74（二維矩陣）則是把 `m×n` 矩陣攤平看成一個有序陣列，用 `idx/n` 和 `idx%n` 還原到 `(row, col)`，普通二分即可。\n\n旋轉排序陣列（33、81）特別一點：陣列被旋轉後不再整段有序，但**對半切後，至少有一半仍是有序的**，於是每一步先判斷哪半有序、再看目標是否落在那半。\n\n```cpp\n// 33：每次至少有一半有序，判斷目標在不在有序的那半\nif (nums[lo] <= nums[mid]) {  // 左半有序\n  if (nums[lo] <= target && target < nums[mid]) {\n    hi = mid - 1;\n  } else {\n    lo = mid + 1;\n  }\n} else { /* 右半有序，對稱處理 */\n}\n```\n\n**實作方式**\n\n**1. 把題目翻譯成「單調的是/否問題」。** 先問：我要找的東西，能不能寫成「第一個滿足某條件的值/位置」？能，就能二分。\n\n**2. 選定搜尋空間。** 可能是「下標範圍」（74、旋轉陣列），也可能是「值域範圍」（69 在 `[0, x]` 上找平方根）。\n\n```cpp\n// 69：在值域 [0, x] 上二分平方根\nlong long lo = 0, hi = x;\nwhile (lo < hi) {\n  long long mid = lo + (hi - lo + 1) / 2;  // 取右中點，避免死循環\n  if (mid * mid <= x) {\n    lo = mid;\n  } else {\n    hi = mid - 1;\n  }\n}\n```\n\n**3. 寫對 `mid` 與邊界收斂。** 找「第一個 true」用左中點配 `hi = mid`；找「最後一個 true」用右中點配 `lo = mid`。選錯會死循環。\n\n**4. 旋轉陣列：先定有序半，再縮範圍。** 每輪用 `nums[lo] <= nums[mid]` 判斷左半是否有序；81 有重複值時，遇到 `nums[lo]==nums[mid]==nums[hi]` 就 `lo++、hi--` 退化處理。\n\n**5. 中位數（4）：在較短陣列上二分切割點。** 把兩陣列各切一刀，使左邊總數固定且「左邊最大 <= 右邊最小」，二分那條切割線即可達 `O(log(min(m,n)))`。\n\n```cpp\n// 4：二分短陣列的切割數 i，推出長陣列切割 j，檢查左右交界\nint i = (lo + hi) / 2;\nint j = (m + n + 1) / 2 - i;  // 保證左半元素總數固定\n```\n\n**6. 邊界與複雜度。** 多數是 `O(log n)`；74 是 `O(log(mn))`；4 是 `O(log(min(m,n)))`。乘法、平方注意用 `long long` 防溢位；空陣列、目標不存在、全相同元素都要單獨測。\n\n**題目線索（Pattern）**\n\n看到下列特徵，往「二分」想：\n\n- **資料已排序（或部分有序、或矩陣行列有序）**，且要查找、定位、或求某個臨界值。有序是二分的天然溫床。\n- **答案能改寫成「第一個／最後一個滿足某條件的值或位置」**，而那個條件具單調性（過了門檻就一直成立）。\n- **暴力是線性掃描 `O(n)`，但題目規模或多次查詢逼你做到 `O(log n)`。**\n- **陣列被旋轉、或要在兩個有序序列間找全域排名**——這些是「變形二分」的招牌題，核心仍是找單調分界。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的搜尋空間是「下標」還是「值域」？範圍上下界設對了嗎？\n2. 我要的是「第一個 true」還是「最後一個 true」？`mid` 取法與更新方向配對了嗎？\n3. 我的 `check(mid)` 條件真的單調嗎（前 false 後 true，不會反覆）？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 4 | 尋找兩個正序陣列的中位數 | N/A | 先把候選集合視為有排名的多重集合；練習寫 `count(<=x)`，再找第一個排名達標的值。 |\n| 33 | 搜尋旋轉排序陣列 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 69 | x 的平方根 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 74 | 搜尋二維矩陣 | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n| 81 | 搜尋旋轉排序陣列 II | N/A | 重點是定義候選值 `x` 的語意；先畫出真假分界，再決定找第一個或最後一個可行值。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 704. 二分查詢 展示核心寫法。\n\n**完整題目**：給定升序整數陣列 `nums` 與目標值 `target`，若 `target` 存在則回傳下標，否則回傳 `-1`。\n\n**線索**：題目提供有序資料或明確的真假分界，答案落在第一個/最後一個滿足條件的位置，這是二分的主要線索。\n\n```cpp\nclass Solution {\n public:\n  int search(vector<int>& nums, int target) {\n    int l = 0, r = (int)nums.size() - 1;\n    while (l <= r) {\n      int mid = l + (r - l) / 2;\n      if (nums[mid] == target) {\n        return mid;\n      }\n      if (nums[mid] < target) {\n        l = mid + 1;\n      } else {\n        r = mid - 1;\n      }\n    }\n    return -1;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 用閉區間維護候選答案；每次比較後刪掉必不可能含答案的一半。若改成二分答案，`target` 會替換成 `check(mid)` 的可行性。\n- `l` 與 `r` 表示仍可能包含答案的範圍，`mid` 只是用來判斷要丟掉哪一半。\n- 每個分支都要能說明被排除的區間為何不可能含答案；這就是二分不變式。\n\n**常見變形**\n\n- 找第一個 >= x / 第一個 > x。\n- 在旋轉陣列、山脈陣列或分段單調結構中先判定落在哪一段。\n- 浮點二分與三分法處理連續答案。\n\n**實作注意事項**\n\n- 固定一種區間寫法，不要在同一題混用。\n- `mid = l + (r-l)/2` 避免溢位。\n- 重複元素題要先決定答案偏左或偏右。\n- 用全小、全大、單元素測資檢查邊界。"
    }
  ],
  "summary": "![二分題單](https://pic.leetcode.cn/1711713491-SoiQWc-t2.png)\n\n> 圖：閉區間二分迴圈結束時的左右指標位置（查詢第一個 8）\n\n**刷題建議**：初學者建議先刷難度 < 1700 分的題目，進階題目常結合資料結構、圖論等\n\n**本題單涵蓋**：\n- 基礎二分查詢（查詢元素、統計個數）\n- 二分答案（最小化最大值、最大化最小值、第 K 小）\n- 三分法與其他應用\n\n**C++ 模板**：\n\n```cpp\n// 閉區間 [left, right] 二分模板\nint search(vector<int>& nums, int target) {\n  int left = 0, right = nums.size() - 1;  // 搜尋範圍 [left, right]\n  while (left <= right) {\n    int mid = left + (right - left) / 2;  // 防溢位寫法\n    if (nums[mid] == target) {\n      return mid;\n    } else if (nums[mid] < target) {\n      left = mid + 1;\n    } else {\n      right = mid - 1;\n    }\n  }\n  return -1;\n}\n```"
}
