{
  "id": 1,
  "title": "網格圖（DFS/BFS/綜合應用）",
  "src": "https://leetcode.cn/circle/discuss/YiXPXW/",
  "last_update": "2026-04-18T11:53:23.324686+00:00",
  "children": [
    {
      "id": 2,
      "title": "1. 網格圖 DFS",
      "summary": "**原理講解**\n\n本節要解決的場景是：給你一張像棋盤一樣的二維格子（matrix），每一格可能是陸地、水、牆或門。你想知道「從某一格出發，走遍所有相連的格子」會碰到哪些東西——這就是網格圖的深度優先搜尋（DFS）。\n\n關鍵的觀念轉換是：**把每一格 `(row, col)` 當成圖上的一個節點，相鄰的格子之間有一條邊。** 通常「相鄰」指上下左右四個方向，有些題目是八方向。我們用一個「方向陣列」一次寫好所有偏移，避免重複貼四段幾乎一樣的程式。\n\n```cpp\n// 方向陣列：上、下、左、右四個鄰格的座標偏移\nint dr[4] = {-1, 1, 0, 0};\nint dc[4] = {0, 0, -1, 1};\n// (r + dr[k], c + dc[k]) 就是第 k 個鄰格\n```\n\nDFS 的精神是「一條路走到底，走不通再退回來換方向」。在格子上，就是站在一格、把它標記成「走過」、然後遞迴走進每個還沒走過的鄰格。為什麼一定要標記走過？因為 A 的鄰居是 B，B 的鄰居又是 A，不標記就會在兩格間無限來回。`visited` 標記正是把「圖可能有環」這件事擋掉的護欄。\n\n```cpp\n// DFS 核心：標記目前格、再往四個方向遞迴\nvoid dfs(vector<vector<int>>& g, int r, int c) {\n    g[r][c] = -1;                  // 就地標記走過（也可用獨立 visited 陣列）\n    for (int k = 0; k < 4; k++) {\n        int nr = r + dr[k], nc = c + dc[k];\n        if (inGrid(nr, nc) && g[nr][nc] == 1)  // 邊界 + 是陸地 + 沒走過\n            dfs(g, nr, nc);\n    }\n}\n```\n\n像「島嶼數量」（200）就是：對每個還沒走過的陸地格啟動一次 DFS，一次 DFS 會把整座島染掉，啟動了幾次就有幾座島。\n\n**實作方式**\n\n**1. 邊界檢查獨立成一個小函式。** 每次要走進鄰格前，先確認沒走出棋盤外。把它寫成一行函式，主邏輯就乾淨很多。\n\n```cpp\n// 邊界檢查：座標是否還在 m x n 的格子內\nbool inGrid(int r, int c, int m, int n) {\n    return r >= 0 && r < m && c >= 0 && c < n;\n}\n```\n\n**2. 決定 visited 怎麼存。** 可以另開一個 `visited` 二維陣列，也可以「就地改值」——例如把走過的陸地直接改成水。後者省空間，但會破壞原始輸入，看題目允不允許。\n\n**3. 對需要的起點啟動 DFS。** 「島嶼數量」是掃整張圖、遇到新陸地就啟動並計數；「被圍繞的區域」（130）則反過來，只從**邊界**上的格子啟動 DFS，把連到邊界的區域標成安全，剩下沒被標記的才是被包圍的。\n\n```cpp\n// 計數型：每啟動一次 DFS = 一個連通塊\nint count = 0;\nfor (int r = 0; r < m; r++)\n    for (int c = 0; c < n; c++)\n        if (g[r][c] == 1) { dfs(g, r, c); count++; }\n```\n\n**4. 邊界與複雜度。**\n\n- 遞迴太深（整張圖連成一條長蛇）可能爆堆疊，格子很大時改用顯式 stack 的迭代 DFS 較安全。\n- 若同一格「以不同狀態」可重複到達（例如「太平洋大西洋水流問題」417 要分別從兩片海各做一次反向 DFS），visited 要對每次搜尋重置或分開存。\n- 複雜度：每格最多進出一次，`O(m * n)`；遞迴堆疊最壞 `O(m * n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「網格圖 DFS」想：\n\n- **輸入是一個二維陣列，問題關於「連在一起的一塊區域」。** 島嶼數量、島嶼周長（463）、區域大小、把某塊區域整片染色，都是 DFS 的拿手好戲。\n- **你只在乎「能不能連通 / 整塊長什麼樣」，不在乎「最短幾步」。** 要算步數最短時改用 BFS；只要遍歷整塊就用 DFS，因為它好寫、自然遞迴。\n- **可以從特定起點（如四條邊界）反向思考。** 「被圍繞的區域」「水流問題」都是先標出「碰得到邊界 / 碰得到海」的格子，再回推答案。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點是什麼？是純 `(row, col)`，還是要再加上鑰匙集合、剩餘步數等額外狀態？\n2. 我的 `visited` 存在哪裡？就地改值會不會破壞之後還要用的資料？\n3. 答案是「連通塊的數量」「某塊的大小」還是「遍歷後的標記矩陣」？我從哪裡讀出它？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 130 | 被圍繞的區域 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 200 | 島嶼數量 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 305 | 島嶼數量 II（Premium） | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 417 | 太平洋大西洋水流問題 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 463 | 島嶼的周長 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n        int m = grid.size(), n = grid[0].size(), ans = 0;\n        int dirs[5] = {1, 0, -1, 0, 1};\n        function<void(int,int)> dfs = [&](int r, int c) {\n            grid[r][c] = '0';\n            for (int k = 0; k < 4; ++k) {\n                int nr = r + dirs[k], nc = c + dirs[k + 1];\n                if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n                    dfs(nr, nc);\n                }\n            }\n        };\n        for (int r = 0; r < m; ++r)\n            for (int c = 0; c < n; ++c)\n                if (grid[r][c] == '1') ans++, dfs(r, c);\n        return ans;\n    }\n};\n```\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。",
      "children": []
    },
    {
      "id": 4,
      "title": "2. 網格圖 BFS",
      "summary": "**原理講解**\n\n本節要解決的場景是：在一張二維格子上，求「從起點走到終點最少幾步」，或「每一格離最近的某個目標有多遠」。這類「步數最少」的問題，靠的是網格圖的廣度優先搜尋（BFS）。\n\n跟 DFS 一樣，我們把每格 `(row, col)` 看成節點、相鄰格之間有邊。差別在走法：**BFS 像水波一樣一圈一圈往外擴。** 先處理離起點 0 步的格子，再處理 1 步的、2 步的……用一個佇列（queue）排隊，先進先出。\n\n為什麼這樣就能得到最短步數？因為每條邊的「成本」都是 1（走一格算一步）。BFS 按距離由近到遠擴展，所以**第一次碰到某格時，那個距離一定就是最短的**，之後再也不必更新它。這就是 BFS 能保證最短路的關鍵前提：每步代價相同。\n\n```cpp\n// BFS 核心：用 queue 一圈圈往外擴，第一次到達即最短\nqueue<pair<int,int>> q;\nq.push({sr, sc});\ndist[sr][sc] = 0;                 // 起點距離為 0\nwhile (!q.empty()) {\n    auto [r, c] = q.front(); q.pop();\n    for (int k = 0; k < 4; k++) {\n        int nr = r + dr[k], nc = c + dc[k];\n        if (inGrid(nr, nc) && dist[nr][nc] == -1) {  // 沒走過\n            dist[nr][nc] = dist[r][c] + 1;           // 距離 +1\n            q.push({nr, nc});\n        }\n    }\n}\n```\n\n有一種強力變形叫「多源 BFS」：把**所有**目標格在一開始就全部塞進佇列、距離都設 0，然後一起往外擴。這樣每格自然得到「離最近目標的距離」。「牆與門」（286）正是把所有門當作源頭一次擴出去，比一扇門一扇門分別 BFS 快得多。\n\n```cpp\n// 多源 BFS：一開始就把所有目標塞進佇列、距離設 0\nfor (auto& [r, c] : targets) {\n    dist[r][c] = 0;\n    q.push({r, c});\n}\n// 之後的擴展迴圈與單源完全相同\n```\n\n**實作方式**\n\n**1. 準備距離陣列當作 visited。** 用 `dist` 同時記「到沒到過」（-1 表示沒到）和「最短距離」，一石二鳥。\n\n```cpp\n// dist 同時當 visited：-1 = 尚未到達\nvector<vector<int>> dist(m, vector<int>(n, -1));\n```\n\n**2. 初始化佇列。** 單源就塞起點；多源就把所有源頭一次塞進去。\n\n**3. 逐層擴展，第一次到達就定案。** 取出隊首、看四個鄰格，只有「在界內、可通行、還沒到過」的才更新距離並入隊。因為步代價都是 1，更新後就不必再改。\n\n```cpp\n// 入隊條件：界內 + 可走 + 尚未到達\nif (inGrid(nr, nc) && g[nr][nc] != WALL && dist[nr][nc] == -1) {\n    dist[nr][nc] = dist[r][c] + 1;\n    q.push({nr, nc});\n}\n```\n\n**4. 邊界與複雜度。**\n\n- **每步代價不全是 1 就不能用樸素 BFS：** 代價只有 0 或 1 用雙端佇列 0-1 BFS，非負任意權用 Dijkstra。\n- 若到達同一格的「狀態」不只座標（例如「迷宮」490 的球會一路滾到撞牆才停，或帶著鑰匙），visited 要把那些額外資訊一起算進去。\n- 複雜度：每格進出佇列一次，`O(m * n)`。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「網格圖 BFS」想：\n\n- **問「最少步數 / 最短距離」，而且每步代價一樣。** 這是 BFS 的招牌題型；只要邊權都相等，BFS 比 Dijkstra 更簡單也更快。\n- **要算「每格離最近的某類格子多遠」。** 用多源 BFS：所有目標同時當起點，一次擴完，例如「牆與門」、「離建築物最近的距離」（317）。\n- **移動規則特別（滾到底、固定方向）。** 「迷宮」系列的球不是一步一格而是滑到撞牆，這時一個「節點」是停下來的位置，邊則是各方向滑一次的結果——觀念不變，只是鄰格的定義換了。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的每步代價真的都一樣嗎？（不一樣就要換 0-1 BFS 或 Dijkstra）\n2. 起點是一個還是一群？（一群就用多源 BFS）\n3. 一個「節點」需要記哪些東西？只有座標，還是要加上方向、鑰匙、停留點？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 286 | 牆與門（Premium） | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 317 | 離建築物最近的距離（Premium） | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 490 | 迷宮（Premium） | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 499 | 迷宮 III（Premium） | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 505 | 迷宮 II（Premium） | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n        int m = grid.size(), n = grid[0].size(), ans = 0;\n        int dirs[5] = {1, 0, -1, 0, 1};\n        function<void(int,int)> dfs = [&](int r, int c) {\n            grid[r][c] = '0';\n            for (int k = 0; k < 4; ++k) {\n                int nr = r + dirs[k], nc = c + dirs[k + 1];\n                if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n                    dfs(nr, nc);\n                }\n            }\n        };\n        for (int r = 0; r < m; ++r)\n            for (int c = 0; c < n; ++c)\n                if (grid[r][c] == '1') ans++, dfs(r, c);\n        return ans;\n    }\n};\n```\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。",
      "children": []
    },
    {
      "id": 5,
      "title": "3. 0-1 BFS",
      "summary": "**原理講解**\n\n本節要解決的場景是：在格子（或圖）上求最短路，但「走某些步是免費的、走另一些步要付 1 元」。例如「使網格圖至少有一條有效路徑的最小代價」（1368）：每格本來指向一個方向，沿著它指的方向走不用錢，要改方向才花 1。這種**邊權只有 0 或 1** 的最短路，用 0-1 BFS 最漂亮。\n\n先回顧：普通 BFS 之所以能保證最短，是因為每步代價都是 1，用一般佇列「先進先出」就能讓距離由小到大被取出。但現在有 0 邊，走 0 邊不該增加距離——如果還用普通佇列，距離小的格子可能排在距離大的後面，順序就亂了。\n\n0-1 BFS 的核心想法只有一句話：**改用雙端佇列（deque）；走 0 邊就放到隊首，走 1 邊就放到隊尾。** 這樣「距離小的永遠在前面」這個性質就被保住了，效果等同 Dijkstra，但因為只有兩種權重，免去了堆（heap）的 `log` 開銷。\n\n```cpp\n// 0-1 BFS 核心：0 邊走前門(push_front)，1 邊走後門(push_back)\ndeque<pair<int,int>> dq;\ndq.push_front({sr, sc});\ndist[sr][sc] = 0;\nwhile (!dq.empty()) {\n    auto [r, c] = dq.front(); dq.pop_front();\n    for (int k = 0; k < 4; k++) {\n        int nr = r + dr[k], nc = c + dc[k];\n        if (!inGrid(nr, nc)) continue;\n        int w = (k == g[r][c] ? 0 : 1);          // 順著箭頭=0，改向=1\n        if (dist[r][c] + w < dist[nr][nc]) {\n            dist[nr][nc] = dist[r][c] + w;\n            if (w == 0) dq.push_front({nr, nc});  // 免費 → 插隊到最前\n            else        dq.push_back({nr, nc});   // 付費 → 排到最後\n        }\n    }\n}\n```\n\n為什麼正確？deque 裡任一時刻的距離最多只有兩種相鄰的值（像 d 和 d+1）。把 0 邊放前面、1 邊放後面，剛好維持「從前到後距離不下降」。於是第一次彈出某格時，它的距離已是最短——這和 Dijkstra「每次取最小」是一樣的保證。\n\n**實作方式**\n\n**1. 把「免費」和「付費」分清楚。** 動手前先想清楚：哪種移動算 0、哪種算 1。「最少側跳次數」（1824）裡，沿同一跑道往前是 0，換跑道（側跳）是 1。\n\n**2. dist 初始化為無限大，鬆弛時才更新。** 跟 Dijkstra 一樣，看到更短的距離才改、才入隊。\n\n```cpp\n// 初始化：所有格距離設為極大，起點為 0\nvector<vector<int>> dist(m, vector<int>(n, INT_MAX));\ndist[sr][sc] = 0;\n```\n\n**3. 入隊位置由邊權決定。** 這是與普通 BFS 唯一的關鍵差別。\n\n```cpp\n// 0 邊插隊首，1 邊排隊尾——這一行就是 0-1 BFS 的靈魂\nif (w == 0) dq.push_front({nr, nc});\nelse        dq.push_back({nr, nc});\n```\n\n**4. 邊界與複雜度。**\n\n- 同一格可能被放進 deque 多次（距離被更新就再放一次），所以**判斷終止要靠「彈出時距離是否仍最小」**，或在彈出時略過已過期的舊紀錄。\n- 一定要確認權重「真的只有 0 和 1」。若出現 2、3 等其它值，0-1 BFS 不再正確，得換成 Dijkstra。\n- 複雜度：每條邊最多處理常數次，`O(節點數 + 邊數)`，在格子上即 `O(m * n)`，比 Dijkstra 的 `O(m * n * log)` 更快。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「0-1 BFS」想：\n\n- **求最短路 / 最小代價，而每一步的花費只有兩種：0 或 1。** 「改方向花 1、順方向免費」「換道花 1、直行免費」「移除障礙花 1、空地免費」（如「到達角落需要移除障礙物的最小數目」2290）都是經典訊號。\n- **本來想用 Dijkstra，但發現權重特別單純（只有 0 和 1）。** 這時 0-1 BFS 是更輕量的替代品，少了堆、常數更小。\n- **題目強調「最少幾次某種特殊操作」（側跳、轉向、拆牆）。** 那個特殊操作就是權重 1 的邊，其餘正常移動是權重 0 的邊。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的邊權是不是真的只有 0 和 1？有沒有混進其它數值？\n2. 哪種移動是 0、哪種是 1？我能一句話講清楚嗎？\n3. 我用 deque 了嗎？0 邊有沒有 `push_front`、1 邊有沒有 `push_back`？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 56 | LCP 56. 信物傳送 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| LCP 56 | LCP 56. 信物傳送 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 1368 | [使網格圖至少有一條有效路徑的最小代價](https://leetcode.cn/problems/minimum-cost-to-make-at-least-one-valid-path-in-a-grid/) | 2069 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 1824 | [最少側跳次數](https://leetcode.cn/problems/minimum-sideway-jumps/) | 1778 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 2290 | [到達角落需要移除障礙物的最小數目](https://leetcode.cn/problems/minimum-obstacle-removal-to-reach-corner/) | 2138 | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n        int m = grid.size(), n = grid[0].size(), ans = 0;\n        int dirs[5] = {1, 0, -1, 0, 1};\n        function<void(int,int)> dfs = [&](int r, int c) {\n            grid[r][c] = '0';\n            for (int k = 0; k < 4; ++k) {\n                int nr = r + dirs[k], nc = c + dirs[k + 1];\n                if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n                    dfs(nr, nc);\n                }\n            }\n        };\n        for (int r = 0; r < m; ++r)\n            for (int c = 0; c < n; ++c)\n                if (grid[r][c] == '1') ans++, dfs(r, c);\n        return ans;\n    }\n};\n```\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。",
      "children": []
    },
    {
      "id": 6,
      "title": "4. 綜合應用",
      "children": [],
      "summary": "**原理講解**\n\n本節要解決的場景是：題目仍是在格子上移動，但加了一層「狀態」，使得單純記座標已經不夠。最典型的就是 LCP 系列尋寶類題目——「尋寶」（LCP 13）、「變換的迷宮」（LCP 31）、「腳踏車炫技賽場」（LCP 45）——它們把 BFS / DFS 與額外資訊（拿到哪些機關、第幾個時刻、剩多少能量）綜合在一起。\n\n關鍵觀念升級成一句話：**節點不再只是 `(row, col)`，而是 `(row, col, 額外狀態)`。** 同一格在「沒拿鑰匙」和「拿了鑰匙」兩種情況下，是兩個不同的節點，可以各自被訪問一次。沒有把額外狀態放進節點，BFS 就會把本該分開的兩種情況誤當成同一格而漏算。\n\n一個常見的額外狀態是「已蒐集的物品集合」，用位元遮罩（bitmask）表示：第 i 位是 1 代表第 i 樣已拿到。\n\n```cpp\n// 用 bitmask 表示「已蒐集哪些」：第 i 位為 1 = 第 i 樣到手\nint mask = 0;\nmask |= (1 << i);              // 標記拿到第 i 樣\nbool got = mask & (1 << i);    // 檢查第 i 樣有沒有\n```\n\n於是 visited 要對「座標 + 狀態」整組去重，而不是只看座標：\n\n```cpp\n// visited 對 (座標, mask) 整組標記，而非只看座標\n// dim: 行數 m、列數 n、狀態種類 1<<K\nbool visited[m][n][1 << K];\nif (!visited[r][c][mask]) {\n    visited[r][c][mask] = true;\n    // ... 入隊 / 遞迴\n}\n```\n\n時間也可能是狀態的一部分。「變換的迷宮」裡牆會隨時刻開合，所以「同一格在第 3 秒」和「在第 5 秒」是不同節點，節點得帶上時間。\n\n**實作方式**\n\n**1. 先寫清楚「哪些量會變」。** 動手前列出：座標一定要、再加上會影響後續決策的量（拿了哪些、剩幾次某資源、現在第幾時刻、面朝哪個方向）。這串東西合起來就是你的節點。\n\n**2. 設計狀態編碼。** 把額外狀態壓成整數，方便當陣列索引或塞進 set。\n\n```cpp\n// 把 (r, c, mask) 編碼成一個整數，方便當 key\nint encode(int r, int c, int mask, int n, int K) {\n    return (r * n + c) * (1 << K) + mask;\n}\n```\n\n**3. 選對搜尋骨架。** 求最短時間 / 步數用 BFS（每步代價相同）或 Dijkstra（代價不同）；只問可不可達、要列舉所有走法用 DFS。骨架不變，差別只在「擴展鄰居時順手更新額外狀態」。\n\n```cpp\n// 擴展時：座標走一步，順手更新 mask（踩到第 i 樣就點亮該位）\nint nmask = mask;\nif (item[nr][nc] >= 0) nmask |= (1 << item[nr][nc]);\n```\n\n**4. 邊界與複雜度。**\n\n- visited 維度要與狀態維度一致；漏掉一維就會把不同狀態混為一談而出錯。\n- 狀態數會乘起來：格子 `m * n` 再乘狀態種類 `2^K`，K 一大就會爆。所以這類題的物品數通常很小（十幾個以內），正是暗示你可以用 bitmask。\n- 複雜度：大約 `O(m * n * 狀態數 * 鄰居數)`，務必先估算別超記憶體。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就可以往「綜合應用（帶狀態的網格搜尋）」想：\n\n- **在格子上移動，但有「會累積或改變的附加條件」。** 蒐集機關 / 鑰匙、限定步數或能量、地形隨時間變化，都意味著座標之外還要再記東西。\n- **同一格允許用不同方式重複經過。** 一旦「重複到達同格但情況不同還算有意義」，就代表 visited 必須加上狀態維度。\n- **要蒐集的目標數量很少（個位數到十幾個）。** 這通常是 bitmask 的強烈暗示：用一個整數的每個位元代表一個目標的有無。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的節點除了座標，還必須記哪些量才不會算錯？\n2. 這些額外狀態怎麼編碼？我的 visited 維度有沒有跟著加上去？\n3. 全部狀態數乘起來大概多少？會不會超出時間或記憶體？\n\n三題都答得出來，模型就套對了。\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 13 | LCP 13. 尋寶 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| LCP 13 | LCP 13. 尋寶 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 31 | LCP 31. 變換的迷宮 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| LCP 31 | LCP 31. 變換的迷宮 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n| 45 | LCP 45. 腳踏車炫技賽場 | N/A | 把格子當節點處理；練習明確 visited、方向陣列、障礙與多源/單源的差別。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 200. 島嶼數量 展示核心寫法。\n\n**完整題目**：給定由水與陸地組成的網格，計算四方向相連的陸地島嶼數量。\n\n**線索**：題目在矩陣中移動、擴散或統計連通塊；格子可直接當節點，方向陣列就是隱式邊。\n\n```cpp\nclass Solution {\npublic:\n    int numIslands(vector<vector<char>>& grid) {\n        int m = grid.size(), n = grid[0].size(), ans = 0;\n        int dirs[5] = {1, 0, -1, 0, 1};\n        function<void(int,int)> dfs = [&](int r, int c) {\n            grid[r][c] = '0';\n            for (int k = 0; k < 4; ++k) {\n                int nr = r + dirs[k], nc = c + dirs[k + 1];\n                if (0 <= nr && nr < m && 0 <= nc && nc < n && grid[nr][nc] == '1') {\n                    dfs(nr, nc);\n                }\n            }\n        };\n        for (int r = 0; r < m; ++r)\n            for (int c = 0; c < n; ++c)\n                if (grid[r][c] == '1') ans++, dfs(r, c);\n        return ans;\n    }\n};\n```\n\n**程式碼拆解**\n\n- 每次遇到未訪問陸地就開一個連通塊，DFS 把同島所有格子標記掉。\n- 雙層迴圈枚舉格子，方向陣列負責產生相鄰節點。\n- 修改原網格或標記 visited 的時機，決定同一格是否會被重複處理。\n\n**常見變形**\n\n- DFS 計連通塊與面積。\n- BFS 求最短步數或擴散時間。\n- 帶轉向/代價可轉成 0-1 BFS 或 Dijkstra。\n\n**實作注意事項**\n\n- 方向陣列保持一致，例如 `{1,0,-1,0,1}`。\n- 遞迴 DFS 在大網格可能爆棧，可改顯式棧。\n- 修改原矩陣前確認題目允許。\n- 狀態含鑰匙/方向時 visited 需升維。"
    }
  ],
  "summary": "**網格圖題目核心技巧**：\n- 方向陣列：`int dirs[4][2] = {{0,1}, {0,-1}, {1,0}, {-1,0}};`\n- 邊界檢查：`x >= 0 && x < m && y >= 0 && y < n`\n- 訪問陣列：`vector<vector<bool>> visited(m, vector<bool>(n, false));`\n\n**本題單涵蓋**：\n- DFS\n- BFS\n- 綜合應用\n\n```cpp\n// 網格圖 DFS/BFS 模板\nint dirs[4][2] = {{0, 1}, {0, -1}, {1, 0}, {-1, 0}};\n\n// DFS 遍歷連通塊\nvoid dfs(vector<vector<int>>& grid, int x, int y) {\n    int m = grid.size(), n = grid[0].size();\n    grid[x][y] = 0; // 標記已訪問\n    for (auto& d : dirs) {\n        int nx = x + d[0], ny = y + d[1];\n        if (nx >= 0 && nx < m && ny >= 0 && ny < n && grid[nx][ny]) {\n            dfs(grid, nx, ny);\n        }\n    }\n}\n\n// BFS 求最短路\nint bfs(vector<vector<int>>& grid, int sx, int sy, int tx, int ty) {\n    int m = grid.size(), n = grid[0].size();\n    queue<pair<int, int>> q;\n    q.push({sx, sy});\n    grid[sx][sy] = 0;\n    int step = 0;\n    while (!q.empty()) {\n        int sz = q.size();\n        while (sz--) {\n            auto [x, y] = q.front();\n            q.pop();\n            if (x == tx && y == ty) {\n                return step;\n            }\n            for (auto& d : dirs) {\n                int nx = x + d[0], ny = y + d[1];\n                if (nx >= 0 && nx < m && ny >= 0 && ny < n && grid[nx][ny]) {\n                    grid[nx][ny] = 0;\n                    q.push({nx, ny});\n                }\n            }\n        }\n        step++;\n    }\n    return -1;\n}\n```"
}
