{
  "id": 1,
  "title": "單調堆疊（矩形面積/貢獻法/最小字典序）",
  "src": "https://leetcode.cn/circle/discuss/9oZFK9/",
  "last_update": "2026-02-24T14:03:29.762933+00:00",
  "children": [
    {
      "id": 2,
      "title": "1. 基礎",
      "summary": "**原理講解**\n\n本節要建立「單調堆疊（monotonic stack）」最基礎的直覺。它解決的核心問題只有一句話：**對陣列中的每個元素，快速找到「左邊或右邊第一個比它大（或比它小）的元素」。**\n\n先看暴力做法：要找「每日溫度」（739 題）中，每一天之後第幾天會更暖，你可以對每一天往右掃，遇到更高溫就停。這樣每個元素最壞要掃整個陣列，總共 `O(n^2)`，太慢。\n\n單調堆疊的想法是：用一個堆疊（stack）暫存「還沒找到答案的元素」，並讓堆疊裡的值維持單調（例如由底到頂遞減）。當新元素來了、它比堆頂大，就代表堆頂「終於等到右邊第一個更大的元素」了，可以立刻結算並彈出。\n\n```cpp\n// 單調遞減堆疊：找「右邊第一個更大元素」\nstack<int> st;  // 存「下標」，對應的值由底到頂遞減\nfor (int i = 0; i < n; i++) {\n  while (!st.empty() && nums[i] > nums[st.top()]) {\n    int j = st.top();\n    st.pop();        // nums[i] 就是 nums[j] 右邊第一個更大值\n    ans[j] = i - j;  // 結算 j 的答案\n  }\n  st.push(i);  // i 還沒找到答案，先存著\n}\n```\n\n關鍵在於：**每個元素只會被 push 一次、pop 一次**，所以整體是 `O(n)`，遠快過暴力的 `O(n^2)`。堆疊存的是「下標」而非「值」，這樣才能順手算出距離或寬度。\n\n**實作方式**\n\n**1. 決定堆疊存什麼、維持哪種單調性。** 一般存下標；要找「更大」就維持遞減堆疊，要找「更小」就維持遞增堆疊。\n\n```cpp\n// 狀態表示：堆疊存下標，值維持遞減（為了找更大元素）\nstack<int> st;           // st 由底到頂：對應值遞減\nvector<int> ans(n, -1);  // 預設找不到為 -1\n```\n\n**2. 掃描並彈出。** 每讀到一個新元素，就把所有「被它破壞單調性」的堆頂彈出，彈出的瞬間結算它們的答案。\n\n```cpp\n// 更新流程：破壞單調性的堆頂一律彈出並結算\nwhile (!st.empty() && nums[i] > nums[st.top()]) {\n  ans[st.top()] = nums[i];  // 右邊第一個更大值\n  st.pop();\n}\nst.push(i);\n```\n\n**3. 處理「環形」與收尾。** 像 503（下一個更大元素 II）是環形陣列，技巧是把陣列掃兩遍（用 `i % n` 取下標）。掃完後仍留在堆疊裡的元素，表示右邊沒有更大值，維持預設答案。\n\n```cpp\n// 環形處理：掃兩遍，用取餘數模擬繞回開頭\nfor (int i = 0; i < 2 * n; i++) {\n  int x = nums[i % n];\n  while (!st.empty() && x > nums[st.top()]) {\n    ans[st.top()] = x;\n    st.pop();\n  }\n  if (i < n) {\n    st.push(i);  // 第二遍只結算、不再 push\n  }\n}\n```\n\n**4. 複雜度。** 時間 `O(n)`（每元素進出各一次），空間 `O(n)`（堆疊最壞存下全部）。\n\n**題目線索（Pattern）**\n\n看到下面這些特徵，就往單調堆疊想：\n\n- **問「下一個 / 上一個更大或更小的元素」。** 像 496、503、739 幾乎是模板題；901（股票價格跨度）問的是「往前連續多少天價格不超過今天」，本質也是找左邊第一個更大元素。\n- **暴力是對每個元素往左或往右掃一圈找邊界（`O(n^2)`），而你只關心「最近的那一個」。** 「最近」正是堆疊能省下重複比較的關鍵。\n- **元素一旦被某個新元素「蓋過」就不再有用。** 例如 853（車隊）裡，前車若比後車慢，後車永遠追不過去，可被一次結算掉——這種「被蓋過就出局」的結構天生適合堆疊。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要找的是「更大」還是「更小」？對應堆疊該遞增還是遞減？\n2. 堆疊裡存的是下標還是值？彈出時我要算的是距離、寬度，還是值本身？\n3. 掃完後還留在堆疊裡的元素代表什麼（通常是「那一側沒有符合條件的邊界」）？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 496 | 下一個更大元素 I | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 503 | 下一個更大元素 II | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 739 | 每日溫度 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 853 | [車隊](https://leetcode.cn/problems/car-fleet/) | 1678 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 901 | [股票價格跨度](https://leetcode.cn/problems/online-stock-span/) | 1709 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;\n    vector<int> st;\n    for (int x : nums2) {\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);\n    }\n    return ans;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。",
      "children": []
    },
    {
      "id": 3,
      "title": "2. 進階",
      "summary": "**原理講解**\n\n本節在基礎的單調堆疊上再進一步：基礎題只要「找最近的更大／更小元素」，進階題則要**在彈出的瞬間，順手算出更多資訊，或是把問題巧妙轉化成單調堆疊能處理的形狀**。\n\n關鍵體會是：**單調堆疊的堆頂，永遠維持著「目前還有效的候選」。** 當一個新元素破壞單調性、把堆頂彈出時，你其實同時知道了被彈元素的「左邊界」（堆疊中它下面那一個）和「右邊界」（正要進來的新元素）。這對「左右邊界一起需要」的進階題特別有用。\n\n以 456（132 模式）為例：要找下標 `i < j < k` 使 `nums[i] < nums[k] < nums[j]`。技巧是**從右往左掃**，用單調堆疊維護「可能的中間值（k 的值）」，並記錄被彈出的最大值當作 `nums[k]` 的上界；之後只要遇到比這個上界還小的數，就是合法的 `nums[i]`。\n\n```cpp\n// 132 模式：從右往左，堆疊維護候選，k 記錄被彈出的次大值\nstack<int> st;\nint k = INT_MIN;  // k：目前可當「中間 j」配對的最佳 nums[k]\nfor (int i = n - 1; i >= 0; i--) {\n  if (nums[i] < k) {\n    return true;  // 找到 nums[i] < nums[k] < nums[j]\n  }\n  while (!st.empty() && nums[i] > st.top()) {\n    k = st.top();\n    st.pop();  // 彈出的值能當合法的 nums[k]\n  }\n  st.push(nums[i]);\n}\n```\n\n進階題的共通點是：**狀態不只「最近邊界」，還要在彈出時維護額外量**（次大值、區間長度、子陣列計數等）。\n\n**實作方式**\n\n**1. 想清楚「彈出瞬間」能確定什麼。** 被彈元素的左右邊界此刻同時已知，許多進階量都在這一刻計算。\n\n```cpp\n// 彈出瞬間同時拿到左右邊界\nint mid = st.top();\nst.pop();\nint rightBound = i;                          // 正在進來的元素\nint leftBound = st.empty() ? -1 : st.top();  // 堆疊中它下面的元素\n```\n\n**2. 善用「掃描方向」轉化問題。** 從右往左掃（132 模式）、或先建後拆，常能讓原本不單調的問題變得適合堆疊。\n\n**3. 用單調性直接「建結構」。** 654（最大二叉樹）要遞迴選最大值當根；用單調遞減堆疊一次掃描即可建好：新元素比堆頂大就把堆頂收為自己的左子樹。\n\n```cpp\n// 最大二叉樹：單調遞減堆疊一次建樹\nstack<TreeNode*> st;\nfor (int v : nums) {\n  TreeNode* cur = new TreeNode(v);\n  while (!st.empty() && st.top()->val < v) {\n    cur->left = st.top();\n    st.pop();  // 較小者成為當前節點左子樹\n  }\n  if (!st.empty()) {\n    st.top()->right = cur;\n  }\n  st.push(cur);\n}\n```\n\n**4. 複雜度。** 仍是每元素進出各一次，`O(n)` 時間、`O(n)` 空間；只是每次彈出多做了 `O(1)` 的額外計算。\n\n**題目線索（Pattern）**\n\n- **需要同時用到左右兩側邊界，或在彈出時做計數／取次值。** 像 768（最多能完成排序的塊 II）要判斷「切到哪裡前綴已可獨立排序」，用單調堆疊維護區塊最大值即可。\n- **問題本身不單調，但換個掃描方向（由右往左、先建後拆）就能套堆疊。** 456 是典型代表。\n- **看似要遞迴或分治建結構，但每步都在挑「目前最大／最小」。** 654 就是把分治改寫成一次線性掃描。\n- 1063、1950 這類子陣列計數題，常把答案拆成「每個元素作為最大／最小值時，覆蓋多少子陣列」——這會自然帶到下一節的貢獻法。\n\n確認自己有沒有套對，問三個問題：\n\n1. 在彈出的那一刻，我同時知道了哪些資訊（左界？右界？次大值？）？\n2. 換個掃描方向，問題會不會變得單調好處理？\n3. 我要維護的「額外量」是什麼，它在 push 或 pop 的哪一步更新？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 456 | 模式 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 654 | 最大二叉樹 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 768 | 最多能完成排序的塊 II | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1063 | 有效子陣列的數目（Premium） | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1950 | 所有子陣列最小值中的最大值（Premium） | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;\n    vector<int> st;\n    for (int x : nums2) {\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);\n    }\n    return ans;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。",
      "children": []
    },
    {
      "id": 5,
      "title": "3. 矩形",
      "summary": "**原理講解**\n\n本節用單調堆疊解「矩形面積」類問題，代表題是 84（柱狀圖中最大的矩形）。給一排寬度都是 1、高度不同的長條，要找出「面積最大的矩形」。\n\n關鍵觀察：**以某根長條的高度 `h` 為矩形高度時，這個矩形能往左右延伸到哪裡？答案是「左右兩側第一根比它矮的長條」之間。** 因為一旦碰到更矮的長條，矩形就不能再保持高度 `h` 了。\n\n所以問題化成：對每根長條，找它「左邊第一個更矮」和「右邊第一個更矮」的位置——這正是單調堆疊擅長的事。維持一個**遞增堆疊**（由底到頂高度遞增），當新長條比堆頂矮，就把堆頂彈出並結算它的最大矩形：此刻它的右邊界就是新長條，左邊界就是堆疊中它下面那一根。\n\n```cpp\n// 柱狀圖最大矩形：遞增堆疊，遇到更矮就結算堆頂\nstack<int> st;  // 存下標，高度由底到頂遞增\nint best = 0;\nfor (int i = 0; i <= n; i++) {\n  int h = (i == n) ? 0 : heights[i];  // 末尾補 0 高，逼出剩餘長條\n  while (!st.empty() && h < heights[st.top()]) {\n    int top = st.top();\n    st.pop();\n    int left = st.empty() ? -1 : st.top();            // 左邊第一個更矮\n    best = max(best, heights[top] * (i - left - 1));  // 寬 = 右界-左界-1\n  }\n  st.push(i);\n}\n```\n\n矩形寬度 `i - left - 1` 是「右邊界與左邊界之間的格子數」，這就是堆疊存下標而非值的原因。\n\n**實作方式**\n\n**1. 維持遞增堆疊，存下標。** 高度遞增才能保證彈出時左右邊界都是「第一個更矮的」。\n\n```cpp\n// 狀態表示：遞增堆疊存下標\nstack<int> st;  // heights[st.bottom] <= ... <= heights[st.top]\n```\n\n**2. 結算寬度。** 彈出堆頂時，左界是它下面那個下標，右界是當前下標。\n\n```cpp\n// 結算：寬度 = 右界 - 左界 - 1\nint top = st.top();\nst.pop();\nint left = st.empty() ? -1 : st.top();\nint width = i - left - 1;\nbest = max(best, heights[top] * width);\n```\n\n**3. 末尾補哨兵，逼出殘留長條。** 掃描到結尾時堆疊裡通常還剩遞增的一串，補一根高度 0（或在頭尾各補哨兵），讓它們全部被彈出結算。\n\n**4. 升維到二維。** 85（最大矩形）把每一列當底，逐列累積「往上連續 1 的高度」形成柱狀圖，再對每一列套 84 的解法即可。221（最大正方形）則是同類「以行為底」的思路，只是用 DP 更直接。\n\n```cpp\n// 二維轉一維：逐列累積高度，再對每列跑柱狀圖\nfor (int c = 0; c < cols; c++) {\n  height[c] = (matrix[r][c] == '1') ? height[c] + 1 : 0;\n}\n// 接著對 height[] 跑上面的最大矩形函式\n```\n\n**5. 複雜度。** 84 是 `O(n)`；85 對每一列各跑一次，`O(rows * cols)`。\n\n**題目線索（Pattern）**\n\n- **答案是「某種矩形 / 區間的最大面積或最大寬度」，而限制條件是高度（或數值）要一致或不被破壞。** 84、85 是直接的矩形題。\n- **42（接雨水）是它的鏡像：** 不是找最大矩形，而是每個位置能積多少水，取決於「左右兩側的最高柱」——同樣靠單調堆疊找邊界，彈出時結算一層水量。\n- **二維矩陣裡找全 1 的最大矩形 / 正方形（85、221、755）**，多半能「逐列壓成一維柱狀圖」再套本節技巧。\n- 暴力是枚舉左右邊界算面積（`O(n^2)` 以上），而每個矩形的高度其實由「最近的更矮邊界」決定——這就是改用堆疊的訊號。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我的矩形「高度」由誰決定？它向左右延伸的邊界是不是「第一個更矮的」？\n2. 堆疊該遞增還是遞減？彈出時寬度怎麼算（右界 - 左界 - 1）？\n3. 如果是二維，我能不能逐列累積高度，化成一維柱狀圖？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 42 | 接雨水 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 84 | 柱狀圖中最大的矩形 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 85 | 最大矩形 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 221 | 最大正方形 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 755 | 倒水（Premium） | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用本節代表題 84. 柱狀圖中最大的矩形 展示核心寫法。\n\n**完整題目**：給定柱狀圖高度陣列，找出能由相鄰柱子形成的最大矩形面積。\n\n**線索**：題目把每個位置當作高度或邊界，要求最大矩形/範圍貢獻；左右第一個更矮/更小就是單調棧線索。\n\n```cpp\nclass Solution {\n public:\n  int largestRectangleArea(vector<int>& heights) {\n    vector<int> h = heights;\n    h.push_back(0);\n    vector<int> st;\n    int ans = 0;\n    for (int i = 0; i < (int)h.size(); ++i) {\n      while (!st.empty() && h[st.back()] > h[i]) {\n        int height = h[st.back()];\n        st.pop_back();\n        int left = st.empty() ? -1 : st.back();\n        ans = max(ans, height * (i - left - 1));\n      }\n      st.push_back(i);\n    }\n    return ans;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 每根柱子在被彈出時，左右第一個更矮位置都已確定；哨兵 0 可強制清空堆疊。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 接雨水可用單調堆疊或雙指標。\n- 最大矩形可把每一行轉成柱狀圖。\n- 子陣列最小值貢獻與矩形寬度思路相同。\n\n**實作注意事項**\n\n- 加入高度 0 哨兵強制彈出剩餘柱子。\n- 寬度是 `right-left-1`。\n- 面積可能超出 int 時用 long long。\n- 矩陣題要逐行累積高度。",
      "children": []
    },
    {
      "id": 6,
      "title": "4. 貢獻法",
      "summary": "**原理講解**\n\n本節介紹「貢獻法（contribution technique）」配合單調堆疊。它回答的是這類問題：**把所有子陣列的某個量（最小值、最大值、範圍…）加總起來。** 代表題是 907（子陣列的最小值之和）：對每一個子陣列取最小值，再把這些最小值全部相加。\n\n直接枚舉所有子陣列有 `O(n^2)` 個，逐一算最小值更慢。貢獻法換個角度想：**不要問「每個子陣列的最小值是誰」，而是問「每個元素，當了多少個子陣列的最小值」。** 把這個次數乘上元素值，全部加起來就是答案。\n\n關鍵問題變成：`nums[i]` 在多少個子陣列中是最小值？答案是 `(左邊能延伸的距離) × (右邊能延伸的距離)`。左邊界是「左邊第一個比它小的元素」，右邊界是「右邊第一個比它小的元素」——又回到單調堆疊找邊界。\n\n```cpp\n// 元素 i 當最小值的子陣列數 = 左可延伸 × 右可延伸\nint left = i - L[i];                        // L[i]：左邊第一個更小元素的下標\nint right = R[i] - i;                       // R[i]：右邊第一個更小元素的下標\nlong long count = (long long)left * right;  // i 當最小值的子陣列個數\nans += count * nums[i];                     // i 的「貢獻」\n```\n\n每個元素只被算一次它的貢獻，總時間 `O(n)`。難點不在公式，而在**邊界的嚴格性**（見下節）。\n\n**實作方式**\n\n**1. 用單調堆疊求左右邊界。** 求「左邊第一個更小」用遞增堆疊掃一遍；右邊界同理（或反向掃）。\n\n```cpp\n// 求左邊第一個更小元素的下標 L[i]\nstack<int> st;\nfor (int i = 0; i < n; i++) {\n  while (!st.empty() && nums[st.top()] >= nums[i]) {\n    st.pop();\n  }\n  L[i] = st.empty() ? -1 : st.top();\n  st.push(i);\n}\n```\n\n**2. 一側嚴格、一側非嚴格，避免重複計數。** 若有相等元素（如 `[2,2]`），同一個子陣列的最小值會被兩個 2 同時認領。解法是約定「左邊用嚴格小於、右邊用小於等於」（或反過來），確保每個子陣列只被算一次。上面 `>=` 與下面 `>` 的差別就是這個關鍵。\n\n```cpp\n// 右邊界用「嚴格更小」(>)，與左邊的 (>=) 搭配去重\nwhile (!st.empty() && nums[st.top()] > nums[i]) {\n  st.pop();\n}\nR[i] = st.empty() ? n : st.top();\n```\n\n**3. 累加貢獻，注意溢位。** 子陣列數量可能很大，用 `long long` 並視題目取模。\n\n```cpp\n// 累加每個元素的貢獻\nlong long ans = 0;\nfor (int i = 0; i < n; i++) {\n  ans += (long long)(i - L[i]) * (R[i] - i) % MOD * nums[i] % MOD;\n}\n```\n\n**4. 衍生題型。** 2104（子陣列範圍和）= 所有子陣列最大值之和 − 最小值之和，跑兩次貢獻法相減。1856（子陣列最小乘積最大值）則對每個元素算「以它為最小值的最大區間和」，配前綴和。2962、3359 則是把「計數」掛在彈出時做。\n\n**5. 複雜度。** 求邊界 `O(n)`，累加 `O(n)`，總共 `O(n)` 時間、`O(n)` 空間。\n\n**題目線索（Pattern）**\n\n- **題目要「對所有子陣列／子區間的某個統計量求總和」**，且這個量是最小值、最大值或兩者之差。907、2104 是典型。\n- **逐一枚舉子陣列會是 `O(n^2)` 起跳**，但你發現「每個元素能影響的子陣列」是一段連續範圍——這就該換成「按元素算貢獻」。\n- **答案可拆成「每個元素 × 它被覆蓋的子陣列數」**。能寫出「`nums[i]` 在哪些子陣列裡當最小／最大值」就成功一半。\n- 2962（最大元素出現至少 K 次）這種「計數滿足條件的子陣列」，也常用單調結構在彈出時累加合法區間數。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要加總的量，能不能改寫成「每個元素貢獻幾次」？\n2. 某元素當最小／最大值的左右延伸範圍，是不是由「第一個更小／更大邊界」決定？\n3. 有相等元素時，我哪一側用嚴格、哪一側用非嚴格來去重？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 3359 | 查詢最大元素不超過 K 的有序子矩陣（Premium） | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 2104 | [子陣列範圍和（最大值-最小值）](https://leetcode.cn/problems/sum-of-subarray-ranges/) | 1504 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 2962 | [統計最大元素出現至少 K 次的子陣列](https://leetcode.cn/problems/count-subarrays-where-max-element-appears-at-least-k-times/) | 1701 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 907 | [子陣列的最小值之和](https://leetcode.cn/problems/sum-of-subarray-minimums/) | 1976 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1856 | [子陣列最小乘積的最大值](https://leetcode.cn/problems/maximum-subarray-min-product/) | 2051 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;\n    vector<int> st;\n    for (int x : nums2) {\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);\n    }\n    return ans;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。",
      "children": []
    },
    {
      "id": 8,
      "title": "5. 最小字典序",
      "summary": "**原理講解**\n\n本節用單調堆疊處理「最小字典序（lexicographically smallest）」問題。代表題是 402（移掉 K 位數字）：給一個數字字串，移掉 K 個字元，讓剩下的數字最小。\n\n字典序的直覺是：**越前面的位放越小的數字越好。** 所以從左往右掃，若發現「前一個保留的字元比現在這個還大」，把前一個移掉就能讓結果變小——這正是單調堆疊的「破壞單調性就彈出」。我們維持一個**遞增堆疊（由底到頂遞增）**，當新字元比堆頂小、而且還有刪除額度時，就彈掉堆頂。\n\n```cpp\n// 移掉 K 位數字：遞增堆疊，新字元更小且還能刪就彈堆頂\nstring st;  // 當作堆疊用，存最終保留的字元\nfor (char c : num) {\n  while (!st.empty() && k > 0 && st.back() > c) {\n    st.pop_back();  // 堆頂比 c 大，移掉它能讓數字更小\n    k--;            // 用掉一次刪除額度\n  }\n  st.push_back(c);\n}\n```\n\n這裡和前幾節不同的是：**彈出受「額度 K」限制**——不是想彈就彈，刪夠 K 個就停。它本質仍是「貪心地讓高位盡量小」，而堆疊保證每個字元只進出一次，整體 `O(n)`。\n\n**實作方式**\n\n**1. 維持遞增堆疊，受刪除額度限制。** 只有「堆頂更大、且還有額度」時才彈。\n\n```cpp\n// 核心彈出條件：更小的字元來了 + 還有額度\nwhile (!st.empty() && k > 0 && st.back() > c) {\n  st.pop_back();\n  k--;\n}\nst.push_back(c);\n```\n\n**2. 額度沒用完要從尾端補刪。** 若掃完還剩 `k > 0`（例如輸入本身遞增如 \"12345\"），代表前面沒機會刪，直接砍掉尾端 k 個。\n\n```cpp\n// 額度沒用完：移除尾端 k 個（此時序列已遞增）\nst.resize(st.size() - k);\n```\n\n**3. 清前導零、處理空字串。** 402 要去掉開頭多餘的 0；最後若全被刪光要回 \"0\"。\n\n```cpp\n// 去前導零，並處理結果為空的情況\nint i = 0;\nwhile (i < st.size() && st[i] == '0') {\n  i++;\n}\nstring res = st.substr(i);\nreturn res.empty() ? \"0\" : res;\n```\n\n**4. 變體：限制「只能用一次」或「不可重複」。** 316 / 1081（去除重複字母，留下字典序最小且每字母只出現一次）要多兩個條件：彈出前先確認「被彈字元後面還會再出現」，且「該字元尚未在堆疊中」才 push。\n\n```cpp\n// 去除重複字母：後面還會出現才敢彈；已在堆疊中就不重複加\nif (inStack[c]) {\n  continue;  // c 已保留，跳過\n}\nwhile (!st.empty() && st.back() > c && lastPos[st.back()] > i) {\n  inStack[st.back()] = false;\n  st.pop_back();  // 後面還有，安心彈\n}\nst.push_back(c);\ninStack[c] = true;\n```\n\n**5. 複雜度。** 每字元進出堆疊各一次，`O(n)` 時間；額外的計數陣列 `O(字元集大小)` 或 `O(n)` 空間。\n\n**題目線索（Pattern）**\n\n- **要在「保留 / 刪除若干元素」後，得到字典序最小（或最大、最具競爭力）的序列。** 402（最小）、1673（最具競爭力子序列，本質同 402）、321（拼接最大數，方向反過來找最大）都是。\n- **答案要求「貪心地讓前面的位最優」**，而你發現「後面來了更優的，前面剛保留的就該反悔刪掉」——這種「可反悔的貪心」就用單調堆疊。\n- **常伴隨額度限制（刪 K 個 / 留長度 m）或唯一性限制（每元素只出現一次，如 316、1081）**，這些限制決定彈出條件要加哪些判斷。\n\n確認自己有沒有套對，問三個問題：\n\n1. 我要的是字典序最小還是最大？對應堆疊該維持遞增還是遞減？\n2. 彈出除了「比堆頂更優」外，還受什麼限制（額度？該字元後面是否還會出現？）？\n3. 掃完後額度若沒用完、或有前導零，我該怎麼收尾？\n\n**代表例題**\n\n| 題號 | 題目 | Rating | 解法重點 |\n| --- | --- | --- | --- |\n| 316 | 去除重複字母 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 321 | 拼接最大數 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 402 | 移掉 K 位數字 | N/A | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1081 | [不同字元的最小子序列](https://leetcode.cn/problems/smallest-subsequence-of-distinct-characters/) | 2185 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n| 1673 | [找出最具競爭力的子序列](https://leetcode.cn/problems/find-the-most-competitive-subsequence/) | 1802 | 要找最近的更大/更小邊界；練習用單調結構讓每個元素只進出一次。 |\n\n**C++ 範例講解**\n\n以下用同型延伸示範題 496. 下一個更大元素 I 展示核心寫法。\n\n**完整題目**：對 `nums1` 中每個元素，找出它在 `nums2` 右側第一個比它大的元素，若不存在則為 `-1`。\n\n**線索**：題目要找最近更大/更小邊界或某元素的貢獻範圍；這表示元素應在單調結構中等待被右側元素結算。\n\n```cpp\nclass Solution {\n public:\n  vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) {\n    unordered_map<int, int> nxt;\n    vector<int> st;\n    for (int x : nums2) {\n      while (!st.empty() && st.back() < x) {\n        nxt[st.back()] = x;\n        st.pop_back();\n      }\n      st.push_back(x);\n    }\n    vector<int> ans;\n    for (int x : nums1) {\n      ans.push_back(nxt.count(x) ? nxt[x] : -1);\n    }\n    return ans;\n  }\n};\n```\n\n**程式碼拆解**\n\n- 堆疊內維持遞減；當新元素更大時，它就是被彈出元素右側第一個更大值。\n- 容器中保存尚未結算的元素；新元素破壞單調性時，被彈出的元素就找到了右邊界。\n- 程式通常存下標而非數值，因為答案需要計算距離或回填到原位置。\n\n**常見變形**\n\n- 循環陣列可掃兩遍。\n- 用索引入棧以計算距離或寬度。\n- 貢獻法中分別求左/右第一個嚴格或非嚴格邊界。\n\n**實作注意事項**\n\n- 先決定維持遞增還是遞減。\n- 相等元素用 `<` 還是 `<=` 會影響去重與邊界。\n- 需要寬度時入棧索引而不是值。\n- 哨兵可簡化最後清棧。",
      "children": []
    }
  ],
  "summary": "![單調堆疊題單](https://pic.leetcode.cn/1711714526-rMdiIh-t3-c.png)\n\n> *他向遠方望去，無法看到高山背後的矮山，只看到一座座更高的山峰*\n\n**前置知識**：建議先完成 [資料結構題單](https://leetcode.cn/circle/discuss/mOr1u6/) 中的「列舉右，維護左」及「堆疊」的基礎題\n\n**本題單涵蓋**：\n- 單調堆疊基礎：查詢左/右側更大/更小元素\n- 矩形面積：柱狀圖、最大矩形\n- 貢獻法：計算每個元素的貢獻\n- 最小字典序：構造問題\n\n**C++ 單調堆疊模板 (下一個更大元素)**：\n\n```cpp\n// 單調堆疊：求每個元素的下一個更大元素\nvector<int> nextGreaterElement(vector<int>& nums) {\n  int n = nums.size();\n  vector<int> res(n, -1);\n  stack<int> st;\n  for (int i = 0; i < n; ++i) {\n    while (!st.empty() && nums[st.top()] < nums[i]) {  // 彈出所有比 nums[i] 小的 {\n      res[st.top()] = nums[i];                         // 堆疊頂的下一個更大元素就是 nums[i]\n    }\n    st.pop();\n  }\n  st.push(i);\n}\nreturn res;\n}\n```"
}
